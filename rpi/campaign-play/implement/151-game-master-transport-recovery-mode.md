# Task 151: Game Master transport recovery mode

## Contract

Game Master attempt one keeps the existing automatic native structured-output selection. When attempt one ends in a transport, provider, or deadline failure, the single existing attempt two keeps the same reasoning-bypass model but requests strict tool mode instead of repeating the failed native JSON transport. A semantic `model_contract_invalid` recovery remains default reasoning with automatic native selection.

Provider, model, prompt, schema, compiler, authority review, per-attempt deadline, fresh worker epoch, one-recovery limit, mechanics, persistence fences, UI, and visible copy remain unchanged. The Game Master still disables text fallback and repair; tool mode must satisfy the same strict schema and semantic compiler before any mechanics write.

## Evidence

Fresh lane `pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r91` admitted its first certified contact once. Game Master attempt one returned no provider metadata before the 45-second deadline. The existing transport recovery used reasoning bypass again, but repeated automatic native JSON; it returned 214 output tokens with finish `stop` and then failed `NoObjectGeneratedError: could not parse the response`. No Game Master artifact, command, receipt, actor job, narration, proper scene, or binding was written, and the lane was frozen without Resume.

The narrow correction changes only the structured-output transport of that already-authorized recovery call. Focused coverage freezes automatic mode on attempt one, tool mode after timeout, unchanged bypass model selection, and the existing strict Game Master proposer/reviewer options.

Validation passed: Game Master and player-action runtime suites 115/115, Campaign Play application suite 13/13, backend typecheck, backend build, and `git diff --check`. GitNexus `detect_changes` reports 18 affected flows and critical risk for the central runtime surface; the exact changed behavior is bounded by the complete runtime suite and the staged scope below.

Semantic review verdict: no prompt, narrative prose, or player-visible copy changed.
