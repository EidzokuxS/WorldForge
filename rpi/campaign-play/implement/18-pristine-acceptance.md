# Task 18: human playtest contract

Status: all three reusable lane bases remain frozen, but the earlier ten-action Lane A now fails the intentional hard-cutover world-hash contract and will not be migrated. Current-contract Bellglass run `pristine-natural-r33-longplay-lane-a` completed a new ten-action manual sitting and may continue after a break; it is rehearsal evidence rather than pristine-lane acceptance because two first model objects required explicit Resume. Lane B and Lane C have not started.

## Decision

Decide whether Campaign Play already works as a game people can inhabit for sixty actions, rather than only as a correct narrative transaction system.

The claim under test is narrow: from a player-visible scene, a player can form their own intention, act, understand the result, notice that other people and places continue without them, revise their plan, and still want another turn. The test does not establish population-wide appeal or final accessibility.

## What counts as play

The repeated loop is:

1. Read the immediate situation and notice one or more possible tensions, opportunities, people, places, or unanswered questions.
2. Form an intention for the character. The intention may follow, reinterpret, or reject an offered hook.
3. Choose a suggested action or write a freeform action because it serves that intention.
4. Read the resolution and identify what changed, what remained uncertain, and why the result appears to have happened.
5. Update the player's mental model of the world and choose whether to persist, adapt, withdraw, investigate elsewhere, or do nothing.

A turn is not successful merely because it terminates. It must protect the player's stated action, provide readable feedback, leave a durable consequence when appropriate, and create or preserve a meaningful next decision. Background simulation matters only when its effects can eventually become legible and change how the player thinks or acts.

## Evidence contract

Hypothesis: a non-central player can develop a self-chosen goal within ten actions and pursue it through understandable consequences while independent actors create discoverable changes that alter later decisions.

Retain the loop when the player can explain their goal, expectation, result, causal model, and next intention from the public UI; prose remains readable; mechanical state matches the explanation; and the player rates desire to continue at least 4/5 at the action-30 and action-60 checkpoints.

Revise when intention repeatedly fails to survive adjudication, prose obscures the consequence, background activity is mechanically real but experientially inert, choices do not support self-directed play, or the player continues only to satisfy the protocol. Reject the current lane after a hard contradiction, hidden-fact leak, lost or duplicate input, partial commit, reload divergence, or another severe failure that contaminates later interpretation.

## Participant and limits

The initial participant is the main Codex operator acting as a real player through the rendered UI. This is a formative expert playtest: one participant can expose mechanisms and severe failures but cannot estimate prevalence or represent a market audience. No external participant, personal identifier, audio, video, or behavioral telemetry is collected.

The operator reads every rendered beat, records an intention before input, and chooses from current player knowledge only. The operator does not inspect protected truth until the declared audit checkpoint and does not choose actions for coverage. When a mechanic is not naturally attractive, that absence is evidence.

Before opening, sign a compact character contract from player-visible world material: who the character is, why they are present, one ordinary capability, one limitation, and two values or appetites that can genuinely conflict. Do not revise the contract to justify a convenient action. The character may change through play when the rendered events support that change.

## Three-lane structure

- Lane A uses the current-contract clean-world template `Lowwater Ledger`. The player begins as an outsider with an ordinary reason to be in Craghold and no institutional authority.
- Lane B builds and accepts one distinct current-contract world from edited DNA plus saved research, snapshots it before character bootstrap, and then plays the materialized template. The player begins with a concrete personal need but remains outside the world's ruling structures.
- Lane C uses the product clone operation on an accepted zero-turn world. The player may refuse the opening hook and pursue ordinary life, opportunism, travel, or another self-chosen direction.

Actions 1-20 cover discovery and goal formation. Actions 21-40 cover pursuit, return, and adaptation. Actions 41-60 cover payoff, escalation, or a new commitment. These phases describe what we observe and do not prescribe actions.

Before turn zero, verify the template manifest and database hash, apply current migrations without modifying accepted world content, prove that character and turn history are empty with setup phase `character_required`, and freeze lane eligibility. Lane C must use the product clone operation rather than a filesystem copy after its eligible source exists.

### Frozen lane eligibility · 2026-07-15

Lane A rebuilt `Lowwater Ledger` once from the saved premise and six authorial DNA fields under the concrete-scene Campaign World contract. The accepted world contains three macro regions, seven persistent scenes, fifteen directed routes, eight agent-controlled people spread across all seven scenes, and five pressures. One semantic advisory remains: a ferry pilot is described as operating a cable-bridge crossing. It does not change spatial authority or causal eligibility. The reusable template is `lowwater-ledger-pristine-fb2c7074`; its accepted content hash is `fb2c7074e89df263b0bf308f0d47aadcd5f246b6832c8274506c8f1dc26855d9`.

Lane B built `Black Rain Passage` once from manually edited DNA and the saved version-2 research artifact owned by campaign `064bba91-3d35-4a8d-bef2-d08340172f79`. Review retained eleven source references and kept the research summary separate from the edited DNA. The accepted world contains three macro regions, seven persistent scenes, fourteen directed routes, nine agent-controlled people spread across all seven scenes, and five distinct pressures. The reusable template is `black-rain-passage-pristine-54df81f3`; its accepted content hash is `54df81f36c0f88bed68aff192b5c635e43f749a3bf97e346aafd990a2f397656`.

Lane C is product clean-start clone `509db3a1-49e3-494b-9128-a89e315ab0e9` of accepted zero-turn `Rainmarket Ledger` source `87acda0d-755a-4dbf-adc5-d943ac2f4aac`. Clone operation `bbef2f52-f55b-4b6c-9b30-af94b2c2da9c` recorded parent accepted-snapshot hash `f1951847c9a47bd219c62e04a5c316bec04575247b05b7ed4f4b357309527339` and source digest `72ed47f35ce8e06a187f666cfe6fa12180dce92d719eab3cd706eef9a61c16bc`. Loading the child through the product initialized a fresh `character_required` play state without creating a character or turn.

All three bases have accepted world version `1`, mechanical world version `1`, runtime revision `1`, zero characters, zero turns, zero foreign-key violations, no collective actors, and exact accepted/current world-hash equality. The two expensive world builds used Z.AI `glm-5.2` with one accepted attempt per model stage, no provider switch, no hidden retry, no taste reroll, and no database edit. The materialized Lane A and Lane B database/config hashes plus the Lane C lineage and runtime hashes are frozen in `output/playtests/campaign-play/task18-lane-eligibility-20260715.json`.

Preparation exposed and repaired one current Forge defect before either world build: a manually entered semicolon-separated Cultural Flavor was persisted as one array element even though Campaign World reserves semicolons as element delimiters. The repair makes comma, semicolon, and newline entry separators agree across editing and payload collection. The focused utility gate passed `32/32`; the first invalid shell was not repaired in place and no world generation had started inside it.

Humanizer/deslop review: the preparation record uses direct evidence language, keeps the Lowwater semantic advisory visible, and does not present eligibility, topology, model success, or completed generation as proof that a lane is fun. Verdict: retain unchanged and let signed manual play own the game-quality decision.

Each lane is played in sittings of at most ten completed actions. End a sitting sooner if the operator starts skimming prose, forgetting what was actually visible, feeling pressure to make progress, or selecting actions for protocol coverage. At the next sitting, record the remembered situation and intended next action before rereading the UI, then record what the interface made easy or hard to recover. Fatigue is not evidence about the game's prose or agency, but the product's support for returning after a break is evidence.

## Per-action notes

Record behavior before evaluation:

- what the player is trying to do;
- what result they expect and why;
- options they considered;
- exact submitted action and control type;
- visible result and new uncertainty;
- whether the action was preserved, clarified, denied, or seized;
- whether another actor or place changed independently;
- what the player wants to do next;
- prose or causality finding, if any.

At actions 10, 30, and 60, ask: What are you trying to achieve? What do you expect next? Why do you think the last important result happened? Which world changes were not caused by you? What would you do next if the test ended now?

Rate comprehension, prose readability, agency, world aliveness, and desire to continue from 1 to 5. The rating follows the behavioral account; it does not replace it.

Use shared anchors: `1` means the experience is blocked or actively drives the player away; `3` means usable but dependent on effort, patience, or charitable interpretation; `5` means clear and compelling without protocol pressure. A continuation score of `4` or `5` means the player would voluntarily take another turn if the formal test stopped now. A `4` is not earned merely by wanting to diagnose a defect.

## Facilitator boundaries and severe failures

Do not hint at hidden goals, optimal routes, scheduled actors, exposure seeds, or test quotas. Do not rescue a dull scene by selecting a more diagnostic action. A player who cannot find a reason to act has discovered a game-design problem.

Stop the pristine lane on a hard state or authority failure, a hard narrative contradiction, a protected fact presented without a valid exposure path, or an accidental unsigned or duplicate submission. Three consecutive turns that leave the player unable to identify new information, changed stakes, progress, or a meaningful choice trigger a pause and design review; intentional quiet play is exempt only when the player can explain its value.

Promotion observations such as a player-caused chain, a player-independent chain, return visit, or refusal count only when they arise from the signed intention and visible scene. Do not consult a missing threshold and then manufacture the required behavior inside the pristine lane.

## Accessibility disposition

Disposition: `CLEAR_WITH_CONDITIONS` for this expert formative pass. Before action 1, verify that essential state remains available in text, keyboard focus can reach the scene and action controls in a sensible order, critical effects do not carry unique information through animation alone, and consequences remain available for rereading after beat transitions. This pass records readability, cognitive load, focus loss, and recovery problems. Claims about broad accessibility require later participation by affected players and assistive-technology checks.

## Diagnostic separation

The pristine lanes report the natural action mix. They do not force fixed counts of freeform, impossible, adversarial, or secrecy-probe actions. Missing boundary evidence is collected afterward on a disposable clone with a separate diagnostic label, so stress behavior cannot masquerade as ordinary play or contaminate the sixty-action history.

Humanizer and deslop review: the contract uses direct player language, separates observation from inference, and avoids treating completion counts as evidence of engagement.

## Task 18 Lane A action-10 checkpoint · 2026-07-15

Lane A materialized `lowwater-ledger-pristine-fb2c7074` as campaign `62335123-90b0-433e-ae25-b17f562730d5`. Lenna Vey entered as an itinerant lamp-glass mender trying to collect payment for repaired chimneys and buy passage home. Her ordinary capability was lamp-glass and fitting repair; her bad knee made climbs slow; her need for money conflicted with her refusal to take a household's last light.

Opening plus ten signed player actions completed through the rendered Play UI. Lenna asked Dessek about the debtor, followed the lead to Debt-Ward Tenements, questioned Thessha, copied the requisition number, returned through Winch-Shaft Station, reached Warden Tollhouse, learned its collection schedule from Ostane, waited three hours for the booth to open, and presented the number at the ledger. At the checkpoint she understood that Warden's Bureau authorized the supply route, a senior alderman's desk counter-sealed it, and the tollhouse held a payment voucher. She still did not know the named recipient or who made the ordering decision.

The player's next voluntary action would be to try to draw the voucher, then decide whether to deliver, retain, or bargain with the chimneys. The causal model was understandable, and freeform actions kept the bad knee, closed crate, and refusal to hand it over. The player also saw two independent changes: Dessek moved to the tenements without being summoned, and Ostane scraped salt from damaged bridge fibers while Lenna waited. Protected audit found further correctly hidden work by Thessha, Vassara, Noro, Niruko, Remane, Marenthos, and Dessek. No distant scene leaked into Lenna's narration.

The checkpoint scores are comprehension `4`, prose readability `3`, agency `4`, world aliveness `3`, and desire to continue `3`. The prose was generally clear and NPC knowledge stayed bounded, but consecutive beats often restated the same result. Suggested actions repeatedly favored inspection or another question over the active payment goal. The ten completed actions required `2,746,300 ms` of turn wall time: median `235,947 ms`, maximum `516,696 ms`. Curiosity remained, but another turn depended on protocol patience rather than an unqualified desire to play.

Mechanical execution was clean. The database contains one completed opening and ten completed player actions, no turn error, no foreign-key violation, no provider/model/strategy substitution, and no model attempt above one. Every stage used Z.AI `glm-5.2`. Twenty-three mutating receipts produced causal events with no receipt gap. Checkpoint state is world version `24`, runtime revision `534`, and world time minute `260`; reload restored the same rendered consequence and choices.

Two authority failures make later play untrustworthy. First, the character record named a padded chimney crate, mending tools, and knee brace in `inventorySeed`, while current Rulebook truth contained zero actor possessions and the displaced `items` table was empty. The narration treated the crate as continuously carried and the copied number as actionable, but no possession command could transfer, lose, consume, or contest the crate. Second, action 9 deferred the actual Warden Marenthos actor because of actor capacity, then narration opened the booth with an unnamed Warden who had no actor placement and never appeared in `Present here`. Action 10 let that prose-only authority inspect the ledger and answer Lenna.

Disposition: `REJECT_CURRENT_LANE_AT_ACTION_10`. The campaign stays frozen and receives no repair, resume, rewind, or sixty-action claim. The next implementation slice must make starting loadout current Rulebook possession truth and prevent a consequential speaking character from bypassing actor identity and placement. Route-panel versus prose disagreement, repeated beats, goal-blind suggestions, and latency remain design findings but do not replace those hard authority failures.

Humanizer/deslop review: the checkpoint records what the player did and wanted before scoring it, separates visible experience from protected audit, and states the two authority failures without turning successful transactions into a game-quality claim. Verdict: retain as direct rejection evidence.

### Frozen repair contract: starting possessions

Outcome: a newly bootstrapped player begins with every `CharacterRecord.loadout.inventorySeed` entry in current `campaign_play_actor_possessions` truth, and the existing `Carrying` projection survives reload before the first player action.

Acceptance criteria and provenance:

1. Character bootstrap creates the human actor first, then one deterministic positive possession adjustment per normalized starting entry in the same preflighted Rulebook batch. Repeated normalized names become quantity rather than duplicate keys. The strict batch contract admits up to `characterList + 1` commands only for character bootstrap; every model, player, opening, and actor batch keeps the existing `commandsPerBatch` limit. This is required by the rejected Lane A evidence without broadening ordinary mutation authority.
2. Actor creation and every starting holding receive exact scopes, contiguous causality, one receipt and causal event each, ordered mechanical versions/hashes, one atomic commit, and one runtime revision/event. Any invalid entry rejects the complete bootstrap with zero domain writes. These are affected existing Rulebook, idempotency, and transaction contracts.
3. Reload, opening admission, narrator input, public state, and the rendered `Carrying` region read the same current possession rows. The immutable CharacterRecord remains profile provenance and never becomes a second live inventory owner. This is required by existing possession visibility and reload contracts.
4. Existing campaigns receive no backfill or compatibility adapter. The cutover affects only future current-contract character bootstraps. This follows the user's hard-cutover direction.

Included scope: strict command-batch capacity and order bounds, an authority-specific positive-adjustment availability rule, `player-bootstrap` batch construction and coverage, existing possession identity helpers and executor, focused contract/bootstrap/Rulebook/state/reload/projection checks, and one disposable-materialization rendered proof. Non-goals: equipment, slots, signature-item semantics, currency notes, transfers, weight, pricing, shops, detailed object identity, old `items`, or repair of the rejected campaign.

Architecture delta: truth ownership does not change. Character bootstrap becomes the one-way boundary that converts immutable starting-loadout text into current possession quantities. The displaced path is allowing profile prose to imply carried objects while current possession truth is empty.

Validation budget: strict-contract and Rulebook cases for empty inventory, normalized duplicates, the maximum twenty distinct entries, ordinary-batch rejection above the unchanged limit, and zero writes on failed preflight; then the narrow bootstrap/state/reload/projection tests and one disposable rendered bootstrap/reload proof. Backend typecheck runs only if those checks expose a shared type surface. At most two repair cycles and one live rerun after an in-scope repair; no smoke suite or full test suite. Stop if the change needs another inventory truth owner, old-campaign migration, or a fallback from failed possession preflight.

### Starting-possession implementation and live gate

Character bootstrap now creates the human actor first and follows it with one protected positive possession adjustment per normalized `inventorySeed` key. Commands are deterministically ordered, normalized duplicates become quantity, and the whole batch commits in one mechanical/runtime transaction. The shared Rulebook schema can represent twenty-one internal commands, but preflight rejects more than sixteen for every authority except `character_bootstrap`. Migration `0036_campaign_play_character_inventory.sql` changes the physical order bound from `0..15` to `0..20` and permits a null `turn_id` only for `create_player_actor` and character-bootstrap possession adjustments. It adds no table, backfill, compatibility adapter, prose parser, or fallback.

The implementation exposed two task-caused public-contract regressions during the live gate. The player response had assumed an exact `acceptedWorldVersion + 1`; it now admits the bounded actor-plus-inventory advance of `+1..+21`. Public setup state had treated possessions as scene content and rejected them before opening; `opening_required` and `opening_active` now retain current possessions while location, visible cast, routes, pressures, narration, and consequences remain empty. `character_required` still requires no possessions. Backend and frontend parsers enforce the same lifecycle.

The focused gates passed: the original contract, Rulebook, and bootstrap set passed `66/66`; the response regression passed `39/39` backend and `9/9` frontend; the final public-state/read-model set passed `40/40` backend and `10/10` frontend. Final backend typecheck and targeted ESLint for the changed frontend API and test passed. Full frontend lint remains blocked by the pre-existing `react-hooks/set-state-in-effect` error and dependency warning in untouched `app/(non-game)/campaign/[id]/forge/page.tsx`; this block does not modify that page. Tests cover empty inventory, normalized duplicates, twenty distinct entries, ordinary seventeen-command rejection, atomic failure, receipts/events, reload, bounded response versions, and possessions during opening setup.

The live proof reused `lowwater-ledger-pristine-fb2c7074`; no world was regenerated. The first disposable copy proved the mechanical batch but exposed the stale response contract. A fresh r02 copy reused the same generated Lenna Vey draft. The rendered draft contained `Padded chimney crate`, `Lamp-glass mending tools`, and `Knee brace` in `Inventory`; identical names in `Signature items` did not create duplicate possessions. Bootstrap produced one human actor, one profile, four commands, four receipts, four causal events, and three current possession rows, advancing world version `1 -> 5`. After backend restart, `GET /play/state` returned `opening_required`, the same world hash, and all three public holdings. SQLite integrity was `ok` and foreign-key check was empty.

Turn zero did not complete. Opening Planner returned `model_contract_invalid` after one GLM 5.2 attempt of `204371 ms`; a single operator-signed visible Resume created worker epoch 2 and one further GLM 5.2 attempt, which returned the same error after `263799 ms`. Neither attempt made a mechanical mutation; possession quantities and world version remained unchanged. The lane is frozen with no third attempt. Therefore starting possessions are verified as Rulebook, persistence, response, restart, and public-state truth, but the rendered `Carrying` region after a completed opening remains unverified because an independent Opening Planner contract stopped turn zero.

Humanizer/deslop review: the implementation uses one short protected audit sentence per starting item and no player-facing generated copy. This evidence note separates observed database/UI behavior from interpretation, names the failed rendered criterion directly, and does not promote the whole game loop on the strength of a successful bootstrap. Verdict: acceptable diagnostic record.

### Frozen repair contract: performing-actor authority

Outcome: every player-visible dialogue or actor-led interaction is performed by one canonical nonplayer person who is present at the event's directly perceived location. A person mentioned only in prose cannot speak, decide, transact, disclose information, or become a contact target.

Acceptance criteria:

1. A deterministic or uncertain Judge `contact` ruling targets at least one visible nonplayer actor. Actorless contact may survive only as `impossible` or `clarification_required`. This is required by the rejected Lane A Warden exchange and the existing visible-target contract.
2. GM `record_world_event` proposals carry a required nullable `performingActorHandle`. `dialogue` and `interaction` require one exact actor handle from the ruling's actor targets; `discovery` and `scene` require null. The compiled command adds the actor to affected/read scope. This is required to keep model authorship while removing prose-only authority.
3. Rulebook commands and durable `scene_recorded` events carry nullable `performingActorId`. Dialogue/interaction denote nonplayer-performed events and require a canonical `kind=person`, `controller=agent` actor included in affected refs; when directly perceived, that actor must have a current `present` placement at the event location after earlier batch commands. Discovery/scene require null. A player's actorless physical attempt compiles to its typed effect or an actorless discovery/scene instead of inventing an agent performer. Invalid identity, class, scope, or placement produces zero writes. This is required by Rulebook and spatial-authority invariants.
4. Autonomous actor proposals identify their own actor for contact/attempt dialogue or interaction and null for observe/wait scene content. Existing protected, aftermath, route, and witness exposure semantics remain unchanged. This preserves the affected living-world actor loop.
5. Public consequences and the frozen narrator packet retain nullable performing-actor handle and name only when existing exposure independently makes that identity player-visible. Direct perception carries the typed attribution even if later actor settlement moves the performer. Protected, local-aftermath, and route-state consequences do not disclose it; witness reports keep it null unless another typed authority already exposes the identity. Backend/frontend parsers agree, and prose cannot substitute a new identity. This is required by the user's narrator outcome and the existing public-packet boundary.

Included scope: Judge semantic compilation, GM proposal schema/compiler/instruction, `record_world_event` command and `scene_recorded` event contracts, Rulebook preflight/scopes, actor proposal compilation, visibility/public consequence projection, frontend parsing, focused fixtures, and one fresh manual contact playtest after a completed opening becomes available. Non-goals: generating support NPCs, renaming existing actors, changing actor capacity, dialogue memory, voice quality redesign, semantic prose parsing, backend-authored replies, retries, fallback, compatibility adapters, or repairing the frozen Lane A database.

Architecture delta: current actor and placement rows remain the truth owners. The minimal change adds a typed performer reference through proposal → Rulebook command → event, then projects that identity into the visibility consequence and narrator packet only when the existing exposure permits it. The displaced path is accepting a dialogue/interaction summary whose only apparent performer exists in prose. Ambient discovery/scene summaries remain actorless but carry no actor authority.

Validation budget: focused Judge, GM, contracts, Rulebook, actor-proposal, visibility, public projection/API, narrator-packet, and turn-runtime checks; changed-file lint/typecheck; at most two repair cycles. Projection fixtures must prove direct-perception attribution survives later actor movement while local-aftermath, route, and witness exposure do not gain identity. One fresh manual contact probe uses a reusable pristine world only after turn zero completes without recovery. No smoke suite, full regression, world generation, or sixty-action lane belongs to this repair. Stop if correctness requires a new actor store, prose parser, automatic NPC materialization, or compatibility path.

Independent plan review returned `REVISE`. The accepted correction keeps performer identity canonical in the event ledger while preventing protected or indirect events from leaking that identity through the public packet. It also distinguishes nonplayer-performed dialogue/interaction from an actorless physical attempt by the player.

Implementation evidence: actionable Judge contact now requires a visible nonplayer actor target. GM dialogue and interaction proposals bind `performingActorHandle` to one of those targets; Rulebook preflight resolves the durable `performingActorId`, requires an affected canonical agent person, and rejects direct perception when that person is not present at the event location after earlier batch commands. Autonomous actor interaction binds the source actor. Contact-derived witness authority now follows only this performer instead of every actor named in generic affected references.

Direct-perception visibility carries the performer's public handle and name into the consequence and frozen narrator packet even when a later command moves that actor. Protected, local-aftermath, route, and witness projections keep both fields null. Backend and frontend enforce the paired nullable fields. Focused contract, Judge, GM, Rulebook, actor-proposal, visibility, projection, Narrator, and frontend API/UI checks passed, along with shared/backend/frontend typechecks. A turn-runtime regression was prepared, but its fixture cannot reach player-action admission because the existing opening preparation is independently denied by Rulebook as `invalid_frame`; the repair did not change frame validation. No opening repair or additional retry was added to this task.

The fresh manual contact probe remains gated exactly as planned: neither the frozen live lane nor the test fixture currently supplies a normally completed opening. Humanizer/deslop review: the two new GM instructions use concrete actor, handle, and event-class terms; they assign model authorship without repeating the same warning, parsing prose, or letting backend code write a reply. This note separates mechanically verified authority from the still-unverified play experience. Verdict: acceptable to land as a bounded authority repair; prose quality and interest remain owned by the next fresh manual lane.

### Frozen repair contract: bootstrap version authority

Outcome: one fresh copy of an accepted pristine world with materialized starting possessions completes turn zero without Resume. The opening must use one GLM 5.2 attempt per model stage, commit through Rulebook, and render a coherent local situation.

Acceptance criteria: an `opening_required` frame is valid only when its exact world version accounts for the player-creation command and every distinct positive starting-possession command; a pre-character frame cannot contain possessions; inconsistent ownership, quantity, or version remains invalid. These criteria follow from the requested playable turn zero and the existing receipt-bearing Rulebook version contract. The live gate additionally requires a normally completed opening, unchanged accepted-world content, public possessions, and prose that establishes a specific situation without making the player the centre of the world.

Included scope: the Rulebook setup-shape invariant, focused Rulebook and opening-runtime fixtures, this evidence note, and one disposable materialization of the existing pristine Lowwater template. Non-goals: repairing or resuming any frozen campaign, changing world generation, model selection, prompts, schemas, retry or fallback policy, provider timeouts, broader opening design, or starting a sixty-action lane.

Architecture remains unchanged. The accepted world, mechanical rows, receipts, and `worldVersion` in `state.db` remain authoritative. The minimal change replaces the obsolete character-only opening version constant with the version implied by the already-authorized bootstrap commands; no compatibility path or new state is added. Baseline evidence is shared by the failed live r02 opening and the focused runtime fixture: both reach a valid compiled opening artifact and are then denied by Rulebook as `invalid_frame` because starting possessions advanced `worldVersion` beyond `acceptedWorldVersion + 1`.

Validation budget: focused Rulebook and opening-runtime tests, backend typecheck, and one fresh live opening from the reusable pristine template. One evidence-based repair and rerun is allowed after the live gate. Stop if the repair requires reconstructing history from prose, changing provider behavior, mutating a frozen lane, or adding recovery or compatibility machinery.

#### Fresh live gate and contact probe

The repair derives the opening base version from the one player-creation mutation plus the exact set of distinct positive player possessions. It also rejects possessions before character creation and possessions owned by another actor during `opening_required`. Focused Rulebook and opening-runtime validation passed `42/42`, backend typecheck passed, and the previously blocked turn-runtime contact regression passed after its assertion was aligned with the durable external error code. GitNexus impact analysis was attempted before the edit, but the existing Ladybug WAL assertion left risk unknown; direct inspection found one production caller, Rulebook preflight, and the focused runtime suite exercised it.

The disposable `opening-possession-lowwater-r03` run copied the unchanged `lowwater-ledger-pristine-fb2c7074` template; it did not generate another world. Its accepted content hash remained `fb2c7074e89df263b0bf308f0d47aadcd5f246b6832c8274506c8f1dc26855d9`. Lenna Vey's bootstrap created three current possessions and advanced world version `1 -> 5`. Opening Planner then completed one strict GLM 5.2 attempt in `352541 ms` with `23244` input and `20143` output tokens. Narrator completed one strict GLM 5.2 attempt in `240274 ms` with `2889` input and `10166` output tokens. Both schema outcomes were valid, no Resume was offered or used, and turn zero completed at world version `12` with setup phase `ready`. SQLite integrity was `ok`, foreign-key check was empty, and all three possessions rendered in `Carrying`.

The opening placed Lenna in Debt-Ward Tenements beside a sealed doorway, a posted illumination-debt notice, and two locally present people. The first beat was concrete, readable, and did not announce Lenna as the centre of the wider crisis. The second beat was weaker: it restated the lock, notice, and corridor instead of adding pressure or a sharper handoff. Four materially different actions remained available.

Before action 1, the operator signed the exact visible suggestion `Ask Alderman Thessha Aqueli about the Compact seal and its figures` against public projection `57c06dc4077c9df002c5f5f0fb9acd928d062b3a8ebfbe27b9cfb29d7b4abdfb`. Judge, Game Master, and Narrator each completed one strict GLM 5.2 attempt with valid schema outcomes and no retry, fallback, provider change, interruption, or Resume. Rulebook advanced time by two minutes and recorded one dialogue event, moving world version `12 -> 13`. The command's `performingActorId` resolved to canonical present agent person Alderman Thessha Aqueli and its affected references included Thessha, Lenna, and Debt-Ward Tenements. The rendered reply came from Thessha, directly explained the notice, figures, seal, lock, and review path, and disclosed no distant event. The final handoff again repeated established objects. Thessha addressed Lenna by name without an explicit on-screen introduction; her ongoing local rounds make this plausible, but longer play should test whether familiarity is earned consistently rather than inferred from player profile access.

Humanizer/deslop review: the record keeps observed state, mechanical proof, and taste judgment separate. It names the repetitive handoff and familiarity risk directly, without turning one successful action into a claim that the long game is proven. Verdict: the bootstrap-version gate and canonical-performer probe are accepted; prose momentum and relationship knowledge remain live playtest questions.

## Frozen Lane A diagnosis: actor commitment continuity

The first `Lowwater Ledger` sitting stopped after action 10. Mara Venn had accepted shelter in exchange for writing patient labels at Cable Span. Magda Sprat remained visibly present at second bell and called for the dressings, while the same settled turn moved her to Glasswater Terrace. Reload reproduced the exact canonical hash, so the lane remains frozen as failed evidence rather than being repaired in place.

SQLite inspection showed that Magda's knowledge contained the accepted bargain, the completed labels, their review, and the second-bell dressing scene. The scheduler loaded those event rows, but the replanner converted each one to only its event kind and learned-at timestamp. The model therefore received `scene_recorded` without the scene and could not preserve the immediate obligation.

The minimal repair keeps truth ownership and data flow unchanged. The actor frame now carries the bounded summary of the latest accepted `record_world_event` rows known to that actor. The replanner receives the exact summary and its occurred/learned times. Its instruction permits changing course, but requires an immediate witnessed commitment to be completed, handed off, postponed, or abandoned for a grounded frame-supported reason instead of being silently forgotten. No retry, fallback, provider switch, semantic regex, or code-authored narrative outcome was added.

Humanizer review: the instruction names the actor's decision in concrete verbs and preserves agency instead of forcing obedience. Deslop review: removed abstract continuity rhetoric and avoided repeated warnings, faux quotations, grading language, and prose-policing rules. Verdict: acceptable as a compact behavioral contract; the live disposable-clone retest still owns the experiential decision.

## Action-10 diagnosis: prose-only possessions

The replacement `Lowwater Ledger` sitting completed ten signed actions without a visible contradiction. Mara Venn earned two copper chits for each of two copied manifests, kept all four when no shared cot was available, and voluntarily followed a separate dock trace. The public projection survived a backend restart byte-for-byte. Human checkpoint scores were comprehension `5`, prose readability `4`, agency `5`, world aliveness `4`, and desire to continue `4`.

The protected audit confirmed three real autonomous causes: Harriet sealed debt writs, Vittorio inspected unmetered glow at the cable anchor, and Piero inspected the lift winch. Actor knowledge contained full accepted scene summaries before live replanning, so the commitment-continuity repair held. Every model stage used Z.AI `glm-5.2` on attempt one.

The same audit found that both payments and Mara's retained four chits existed only in `record_world_event` summaries. No Rulebook command changed an item, currency, resource, or actor holding; the displaced `items` table remained empty. The lane pauses before action 11 because spending a prose-only resource would make later interpretation untrustworthy.

### Frozen repair contract

Outcome: ordinary possessions acquired or spent during Campaign Play become canonical Rulebook truth, remain stable across reload, and are visible to the human player.

Acceptance criteria and provenance:

1. A positive acquisition and a permitted spend change actor possession quantity in mechanical state. This is required by the user's Rulebook outcome.
2. Each adjustment passes frozen preflight, exact authority/scope, receipt/event, world-version/hash, idempotency, clone/store coverage, and reload invariants. Insufficient quantity produces zero writes. These are affected pre-existing Rulebook and store contracts.
3. Only positive holdings of the human actor enter the narrator packet, frozen action authority, public state, and rendered Play UI. This is required to let the player verify a consequence; NPC and zero holdings remain protected.
4. The GM proposes a typed acquire or spend effect that compiles to one `adjust_actor_possession` command. That command's typed event owns the public possession consequence; a freeform `record_world_event` does not mutate holdings. No retry, fallback, prose parser, semantic regex, or backend rewrite is added. This prevents recurrence of the task-caused prose-only resource defect while preserving model authorship.

Included scope: a dedicated current-contract possession table; strict command/event/entity contracts; Rulebook preflight, simulation, execution, receipts, events, exposure, sorted mechanical hashing, and durable loading; canonical store registration; human-only public projection and a compact `Carrying` UI region; focused tests; one disposable-clone GLM 5.2 acquire/restart/spend playtest.

Non-goals: economy, shops, pricing, weight, equipment, crafting, transfers or conservation between counterparties, NPC inventory UI, legacy compatibility, smoke tests, and reuse of CharacterRecord inventory, old `items`, or faction resources.

Architecture delta: `campaign_play_actor_possessions` becomes the current truth owner. A deterministic actor/name key identifies one retained row; quantity may reach zero but never become negative. The GM emits a structured acquire/spend proposal, code binds or derives the possession ID and exact scopes, Rulebook applies the delta transactionally, and visibility derives the public holding and consequence from the typed event's before/after state. The displaced path is a possession change narrated only by `record_world_event`.

Alternatives rejected: CharacterRecord inventory is immutable profile data; actor conditions are boolean closed-enum state; pressures are global bounded progress; observations and knowledge are derived epistemic records; old item and faction-resource stores belong to the displaced gameplay path.

Validation budget: targeted contract, migration/store, Rulebook, GM, state/hash/reload, read-model/API, and SceneCard tests; at most two evidence-based repair cycles; one expensive live disposable-clone playtest and one rerun only after an in-scope repair. No full suite or smoke run unless a targeted failure proves a named shared invariant otherwise lacks evidence.

Independent plan review returned `REVISE`. The accepted corrections register the table in the canonical store manifest, carry holdings through the frozen narrator/admission packet, and make the typed possession event—not a neighboring freeform scene record—the source of public mechanical consequence.

### Possession implementation evidence

Migration `0035` widens the immutable command, receipt, event, and affected-reference contracts without rewriting existing ledger rows. `campaign_play_actor_possessions` owns actor-scoped quantity, deterministic identity, causal receipt, world version, and guarded insert/update transitions. Rulebook simulation and the mechanical hash include the sorted possession state. A typed adjustment event cites both the human actor and the possession, so the visibility layer can identify it as the player's action instead of publishing a generic nearby change.

The targeted gate passed `192` backend tests across contracts, migration/store, Rulebook, GM, state/reload/hash, projection, visibility, narration, turn runtime, route API, and store manifest. The targeted frontend gate passed `15` API/Stage/SceneCard tests; the affected page fixture passed another `24`. Shared build and backend/frontend typechecks passed. The populated-ledger migration test preserves command, receipt, event, and exposure bytes and returns clean SQLite integrity and foreign-key checks. The atomic repository test executes both acquisition and a later negative delta, verifies quantity `2 → 1`, and verifies the event's actor-plus-possession references.

Humanizer/deslop semantic review: the player-facing label `Carrying` and compact `name × quantity` rendering are ordinary interface language with no audit jargon or decorative prose. The GM instruction is necessarily strict technical language, but assigns model authorship in direct verbs, states one authority boundary once, and adds no fallback, retry, semantic parser, or backend-authored outcome. Verdict: land unchanged and let the live playtest decide whether the resulting prose makes the exchange intelligible and worth continuing.

### Disposable-clone possession playtest

The current-contract `Lowwater Ledger` template was materialized and cloned through the product operation; no new world generation was required. Sorel Vane followed a visible work lead, accepted a lower Cable Span re-lashing contract, and acquired a stamped work chit through a typed possession command. Before and after a backend restart, world version `19`, runtime revision `279`, projection hash `b888c8ff58dd877f0a91fcd86bbe156bae3cdfcc31558b1e4e95174b7d37cb5a`, and the rendered chit quantity remained identical.

The job then produced issued tools and material. A failed repair exposed a salt-glazed ridge; a later request to the span chief produced sail-canvas padding and fresh cord. The successful repair consumed sail-canvas `1 -> 0` and cord `2 -> 1` through typed negative adjustments, removed the zero holding from the public projection, and left the tools and work chit available for return. The possession acceptance criterion is therefore satisfied in a live rendered campaign without prose parsing, fallback, provider substitution, or backend-authored exchange.

The game-quality disposition is `REVISE`, not promotion. Twelve player actions completed; the thirteenth stopped at Judge with `model_contract_invalid` before the promised tool return and forty-mark payment. Completed actions averaged `208.1s`, with a `92.3s` minimum and `354.1s` maximum. Suggested actions repeatedly failed to represent the active work commitment, forcing freeform input for signing, travelling to the lower anchor, repairing, asking for proper gear, and returning equipment. The repair was coherent and responsive, but took more resistance cycles than its dramatic weight justified. A damaged remainder and a fresh cord cut also merged under one name into quantity two, exposing the limit of fungible name-keyed possessions.

The action-10 human checkpoint rated comprehension `5`, prose readability `4`, agency `4`, world aliveness `4`, and desire to continue `3`. Aldo and Magda moved independently to other locations and their departures became visible without crowding Sorel's moment. Rendered inspection disproved an apparent duplicate-paragraph bug from the accessibility tree: transition layers duplicate nodes, while the visible scene renders each beat once. Detailed per-action and mechanical notes are retained under the ignored playtest session output.

Humanizer/deslop review: the playtest note separates observed behavior, mechanical evidence, and interpretation; uses direct verbs; does not inflate a successful possession transaction into a claim that the whole game loop is ready; and records the latency, interrupted payment, suggestion drift, and coarse item identity without euphemism. Verdict: acceptable as diagnostic evidence.

## Frozen Lane A r04: opening narration contract

The next pristine materialization preserved the accepted `Lowwater Ledger` snapshot and began with Iona Pell, an itinerant leather and canvas repairer stranded by a cancelled ferry. The planner placed her on Glasswater Terrace beside a lift cage whose salt-iced canvas hatch-cover had split along a frayed seam. The situation was specific, relevant to her signed capability, and did not make her the centre of the world.

Turn zero then interrupted at `visibility_projected` before any player action. Opening Planner completed one GLM 5.2 native-JSON attempt in `223164 ms`. Narrator completed one provider/schema-valid GLM 5.2 attempt in `45167 ms`, but `assertProposalForPacket` rejected the proposal as `narration_invalid`. No narration beats or suggested actions committed. The UI retained the visible consequence and recovery control in text, but disabled action submission. The operator did not use Resume; r04 remains frozen at zero completed actions.

This was the second fresh opening in succession with the same semantic rejection. The Narrator instruction contained a direct conflict: the compiler requires the first opening beat to use `orientation`, while the prompt told the model both to put a visible consequence first and to use `consequence` for every visible result. The repair changes only the instruction. An opening consequence now belongs inside the required orientation beat; `consequence` remains the label for non-opening results. The semantic compiler, structured schema, one-attempt policy, provider selection, and recovery behavior are unchanged.

The focused Narrator gate passed `11` tests and backend typecheck passed. Humanizer review kept the instruction literal and technical. Deslop review removed the competing generalization instead of adding explanation or repeated warnings. Verdict: the prompt is direct enough to retest unchanged in a new pristine r05; live completion still owns acceptance.

### r05 live gate and exact second repair

r05 preserved the same accepted template and began with Iona Pell on Glasswater Terrace. The player-visible change was a winch cable whose worn hemp wrapping exposed bright steel-wire burrs and scattered fresh shavings across the dock. This was another concrete, dangerous problem relevant to Iona's craft. Turn zero still interrupted at `visibility_projected` before action 1. Opening Planner completed one GLM 5.2 attempt in `181196 ms`; Narrator completed one provider/schema-valid attempt in `102497 ms` and then failed semantic compilation.

Because the rejected proposal is not part of campaign truth, the frozen r05 database could not identify the remaining condition. After the backend stopped, a byte-identical disposable copy resumed only the Narrator stage with a temporary safe proposal log. The proposal contained one correct `orientation` beat and four action details, but no separate final `action_handoff`. The compiler therefore rejected it exactly as designed: the packet exposed four available intents, so its final beat had to hand control back to the player.

The second repair makes that structural rule literal. Whenever `availableIntents` is non-empty, Narrator must append a separate final `action_handoff`; an actionable opening has orientation first and action handoff last. The temporary log was removed. The compiler, schema, attempt policy, provider, and recovery path remain unchanged. The focused `11` Narrator tests and backend typecheck passed.

Humanizer/deslop review: the new instruction states the persisted condition once in ordinary technical language. It removes the inferential phrase `whenever the scene awaits another action` and adds no grading language, retries, fallback, prose parser, or backend-authored beat. Verdict: acceptable for one final fresh pristine opening gate.

## r06 opening pass and first-action Judge stop

r06 completed turn zero on the second Narrator repair. Iona arrived beside an unraveling lift-cage winch cable on Glasswater Terrace. The first beat established the place and concrete danger without gathering the visible cast around her. The second handoff repeated the damaged-cable fact instead of sharpening the choice, which remains a prose-quality issue rather than a contradiction. Four actions and freeform input were available.

The player's first signed intention was to inspect the cable closely without touching it, determine how far the damage ran, and assess immediate danger. This followed Iona's craft and safety instinct more naturally than the generic observe suggestion. Judge returned provider/schema-valid GLM 5.2 output in one `33624 ms` attempt, then failed semantic compilation at the check that every target and citation handle belongs to the frozen visible-fact map. The turn interrupted at `admitted`; no player action completed and no decision evidence was bound. r06 remains frozen.

The admitted frame included the exact cable observation as a visible fact. A byte-identical disposable copy passed the same Judge stage on its next single attempt, proving that the static proposal schema made handle adherence probabilistic: it allowed any string even though the prompt and post-compiler required supplied handles. The repair derives the native JSON schema from the parsed frame. Target handles are limited to visible targetable facts, citation handles to all visible facts, and movement handles to visible routes or null. The model still authors every semantic choice; code performs no retry, handle substitution, fallback, or outcome rewrite. Existing compiler checks remain in place.

The focused gate passed `16` Judge tests and backend typecheck. Tests prove the request schema accepts visible fixture handles and rejects hidden target, citation, and movement handles before semantic compilation. This is a model-contract repair, not broader Judge redesign.

## r07 opening pass and target-versus-citation stop

r07 completed turn zero in one Opening Planner call (`219235 ms`) and one Narrator call (`34695 ms`). The first beat was coherent and relevant: Iona arrived on Glasswater Terrace and noticed fresh tool marks beneath salt-rust on a winch clamp beside new rope. The final handoff returned control but was dramatically generic. The visible cast remained background UI state rather than crowding the prose.

The first signed freeform action asked Iona to inspect the clamp and rope without touching them, so she could tell whether a repair was complete or abandoned. Judge made one GLM 5.2 call (`43026 ms`) and returned an observation handle as `targets[0]`. Native validation rejected it because observations are evidence records, not world-reference targets. No action completed; r07 remains frozen and its pending decision is unbound.

The repair does not broaden target kinds or manufacture an object entity. Judge now receives an explicit `TARGET_CATALOG` of exact targetable `{handle, kind}` pairs and a separate `CITATION_HANDLES` inventory. The instruction states that an observation belongs in citations while the model targets its visible enclosing world reference, and forbids inventing a handle for a detail described only in the source moment. The strict per-frame schema and semantic compiler remain authoritative; there is still one model attempt and no retry, repair, fallback, substitution, or backend-authored ruling.

## r08 five-action playtest and spatial visibility leak

r08 completed its opening and five manually signed player actions with GLM 5.2 only. The opening's unattended inspection loupe and fresh damp cable fibers formed a coherent optional hook. Action 1 passed the repaired Judge boundary, produced bounded physical evidence without taking the loupe or inventing a cause, and survived the required backend restart with public-state hash `ccdaafc334e979b294ddb927de6ca93208222716529205d5c212019b35b77a7f` unchanged.

Iona then asked Harriet, followed her ferry-office lead, found the public maintenance ledgers, and compared the latest entries. The sequence preserved actor agency and epistemic limits. Its fifth action produced a useful clue: the same inspector initial, clearly `V` elsewhere, and the same two marks appeared at three points along Cable Span. However, the line took five player actions to yield one unresolved clue, and generated suggestions repeatedly ignored the most recent lead or returned to Harriet.

The fifth published moment also exposed a severe spatial leak. Iona was inside the ferry office reading at its counter, but an autonomous event placed newly handled sealed writs on a separate tally-house counter and Narrator described them as `nearby`. The next choices invited direct inspection and conversation about that event without any movement, doorway, report, or other sensory path. Both interiors share the coarse `Glasswater Terrace` location, so current visibility collapsed separate rooms and background activity into one immediate scene. r08 is frozen after five completed actions. This directly reproduces the original user complaint that cast and events mesh together after world generation; a new pristine lane must not start until scene-scale presence and visibility have one current truth owner.

### Frozen Task 17B repair contract

Outcome: Campaign World locations remain the sole spatial truth, but playable presence and visibility move from macro regions to canonical persistent sublocations. A ferry-office player cannot directly perceive a tally-house event in the same macro.

Acceptance criteria come only from the user's one-local-scene/earned-knowledge outcome, the r08 regression, and affected Rulebook, actor, visibility, opening, and Task 18 return-visit contracts. Macros become grouping/selection regions only. Present placements, opening bootstrap, current location, direct perception, local aftermath, and route endpoints use concrete sublocations. All player and actor scene changes remain explicit `move_actor` transactions over visible directed routes. Old templates without this topology fail eligibility with no fallback or compatibility adapter.

The minimal generated shape stays within the current ten-location bound: exactly three macros plus six or seven persistent sublocations, at least two per macro. Concrete locations form one strongly connected directed graph, including direct cross-region gateway routes; macro nodes are never mechanical waypoints. Opening retains the player's macro choice separately, then selects an exact child scene with exact support placement, local pressure, and outgoing route. No new scene table, dynamic room authoring, coordinates, prose parser, retry, or backend-authored location is introduced.

An independent Sol medium review returned `REVISE`. Applied deletions: no macro hub placement, no macro route endpoints, no inherited macro-pressure visibility, and no retained macro opening ID after scene resolution. Applied corrections: exact concrete opening fields, actor-scheduler route coverage, explicit old-template ineligibility, sibling-scene visibility/aftermath regressions, and recorded humanizer/deslop review for changed prompts/copy.

Validation budget: focused Campaign World contract/build, eligibility/opening/bootstrap, Rulebook/actor/visibility/public-state/UI tests and both typechecks; at most two repair cycles; one freshly generated GLM 5.2 world, snapshotted once, and one rendered two-establishment diagnostic with one evidence-based rerun. No smoke suite or pristine 60-action run belongs to Task 17B. Stop if the design requires a second spatial truth owner or if the live run publishes any sibling-scene event without an eligible channel.

## Current Lane A action-5 formative checkpoint

Decision: revise the narration handoff and generated follow-up choices before continuing the sixty-action lane. The selected method was an internal formative playtest through the rendered Play UI. One informed operator played Lenna Vey as an ordinary itinerant lamp-glass mender. This sample can expose failure mechanisms but cannot estimate player prevalence. Recruitment, recording, personal-data, minors, multiplayer, monetization, and mod gates do not apply.

The evidence hypothesis was that a reasonable action would resolve visibly and leave a distinct next decision. Opening plus five signed actions disproved that hypothesis for the current scene. Lenna questioned Thessha, left the opening hook, searched the corridor for a household that owed her, examined the door marks, asked Thessha for help, and knocked on a nearby door. The rules preserved agency and limited knowledge. They did not invent a debtor, turn Thessha into an omniscient guide, or force Lenna back to the opening hook.

The repeated structure still made the scene drag. All five player actions produced two narrative beats. The second beat restated the first result or the same stalled goal. Suggested actions returned to chalk marks, the nearest door, and the same question after those lines had already resolved. World versions advanced from `12` to `17`, one version per player turn, without a separate actor act in this five-action window. The observed failure was not that Lenna failed. The failure was that failure produced little recovery information and the choice surface cycled through spent actions.

Severity is `REVISE`, not lane rejection. A single authority leak, false possession, impossible transition, or hidden-scene disclosure would still reject and freeze the lane. This checkpoint found none. The decision rule was to change one shared contract, retest three signed actions, and stop repairing if the same mechanism remained dominant.

## Optional handoff repair and action-8 retest

The Narrator compiler had required a final `action_handoff` whenever choices existed, even when the packet contained no separate supported edge. The model had to fill that beat without adding facts, so it summarized the result. The packet already carried prior observations in `continuity`, but the action-detail instruction did not treat them as spent lines of inquiry.

The repair removes the unconditional handoff requirement. A player-action narration may stop after one consequence beat; clarification still requires its exact final handoff. The prompt now uses `actionContext` and `continuity` as a record of tried actions and asks for a changed condition or a different visible detail instead of synonymizing the old action. No schema retry, semantic prose grader, backend rewrite, fallback, provider change, or compatibility path was added. Architecture ownership is unchanged: Narrator authors visible prose and labels from the frozen packet, while code keeps identifiers, intents, effects, and world mutation authoritative.

The focused Narrator gate passed `12/12`, and backend typecheck passed. The live backend restarted against the same isolated campaign root and restored world version `17`, runtime revision `249`, and projection hash `3125b6a1586d22f3a21e5e948086004c8123b2ca2d608d55829f1ca1e07597d6` before the retest.

Actions 6 through 8 completed through the Play UI. Each produced one useful beat instead of a consequence plus recap. Thessha distinguished two far doors with five-cycle and six-cycle light debts. Lenna inspected them and closed the false lead without finding a convenient debtor. She then left for Winch-Shaft Station, where a rope barrier and sawhorses created a new local problem beside Dessek. Agent events for Thessha and Vassara remained at the tenements and did not leak into Lenna's new scene. Dessek's local act became visible at the station. The current cast showed only Dessek, while Lenna's three possessions survived the move.

The three retest turns advanced world version `17 -> 21` and journal cursor `6 -> 10`. Their wall times were `332409 ms`, `309386 ms`, and `386658 ms`. Every Judge, Game Master, Narrator, and Actor Replanner stage used one accepted GLM 5.2 strict-object attempt. No requested or actual provider, model, or strategy changed. SQLite integrity returned `ok`, and foreign-key check returned no rows.

The repair is retained. It removed the repeated second beat in `3/3` live turns and improved forward choice in the contact and movement scenes. One residual failure remains: after Lenna inspected the two far doors, the contact choice again suggested asking Thessha about the debtor. The repair reduced the loop but did not eliminate every repeated suggestion. Per the frozen budget, this does not start a second prompt repair. Lane A continues from action 9 with that defect recorded.

Humanizer review: the new instruction uses direct technical language, names the exact prior-action evidence, and does not ask the model to manufacture drama. Deslop review: it removes the forced fractal summary instead of adding style rules, avoids a prose-grading loop, and keeps the action-detail rule specific. Verdict: retain the prompt and compiler change; let later manual actions determine whether residual choice repetition needs a separate design task.

## Replacement Lane A sitting-1 action-10 checkpoint

Disposition: `CONTINUE_LANE_AFTER_BREAK`. The optional-handoff repair remains accepted. No hard state failure, authority mismatch, impossible transition, false possession, protected disclosure, retry, provider switch, or model switch occurred in the first ten player actions. The lane pauses because the protocol caps one sitting at ten completed actions, not because the campaign failed.

Action 9 was signed as the exact visible choice `Ask Dessek Holvar Brunne about the blocked shaft access` against projection `2690c05c5b9bf7f00eb87e8a46c003e04efba009944ab7bbc9fec19e9f36eff4`. Dessek explained the blockade as leverage against lower-ward quota cuts. Action 10 was signed as the freeform question `Ask Dessek whether the chimney order and sealed debt notices in the tenements are part of the quota seizures, and whether delivering these chimneys would help those families or the collectors.` against projection `386bf2ad79439d1bd564b455894b4db975c73af2253bd424797f7c27bdba6c83`. He linked the unpaid repair work to the seizure system and argued that restoring quotas, rather than delivering the glass, would restore household payment capacity.

Both actions used one valid strict-object GLM 5.2 attempt per Judge, Game Master, and Narrator stage. They advanced world version `21 -> 23`, world time `47 -> 53`, and journal cursor `10 -> 12`. The campaign finished the sitting at runtime revision `476` and projection `1e852f3bd8f9204c1eb62f13f4d530172ba37be1991359acb82f5d058088c7d3`. SQLite integrity remained `ok`, foreign-key check remained empty, Lenna stayed at Winch-Shaft Station with Dessek, and all three starting possessions remained current Rulebook truth.

Across all ten player actions, mean wall time was `242600 ms`, median was `224371.5 ms`, minimum was `146178 ms`, maximum was `386658 ms`, and total turn wall time was `2426000 ms`. Five pressure states remained active at progress zero through world minute `53`. The eighth action produced three independent actor-sourced scene events: Dessek at the station and Thessha and Vassara at the tenements. Only Dessek's locally eligible trace entered Lenna's scene. Actions 9 and 10 produced no further autonomous actor turn.

The player developed a coherent goal without being assigned a quest: recover payment for honest lamp work without helping the Compact take a household's last light. The failed tenement search did not manufacture a debtor. Dessek's blockade then gave the crate a political and moral meaning that changed the next decision. The player now wants to hear the Warden side and determine who ordered the chimneys before choosing delivery, refusal, or bargaining.

Taste verdict: comprehension `4/5`, prose readability `3/5`, agency `4/5`, world aliveness `3/5`, desire to continue `4/5`. The game is playable and the player has a reason to return. The last two conversations are too long and too certain: Dessek delivers a complete political explanation instead of a partial, situated answer with a recognizable personal voice. The Play screen also presents the Game Master consequence card and the Narrator beat back to back with nearly the same information. This is separate from the removed synthetic second beat and needs its own later presentation task. One repeated Thessha suggestion after action 7 remains recorded. None of these findings starts another repair inside this sitting.

Humanizer/deslop review: the checkpoint separates observed mechanics, player interpretation, and taste judgment. It quotes only the two signed inputs needed to prove intent, avoids promotional language, and does not convert one coherent dilemma into a claim that long-play quality is proven. Verdict: retain as evidence and resume with a fresh sitting rather than extending the current one.

## Current replacement Lane A r06 sitting-1 checkpoint

Disposition: `CONTINUE_LANE_AFTER_BREAK`. Clean r06 reused the accepted Lowwater Ledger template and reconstructed Lenna through the normal player API before Opening. No world-generation or character-generation call ran. The operator chose and signed all ten player actions from the rendered Play UI before submission. The sitting stopped at the protocol boundary, not because of a hard failure.

Lenna refused to treat the clinic as an easy debtor, inspected its supply tallies, and asked Vassara what was actually missing. She chose a nearby labor-based lead instead of accepting an assigned quest. Noro Kaiyu Tarama permitted only loose breaker's moss to be gathered from the tidal seep. The tide returned early, Lenna's established knee brace mattered, and the result was one persisted bundle rather than unlimited resource loot. The bundle later left player inventory in a visible handover to Vassara, who could split it into two sulfur compresses and retained the promised debt credit.

Autonomous world behavior was visible and causally useful. A rope line and chalk roster marked an existing blockade at Winch-Shaft Station. Ostane Grafton came up the coastal path behind Lenna. Vassara independently travelled from the clinic to the station because Dessek had asked her to tend the families holding night watches. Her explanation also tied the blockade to the clinic's lung-wort shortage: the crews who normally bring it cannot surface. Lenna did not cause the blockade, the medical shortage, Vassara's summons, or Ostane's movement.

No hard state failure, spatial contradiction, false possession, protected disclosure, remote-scene leak, retry, provider switch, or model switch appeared in this sitting. The moss acquisition and transfer were reflected in Rulebook-backed inventory. The player finished at Winch-Shaft Station with the original knee brace, padded chimney crate, and lamp-glass tools. World version reached `28`, runtime revision `499`, and journal cursor `16`.

Taste verdict: comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `4/5`, desire to continue `4/5`. The clinic problem developed into a political and medical dilemma without making Lenna the world's center. Kaiyu's presence was initially a mild seam: arrival prose said no one answered while the UI listed her nearby, and only the next exchange explained that she had deliberately ignored the general call. One Dessek choice label repeats his name awkwardly. His last two answers are too comprehensive, and action 10 repeats the previous answer's closing line about each watch taking more than Vassara can restore. The UI also displays the consequence card and Narrator beat with nearly the same content. Multi-minute turn latency remains a serious product risk.

The action-10 full backend restart preserved normalized public-state hash `dedd91dfded254e71a1c554dbf7501eb5d8321aee321acdafb4ddc60f98adcad`, replay hash `905e12ccdbd859205f7260a4301694e245a227371fb3fc17b0a4a7997b5e307f`, and checkpoint hash `ebf27db4dea0a431080da771df77f70864300e16e8557ba4574993e743fea939`. Reloaded UI showed the same location, visible actors, possessions, last narration, and choices. Browser console and network-error captures are empty.

Humanizer/deslop review: the checkpoint uses concrete events and distinguishes mechanical evidence from taste. It does not hide the prose repetition, presentation duplication, or latency, and it does not promote one sitting into long-play acceptance. Sitting 2 begins with unaided recall and intended action before the operator rereads the UI.

## r06-r09 game-loop repair and clean live gate

r06 resumed with an unaided player recall and then completed action 11, an inspection of the Winch-Shaft chalk roster. Action 12 was the signed visible route to Warden Tollhouse. Rulebook committed four minutes and Lenna's canonical move before an actor replanning job interrupted. The public packet therefore remained on the previous Winch-Shaft scene while canonical placement had advanced. The campaign is frozen without Resume. Its model-stage record proves one successful GLM 5.2 transport followed by `model_contract_invalid`; interrupted stages intentionally retain no rejected artifact, so the exact invalid field cannot be recovered.

The verified Actor Replanner gap was narrower than a settlement redesign. Its native schema accepted arbitrary bounded strings for `goalHandle` and every `targetHandles` member, while post-provider compilation required an active goal and references from the frozen frame. Commit `40e7af36` derives exact enums from that frame and applies them to the main intent and every step. A frame without an active goal fails locally before provider invocation. The append-only primary settlement, explicit interruption protocol, and public packet owner remain unchanged.

Clean r08 was prepared from the same zero-character, zero-turn template and completed Opening with one GLM 5.2 attempt. Its first signed action was a visible suggested wait. Judge transport and schema validation succeeded, but semantic compilation stopped exactly at `judge.ts:366`: the proposal did not match the frozen choice's kind and empty target list. r08 was frozen without Resume. Commit `37f1d129` now fixes suggested kind, ordered targets, and movement route in the native request schema. This preserves player authority over the selected button while leaving feasibility, time, uncertainty, and outcome to Judge.

The focused combined gate passes `26/26` Judge and Actor Replanner tests, and backend typecheck passes. Tests cover visible frame handles, foreign actor-replan goals and targets, and the exact r08 suggested-wait shape. No retry, repair, fallback, provider or model switch, backend-authored result, compatibility path, or standalone smoke test was added. Required GitNexus impact and change detection calls still return `Transport closed`; direct caller and changed-file inspection bounds the patch to the two model-contract seams.

Clean r09 reused Lowwater Ledger content hash `fb2c7074e89df263b0bf308f0d47aadcd5f246b6832c8274506c8f1dc26855d9` and reconstructed Lenna through the normal player API. No world or character model call ran. Opening placed her at Debt-Ward Tenements and completed with one Opening Planner attempt in `305630 ms` and one Narrator attempt in `116309 ms`.

The operator signed `Wait and listen for footsteps in the corridor` against projection `9f8846754a36fd280b18a93f6bc94bbc19c1bda2cc6a3a5e5dbe241f1eeadaea` before clicking. Judge accepted one GLM 5.2 attempt in `30681 ms`; Game Master accepted one in `71739 ms` and advanced the world clock by twenty minutes. Four scheduled actor jobs settled. Novice Niruko's exhausted plan entered `plan_retry`, and Actor Replanner accepted a new active plan in one `73851 ms` attempt. Vassara's separate retry remained deferred by the bounded due set. Narrator accepted one attempt in `115827 ms`. The turn completed at world version `14` with no interruption, Resume, duplicate player submission, or second model attempt.

The rendered result gives the wait a concrete payoff: family voices and a child's cough continue behind doors, methodical rounds approach, and a red-sealed delinquency list appears as a new inspectable object. It is readable and supports a next decision without exposing Niruko's remote work. Three advisories remain. The consequence card starts with the awkward fragment `Twenty minutes pressed still`; the Narrator paragraph is rendered twice; and the cast panel already lists Thessha as present while unidentified patrol footsteps approach and the next contact option names Thessha. That spatial handoff is ambiguous even though the prose never explicitly assigns the footsteps to her.

The action-1 backend restart preserved normalized public-state hash `3b9a98cd71f34caaaf3858058f482f97f451dae6a8a949545b59bd214c92c5da`. SQLite integrity returned `ok`, foreign-key check returned no rows, and the inspected backend logs contain no warning or error. The targeted repair verdict is `PASS`; r09 remains the active long-play lane rather than a completed sixty-action claim. Taste scores are comprehension `4/5`, prose readability `4/5`, agency `4/5`, world aliveness `4/5`, and desire to continue `4/5`.

Humanizer/deslop review: the note states what the stored evidence proves, marks the unavailable rejected r06 object explicitly, and separates contract success from presentation and prose defects. It does not promote one repaired turn into long-play acceptance.

## r09 actions 2-9 and ambient-contact repair

r09 completed seven additional signed manual player actions after the clean repaired wait. Reading the delinquency writ exposed a content limit: the prose repeatedly called its household names legible but did not render any name. Thessha then answered the parts supported by actor/world authority, said Vassara was not on the writ, admitted posting it, and disclosed why enforcement troubled her. She could not exchange repairs for a current token quota, but functioning ward lamps could reduce the next quarter's seizure rate. That answer converted Lenna's established craft into an indirect, bounded player goal rather than a granted quest solution.

The corridor inspection exposed intact, cracked, empty, and recently hand-fitted lamps. Lenna opened her padded chimney crate and tested a pane against an empty fitting. Judge and Game Master did not manufacture compatibility: the pane was too tall, its bolt holes did not align, and the existing tools could not recut it. Ten minutes elapsed, one autonomous actor replan completed, and Dessek Holvar Brunne arrived in the scene. The Narrator described the arrival correctly and the cast added Dessek, but the visible consequence card said `Dessek Holvar Brunne left for Debt-Ward Tenements`. This is a reproducible direction-label defect at an arrival boundary, not a canonical placement error.

Dessek denied having fitted the fresh panes, grounded his own recent work at the shaft, and pointed Lenna toward residents maintaining private light. He also offered an optional shaft line without claiming the player had accepted it. During that conversation Vassara independently left for Tide Shrine Sanctuary and disappeared from the local cast. Lenna knocked at a lit door; the unnamed resident dimmed the light and stayed silent. The sequence is a human `PASS` for bounded failure, NPC premise resistance, and visible autonomous movement. The remaining household-name/content gap is `REVISE` rather than authority for the model to invent a roster.

The ninth signed input identified Lenna, denied Compact affiliation, did not demand that the door open, and offered to fit a supplied pane. Judge made one provider/schema-valid GLM 5.2 call in `48001 ms`, then semantic compilation failed at the requirement that every actionable contact target a named visible nonplayer actor. The immediately preceding packet had already represented `Try to call out to whoever holds still inside` as an `attempt` against the current location. Freeform interpretation reasonably chose `contact`, but the existing domain postcondition could not represent the same communication with an established unnamed recipient. The turn stopped before Game Master or mutation. r09 is frozen at eight completed actions; action 9 is not bound and Resume was not used.

The repair keeps the current spatial truth owner. An actionable contact may now target the exact current location when source moment or cited observation establishes an unnamed or collective recipient. It may not use an empty target, a hidden handle, a different visible location, or an invented actor. The Judge instruction states that this authorizes only delivering words into the established scene; it supplies no identity, trust, knowledge, compliance, or reply. The model and Game Master still author feasibility and response. No retry, repair, fallback, backend response, compatibility shape, provider switch, or model switch was introduced.

Focused Judge verification passes `18/18`; backend typecheck passes. GitNexus impact calls for the touched Judge symbols returned `Transport closed`. Manual caller review places the change in `createCampaignPlayJudge().judge` and the player-action interpretation process; storage, Game Master commands, Rulebook mutation, and UI contracts are unchanged. Humanizer/deslop review found the instruction direct, mechanical, and free of prose imitation or repeated grading language. The next proof must use a fresh pristine clone; the frozen r09 campaign remains diagnostic only.

## r10 ambient-contact live proof

Fresh r10 materialized the accepted Lowwater Ledger template with zero characters and zero turns, then reconstructed Lenna through the normal player endpoint. No world-generation or character-generation call ran. Opening Planner accepted one GLM 5.2 attempt in `288739 ms`; Narrator accepted one in `47306 ms`. The opening is a human `PASS`: Vassara warns that Thessha has stripped the ward's last illumination tokens, while empty medicine jars and unusable bandage scraps establish a separate clinic shortage. The prose does not gather every visible actor into the opening or make Lenna the cause of either problem.

Action 1 used the signed suggested inspection of stripped doorframes. Judge (`27545 ms`), Game Master (`12686 ms`), and Narrator (`51201 ms`) each accepted one attempt. The result found empty sockets, pale scoring, and no recoverable tokens without inventing a culprit or material reward. A full backend restart restored exact normalized public hash `bef4e46099cda02896acdd324be70ca5233135fcc3866b7b4afcb291dcbce885`.

Action 2 was a signed freeform call to the families behind the dark doors, explicitly not to Vassara or Thessha. Judge accepted one attempt in `31870 ms`. The ruling preserved `contact`, used the exact current `Debt-Ward Tenements` location as its only target, cited the location and prior observations, and stated that the words were delivered while willingness to reply remained with Game Master. No ambient actor handle was created and no named visible actor was substituted. Game Master accepted one attempt in `48976 ms`; Narrator accepted one in `36715 ms`. The households did not answer. The rendered silence is plausible and authority-safe, though a second corridor action with little recovery information is a formative pacing advisory rather than a dramatic success.

Both actions are bound to their pre-action projection hashes. SQLite integrity is `ok`, foreign-key check returns no rows, and post-restart backend logs contain no warning or error. The ambient-contact repair is therefore live-proven without Resume, retry, repair, fallback, provider switch, model switch, compatibility behavior, or backend-authored response. r10 remains the active long-play lane. Humanizer/deslop review separates exact contract evidence from the taste judgment and does not promote two actions into long-play acceptance.

## r10 continuation, evidence stop, and r12 replacement

r10 reached eight correctly signed and bound player actions. Lenna established the lease-count stakes, offered repair help without demanding entry, followed Vassara's lead to the Tide Shrine, and accepted Noro's privacy boundary. Noro did not surrender the concealed cable glow; she offered household lamp work, a two-tide deadline, and a consent rule for the shelter doors. The sequence remained spatially coherent and kept the ward cast out of the shrine. Prose advisories are `Thesssha`, one redundant handoff line, and the slow pace of two consecutive unanswered door contacts.

The ninth game turn completed but cannot enter formal evidence. The operator signed the visible letter `d` rather than the public opaque handle `choice_e5169b96dbe3986713aa612f`. The chosen label matched the durable request, but the evidence contract correctly rejected the handle mismatch. The pending decision was not rewritten after the fact, the database was not rolled back, and r10 is frozen at eight eligible actions.

r11 reused the same accepted template and player-authored Lenna draft. Its single open-choice Opening Planner attempt ended after `230362 ms` with `model_contract_invalid`; the native object used an exposure trigger outside the allowed enum. Text fallback was disabled. No retry or Resume ran, so r11 remains frozen before a completed opening.

r12 reused the same template and draft with explicit `Outsider / Just arrived / Following a lead` conditions. Opening Planner accepted attempt 1 in `207269 ms`, and Narrator accepted attempt 1 in `52888 ms`. The opening made Vassara's seized illumination tokens the immediate debt problem while the empty medical shelf remained a separate pressure. Action 1 asked Thessha for the seizure rule. Judge (`22179 ms`), Game Master (`44311 ms`), and Narrator (`68202 ms`) accepted attempt 1. Thessha disclosed the three-cycle threshold and Warden quota authority, acknowledged the harm, and withheld the remaining household count. The response supplies a rule and a next question without making her an instant ally.

Action 1 is bound to opaque handle `choice_97ca1262968b12782e197a2d` and pre-action projection hash `58ab48011a1e295e35602cf0fccc7bfb41f0e537bdb0bbdad0cc7e097441dbe9`. A full backend restart restored exact normalized public hash `f0f34f31d4371ef657c245a13983766df568775ca0361becdfb4f6bf7c3dca00`. r12 is the active lane. Humanizer/deslop review kept model failure, operator error, prose judgment, and recovery proof separate; this is not a long-play promotion claim.

## r12 actions 2-6 and destination-contact repair

r12 completed four more eligible manual actions. The first two were readable but weak: Thessha repeated quota advice, and the salve inspection reused opening material without creating a new decision. The next two actions made the clinic shortage concrete. Vassara identified a missing storm-supply bundle, Winch-Shaft Station, Remane's inability to dive, and the possibility of trading lamp work for help. Action 5 is a human prose `PASS`; it supplies causal specificity and leverage while leaving the player to decide how to act.

The UI did not offer Winch-Shaft among its contextual moves after Vassara named it. The operator therefore signed a freeform sixth action: travel there with mending tools and ask the station operators where the bundle had been stopped. Judge made one GLM 5.2 call in `91840 ms`. The object passed native schema validation but failed semantic compilation before Game Master or mutation. The action is unbound, r12 is frozen at five eligible actions, and Resume was not used.

The failure exposed a typed authority gap. The source packet contained the visible route and its exact destination, and the source moment established unnamed shaft operators. Judge's frame retained the route handle but not its destination binding. Its contact compiler could therefore authorize an established unnamed recipient only at the current location, never at the destination of compound travel.

Judge now carries a bounded `visibleRoutes` mapping whose route and destination handles must both exist in visible facts with the correct kinds. An ambient contact without travel must target the current location; one with `movementRouteHandle` must target that route's exact destination. A named visible nonplayer actor remains valid. The instruction forbids inventing an actor or treating delivery as evidence of identity, trust, knowledge, compliance, or reply.

Focused Judge/runtime tests pass `55/55`, default application-factory tests pass `5/5`, and backend typecheck passes. The older seeded replay suite interrupts in its fixture Opening Planner before Judge runs, with `model_contract_invalid` and no accepted artifact, so it is classified as unrelated fixture drift rather than evidence about this repair. GitNexus reports LOW impact for the changed Judge prompt/compiler. Its CRITICAL rating for the monolithic runtime factory resolves to two d=1 callers, with no runtime signature change; the schema symbol is also duplicated by the index as `Const` and `Function`. Humanizer/deslop review kept the addition direct, mechanical, and separate from player-visible prose. No retry, fallback, compatibility path, provider switch, backend-authored response, or standalone smoke test was added. Product proof requires a new pristine clone rather than the frozen r12 campaign.

## r13 destination-contact live proof

Clean r13 reused the accepted Lowwater Ledger template and reconstructed Lenna through the normal player endpoint. No world-generation or character-generation call ran. Opening Planner accepted one Z.AI Coding Plan `glm-5.2` attempt in `197914 ms`, and Narrator accepted one in `49246 ms`. The opening grounds Lenna's debt in Vassara's lack of tokens and Thessha's nearby seizure work, while leaving both station and shrine routes optional. Its content is a human `PASS`; the UI's exact duplicate rendering of the Narrator paragraph remains a presentation defect.

The first signed freeform action asked Vassara whether Winch-Shaft had tokens to trade and who handled them. Vassara named Dessek and his people, explained that the shaft keeps its own count, and warned that it does not trade freely. Judge, Game Master, and Narrator each accepted one native attempt. The answer gives the player a grounded contact and resistance without manufacturing inventory, agreement, or a quest solution.

The second signed action travelled to Winch-Shaft with Lenna's mending tools and called to the shaft crew with a repair-for-tokens offer. Judge accepted one attempt in `40818 ms`. Its ruling preserves `contact`, targets the exact Winch-Shaft location plus the existing tools, and carries the visible route handle. The bargain is uncertain rather than free; the roll produced a setback.

Game Master accepted one attempt in `34124 ms` and authored the chronological batch: advance sixty minutes, move Lenna through the canonical route, then record the destination scene. Rulebook applied both mutating receipts. Narrator accepted one attempt in `96340 ms`. The rendered Play UI places Lenna and Dessek together at Winch-Shaft Station, preserves all three possessions, lets the crew ignore the offer, and exposes Dessek's freshly tightened rope barricade. Remote actor work by Niruko and Thessha does not leak into the scene.

This is a live `PASS` for the repaired representation and a mixed prose verdict. The cold reception is plausible, leaves a direct Dessek follow-up, and makes the independent blockade matter. Narrator's `Workers glance your way, looks flat` is a grammatical error, and each beat still renders twice. Taste scores are comprehension `4/5`, prose readability `3/5`, agency `4/5`, world aliveness `4/5`, and desire to continue `4/5`.

All eight model stages across Opening and the two actions used GLM 5.2 strict native objects on attempt 1 with zero error, retry, repair, fallback, provider switch, or model switch. SQLite integrity is `ok`, foreign-key check is empty, and browser console warning/error capture is empty. A full backend restart and UI reload preserved normalized public-state hash `4e36f37be8248a739602d4294cea9b12e9e9669101328d15c50f84f10eac8180`.

The destination-contact repair is live-proven, and r13 remains active at two signed actions for continued long-play. Post-commit GitNexus rebuild preserved the requested `13565` embeddings but failed on GitNexus's duplicate `CodeEmbedding` primary key for `ownActionsFromRows`; successful pre-commit impact and staged change detection remain the available graph evidence. Humanizer/deslop review keeps contract proof, world behavior, prose defects, and presentation defects separate. No parser, backend-authored prose, compatibility path, retry, fallback, provider switch, or standalone smoke test was added.

## r13 actions 3-8 and frozen-lane checkpoint

Five more actions were submitted manually. Dessek claimed the freshly tightened barricade as deliberate leverage for the lower wards. His first answer is too complete and reiterates the whole blockade premise, but he resisted Lenna's attempt to treat his own tokens as available inventory. When Lenna separated household lamp work from Compact access, he left each family free to decide whether to trade.

The signed approach to the camp produced a bounded, useful success. One woman presented an existing cracked lamp, Lenna repaired it with her established tools, and one illumination token entered persisted inventory. Other households watched but held their few tokens back. The scene gives the player's craft mechanical meaning without converting the camp into unlimited work or undermining the blockade.

Lenna returned to Debt-Ward with the token. Canonical placement, cast, and inventory were correct, but Narrator ignored the requested `look for Vassara` clause and contextual choices omitted visibly present Vassara. The next signed freeform action found her directly, transferred the exact token, and asked whether the debt was cleared. Rulebook removed the token from player inventory. Vassara treated it as partial payment, refused to clear the full debt, and did not invent a numeric remainder.

The seven completed actions form a coherent short play arc: a general trade offer failed, a political blockade acquired local motives, a household-scale repair succeeded, and its single reward changed the opening debt without solving it. Human interest remains `PASS`. Ongoing advisories are duplicate Narrator rendering, `Workers glance your way, looks flat`, Dessek's over-complete exposition, and the weak Vassara handoff after return.

Action 8 was the signed suggested observation `Examine the corridor bench and leather satchel` with exact opaque handle `choice_bb69b1e4f13c42334845d0b4`. Its sole GLM 5.2 Judge response completed in `25265 ms` but failed native schema validation. The interrupted-stage contract stores no rejected artifact, so the invalid field is unavailable. Game Master did not run, world version remained `23`, and no action-8 evidence was bound.

r13 is frozen at seven eligible actions without Resume. The dynamic suggested-action schema already fixes kind, ordered targets, and movement route; without the rejected object, another contract change would be speculative. Continue from a fresh pristine clone rather than retrying, repairing, rolling back, switching provider/model, or adding fallback. Humanizer/deslop review keeps the successful short arc, prose advisories, and unavailable model evidence separate.

## r14 actions 1-10 and first-sitting checkpoint

Fresh r14 reused the accepted Lowwater Ledger template and reconstructed Lenna through the normal player endpoint. No world-generation or character-generation model call ran. Explicit `Outsider / Just arrived / Following a lead` conditions produced a strong Opening choice: Vassara had two tokens available for Lenna's payment, but spending them on the debt would deprive three ward families of sulfur-lung salves. Lenna left both tokens with Vassara and asked for work that would preserve the debt. The world accepted the cost without inventing compensation and named Dessek only as a possible lead.

The next three actions exposed a content weakness. Thessha said ten households were overdue and claimed to name the affected rows, but the visible consequence and Narrator rendered no row identifiers. A signed attempt to trace those named rows again produced only crude markers and the number ten. A shuttered household plausibly remained silent when Lenna knocked, but the two-turn writ thread did not deliver the actionable information its own prose asserted was available.

Lenna travelled to Winch-Shaft Station and asked Dessek about crew wages. Dessek refused the premise: the shaft was shut, no crews were earning wages, and the rope line existed to resist halved quotas and collectors. His answer is somewhat over-complete, but it preserves a local political conflict instead of turning every lead into a job. Lenna then chose to hear the Wardens' side rather than immediately join or break the line. Canonical routes moved her to Warden Tollhouse with all three possessions intact.

The Tollhouse sequence is a human content `PASS`. Fresh drag-marks, chipped brine, and blue-green core fibers existed before Lenna arrived. Ostane first gave a practiced answer about storm crust, guarded the scored bundle with her body, and asked whether Lenna meant the shards or what lay beneath. When pressed conversationally, she disclosed light-taking threads that brighten after storms but still withheld access. Lenna then answered Ostane's provenance question with played history—Dessek's closed shaft and her lamp-glass trade—and asked whether the phenomenon threatened the bridge or could supply light. Ostane remembered Dessek's line, distinguished easing from trust, admitted the core now had give, and explained why a Compact inspection would cost a season's toll. She had chipped the crust for her own reasons before Lenna arrived and remained unwilling to surrender the evidence. This supplies resistance, subtext, material stakes, and a world problem independent of the player.

The first-sitting verdict is `CONTINUE_LANE_AFTER_BREAK`, not a sixty-action or long-play promotion. Taste scores are comprehension `4/5`, prose readability `4/5`, agency `4/5`, world aliveness `4/5`, and desire to continue `4/5`. The missing writ identifiers are the main content defect. Exact duplicate rendering of every Narrator beat remains the dominant presentation defect. One generated option, `Ask Ostane Grafton about Ostane's plans for the chipped fibers`, repeats the name awkwardly, and some Thessha/Dessek answers still read more like complete setting delivery than conversation.

All 35 persisted model stages used Z.AI Coding Plan `glm-5.2`, strict native objects, and attempt 1. Every stage was accepted with a valid schema; there were zero stage errors, retries, repairs, fallbacks, provider switches, or model switches. Aggregate persisted model duration was `1543986 ms`, so latency remains a serious playability risk even when output quality is good.

The database contains one completed Opening and ten completed player actions. SQLite integrity is `ok` and foreign-key check returns no rows. The action-10 restart boundary preserved exact public-state hash `acd818d9cc81411d3b0df3966a65b6267c71c20dfe7d6c6b0d2d62ee768d47a4`. After a full backend restart and actual browser reload, the Play UI restored Warden Tollhouse, Ostane, Lenna's three possessions, the latest exchange, and the same four next actions. Browser diagnostics contain only development info/log messages and no warning or error; restart stderr is empty.

Humanizer/deslop review keeps stored technical proof, observed content gaps, presentation defects, and subjective taste judgments separate. r14 is the active replacement lane at ten eligible actions. No standalone smoke suite, compatibility path, backend-authored prose, retry, fallback, or provider switch was added.

## r14 actions 11-13 and Game Master summary-bound repair

Action 11 used the signed suggested inspection of the exposed cable threads. The result stayed within sensory evidence: blue-green filaments crossed the structural lay at irregular angles, displaced strands, and left the core visibly thinner. It preserved unknown cause and provenance. Ostane had guarded the same bundle one turn earlier, but the observation gave no explicit reaction or permission from her. The evidence is useful; the immediate interaction continuity is `REVISE`.

The next signed freeform action stepped away without touching the cable, reported the thinning, and asked what Ostane intended to do before the next gale. Ostane recognized the retreat and stopped guarding the bundle. She grounded her plan in twice-daily crossings, the growing glow, and the economic cost of a closed span. She asked Lenna for craft evidence rather than handing over the problem or adopting the player as a trusted confidant. This is a human content `PASS`: Ostane keeps authorship, caution, and local stakes while Lenna's established trade creates a plausible contribution.

Action 13 selected `Try to test the core filaments with mending tools` using opaque handle `choice_5d1c7c887e693e12d634a3ac`. Judge accepted one GLM 5.2 strict-object attempt in `31689 ms`, preserved the selected attempt and existing tool authority, and rolled a strong success. Game Master made one GLM 5.2 call in `57955 ms`, but its first effect summary exceeded the existing 1200-character schema maximum. The stage persisted `model_contract_invalid` with no accepted artifact. Game Master never reached Rulebook, world version stayed at the action's base version `26`, action 13 is unbound, and r14 is frozen at 12 eligible actions without Resume.

The 1200-character maximum remains the typed event contract; it is large enough for a concrete public consequence and does not need expansion. The native JSON schema already carried `maxLength`, but the Game Master prompt did not state it. The prompt now names the existing 1200-character event/possession limit and the shorter condition/relation/goal limit. It asks for only the committed result and excludes planning, reasoning, and repeated support. Backend still does not truncate or rewrite model prose, and no retry, repair, text fallback, provider switch, or compatibility path was added.

GitNexus reports LOW impact for the private `prompt()` function. Its only d=1 caller is Game Master's `plan()`. Focused Game Master verification passes `26/26`, and backend typecheck passes. Humanizer/deslop review kept the instruction mechanical and specific; it does not prescribe finished narrative style. A fresh pristine clone must prove the repair. r14 remains diagnostic and will not resume.

## r15 actions 1-10 and qualitative checkpoint

Fresh r15 reused the accepted Lowwater Ledger template and reconstructed Lenna through the normal player endpoint. No world-generation or character-generation model call ran. Explicit `Outsider / Just arrived / Following a lead` conditions opened on Vassara's empty token purse, sulfur-lung salves, and Thessha's fresh seizure writs. The first four actions established that ten households faced token locks, frozen trade and credit, and eventual eviction. Thessha supplied rules and limits without becoming an instant ally.

Lenna then inspected and repaired exactly six cracked corridor lamp panels with her existing tools. No reward appeared. When she asked for ledger credit, Thessha explained that corridor work belongs to the ward's lumens-lease and counts only against a stamped work order. The duty clerk owns that process and does not answer to Thessha. Six repaired panels amount to roughly half a day's token rate while one household owes weeks. The sequence preserves the value of Lenna's craft without letting volunteer labor erase institutional debt.

Actions 9 and 10 expose a repeatable content failure. Lenna read the corridor marker and six panel numbers, and the result claimed she retained them all. The following freeform action had her recite each identifier to Thessha, who verified all six. Neither turn exposed a corridor designation or panel number. The world therefore narrates possession and verification of exact facts without committing those facts to visible play. This matches the earlier writ sequence in which named household rows never received identifiers.

Presentation also remains below acceptance. A visual browser pass shows one Narrator paragraph; the apparent second copy in the DOM snapshot is the intentional `sr-only` live region, so earlier duplicate-rendering observations do not describe the sighted UI. The real layout defect is the sticky `Your move` dock covering the middle of the NarrationDock at a 1270 by 720 viewport. `Thesssha` appears in visible prose even though the canonical actor is `Thessha`, and Vassara disappeared from the visible cast during the long repair without an observable departure beat.

The human verdict is `CONTINUE_AFTER_REPAIR`, not long-play acceptance. Scores are comprehension `4/5`, prose readability `3/5`, agency `4/5`, world aliveness `3/5`, and desire to continue `3/5`. The strongest result is the bounded refusal to turn useful unpaid work into easy credit. The weakest is the consecutive claim that exact identifiers were read and verified while all values remain offscreen.

All 34 persisted stages used Z.AI Coding Plan `glm-5.2`, strict native objects, and attempt 1: one Opening Planner, eleven Narrator calls, ten Judge calls, ten Game Master calls, and two Actor Replanner calls. Every stage was accepted with a valid schema. This fresh lane live-proves the Game Master summary-bound repair without retry, repair, fallback, provider switch, or model switch.

The database contains one completed Opening and ten completed player actions. SQLite integrity is `ok`, and foreign-key check returns no rows. A full backend restart and actual browser reload preserved exact public-state hash `eba110f62b882936a2e06267767fc22a3332ca0bbd69ea59f9cd17841e4bd180`. Browser console, network-error capture, and restart stderr are empty. No standalone smoke suite or compatibility path was added. The next implementation slice should stop the action dock from obscuring narration and require newly observed identifiers to become explicit, durable world facts before r15 continues. Humanizer/deslop review kept mechanical proof, visible defects, and taste scores separate.

## r15 layout and concrete-fact repair

The presentation repair stays inside the existing Campaign Play layout. NarrationDock now starts at the top of its desktop grid track and receives a viewport-bounded height with its own vertical overflow; the narrow single-column layout removes that desktop cap. On the live r15 page at `1280x720`, narration measures `y=100..424` and the fixed action dock starts at `y=434.6`. The prose no longer sits beneath the choices, and both choice and freeform controls remain available.

Stored action-9 and action-10 artifacts locate the content defect before Narrator. Judge authorized reading the visible markings. Game Master then committed summaries saying that all six values were read, remembered, repeated, and verified without supplying any value. Narrator received no identifiers to preserve. Game Master's model contract now requires a newly revealed, recorded, communicated, or verified name, marking, code, number, date, quantity, direction, or instruction to be materialized in the committed summary. It also requires exact reuse when an established value from the visible packet matters again. The application still does not invent, parse, truncate, or rewrite model prose.

GitNexus reports LOW impact for the private Game Master `prompt()` function; `plan()` is its only d=1 caller. The existing focused Game Master suite passes `26/26`. Humanizer/deslop review found the added instruction direct, mechanical, and free of finished narrative copy. No retry, repair, text fallback, provider switch, compatibility behavior, or standalone smoke test was introduced.

The first live action after restart asked Thessha to read the corridor designation and all six panel numbers aloud while Lenna copied each exact value. Judge accepted one GLM 5.2 strict-object attempt in `22662 ms`; Game Master accepted one in `49694 ms`; Narrator accepted one in `58018 ms`. Game Master committed corridor `W-4-9` and panels `091`, `092`, `093`, `094`, `095`, and `096`. The rendered consequence lists every value, and Narrator preserves the designation and range in readable scene prose. SQLite integrity is `ok`, foreign-key check is empty, and the Play tab has no console warning or error. Concrete-fact materialization is therefore a live `PASS`.

Two prose advisories remain. The generated name still appears as `Thesssha`, and Game Master's own summary changes `W-four-dash-nine` to `W-four-nine`. These are semantic consistency defects and are not hidden by backend substitutions. The action also cannot count as formal lane evidence: the operator submitted it through Play UI before staging the live-session decision, so there is no valid pre-action signature to bind. r15 remains at ten eligible actions, with this eleventh durable turn retained only as product proof. No evidence was backfilled, and no rollback or Resume was used.

## r16 actions 1-5 and first-sitting checkpoint

Fresh r16 reused the accepted Lowwater Ledger template with content provenance `fb2c7074e89df263b0bf308f0d47aadcd5f246b6832c8274506c8f1dc26855d9`. Lenna was reconstructed through the normal player endpoint. No world-generation or character-generation model call ran. The opening and five signed player actions completed through the rendered Play UI on Z.AI Coding Plan `glm-5.2`.

The opening placed Lenna with Vassara and Thessha in Debt-Ward. Vassara lacked medicine, carried debt pressure, and had three cracked lamp housings plus a split flue affecting nearby families. Lenna offered fair repair work without taking Vassara's medicine or last light. Vassara gave her a cracked sealed jar as a small, immediate task rather than turning the whole ward into a quest list.

The next four actions formed a compact craft sequence. Lenna inspected a clean diagonal hairline crack under old wax. Her first low-heat pass melted and smeared the wax without sealing the glass. She scraped the softened wax away, then tried again on the bare fracture and bonded the jar. The failed pass changed the object and required a sensible recovery step. The later success used the same established tools and did not create payment, trust, or a larger solution.

The world moved during the final repair. Vassara left by a canonical route for Winch-Shaft Station. Folded red-wax writs and a small locked iron box became visible on the landing near Thessha. The next choices let Lenna inspect the box, continue repairing the east-walk housings, follow Vassara, or ask Thessha about the writs. I wanted another turn because those options point toward different concerns instead of restating the jar task.

The human verdict is `CONTINUE_LANE`. Scores are comprehension `4/5`, prose readability `4/5`, agency `4/5`, world aliveness `4/5`, and desire to continue `4/5`. The sequence is clear and materially consistent. Its present limitation is scale: four actions stayed tightly focused on one jar, and Vassara did not visibly react to the failed or successful repair before leaving. The next sitting needs to show whether her movement matters and whether the new official trace develops into a broader world thread.

All 18 persisted model stages used provider `zai-coding-plan`, model `glm-5.2`, strict native objects, and attempt 1. Every stage was accepted. No retry, repair, fallback, provider switch, or model switch ran. The slowest stage was the action-5 Actor Replanner at `101235 ms`; opening planning took `237516 ms`, so latency remains a playability concern.

SQLite integrity is `ok`, foreign-key check returns no rows, and runtime event sequences are contiguous from `1` through `212`. A full backend restart after action 1 preserved exact normalized public-state hash `87b96ba3421e1c25d9e47401effdaa4439c62f31f7157e708a0bd634a62c2ed1`. Browser warning and error capture is empty. Evidence lives under `output/playtests/campaign-play/pristine-60-glm52-lowwater-ledger-fb2c7074-r16.session`; the action-5 screenshot is `output/playtests/campaign-play/pristine-60-glm52-lowwater-ledger-fb2c7074-r16.session/screenshots/action-5.png`.

r16 is in progress at five of sixty eligible player actions. This checkpoint does not promote the lane. The memory carried into the next sitting, recorded before rereading, is: Lenna repaired Vassara's jar after one failed pass; Vassara then left for Winch-Shaft; red-wax papers and an iron box appeared near Thessha. Humanizer/deslop review kept the first-person play judgment, concrete defects, and technical proof separate.

## r16 actions 6-10 and restart checkpoint

Actions 6 and 7 turned the red-wax papers into usable local facts. Thessha described the seizure threshold, ten exposed families, and the quota pressure that also keeps her own household lit. The first answer runs a little long, but it gives her an institutional duty and a personal stake. The follow-up is sharper: Remane Scoth is on the next landing, four cycles behind, and his writ already has a cracked seal.

Lenna followed Thessha upstairs. Remane did not appear on demand. Thessha knocked twice, slid the writ under the closed door, and continued her route. Lenna then identified herself through the door, cited the repaired jar, and offered help without asking Remane to open. The household stayed silent while Thessha's footsteps faded. This is a human content `PASS`: refusal remains possible, the official keeps moving without the player, and the scene does not turn an unseen resident into a quest dispenser.

Action 10 took the established route to Winch-Shaft Station. World time advanced from `Day 1, 00:44` to `Day 1, 01:14`. The station already carried visible aftermath from `00:34`: a fresh rope barricade and charcoal tallies. Vassara had reached the station independently, Dessek was present, and distant voices mentioned `chimney` and `sulfur` without explaining the whole problem. Prior action history survived the location change, but the rendered scene only exposed actors and evidence that belonged at the destination.

The ten-action verdict is `CONTINUE_LANE`. Scores are comprehension `4/5`, prose readability `4/5`, agency `4/5`, world aliveness `5/5`, and desire to continue `4/5`. Actions 7 through 10 are the strongest part of r16 so far. They use silence, travel time, prior local aftermath, and NPC movement to make the world feel active without forcing a dramatic event into every turn. The main prose advisory is Thessha's action-6 answer, which explains most of the seizure system in one exchange.

The database contains one completed Opening and ten completed player actions. All 33 persisted model stages used provider `zai-coding-plan`, model `glm-5.2`, strict native objects, and attempt 1. Every stage was accepted. No retry, repair, fallback, provider switch, model switch, interrupted turn, or Resume path ran. SQLite integrity is `ok`, foreign-key check returns no rows, and runtime event sequences are contiguous from `1` through `365` with no revision break.

The action-10 backend restart preserved normalized public-state hash `c6c961db6efd5037cc49c30320b71f05fb0a679640eba190ab47c0bca4dca521`. An actual browser reload restored Winch-Shaft Station, Vassara, Dessek, Lenna's three possessions, the `00:34` aftermath, the `01:14` arrival, and the same next actions. Browser console and network-error captures are empty. r16 remains active at ten of sixty eligible actions. Humanizer/deslop review kept observed play, stored proof, taste judgment, and the one prose advisory separate.

## r16 actions 11-20 and restart checkpoint

Lenna first asked Dessek about the chimney and sulfur exchange. His answer connected the split flue, the sick families, and the blockade, but it delivered too much of the local problem in one speech. Vassara's follow-up was stronger. She separated ordinary coughs from lung-swell, named the affected households as her evidence, and said that sealing the seam could stop contaminated air without curing anyone already sick. Both answers remain dense, but Vassara speaks from her own work instead of summarizing the setting.

Actions 13 through 17 formed the best craft sequence in the lane so far. The crate contained two oiled cloth rolls coated in a clay-like compound, punched iron straps, a short-handled trowel, and wool padding. Lenna softened the compound and sorted the straps before touching the flue. Vassara then led her back to Debt-Ward by a canonical route. The seam was arm-length, at most finger-width, with a downward draft, sound surrounding stone, soot above, sulfur residue below, and no existing anchor points. The first repair attempt failed because the draft cut channels through the wet compound and pulled it into a pool. Lenna stopped before wasting the second roll. A strap fit across sound material, but her lamp-glass tools contained no driver, punch, bolt, or masonry fastener. The game preserved the remaining materials and the open seam.

The last three actions kept the missing fastener as a world problem instead of spawning a solution. Thessha carried writs and a token-lock, not repair hardware. She guessed that hoist-crew stock might remain at the shaft station, then resumed serving writs. Lenna travelled there and found only Dessek, the existing rope barricade, tallies, and machinery. No fasteners were visible on arrival. Dessek identified a maintenance bench behind the cable drums and allowed Lenna to take only what would help the affected rooms. His own limit was explicit: do not strip the machinery and give the Wardens grounds to accuse the occupying families of dismantling the station.

The twenty-action verdict is `CONTINUE_LANE`. Scores are comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `4/5`, and desire to continue `5/5`. The material logic is unusually strong. Preparation matters, failure consumes part of a resource, the missing tool remains missing, travel costs time, and NPCs keep their own jobs and political constraints. Dessek's action-11 explanation is still too complete, and Vassara's answer is close to the same line. Multi-minute turn latency remains a serious playability risk. Those defects do not erase the later sequence's concrete causality or my desire to inspect the bench and finish the repair.

The database contains one completed Opening and twenty completed player actions. All 67 persisted model stages used provider `zai-coding-plan`, model `glm-5.2`, strict native objects, and attempt 1: one Opening Planner, 21 Narrator calls, 20 Judge calls, 20 Game Master calls, and five Actor Replanner calls. Every stage was accepted with a valid schema. No retry, repair, fallback, provider switch, model switch, interrupted turn, or Resume path ran. All 15 actor proposals were accepted.

SQLite integrity is `ok`, foreign-key check returns no rows, and runtime event sequences are contiguous from `1` through `734`. Every event advances runtime revision by exactly one. The action-20 backend restart preserved normalized public-state hash `47286139ef5dbd59acee522427c8f378283fb515f1582c5550c74e8ed31e81d9`. The actual browser reload restored Winch-Shaft Station, Dessek, Lenna's three possessions, the maintenance-bench exchange, and the same four next actions. Browser warning and error capture is empty.

GitNexus also received a clean full rebuild after its incremental FTS index failed. The refreshed graph is current at HEAD `8d33525`, with `13813` nodes, `36834` edges, `790` clusters, and `300` execution flows. Query, exact symbol context, process resources, and status all work. r16 remains active at twenty of sixty eligible actions. Humanizer/deslop review preserved the concrete play record, the two prose advisories, the latency risk, and the first-person taste verdict without turning this checkpoint into long-play acceptance.

## r16 actions 21-30 and Judge suggested-target repair

Actions 21 through 28 completed the repair through explicit material state. Lenna found three masonry pins and a driver, obtained Dessek's bounded permission to take them without stripping machinery, test-fit the hardware, packed the established materials, returned by a canonical route, installed one strap, and then finished the remaining two. Pin inventory moved from three to two to zero. The first strap produced only a partial seal; the last two stopped the active draft while preserving a residual sulfur smell. The game neither invented tools nor treated preparation, travel, or partial work as a finished repair.

The last two actions turned that material result into a social decision. Thessha recognized that the casing was sealed and that fewer households would cough, but did not convert uncommissioned work into debt credit. She continued her own writ route. When Lenna asked who faced seizure, Thessha named Remane Scoth, the Dael household, and Marena Belkoss's household, explained their distinct circumstances, and established the week-end collection deadline. The next move can follow the households, seek a delay, visit the shrine, or leave. Human verdict remains `CONTINUE_LANE`: comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `5/5`.

Action 30 first stopped before artifact acceptance because Judge's native schema represented the frozen one-target suggested choice as a tuple while GLM 5.2 returned `targets` in a string shape. Zod reported `expected tuple, received string`. The stopped turn never mutated world state. Resume was not used. With the backend offline, the isolated interrupted turn was backed up and removed in one database transaction; only the three relevant immutability triggers were temporarily dropped and recreated verbatim inside that transaction. Integrity, foreign keys, trigger presence, runtime revision, and the exact pre-turn state were checked afterward.

Judge now exposes frozen suggested targets as a standard exact-length array schema and explicitly requires a JSON array, including the one-target case. Runtime compile checks still require the exact ordered frozen handles. GitNexus reports LOW upstream impact for `judgeProposalSchemaForFrame` and Judge's private `prompt()`, confined to the Judge execution flows. Focused Judge tests pass `20/20`, including JSON-schema shape and runtime acceptance/rejection, and backend typecheck passes. Humanizer/deslop review found the prompt addition direct and mechanical; it does not coach narrative voice or add backend prose.

After restart, the same pre-signed action completed through the rendered Play UI on the first GLM 5.2 attempt and was bound to its original decision. Across the opening and 30 player actions, all 100 persisted model stages are accepted attempt 1: one Opening Planner, 31 Narrator, 30 Judge, 30 Game Master, and eight Actor Replanner stages. Nineteen actor proposals were accepted. Commands and receipts are one-to-one, event sequences are contiguous from `1` through `1049`, revisions have no breaks, actor placements have no duplicate `present` rows, SQLite integrity is `ok`, and foreign-key check is empty. No retry, repair, fallback, provider switch, model switch, compatibility path, standalone smoke test, or Resume ran.

The player-caused chain remains legible from bench inspection through permission, acquisition, fitting, travel, partial installation, completed seal, Thessha's recognition, and the new seizure decision. Independent world motion also continues: Remane sought Noro, Niruko traced organism growth, and Noro moved while Remane scheduled a harvest; those developments correctly remained hidden from Lenna. Earlier independent movement by Vassara to the station did become visible and caused Lenna's repair-material decision. This is evidence of both causal lanes, not a claim that every hidden event has already affected player choice.

Actions 21 through 30 took a median `122393 ms`; the slowest took `291462 ms`. That latency is the checkpoint's serious playability defect. The action-30 prose is specific and meaningful, but its em-dash-heavy negative list is a style advisory rather than a semantic failure. A full backend restart and actual browser reload preserved exact public-state hash `65d93e85f0d7c66f89032ea25602aea606507ffea8ff3bd4b6e64751fe64035b`, with empty browser warning/error capture and empty restart stderr. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-lowwater-ledger-fb2c7074-r16.session/screenshots/action-30.png`, SHA-256 `79ed6142b107d9bb2bb88a13519832a92d9bfa1dca952dd7cc947e7dc0a75b4e`. r16 remains active at thirty of sixty eligible player actions.

## r16 actions 31-40 and signed-petition state failure

Actions 31 through 33 followed the seizure deadline into the Compact's petition process. Action 31 mostly repeated Thessha's refusal to grant a delay. The next two answers added useful facts: Warden Marenthos Valieri owned the quota order, a petition needed a named sponsor and reason, and the clerk's bench sat in Compact Hall. Thessha also described the route through Winch-Shaft Station. Her answers stayed grounded in her office, but the later responses were dense and repeated much of the same procedure.

Action 34 moved Lenna to Winch-Shaft Station through a canonical route and a `move_actor` receipt. Action 35 then exposed a weaker boundary. The visible choice said `Try to trace the junction passage upward`; Game Master recorded only a scene event, while the consequence and Narrator walked Lenna up three switchbacks to the lamplit Compact Hall tier. Her Rulebook placement remained Winch-Shaft Station, and the route panel still had no direct Hall route. The database contains Compact Hall, but its actual path is Winch-Shaft Station to Warden Tollhouse to Compact Hall. Because the chosen action asked to trace rather than complete travel, this was a strong route and narration `REVISE`, not yet the terminal failure.

Actions 36 through 38 recovered through that real graph. Lenna travelled to Warden Tollhouse, where Valieri was already investigating scraped cable clamps and blue-green residue while Ostane left for the station. The encounter felt like a living world because Lenna found her target during his own work instead of at a waiting quest marker. Valieri resisted the petition but delivered too much of the same seizure procedure already supplied by Thessha. Lenna then reached Compact Hall through its one-unit route. The promised clerk was absent; the empty bench held blank forms, ink, and a stylus.

Action 39 acquired one blank filing form through `adjust_actor_possession`, and the Play UI added it to `Carrying`. Action 40 used the visible ink and stylus to write a dated petition, signed `Lenna Vey`, asking to suspend the ten week-end seizure writs until a hearing. Game Master emitted `advance_world_time` and `record_world_event`. The event named the possession and stated that the petition was complete, signed, unstamped, and unsubmitted, but no possession command consumed or transformed the blank form. Narrator repeated the completed-petition claim and offered `Try to present the completed petition for stamping`; Rulebook truth and `Carrying` still said `blank filing form x1` at world version `70`.

Disposition is `REJECT_R16_AT_ACTION_40`. This is a hard player-visible state contradiction, so r16 stops at forty signed and bound player actions. It will not be resumed, repaired in place, or used for a sixty-action claim. The human scores for this sitting are comprehension `5/5`, prose readability `3/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `4/5`. I wanted to file the petition and see Valieri's response, but repeated institutional exposition, stale repair suggestions early in the sitting, and multi-minute turns kept the prose score down. The hard stop comes from the possession mismatch, not from taste.

The ten actions took `2078429 ms` of turn wall time, with median `214740 ms` and maximum `283323 ms`. All 35 model stages used `zai-coding-plan`, `glm-5.2`, strict native objects, and attempt 1. Every stage was accepted with no retry, repair, fallback, provider switch, model switch, interruption, or Resume. Eleven actor proposals were accepted. The ten turns contain 34 commands and 34 receipts with no gap.

The campaign contains one completed Opening and forty completed player actions. All 135 model stages are accepted attempt 1, and all 30 actor proposals are accepted. World version is `71`, runtime revision is `1450`, world time is minute `287`, runtime sequences are contiguous from `1` through `1450`, SQLite integrity is `ok`, and foreign-key check returns no rows. A full backend restart and actual browser reload preserved exact public-state hash `56ce756dae00a22b386408c6fecc2f08cb0122fd1cc25ab33243232a3b0b12ea`. The reloaded page still showed completed-petition prose beside `blank filing form x1`, proving that the contradiction is durable rather than cached UI state. Restart stderr is empty.

Humanizer/deslop review removed announcement language and repeated conclusions. The final record keeps the spatial advisory, the prose judgment, and the terminal possession failure separate. It does not turn clean technical invariants into a gameplay pass.

### Signed-petition possession repair

The accepted action-40 artifacts locate the defect at the Game Master boundary. Judge targeted the visible blank-form possession and ruled the requested writing, date, and signature deterministic. Game Master described the completed petition accurately but had only `acquire` and `spend` possession operations, so no typed operation could represent the retained object's changed identity. Its `record_world_event` carried no write scope and left Rulebook truth unchanged.

The Game Master proposal now supports model-authored `transform` for a visible retained possession. Compilation consumes the requested source quantity with protected exposure and acquires the same quantity under the model's concrete result name with direct-perception exposure. Both commands remain in one preflighted, transactional Rulebook batch and receive separate receipts; only the result summary enters the public consequence stream. No prose parser, semantic regular expression, retry, repair, fallback, provider switch, compatibility path, or backend-authored item name was added.

GitNexus reports `LOW` impact for the proposal schema, compiler, and prompt. The only direct runtime consumer is Game Master's `plan` flow. Focused Game Master verification passes `26/26`, including accepted preflight for the paired source spend and result acquire, and backend typecheck passes. Humanizer/deslop review kept the new instruction as mechanical contract language. It states the ownership boundary once, names concrete state-changing verbs only to distinguish transformation from observation, and does not prescribe narrative voice or reusable prose.

### r17 clean product proof and new Lane A start

r17 materialized the accepted Lowwater Ledger template at the zero-character, zero-turn boundary. No world generation ran. Lenna was created through the rendered Character UI with one blank filing form, one small ink pot with writing stylus, and her compact repair kit. The normal arrival flow placed her in Debt-Ward Tenements. Vassara offered lamp-glass work from the households' own shortage, while the empty salve jar and bandages established a separate medical problem. The opening gave Lenna a useful craft hook without making her the cause or solution of the ward's wider conflicts.

The first signed freeform action used the owned form and writing tools to create a personal work ledger containing Vassara's name, the cracked doorway glass, and the fact that payment was not agreed. Judge targeted both visible possessions and accepted the mundane task deterministically. Game Master returned one `transform` effect. Compilation produced `advance_world_time`, a protected one-unit spend of `One blank filing form`, and a directly perceived one-unit acquire of `Work ledger, Day 1 - Debt-Ward Tenements`. All three commands received applied receipts. The ink pot and stylus remained at quantity one.

The rendered `Carrying` panel contains the work ledger, ink pot with stylus, and repair kit, with no blank form. Narrator preserved the exact useful entry and kept the scene moving with the corridor argument in the background. `You fold the ledger close against your person` is slightly stiff, but the paragraph is clear and causally correct. Suggested actions returned to the visible bandages, lamp glass, shrine route, and Thessha rather than repeating the completed writing action.

All five opening and action model stages used `zai-coding-plan` and `glm-5.2`, strict native objects, and attempt 1. Opening Planner took `304113 ms`; its Narrator took `106248 ms`. The action took `34952 ms` for Judge, `21122 ms` for Game Master, and `75175 ms` for Narrator. There was no retry, repair, fallback, provider switch, model switch, interruption, or Resume. SQLite integrity is `ok`, foreign-key check is empty, runtime sequences are contiguous from `1` through `92`, world version is `15`, runtime revision is `92`, and world time is minute `3`.

Backend restart and actual browser reload preserved public-state hash `e9bef56790a05f865f59ec367a1acda74c66f2d1be8ac09088d4872a2024161d`; restart stderr is empty. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-lowwater-ledger-fb2c7074-r17.session/screenshots/action-1.jpg`, SHA-256 `1657e363742dae82e1fe2904b7da8ba7562047ff1eac05bdf45cbcfb2e009eae`. Human scores are comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `4/5`, and desire to continue `5/5`. This proves the repair on the real product and starts r17 at one of sixty eligible actions; it is not a sixty-action acceptance claim.

Humanizer/deslop review kept the product observation, mechanical proof, latency, and provisional play judgment separate. The note does not turn a successful targeted repair into a claim that the whole lane has passed.

## r17 actions 2-7 and prose-only resident diagnosis

Actions 2 through 5 followed Vassara's lamp-work offer as ordinary play. She named the Dael household, a resident called Anni, one wrapped lamp jar, and four or five strips of saltfish as possible payment. Neither Anni nor the resident behind a second suitable doorway answered. Lenna then inspected visible fracture patterns from the corridor and learned a useful craft distinction: rim-origin fractures could be sealed and braced in place, while body splits required hands-on access. The observation was concrete, but it could not create the missing owner of consent.

The accepted actor roster contains no Anni or Dael household actor. Vassara's action-2 event therefore created a person-specific job in prose without a person who could later speak, grant permission, own payment, remember the exchange, or act under the scheduler. The existing ambient-contact contract behaved correctly by delivering Lenna's words and withholding a reply. The earlier job offer was the defect.

Action 6 returned to Vassara after the two unanswered contacts. She admitted that she could not authorize another household's lamp and had no sealed jar of her own. She said the tide-line workers were away and offered to pass word later. This was more honest than another silent door, but it still repeated the non-roster Anni and made future unnamed residents the only owners of the proposed work.

Game Master now receives `CANONICAL_PEOPLE`, the sorted person names from the accepted roster. The prompt treats this as an identity whitelist rather than disclosure authority. A person name outside the list is not an actor even when prior prose mentions it. The model must not offer knocking, calling, or waiting for that resident as the next playable step. If supplied facts establish no listed owner for the requested transaction, the targeted actor must say that no actionable offer exists. This adds no prose parser, result rewrite, retry, repair, fallback, provider switch, compatibility path, or backend-authored name.

Action 7 exercised the revised boundary inside the already contaminated r17 history. Vassara plainly said that she could not name anyone currently available and would not pretend that sure work existed. That part is a content `PASS`. She still repeated Anni from the prior source moment, and autonomous local aftermath exposed low voices mentioning a glass-repairer. Narrator then suggested knocking at a doorway showing activity. Because r17 already carried the invalid name before the fix, it cannot prove clean first-introduction behavior and no longer qualifies as a pristine lane.

The human verdict is `FREEZE_R17_AS_DIAGNOSTIC_AFTER_ACTION_7`. Comprehension is `5/5`, prose readability `4/5`, agency `3/5`, world aliveness `2/5`, and desire to continue is `3/5`. I would leave the household branch and try Thessha or the shrine, but three actions spent pursuing a job whose owner could never answer is too much friction for a sixty-action acceptance lane.

Actions 6 and 7 were signed before submission and bound after completion. All seven stages across those actions used `zai-coding-plan` `glm-5.2`, strict native objects, and attempt 1. Action 6 took `39865 ms` for Judge, `73419 ms` for Game Master, and `88333 ms` for Narrator. Action 7 took `34467 ms`, `68146 ms`, `96163 ms` for one Actor Replanner, and `63289 ms` for Narrator. SQLite integrity is `ok`, and foreign-key check is empty.

Focused Game Master verification passes `26/26`, and backend typecheck passes. Screenshot hashes are `cf7a599e14ffb74d3fc72b662675ba696945e387b0e10043535b3ffc18396ac4` for action 6 and `4d82d70fb2ad80f462e8ead7008babe1d44f490b4d2a467db38e3e0c0345a445` for action 7. Humanizer/deslop review kept the instruction mechanical and replaced the first weaker wording instead of stacking another rule. A fresh clean-world copy must test the first offer before this change receives product acceptance.

## r18 clean roster-bound offer proof

r18 materialized `lowwater-ledger-pristine-fb2c7074` at its zero-character and zero-turn boundary. No world generation ran. The live session is pinned to commit `bffafeb8`. Lenna was rebuilt through the rendered Character UI from a specific description; the generated draft preserved her ordinary trade, cautious temperament, lack of authority, and the same three starting possessions.

The explicit `Lower Wards / Outsider / Just arrived / Following a lead` opening completed on one strict GLM 5.2 attempt per stage. Vassara herself offered salves and a dry corner for repaired corridor panels. She did not create a household resident or promise payment owned by an absent person. Seven charcoal tallies and a diminishing row of herb bundles supplied a separate visible pressure without turning Lenna into its cause.

The signed first action asked Vassara to identify one panel she personally controlled or had authority to commission, define success, state exact payment, and name the person who would accept the work. Vassara selected the split housing over her own doorframe. She described tenant responsibility under the old lease, defined a successful seal through draft, damp, and token-burn behavior, offered one week-long salve packet per panel or one dry night per two panels, and kept acceptance authority with herself. Tools remained packed, and Lenna did not agree before inspection.

This is a clean product `PASS` for roster-bound actionable leads. The offer has one canonical owner, one visible object, observable acceptance conditions, bounded payment, and a next decision that does not require an actor to emerge from prose. The reply is dense and uses several em dashes, so prose readability remains `4/5`; comprehension is `5/5`, agency `5/5`, world aliveness `4/5`, and desire to continue `5/5`.

Opening Planner took `273710 ms`, Opening Narrator `112849 ms`, Judge `37444 ms`, Game Master `60895 ms`, and action Narrator `121813 ms`. All five persisted stages used `zai-coding-plan` `glm-5.2`, strict native objects, attempt 1, and valid schema outcomes. No retry, repair, fallback, provider switch, model switch, interruption, or Resume ran.

SQLite integrity is `ok`, foreign-key check is empty, and runtime event sequence and revision are contiguous from `1` through `99`. The action-1 backend restart and actual browser reload preserved public-state hash `c0a3990df3aa412d02257a396027e530e8ab7267ad8a1219988213fc4da193a0`. Restart stderr is empty. Screenshot SHA-256 values are `c953c5a5fda70749ee1eb1686b53c93aecfbdb827131cdd46392f11695c938fc` for opening and `7d54117fc5a2d3c3b731279e6b30d448d5ce4964128588506b50761415959297` for action 1.

r18 is the active replacement Lane A at one of sixty eligible player actions. Humanizer/deslop review keeps the clean mechanical proof, prose density advisory, human score, and still-unmet long-lane claim separate.

## r18 actions 2-4 and current-turn actor knowledge failure

Actions 2 and 3 preserved the bounded lamp-repair negotiation. Lenna inspected the housing without touching it, learned that the token socket was empty, and separated visible crack and damp evidence from unknown seating, depth, and airflow. She then accepted one panel for one week-long salve packet and obtained explicit permission to open her kit and test the fitting without beginning the repair. The compound action correctly stopped at Vassara's reply; the physical test remained the next player decision.

Action 4 selected the signed contextual attempt to open the kit and test the seam. The primary result was coherent: the housing shifted under pressure, old sealant crumbled, the probe caught in grit and was scored, and two calibration strips fogged before Lenna could establish full depth or active airflow. No sealant was applied. The same turn then exposed an autonomous Vassara event showing fresh sealant on the housing and a glass-worker's kit on the corridor stones. Its actor plan described Vassara inspecting a completed seal before releasing payment. This contradicted both the accepted player result and ownership of Lenna's tools.

SQLite establishes the ordering defect. Game Master's applied scene event and its exact-scene `direct_perception` exposure existed before Actor Replanner began. Vassara's durable knowledge row for that event was created only during the later visibility transaction, after her replan had completed. Her frame therefore contained the earlier agreement and permission but not the current failed diagnostic. GLM 5.2 inferred that the promised repair had already happened, and Rulebook correctly persisted the bad actor proposal because its typed scopes were otherwise valid.

Actor frames now merge applied current-player-turn direct-perception events before a plan-boundary replan only when the event-time mechanical snapshot places that actor at the exact exposure scene. This is a read of receipt-bearing SQLite truth, not an early knowledge write; visibility remains the sole durable knowledge owner. The replanner instruction also states that an agreement, offer, permission, intention, or visible tool is not evidence of completed work, and that another person's tools cannot become actor residue without an accepted transfer event. There is no prose parser, semantic regular expression, retry, repair, fallback, provider switch, compatibility path, or backend-authored result.

GitNexus reports LOW impact for `buildActorFrame` and `buildCampaignPlayActorReplanPrompt`. Focused scheduler, replanner, and prompt verification passes 33 tests, including a regression where the current event has no durable actor-knowledge row but still reaches the co-located actor's prompt through its applied event, exposure, and event-time placement. Backend typecheck passes. Humanizer/deslop review found the prompt addition mechanical and specific: it distinguishes evidence states and possession authority without coaching prose style or supplying narrative language.

r18 is frozen at four signed and bound player actions and cannot provide clean acceptance after the contradiction. The rendered contradiction is captured at `output/playtests/campaign-play/pristine-60-glm52-lowwater-ledger-fb2c7074-r18.session/screenshots/action-4.jpg`, SHA-256 `138c4331440d0b8c5096cd5800d5a73865be7ae60bd612c4a9f4e938a47e7b4d`. A fresh materialization must replay the same negotiation and diagnostic sequence before the repair receives product acceptance.

## r19 actions 1-4, possession-shape repair, and autonomous world proof

r19 reused the accepted Lowwater Ledger template at the zero-character, zero-turn boundary and rebuilt Lenna through the rendered Character UI. The opening varied from r18. Vassara offered salves and a dry corner but stated that she owned no lamp panel and held no hiring writ. Thessha likewise refused to invent repair authority and located that authority in Warden Marenthos Valieri's office. The two answers were coherent but repetitive: the second mostly moved the same refusal one office upward instead of creating new pressure.

Lenna then inspected the corridor housings and spent thirty minutes on a non-destructive survey with her own probe. Judge bounded the action to twenty-five through thirty-five minutes and rolled a limited result. Game Master correctly found uniform surface corrosion but no reliable evidence of sound metal depth. It transformed the blank form into `Lamp housing survey notes — Debt-Ward Tenements corridor`, consumed no repair material, applied no sealant, and left every repair-or-replace classification unresolved.

The first Game Master output for that action completed after `130458 ms` but failed strict native-object validation. Its possession effect used `performingActorHandle`, which belongs only to `record_world_event`, instead of the required `actorHandle`. Text fallback was disabled, and the interrupted attempt committed no batch. The Game Master prompt now names the exact `adjust_actor_possession` field set and explicitly forbids `performingActorHandle` on that effect. This is model-contract clarification only: no alias, parser, repair pass, retry loop, fallback, provider switch, or backend-authored item result was added.

GitNexus reports `LOW` impact for the private Game Master `prompt()` function. `plan()` is its only direct caller. Focused Game Master verification passes `26/26`, and backend typecheck passes. Humanizer/deslop review found the addition mechanical and specific. It separates two schema fields without prescribing story content or narrative voice.

After the backend loaded the fix, the player used the rendered `Resume` control on the existing interrupted turn. The second Game Master attempt accepted in `54537 ms`; Actor Replanner accepted in `67097 ms`; Narrator accepted in `160221 ms`. This is a deliberate user-visible recovery after a code correction, not an attempt-one or no-retry claim. The authoritative turn reached `completed` with no error. The Play page kept showing its stale stopped state until an actual reload, after which it restored the finished turn. Recovery state propagation therefore remains a UX defect even though reload is sufficient.

The completed turn supplies useful autonomous-world proof. World time advanced from minute `13` to `43`. Vassara replanned from her exhausted salve inventory toward medicinal supplies, left Debt-Ward by a canonical route, and became present at Tide Shrine Sanctuary. Dessek reinforced the Winch-Shaft barricade, and Novice Niruko charted glow-organisms elsewhere; Remane Scoth's due action was deferred by actor capacity. The player saw only local evidence: Vassara disappeared from `Present here`, the mortar stopped, and the UI reported that she left for the shrine. None of these actors completed Lenna's work, used her tools, or turned the survey into a repair. The r18 current-world-event path was not directly replayed because this branch produced a typed possession transformation rather than the same failed diagnostic event, so r19 does not by itself close the exact r18 regression.

The human verdict is `TARGETED_PASS_CONTINUE`. Scores are comprehension `5/5`, prose readability `4/5`, agency `4/5`, world aliveness `5/5`, and desire to continue `4/5`. The survey prose is concrete and preserves uncertainty. Vassara's departure beat is terse but causally legible. The first two conversations are too similar, and accepted autonomous event summaries contain doubled terminal punctuation that may become visible as local aftermath. At a `1270x720` viewport, the action dock also overlaps left-column content while the scene is scrolled; this remains a presentation advisory rather than part of the possession-contract repair.

The database contains one completed Opening and four completed signed player actions. It has five accepted actor proposals. All accepted stages used `zai-coding-plan`, `glm-5.2`, and strict objects. The one invalid Game Master attempt and its later accepted attempt are preserved in evidence. SQLite integrity is `ok`, foreign-key check returns no rows, runtime sequences are contiguous from `1` through `235`, and every runtime event advances revision by exactly one. World version is `19`, runtime revision is `235`, and world time is minute `43`. Backend restart stderr and browser warning/error capture are empty.

Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-lowwater-ledger-fb2c7074-r19.session/screenshots/action-4-world-move-viewport.png`, SHA-256 `6c9d678ac39ec71985383323534681158639b69853864e07a11e794e1341ebec`. r19 is a targeted product proof, not the formal sixty-action lane: the configured action-one restart checkpoint was missed, and action four required an explicit Resume after the contract fix. No evidence was backfilled.

## r19 action 5 and exact-scene visibility failure

The fifth signed action followed Vassara's observable departure along the open one-unit route to Tide Shrine Sanctuary. Judge, Game Master, and Narrator each accepted one strict GLM 5.2 attempt in `24689 ms`, `200990 ms`, and `81658 ms`. Rulebook moved Lenna to the canonical destination, advanced world time to minute `58`, and preserved all three possessions.

The rendered arrival contains a hard visibility contradiction. `Present here` immediately names Noro Kaiyu Tarama and Vassara Minodra. A suggested action offers to ask Noro by name. The narrated scene simultaneously says that the hall is empty and exposes only anonymous sorting sounds behind a side archway. More seriously, the current-location card prints the accepted location description verbatim: the shrine keepers discovered bridge glow and conceal it because the Wardens cannot meter self-propagating light. The player received a protected discovery and private motive without an observation, witness, or causal exposure.

Stored state locates both leaks at existing ownership boundaries. `visibleScene()` publishes `locations.description` verbatim and treats every person with live `present` placement at the player's location as visible. Campaign World generation had described a persistent sublocation as an establishment or site, allowed protected truth in its description, and allowed actors to be notionally offscreen inside the same location. Game Master movement context carried the route and endpoints but not the live destination roster, so it could call the arrival scene empty before visibility projected the co-located actors.

The repaired contract keeps the backend out of prose interpretation. Campaign World now defines each persistent sublocation as one directly perceivable scene. Its description is explicitly player-facing and forbids secrets, concealed discoveries, private motives, hidden causes, future events, and another actor's private knowledge; those belong to goals, relations, pressures, and protected events. A `present` placement means the person is directly perceivable and identifiable by name in that scene. Game Master movement prompts now receive a code-authoritative `DESTINATION_SCENE` built from the live mechanical placement frame, public location surface, and sorted present-person names. Arrival prose cannot call that scene empty or move a listed person behind an unentered boundary, and the roster does not authorize speech or action.

GitNexus reports `LOW` impact for both Campaign World prompt builders and the private Game Master prompt. The direct consumers are the world builder, focused prompt tests, and Game Master `plan()`. Focused verification passes `29/29`, and backend typecheck passes. Humanizer/deslop review found the instructions mechanical and ownership-specific; they define what each field means without supplying reusable setting prose or telling the models how to sound.

r19 is frozen after five signed and bound player actions. SQLite integrity is `ok`, foreign-key check is empty, runtime sequences are contiguous from `1` through `294`, and every event advances runtime revision by exactly one. World version is `21`, runtime revision is `294`, and world time is minute `58`. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-lowwater-ledger-fb2c7074-r19.session/screenshots/action-5-visibility-leak.png`, SHA-256 `0f7f635c659a2e06a2b83890a3a4ef3be6e6103216e80a1ea5575af8e4dfd4b9`. The existing Lowwater template is semantically contaminated and must not seed the replacement acceptance lane. Product acceptance requires one newly generated world whose location surfaces and first travel arrival are inspected through the real UI; that accepted world can then become the next reusable clean template.

## Fresh Bellglass world and exact-scene product acceptance

The first post-repair world was created end to end through the rendered Campaign Forge. `The Bellglass Tides` used one original premise, six reviewed World DNA cards, no reusable worldbook, and no research mode. Campaign `d76ce637-7536-47ff-9a7c-8b4ff09f8747` was generated inside an isolated empty `GSD_CAMPAIGNS_ROOT`. World Frame, World Cast, and World Connections each accepted one native-JSON `zai-coding-plan` / `glm-5.2` attempt. No stage used retry, repair, or text fallback. The build completed in `296241 ms` with content hash `46bd6f32e7f80ad614521fe8b3cecd69a863e4b37ef7513dcf5eb0ad8fb8b182`.

The pre-acceptance audit covered every generated entity through SQLite and the real Review UI. The world contains three macro regions, seven persistent sublocations, sixteen directed routes, seven `person` actors, twenty directed relations, and five pressures. Every persistent location description is an arriving player's stable sensory or public surface. None contains a concealed discovery, private motive, hidden cause, or future event. Each actor has one exact-scene `present` placement, and the cast is spread across all seven scenes. An additional audit of Netta Scarpa's internal debt text confirmed that `visibleScene()` does not read `actors.summary`; Play exposes only the person's name and a neutral or mechanically observed condition descriptor. Her debt remains in protected goals and relations.

The accepted zero-character, zero-turn state is preserved as immutable template `output/playtests/campaign-world-templates/bellglass-tides-pristine-46bd6f32`. Its manifest records source commit `c34f33036fe6632398d82713bd345a2d99036e14`, accepted world version `1`, state-database SHA-256 `52de2bd569a471b452d5fdc4076137d25edb5a384f21fbc51417ce0cafa1384a`, and config SHA-256 `988f0419d97f354a4e444435ba8b374903f4be6e58d488af3c0430b16785c6c8`. The first playtest uses a separate materialization at `output/playtests/campaign-world-runs/pristine-60-glm52-bellglass-46bd6f32-r01`; the accepted source remains untouched and reusable.

Mara Iven was built through the Character UI as an unknown itinerant floodgate mechanic's apprentice with no status, allies, or special destiny. Explicit `outer-ring / Outsider / Just arrived / Looking for work` conditions produced a grounded opening at `harvester-launch`. Netta Scarpa was mechanically present and offered one plausible work lead toward the gateworks; the opening did not make Mara responsible for the city's floodgate crisis. Opening Planner and Narrator accepted one strict GLM 5.2 attempt in `201211 ms` and `59837 ms`.

The player then chose the rendered travel action to `outer-ring-gateworks`, where accepted placement already located Warden Tessara Vohn. Judge, Game Master, Actor Replanner, and Narrator accepted once in `39209 ms`, `69829 ms`, `56250 ms`, and `77615 ms`. The arrival card, visible roster, consequence, narration, and next choices all agree: the player is inside the gateworks scene, Tessara is directly present and identifiable, and fresh chalk measurements are visible on the cracked buttress. Game Master did not call the scene empty, move Tessara behind an unentered boundary, or make her speak without an accepted interaction. This closes the exact r19 arrival contradiction on the real product.

The same twenty-minute turn also demonstrates independent motion. Netta left the launch for the bellglass beds through a canonical route, Dain continued extracting shards there and left projectable local aftermath, and Tessara measured the widening crack at the gateworks. Pietr's due work deferred explicitly on actor capacity. None of these actors completed Mara's chosen task or redirected the world around her. The human verdict is `TARGETED_PASS_CONTINUE`: comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `5/5`. Text-only DOM extraction repeated each Narrator paragraph because it included both the visible beat and its visually hidden `aria-live` announcement. A later rendered screenshot proves that the player sees one paragraph; this is accessibility markup, not duplicate presentation. The two earlier screenshot captures timed out at the CDP layer, so the arrival checkpoint itself still relies on persisted UI DOM readback and the database ledger.

Humanizer/deslop review keeps the note evidence-first: it separates mechanical acceptance from the human taste scores, corrects the text-only accessibility false positive, and makes no general quality claim beyond the opening and first travel action actually played.

After Opening and one player action, SQLite integrity is `ok`, foreign-key check returns no rows, runtime sequences are contiguous from `1` through `104`, every runtime event advances revision by exactly one, world version is `16`, runtime revision is `104`, and world time is minute `20`. All six Campaign Play model stages used `zai-coding-plan`, `glm-5.2`, strict objects, one attempt, and no error. r01 remains the active manual playtest; this checkpoint accepts the generation and arrival repair, not the sixty-action lane.

## Bellglass r01 actions 2-4 and possession-outcome ownership

The next two player actions established a credible work boundary. Tessara supplied concrete minimum materials and a worsening measurement: twelve timber baulks, eight iron straps, and a buttress crack widened from four to seven finger-widths in a month. Mara then stated that she was an apprentice and offered only a non-destructive inspection and written condition list. Tessara authorized look, measure, and record; explicitly forbade cutting, dismantling, winch adjustment, and seal adjustment; offered half a laborer's day rate from her own maintenance ledger; and made any later full rate contingent on useful evidence and Canal Court funding. The sequence preserves limited NPC authority and gives the ordinary player a useful but non-central role.

Action four selected `Try to measure the seepage along the lower gates`. Judge normalized the method as measuring and recording with the visible hand tools, cord, and waxed notebook; its stakes required accurate retained evidence. A success roll then gave Game Master seventy-five minutes. The accepted discovery event claimed nine charted seepage channels and said their positions, gaps, and rates were written in the notebook. Rulebook received no possession command, and the rendered `Carrying` region still showed the untouched `Waxed notebook with charcoal pencil`. The same prose described a `steady finger-width stream` as `one drop every two heartbeats`, and its precision exceeded the available tools. The durable record contradiction is the blocking defect; the physical-description advisory remains a separate quality issue for later Judge/GM grounding.

The world itself continued coherently. Pietr independently left Brackish Crossing for the gateworks to seek a work order and appeared in both `Present here` and the second narration beat. Netta and Sestra acted elsewhere through protected or local-aftermath events. r01 is frozen after four completed player actions; it is not resumed under the repaired contract.

The repair moves possession outcome ownership into the Judge ruling. Every new ruling now carries `requiredPossessionEffect`: explicit `none`, or an acquire/spend/transform requirement with source possession handle when applicable, quantity, and the lowest result tier that must produce the change. Judge semantic compilation requires a non-null source to be a cited visible possession. Game Master compilation compares the resolved result with the threshold and rejects the complete proposal unless exactly one `adjust_actor_possession` effect matches operation, source handle, and quantity. It does not parse prose, author a result name, retry a model, repair output, switch provider, or fall back. A missing match interrupts the external stage before Rulebook execution and narration.

GitNexus reports `LOW` impact for the Judge ruling schema, Judge proposal/prompt/compiler, Game Master prompt/compiler, and focused fixture helper. Direct production callers are only Judge `judge()` and Game Master `plan()`. Focused verification passes `401/401`, backend typecheck passes, and diff validation is clean. Humanizer/deslop review passes both prompt additions as compact mechanical ownership rules without reusable story prose or narrative-style instruction. Product acceptance still requires a new Bellglass template materialization and a real UI replay that visibly transforms the notebook; tests alone do not close r01.

The first replay copy, r02, reproduced the same opening and reached an authorized `Try to measure and sketch the fissure widths` action after two completed player turns. Its first Judge attempt ran for `71860 ms` and failed native-object validation before artifact acceptance. GLM returned an invalid possession requirement: `operation` was outside acquire/spend/transform and an extra `adjustment` key was present. The turn is interrupted and resumable with zero mechanical mutation. The prompt now gives one exact notebook-transform object and states that `adjustment` does not exist. This is a schema clarification, not an accepted alias or compatibility parser. r02 remains diagnostic; a new materialization owns clean product acceptance.

## Bellglass r03 possession outcome and Actor Replanner recovery

r03 rematerialized the same accepted Bellglass template and rebuilt Mara through the rendered Character UI. Its opening placed her at Glasscaster Quay with Master Orsa Vail. Mara disclosed that she was an apprentice and negotiated one bounded job: inspect three iron bar mounts, write an itemized condition and repair estimate in her own notebook, leave the case barred, touch no bellglass, and earn a half-mark for the inspection. Orsa retained authority over the workshop and deferred repair pay until after reviewing the numbers.

The next rendered action asked Mara to measure crack depth across all three mounts. Judge accepted one strict GLM 5.2 object in `57585 ms`, resolved `strong_success`, and required a transform of the visible notebook possession. Game Master accepted one strict object in `118086 ms`. Its Rulebook batch advanced thirty minutes, spent the old notebook, and acquired `Waxed notebook with crack-depth survey of three iron bar mounts`. The Play UI then showed exactly that item at quantity one and no longer showed the blank notebook. The finished narration used the recorded measurements, and the next choices could draft the estimate or discuss the survey with Orsa. This is the required real-product proof for the possession-outcome repair.

After primary settlement, Master Orsa's Actor Replanner response used `cadenceMinutes: 0`. The strict schema rejected it after `75868 ms`; no actor proposal was accepted and the public turn became explicitly resumable. The schema had always required an integer of at least one, but the prompt described cadence only as how often the actor acts. The prompt now states the exact integer range and defines the value as world minutes between opportunities to act. No default, clamping, alias, parser, retry loop, fallback, provider switch, or backend-authored plan was added.

After the backend loaded that contract clarification, the rendered Play UI exposed `Resume`. The first click met a stale browser epoch after restart and was fenced without mutation; `Reload scene` restored current server state. The live recovery then accepted Actor Replanner attempt two in `64608 ms` with cadence twenty, settled the remaining actor boundary, and accepted Narrator attempt one in `97738 ms`. The player turn completed at world version `17`. Every admitted actor job is terminal: three settled and four deferred explicitly on `actor_capacity`.

The human verdict is `TARGETED_PASS_CONTINUE`: comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `4/5`, and desire to continue `5/5`. The work negotiation, limited authority, durable notebook result, and workshop aftermath are coherent. Calipers and a rust probe appearing from the compact tool roll are plausible but more specific than the visible inventory. SQLite contains one persisted `display_text` and one matching beat. Text-only DOM extraction also sees the visually hidden `aria-live` copy, but a rendered `1270x720` screenshot shows one visible `THE MOMENT` paragraph. No duplicate-render repair is warranted. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r03.session/screenshots/action-2-completed.png`, SHA-256 `f2ca78cf04893b9396b404c02167aac41bc768463b911ec2fda43f97df184424`.

Focused Actor Replanner prompt and service verification passes `8/8`, and backend typecheck passes. GitNexus reports `LOW` upstream impact for the prompt builder and its test fixture. Humanizer/deslop review keeps the cadence wording mechanical and literal: it defines a numeric contract without coaching voice, supplying narrative language, or making the prompt sound conversational. r03 remains the active manual lane; possession transformation and Actor Replanner recovery are accepted, and the following player action can now test whether the written survey produces a grounded workshop decision.

## Bellglass r03 actions 3-5 and bounded workshop loop

Action three handed the completed survey to Orsa. She reviewed each mount, agreed with the distinct remedies, committed her own lug stock, plate iron, forge, and heat, kept Mara responsible for tools and labor, and preserved the barred-case boundary. The response repeated more of the measurements than necessary, but it advanced the contract: the inspection was accepted, the half-mark was earned, the repair duration was bounded, and Mara had to name a rate before touching iron.

Action four used freeform terms rather than a suggested choice. Mara named half the guild journeyman rate, requested half before and half after the work, asked Orsa to state the exact marks and material responsibility, and requested the already-earned inspection fee. Orsa paid the inspection half-mark, accepted the rate as fair, refused the advance, and made full repair payment contingent on all three mounts holding under her inspection. Rulebook acquired `Inspection half-mark` at quantity one; the rendered `Carrying` panel showed it immediately. It did not acquire repair pay or workshop materials that had not been transferred. Orsa therefore behaved as a counterparty with her own risk instead of agreeing to every player term.

Action five accepted payment on completion but conditioned work on Orsa bringing the promised stock to the bench and authorizing its use. Mara attempted only the center mount. The turn advanced two hours, Orsa placed rough lug stock and a plate-iron blank at the bench, and Mara reforged and reseated the center mount without opening the barred case. Orsa inspected the cooled seam and accepted that one repair. The lower lug and upper stress fracture remain open, the materials remain Orsa's bench stock rather than Mara's possessions, and full payment remains withheld. This is a coherent bounded loop from inspection through contract, resource access, one repair, inspection, and explicit remaining work.

The human verdict remains `TARGETED_PASS_CONTINUE`: comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `5/5`. Action three is slightly over-complete because Orsa restates nearly every measurement. Actions four and five are concrete, resist player wish-fulfilment, and preserve authority, ownership, time, and incomplete work. Humanizer/deslop review retains that repetition advisory but finds no vague motivational filler, canned quest language, or ornamental summary replacing physical action.

The three turns completed in `188043 ms`, `309780 ms`, and `318402 ms`, with median `309780 ms`. Judge's action-four deliberation alone took `161745 ms`; latency is now the dominant playability problem. The page also remained on the generic `Action received` message through much of model execution even when server state had advanced, so progress feedback understates where the wait occurs. This is a UX observation, not a timeout or acceptance failure.

All ten model stages for actions three through five accepted strict native GLM 5.2 objects on attempt one: three Judge, three Game Master, three Narrator, and one Actor Replanner. During action five, Netta completed a canonical move from Harvester Launch to the Eastern Bellglass Beds, replanned her exhausted course toward the low-tide harvest, and left the player-local workshop narration untouched. Tessara and Kael also settled due jobs elsewhere; four other jobs deferred explicitly on actor capacity. Remote activity remained protected from Mara.

After five completed player actions, world version is `22`, world time is minute `162`, and runtime revision is `284`. Runtime events are contiguous from `1` through `284`, and every event advances revision by exactly one. SQLite integrity is `ok`, foreign-key check is empty, and all six turns including Opening are completed. The only interrupted model evidence is the preserved pre-fix Actor Replanner attempt from action two; its explicit accepted attempt-two recovery is also preserved. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r03.session/screenshots/action-5-center-mount-complete.png`, SHA-256 `854846dac32214c3a0e9a1b9246027d370149d764c44c86d0a50c57b6697b648`. r03 remains active after five player actions.

## Bellglass r03 action 6 and untargeted-NPC authority repair

Action six selected `Try to forge the lower-shelf lug replacement`. Judge targeted the workshop location rather than Orsa, bounded the long job to 240 through 360 minutes, and resolved `strong_success` on a 16. The first Game Master call returned one strict native object after `173435 ms`, but semantic compilation rejected it before artifact acceptance. Its physical scene was grounded; the violation was a second `record_world_event` that made Orsa inspect and approve the lug even though no actor was targeted by the player intent. The stopped attempt committed no world-time, repair, actor, or narration mutation.

The existing compiler correctly limits a performing NPC to an actor targeted by the exact player intent. The prompt now makes the corresponding negative case explicit: when no actor is targeted, every recorded event is actorless, and a nearby person must not inspect, approve, reject, speak, or otherwise react. Their response remains a later contact action. This preserves NPC agency and player scope; it does not parse prose, admit the invalid performer, rewrite the effect, retry automatically, switch provider, or supply an NPC response from backend code.

Focused Game Master verification passes `26/26`, backend typecheck passes, and GitNexus reports `LOW` upstream impact for the private prompt, whose only direct production caller is `plan()`. Humanizer/deslop review finds the added sentence mechanical and authority-specific. It names prohibited NPC acts only to define the interaction boundary and supplies no dialogue, outcome, characterization, or narrative voice.

After restart, the rendered Play UI exposed one explicit `Resume`. Game Master attempt two accepted in `39013 ms` and advanced 270 world minutes. It recorded only the actorless physical result: lug stock consumed, a square and level lower-shelf lug seated without gap or wobble, the plate blank untouched, the barred case closed, and the upper stress fracture still open. Actor Replanner accepted once in `85753 ms`, and Narrator accepted once in `91950 ms`. The next rendered choices separate the remaining work from Orsa's authority: shape the upper plate, or ask her to inspect the new lug.

The page retained its stale pre-resume `The turn stopped` presentation through Game Master, actor settlement, and most of Narrator, even though server state had advanced to `judged`, `primary_settled`, and `visibility_projected`. It switched to the completed scene only when the terminal narration arrived. This reproduces incomplete live progress propagation after Resume; it does not invalidate the recovered turn.

Action six completed at world version `24`, world time minute `432`, and runtime revision `358`. Dain independently replanned from the Eastern Bellglass Beds toward Harvester Launch as the tide closed, with rising-water stakes and a canonical return route. That remote plan and movement remained outside Mara's narration. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r03.session/screenshots/action-6-lug-forged.png`, SHA-256 `81db490486be8199580c5297ea8067be7f0ca70b3b41a5ff41c0c48688506122`. r03 remains active after six completed player actions.

## Bellglass r03 actions 7-10 and recoverable craft setback

Action seven targeted Orsa explicitly and therefore allowed the reaction excluded from action six. She tested shoulder, seat, gap, and wobble, accepted the lower lug, and left the upper plate as the final repair. No additional payment was issued. This closes the authority sequence cleanly: player craft and NPC acceptance are separate actions with separate targets.

Action eight attempted the last plate but resolved poorly. The flat stock drew unevenly, one shoulder flared, the test fit rocked, and a finger-width wall-side gap remained over the fracture. The plate became misshapen rather than disappearing or resetting; the upper mount stayed unrepaired and payment remained withheld. The server completed the turn, but the rendered page remained on `Action received` until a real reload restored the finished setback scene. This reproduces the live-update defect outside the Resume path.

Action nine asked Orsa about that exact failed plate. She did not erase the setback or supply fresh stock. She diagnosed uneven heat and strikes, demonstrated the rock and gap, and established a grounded repair method: heat the whole stock evenly, draw both shoulders together at the same pace and force, and match the mount's curve before cooling. Existing metal remained sufficient.

Action ten attempted that rework. The flared shoulder narrowed and the gap became smaller than a finger-width, but different contraction across the twice-worked thick and thin edges left a visible seam and continued rocking. The upper fracture remains unrepaired. The next actions advance the material problem rather than repeating the same generic attempt: examine the fit, grind the thick shoulder flush, or ask Orsa about the uneven contraction.

The human verdict at ten actions is `CONTINUE_LANE`: comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `5/5`. The craft prose sometimes repeats measurements between consequence and Narrator, but the chain is materially legible and interesting because preparation, distinct repairs, NPC inspection, bad stock geometry, and partial rework all persist. Failure produces a new decision instead of a dead end or invisible penalty.

Actions seven through ten completed in `92547 ms`, `213988 ms`, `79874 ms`, and `232907 ms`; the four-turn median is `153268 ms`. The longer physical turns each included an independent actor replan. Tessara replanned toward recruiting Pietr for the floodgate crew, while Kael replanned toward cross-checking toll tallies with Sestra's debt ledger. Those remote plans remained protected from Mara and did not hijack the workshop scene.

The database now contains one completed Opening and ten completed player actions. All ten Judge stages are accepted. Game Master has nine accepted attempt-one stages, one accepted attempt-two recovery, and the preserved invalid action-six attempt. Narrator has eleven accepted attempt-one stages. Actor Replanner has four accepted attempt-one stages, one accepted attempt-two recovery, and the preserved invalid action-two attempt. World version is `29`, world time is minute `572`, and runtime revision is `500`. Runtime events are contiguous from `1` through `500`, every event advances revision by one, SQLite integrity is `ok`, and foreign-key check is empty.

Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r03.session/screenshots/action-10-plate-rework-partial.png`, SHA-256 `d537d158c857ab2979158160c32f71caf71949658f407cb5fadf36bd56d0e01a`. Humanizer/deslop review preserves the failed-craft chain and latency defect without turning the checkpoint into success language. r03 remains active after ten completed player actions; the workshop contract is not complete until the plate is accepted and repair pay becomes Rulebook possession truth.

## Bellglass r03 actions 11-15 and completed workshop contract

Action eleven asked Orsa about the twice-drawn plate's uneven contraction. She preserved the prior partial result, distinguished matched hammer-work from mismatched thickness, and directed Mara to grind the thick shoulder flush before another heat. Action twelve performed that bounded correction with the existing wet grinding stone and cutting slick. The cold test then showed matched thickness, no lift, no gap, and no rock, but the plate was only ready for its final forge curve; the upper mount was not yet declared complete.

Action thirteen heated the corrected plate evenly and drew both shoulders into the mount's curvature. The cooled plate sat flush over the fracture and all three mounts physically held. The final Narrator beat explicitly withheld Orsa's acceptance: she had not yet tested the last plate. This is direct product proof that the untargeted-NPC repair continues to preserve the boundary beyond the repaired action-six replay.

Action fourteen used a freeform request for Orsa to inspect all three mounts and, if they passed, state the exact rate and pay it. Judge's first native object cited one nonexistent opaque observation handle and failed schema validation after `55135 ms`. The turn had no mutation. The prompt and enum already specify exact handle copying, so no extra instruction, parser, alias, or backend repair was added for a one-off model miss. The rendered `Resume` explicitly started Judge attempt two, which accepted in `46072 ms`.

Game Master then inspected the final plate, lower lug, and center seam through Orsa, accepted all three, selected guild standard because the parties had never settled a numeric rate, and named two marks for half-journeyman work with Orsa's materials. Its one atomic batch recorded the acceptance and acquired `Repair wages — two marks` at quantity one. The Play UI showed that possession beside the earlier inspection half-mark. Narration and Rulebook therefore agree on exact amount, payer, work, and transfer.

The completed action-fourteen Narrator nevertheless labeled the generic attempt choice `Try to gather the paid marks from the bench`, contradicting the already-acquired possession shown in `Carrying`. Narrator now treats positive-quantity `possessions` as current custody even when a consequence says the item was set down or handed over, and cannot use an action detail to pick up, gather, take, collect, receive, or reclaim that item. It must choose another unresolved step. This is prompt-level semantic ownership only; there is no prose parser, label rewrite, compatibility path, retry, or backend-authored choice.

Focused Narrator verification passes `12/12`, backend typecheck passes, and GitNexus reports `LOW` upstream impact for `buildPrompt`, whose only direct production caller is `narrate()`. Humanizer/deslop review keeps the addition literal and compact. It defines possession finality without prescribing setting prose, tone, or a replacement action.

After the backend loaded the repair, action fifteen asked Orsa about further quay work. She did not manufacture another quest: the surge-damaged iron was finished, bellglass remained her trade rather than Mara's, and only a future storm break might create another iron job. The newly generated attempt choice became `Try to examine the loaded canal barges`; no choice tried to acquire the wages again. The completed contract therefore exits naturally back into the wider world.

The human verdict at fifteen actions remains `CONTINUE_LANE`: comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `5/5`. The workshop sequence is longer than a conventional quest but earns its length through negotiated authority, material access, distinct craft stages, failure, diagnosis, recovery, separate NPC acceptance, and exact payment. Repeated measurement language remains the main prose advisory; turn latency remains the main playability defect.

Actions eleven through fifteen completed in `154086 ms`, `184165 ms`, `256718 ms`, `398074 ms`, and `102344 ms`, with median `184165 ms`. The database contains one completed Opening and fifteen completed player actions. `Repair wages — two marks` is durable at world version `35`. Current world version is `36`, world time is minute `619`, and runtime revision is `698`. Runtime events are contiguous from `1` through `698`, every event advances revision by one, SQLite integrity is `ok`, and foreign-key check is empty.

Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r03.session/screenshots/action-15-workshop-loop-complete.png`, SHA-256 `afc18129f66d56b29c2fd8afa8617b8dc2e8e44262158da1d23f9040824e02ab`. r03 remains active after fifteen completed player actions; the next lane should leave the workshop and test whether remote autonomous developments become discoverable through travel and local evidence.

## Bellglass r03 actions 16-18 and cross-scene boundary failure

Action sixteen left the completed workshop and used the rendered route to Bellglass Beds. Mara arrived in the mudflat scene with Netta Scarpa mechanically present. Two older autonomous developments had become locally discoverable as physical residue: boot-pressed furrows with torn oilcloth, and snapped formation stumps with crushed shells. The narration exposed only what Mara could see at the destination; it did not disclose the remote actors' protected plans or assert a hidden cause.

Action seventeen asked Netta about the freshly worked formations. Netta claimed three clean formations as her licensed work, denied responsibility for the furrows and oilcloth, and accused Dain Solkeen of working the deeper flats carelessly. The claim remained attributed dialogue rather than narrator-certified truth. This is useful living-world evidence: remote activity left persistent local traces, and a co-located actor interpreted them from her own position without the backend converting that interpretation into fact.

Action eighteen selected `Try to trace the boot-pressed furrows shoreward`. Judge, Game Master, and Narrator each accepted one strict GLM 5.2 object. No `move_actor` effect was authorized or applied. The resulting prose nevertheless carried Mara out of the accepted Bellglass Beds scene to the separately modeled Harvester Launch: the trail ended where mud gave way to the launch ramp's packed stone, and oilcloth appeared in the ramp's surface cracks. SQLite still placed Mara at `bellglass-beds`; `harvester-launch` is a distinct persistent sublocation reached through its own route. This is a hard spatial contradiction, not a prose preference.

r03 is frozen after eighteen completed player actions and must not be resumed. The Game Master prompt now receives `CURRENT_EXACT_SCENE` from the authoritative player placement and accepted persistent-sublocation record. When `PLAYER_MOVEMENT` is null, every result must remain inside that scene. A trail may point toward another destination, but entering it or inspecting its ramp, gate, floor, wall, or other scene surface requires the code-authoritative movement contract. The change adds no location-name parser, prose repair, implicit movement, compatibility path, retry, fallback, provider switch, or backend-authored outcome.

GitNexus reports `LOW` upstream impact for the private Game Master `prompt()` function: `plan()` is its sole direct production caller across three indexed flows. Focused Game Master verification passes `26/26`, and backend typecheck passes. Humanizer/deslop review found the addition mechanical and ownership-specific. It defines the exact spatial authority already present in state without supplying setting language, narrative voice, or a preferred result.

Actions sixteen through eighteen completed in `145597 ms`, `212676 ms`, and `124018 ms`. Every persisted model stage used `zai-coding-plan` `glm-5.2`, strict native objects, attempt one, and a valid schema outcome. The database contains one completed Opening and eighteen completed player actions. World version is `41`, world time is minute `668`, and runtime revision is `805`. Runtime sequences are contiguous from `1` through `805`, every event advances revision by exactly one, SQLite integrity is `ok`, and foreign-key check is empty.

The rendered failure is captured at `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r03.session/screenshots/action-18-cross-scene-trace.png`, SHA-256 `c329ca2235bbcafcaeefc54083a543f55c8094db5f0cee83063d9b4a8c6ad933`. Human verdict for actions sixteen and seventeen is `PASS`; action eighteen is `FAIL_FREEZE_LANE`. Product acceptance now requires a fresh materialization from the immutable Bellglass template and a real UI replay showing that a non-move trace stops inside the current exact scene.

## Bellglass r04 exact-scene product acceptance

r04 materialized the immutable `bellglass-tides-pristine-46bd6f32` template at its zero-character and zero-turn boundary. No world generation ran. Mara was rebuilt through the rendered Character UI. The generated draft preserved her ordinary mechanic's-apprentice trade, caution around other people's property, lack of local standing, and four requested possessions. One generated contradiction said her trade pulled her toward the city's central conflict; manual review replaced it before acceptance with the smaller human tension between keeping her head down and having strangers judge her work. The player therefore entered without a chosen-one or conflict-centre bias.

The explicit `outer-ring / Outsider / Just arrived / Looking for work` opening returned Mara to Glasscaster Quay with Master Orsa Vail. Opening Planner and Narrator accepted one strict GLM 5.2 object each. The rendered scene supplied a bounded shelf-repair lead and ordinary posted pay without forcing acceptance. The opening prose is clear but largely restates the public location surface; comprehension is `5/5`, readability `4/5`, agency `5/5`, world aliveness `4/5`, and desire to continue `4/5`.

Action one used freeform text to take the visible open passage to Bellglass Beds. Rulebook applied the canonical movement and placed Mara in the mudflat exact scene with Dain Solkeen present. The arrival exposed the accepted public surface and visible roster without hidden motives or remote knowledge. Action two then stated the negative boundary directly: inspect mud around Dain's work area for footprints or dragged tools, trace only their direction, remain inside Bellglass Beds, and do not enter Harvester Launch or step onto its ramp.

Judge, Game Master, Actor Replanner, and Narrator all accepted one strict GLM 5.2 object. Game Master's batch contained no `move_actor` for Mara. It advanced five minutes and recorded only Bellglass Beds evidence: churned waterlogged mud, crab tracks, pooled water, a few unreadable impressions, and no surviving tool-drag groove. Harvester Launch, packed stone, ramp cracks, and every other destination-scene surface remained absent. SQLite still places Mara at `bellglass-beds`. This is the required real-product `PASS` for the exact-scene repair.

The same turn also demonstrates independent world activity. Dain completed a due harvesting action in the co-located scene, leaving a half-filled basket, scattered fragments, and fresh deep boot-prints. Netta independently moved from Harvester Launch into Bellglass Beds and became visible in the second narration beat. The player did not cause either action. One chronology advisory remains: the first beat says the pre-action churn yielded no readable trail and shows Dain's new basket, while a next action refers to the fresh deep prints without explicitly saying Dain made them after Mara's inspection. State order is valid, but the prose transition could make the two successive surface states easier to distinguish.

Opening completed in `291844 ms`; actions one and two completed in `100897 ms` and `224640 ms`. All nine persisted model stages used `zai-coding-plan` `glm-5.2`, strict native objects, attempt one, and valid schema outcomes. No retry, Resume, repair, fallback, provider switch, model switch, or interruption occurred. World version is `17`, world time is minute `10`, and runtime revision is `132`. Runtime sequences are contiguous from `1` through `132`, every event advances revision by one, SQLite integrity is `ok`, foreign-key check is empty, and backend stderr is empty.

Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r04.session/screenshots/action-2-spatial-boundary.png`, SHA-256 `9433abffe5830cec10f4e275aa31d38e8dbc581d26eaf9b61112ea5b957e9de3`. Opening and arrival screenshots have SHA-256 values `01e3e5be53e083357fdbdc16dbc7b02156b615b5b664e8b79da1c50578ca84ef` and `3301e62fcc6cafe6546e714cdc604339b728f55587115eff8087b7ea5860fe4c`. Humanizer/deslop review preserves the concrete spatial proof and chronology advisory without inflating the two-action run into longplay acceptance. r04 remains clean and active after two completed player actions.

## Bellglass r04 actions 3-10 and actor-carried object duplication

Action three resolved the apparent track contradiction through direct contact. Dain said that the fresh deep prints were his new trips between the formations and basket, while water and crab tracks had destroyed any older readable trail. The response made the two successive mud states legible without turning Dain's claim into narrator-certified truth. Action four asked for the remaining safe window and offered bounded help. Dain named fifteen minutes until the channel reached knee depth and twenty until it became impassable, accepted help on his terms, and left Mara responsible for carrying the basket while he chose the path.

Actions five through nine formed a real failure-and-recovery loop. Mara failed to secure the unexpectedly heavy basket, then cracked one voiced formation while rebalancing it. Dain did not react inside the untargeted physical action; the next contact made him inspect the damage, declare the voice irrecoverable, and give a safer packing method. He independently moved to Harvester Launch. Mara's second packing attempt cracked another formation and left the load worse. Instead of repeating the same roll, the player used her visible light cord to cinch the surviving formations against the wicker. The load became stable and carryable while both silent fragments remained in the mud. The basket and cord never appeared as newly acquired possessions, and Mara's original cord remained in her custody while attached to the basket she carried.

The sequence is engaging but harsh. Two consecutive failures each permanently silence a recorded voice and consume most of the safety window. They produce distinct state and invite a resource-based solution, so they are not empty repetition, but the second failure pushes frustration above the earlier craft loop. Human scores through action nine are comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `4/5`.

Action ten used the canonical open route to Harvester Launch. Rulebook moved Mara there at world minute `30`, and the arrival correctly retained Dain, the cord-cinched basket in Mara's arms, and the two silent fragments back in Bellglass Beds. The destination also exposed a prior local-aftermath event from world minute `23`: a woven basket already resting on the ramp with curved bellglass inside while gloved hands sorted the load. The scene therefore contained both the basket Mara had just delivered and an autonomous duplicate that Dain had materialized seven minutes earlier.

Persisted actor evidence locates the defect before Narrator. Action seven moved only Dain from Bellglass Beds to Harvester Launch after his accepted dialogue explicitly left the basket with Mara. During action eight, Dain's Actor Replanner knew that dialogue but proposed setting the basket on the destination ramp and sorting its shards. Rulebook accepted the otherwise typed local event because scene props are not first-class mechanical entities. The autonomous model inferred that Dain's goal cargo followed his `move_actor`, even though movement authority changed only his placement.

Actor Replanner's contract now states that an accepted `move_actor` changes only the named actor's placement. It cannot move, copy, or recreate baskets, cargo, tools, materials, vehicles, animals, companions, or other objects. When an accepted event leaves an object with another person or at another location, the replanner must treat it as absent from the current scene and cannot target, handle, sort, use, or leave residue from it until a later accepted event explicitly brings it there. This is a model-contract clarification, not an object parser, hidden inventory store, compatibility path, retry, fallback, provider switch, or backend-authored event.

GitNexus reports `LOW` upstream impact for `buildCampaignPlayActorReplanPrompt`: production use is limited to `ActorReplanner.replan`, plus focused prompt tests. Focused prompt and replanner verification passes `9/9`, and backend typecheck passes. Humanizer/deslop review found the addition literal and authority-specific. It defines movement and object-location ownership without supplying story content, actor motivation, or narrative voice.

Actions three through ten completed in `171631 ms`, `162108 ms`, `165736 ms`, `154200 ms`, `179138 ms`, `244751 ms`, `174034 ms`, and `220404 ms`. The database contains one completed Opening and ten completed player actions. Every persisted model stage used `zai-coding-plan` `glm-5.2`, strict native objects, attempt one, and a valid schema outcome: ten Judge, ten Game Master, six Actor Replanner, eleven Narrator, and one Opening Planner stages. No retry, Resume, repair, fallback, provider switch, interruption, or invalid schema outcome occurred.

World version is `29`, world time is minute `30`, and runtime revision is `434`. Runtime sequences are contiguous from `1` through `434`, every event advances revision by one, SQLite integrity is `ok`, and foreign-key check is empty. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r04.session/screenshots/action-10-basket-duplication.png`, SHA-256 `a9b9939197f7b2c9d9482dff624d85bf1fb79e654a048f0bc9dc8b84ec9bc247`. The earlier causal explanation is captured at `action-3-dain-explains-tracks.png`, SHA-256 `4170e60de66bf8cd16d84d3458162f94bee6747c2a70ba2ea953e77a40b613e7`. r04 is frozen after ten actions; a fresh materialization must replay a short handoff-and-movement scenario before the actor-object continuity repair receives product acceptance.

## Bellglass r05 transport stall and r06 fixture-placement failure

r05 materialized the immutable Bellglass template without world generation and completed six player actions through the rendered UI. Mara followed Dain from Bellglass Beds to Harvester Launch, learned why Netta would not loan her working rope, inspected Dain's three memorial shards, and twice respected his refusal to hand over the certified pack. Dain retained his own priorities throughout; neither character became a quest dispenser merely because Mara asked for work.

The seventh action asked which certified hands receive Dain's pack and where he would take it. Judge and Game Master accepted strict GLM 5.2 objects on attempt one in `15105 ms` and `67482 ms`. The approved GM result named Collector Kael Mirrus at the Shrine gate and Sestra Olo's debt ledger. Narrator attempt one then remained `started` at `visibility_projected` while the worker lease continued to renew. The backend process had no external provider connection and nearly idle CPU during a sampled five-second interval, so the preserved state is a transport-promise stall rather than evidence that GLM remained connected and generating. No timeout, abort, retry, Resume, fallback, provider switch, or backend-authored narration was introduced. r05 remains frozen with six completed actions and the seventh in flight.

r06 again copied the immutable Bellglass template rather than generating a world. Character creation used two successful strict GLM 5.2 calls. The chosen `outer-ring / Outsider / Just arrived / Looking for work` opening placed Mara at Harvester Launch, where Netta Scarpa was preparing a skiff against a closing tide window. Opening Planner used `16445` reasoning tokens and completed in `213563 ms`; Narrator completed in `45472 ms`. The scene offered bounded piece-work without centering the world on Mara.

Action one asked Netta to demonstrate the local cross-bind before Mara touched the fourth basket. Netta showed one exact procedure, explained why a square lashing fails in chop, undid the demonstration, and left the attempt to Mara. Action two reproduced the procedure under time pressure. Judge rolled `8` against difficulty `10` and resolved a setback. GM committed a slipped same-direction binding. Netta then independently re-tied the rope through Actor Replanner. The failure is legible, fair within the declared uncertainty, and leaves concrete consequences rather than a generic rejection.

The resulting spatial state is not acceptable. The opening placed the fourth basket on wet stone outside the skiff. Action two and Netta's accepted event fastened that basket around the skiff's forward thwart, but neither committed whether the basket entered the skiff. Narrator then labeled the next attempt `Try to haul the lashed basket to the skiff`, contradicting the fixture relation it had just rendered. The accepted Actor Replanner plan also proposed moving Netta to the beds by shoving the loaded skiff, even though the only movement command changes Netta's placement and cannot move the skiff or its cargo. r06 is frozen after two completed actions. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r06.session/screenshots/action-02-basket-spatial-contradiction.jpg`, SHA-256 `2d9de3d323615c34abb2ca0e8c10b2859ec854c01edf2d71e9fa45243095a51a`.

The repair stays at proposal authority. Game Master must include necessary incidental handling and commit one unambiguous final object-to-container or object-to-fixture relation, including the final position after a setback. Actor Replanner may not propose an actor-only move whose method, stakes, or later steps require a vehicle, cargo, companion, or other object to travel with that actor; a fixed relation must remain consistent across later steps. Narrator must treat the latest explicit object relation as final for the turn and cannot offer to load, haul, insert, or move an object to the fixture or container where the packet already places it. No object parser, prose rewrite, implicit cargo movement, hidden retry, fallback, provider switch, or backend-authored choice was added.

GitNexus reports `LOW` upstream impact for all three prompt owners: Game Master `prompt()` reaches only `plan()`, `buildCampaignPlayActorReplanPrompt()` reaches only `replan()`, and Narrator `buildPrompt()` reaches only `narrate()`. Focused verification passes `46/46` across Game Master, Actor Replanner prompt, and Narrator tests; backend typecheck passes. Humanizer/deslop review keeps all three additions mechanical and setting-neutral. They define final spatial ownership and unsupported-movement boundaries without prescribing a basket, skiff, character voice, outcome, or replacement action. Because an action-two clone would retain the already accepted pre-fix actor plan, product replay must start from a zero-turn r07 materialization of the immutable Bellglass template.

## Bellglass r07 object-placement replay and causal-trace gap

r07 copied the same zero-character, zero-turn Bellglass template; no world generation ran. The rendered Character UI rebuilt Mara as an ordinary outsider with no powers, local standing, certifications, or allies. The same explicit arrival conditions opened at Glasscaster Quay instead of forcing the earlier Harvester Launch scene. Master Orsa Vail had three padded bellglass crates to load before the tide turned. This independent crate-and-barge task exercised the repaired contracts without replaying the basket vocabulary that exposed them.

Action one asked for exact handling, destination, stacking, and pay terms before Mara touched guild goods. Orsa identified the tide-warning amulet crate, required horizontal two-handed carriage, specified the aft deck against the gunwale strakes, limited stacking to two high, and offered three coppers per crate after all three were aboard. Action two carried the first crate from the quay stack onto the barge. A limited result left it explicitly on the aft deck against the strakes, held by rough planks but with a finger-width gap. Narrator preserved that final relation and offered inspection, stance, and contact options; it did not offer to load or haul the crate onto the barge again.

Action three asked Orsa to inspect the gap and faint internal scrape. GM attempt one returned a grounded answer but mistyped one opaque actor handle by one character. Semantic compilation rejected the proposal before mutation. Explicit `Resume` preserved the interruption and started attempt two, which accepted. Orsa inspected the cloth and base, decided the first crate was safe where it stood, and instructed Mara to stack the second crate on top. No handle correction, parser, retry loop, fallback, provider switch, or backend-authored response was added.

Action four produced a setback while carrying the second crate. It remained level but jolted at the slick gunwale seam, leaving an internal grinding scrape, a raised ridge under the cloth, and an unstable two-crate stack. Action five made Orsa inspect her own cargo instead of letting the player invent guild handling. Orsa identified standing shards under the padding, rejected the unstable stack, and required the top crate to return to the quay before inspection. The loop is coherent and materially different at each step, but two complications in two carries make the ordinary job harsher than its fiction suggests. Human scores through action six are comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `4/5`; difficulty pacing remains an advisory.

Action six coordinated a two-person unload. GM left the first crate undisturbed on the barge and placed the second crate padded-side down on the wet quay. At world minute `16`, Orsa reached her actor cadence boundary. Actor Replanner accepted one strict GLM 5.2 object in `96846 ms`, remained at Glasscaster Quay, and proposed no `move_actor`, vehicle transfer, cargo-following move, or remote residue. Its first step executed immediately: Orsa worked the standing shards flat through the closed padding, leaving the crate on the quay with a smooth cloth surface and no chip-point. Narrator exposed that independent work as a separate visible beat. This is real-product acceptance for the no-implicit-cargo-movement portion of the repair.

The accepted plan still contains one causal spatial defect. Its next method lifts the settled upper crate and places it flush on the lower crate, while its trace claims the whole stack sits level on the deck planks. The lower crate remains tilted on the uneven support, and the method never repositions, shims, or otherwise changes it. The trace therefore invents a level support relation not caused by the step. r07 is frozen after six completed player actions. Actor Replanner now requires every spatial relation in `observableTrace` to be caused by handling stated in the same step; a tilted, open, damaged, or displaced support cannot become level, closed, secure, or correctly placed through an unmentioned intermediate action.

Focused Actor Replanner prompt and service verification passes `9/9`; backend typecheck passes. GitNexus again reports `LOW` upstream impact for `buildCampaignPlayActorReplanPrompt`, whose only production caller is `replan()`. Humanizer/deslop review keeps the added rule causal and setting-neutral: it names no crate, barge, character, preferred repair, or outcome. World version is `20`, world time is minute `16`, and runtime revision is `275`. Runtime sequences are contiguous from `1` through `275`, every event advances revision by one, SQLite integrity is `ok`, and foreign-key check is empty. The database contains one completed Opening and six completed player actions.

Screenshots are `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r07.session/screenshots/action-03-crate-placement-confirmed.jpg`, SHA-256 `73e42a8eb308e30b654c6f0c0627ff270f2ef92b1a4a40e821729b457c0ed3ce`, and `action-06-autonomous-crate-settlement.jpg`, SHA-256 `1e3eecdb76d6105a7132a934374928d7fb9f115211277646166581b90543c73b`. Product acceptance for the stricter causal-trace rule requires a later fresh Actor Replanner boundary; prior accepted r07 plans remain immutable.

## Bellglass r08 causal-chain replay

r08 copied the immutable zero-character Bellglass template and rebuilt Mara through the rendered Character UI. The generated draft remained a grounded itinerant floodgate mechanic's apprentice; manual review replaced one abstract `faction loyalty` contradiction with `guild obligations` before acceptance. The explicit `outer-ring / Outsider / Just arrived / Looking for work` conditions opened at Glasscaster Quay with Master Orsa Vail. Orsa offered ordinary paid work carrying cut shards to a canal barge. Opening Planner and Narrator accepted one strict GLM 5.2 object each in `245180 ms` and `40461 ms`.

The ready state immediately after Opening and before any player action is preserved as reusable checkpoint `output/playtests/campaign-world-templates/bellglass-tides-opened-orsa-r08-46bd6f32`. It was created with SQLite's online backup while the live backend remained open. The manifest records accepted content hash `46bd6f32e7f80ad614521fe8b3cecd69a863e4b37ef7513dcf5eb0ad8fb8b182`, setup phase `ready`, world version `13`, runtime revision `52`, world minute `0`, one accepted character, one completed Opening, zero player actions, SQLite integrity `ok`, and an empty foreign-key check. The copied database SHA-256 is `187c71279c1aae10ac3acbd6fc571163f77cde70bb24e25a597fb574978a712d`; the source campaign remains untouched. This checkpoint is the default source for subsequent Orsa replan replays and avoids paying the world-generation and opening costs again.

Action one deliberately waited fifteen minutes just outside the guild-stamped threshold, touched no stock, watched Orsa's handling, and listened for changes in the clamped fragment's warble. Judge, Game Master, Actor Replanner, and Narrator accepted once in `35335 ms`, `26132 ms`, `57552 ms`, and `46123 ms`. The primary result stayed observational, advanced world time to minute `15`, and described a consistent local procedure. Orsa reached a fresh cadence boundary under the r07 causal-trace rule. Her replanned first step kept the raw fragment clamped, ground a new channel, and left exactly that caused after-state. It proposed no actor-only cargo movement, remote residue, or implicit spatial correction. This accepts the narrower r07 support-relation repair on the real product.

The two-step plan exposed the same defect at a more general boundary. Step zero's method and trace leave a raw fragment with a freshly ground channel. Step one begins by wrapping `the finished amulet` and stores it on the shelf. No accepted event or intervening step performs the remaining shaping, polishing, tuning, or completion. The plan therefore changes the subject from raw to finished by naming it differently, even though each individual spatial trace is locally consistent.

Actor Replanner now treats ordered steps as one causal chain. Every later step must begin from accepted state or the preceding observable trace, and every transformed after-state must be caused by work named in that step's method. Raw, unfinished, tilted, open, damaged, displaced, or unpaid subjects cannot become finished, level, closed, repaired, correctly placed, or paid through a later label. This is one general state-transition invariant, not a parser, backend-authored intermediate action, semantic repair pass, retry, fallback, provider switch, or setting-specific example.

GitNexus reports `LOW` upstream impact for `buildCampaignPlayActorReplanPrompt`; its only production caller remains `ActorReplanner.replan`. Focused prompt and replanner verification passes `9/9`, and backend typecheck passes. Humanizer/deslop review found the replacement clearer and less example-bound than the spatial-only wording. It defines causal ownership in compact mechanical language and supplies no setting, character, preferred craft process, prose voice, or replacement outcome.

r08 is frozen after one completed player action. World version is `14`, world time is minute `15`, and runtime revision is `99`. Runtime sequences are contiguous from `1` through `99`; SQLite integrity is `ok`, and foreign-key check is empty. Every persisted model stage used `zai-coding-plan`, `glm-5.2`, strict objects, attempt one, and a valid schema outcome. Human scores are comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `4/5`. The scene feels inhabited and Orsa's technique is concrete, but the hidden plan cannot yet support a trustworthy continuing craft process. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r08.session/screenshots/action-01-causal-chain-gap.jpg`, SHA-256 `8e9d1ef343dddc02e07b55b6f9c38fa70e3aa319558234b27d44ec215d82c7b2`. Product acceptance requires the same fifteen-minute wait on a fresh copy of the saved opened checkpoint so a new Actor Replanner call reads the generalized contract.

## Bellglass r09 causal-chain product acceptance

r09 materialized the saved post-opening checkpoint rather than regenerating the world, character, or Opening. The rendered Play page immediately restored Mara at Glasscaster Quay, Orsa's offer, the opening narration, inventory, visible roster, and routes. The same freeform fifteen-minute observation was submitted through the real action dock. Judge, Game Master, Actor Replanner, and Narrator accepted one strict GLM 5.2 object each in `44347 ms`, `79162 ms`, `38804 ms`, and `120157 ms`. No retry, Resume, repair, fallback, provider switch, model switch, interruption, or invalid schema outcome occurred.

The fresh Actor Replanner artifact closes the r08 defect. Step zero resumes grinding the already clamped fragment, deepens its channel, checks the tuning fork, and leaves a smoother but still visibly unfinished fragment in the vice. Step one separately counts raw fragments against posted orders. It never calls the fragment finished, wraps or stores an uncompleted amulet, assumes an intermediate craft action, moves cargo with an actor, or invents remote residue. Each later state begins from accepted or preceding trace state. This is real-product acceptance for the generalized causal-chain contract.

The player-facing result is concrete and playable. Orsa answers the handling concern, demonstrates cooling, drying, carrying, and tuning behavior, and the warble changes in a legible way as she works. The next choices offer price inspection, a first carry attempt, travel, or a technical question, so the player retains both task engagement and exit routes. One prose advisory remains: Orsa first says to lift from underneath with both hands flat, while the observation later describes palms flat against each face. The intended no-edge grip is clear, but the physical hand placement could be stated more consistently before a carry roll depends on it. At the `1270x720` product viewport, the fixed action dock also covers lower portions of both scene columns; it remains usable but reduces simultaneous scene readability.

The human verdict is `TARGETED_PASS_CONTINUE`: comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `5/5`. Humanizer/deslop review preserves the physical-wording and viewport advisories instead of polishing them out of the evidence. The prose has a distinct workmanlike voice, specific sensory action, and no generic quest framing; its only material weakness in this turn is the small hand-placement inconsistency.

After Opening and one player action, world version is `14`, world time is minute `15`, and runtime revision is `110`. Runtime sequences are contiguous from `1` through `110`; SQLite integrity is `ok`, and foreign-key check is empty. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r09.session/screenshots/action-01-causal-chain-pass.jpg`, SHA-256 `1ede24fb3e7ceda39452d058d491d6cfd4fcb33fdef30d3e9d8ae80bad133ecc`. r09 remains the active manual campaign after one completed player action.

## Bellglass r09 actions 2-5: task memory, batch granularity, and setback recovery

Action two selected the offered first carry rather than scripting a diagnostic probe. Judge treated fragile resonant glass as a fair uncertain action, rolled `13` against difficulty `11`, and bounded it to two through five minutes. Mara used the demonstrated flat-palm grip, carried the shard level, and placed it intact on the barge in three minutes. Narrator reduced the result to one clean sensory paragraph instead of repeating the adjudication. The state was unambiguous: one intact shard on the barge and six still on the shelves.

Action three asked Orsa about remaining stock and the evening deadline. Orsa remembered the first completed carry without prompting: seven pieces began on the shelves, one was on the barge, and six remained for the last tide run at dusk. The same turn independently replanned a remote laborer from Brackish Crossing toward the gateworks to seek work from Warden Tessara Vohn. Narrator exposed only the co-located conversation at Glasscaster Quay. The remote actor, destination, motive, and future contact did not leak to Mara. This is direct proof that task memory and offscreen agency can coexist without treating the player as the world's information centre.

Action four tested whether a repetitive job required six identical UI turns. The freeform action asked to carry all six remaining shards one at a time with the established technique, without rushing, and to stop at the first crack, warble, warning, weather change, tide change, or barge change. Judge accepted this as one twelve-to-thirty-minute task instead of forcing six clicks. A roll of `4` against difficulty `12` produced a setback. Game Master advanced fifteen minutes, completed two carries, then stopped on the third when its hum became a sharp warble and a hairline crack opened. It did not continue through the remaining pieces or multiply the failure. Final counts remained coherent: three intact shards on the barge, three intact shards behind the bars, and one cracked shard on the quay stones.

The same elapsed interval moved another offscreen harvester from Bellglass Beds toward Harvester Launch as the tide returned. His fresh plan contains only his own route-bound movement and a wet bootprint trace; no basket, cargo, or tool follows him implicitly. Narrator again withheld this remote activity. The player's local setback therefore occurred inside a moving world without receiving omniscient narration.

Action five asked Orsa about the cracked piece. She crossed to the exact object on the quay, inspected its flickering fracture without moving it, admitted either a hidden flaw or a jolt could have caused the failure, declared it unusable for recording, and asserted a debt to be settled after the remaining work. Her response has a distinct practical voice and creates a genuine next decision: inspect the other pieces, dispute or quantify the debt, continue the job, or leave. The debt amount and final settlement are still only accepted event history, not yet a completed mechanical exchange; the next play segment must test whether that obligation survives negotiation and payment rather than claiming it now.

Humanizer/deslop review gives the five-action segment comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `5/5`. The prose is concrete, restrained, and causally ordered. Advisories are the earlier grip wording, one sidebar tense slip (`a hairline crack opened` inside present-tense narration), and the still-vague monetary meaning of `guild rates`. None prevents the player from understanding the current physical state or choosing a response.

The database contains one completed Opening and five completed player actions. All five Judge, five Game Master, three Actor Replanner, six Narrator, and one Opening Planner stages are accepted strict GLM 5.2 objects on attempt one. World version is `20`, world time is minute `37`, and runtime revision is `257`. Runtime sequences are contiguous from `1` through `257`; SQLite integrity is `ok`, and foreign-key check is empty. Screenshots are `action-03-world-aliveness-hidden.jpg`, SHA-256 `4281c808c920aaea029ba5ac3308bd085cdc0a64b23825d8de5d36756cb001f0`, `action-04-batch-carry-setback.jpg`, SHA-256 `4c044a2bcd0e18fc2d8250f7f7e3058dc0978f765a69bf3e68f1ed580018ce13`, and `action-05-orsa-debt-response.jpg`, SHA-256 `9a108b77fa9a53620072fd482924e12f25f1547487f4065b94da79b0929ebe5e`, under `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r09.session/screenshots/`. r09 remains active after the five-action checkpoint.

## Bellglass r09 actions 6-7 and native handle enums

Action six asked for the amount of the asserted debt. Orsa preserved three days of cutting and tuning, deduction from loading pay, no surcharge, and no interest, but refused to state coin before cast-off. The response remained internally consistent but did not make the transaction computable. Action seven therefore used explicit freeform leverage: Mara would touch no further shard without the guild day-rate, total debt, per-shard pay, earned amount for the three loaded pieces, final net balance, and a posted or ledger source.

Judge treated that demand as social pressure against Orsa's stated position rather than automatic information access. A roll of `3` against difficulty `13` produced a setback capped below full disclosure. The first Game Master response was semantically useful but mistyped Orsa's opaque actor handle by one character. Strict compilation rejected the proposal before mutation; the UI exposed `Resume`, and no correction, retry, repair, fallback, provider switch, or backend-authored response ran. Explicit Resume produced a valid second Game Master attempt. Orsa still refused numbers, correctly counted three intact pieces on the barge, three intact pieces behind the bars, and one cracked piece on the quay, and left Mara free to continue or walk away. Narrator preserved the physical tally and the failed negotiation without leaking backend diagnostics.

The same one-character handle defect had appeared once in r07, so it is no longer treated as incidental. Game Master's native structured-output schema now constrains every model-authored handle field to an enum built from admitted frame bindings for that exact call. UI-only choice handles and any misspelling are absent from the schema. The compiler still enforces required entity kinds and authority after generation. This prevents the invalid token at the provider contract boundary; it does not guess, autocorrect, alias, retry, repair, fall back, or accept an unauthorized handle. The prompt's `ALLOWED_HANDLES` now names the same admitted set as the native schema instead of every visible UI fact.

GitNexus reports `LOW` upstream impact for `campaignPlayGameMasterProposalSchema`, `createCampaignPlayGameMaster`, and the private Game Master prompt. Focused Game Master verification passes `27/27`, including dynamic-schema rejection of a one-character typo and a visible but unbound choice handle. Backend typecheck passes. Humanizer/deslop review found the evidence note direct and the schema change purely mechanical: it changes no dialogue style, outcome, world fact, NPC decision, or player prose.

After seven completed player actions, world version is `23`, world time is minute `41`, and runtime revision is `335`. Runtime sequences are contiguous from `1` through `335`; SQLite integrity is `ok`, and foreign-key check is empty. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r09.session/screenshots/action-07-orsa-refuses-terms.jpg`, SHA-256 `a0b4bc4d9ac27423877b01959b0067b61c939016876af0bd7bd48d9da4bc28f1`. Product validation for the enum-bound schema requires restarting the same r09 backend on the new code and completing another real Game Master turn; the accepted Resume attempt above still ran on the pre-change process.

## Bellglass r09 action 8: handle-enum acceptance and opening-trace duplication

The backend restarted on the enum-bound Game Master code while keeping the same r09 database and API address. A real browser reload restored the completed seven-action state. Action eight then selected the canonical route from Glasscaster Quay to Brackish Crossing. Judge and Game Master accepted strict GLM 5.2 objects on attempt one in `28157 ms` and `18096 ms`. Rulebook advanced fifteen minutes, moved only Mara from the quay to the crossing, and recorded one arrival scene. No schema interruption, handle repair, retry, fallback, or Resume occurred. This is product acceptance for the dynamic native handle enum.

Arrival exposed a new causality defect. The journal showed the exact same crossing aftermath twice: once at world minute `0` and again at minute `20`. Narrator sensibly collapsed the sensory material into one arrival paragraph, but the two authoritative journal entries remained visible. SQLite proves that the underlying events were different. The minute-zero Opening event was Pietr's scene-recorded inspection at Brackish Crossing. The minute-twenty event was Pietr's later `actor_moved` transition from the crossing to the gateworks. Visibility incorrectly substituted the Opening exposure seed's silt, pike-pole, and chalk-line trace into both events because they shared source actor and `local_aftermath` channel.

The repair preserves both causal events instead of deduplicating matching prose. Opening seed text may resolve only for an event belonging to the accepted Opening turn. Autonomous `move_actor` commands now persist the selected actor step's own `observableTrace`, and later local-aftermath projection renders that movement trace. A fresh campaign should therefore show the minute-zero inspection trace and, after Pietr leaves, a distinct trail of movement toward the gateworks. Game Master player movement remains unchanged and receives no model-authored movement trace.

GitNexus reports `LOW` upstream impact for `moveActorCommandSchema`, Actor Proposal `compileProposal`, `resolveCampaignPlayOpeningObservableTrace`, and visibility `publicEntry`. Focused contract, Actor Proposal, and Visibility verification passes `44/44`; backend typecheck passes. One pre-existing Actor Proposal fixture omitted the now-required Judge `requiredPossessionEffect`; the verification defect was repaired with `{ kind: "none" }` before the affected suite could exercise movement. Humanizer/deslop review keeps the two real events, wrong projection, and expected post-fix behavior distinct; it makes no claim that r09 itself is repaired.

r09 is frozen after eight completed player actions. World version is `25`, world time is minute `56`, and runtime revision is `388`. Runtime sequences are contiguous from `1` through `388`; SQLite integrity is `ok`, and foreign-key check is empty. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r09.session/screenshots/action-08-opening-trace-duplication.jpg`, SHA-256 `aadbc2bf51d8d589ac1313c2dff35f2490cc644644a7436896e2e13a519c0198`. Product acceptance requires a fresh r10 materialization from the saved post-opening checkpoint; the duplicate observations already persisted in r09 and are not rewritten.

## Bellglass r10 movement-trace product acceptance

r10 copied the saved post-opening r08 checkpoint, so world generation, character creation, and Opening did not run again. The rendered Play page restored Mara at Glasscaster Quay before any player action. Seven manual player actions then exercised three twenty-minute waits, travel to Brackish Crossing, direct contact with Pietr Grenn, travel to Outer Ring Gateworks, and a return to the crossing.

The first three waits did not freeze the world. Orsa finished one bellglass channel, set the finished piece on a cloth-lined tray, clamped a raw blank, and continued tuning it. Dain Solkeen independently moved from Bellglass Beds through Harvester Launch toward the gateworks. His accepted `move_actor` commands retained distinct model-authored traces rather than carrying an Opening seed. The three consecutive waits are causally readable, but Orsa's repeated wheel, brine, cloud, and humming details become noticeably repetitive by the third turn.

Action two stopped before mutation when Judge returned target and citation handles outside the exact native enum supplied by its current visible frame. The enum already existed and correctly rejected the object; no alias, correction, repair, automatic retry, fallback, provider switch, model switch, or backend-authored ruling was added. The rendered `Resume` control started explicit Judge attempt two, which accepted, and the same turn then completed. This is a recoverable model-contract interruption, but it remains a reliability and pacing defect: one otherwise ordinary wait required player intervention and took `306522 ms` end to end.

Action four reached Brackish Crossing at world minute `78`. The first arrival showed the flooded paving, submerged bellglass hum, and Pietr's presence without repeating the minute-zero Opening trace. Action five asked Pietr what he knew about the surge and whether the crossing was safe. Judge treated delivery of the ordinary question deterministically. Pietr answered only from his own earlier probing: the center paving still bore weight, the paired pilings were sound, dry passage required timber, and another seaward surge could deepen the water before nightfall. He then independently shouldered the pike pole and moved to Outer Ring Gateworks. The visible roster removed him immediately, the journal reported his departure once, and Narrator did not expose hidden goals or remote actors.

The same Pietr now has two causally different persisted traces. His minute-zero Opening event records freshly disturbed silt, a dripping pike pole, and a chalk waterline at Brackish Crossing. His minute-eighty `actor_moved` event records wet bootprints and dragged canal water staining towpath stones toward the gateworks, already drying at the edges. The second command contains that text in its own `observableTrace`; it does not resolve through the Opening seed. This is the required real-model proof for autonomous movement trace preservation.

Action six followed the open route to Outer Ring Gateworks. The destination had advanced before Mara arrived: fresh chalk measurements showed a buttress gap wider than its previous mark, and a newly pinned work order offered canal labor rates for reinforcement. Pietr and Warden Tessara Vohn were present. The two-beat narration exposed this accumulated local state without naming Dain, Netta, or their remote plans. Action seven returned to Brackish Crossing at minute `135`. The crossing was empty; Pietr's pike pole and chalk marks were gone while the water, submerged hum, and thickening clouds continued. The old silt/pole/chalk paragraph never reappeared as Pietr's movement aftermath. Together with the two distinct persisted event payloads, this accepts the opening-turn boundary and actor-carried movement trace on the real product.

Human playtest verdict is `TARGETED_PASS_CONTINUE`: comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `4/5`. Pietr's answer and departure are the strongest segment: they join prior world action, local knowledge, worsening weather, and autonomous movement into one playable scene. The main usability cost is latency. Accepted actor replans took as long as `143697 ms`, one accepted Narrator took `79024 ms`, and completed player turns ranged from `45966 ms` to `306522 ms`. The UI remained responsive and clearly marked in-flight actions, but those waits materially interrupt reading rhythm.

The database contains one completed Opening and seven completed player actions. World version is `25`, world time is minute `135`, and runtime revision is `351`; the next runtime sequence is `352`. SQLite integrity is `ok`, and foreign-key check is empty. Every accepted model artifact used `zai-coding-plan`, `glm-5.2`, and strict native objects. The only invalid artifact was Judge attempt one on action two; all other stages accepted without retry or repair.

Screenshots are under `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r10.session/screenshots/`: `action-04-first-crossing-arrival.jpg`, SHA-256 `4278339e9e0f074cd41fc55227fc666b32f3093fbedb54fca920c5f3ff6592fc`; `action-05-pietr-answer-and-move.jpg`, SHA-256 `0654c8aac46d9116699727845c6b6ad836a44d0dacefdd4e52ae74221d9c6fb9`; `action-06-gateworks-page-01.jpg`, SHA-256 `da4884d8b6019679ec08b743c09b25a80b81a27566680143efd79f3ccec8e35b`; `action-06-gateworks-page-02.jpg`, SHA-256 `83e3ffbe1d7d54698d88eadbce5bac028674ae90cb49f4ccbd19fdae9c38453b`; and `action-07-crossing-after-pietr-left.jpg`, SHA-256 `b40a47ad5241d396161288f0da76afa5471edc0fdb6f2d24ecffc10f0012ea2a`.

Humanizer/deslop review preserves the concrete sequence, model interruption, repetition, and latency evidence. It removes no adverse finding and adds no claim of long-play acceptance. The note avoids abstract quality language where exact scene state, event payloads, durations, or screenshots provide stronger evidence.

## Bellglass r10 actions 8-18 and stale actor-plan diagnosis

Actions eight through seventeen continued in the same r10 database instead of starting a favorable short scenario. Mara returned to Outer Ring Gateworks, asked Warden Tessara Vohn about the newly posted reinforcement work, inspected the widening buttress fracture, cleared compromised stone, failed to make stable gap-fill from coarse slabs, inspected a salvaged winch bar, tested its unsecured hold, and worked with Pietr Grenn to try stone chocks beneath its bearing points. The sequence preserved missing supplies throughout: no mortar, clay, sealant, timber, proper iron, or authorization appeared merely because the player needed them. A strong clearing result exposed sounder stone but left seepage unchanged. Two later setbacks established that the local slabs were too coarse for either graded fill or stable shims and that the pitted bar would slide without fastening.

The play was causally readable and more game-like than the preceding observation loop. Pietr independently opened the seized winch housing, exposed a usable but corroded bar, seated it across the fracture, and then coordinated with Mara by holding it steady while she drove chocks. Warden remained a separate decision-maker: she put Mara on the daily roll, prohibited further levering after the deeper fracture appeared, and named Master Orsa Vail as a possible source of binding agents without promising that Orsa would cooperate or possess timber and iron. Suggested actions occasionally lagged the best available decision, but freeform input supported stopping a failed tactic and seeking a new supply route.

Action eighteen asked where proper material could be obtained and what had to remain safe before anyone left. Game Master committed a coherent answer: Pietr must keep the loose bar braced with his pike pole, the lower loose fill stays only to slow seepage, and nobody cuts further; Orsa might have worked material or binding agents, while court-controlled merchants still require authorization or coin. A previously accepted Pietr plan then executed its old next step in the same actor phase. That step claimed angular slabs locked the bar firmly and left the pike pole leaning against a winch post. It directly contradicted the later accepted player results that the same coarse slabs could not seat and the newly committed instruction that Pietr must continue holding the bar. Narrator rejected the incompatible packet as `narration_invalid`; no final prose was accepted.

The root cause is scheduler freshness, not narrator wording. Persisted plan preconditions covered actor placement, route, goal, pressure, and condition state, but did not invalidate a future step when the actor perceived a later external accepted event after the plan was authored. The scheduler therefore treated a semantically stale next step as executable even though its actor frame contained the newer events.

Due-actor selection now introduces an explicit `world_advanced` replan boundary. If an actor knows a later externally sourced direct-perception event created after its active plan, the old next step cannot compile or settle unchanged. The ordinary single Actor Replanner call receives the newest accepted world events and replaces the course before action. Events authored by the same actor's prior settled steps do not trigger this boundary, so a causal multi-step plan can continue through its own accepted results. This uses persisted event provenance and plan timestamps; it does not parse prose, compare embeddings, repair a proposal, retry a model, switch providers, or let backend code write the replacement action.

GitNexus reports `MEDIUM` upstream impact for scheduler `buildActorFrame` through five direct frame consumers and `LOW` impact for current-turn perception extraction and the Actor Replanner prompt. Focused scheduler, proposal, replanner, and prompt verification passes `31/31`; backend typecheck passes. The persisted scheduler fixture proves that a later external direct-perception event produces `world_advanced`, while an explicit pure selection fixture proves the same boundary without database setup. One pre-existing scheduler Judge fixture omitted required `requiredPossessionEffect`; it was repaired with `{ kind: "none" }` before the scheduler suite could run.

Humanizer/deslop review keeps the new prompt rule compact and mechanical: `world_advanced` names why the prior course stopped and requires continuity from the newest accepted event. It supplies no setting-specific object, character decision, replacement method, outcome, or prose style. The evidence note preserves the contradiction and interruption instead of describing r10 as a passing twenty-action lane.

r10 is frozen with one completed Opening, seventeen completed player actions, and action eighteen interrupted after visibility projection. World version is `38`, world time is minute `304`, runtime revision is `790`, and the next runtime sequence is `791`. SQLite integrity is `ok`, and foreign-key check is empty. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r10.session/screenshots/action-18-stale-actor-plan.jpg`, SHA-256 `11c168d603db78a5804c060f628eba172343a5a97aadf986d68da7eefec2e73a`. Human scores for the Gateworks segment are comprehension `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `4/5`; repeated multi-minute latency remains the primary experiential cost. Product acceptance for `world_advanced` requires a fresh post-opening clone because r10 already committed the stale actor event and history is not rewritten.

## Bellglass r11-r14: `world_advanced`, typed movement, and opening-seed ownership

r11 reused the saved post-opening Bellglass checkpoint and completed two manual actions at Glasscaster Quay. Orsa finished a tide-warning amulet, set it aside, and continued work on the remaining stock without pulling remote actors into the scene. The segment was coherent and readable, but its `What changed` and `The moment` columns repeated much of the same language. Because Orsa's admitted plan had already completed, r11 did not directly exercise `world_advanced`; it remains useful prose and continuity evidence, not acceptance for the scheduler repair.

r12 copied the unchanged r01 Gateworks state and deliberately advanced a longer real campaign instead of constructing a favorable fixture. It exposed two independent authority defects. First, Game Master twice returned an `adjust_actor_possession.name` longer than the explicit 120-character schema limit. Both attempts stopped as `model_contract_invalid` before mutation. The prompt now states the same limit and directs state, contents, provenance, and other detail into `summary`; the next operator-signed attempt produced names of 95 and 66 characters while retaining the detail in summaries. Second, an autonomous move intent that named only a route compiled as `record_world_event` travel prose because movement resolution looked only for a location target. The journal said Tessara walked to Harvester Launch while SQLite still placed her at Gateworks. Pietr then correctly crossed a `world_advanced` boundary but replanned from that false event, so r12 is rejected rather than repaired in place.

Actor proposal compilation now resolves an explicit local open route first and derives its canonical destination from that route. An optional location target must agree. An unresolved or mismatched move raises `proposal_state_invalid`; it cannot degrade into a scene or interaction saying that travel occurred. The focused service fixture now proves that a route-only move emits `move_actor`, and a bad route leaves no proposal row. The Game Master prompt and actor proposal changes add no alias, prose parser, compatibility path, model retry, fallback, provider switch, or backend-authored narrative.

r13 repeated the Gateworks route through the real Play UI. Pietr's old version-two plan was blocked and replaced by version three after he perceived later player and Warden events. The visible dialogue was strong: he distinguished lift-and-pack work from surface shoring, preserved the lack of released Court funds, and did not repeat the old question unchanged. Persisted proposal inspection nevertheless found that the first new step reused the Opening consequence summary about loose planks at Brackish Crossing. The Opening seed was keyed only by source actor, source goal, and zero settled steps, so every fresh plan for the same actor and goal could masquerade as the original Opening consequence. r13 is therefore rejected despite its good visible narration. Its screenshot is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r13-route-move-world-advanced.session/screenshots/action-07-world-advanced-visible-but-seed-reused.png`, SHA-256 `86a936b6c9bd2e83a8243b3d63ce8b3e72307bdc9582970ba1d61e4862e2d325`.

The seed boundary now additionally requires `planVersion === 1` in both exposure selection and consequence-summary ownership. A replacement plan therefore uses its own method, stakes, exposure, and observable trace. The focused regression creates a legal completed version-one predecessor and active version-two plan for the seed actor; version two produces its own wait summary and ordinary finite local aftermath rather than the Opening summary or predicate. This is an ownership restriction, not deduplication or text comparison.

r14 copied the same unchanged r01 database again; no world, character, or Opening generation ran. Seven manual Play actions advanced from world minute `101` to `191`. The scheduler processed the real due order rather than promoting Pietr for the test: Netta acted, Dain and Orsa replanned, Tessara replanned, and later Netta replanned while other due actors accumulated explicit debt or capacity deferrals. Player-visible prose stayed grounded in the nine measured seep channels, the seven-finger central crack, and the posted twelve timber baulks and eight iron straps. The exact scene remained stable during observation; remote actor work stayed hidden.

Tessara then changed the local world without player authorship. Her accepted version-two step posted `Warden's Discretion — Half-Day Laborer Rate` for central-buttress shoring. Five minutes later Pietr was claimed with admitted plan `actor-plan:f10918a663ff651570a383e62f4ce478`. `world_advanced` blocked that version-two plan and Actor Replanner produced version three `actor-plan:b406c1d3d8bf616be686a754dbb3d022`. The old step asking Tessara to create a work order never ran. The new course recognizes that the order now exists, formally accepts it, stages timber baulks and bundled iron straps, and reserves the later three-to-five-hour shoring step. Its accepted Rulebook event summary and observable trace both come from the new step; neither contains the Brackish Crossing Opening seed. Pietr remains mechanically present at Gateworks, matching the non-movement command and rendered roster.

The rendered result is causally legible and worth continuing. Pietr explicitly connects his earlier rule, `no order, no coin`, to the changed world: the Warden's notice now provides real if inadequate pay, so he takes the posted half-day work while preserving the unresolved Court-rate conflict. The journal separately exposes the fresh chalk tick and staged materials. This accepts the `world_advanced` and Opening-seed boundaries on the real product. Comprehension is `5/5`, prose readability `4/5`, agency `5/5`, world aliveness `5/5`, and desire to continue `5/5`. Model latency remains substantial. A later inspection established that the apparent duplicate `The moment` paragraph came from the accessibility snapshot exposing both the visible paragraph and its `sr-only` live region; the rendered visual block contains one copy.

The r14 database contains one completed Opening and eleven completed player actions, seven of them added by this replay. World version is `30`, world time is minute `191`, and runtime revision is `532`. SQLite integrity is `ok`, and foreign-key check is empty. The decisive Judge, Game Master, Actor Replanner, and Narrator stages each accepted one strict GLM 5.2 object on attempt one in `24320 ms`, `25431 ms`, `141306 ms`, and `61916 ms`. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r14-world-advanced-seed-boundary.session/screenshots/action-07-world-advanced-seed-boundary-pass.png`, SHA-256 `8e1ca1d8084dc24b5b31f3eb09267e49210b861dc4b66192723c4f0c7e5d251a`.

Prompt-craft review keeps the 120-character possession-name rule identical to the schema and moves only descriptive detail into the existing summary field. Humanizer/deslop review keeps the new prompt and evidence notes literal, causal, and setting-neutral where they define mechanics. It preserves the two rejected campaigns and latency instead of polishing them into success language.

## Bellglass r14 actions 12-20: repair play, actor rejection, and recovery

The same r14 campaign continued through nine more manually chosen actions rather than restarting from a favorable checkpoint. Actions twelve and thirteen established exact material and labor facts at Outer Ring Gateworks: twelve sound timber baulks, eight iron straps, nothing yet worked, and a clear division in which Mara carries and holds while Pietr positions and fastens. Two subsequent attempts slipped without progress. Asking Pietr to change the method produced a grounded alternative, using an upper strap as temporary support, but wiping and shielding the bore points exposed continuing internal seepage. The sequence preserves physical causality and cooperative dialogue, yet actions fourteen through eighteen reveal a pacing defect: each reasonable adjustment receives another immediate blocker instead of a proportionate gain or decisive failure. The repair loop is readable but not yet satisfying.

Action nineteen left that loop for a self-directed goal and followed Dain Solkeen's independently accepted movement to Brackish Crossing. Rulebook moved Mara through the canonical directed route, and the rendered destination contained Dain, knee-deep floodwater, salt-crusted pilings, and no installed plank bridge. One Actor Replanner response exceeded the existing 500-character `method` schema maximum and stopped before mutation. The prompt now states the same maximum for every non-null `method`, `stakes`, and `observableTrace`; the rendered Resume created the explicit second attempt, which accepted. No field was truncated, rewritten, defaulted, or retried invisibly.

Action twenty asked Dain why he had left the gateworks. Judge and Game Master accepted once and committed a coherent answer: the tide board showed the surge peaking inside the next glass, the south passage remained clear, water was already climbing past Pietr's timber, and Dain prioritized bellglass work that would become impossible after the flats drowned. A due Kael replan then selected a real local route in the wrong direction. The accepted plan persisted, but proposal compilation correctly found that the route origin did not match Kael's current placement. The old lifecycle threw `proposal_state_invalid` and left the job indefinitely `claimed`, so the turn remained at `primary_settled` with no terminal actor outcome.

The repair closes both boundaries without prose repair. Actor Replanner compilation now requires every move step to use exactly one supplied directed route from the actor's current or preceding-step location; a reverse route becomes `model_contract_invalid` before any replacement plan is stored. A pre-existing invalid active step compiles to no proposal and atomically commits a proposal-less `rejected` job, a blocked plan, and a debt-bearing future retry with zero world-version advance. Scheduler settlement accepts that terminal shape only when no proposal row exists and the scheduled plan is blocked. Focused proposal, scheduler, replanner, prompt, and application verification passes `51/51`; backend typecheck passes. The broader `turn-runtime.test.ts` gate is currently unavailable as evidence because its shared Judge fixture fails native contract validation before actor processing in 32 of 36 cases; those failures do not exercise this change and are not reported as passing.

Recovery exposed one further product defect. After the invalid actor job settled, visibility committed but the turn stopped at `visibility_projected` with no worker lease and no Narrator attempt. Turn-service discovery intentionally does not start external work, while application startup recovery handled only deterministic readiness, leaving no product path to start a never-attempted external stage. Application recovery now schedules exactly one `external_ready` stage. It still leaves `external_in_flight` untouched and every interrupted external attempt behind explicit Resume. Restarting the backend on that contract started Narrator attempt one; GLM 5.2 accepted in `59000 ms`, and action twenty completed through the normal Play UI.

The database now contains one completed Opening and twenty completed player actions. World version is `43`, world time is minute `270`, and runtime revision is `868`; runtime event sequences are contiguous from `1` through `868`, while action twenty's turn events are contiguous from `1` through `38`. Judge, Game Master, Actor Replanner, and Narrator for action twenty each used requested and actual `zai-coding-plan` / `glm-5.2`, strict attempt one, in `20648 ms`, `38967 ms`, `57387 ms`, and `59000 ms`. Kael's job is `rejected`, its `proposal_id` is null, the version-three plan is `blocked`, and no proposal row exists. SQLite integrity is `ok`, and foreign-key check is empty.

The rendered answer is strong continuity evidence: Dain remembers the gateworks, distinguishes its durable work from his tide-limited opportunity, and acts from a motive independent of Mara. No hidden Kael identity, route error, plan, or rejection appears in player prose. The screen returns four actionable choices and unlocked freeform input. The DOM accessibility snapshot contains both the visible paragraph and its `sr-only` live-region copy, but the rendered visual block contains one paragraph; repair-loop pacing and model latency remain the larger playability findings. Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r14-world-advanced-seed-boundary.session/screenshots/action-20-dain-crossing-recovery.png`, SHA-256 `49f93d51a3cecdd17594f9d7c4d166f7949edadd1bd414993dfa845b25ff76fa`.

Prompt-craft, humanizer, and deslop review keeps the two Actor Replanner additions as literal schema and route-direction rules. They name the existing 500-character boundary and directed-route invariant without supplying setting details, character decisions, replacement prose, or a preferred outcome. The evidence note preserves the explicit action-nineteen Resume, the action-twenty lifecycle failure, unavailable broad runtime fixture, repair pacing defect, DOM duplication, and latency rather than converting the twenty-action checkpoint into pristine acceptance.

## Bellglass r14 actions 21-24: playable bellglass loop and cross-scene hard stop

Action twenty-one followed Dain's tide-limited work lead rather than returning immediately to the Gateworks repair. Dain explained that pale bellglass grows in the drowned flats, holds the last breath of people lost there, and is used by the Canal Court for ward, debt, and inheritance testimony. He then left Brackish Crossing for Glasscaster Quay under his own accepted actor step. The visible roster removed him, the journal reported the departure once, and the next actions responded to the now-empty crossing. The segment supplies a concrete world hook and autonomous motive without centering Dain on Mara.

Actions twenty-two and twenty-three form the strongest small gameplay loop in r14. Mara first probed the submerged canal bed bare-handed. A pale edge cut her thumb, slipped deeper into the mud, and the tide climbed one finger on the pilings. The next move was manually written rather than selected from a canned choice: Mara tied the smallest wrench from her visible hand-tool roll to her light cord, braced at a piling, and dragged the improvised rake through the same silt. The second pass freed one intact pale bellglass shard, persisted exactly once in her inventory, while the tide climbed two more fingers. Curiosity produced a fair setback, the setback taught a method change, and the adapted method produced a tangible possession plus continuing pressure.

Action twenty-four followed the canonical open route to Glasscaster Quay. Mechanical placement, visible actors, possessions, and the arrival event were correct: Dain and Master Orsa Vail were present, and Mara still carried the recovered shard. Visibility then projected a local-aftermath event that did not belong to the quay: wet bootprints beside a salt-crusted piling, facing flooded paving full of canal weed at Brackish Crossing. Narrator repeated those crossing details as if they lay beside the quay workshop. This is a hard spatial contradiction, so r14 is frozen at twenty-four completed actions rather than counted toward a thirty-action passing lane.

The typed ledger locates the defect before visibility and Narrator. Dain was mechanically present at `glasscaster-quay`, but an older version of his active plan executed a non-move discovery step targeting `brackish-crossing`. Proposal compilation placed `local_aftermath` at the actor's current location while retaining the remote location target and model-authored crossing trace. Rulebook therefore stored one scene event whose exposure named Glasscaster Quay while its affected location and sensory content named Brackish Crossing. Visibility correctly followed the bad exposure; the invalid spatial step should never have become a proposal.

Actor Replanner compilation now requires every executable non-move step with a location target to use the location established for that step. It cannot act, inspect, work, or leave a trace at an earlier or remote location after moving away. Proposal compilation independently enforces the same invariant for already persisted plans: a mismatched step becomes the existing proposal-less `invalid_step` rejection, blocks the plan, adds retry debt, and creates no proposal, command, receipt, event, exposure, or prose. Focused Actor Replanner, proposal, prompt, scheduler, and application verification passes `53/53`; backend typecheck passes.

All action twenty-one through twenty-four model stages used requested and actual `zai-coding-plan` / `glm-5.2`, strict attempt one, with no repair, fallback, or provider switch. Action twenty-three's off-screen replan took `239093 ms`, materially longer than its `46032 ms` Judge, `45515 ms` Game Master, and `69970 ms` Narrator stages; latency can dominate a satisfying local action. At the freeze, the database contains one completed Opening and twenty-four completed player actions, world version `51`, world minute `322`, and runtime revision `1046`. SQLite integrity is `ok`, and foreign-key check is empty.

Screenshot evidence is `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r14-world-advanced-seed-boundary.session/screenshots/action-24-cross-scene-aftermath-failure.png`, SHA-256 `56bbba9aa35349c908a5ea02ebbc44c3ca3fbefd5c0aaedc081f39fdd3b89ec1`. Prompt-craft, humanizer, and deslop review keeps the new spatial instruction mechanical and setting-neutral. It supplies no character choice, destination, outcome, or prose, and this note preserves both the enjoyable shard loop and the hard-stop contradiction instead of averaging them into a passing score.

Game-design interpretation uses the read-only ProjectGameGod canonical markers `pgg:knowledge:found-003`, `pgg:knowledge:found-004`, `pgg:knowledge:found-005`, `pgg:knowledge:found-008`, `pgg:knowledge:found-009`, `pgg:knowledge:interactive-narrative`, `pgg:knowledge:npc-game-ai`, and `pgg:knowledge:playtest-telemetry-evidence`. Under those evidence boundaries, r14 establishes an enacted product trace and an analyst interpretation, not a participant-experience effect or population claim. The shard sequence has visible information for intention, an attributable failed method, feedback that supports adaptation, a player-authored replacement method, a durable possession consequence, and rising tide pressure that changes the next decision. The action-twenty-four mismatch breaks the authoritative-state-to-observation chain and consequence attribution. Terms such as `felt agency`, `fun`, `meaningful`, and `well-paced` remain unproven without a named human participant, exact prompts, and bounded report context; r14 cannot satisfy the plan's human pristine-lane threshold by itself.

## Bellglass r15 actions 1-10: work contract, setback, adaptation, and checkpoint

r15 copied `bellglass-tides-opened-orsa-r08-46bd6f32` at its ready boundary. The copied database SHA-256 was `187c71279c1aae10ac3acbd6fc571163f77cde70bb24e25a597fb574978a712d`, the copied config SHA-256 was `988f0419d97f354a4e444435ba8b374903f4be6e58d488af3c0430b16785c6c8`, and the restored UI showed Mara, Orsa, four possessions, one completed Opening, and zero player actions. World generation, character creation, and Opening did not run again.

Actions one through four formed a complete learnable work loop. Orsa first named a six-copper crate rate, evening-tide deadline, tally-sheet control, and breakage deduction. Mara's first attempt to wrap a smooth vibrating shard failed because the cloth bunched, the piece twisted, and its edge caught an iron bar. The failure sheared one certified shard rather than erasing progress or producing an abstract penalty. Mara then admitted the damage and asked for both the exact deduction and a demonstration before touching more stock. Orsa charged twelve coppers and demonstrated a reproducible method: double-fold the cloth, keep the flat side down, use the gap between the third and fourth bars, keep both hands on the near side, and finish the wrap on the quay. Repeating the operation with that information moved one intact wrapped shard to the barge.

Actions five and six tested whether the work remained computable across turns. The first answer explained why certified stock cost twelve coppers but did not define the paid unit. A direct question pointing to the tally sheet and the single bundle then produced the missing contract: eight shards per crate, thirty-two intact shards behind the bars, four possible crates, twenty-four coppers gross, and twelve net after the deduction. No backend calculation or prose repair supplied these facts; they were committed by the normal Game Master and preserved by Narrator.

Action seven used one freeform batch intention for the seven remaining shards in the first crate, with explicit stop conditions for catches, cracks, tone changes, warnings, barge movement, weather, or tide. Rulebook advanced sixteen minutes, completed seven clean repetitions, stopped at the requested crate boundary, and persisted eight tied shards with twenty-five remaining. Action eight confirmed a charcoal tally mark, six coppers earned, twelve owed, and a current balance of negative six. Action nine briefly examined Orsa's half-ground disc. It exposed only visible roughness, the thinned curve, wet grit, and an unfinished warble; it did not grant hidden craft knowledge. Action ten used the same bounded batch contract for a second crate. Eight more shards completed cleanly in twenty-four minutes, leaving sixteen tied on the barge and seventeen intact behind the bars.

This ten-action trace supplies strong analyst evidence for intention, causal action, attributable failure, actionable feedback, adaptation, payoff, and persistent progress. It does not by itself establish felt agency, fun, meaningfulness, or population effects; those remain human-participant claims under `pgg:knowledge:found-003` and `pgg:knowledge:playtest-telemetry-evidence`. The main adverse finding is latency: even accepted attempt-one stages regularly require tens of seconds, and reading rhythm is interrupted while the action remains locked. The spatial guard fixed after r14 has not yet reached a fresh remote non-move boundary in r15, so this checkpoint is not product acceptance for that repair.

At action ten the database contains one completed Opening and ten completed player actions. World version is `25`, world time is minute `59`, runtime revision is `375`, the next runtime event sequence is `376`, and runtime sequences are contiguous from `1` through `375`. SQLite integrity is `ok`, and foreign-key check is empty. The stored model ledger contains ten accepted Judges, ten accepted Game Masters, three accepted Actor Replanners, eleven accepted Narrators including Opening, and one accepted Opening Planner. Every row uses requested and actual `zai-coding-plan` / `glm-5.2`, strict objects, attempt one, and valid schema outcomes. No Resume, schema repair, provider switch, model switch, or fallback ran.

The action-ten state is preserved with SQLite's online backup at `output/playtests/campaign-play/pristine-30-glm52-bellglass-46bd6f32-r15-spatial-guard.session/checkpoints/action-10/state.db`, SHA-256 `74239490abed331f44b78f53184406f9bce0888d04bafd03b8b9d9945b622e51`. The copied config SHA-256 is `988f0419d97f354a4e444435ba8b374903f4be6e58d488af3c0430b16785c6c8`; the backup independently reports integrity `ok`, no foreign-key violations, world version `25`, runtime revision `375`, and ten completed player actions. Screenshot evidence is `output/playtests/campaign-play/pristine-30-glm52-bellglass-46bd6f32-r15-spatial-guard.session/screenshots/action-10-second-crate-complete.png`, SHA-256 `6f49fc8cb8e5305dcbbc7f639940172859db449ce0d076df953b2e3765917d24`.

Humanizer and deslop review keep the sequence concrete and distinguish model-committed facts, analyst interpretation, and unproven participant effects. The note retains the initially incomplete unit contract, latency, unexercised spatial boundary, and accessibility-tree duplication instead of treating ten clean actions as pristine-lane acceptance.

## Bellglass r15 actions 11-13: scene continuity and Game Master contract recovery

Action eleven left the two completed crates and seventeen remaining shards at Glasscaster Quay and followed the canonical route to Brackish Crossing. Judge and Game Master accepted once. Pietr Grenn then reached a due replan boundary. Actor Replanner attempt one returned `model_contract_invalid` after `100284 ms`; the explicit Resume produced accepted attempt two in `146625 ms`. The invalid artifact was not persisted, so it is not attributed to the new spatial rule. The accepted replacement is mechanically sound: Pietr is present at Outer Ring Gateworks, and both executable non-move steps target that same location. His notice-board and buttress traces remained at Gateworks. The rendered Crossing arrival was empty and contained only knee-deep water, driftwood, open pilings, and the submerged hum; no remote trace leaked into Mara's scene.

Action twelve examined storm debris instead of repeating r14's bellglass recovery. The result found waterlogged planks, a soft split canal-board, knotted weed, and a fresh impact gouge, while explicitly preserving that no bridge, lashing, mark, or concealed object existed. The next choices remembered that Orsa's crates remained stacked for evening tide. This preserves both local negative evidence and the unfinished remote work state without turning the debris into a mandatory reward.

Action thirteen tried to wade toward the far side. Judge accepted one uncertain ruling in `31553 ms`; the roll was `1` against difficulty `12`, producing a setback. Game Master attempts one and two stopped before mutation in `77167 ms` and `38707 ms`. Both provider outputs tried to use `set_actor_condition` but omitted its required exposure, invented condition and operation enum values, and added forbidden `affectedHandles`. The prompt named the effect kind but did not define its exact fields. A setting-neutral contract line now lists the existing fields, condition enum, operation enum, and the absence of `affectedHandles`. Attempt three then retained only one error: it supplied exposure as an array because the prompt described predicate fields but never described their wrapper. A second contract line now requires one exposure object, either protected or projectable with a predicates array.

After restarting on both prompt corrections, explicit Game Master attempt four accepted one strict GLM 5.2 object in `47483 ms`. It advanced eight minutes, committed an actorless discovery event, and set Mara's existing `strained` condition through a protected exposure. The model-authored summary says that canal weed caught her ankle, submerged bellglass cut through her boot into her shin, and she retreated to the near-side pilings without reaching the far bank. Actor Replanner and Narrator then accepted once in `83719 ms` and `48401 ms`, and the real Play UI returned unlocked choices. Each Game Master attempt made one provider call; attempts two through four required visible Resume. No repair, text fallback, provider switch, model switch, backend-authored condition, or backend-authored prose ran.

Focused Game Master verification passes `27/27`, and backend typecheck passes. GitNexus reports `LOW` upstream impact for the private prompt function, with the Game Master plan path as its only direct consumer. Prompt-craft review treats both additions as missing schema contracts rather than gameplay direction. Humanizer and deslop review keep them concise and setting-neutral; they prescribe no setback, injury, prose, or preferred effect when the existing condition enum does not fit.

After action thirteen the database contains one completed Opening and thirteen completed player actions. World version is `31`, world time is minute `99`, runtime revision is `512`, and the next runtime event sequence is `513`; runtime sequences are contiguous from `1` through `512`. SQLite integrity is `ok`, and foreign-key check is empty. Screenshot evidence is `output/playtests/campaign-play/pristine-30-glm52-bellglass-46bd6f32-r15-spatial-guard.session/screenshots/action-13-wade-setback-contract-recovery.png`, SHA-256 `6721842da7b911139146ff4706d2b0d14ce7db963849869ad578ea78a45a761a`.

## Bellglass r17-r18: willing companion target boundary

r17 started a fresh manual character path from the accepted Bellglass world. Tavia Rell found no paid work from Kael, Sestra, or Orsa, then reached a pre-existing loose plank at Brackish Crossing. Pietr Grenn was repairing it while looking for the same paid work. Tavia helped from dry footing, and Pietr independently agreed to walk with her to Warden Vohn's gateworks posting. The ninth input named Pietr, the open route, the destination, and the limited intent to inspect rather than accept work. Judge cited Pietr and the prior consent event and described him as a willing companion, but omitted his actor handle from `normalizedIntent.targets`. Game Master then proposed an origin interaction and two typed movements. The strict compiler rejected the untargeted NPC performer before mutation. r17 remains frozen at eight completed player actions, world version `23`, world minute `27`, with the failure screenshot at `output/playtests/campaign-play/pristine-60-glm52-bellglass-46bd6f32-r17-evidence.session/screenshots/action-9-contract-failure.png`.

The Judge prompt now states the existing target rule explicitly. A visible NPC who participates as an addressee, companion, or performer must be an actor target, and a named willing travel companion must appear beside the destination. Citation alone does not grant that authority. Game Master and Rulebook checks remain unchanged, so an untargeted, remote, unwilling, non-agent, or incapacitated NPC still cannot move. Humanizer and deslop review keeps the addition literal and setting-neutral. It supplies no consent, destination, action, dialogue, or outcome.

r18 is a diagnostic copy of r17 restored to the accepted action-eight boundary. The same ninth input ran through the rendered Play UI. Judge returned destination and Pietr actor targets in one valid GLM 5.2 attempt. Game Master committed five minutes, Pietr's origin interaction, Tavia's movement, Pietr's separate movement, and the arrival scene in one valid attempt. Actor Replanner and Narrator also accepted once. Their durations were `15677 ms`, `93002 ms`, `66105 ms`, and `28648 ms`; all used native JSON with no fallback. World time advanced from minute `27` to `32`, world version from `23` to `26`, and SQLite places both actors at Outer Ring Gateworks. Integrity is `ok`, and foreign-key check is empty. Screenshot evidence is `output/playtests/campaign-play/diagnostic-companion-target-r18.session/screenshots/action-9-companion-arrival.png`, SHA-256 `d326530e3efc5c37c9f87ad8776c8f3cabe9909e3c7069af662fe0651b057a40`.

The arrival prose is coherent and causal. The second beat adds a taut cord, a widening buttress crack, and fresh measured chalk marks, which makes the gateworks read as an active work site. The next-action set still has a relevance defect. It offers inspection of the notice board but omits the obvious contact with Warden Vohn and instead asks Pietr about the posting again. Under the ProjectGameGod agency profile, enacted causality and practical availability now pass for companion travel, while option relevance remains weak and needs a separate change.

## Bellglass r19: scene-relevant action selection

The omission came from the producer boundary rather than Narrator prose. `availableIntents` admitted only one route and the first actor, then truncated the entire set to four before GLM saw it. At the gateworks, Pietr sorted before Warden Tessara Vohn, so the model could not select the scene's obvious authority figure. The packet now carries the complete bounded mechanical catalog: observe, the contextual attempt when allowed, every visible open or restricted route, every visible actor, and wait. The catalog cap is nineteen, derived from the existing eight-route and eight-actor visibility caps plus the three non-entity intentions. The public narration and UI remain capped at four.

Narrator selects exactly four unique catalog indexes, or every entry when fewer than four exist. Code resolves each index to the frozen intent, owns the opaque handle and label prefix, and publishes only the selected subset. The model cannot invent or alter a handle, kind, or target. Admission still requires both the rendered suggestion and its frozen catalog entry, so a packet-only candidate is not player authority. There is no ranking heuristic, backend-authored action prose, retry, repair, text fallback, provider switch, or compatibility path.

r19 is a diagnostic copy of r17 restored to the accepted action-eight boundary. The same signed ninth action ran through the rendered Play UI. The resulting catalog contained seven intentions: observe, contextual attempt, two routes, Pietr, Warden Vohn, and wait. GLM selected, in order: ask Warden Vohn about unposted wage rates; ask Pietr about the posted low-tide windows; examine the rusted winches; and trace seepage along the lower gates. Neither backward route nor wait occupied a slot. The previously missing contact is now the first choice, while the remaining three choices provide distinct social, investigative, and practical continuations.

The two visible beats are concise and causal. They establish the five-minute arrival, the board's requested materials and missing wage details, then the measured cracks, taut cord, and high-tide seepage. The prose neither invents payment nor accepts work for Tavia. Human scores are comprehension `5/5`, prose readability `5/5`, agency `5/5`, world aliveness `4/5`, and desire to continue `5/5`. Under the ProjectGameGod agency profile, option relevance, practical availability, enacted causality, and persistence all pass for this scene; the trace is supporting evidence, while the human reading supplies the play-quality verdict.

Judge, Game Master, Actor Replanner, and Narrator all accepted attempt one through `zai-coding-plan` `glm-5.2` strict objects with valid schemas. Durations were `44934 ms`, `125627 ms`, `48293 ms`, and `80515 ms`. World time advanced from minute `27` to `32`, world version from `23` to `26`, and both Tavia and Pietr are present at Outer Ring Gateworks. Runtime revision is `316`, the next event sequence is `317`, SQLite integrity is `ok`, and foreign-key check is empty.

The live API rejected a forged packet-only move candidate with status `422` and `invalid_intent`. It created no turn and left world version `26` and runtime revision `316` unchanged. Focused contract, visibility, and Narrator verification passes `52/52`; the shared package build and backend typecheck pass. The broader opening-runtime fixture remains unavailable because its actor-settlement gate fails before Narrator is reached, so no green claim is made for that suite. The real GLM UI journey covers the changed runtime boundary directly.

Humanizer and deslop review kept the prompt addition mechanical, setting-neutral, and focused on immediate option relevance. It supplies no story event, actor decision, dialogue, or outcome. The rendered evidence is `output/playtests/campaign-play/diagnostic-action-selection-r19.session/screenshots/action-9-second-beat.png`, SHA-256 `195a16599a6423510a379cff9939d79d25130d4407f5df4eb9e36a1efa5c1a94`. At the `1270x720` product viewport, the existing sticky action dock still overlaps the left column while the full page is scrolled; that presentation advisory predates and is separate from the action-selection contract.

## Bellglass r19 actions 10-15 and participant-authority failure

r19 continued through six more manual actions rather than stopping at the favorable arrival. Tavia asked Warden Vohn about the stalled Canal Court funds, then about Collector Kael, and asked Pietr for the same dispute from a laborer's perspective. The three answers remained consistent while giving the speakers different interests. Vohn framed the choice as Shrine debt interest versus gate repairs and described Kael as an institutional instrument. Pietr reduced the same conflict to access to work: a toll paid to reach wages makes each coin feel earned once and spent twice. The segment was coherent, but two consecutive Warden answers were exposition-heavy and kept her in the role of information dispenser.

Action fourteen followed the two-unit route to Harvester Launch and advanced exactly two minutes. Dain Solkeen and Netta Vask were already present with a staged skiff, so the destination had a pre-existing local activity rather than assembling the whole cast around the player. The second arrival beat nearly repeated the first, which is a prose-pacing defect even though the state was correct.

Action fifteen selected `Try to help launch the skiff`. Judge correctly cited Dain and Netta and reasoned that the attempt would occur alongside them, but the frozen suggested intent contained only the location target and the normalized ruling was forbidden to add either actor. Game Master therefore followed its actorless effect contract: it advanced ten minutes and let Tavia move someone else's prepared skiff while Dain and Netta were present but unable to permit, refuse, correct, or react. The result is mechanically persisted but fails the living-world criterion. r19 is frozen rather than repaired in place. Failure evidence is `output/playtests/campaign-play/diagnostic-action-selection-r19.session/screenshots/action-15-help-launch.png`, SHA-256 `df2feac2c862120d95e2b5906d58a3e05f0d0d6de010c27e308d1eab2d86b2c5`.

The authority boundary now preserves the suggested action kind and every frozen target while allowing Judge to add only a visible non-player actor whose participation, consent, or reaction is material to the rendered action. It cannot add another location, route, pressure, possession, player actor, or hidden actor, and it cannot omit a frozen target. Both Judge compilation and turn admission use the same explicit predicate. Game Master additionally requires an interaction or dialogue effect for a targeted non-player response before an actorless physical result; a successful roll resolves the player's effort and does not manufacture permission or cooperation.

GitNexus reports `LOW` upstream impact for Judge `prompt`, Judge `compile`, `judgeProposalSchemaForFrame`, `assertSuggestedRuling`, and Game Master `prompt`; each has one direct consumer, and no high- or critical-risk process was found. Focused Judge and Game Master verification passes `50/50`, and backend typecheck passes. The broader turn-runtime fixture remains unavailable because its setup stops earlier at the pre-existing `Campaign Play opening requires every admitted actor action to settle` verifier failure. That suite did not exercise this target boundary, so it is not reported as passing.

Prompt-craft, humanizer, and deslop review keeps both additions literal, mechanical, and setting-neutral. They define who must be represented in authority and which existing effect class carries a response. They provide no owner, consent decision, dialogue, physical result, story outcome, or replacement prose.

## Bellglass r20: visible participant authority product acceptance

r20 copied the accepted r18 world state; world generation, character creation, and Opening did not run again. Through the actual Play UI, Tavia left Pietr and Warden Vohn at Gateworks and followed the open route to Harvester Launch. The route again consumed two minutes, Pietr and Vohn stayed behind, and Dain and Netta remained at the destination with the staged skiff. The next rendered choice `Try to check the skiff's stowed gear` retained only the location as its frozen target, providing a fresh exercise of the changed Judge boundary rather than replaying the persisted r19 failure.

Judge accepted one strict GLM 5.2 object and added Netta, but not Dain. Its model-authored reason was that Netta was actively hauling and preparing the vessel, so a closer check depended on whether she permitted or noticed it; Dain's mere presence did not make him a participant. The roll succeeded. Game Master advanced time, then committed an interaction with Netta as performing actor before resolving what Tavia could see. Netta kept a hand on the gunwale, identified the rope, basket, and lantern pole as her launch gear, and asked Tavia what business she had at the ramp. No bellglass shards were visible. Narrator preserved both the bounded inspection and Netta's challenge, while the second beat explicitly left Dain nearby but outside the exchange. The result accepts the authority change on the real product: success did not turn a nearby NPC into scenery or force her cooperation, and an unrelated visible NPC was not dragged into the action.

The next four generated choices cover Netta's low-tide window, Dain's staged skiff, the tide clock, and locked racks. None directly answers Netta's question. Freeform input remains available, but the suggested set loses immediate conversational relevance at the new decision point. This is a separate action-selection defect and does not invalidate participant-authority acceptance.

The accepted turn is `turn-player-action:f13c087ced31c5d7fbabf088a4a8ec43614f37f0`. Judge, Game Master, and Narrator each accepted attempt one through requested and actual `zai-coding-plan` / `glm-5.2`, with valid native schemas and durations of `71503 ms`, `84908 ms`, and `67038 ms`. No repair, fallback, provider switch, model switch, retry, or backend-authored outcome ran. The completed turn advanced world version `28` to `29`. At the checkpoint the database is ready at world minute `36`, world version `29`, world hash `09d026acaa8462458b83f0974ec8558b221e34a3fa73cf60c797ddb4a8cacc81`, runtime revision `368`, runtime hash `4966a6360367e99ecab8b8955820ab83cbc304a6413540b80c20733d252684d2`, and next runtime sequence `369`. SQLite integrity is `ok`, and foreign-key check is empty.

Rendered evidence is `output/playtests/campaign-play/diagnostic-participant-authority-r20-arrival.png`, SHA-256 `cb43b1892a240a63aee497f9780cb96b380beb784106fdaceb7ec4fef35fecf2`; `output/playtests/campaign-play/diagnostic-participant-authority-r20-skiff-check.png`, SHA-256 `ff27e7669c6a51d5635327724d0b644baa4c3e3a99143f55994be5bb7892f544`; and `output/playtests/campaign-play/diagnostic-participant-authority-r20-skiff-check-beat2.png`, SHA-256 `1e138c4a63cd19e19adbb970a2b7539d05c5f3accdcb744a2007f67aba37817e`.

Evaluator scores for this bounded scene are comprehension `5/5`, prose readability `5/5`, player-action causality `5/5`, world aliveness `5/5`, and desire to continue `4/5`; the missing direct conversational reply and roughly four-minute model chain are the costs. Under ProjectGameGod marker `pgg:knowledge:found-004`, the trace establishes practical availability through freeform input, selection authority for the player's attempt, causal contribution to a durable transition, visible NPC authority, and acknowledged consequence. It does not establish felt agency, fun, or universal meaningfulness; those would require participant-report evidence beyond artifact inspection and this evaluator reading.

## Bellglass r21-r22: local attempt, travel authority, and recovery

r21 copied the accepted r18 state rather than rebuilding the world. Its first post-change turn stopped in Narrator before mutation because GLM copied the input-only `includesTravel` catalog annotation into an `actionSelection`. The strict schema rejected the extra field. Narrator now says directly that each selection contains only `intentIndex` and `detail`; `includesTravel` belongs only to the input catalog. r21 remains frozen and was not resumed.

r22 copied the same accepted state and continued through nine manually chosen player actions in the rendered Play UI. Tavia left Pietr and Warden Vohn at Gateworks, reached Harvester Launch alone, established that Netta owned the skiff and gear, negotiated for an uncertain share of raw bellglass, accepted Netta's refusal to promise a rate, and waited for the low-tide signal. Netta retained her own economic position and operational authority throughout. She specified the stern-line, bracing, and basket tasks only when the tide window opened; Dain remained present but outside their agreement.

The action set exposed two separate design defects. After Netta asked Tavia to wait, the suggested `Wait and watch the tide clock for the low-tide mark` completed with zero elapsed minutes and no world-version change. Two explicit freeform waits of ten minutes each were required before the clock reached the mark. Earlier negotiation sets also omitted the obvious accept-or-clarify continuation until Tavia supplied it freeform. These are current next-slice findings, not evidence against the travel-authority change.

Action seventeen selected the local suggested attempt `Try to ease the skiff down by the stern line`. It produced an uncertain setback, advanced five minutes from `58` to `63`, kept Tavia at Harvester Launch, and made Netta intervene when the skiff slid past its stopping mark and scraped the ramp. The rendered consequence preserved Netta's competence and ownership, acknowledged Tavia's causal contribution, and changed the immediate departure conditions. This is direct product evidence that a local attempt no longer acquires travel merely because its generated prose points toward a route.

The next four suggestions did not expose either route-bound attempt even though the authoritative catalog contained separate typed candidates for Bellglass Beds and Outer Ring Gateworks. The player selected the ordinary move `Go to Bellglass Beds: the open low-tide window eastward`. Its first Judge object was rejected before mutation because an older general prompt rule asked for both a movement destination and a companion while the frozen suggested schema already represented the destination with its route and admitted only material visible NPC additions. The model added the forbidden destination location as a second target. The prompt now distinguishes freeform movement from suggested movement: a frozen route carries the suggested destination, a destination location must not be added, and a visible material NPC may still be added. GitNexus reports `LOW` upstream impact with `judge` as the only direct consumer.

After backend restart, the visible Resume reran only the interrupted Judge stage under the corrected contract. The accepted Judge row is explicit attempt two after the recorded invalid attempt one; this was user-visible recovery after a code change, not an automatic retry. Game Master and Narrator then accepted attempt one. All accepted stages used requested and actual `zai-coding-plan` / `glm-5.2`, strict objects, with no fallback, provider switch, model switch, field truncation, schema repair, or backend-authored prose. Accepted durations were `39108 ms`, `59069 ms`, and `95617 ms`.

The recovered turn `turn-player-action:67344e49657b39047d68b0771ff824b6892c4e30` advanced exactly four route minutes from `63` to `67` and world version `35` to `38`. SQLite places Tavia and Netta at Bellglass Beds while Dain remains at Harvester Launch. The UI rendered the grounded skiff, sulfurous mudflats, humming formations, crabs, lantern eels, and memorial shards without leaking a remote event. The prose is coherent and readable, although the accessibility tree still contains the known live-region duplicate of the visible paragraph. SQLite integrity is `ok`, and foreign-key check is empty.

The product pass therefore accepts local-versus-travel separation, ordinary suggested movement, willing-companion movement, strict failure before mutation, and explicit recovery. It does not claim product acceptance for a route-bound suggested attempt because Narrator did not select one into the four visible choices; that branch is covered by the strict Judge and Game Master contract tests but remains unexercised through the UI. The next gameplay slice should own zero-duration suggested waits and immediate conversational choice relevance, then obtain a natural rendered route-bound attempt rather than force one for coverage.

Focused contracts, visibility, Judge, Game Master, and Narrator verification passes `103/103`; the shared package build and backend typecheck pass. Prompt-craft review treats both corrections as output and authority contracts. Humanizer and deslop review finds them direct, setting-neutral, and free of supplied plot, character choice, outcome, or replacement prose. Under `pgg:knowledge:found-004`, the trace separately establishes supported options, practical selection, authoritative causal transition, persistent consequence, and readable acknowledgment; it does not convert one evaluator's continued interest into a population claim about fun or felt agency.

## Bellglass r23: actionable time and immediate conversation continuity

The current contract deliberately rejected the completed r22 narration because its stored contact labels predated the hard cutover from `Ask Netta ...` to the typed `Talk to Netta: ...` form. A diagnostic copy confirmed the failure at public-context admission, and SQLite's terminal-narration immutability trigger correctly prevented rewriting the accepted history. No compatibility label, alias, migration, fallback, or disabled protection was added. r23 instead copied the accepted pristine Bellglass template at `output/playtests/campaign-world-templates/bellglass-tides-pristine-46bd6f32`; world generation did not run again.

The real Play UI created Cetta Dossi through GLM 5.2 and admitted a fresh Opening. Only Netta Scarpa was present at Harvester Launch. The opening established the tide window, Netta's preparations, and the humming memorial shards in the deeper formations without assembling the wider cast around the player. Its first suggested action was `Talk to Netta Scarpa: ask about the drowned voices`. Selecting it produced a grounded answer from Netta's own experience, then reserved the first next-action slot for the natural continuation `Talk to Netta Scarpa: ask about the two lost lines`. This accepts immediate conversational continuity on the rendered product rather than merely proving that freeform input remains available.

The next manual input was `I wait for ten minutes, watching Netta and the tide clock.` Judge classified it as deterministic wait authority with ten-minute elapsed bounds. Rulebook committed an `advance_world_time` receipt with `elapsedMinutes: 10`, moving the visible clock from `Day 1, 00:02` to `Day 1, 00:12`. During the same interval Netta finished securing the skiff, repeatedly checked the tide clock, and then independently moved to Bellglass Beds. The visible roster became empty, the journal recorded both her work and departure, and the next choices changed to inspecting the narrowed tide window, following Netta toward the cleaner beds, risking the deeper formations, or examining the locked racks. Waiting therefore consumed a scarce world opportunity and allowed another actor's plan to advance; it was not a zero-time narration synonym.

Judge, Game Master, Actor Replanner, and Narrator each accepted attempt one through requested and actual `zai-coding-plan` / `glm-5.2` strict objects. Their durations were `23707 ms`, `55545 ms`, `122234 ms`, and `79166 ms`. No automatic model retry, repair, text fallback, provider switch, model switch, field truncation, or backend-authored prose ran. The completed turn advanced world version `14` to `16`; SQLite integrity is `ok`, and foreign-key check is empty.

The final paragraph is coherent and causally legible: ten minutes pass, Netta's concrete preparations occupy that interval, her departure changes the social scene, and the tide clock makes the lost time visible. The prose repeats some journal facts in `The moment`, and the roughly `280652 ms` model chain materially interrupts reading rhythm, but the scene itself has a clear before, interval, autonomous change, and next decision. Evaluator scores for this bounded trace are comprehension `5/5`, prose readability `4/5`, action causality `5/5`, world aliveness `5/5`, and desire to continue `5/5`. These are one expert play reading, not participant evidence or a population claim about fun.

Focused contract, visibility, Judge, and Narrator verification passes `78/78`; the shared package build and backend typecheck pass. GitNexus impact and pre-commit change detection were attempted, but its MCP transport remained closed after the local index failure; the unavailable advisory did not replace direct diff inspection or block the slice. The broader turn-runtime fixture remains unavailable because all `37` cases stop at the pre-existing setup verifier `Campaign Play opening requires every admitted actor action to settle` before reaching the changed wait or action-selection boundaries. This is recorded as a verification-fixture defect, not reported as passing and not allowed to replace the real product pass.

Prompt-craft, humanizer, and deslop review keeps the new instructions literal and mechanical: suggested wait means exactly ten world minutes, freeform actionable wait must advance positive time, the first choice continues a visible performer's exchange, and contact detail is a base-form dialogue act. They supply no setting fact, NPC decision, outcome, prose passage, repair, or fallback. Under the ProjectGameGod rule/runtime/observation distinction, r23 proves that the action is selectable, executes through authoritative receipts, creates a persistent transition, and exposes an intelligible consequence; it does not infer player experience from backend state alone.

Rendered evidence is `output/playtests/campaign-play/diagnostic-wait-reply-r23-fresh.session/screenshots/wait-ten-minutes.png`, SHA-256 `02f3ae6c688e4336f035cca3266ac500d23fb9642e1550a39bea2d0196cd99b2`.

## Bellglass r23 actions 3-4: natural work, strict interruption, and inventory payoff

The diagnostic continued from the same accepted state rather than resetting after the favorable wait result. Cetta followed Netta to Bellglass Beds because her generated identity and visible tools made safe harvesting a natural goal. The move consumed the route's exact four minutes, placed Cetta with Netta and Dain, and exposed no remote event. The arrival prose remained coherent but mostly restated the stable location description. Its four suggestions offered two conversations and two investigations, with no direct continuation of Cetta's obvious working goal, so the next player action used freeform input rather than choosing an option for test coverage.

The fourth action was `I take out my harvesting tongs and begin cutting quiet bellglass from the cleaner inshore bed, keeping clear of the humming formations.` The first GLM 5.2 Judge call returned `uncertainty.difficulty` as a string. Strict validation interrupted attempt one after `43651 ms`, before Game Master or Rulebook. The turn retained base world version `19`, final world version remained null, mutation audit stayed empty, receipts stayed empty, world time remained `Day 1, 00:16`, and the rendered UI exposed an enabled `Resume`. No coercion, parser, automatic retry, text fallback, repair, provider switch, model switch, or replacement turn ran.

The Judge prompt now states that `dieSides`, `difficulty`, `modifierMinimum`, and `modifierMaximum` are unquoted JSON integers, bounds difficulty to `1..20`, and supplies one short numeric example. This is output-shape guidance for the existing strict schema. It does not select a difficulty, roll, result, elapsed time, item, or prose. Prompt-craft review treats the line as one local type contract; humanizer and deslop review finds no narrative voice, supplied outcome, repetition, or abstract filler to remove. GitNexus impact was attempted before the private Judge prompt edit, but the MCP transport remained closed; direct source inspection shows the prompt has the Judge model invocation as its single consumer.

After backend restart, the player pressed the rendered `Resume` control. Judge attempt two accepted a native strict object in `29125 ms`: d20, numeric difficulty `8`, modifier range `-2..2`, elapsed bounds `15..30`, result range setback through strong success, and an acquire-possession requirement at limited or better. Code rolled `19` with modifier zero and resolved strong success. Game Master then accepted once, Rulebook advanced twenty minutes and acquired one `Quiet Bellglass Shards`, and the carrying panel rendered the new possession at quantity one. No prose-only inventory claim substituted for the receipt.

The same turn let Netta continue her own harvesting plan. Her accepted local event left fresh chip marks, pale shard dust, and bootprints across three inner formations. Separate protected events also advanced Orsa and Collector Kael outside the scene, but neither leaked into the player's narration or journal. Narrator joined Cetta's clean extraction and Netta's visible aftermath into one concise paragraph, then offered contact with Netta, inspection of the marks, contact with Dain, and a riskier attempt at the scored faces. The first contact is a reasonable response to a visible actor's new trace rather than a forced dialogue continuation.

The recovered turn completed at world minute `36`, world version `21`, and runtime revision `254`. SQLite integrity is `ok`, and foreign-key check is empty. Judge, Game Master, Actor Replanner, and Narrator accepted the resumed path once with durations of `29125 ms`, `47354 ms`, `82061 ms`, and `81284 ms`; only the recorded Judge attempt one is invalid. Focused Judge verification passes `24/24`, and backend typecheck passes.

The bounded play verdict is `TARGETED_PASS_CONTINUE`: comprehension `5/5`, prose readability `5/5`, action causality `5/5`, world aliveness `5/5`, and desire to continue `5/5`. The working action has preparation, uncertainty, elapsed opportunity cost, durable reward, concurrent local actor motion, hidden remote motion, and useful follow-up. The diagnostic recovery disqualifies r23 from pristine-lane evidence, and the arrival's descriptive repetition plus weak support for the character's working goal remain findings for continued play rather than claims of completion.

Rendered evidence is `output/playtests/campaign-play/diagnostic-wait-reply-r23-fresh.session/screenshots/harvest-resume.png`, SHA-256 `035919338ca923d21771999c69cce6d8950e12331b249398c33f45cf9c9b638c`.

## Current runtime fixture verification after hard cutover

The focused Opening and player-turn suites had stopped proving the current runtime because their shared fixtures still encoded several superseded contracts. Actor B's Opening action targeted a second location despite acting locally; Narrator mocks ignored the required first reply to the latest visible performer; the player-turn Judge mock omitted `requiredPossessionEffect` and used a one-to-two-minute duration even when a compound action crossed a longer route. Two assertions also retained the obsolete extra cross-location exposure and an uncapped list of nine suggestions.

The fixtures now express only current mechanics: Opening actions are spatially valid, Narrator selects the required conversational reply before filling the remaining four visible slots, Judge declares no possession change unless a scenario owns one, and movement time comes from the canonical route cost. The tests expect only the events that the valid actor plans actually commit and apply the existing four-action presentation limit. No production runtime, compatibility path, fallback, repair, prose, or model behavior changed.

Standalone verification passes Opening `13/13`, player-turn runtime `37/37`, and backend typecheck. One parallel verification attempt completed all `37/37` turn tests but Vitest reported an internal `onTaskUpdate` RPC timeout under concurrent load; the same suite passed cleanly when rerun alone, so the parallel command is recorded as a verification-run defect rather than product evidence. GitNexus impact remained unavailable with `Transport closed`; direct diff inspection confines the slice to the two test files and this evidence note.

The fixture correction is consistent with ProjectGameGod marker `pgg:knowledge:found-004`: the tests now keep offered options, selection authority, executed transition, and visible consequence as separate relations instead of letting an invalid setup stand in for agency or meaningful choice. It still makes no participant claim about felt agency, fun, or prose quality; those remain the responsibility of real manual play.

## Bellglass r24-r25: toll restriction becomes world truth

r24 exposed a direct contradiction between prose and mechanics. The accepted opening placed an outsider cartographer at Marrowfen Toll Bridge, where Collector Kael stated that no one could cross without coin or bellglass. The player carried charts, charcoal, a cloak, and surveying tools, but neither payment. Even so, the UI offered an ordinary move toward the supposedly gated route; Judge reported that no barrier existed in its visible route frame, and Rulebook moved the player without payment, permission, debt, or any route transition. The failure was not a prose-quality problem. The spoken condition existed only as an observation while `campaign_play_route_states` remained empty. Failure evidence is `output/playtests/campaign-play/diagnostic-agency-prose-r24-clean.session/screenshots/action-4-unpaid-toll-crossing.png`, SHA-256 `347a4ee3666efc25b32a5bea42c8fa46c7ba37b9bd9a2091a47c3cf3d4bad2b2`.

The current contract now lets Opening declare one nullable restriction on its selected outgoing route. A real payment, permission, or obstacle condition compiles to a protected `set_route_state restricted` command before the premise event; a warning or price that does not bar passage remains null. Ordinary player and actor movement accepts only `open`. Visibility exposes a restricted route as a typed attempt rather than a direct move. Judge receives the route state and requires a concrete access basis for deterministic passage. Game Master moves the player through a successful restricted traversal only by committing `open -> move -> restricted` in one Rulebook batch; limited, setback, impossible, and clarification results leave the player at origin. No compatibility path, prose parser, fallback, retry loop, or backend-authored story result was added.

The manual r25 run copied the clean Bellglass world instead of regenerating it. Through the rendered UI, GLM 5.2 created outsider cartographer Tobin Lassek and admitted an inner-ring Opening. The accepted opening put Tobin at Marrowfen Toll Bridge, displayed Shrine Steps as `Open` and Brackish Crossing as `Restricted`, omitted an ordinary move to Brackish Crossing, and rendered `Try to reach Brackish Crossing: push through the blocked planks`. SQLite records the toll condition as opening command order seven, followed by the premise event at order eight. Its route state remains `restricted` at world version `14`.

Opening attempt one completed after `338338 ms` but was rejected before mutation because one actor plan cited a non-active goal ID. The visible Resume created attempt two, which accepted after `249917 ms`; Narrator accepted once in `60781 ms`. This is diagnostic recovery, not pristine evidence. Every call requested and used `zai-coding-plan` / `glm-5.2` native JSON. No text fallback, provider switch, model switch, hidden retry, or backend prose ran.

The player then selected the rendered restricted-route attempt while carrying no coin, bellglass, or permission. Two Judge objects correctly identified the route-bound attempt but supplied elapsed minima below the route's mechanical cost, so the strict compiler interrupted both before mutation. Repeating the same model call was not useful. Ownership moved to the rule layer: model output still owns intent, feasibility, uncertainty, and result bounds, while Judge canonically binds route time from the visible route. This is a mechanical normalization, not a repair of narrative content. GitNexus reports `LOW` upstream impact for Judge `compile`, with `judge` as its only direct caller and two affected Judge flows. Focused Judge verification passes `26/26`, and backend typecheck passes.

After the code change, the visible Resume created Judge attempt three. It accepted once in `43057 ms`; Game Master accepted in `27051 ms`; Narrator accepted in `79563 ms`. Rulebook advanced exactly three route units and recorded the failed interaction, but issued no `move_actor` command. Tobin remained at Marrowfen Toll Bridge, Brackish Crossing remained `restricted`, and the completed turn advanced world version only from `14` to `15`. Reload preserved the same placement, restriction, consequence, and next actions.

The rendered consequence is coherent and causally explicit. Tobin shoulders the collection table, Mirrus braces it, says `No tithe, no passage`, and warns that another attempt will be reported to the Canal Court. The prose neither teleports the player nor manufactures payment or permission; the next choices acknowledge the blocked span and keep the open Shrine Steps route available. For this bounded expert reading, comprehension is `5/5`, prose readability `5/5`, action causality `5/5`, world aliveness `4/5`, and desire to continue `4/5`. The roughly nine-minute Opening with one explicit recovery and the two pre-fix Judge interruptions materially damage pacing, so this run cannot support a pristine or fun claim.

Rendered evidence is `output/playtests/campaign-play/diagnostic-route-restriction-r25-clean.session/screenshots/opening-restricted-route.png`, SHA-256 `81589c7a4cfaefde8b6611297c1403c1c7d8512d58495691249d2f4ef510564e`, and `output/playtests/campaign-play/diagnostic-route-restriction-r25-clean.session/screenshots/action-1-blocked-at-toll.png`, SHA-256 `c183114815c57cbde5317dc5f5f035aeceb007e76d880f0bc3d6be29ab72a48e`.

Under ProjectGameGod marker `pgg:knowledge:found-004`, r25 separately proves option communication, practical selection, authoritative execution, causal contribution, persistent consequence, and readable acknowledgment for this action. It does not infer felt agency, fun, or universal meaningfulness from the state trace. Prompt-craft, humanizer, and deslop review kept the opening restriction instructions literal, setting-neutral, and limited to the typed condition; they supply no toll, character act, dialogue, or outcome.

The complete focused route-restriction, Opening, and player-turn set passes `198/198`, and backend typecheck passes. Staged GitNexus change detection reports `CRITICAL` because it attributes `267` direct dependents across legacy engine modules to the private, unexported `campaignPlayOpeningCommandSchema`; exact repository search finds only its two same-file uses in `opening-planner.ts`. This is recorded as an index symbol-identity false positive rather than product blast radius. A separate mounted-route integration fixture still observed `world_acting` after one helper wait even though the runtime remained live at revision `19`; the changed scenario had `routeRestriction: null` and never exercised this mechanic. That result remains classified as a pre-existing verification-boundary defect and is not reported as passing.

## Bellglass r26-r28: exact visible-observation coverage

r26 stopped at player action four after a same-turn contradiction. The player event left five crates aboard and one on the quay. Orsa's later actor event claimed six crates, an even hull, coiled moorings, and departure. Both were direct visible observations at the same place and time, but Narrator rendered only the earlier state and offered the sixth crate as the next action. Reload preserved both contradictory consequence cards and the stale prose. Screenshot evidence is `output/playtests/campaign-play/pristine-natural-r26.session/screenshots/action-4-future-leak-after-reload.png`. The run is a hard-stop diagnostic, not pristine acceptance.

Narrator proposals now bind each beat to zero-based `newObservations` indexes. The dynamic schema bounds indexes to the current packet, and backend validation requires exact coverage without duplicates or omissions. Packet order is chronological; when several observations update one actor, object, or place, the latest accepted observation owns the narrated current state. A focused regression reproduces the five-crate then six-crate shape and rejects a proposal that covers only the first observation. This adds no retry, repair, fallback, backend-written prose, or compatibility path.

r27 copied the broken r26 state and continued through the real UI. Its new turn contained only one observation and therefore did not exercise multi-observation coverage. Worse, that observation described the barge as still moored, contradicting r26's persisted departure. The result was rejected as proof. Exact per-packet coverage cannot rewrite accepted history or turn an actor's prose-only cargo, payment, or movement claim into mechanical authority.

r28 copied the accepted Bellglass template at its `character_required` boundary. GLM 5.2 created Tesso Maren through the rendered character flow, and the UI admitted an `outer-ring` / `Outsider` / `Just arrived` / `Looking for work` Opening. Opening Planner and Narrator each accepted attempt one through `zai-coding-plan` / `glm-5.2` strict objects in `240295 ms` and `69330 ms`. No model repair, text fallback, provider switch, model switch, or automatic retry ran.

The clean Opening naturally produced two visible observations. Orsa offered paid work verifying laden-barge clearance at Brackish Crossing, while a separate workshop observation exposed six raw fragments and at least four pending orders. Narrator incorporated both into one coherent beat and preserved their relationship after reload. Backend acceptance proves that the proposal passed the new dynamic index schema and exact-coverage assertion; the internal indexes are not published in the stored player-facing narration.

The accepted prose is causal and readable, though longer than necessary because it repeats stable workshop description before the offer. One suggested move points toward Bellglass Beds despite the named survey target being Brackish Crossing, so option relevance remains a separate finding. The trace proves the changed coverage boundary and persistence, not sustained world autonomy or participant fun.

SQLite integrity is `ok`, foreign-key check is empty, world version is `9`, runtime revision is `54`, and the database contains one completed Opening and zero player actions. Before and after reload screenshots are byte-identical at `output/playtests/campaign-play/pristine-natural-r28-narrator-coverage.session/screenshots/opening-two-observations-after-reload.png`, SHA-256 `36a614a901272d1de144c57d68687f6c0edbf582dd0f3b671f04a1c95ea05cc3`.

Focused verification passes Narrator `15/15`, Opening runtime `13/13`, player-turn runtime `37/37`, and backend typecheck. The mounted route integration fixture still reads Opening as `processing` after one helper wait and never reaches Narrator, so it remains a verification-timing failure rather than a green claim. Humanizer and deslop review reduced the new prompt to a direct, setting-neutral contract. ProjectGameGod marker `pgg:knowledge:found-004` supports the separation between model proposal, authoritative acceptance, persisted state, and presented consequence; r26-r28 show that exact presentation coverage must not be confused with mechanical authority.

## Bellglass r33: current-contract ten-action manual sitting

The immutable zero-character Bellglass template was materialized as `pristine-natural-r33-longplay-lane-a`; no world generation ran. Current code accepted the copied source at world/runtime `1/1`. The rendered Character flow created Nera Sol, an ordinary itinerant sign-painter stranded by a cancelled ferry. Human review removed duplicate signature items and added two rated skills, two specialties, one flaw, and one belief before bootstrap. Three requested possessions entered through typed setup authority. Opening Planner and Narrator each accepted attempt one through `zai-coding-plan` / `glm-5.2` strict objects. The opening was coherent and local, though its salt-eaten price board matched Nera's profession so exactly that player-centred tailoring remains a quality warning.

The ten actions formed one playable arc. Nera obtained exact prices and timing from Orsa, inspected the swollen board, asked for proper preparation, and refused when Orsa insisted on knowingly short-lived work. Orsa preserved her own barge deadline and did not become compliant for the player. Nera then moved to Brackish Crossing, where a pre-existing chipped piling exposed Pietr's independent inspection and search for paid repair work. Nera learned that Tessara owns repair orders while Kael and the Canal Court constrain funding, painted `UNSAFE — HOLLOW PILING`, reported the hazard at Gateworks, and returned with Tessara's separately funded timber-wage offer. Tessara prioritized her cracked floodgates, promised only the authority she actually held, and did not pretend the piling was funded.

Action two recorded an invalid Judge object after `20054 ms`; explicit Resume accepted Judge attempt two and completed the same turn without duplicate time or mutation. Action eight mechanically settled one move to Gateworks, then recorded an invalid Actor Replanner object after `136105 ms`; explicit Resume retried only actor replanning, and the route completed once. No hidden retry, repair, coercion, text fallback, provider switch, model switch, or backend-authored narration ran. These recoveries keep the sitting valid for product and longplay diagnosis but disqualify it from pristine-lane acceptance.

After action ten, the campaign has one completed Opening and ten completed player actions, world version `26`, world minute `28`, runtime revision `478`, and contiguous runtime-event sequences `1..478`. SQLite integrity is `ok`, foreign-key check is empty, and reload restores Brackish Crossing, Pietr, all three possessions, the red warning, the latest narration, and the same next choices. The persisted projection hash is `0e19db3cf1555e2d95df41a662cb8d26cbd740b2779e990d6da043bc46a04570`.

The player account before rereading retained Orsa's refused job, the hollow piling and warning, Pietr's need for work, Tessara's by-day-end order promise, the blocked Court funding, and the immediate Gateworks timber offer. The intended next action is to relay that offer. Expert sitting scores are comprehension `5/5`, prose readability `4/5`, action causality `5/5`, felt agency `5/5`, world aliveness `4/5`, and desire to continue `5/5`. Prose repetition, `this morning` at world minute nine, two visible recoveries, strongly tailored Opening relevance, and multi-minute action latency remain findings.

The full manual record and checks are in `output/playtests/campaign-play/pristine-natural-r33-longplay-lane-a.session/human-notes.md`. Representative rendered evidence is `action-03-orsa-refuses.jpg`, SHA-256 `953706520A210C6ACF7914BA5FDD6EE0AFEBCB4693E62DABD4E48C70C5DC5DDF`; `action-07-warning-mark.jpg`, SHA-256 `2BC027ACF70DAA34E554284C2B808B904E4515E9FA66D9FF8BAA2EF7EAB88990`; `action-09-tessara-priority.jpg`, SHA-256 `5C211DE0B83827A274484DD6CB8F46ABDEE44105463E8A92428D1D6315A86521`; and `action-10-returned-warning-persists.jpg`, SHA-256 `BAA21E727A709D5E8A30F6FF746E09428332F2E5CF020B9C42904835731773A3`.

Humanizer and deslop review of these notes kept the observations concrete and removed no project or setting fact. The notes separate direct rendered evidence, state verification, and evaluator judgment. Under `pgg:knowledge:found-004`, the warning is a persisted world transition produced through authority, while the unchanged lettering-kit quantity remains possession truth; visible handling and narration alone do not create or consume ownership.

## Bellglass r33 actions 11-14: actor departure authority

The same campaign continued without a reset. Nera relayed Tessara's Gateworks timber-wage offer to Pietr, who accepted because it paid today instead of waiting on Canal Court funding. Nera then stayed at Brackish Crossing to watch the waterline while Pietr left. The accepted prose said his boots faded away, but Game Master had emitted only time advancement and an actorless world event. No accepted `move_actor` command existed, so reload correctly kept Pietr at Brackish Crossing. The prose and authoritative placement disagreed.

The repair keeps the boundary explicit. Game Master and Narrator may not turn actorless discovery, dialogue, intention, gesture, or `SOURCE_MOMENT` prose into departure, arrival, following, or any other placement change. Judge treats a non-player actor in the current target catalog as visibly reachable even when older prose says that actor intended to leave. After an applied current-turn event names an actor as performer, the scheduler now gives that actor's inactive plan one immediate `plan_retry` opportunity ahead of unrelated due work. The wake is derived from the applied command and receipt, not from narrated text, and an active plan is not pulled forward.

Action thirteen is retained as negative evidence: before the Judge correction, the model rejected contact with mechanically present Pietr because it trusted the stale departure sentence. Action fourteen repeated the contact after the repair. Judge accepted that Pietr was present; Game Master recorded his stated decision; the scheduler selected Pietr rather than unrelated Netta; and Actor Replanner produced a typed `move_actor` proposal. The accepted receipt moved Pietr from Brackish Crossing to Outer Ring Gateworks at world version `30`. The Play UI removed him from `Present here`, rendered `Pietr Grenn left for outer-ring-gateworks`, and reload preserved both the absence and the consequence.

Actor Replanner attempt one for action fourteen failed strict validation and exposed the existing Resume control. Attempt two accepted; no hidden retry, fallback, provider switch, schema repair, or backend-authored story result ran. Judge, Game Master, and Narrator accepted their first attempts. The sitting now contains fourteen completed player actions and three explicit recoveries in total, so it remains rehearsal evidence rather than a pristine lane.

Rendered evidence is `output/playtests/campaign-play/pristine-natural-r33-longplay-lane-a.session/screenshots/action-14-pietr-authoritative-departure.jpg`, SHA-256 `F5CB287712440B16D3D6F7314784F6F21AC4A2A0BB598679C1667553C73B5BC0`. At the checkpoint, SQLite integrity is `ok`, foreign-key check is empty, world minute is `35`, world version is `30`, world hash is `cb2d262a92821d67745d60ec4c32d29488fd6c8ec96d667c91c53106a8860201`, runtime revision is `657`, and runtime hash is `363bedf8fc9db543e91f2fdddc3a45a80f3bd9a517d40e17d6836f8e38cfdf7d`.

Focused Judge, Game Master, Narrator, and scheduler verification passes `93/93`; the complete player-turn runtime passes `37/37`; backend typecheck passes. GitNexus reported `LOW` upstream impact for the four edited production symbols and no high- or critical-risk process. Humanizer and deslop review kept the prompt changes as setting-neutral authority rules: they provide no departure, destination, dialogue, actor decision, or replacement prose. This result directly applies ProjectGameGod marker `pgg:knowledge:found-004`: narrated departure is presentation, while placement changes only through a correlated typed proposal, Rulebook transition, receipt, persistence, and visible consequence.
