# Turbo r17 Opening hard stop

## Outcome

Run `pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r17` materialized the unchanged frozen Lowwater Ledger template on commit `eba68be1`. Template hashes, empty character/play/turn tables, and Lane A eligibility were frozen before the rendered character flow.

The UI generated Verran Solwick in one draft request. Human review kept his ordinary cable-bridge work, family debt, practical loadout, and local need, but removed an unsupported secret about unauthorized bridge light, replaced a direct alderman tie with an ordinary lender, and corrected the inconsistent `Ferry Crew` trait to `Cable Bridge Crew`. The accepted character remained outside Craghold's ruling structures. The player selected `lower-wards / Local / Already here / Looking for work` and submitted Begin once.

Opening Planner attempt 1 ended after `381,347 ms` with `finish_reason=stop`, but the strict artifact failed validation. SQLite records the model stage as `interrupted/model_contract_invalid`, `schema_outcome=invalid`, using Z.AI Coding Plan `glm-5-turbo`. No Opening artifact, narration, provider switch, model switch, hidden retry, text fallback, restore, rewind, or duplicate submission occurred.

The rendered Play UI remained at `opening_active`, `worldVersion=7`, `runtimeRevision=42`, projection hash `b6b9981aeaeb203d0def8160ea2e440e226f5ca3be4dff3880d13408e1e2ccc7`, with the same interrupted Opening turn and one Resume control. The ledger contains the six matched character-bootstrap commands and receipts. SQLite `integrity_check` is `ok`; `foreign_key_check` returns no rows.

The terminal screenshot is `output/playtests/campaign-play/pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r17.session/screenshots/opening-hard-stop.png`, SHA-256 `b2dd1ab76bbc38bc689fcae3d69e394dd6d11c2e494c39b985721b4b0fb481bf`.

## Disposition

Task 18 permits recovery only for a recorded transport failure or loss of worker authority. This contract failure ends r17, so Resume is not used. The next Lane A attempt materializes the same zero-turn template under a new run id and keeps the current provider/model configuration.

Humanizer review kept Verran's profile occupational and personal instead of plot-chosen. Deslop review preserved the exact model classification, duration, state, hashes, integrity results, and no-recovery boundary without repeated framing.
