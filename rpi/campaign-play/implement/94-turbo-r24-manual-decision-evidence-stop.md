# Task 18 Lane A r24: manual-decision evidence stop

Run `pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r24` materialized the frozen current-contract Lowwater Ledger template without regenerating the world. Before character bootstrap, SQLite was at world/runtime `1/1`, character and turn tables were empty, integrity was `ok`, foreign-key check returned no rows, and the frozen eligibility manifest matched the accepted snapshot. The rendered Character flow created Sella, an ordinary lower-ward basket and reed-mat repairer. Human review removed one campaign-tailored contradiction before acceptance. Opening Planner and Narrator each accepted their first `glm-5-turbo` strict-object attempt. Opening completed at world version `15` and runtime revision `99`; its two actor jobs settled, runtime event sequences were contiguous `1..99`, and the action-zero UI showed one exact local scene, two present people, six possessions, and two open routes.

Action one was signed and bound correctly. Sella asked Vedris Kast about the darkened market stalls because he had identified them as the visible lead toward routine paid repairs. Judge, Game Master, and Narrator accepted their first attempts in `65942 ms`, `47077 ms`, and `49669 ms`. The result advanced one world minute and explained that unpaid light-token fees had darkened the stalls. SQLite reached world version `16` and runtime revision `128`; integrity was `ok`, foreign-key check was empty, and runtime event sequences were contiguous `1..128`. A full backend restart restored exact projection hash `488c9ef37ddf202390cd905d6e2eb812934ac8bf3025da3837e0e0dd536dce73`. Screenshots `action-0.png` and `action-1.png` have SHA-256 `9f30ba1f6c29bbb23bd5645eddc2f86920f0a033087f840c338b26fb925ea35a` and `3208b0fb05c78793d943ce900739b51d311bec5ac320b88f36c2d903a4c241b2`.

Action two completed cleanly in the product but cannot enter pristine evidence. The manual player chose the rendered label `Go to Dim Ward Market`; browser control clicked that current button once, and the API admitted exactly one player turn. The pre-action projection hash was `ae7b1ea6d69b5abb82ff219518b4b0c371146ca1f903a4b035a58b70e2c9bedd`. Before submission, however, the operator staged Opening's obsolete handle `choice_9e2ec7059a94acbb57d04c30` instead of the current narration's handle `choice_bb2eb2af2984e812dbd450b8`. The durable turn correctly records the latter. The strict binder rejected the mismatch with `The durable turn does not match the signed manual decision.`

No pending evidence was rewritten after submission, no action was resubmitted, and SQLite was not restored or edited. The completed turn moved Sella to Dim Ward Market and ended at world version `18`, runtime revision `171`, and world minute `2`. Judge, Game Master, and Narrator accepted their first attempts; the Narrator took `242373 ms`. Runtime event sequences remain contiguous `1..171`, integrity is `ok`, and foreign-key check is empty. The rendered stop screenshot is `action-2-unbound-evidence-stop.png`, SHA-256 `47c150e52c5a33ea415270d91a6bd8dae108809dcf2b152ee78abfb9430eebc1`.

r24 is frozen with one eligible signed player action and one completed but unbound diagnostic action. This is an evidence-integrity hard stop, not a product-state or model-contract failure. It repeats the previously documented r10 opaque-handle operator error, so no runner recovery feature or product change is justified. Lane A continues from a fresh materialization of the same frozen zero-character template.

Humanizer and deslop review kept the note chronological and distinguished rendered evidence, durable state, and operator error. It makes no pristine or long-play claim from the clean product turn.
