# Task 18 Lane A r26: Opening Planner hard stop

Run `pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r26` materialized the unchanged frozen Lowwater Ledger template. Before UI mutation it was accepted/world/runtime `1/1/1`, `character_required`, with zero characters and turns, SQLite integrity `ok`, and no foreign-key violations. Runner preparation froze the same template database, config, eligibility, and topology evidence used by the preceding Lane A attempts.

The rendered Character flow submitted one draft request and generated Brina, an ordinary lower-ward porter seeking household and market delivery work. Character synthesis and power assessment accepted their first `glm-5-turbo` native-object attempts in `130485 ms` and `17085 ms`. Human review removed references to the campaign's light crisis from her worldview, contradiction, and sample line, then added one ordinary motive and drive about paid carrying work, lodging, and meals. Brina retained no faction role, office, secret, power, or prior conflict connection. The human player selected `lower-wards / Local / Already here / Looking for work` and submitted Begin once.

Opening Planner attempt one stopped after `371686 ms`. The provider returned normally with finish reason `stop`, but strict validation rejected the proposal because `hiddenConsequence.exposure.triggers` was missing: the schema expected an array and received `undefined`. SQLite records one `opening_planner` attempt with requested and actual model `glm-5-turbo`, strategy `strict_object`, status `interrupted`, schema outcome `invalid`, and error `model_contract_invalid`; accepted artifact and artifact hash are null.

The opening turn remains interrupted at `admitted`, with empty mutation audit, no final world version, result, actor job, narration, or Opening mutation. Setup remains `opening_required` at world version `8` and runtime revision `42`; runtime event sequences are contiguous `1..42`. SQLite integrity is `ok`, and foreign-key check is empty. The real Play page renders `The opening stopped before it finished.` and an unused `Resume` control. Screenshot `output/playtests/campaign-play/pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r26.session/screenshots/opening-hard-stop.png` has SHA-256 `dd2b5433f7d4eafcbf9424fba545ee053cc2b4233b115cabf235bdf4e705121e`.

The exact missing-field path is live-log evidence; durable SQLite retains the invalid schema outcome and model-contract error. Task 18 does not permit Resume for this failure. r26 is frozen before a completed Opening. This is another already covered strict Opening-object failure, not evidence for a fallback, retry, prompt change, database edit, or harness expansion. The next Lane A attempt materializes the same zero-character template under a new run ID and preserves the provider/model configuration.

Humanizer and deslop review kept the note chronological, separated durable and live-log evidence, and made no product or long-play claim from the failed Opening.
