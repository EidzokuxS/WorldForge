# Task 18 Lane A r27: Opening plan-sequence hard stop

Run `pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r27` materialized the unchanged frozen Lowwater Ledger template at accepted/world/runtime `1/1/1`, `character_required`, with zero characters and turns, SQLite integrity `ok`, and no foreign-key violations. The rendered Character page imported a canonical Character Card V2 for Brina Hael through its normal native-resident UI path. Card synthesis and power assessment accepted their first `glm-5-turbo` native-object attempts in `65959 ms` and `19705 ms`.

Human review removed donor-synthesis references to the halved lumens lease and rising debt from Brina's biography, worldview, contradiction, and sample line before acceptance. The accepted player is an ordinary lower-ward parcel carrier seeking regular customers and fair fees, with no faction role, office, secret, power, or prior conflict connection. Three generated current possessions entered through typed character bootstrap. The human player selected `lower-wards / Local / Already here / Looking for work` and submitted Begin once.

Opening Planner attempt one returned normally after `699533 ms` with finish reason `stop`. The native object passed provider/schema parsing but failed strict semantic compilation as `opening_proposal_invalid` inside `validatePlanStepSequence` and `compilePlans`. That boundary rejects an actor plan whose move route does not begin at the actor's current step location, whose optional move destination conflicts with the route, or whose non-move step targets a location other than the actor's location after preceding steps. The rejected artifact is not persisted, so the exact offending plan branch cannot be recovered.

SQLite records one `opening_planner` attempt with requested and actual model `glm-5-turbo`, strategy `strict_object`, status `interrupted`, schema outcome `invalid`, error `model_contract_invalid`, and no artifact hash. The opening turn is interrupted at `admitted` with empty mutation audit, no final world version, result, actor job, narration, or Opening mutation. Setup remains `opening_required` at world version `5` and runtime revision `74`; runtime event sequences are contiguous `1..74`. SQLite integrity is `ok`, and foreign-key check is empty.

The real Play page renders `The opening stopped before it finished.` and an unused `Resume` control. Screenshot `output/playtests/campaign-play/pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r27.session/screenshots/opening-hard-stop.png` has SHA-256 `dba2b9e228fffe17ab4254d457249f4c57ba901f5452716416d8477768deb414`. Task 18 does not permit Resume for this model-contract result, so r27 is frozen before a completed Opening.

This matches the already documented r20 actor-plan sequence rejection. It is distinct from r25's invalid trigger member and r26's missing trigger array, and it does not justify a fallback, retry, prompt change, database edit, or harness expansion. The next Lane A attempt materializes the same frozen zero-character template under a new run ID and keeps the provider/model configuration unchanged.

Humanizer and deslop review kept the note chronological, bounded the inference to the validation site, and made no product or long-play claim from the failed Opening.
