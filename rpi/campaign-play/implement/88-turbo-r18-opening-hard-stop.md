# Turbo r18 Opening hard stop

## Outcome

Run `pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r18` materialized the unchanged frozen Lowwater Ledger template on commit `e9ce7fd2`. Template hashes, zero-turn state, and Lane A eligibility were frozen before the rendered character flow.

The UI generated Vesa Torlin in one draft request. Human review kept her ordinary freelance lumens work, family debt, practical tools, and independence, while removing a generated secret-light plot hook and replacing a token-reader bypass key with an insulated meter probe. The player accepted her outside the ruling structures, selected `lower-wards / Local / Already here / Looking for work`, and submitted Begin once.

Opening Planner attempt 1 ended after `462,988 ms` with `finish_reason=stop`, but the strict artifact failed schema validation. The proposal placed `observableTrace`, `possessionOutcome`, `obligationOutcome`, and `elapsedBounds` inside each step's `intent` while omitting them from the step envelope. SQLite records `interrupted/model_contract_invalid`, `schema_outcome=invalid`, with Z.AI Coding Plan `glm-5-turbo`. This differs from r17's invalid zero cadence and does not establish one repeatable prompt or compiler defect.

The rendered UI remained at `opening_active`, `worldVersion=7`, `runtimeRevision=51`, projection hash `27902740aa7146acc102548f1309171f8331a584e2ceef8038aa23191309e771`, with the same interrupted Opening turn and one Resume control. The ledger contains six matched character-bootstrap commands and receipts. SQLite `integrity_check` is `ok`; `foreign_key_check` returns no rows. No Opening artifact, narration, hidden retry, text fallback, provider switch, model switch, restore, rewind, or duplicate submission occurred.

The terminal screenshot is `output/playtests/campaign-play/pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r18.session/screenshots/opening-hard-stop.png`, SHA-256 `1c995a56fbed333e44a01fbf8cc749c053e89c0a7e31b3ef1cf2255dd2765df0`.

## Disposition

The contract failure ends r18 under Task 18's pristine boundary, so Resume is not used. The next Lane A attempt materializes the same zero-turn template under a new run id and keeps the provider/model configuration unchanged.

Humanizer review kept Vesa's accepted profile occupational and personal. Deslop review preserved the exact validation shape, model duration, state, hashes, integrity results, and no-recovery boundary without redundant framing.
