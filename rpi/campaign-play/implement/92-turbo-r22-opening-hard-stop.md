# Turbo r22 Opening hard stop

## Outcome

Run `pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r22` materialized the unchanged frozen Lowwater Ledger template on commit `f2dbe9ef`. Template hashes, zero-turn state, and Lane A eligibility were frozen before the rendered character flow.

The human player used the normal Describe path. One GLM-5-Turbo draft produced Maren Vell, an ordinary lower-ward rope and harness repairer with a lease need, no faction, no hidden knowledge, and no power-structure connection. The draft was accepted unedited. The player selected `lower-wards / Local / Already here / Looking for work` and submitted Begin once.

Opening Planner attempt 1 ended after `130,176 ms`. Native structured output was unavailable because the provider response could not be parsed, producing `NoObjectGeneratedError` and `finish_reason=other`. SQLite records `interrupted/model_contract_invalid`, `schema_outcome=invalid`, with Z.AI Coding Plan `glm-5-turbo`. This repeats r19's failure class, while r20 and r21 between them produced parseable objects rejected by two distinct semantic validators.

The rendered UI remained at `opening_active`, `worldVersion=8`, `runtimeRevision=17`, projection hash `61085f048253e83a86d813752dc4eb7bbd07ae26175dc1f4b52aaf641a7b9ec0`, with the same interrupted Opening turn and one Resume control. The turn retained `worker_epoch=1`. Seven character-bootstrap commands have seven matching receipts. SQLite `integrity_check` is `ok`; `foreign_key_check` returns no rows. No Opening artifact, narration, hidden retry, text fallback, provider switch, model switch, restore, rewind, or duplicate submission occurred.

The terminal screenshot is `output/playtests/campaign-play/pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r22.session/screenshots/opening-hard-stop.png`, SHA-256 `11de098dca26175841d6ef5a3fd43d93a521358617b1cd7b97d5989c89eccbe4`.

## Disposition

The model-contract result ends r22 under Task 18's pristine boundary, so Resume is not used. The next Lane A attempt materializes the same zero-turn template under a new run id and keeps the provider/model configuration unchanged.

Humanizer review kept Maren Vell's profile concrete and ordinary. Deslop review preserved the exact parse failure, duration, worker epoch, state, hashes, integrity results, and no-recovery boundary without redundant framing.
