# Task 18 Lane A r32: Opening semantic hard stop

Run `pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r32` materialized the unchanged frozen Lowwater Ledger template at accepted/world/runtime `1/1/1`, `character_required`, with zero characters and turns, SQLite integrity `ok`, and no foreign-key violations.

The rendered Character page imported the exact Character Card V2 JSON captured from r31 once through its native-resident UI path. Synthesis and power assessment accepted their first `glm-5-turbo` native-object attempts in `133270 ms` and `32075 ms`. Human review replaced campaign-tailored details with the same ordinary carrier profile used in r28 through r31. One player PUT produced one character and four typed bootstrap commands, receipts, and events, advancing world version `1` to `5`. The player selected `lower-wards / Local / Already here / Looking for work` and submitted Begin exactly once.

Opening turn `turn-opening:848594cae5e0bb7528ea2ab8c7d0199a3cc3958b` had one Planner stage and one model attempt. The native-object call succeeded after `960550 ms`, with finish reason `stop`, `16590` input tokens, and `21491` output tokens. The live backend then rejected the proposal as `opening_proposal_invalid` during semantic compilation in `validateIntent`, whose guard requires every intent target to exist and have a directed path from the actor's location. The durable stage records requested and actual provider `zai-coding-plan`, model `glm-5-turbo`, and strategy `strict_object`; it is `interrupted`, schema outcome `invalid`, error `model_contract_invalid`, with no accepted artifact or artifact hash.

The turn is interrupted at `admitted`, with empty mutation audit, no final world version, public packet, result, commands, receipts, world events, actor jobs, proposals, narration, or Opening mutation. Setup remains `opening_required` at world version `5`. Runtime events are contiguous `1..100` with matching revision steps; sequences `2..100` remain on world version `5`. SQLite integrity remains `ok`, and foreign-key check remains empty.

The real Play page renders `The opening stopped before it finished.` and an unused `Resume` control. Screenshot `output/playtests/campaign-play/pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r32.session/screenshots/opening-hard-stop.png` has SHA-256 `44269194d35505d36876c9af144ed5daeacc743fa3f8e411b11d67b04d459bd0`.

Task 18 treats any model, schema, or semantic invalidity as a hard stop. Therefore r32 is frozen before a completed Opening despite the runtime recovery contract marking the turn resumable. Unlike r29 through r31, this response parsed as a native object and failed the existing semantic target-reachability guard. It remains provider/model evidence, not permission to resume the turn, switch provider or model inside the lane, enable fallback, change prompts, edit SQLite, or expand the harness. The next Lane A attempt materializes the same frozen zero-character template under a new run ID and keeps the declared provider/model configuration unchanged.

Independent Terra High review confirmed the one-attempt hard stop, intact runtime ledger, and absent Opening mutation from the exact r32 database. A separate read-only authority review confirmed that the plan and committed r28-r31 evidence do not authorize a provider/model swap, fallback, prompt change, or harness expansion inside the declared lane.

Humanizer and deslop review kept the note chronological, separated the successful native parse from the semantic compiler rejection, and made no product or long-play claim from the failed Opening.
