# Task 18 Lane A r28: Action 2 Game Master hard stop

Run `pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r28` materialized the unchanged frozen Lowwater Ledger template at accepted/world/runtime `1/1/1`, `character_required`, with zero characters and turns, SQLite integrity `ok`, and no foreign-key violations. The rendered Character page imported the canonical Character Card V2 for Brina Hael through its native-resident UI path. Human review removed conflict-specific donor material before acceptance. Three ordinary current possessions entered through typed character bootstrap, and the human player submitted Begin once with `lower-wards / Local / Already here / Looking for work`.

Opening Planner and Narrator accepted their first `glm-5-turbo` strict-object attempts in `549816 ms` and `101006 ms`. The completed Opening reached world version `12` and runtime revision `89`. Screenshot `output/playtests/campaign-play/pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r28.session/screenshots/action-0.png` has SHA-256 `a431676d6703cadffbff29129defbf9ee87b8e2781cb7fcc2c64c5f910580ed5`.

For player action 1, the human player selected the current signed choice handle `choice_8e265a6b2c86103b0656eaed`, `Talk to Dren Vask: ask what the vendors need moved`. The browser submitted one turn, `turn-player-action:07af45b10a9f0ca67a878781494d0e97bab5274d`. Judge, Game Master, and Narrator each accepted attempt one on `glm-5-turbo` with strategy `strict_object`, valid schema, and no error in `154702 ms`, `81414 ms`, and `66160 ms`. The turn completed once from world version `12` to `13` with terminal reason `action_resolved`; its two due actor commands have matching receipts. Runtime events were contiguous through revision `133`.

The rendered result kept Brina in the alderman's hallway. Dren could not name a specific load and directed her to ask the market vendors, adding that some were short-handed after trouble at the winch shaft. Screenshot `output/playtests/campaign-play/pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r28.session/screenshots/action-1.png` has SHA-256 `03ca1e0acd270fd83ca897f8481d3590c1f80b3f3fea8bfba36ba7dcae6f3a9a`. The mandatory cold reload after action 1 reproduced public-state hash `9cf7acbdbae3509715ff90da9c9cb5cf270dda8fa3f409adb81d9631067a26bb` exactly.

For player action 2, the human player selected the current signed choice handle `choice_d03ac849eaf27578ae871d10`, `Go to Dim Ward Market`, because Dren had established that the vendors themselves held the direct work lead. The browser submitted exactly one new turn, `turn-player-action:47466ddc5943d63dde387ea72fea464186c047d7`, from world version `13` and runtime revision `133`. Judge attempt one accepted a valid deterministic move ruling in `48998 ms`.

Game Master attempt one returned normally after `72916 ms` with finish reason `stop`, but its native object failed semantic compilation. The live backend warning retained the rejected proposal and showed a `move_actor` effect with `actorHandle: null`, followed by `enter_local_scene` and `record_world_event`. The stage is recorded as `interrupted`, schema outcome `invalid`, error `model_contract_invalid`, with no artifact or artifact hash.

The action 2 turn is interrupted at `judged`, with empty mutation audit, no final world version, turn result, actor job, narration, public packet, command, receipt, or world event. World version remains `13`; the previous action 1 narration and consequence remain authoritative. Runtime events are contiguous `1..149`, SQLite integrity is `ok`, and foreign-key check is empty. The real Play page renders `The turn stopped before it finished.` and an unused `Resume` control. Screenshot `output/playtests/campaign-play/pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r28.session/screenshots/action-2-hard-stop.png` has SHA-256 `2fb5c377f54118c33857ef49c8085d9e0246e620f61991ac0d4ddaac05f9920c`.

Task 18 treats any model, schema, or semantic invalidity as a hard stop even when the runtime recovery contract marks a turn resumable. Therefore r28 is frozen with one eligible completed player action; its action 1 checkpoint remains valid, but the run cannot contribute a second eligible action or continue toward 60. The next Lane A attempt materializes the same frozen zero-character template under a new run ID and keeps the provider/model configuration unchanged.

Humanizer and deslop review kept the note chronological, distinguished the clean action 1 checkpoint from the later run-level stop, and made no fallback, retry, prompt-change, or product-success claim from the rejected action.
