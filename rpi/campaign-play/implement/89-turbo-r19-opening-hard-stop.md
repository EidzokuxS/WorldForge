# Turbo r19 Opening hard stop

## Outcome

Run `pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r19` materialized the unchanged frozen Lowwater Ledger template on commit `30fd8f95`. Template hashes, empty character/play/turn tables, and Lane A eligibility were frozen before the rendered character flow.

The UI generated Verra Kest in one draft request. Human review retained her ordinary cable-maintenance trade, debt history, practical loadout, and need for steady work, while removing the generator's repeated secret-light hook. The player accepted her outside Craghold's ruling structures, selected `lower-wards / Local / Already here / Looking for work`, and submitted Begin once.

Opening Planner attempt 1 ended after `400,796 ms`. Native structured output was unavailable because the provider response could not be parsed, producing `NoObjectGeneratedError` and `finish_reason=other`. SQLite records `interrupted/model_contract_invalid`, `schema_outcome=invalid`, with Z.AI Coding Plan `glm-5-turbo`. This differs from r17's zero-cadence artifact and r18's misplaced step fields, so the three lanes do not identify one repeatable prompt or compiler defect.

The rendered UI remained at `opening_active`, `worldVersion=6`, `runtimeRevision=44`, projection hash `07ba80843b9b3bb0c9322d477c995187d762fff9f8a23d45a9509d6715c6c4d5`, with the same interrupted Opening turn and one Resume control. Five character-bootstrap commands have five matching receipts. SQLite `integrity_check` is `ok`; `foreign_key_check` returns no rows. No Opening artifact, narration, hidden retry, text fallback, provider switch, model switch, restore, rewind, or duplicate submission occurred.

The terminal screenshot is `output/playtests/campaign-play/pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r19.session/screenshots/opening-hard-stop.png`, SHA-256 `2289e6b3fbc655a0d0b95c05df95fdecada6bf3334498fc55e7ba3b83081cbda`.

## Disposition

The model-contract result ends r19 under Task 18's pristine boundary, so Resume is not used. The next Lane A attempt materializes the same zero-turn template under a new run id and keeps the provider/model configuration unchanged.

Humanizer review kept Verra's profile grounded in work and personal need. Deslop review preserved the exact parse failure, duration, state, hashes, integrity results, and no-recovery boundary without repeated framing.
