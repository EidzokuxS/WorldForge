# Turbo r20 Opening hard stop

## Outcome

Run `pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r20` materialized the unchanged frozen Lowwater Ledger template on commit `30f8d875`. Template hashes, zero-turn state, and Lane A eligibility were frozen before the rendered character flow.

The human player used the normal Describe path after three random drafts repeated the same secret-light bias. One GLM-5-Turbo draft produced Tessa Vellin, an ordinary lower-ward rope and harness repairer with no faction, office, hidden knowledge, supernatural ability, or ruler connection. Human review accepted the draft and added one explicit motive and drive: earn enough routine repair work to keep her lease current. The player selected `lower-wards / Local / Already here / Looking for work` and submitted Begin once.

Opening Planner attempt 1 returned a schema-valid object after `326,123 ms`, but semantic compilation rejected its actor-plan step sequence as `opening_proposal_invalid`. SQLite records the stage as `interrupted/model_contract_invalid`, `schema_outcome=invalid`, `finish_reason=stop`, with Z.AI Coding Plan `glm-5-turbo`. This is distinct from the zero cadence in r17, misplaced step fields in r18, and unparseable output in r19.

The rendered UI remained at `opening_active`, `worldVersion=8`, `runtimeRevision=37`, projection hash `0282915224de16ddd0923dedc043f38380340ea0b30864ffb4104d0832c47fd7`, with the same interrupted Opening turn and one Resume control. Seven character-bootstrap commands have seven matching receipts. SQLite `integrity_check` is `ok`; `foreign_key_check` returns no rows. No Opening artifact, narration, hidden retry, text fallback, provider switch, model switch, restore, rewind, or duplicate submission occurred.

The terminal screenshot is `output/playtests/campaign-play/pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r20.session/screenshots/opening-hard-stop.png`, SHA-256 `60616c90ab7d1cc20a8fb5067413bce79eafd925ce5b34639c2b3013acbc7df6`.

## Disposition

The model-contract result ends r20 under Task 18's pristine boundary, so Resume is not used. The next Lane A attempt materializes the same zero-turn template under a new run id and keeps the provider/model configuration unchanged.

Humanizer review kept Tessa's profile concrete and free of plot privilege. Deslop review preserved the exact semantic failure, duration, state, hashes, integrity results, and no-recovery boundary without redundant framing.
