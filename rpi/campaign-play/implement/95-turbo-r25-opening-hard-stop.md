# Task 18 Lane A r25: Opening Planner hard stop

Run `pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r25` materialized the unchanged frozen Lowwater Ledger template after the r24 evidence stop. The pre-UI state was accepted/world/runtime `1/1/1`, `character_required`, with zero characters and turns, SQLite integrity `ok`, and zero foreign-key violations. Independent read-only verification reproduced the template database hash `6cb291d11ce6578e3395a10d2c5d590070e3ccdd8b2b67451410869ce97897d2`, config hash `d8362b1af976c00cb8f14c564c8196a2d1ab137743ff368ea83d762a4ad2e065`, eligibility hash `f452c21cc0c12d382976d89cdcd3678ef6a922924be8604bc74fa120f397a852`, and the eligible three-macro, seven-scene, sixteen-route topology.

The rendered Character flow submitted one draft request and generated Vessa Trellin. Synthesis and power assessment accepted their first `glm-5-turbo` native-object attempts in `52595 ms` and `17527 ms`. Human review removed campaign-tailored light-debt knowledge from her worldview, contradiction, and sample line before acceptance. The resulting player was an ordinary lower-ward cloth mender seeking routine paid work, with no faction role, office, secret, power, or prior conflict connection. The human player selected `lower-wards / Local / Already here / Looking for work` and submitted Begin once.

Opening Planner attempt one stopped after `496908 ms`. The provider returned normally with finish reason `stop`, but strict schema validation rejected `hiddenConsequence.exposure.triggers[0]` because its value was outside the allowed `inspect | attempt | traverse` enum. SQLite records `opening_planner`, attempt `1`, requested and actual model `glm-5-turbo`, strategy `strict_object`, status `interrupted`, schema outcome `invalid`, and error `model_contract_invalid`. It retains no accepted artifact. The opening turn remains interrupted with no result, actor job, narration, or Opening mutation; setup is `opening_required`, world version `8`, runtime revision `54`, and runtime events are contiguous `1..54`. SQLite integrity is `ok`, and foreign-key check is empty.

The real Play page renders `The opening stopped before it finished.` and the normal `Resume` control. Task 18 permits pristine recovery only for transport failure or loss of worker authority, so Resume was not used. r25 is frozen before a completed Opening. The exact invalid field is live-log evidence; durable SQLite retains the stage-level schema outcome and error code. Screenshot `output/playtests/campaign-play/pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r25.session/screenshots/opening-hard-stop.png` has SHA-256 `a2d69433ff7fe9054e2eb216eee8e3c1e7996f08154193e4ec39d09700d7249b`.

This matches the already covered invalid Opening exposure-trigger failure and does not justify a fallback, retry, prompt change, database edit, or harness expansion. The next Lane A attempt materializes the same frozen zero-character template under a new run ID and keeps the provider/model configuration unchanged.

Humanizer and deslop review kept the note chronological, identified the live-log-only validation detail, and made no product or long-play claim from the failed Opening.
