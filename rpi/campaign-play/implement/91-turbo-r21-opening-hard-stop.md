# Turbo r21 Opening hard stop

## Outcome

Run `pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r21` materialized the unchanged frozen Lowwater Ledger template on commit `0f05fc73`. Template hashes, zero-turn state, and Lane A eligibility were frozen before the rendered character flow.

The human player used the normal Describe path. One GLM-5-Turbo draft produced Maren, an ordinary lower-ward rope and harness repairer with a current lease need, no faction, no hidden knowledge, and no special connection to the city's power structures. The draft already contained the intended motive and drive and was accepted unedited. The player selected `lower-wards / Local / Already here / Looking for work` and submitted Begin once.

Opening Planner attempt 1 returned a schema-valid object after `602,363 ms`, but semantic compilation rejected an actor-plan intent as `opening_proposal_invalid` in `validateIntent`. SQLite records `interrupted/model_contract_invalid`, `schema_outcome=invalid`, `finish_reason=stop`, with Z.AI Coding Plan `glm-5-turbo`. r20 failed the separate plan-step sequence validator, so the two semantic failures are not the same rejected condition.

The turn retained `worker_epoch=1`; no ledger evidence records a worker-authority loss despite development-watcher restart messages in the server log. The rendered UI remained at `opening_active`, `worldVersion=8`, `runtimeRevision=64`, projection hash `3ab03658c85f06886e22ff7cc53f9ac4b9c88d4121654e5829fa9342911fb234`, with the same interrupted Opening turn and one Resume control. Seven character-bootstrap commands have seven matching receipts. SQLite `integrity_check` is `ok`; `foreign_key_check` returns no rows. No Opening artifact, narration, hidden retry, text fallback, provider switch, model switch, restore, rewind, or duplicate submission occurred.

The terminal screenshot is `output/playtests/campaign-play/pristine-60-glm5-turbo-lowwater-ledger-93a09e46-r21.session/screenshots/opening-hard-stop.png`, SHA-256 `6fc500c417b8ed7d26b08fe1ff7f8ab7341ca5899e8a89cc0310c3b3c59532e5`.

## Disposition

The model-contract result ends r21 under Task 18's pristine boundary, so Resume is not used. The next Lane A attempt materializes the same zero-turn template under a new run id and keeps the provider/model configuration unchanged.

Humanizer review kept Maren's profile concrete and ordinary. Deslop review preserved the semantic failure location, duration, worker epoch, state, hashes, integrity results, and no-recovery boundary without redundant framing.
