# Campaign World Build plan package

The canonical implementation plan is:

`docs/goals/campaign-world-build/PLAN.md`

The execution handoff is:

`docs/goals/campaign-world-build/GOAL.md`

This folder keeps review artifacts that must survive chat compaction:

- `prompt-review.md` for model instructions and visible copy;
- `ui-plan.md` for the required Droid GLM UI planning gate;
- `pre-review.md` for the Krypton PRE verdict and applied corrections.

Production work starts only after the PRE review is aligned and Task 0 records GitNexus impact.
