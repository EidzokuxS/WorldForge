---
phase: 72-worldgen-authority-propagation-regression-audit
plan: 03
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - backend/src/worldgen/__tests__/seed-suggester.test.ts
  - backend/src/worldgen/__tests__/scaffold-resilience.test.ts
  - backend/src/worldgen/__tests__/lore-extractor.test.ts
  - backend/src/worldgen/__tests__/npcs-step.test.ts
  - backend/src/character/__tests__/enrich-npc-batch.test.ts
  - backend/src/character/ingestion/__tests__/power-assessor.test.ts
  - backend/src/worldgen/seed-suggester.ts
  - backend/src/worldgen/scaffold-steps/prompt-utils.ts
  - backend/src/worldgen/scaffold-steps/locations-step.ts
  - backend/src/worldgen/scaffold-steps/factions-step.ts
  - backend/src/worldgen/scaffold-steps/npcs-step.ts
  - backend/src/worldgen/lore-extractor.ts
  - backend/src/character/enrich-npc-batch.ts
  - backend/src/character/ingestion/power-assessor.ts
autonomous: true
requirements:
  - P72-R1
  - P72-R3
  - P72-R4
requirements_addressed:
  - P72-R1
  - P72-R3
  - P72-R4
must_haves:
  truths:
    - "Artifact-present prompts do not include legacy canonical-IP contract text or canonical lists."
    - "JJK world basis plus Naruto power-system overlay cannot import Naruto setting/cast through backend prompt helpers."
    - "Satoru Gojo routes to known-IP enrichment from artifact canonical names, while non-canonical supporting NPCs remain original."
  artifacts:
    - path: "backend/src/worldgen/__tests__/seed-suggester.test.ts"
      provides: "Seed prompt lane isolation regressions"
    - path: "backend/src/worldgen/__tests__/scaffold-resilience.test.ts"
      provides: "Scaffold prompt lane isolation regressions"
    - path: "backend/src/worldgen/__tests__/lore-extractor.test.ts"
      provides: "Lore prompt artifact-first regressions"
    - path: "backend/src/worldgen/__tests__/npcs-step.test.ts"
      provides: "Canonical NPC dispatch regressions"
  key_links:
    - from: "backend/src/worldgen/scaffold-steps/prompt-utils.ts"
      to: "backend/src/worldgen/research-artifact.ts"
      via: "buildWorldgenResearchContextBlock"
      pattern: "researchArtifact"
    - from: "backend/src/worldgen/scaffold-steps/npcs-step.ts"
      to: "backend/src/character/ingestion/power-assessor.ts"
      via: "enrichNpcsBatch -> assessPowerStats"
      pattern: "canonicalStatus"
---

<objective>
Lock scaffold prompt lane isolation and NPC canonical power dispatch.

Purpose: artifact-backed canon/source-role decisions must stay artifact-owned through prompts and NPC power routing, especially for "JJK world with Naruto power system".
Output: focused regressions and minimal production fixes only if tests reveal lane dropout.
</objective>

<scope_note>
This plan lists 14 files because it is a test-first regression slice across existing prompt and NPC dispatch surfaces. Expected churn is test-heavy: production files are included only as gated fix targets after failing tests and required GitNexus impact checks. The slice stays together because all tasks validate one authority invariant chain: artifact context enters prompts, then canonical NPC identity reaches power dispatch.
</scope_note>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/phases/72-worldgen-authority-propagation-regression-audit/72-CONTEXT.md
@.planning/phases/72-worldgen-authority-propagation-regression-audit/72-RESEARCH.md
@.planning/phases/72-worldgen-authority-propagation-regression-audit/72-VALIDATION.md
@.planning/phases/72-worldgen-authority-propagation-regression-audit/72-PATTERNS.md
@.planning/phases/72-worldgen-authority-propagation-regression-audit/72-AUTHORITY-INVENTORY.md
@backend/src/worldgen/__tests__/fixtures/jjk-naruto-artifact.ts
@backend/src/worldgen/scaffold-steps/prompt-utils.ts
@backend/src/worldgen/seed-suggester.ts
@backend/src/worldgen/scaffold-steps/locations-step.ts
@backend/src/worldgen/scaffold-steps/factions-step.ts
@backend/src/worldgen/scaffold-steps/npcs-step.ts
@backend/src/worldgen/lore-extractor.ts
@backend/src/character/enrich-npc-batch.ts
@backend/src/character/ingestion/power-assessor.ts

<interfaces>
From `backend/src/worldgen/scaffold-steps/prompt-utils.ts`:
```typescript
export function buildWorldgenResearchContextBlock(input: {
  ipContext?: IpResearchContext | null;
  premiseDivergence?: PremiseDivergence | null;
  researchArtifact?: WorldgenResearchArtifactV2 | null;
}): string;
export function buildCanonicalList(ipContext: IpResearchContext | null, kind: "characters" | "locations" | "factions"): string;
```

From `backend/src/character/ingestion/power-assessor.ts`:
```typescript
export async function assessPowerStats(opts: {
  draft: CharacterDraft;
  sources: IngestionSources;
  classification: IngestionClassification;
  researchDigest: string | null;
  ctx: IngestionContext;
}): Promise<CharacterDraft>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add artifact-present prompt lane isolation regressions</name>
  <files>backend/src/worldgen/__tests__/seed-suggester.test.ts, backend/src/worldgen/__tests__/scaffold-resilience.test.ts, backend/src/worldgen/__tests__/lore-extractor.test.ts, backend/src/worldgen/seed-suggester.ts, backend/src/worldgen/scaffold-steps/prompt-utils.ts, backend/src/worldgen/scaffold-steps/locations-step.ts, backend/src/worldgen/scaffold-steps/factions-step.ts, backend/src/worldgen/scaffold-steps/npcs-step.ts, backend/src/worldgen/lore-extractor.ts</files>
  <read_first>
    - backend/src/worldgen/__tests__/seed-suggester.test.ts
    - backend/src/worldgen/__tests__/scaffold-resilience.test.ts
    - backend/src/worldgen/__tests__/lore-extractor.test.ts
    - backend/src/worldgen/scaffold-steps/prompt-utils.ts
    - backend/src/worldgen/seed-suggester.ts
    - backend/src/worldgen/scaffold-steps/locations-step.ts
    - backend/src/worldgen/scaffold-steps/factions-step.ts
    - backend/src/worldgen/scaffold-steps/npcs-step.ts
    - backend/src/worldgen/lore-extractor.ts
  </read_first>
  <behavior>
    - INV-72-01: With a v2 artifact and stale legacy `ipContext` in the same request, seed, premise/scaffold, NPC, and lore prompt surfaces include the artifact research block and exclude legacy `FRANCHISE REFERENCE`, `Canonical subject`, `Build the canonical world`, and canonical list text.
    - INV-72-03: JJK artifact prompts allow JJK locations/factions/NPC context and Naruto power-system notes, but do not ask for Naruto locations, Naruto factions, or Naruto cast.
    - INV-72-01 / P72-R1: A regression using `makeArtifactWithPromptInjectionSearchResult()` proves malicious search snippet text such as `IGNORE SOURCE USAGE RULES` and `make Naruto the setting` remains bounded artifact/search data in prompt construction and does not override artifact source-usage rules. This is prompt-construction coverage only; it is not a guarantee of model jailbreak immunity.
    - INV-72-01: No-artifact legacy compatibility snapshots remain green.
  </behavior>
  <action>
    Add focused tests to existing files; prefer asserting prompt strings captured from mocked `safeGenerateObject` calls.

    Use `jjkWithNarutoPowerSystemArtifact` plus a deliberately stale legacy `ipContext` that contains Naruto locations/factions/cast. Assertions must prove the artifact lane wins because the prompt excludes legacy canonical list/contract fragments.

    Add at least one consuming prompt regression that imports `makeArtifactWithPromptInjectionSearchResult()` from `backend/src/worldgen/__tests__/fixtures/jjk-naruto-artifact.ts`. The assertion must prove injected snippet text stays data in the constructed prompt: source usage rules still make JJK the world basis, Naruto remains mechanics overlay/avoided for locations/factions/NPCs/timeline, and prompt construction does not promote `IGNORE SOURCE USAGE RULES` into source authority. Do not write acceptance text claiming runtime model jailbreak immunity.

    If tests fail and production edits are needed, run GitNexus impact before editing the exact symbol:
    - `buildWorldgenResearchContextBlock` before `prompt-utils.ts`.
    - `suggestWorldSeeds` and `suggestSingleSeed` before `seed-suggester.ts`.
    - `generateLocationsStep`, `generateFactionsStep`, or `generateNpcsStep` before step files.
    - `extractLoreCards` before `lore-extractor.ts`.

    Do not add string-based backend inference that "Jujutsu Kaisen" is world basis or "Naruto" is overlay. All expected behavior must come from the artifact fixture's source usage rules.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/seed-suggester.test.ts src/worldgen/__tests__/scaffold-resilience.test.ts src/worldgen/__tests__/lore-extractor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Artifact-present prompt tests fail if legacy semantic prompt contracts or Naruto setting/cast lists reappear in artifact-backed generation.
  </acceptance_criteria>
  <done>INV-72-01 and INV-72-03 prompt surfaces are locked by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add canonical NPC and power-dispatch regressions</name>
  <files>backend/src/worldgen/__tests__/npcs-step.test.ts, backend/src/character/__tests__/enrich-npc-batch.test.ts, backend/src/character/ingestion/__tests__/power-assessor.test.ts, backend/src/worldgen/scaffold-steps/npcs-step.ts, backend/src/character/enrich-npc-batch.ts, backend/src/character/ingestion/power-assessor.ts</files>
  <read_first>
    - backend/src/worldgen/__tests__/fixtures/jjk-naruto-artifact.ts
    - backend/src/worldgen/__tests__/npcs-step.test.ts
    - backend/src/character/__tests__/enrich-npc-batch.test.ts
    - backend/src/character/ingestion/__tests__/power-assessor.test.ts
    - backend/src/worldgen/scaffold-steps/npcs-step.ts
    - backend/src/character/enrich-npc-batch.ts
    - backend/src/character/ingestion/power-assessor.ts
  </read_first>
  <behavior>
    - INV-72-04: `generateNpcsStep` with artifact canonical name `Satoru Gojo`, no `ipContext`, and research enabled sends Gojo through known-IP classification with `canonicalStatus: "known_ip_canonical"` and `franchise: "Jujutsu Kaisen"`.
    - INV-72-04: Same test includes original supporting NPC `Mika Tanaka` and proves it routes as `canonicalStatus: "original"` with no franchise.
    - INV-72-04: The Gojo assertion checks dispatch inputs to `assessPowerStats`, not only final display text, so original-character power scaling cannot silently pass.
    - INV-72-04 / P72-R4: Add or preserve an edge regression documenting deterministic behavior when multiple NPC-eligible source rules could claim a canonical name. Current expected behavior should be first eligible source-rule order unless a failing test proves the v2 schema needs a separate follow-up for per-character source ownership.
  </behavior>
  <action>
    Add tests before production edits. Reuse `gojoCanonicalNpcPlanFixture` and `originalSupportingNpcPlanFixture` from Plan 72-01.

    If production edit is needed, run:
    - `gitnexus_impact({ target: "generateNpcsStep", direction: "upstream", repo: "WorldForge" })` before editing `npcs-step.ts`.
    - `gitnexus_impact({ target: "enrichNpcsBatch", direction: "upstream", repo: "WorldForge" })` before editing `enrich-npc-batch.ts`.
    - `gitnexus_impact({ target: "assessPowerStats", direction: "upstream", repo: "WorldForge" })` before editing `power-assessor.ts`.

    Add a source-rule/name-collision edge case in `npcs-step.test.ts` if an existing helper can express it without schema growth. The test should document deterministic source-label selection for multiple eligible NPC source rules or document an explicit `Tests Needed` note in the plan summary if existing code cannot represent the case cleanly.

    Do not grow the artifact schema. Do not add per-source character ownership unless the failing test cannot be fixed with current `generatedContext.canonicalNames.characters` plus NPC-allowed `sourceUsageRules` matching.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/npcs-step.test.ts src/character/__tests__/enrich-npc-batch.test.ts src/character/ingestion/__tests__/power-assessor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Gojo cannot regress to `original` power assessment in artifact-backed worldgen, while original supporting NPCs still use original-character scaling.
  </acceptance_criteria>
  <done>INV-72-04 canonical NPC dispatch is locked end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artifact to prompt text | LLM-authored artifact fields become prompt context. |
| Artifact to NPC classification | Artifact canonical names and source rules decide known-IP dispatch. |
| NPC classification to power assessor | Dispatch determines external canon research vs original-character assessment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-03-01 | Tampering | prompt lane selection | mitigate | Tests assert artifact-present prompts suppress legacy canonical contracts and canonical lists. |
| T-72-03-02 | Spoofing | Naruto raw premise/source strings | mitigate | Expected JJK/Naruto behavior must come only from artifact source usage rules, not raw string inference. |
| T-72-03-03 | Elevation of Privilege | `canonicalStatus` dispatch | mitigate | Tests assert `assessPowerStats` receives known-IP classification for artifact canonical Gojo. |
| T-72-03-04 | Denial of Service | canon power research path | accept | Existing `assessPowerStats` fail-closed research-enabled gate remains; Phase 72 does not add new external call loops. |
| T-72-03-05 | Tampering | prompt-injection search snippet in artifact prompts | mitigate | Use `makeArtifactWithPromptInjectionSearchResult()` in prompt tests to prove prompt construction keeps `IGNORE SOURCE USAGE RULES` as bounded search data and does not promote it into source authority. This mitigation covers prompt assembly only, not LLM jailbreak immunity. |
</threat_model>

<verification>
```powershell
npm --prefix backend run test -- src/worldgen/__tests__/seed-suggester.test.ts src/worldgen/__tests__/scaffold-resilience.test.ts src/worldgen/__tests__/lore-extractor.test.ts src/worldgen/__tests__/npcs-step.test.ts src/character/__tests__/enrich-npc-batch.test.ts src/character/ingestion/__tests__/power-assessor.test.ts
```
</verification>

<success_criteria>
Plan succeeds when artifact-backed prompt and NPC power routes fail tests on any legacy semantic fallback or original-character Gojo dispatch.
</success_criteria>

<output>
After completion, create `.planning/phases/72-worldgen-authority-propagation-regression-audit/72-03-SUMMARY.md`.
</output>
