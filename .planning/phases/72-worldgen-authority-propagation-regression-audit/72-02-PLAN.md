---
phase: 72-worldgen-authority-propagation-regression-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - backend/src/worldgen/__tests__/research-artifact.test.ts
  - backend/src/worldgen/__tests__/ip-researcher.test.ts
  - backend/src/routes/__tests__/schemas.test.ts
  - backend/src/routes/__tests__/worldgen.test.ts
  - backend/src/worldgen/research-artifact.ts
  - backend/src/worldgen/ip-researcher.ts
  - backend/src/routes/schemas.ts
  - backend/src/routes/worldgen.ts
autonomous: true
requirements:
  - P72-R2
  - P72-R5
requirements_addressed:
  - P72-R2
  - P72-R5
must_haves:
  truths:
    - "Provider/search payload overflow cannot crash strict artifact parsing."
    - "`researchArtifact: null` in `/generate` is treated as omitted, not as authority to bypass a stored campaign artifact."
    - "Legacy no-artifact compatibility still works when no request or stored artifact exists."
  artifacts:
    - path: "backend/src/worldgen/__tests__/research-artifact.test.ts"
      provides: "Parser boundary regressions"
    - path: "backend/src/worldgen/__tests__/ip-researcher.test.ts"
      provides: "Provider ingress and sufficiency follow-up regressions"
    - path: "backend/src/routes/__tests__/worldgen.test.ts"
      provides: "Generate route nullable artifact lane regression"
    - path: "backend/src/routes/worldgen.ts"
      provides: "Route fix only if failing test proves current nullable semantics are wrong"
  key_links:
    - from: "backend/src/routes/worldgen.ts"
      to: "backend/src/campaign/manager.ts"
      via: "loadWorldgenResearchArtifact(campaignId)"
      pattern: "loadWorldgenResearchArtifact"
    - from: "backend/src/worldgen/ip-researcher.ts"
      to: "backend/src/worldgen/research-artifact.ts"
      via: "parseWorldgenResearchArtifact"
      pattern: "parseWorldgenResearchArtifact"
---

<objective>
Harden provider/schema payload boundaries and lock `/generate` nullable artifact semantics.

Purpose: artifact authority must not disappear because a provider returned a long snippet or because a client accidentally serialized `researchArtifact: null`.
Output: focused backend regressions and production fixes only where those regressions fail.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/phases/72-worldgen-authority-propagation-regression-audit/72-CONTEXT.md
@.planning/phases/72-worldgen-authority-propagation-regression-audit/72-RESEARCH.md
@.planning/phases/72-worldgen-authority-propagation-regression-audit/72-VALIDATION.md
@.planning/phases/72-worldgen-authority-propagation-regression-audit/72-PATTERNS.md
@.planning/phases/72-worldgen-authority-propagation-regression-audit/72-AUTHORITY-INVENTORY.md
@backend/src/worldgen/research-artifact.ts
@backend/src/worldgen/ip-researcher.ts
@backend/src/routes/schemas.ts
@backend/src/routes/worldgen.ts
@backend/src/worldgen/__tests__/fixtures/jjk-naruto-artifact.ts
@backend/src/worldgen/__tests__/research-artifact.test.ts
@backend/src/worldgen/__tests__/ip-researcher.test.ts
@backend/src/routes/__tests__/schemas.test.ts
@backend/src/routes/__tests__/worldgen.test.ts

<interfaces>
From `backend/src/worldgen/research-artifact.ts`:
```typescript
export function parseWorldgenResearchArtifact(value: unknown): WorldgenResearchArtifactV2;
export function formatWorldgenResearchArtifactBlock(artifact: WorldgenResearchArtifactV2): string;
```

From `backend/src/worldgen/ip-researcher.ts`:
```typescript
export async function researchWorldgenArtifact(req: ResearchableRequest, gen: ResolvedRole, maxSearchSteps: number): Promise<WorldgenResearchArtifactV2 | null>;
export async function evaluateResearchArtifactSufficiency(artifact: WorldgenResearchArtifactV2, step: WorldgenResearchStep, ...): Promise<WorldgenResearchArtifactV2>;
```
</interfaces>
</context>

<tasks>

<integration_note>
Recommended serial order inside Wave 2: execute 72-02 before 72-04 when not running wave agents in parallel. Nullable artifact semantics from this plan and frontend artifact transport from 72-04 interact at `/generate`; 72-05 remains the blocking integrated gate after both land.
</integration_note>

<task type="auto" tdd="true">
  <name>Task 1: Add provider and schema payload hardening regressions</name>
  <files>backend/src/worldgen/__tests__/research-artifact.test.ts, backend/src/worldgen/__tests__/ip-researcher.test.ts, backend/src/routes/__tests__/schemas.test.ts, backend/src/worldgen/research-artifact.ts, backend/src/worldgen/ip-researcher.ts, backend/src/routes/schemas.ts</files>
  <read_first>
    - backend/src/worldgen/__tests__/fixtures/jjk-naruto-artifact.ts
    - backend/src/worldgen/__tests__/research-artifact.test.ts
    - backend/src/worldgen/__tests__/ip-researcher.test.ts
    - backend/src/routes/__tests__/schemas.test.ts
    - backend/src/worldgen/research-artifact.ts
    - backend/src/worldgen/ip-researcher.ts
    - backend/src/routes/schemas.ts
  </read_first>
  <behavior>
    - INV-72-02: `parseWorldgenResearchArtifact(makeArtifactWithOverlongSearchDescription())` returns a parsed artifact with `searchResults[0].description.length === 700`.
    - INV-72-02: `researchWorldgenArtifact(...)` caps overlong provider `title`, `description`, and `url` before final artifact validation.
    - INV-72-02: `evaluateResearchArtifactSufficiency(...)` handles follow-up search results with overlong descriptions without throwing; it either returns normalized enriched artifact or explicit documented fallback to the original normalized artifact.
    - INV-72-05: `generateWorldSchema` and `suggestSeedSchema` accept `researchArtifact: null` at schema level; route semantics are tested in Task 2.
  </behavior>
  <action>
    Write tests first. Reuse `makeArtifactWithOverlongSearchDescription()` from Plan 72-01.

    If any production fix is needed, run GitNexus impact before editing:
    - `gitnexus_impact({ target: "parseWorldgenResearchArtifact", direction: "upstream", repo: "WorldForge" })` before editing `backend/src/worldgen/research-artifact.ts`.
    - `gitnexus_impact({ target: "researchWorldgenArtifact", direction: "upstream", repo: "WorldForge" })` and `gitnexus_impact({ target: "evaluateResearchArtifactSufficiency", direction: "upstream", repo: "WorldForge" })` before editing `backend/src/worldgen/ip-researcher.ts`.

    Limit production changes to deterministic trim/slice/cap at provider or artifact parser ingress. Do not loosen semantic fields, do not add backend source-role interpretation, and do not grow `WorldgenResearchArtifactV2`.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/research-artifact.test.ts src/worldgen/__tests__/ip-researcher.test.ts src/routes/__tests__/schemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Tests prove provider/search overflow is mechanically bounded before strict parse and nullable request artifact payloads remain schema-valid without defining clear-artifact semantics.
  </acceptance_criteria>
  <done>INV-72-02 has executable regression coverage and any necessary hardening stays mechanical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock `/generate` stored artifact precedence over accidental null</name>
  <files>backend/src/routes/__tests__/worldgen.test.ts, backend/src/routes/worldgen.ts</files>
  <read_first>
    - backend/src/routes/__tests__/worldgen.test.ts
    - backend/src/routes/worldgen.ts
    - backend/src/campaign/manager.ts
    - .planning/phases/72-worldgen-authority-propagation-regression-audit/72-AUTHORITY-INVENTORY.md
  </read_first>
  <behavior>
    - INV-72-05: When `loadWorldgenResearchArtifact(campaignId)` returns the JJK/Naruto artifact and `/api/worldgen/generate` body contains `{ campaignId, researchArtifact: null, ipContext: staleLegacyContext, premiseDivergence: staleLegacyDivergence }`, route passes the stored artifact to `generateWorldScaffold`.
    - INV-72-05: Same request does not call/load/forward legacy `ipContext`, `premiseDivergence`, or `worldgenResearchFrame` for semantic ownership.
    - INV-72-05: With no request artifact and no stored artifact, legacy no-artifact compatibility still loads legacy context as before.
  </behavior>
  <action>
    Add failing route tests to `backend/src/routes/__tests__/worldgen.test.ts` using the existing Hono `app.request()` pattern.

    If the null test fails, run `gitnexus_impact({ target: "worldgen.ts", direction: "upstream", repo: "WorldForge" })` or symbol-specific impact for the generate route handler before editing `backend/src/routes/worldgen.ts`.

    Fix target state in `backend/src/routes/worldgen.ts`: `researchArtifact: null` must be equivalent to an omitted request artifact when a stored campaign artifact exists. Do not add a clear-artifact behavior. Do not call `saveWorldgenResearchArtifact` with null. Preserve body artifact precedence when the body contains a non-null artifact.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/routes/__tests__/worldgen.test.ts</automated>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/research-artifact.test.ts src/worldgen/__tests__/ip-researcher.test.ts src/routes/__tests__/schemas.test.ts src/routes/__tests__/worldgen.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `/generate` cannot silently bypass stored campaign artifact authority via `researchArtifact: null`, and legacy no-artifact route behavior remains explicitly tested.
  </acceptance_criteria>
  <done>INV-72-05 route nullable semantics are locked and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search provider to artifact parser | Untrusted provider strings enter bounded Zod parse. |
| Browser/client to `/api/worldgen/generate` | Request body can contain null or stale legacy fields. |
| Campaign config to route handler | Stored artifact must remain authoritative unless a tested clear API exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02-01 | Denial of Service | `parseWorldgenResearchArtifact` / `researchWorldgenArtifact` | mitigate | Add overlong provider/search tests and cap fields at ingress only if needed. |
| T-72-02-02 | Tampering | `/api/worldgen/generate` nullable artifact body | mitigate | Test and fix null-as-omitted semantics so client null cannot erase stored artifact authority. |
| T-72-02-03 | Elevation of Privilege | stale `ipContext` beside artifact | mitigate | Route tests assert artifact lane suppresses legacy semantic lane. |
| T-72-02-04 | Repudiation | production changes without scope proof | mitigate | Require GitNexus impact before production edits and detect_changes or documented fallback before commit. |
</threat_model>

<verification>
```powershell
npm --prefix backend run test -- src/worldgen/__tests__/research-artifact.test.ts src/worldgen/__tests__/ip-researcher.test.ts src/routes/__tests__/schemas.test.ts src/routes/__tests__/worldgen.test.ts
```
</verification>

<success_criteria>
Plan succeeds when payload caps and nullable route semantics are proven by tests and any production change is limited to mechanical boundary handling.
</success_criteria>

<output>
After completion, create `.planning/phases/72-worldgen-authority-propagation-regression-audit/72-02-SUMMARY.md`.
</output>
