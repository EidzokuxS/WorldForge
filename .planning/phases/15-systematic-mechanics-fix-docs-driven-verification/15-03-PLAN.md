---
phase: 15-systematic-mechanics-fix-docs-driven-verification
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Strong Hit never causes HP loss in non-combat (Bug 1 verified)"
    - "Player can travel between locations via move_to tool (Bug 2 verified)"
    - "NPCs interact with player when present at same location (Bug 3 verified)"
    - "No tool-call text visible in narrative output (Bug 4 verified)"
    - "Auto-checkpoint created when HP drops to 2 or below (Bug 5 verified)"
    - "HP=0 triggers contextual death/defeat narration (Bug 6 verified)"
    - "All docs/ mechanics (tags, Oracle, tiers, factions, reflection, tools) working"
  artifacts: []
  key_links: []
---

<objective>
Systematic verification playtest: play through a multi-turn scenario testing ALL 6 bug fixes and every mechanic listed in docs/concept.md, docs/mechanics.md, docs/memory.md against the running game.

Purpose: Prove all bugs are fixed and all documented mechanics work correctly. This is the final validation.
Output: Verified playtest results confirming mechanics compliance.
</objective>

<execution_context>
@C:/Users/robra/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-systematic-mechanics-fix-docs-driven-verification/15-01-SUMMARY.md
@.planning/phases/15-systematic-mechanics-fix-docs-driven-verification/15-02-SUMMARY.md
@.planning/phases/15-systematic-mechanics-fix-docs-driven-verification/15-CONTEXT.md
@docs/concept.md
@docs/mechanics.md
@docs/memory.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API-driven verification of all 6 bug fixes</name>
  <read_first>
    - .planning/phases/15-systematic-mechanics-fix-docs-driven-verification/15-CONTEXT.md (bug descriptions and verify criteria)
    - docs/mechanics.md (Oracle flow, tool sets, character tiers)
    - docs/concept.md (turn anatomy, location graph)
    - docs/memory.md (checkpoints, episodic memory)
  </read_first>
  <files></files>
  <action>
Start both servers (backend on :3001, frontend on :3000). Create or load a campaign that has:
- World with multiple connected locations
- Key NPCs assigned to locations
- Player character with HP=5

Run the following verification sequence via API calls (curl/fetch):

**Bug 1 — HP guard on Strong Hit:**
- POST /api/chat/action with a simple action that gets a Strong Hit
- Verify: if any set_condition tool call with negative delta was made, it should be REJECTED (error in tool result)
- If HP decreased on Strong Hit, the fix is broken

**Bug 2 — move_to tool:**
- POST /api/chat/action with intent to travel: "I walk to [connected location name]"
- Verify: state_update event contains move_to tool call result with locationName
- Verify: GET /api/campaigns/active shows player at new location
- If player location didn't change, the fix is broken

**Bug 3 — NPC interaction:**
- Ensure player is at a location with a Key NPC
- POST /api/chat/action with "I look around"
- Verify: narrative text mentions the NPC by name
- Verify: NPC states section was in the prompt (check server logs)

**Bug 4 — Tool-call sanitization:**
- Play 5+ turns with various actions
- Verify: no narrative output contains patterns like `add_tag(`, `set_condition(`, `default_api.`, or any function-call syntax

**Bug 5 — Auto-checkpoint:**
- Get HP to 3, then submit an action that causes damage (combat)
- If HP drops to 2 or below, verify: auto_checkpoint SSE event was emitted
- Verify: GET /api/campaigns/:id/checkpoints shows an auto-danger checkpoint

**Bug 6 — HP=0 context:**
- Continue combat until HP reaches 0
- Verify: Storyteller narrates a death/defeat/KO outcome contextually
- Verify: narrative does NOT continue with normal gameplay after HP=0

Record all results with pass/fail for each bug.
  </action>
  <verify>
    <automated>curl -s http://localhost:3001/api/health | grep -q "ok" && echo "Backend running"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 bugs verified via API testing
    - No Strong Hit causes HP loss
    - move_to tool produces location changes
    - NPC names appear in narrative when at player's location
    - Zero tool-call text in any narrative response
    - Auto-checkpoint created on HP<=2
    - HP=0 produces contextual death narration
  </acceptance_criteria>
  <done>
    - All 6 bug fixes confirmed working
    - Results documented with specific API call/response evidence
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of gameplay quality</name>
  <files></files>
  <action>
Present the automated verification results to the user and request manual gameplay testing.
All 6 mechanics bug fixes have been applied: HP guard on Strong Hit, move_to tool for Storyteller, NPC visibility, tool-call sanitization, reactive auto-checkpoint, HP=0 death narration.
  </action>
  <verify>User confirms gameplay quality</verify>
  <done>User types "approved" confirming all mechanics work correctly in browser gameplay</done>
  <what-built>All 6 mechanics bug fixes (HP guard, move_to, NPC visibility, tool sanitization, auto-checkpoint, HP=0 narration) plus docs-driven systematic verification</what-built>
  <how-to-verify>
    1. Open http://localhost:3000 in browser
    2. Start or load a campaign
    3. Play 5-10 turns exercising:
       - Travel between locations (should see location change in sidebar)
       - Interact with NPCs (should see NPC dialogue/reactions)
       - Get into combat (HP should only decrease on Weak Hit or Miss, never Strong Hit)
       - Let HP drop to 2 (should see auto-checkpoint notification)
       - Check that no tool-call text is visible in narrative
    4. Verify overall gameplay feels correct per docs/ descriptions
  </how-to-verify>
  <resume-signal>Type "approved" if all mechanics work correctly, or describe any remaining issues</resume-signal>
</task>

</tasks>

<verification>
All 6 bugs from CONTEXT.md verified:
- Bug 1: HP guard on Strong Hit — backend enforcement
- Bug 2: move_to tool available to Storyteller — location changes work
- Bug 3: NPCs interact when present — prompt instructions enforced
- Bug 4: Tool-call text sanitized — extended regex catches all patterns
- Bug 5: Auto-checkpoint on HP<=2 — reactive trigger during turn
- Bug 6: HP=0 death handling — context injection + immediate narration
</verification>

<success_criteria>
1. All 6 bug fixes verified working via API testing
2. Human confirms gameplay quality in browser
3. No regressions in existing functionality
4. All docs/ mechanics working as described
</success_criteria>

<output>
After completion, create `.planning/phases/15-systematic-mechanics-fix-docs-driven-verification/15-03-SUMMARY.md`
</output>
