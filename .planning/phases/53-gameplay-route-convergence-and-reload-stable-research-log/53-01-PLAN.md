---
phase: 53-gameplay-route-convergence-and-reload-stable-research-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/chat.ts
  - shared/src/index.ts
  - shared/src/__tests__/chat.test.ts
  - backend/src/campaign/chat-history.ts
  - backend/src/campaign/__tests__/chat-history.test.ts
  - backend/src/routes/chat.ts
  - backend/src/routes/__tests__/chat.test.ts
autonomous: true
gap_closure: true
requirements:
  - SCEN-01
  - WRIT-01
  - RES-01
must_haves:
  truths:
    - "No live gameplay request can still stream narration through the legacy plain-text `/api/chat` bypass."
    - "Explicit `/lookup` and `/compare` exchanges persist on the same chat-history lane that `/game` reloads."
    - "Lookup persistence stays factual-only: it does not emit turn-only SSE events or unlock retry/undo."
  artifacts:
    - path: "shared/src/chat.ts"
      provides: "Canonical lookup-log formatter/parser shared by backend and frontend"
      exports: ["isChatMessage", "formatLookupLogEntry", "parseLookupLogEntry"]
    - path: "backend/src/campaign/chat-history.ts"
      provides: "Authoritative append/read helpers for persisted gameplay log entries"
    - path: "backend/src/routes/chat.ts"
      provides: "Single truthful gameplay route family with persisted lookup logging and retired legacy `/chat`"
  key_links:
    - from: "backend/src/routes/chat.ts"
      to: "backend/src/campaign/chat-history.ts"
      via: "appendChatMessages on `/chat/lookup`"
      pattern: "appendChatMessages"
    - from: "backend/src/routes/chat.ts"
      to: "backend/src/engine/grounded-lookup.ts"
      via: "runGroundedLookup for factual replies"
      pattern: "runGroundedLookup"
    - from: "shared/src/chat.ts"
      to: "frontend/lib/gameplay-text.ts"
      via: "shared lookup-log parsing contract"
      pattern: "parseLookupLogEntry"
---

<objective>
Retire the remaining backend gameplay-route bypass and make lookup/compare exchanges persist on the authoritative chat-history lane.

Purpose: Close the implementation gap the milestone audit found in the live backend route matrix.
Output: Shared lookup-log helpers, backend route convergence, persisted factual lookup history, and backend/shared regressions proving the repaired contract.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-gameplay-route-convergence-and-reload-stable-research-log/53-CONTEXT.md
@.planning/phases/53-gameplay-route-convergence-and-reload-stable-research-log/53-RESEARCH.md
@.planning/phases/53-gameplay-route-convergence-and-reload-stable-research-log/53-VALIDATION.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-VERIFICATION.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-VERIFICATION.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-VERIFICATION.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-02-SUMMARY.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-02-SUMMARY.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-03-SUMMARY.md
@backend/src/routes/chat.ts
@backend/src/campaign/chat-history.ts
@backend/src/routes/__tests__/chat.test.ts
@backend/src/campaign/__tests__/chat-history.test.ts
@shared/src/chat.ts
@shared/src/__tests__/chat.test.ts

<decisions>
D-01: `POST /api/chat` must no longer remain as a live alternate gameplay path that bypasses Phase 45 authoritative scene assembly and Phase 47 storyteller preset/guard logic.
D-02: The product must have one truthful gameplay chat/runtime lane for visible narration behavior.
D-03: `/lookup` and `/compare` must behave like a real part of the game log, not a session-only transient overlay.
D-04: Lookup persistence must preserve the distinction between factual lookup replies and ordinary narrated turns.
D-05: Phase 53 must explicitly prove route behavior for `action`, `retry`, `opening`, and `lookup` across both streaming and reload/history boundaries.
</decisions>

<interfaces>
From `shared/src/types.ts`:
```ts
export type ChatRole = "user" | "assistant" | "system";

export interface ChatMessage {
  role: ChatRole;
  content: string;
}
```

Current shared guard in `shared/src/chat.ts`:
```ts
export function isChatMessage(value: unknown): value is ChatMessage;
```

Expected contract to add in `shared/src/chat.ts` for downstream consumers:
```ts
export function formatLookupLogEntry(kind: string, answer: string): string;
export function parseLookupLogEntry(
  content: string,
): { lookupKind: string; answer: string } | null;
```

Relevant backend seam in `backend/src/routes/chat.ts`:
```ts
app.post("/lookup", async (c) => { /* runGroundedLookup -> SSE */ });
app.get("/history", async (c) => {
  return c.json({ messages, premise, hasLiveTurnSnapshot });
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the backend lookup-history and route-retirement contract in regressions</name>
  <files>shared/src/__tests__/chat.test.ts, backend/src/campaign/__tests__/chat-history.test.ts, backend/src/routes/__tests__/chat.test.ts</files>
  <read_first>
    - .planning/phases/53-gameplay-route-convergence-and-reload-stable-research-log/53-CONTEXT.md
    - .planning/phases/53-gameplay-route-convergence-and-reload-stable-research-log/53-RESEARCH.md
    - backend/src/routes/chat.ts
    - backend/src/campaign/chat-history.ts
    - shared/src/chat.ts
    - frontend/app/game/page.tsx
    - frontend/lib/gameplay-text.ts
  </read_first>
  <behavior>
    - Test 1: per D-01 and D-02, `POST /chat` no longer streams storyteller narration or reaches `callStoryteller()`.
    - Test 2: per D-03 and D-04, successful `/chat/lookup` persists both the user command and the assistant factual reply, and `GET /chat/history` returns them in order.
    - Test 3: per D-03, lookup-only interactions do not create `hasLiveTurnSnapshot` and do not emit turn-only SSE events.
    - Test 4: the shared lookup formatter/parser round-trips normal lookup kinds and `power_profile` without frontend-only regex drift.
  </behavior>
  <action>
    Add or update the shared/backend regression surfaces so the audit gap is mechanically locked before implementation:
    - extend `shared/src/__tests__/chat.test.ts` to prove one canonical lookup-log formatter/parser contract;
    - extend `backend/src/campaign/__tests__/chat-history.test.ts` to prove lookup entries persist through the existing append/read lane;
    - extend `backend/src/routes/__tests__/chat.test.ts` to prove legacy `/chat` is retired or hard-failed, `/chat/lookup` writes reload-stable history, and lookup does not unlock turn snapshots.

    Keep the tests inside the existing Vitest + Hono patterns. Do not add a second research-log file, local-storage fallback, or manual-only verification lane.
  </action>
  <acceptance_criteria>
    - `shared/src/__tests__/chat.test.ts` contains explicit lookup formatter/parser assertions.
    - `backend/src/routes/__tests__/chat.test.ts` contains a legacy-route retirement assertion and a lookup-history reload assertion.
    - `backend/src/campaign/__tests__/chat-history.test.ts` proves persisted lookup messages round-trip through `getChatHistory()`.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix shared exec vitest run src/__tests__/chat.test.ts && npm --prefix backend exec vitest run src/campaign/__tests__/chat-history.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>Shared and backend tests fail for the current gap and precisely describe the intended Phase 53 backend contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement one authoritative backend gameplay lane and persisted factual lookup logging</name>
  <files>shared/src/chat.ts, shared/src/index.ts, backend/src/campaign/chat-history.ts, backend/src/routes/chat.ts</files>
  <read_first>
    - shared/src/__tests__/chat.test.ts
    - backend/src/campaign/__tests__/chat-history.test.ts
    - backend/src/routes/__tests__/chat.test.ts
    - shared/src/chat.ts
    - shared/src/index.ts
    - backend/src/campaign/chat-history.ts
    - backend/src/routes/chat.ts
  </read_first>
  <behavior>
    - Make the Task 1 regressions pass without widening `ChatMessage`.
    - Keep `/chat/action`, `/chat/opening`, and `/chat/retry` as the only narrated gameplay SSE routes.
    - Persist lookup/compare replies as factual chat-history messages while keeping them outside turn processing and snapshot state.
  </behavior>
  <action>
    Implement the repaired backend/shared contract per D-01 through D-04:
    - add `formatLookupLogEntry()` and `parseLookupLogEntry()` to `shared/src/chat.ts` and re-export them from `shared/src/index.ts`;
    - use the shared helper in `backend/src/routes/chat.ts` so `/chat/lookup` appends the user command plus the assistant factual reply to the existing `chat_history.json` lane after a successful lookup;
    - keep lookup outside `processTurn()` and do not mutate `setLastTurnSnapshot()` / `hasLiveTurnSnapshot()` behavior for lookup-only requests;
    - retire legacy `POST /chat` by removing it or returning a hard failure that cannot reach `callStoryteller()` or stream plain-text narration;
    - clean up any dead imports left by the route retirement in the same change.

    Do not invent a second research-log store, do not add frontend-specific formatting logic here, and do not contaminate factual lookup replies with ordinary narration semantics.
  </action>
  <acceptance_criteria>
    - `backend/src/routes/chat.ts` no longer exposes a working narrator stream on `POST /chat`.
    - `/chat/lookup` writes ordered `{ role: "user" }` then `{ role: "assistant" }` history entries using the shared formatter.
    - The backend/shared regression command passes without introducing new route families or message metadata fields.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix shared exec vitest run src/__tests__/chat.test.ts && npm --prefix backend exec vitest run src/campaign/__tests__/chat-history.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>The backend no longer has a live bypass around Phases 45 and 47, and lookup/compare exchanges are persisted on the authoritative gameplay log lane.</done>
</task>

</tasks>

<verification>
Run the backend/shared Phase 53 contract suite:

`npm --prefix shared exec vitest run src/__tests__/chat.test.ts && npm --prefix backend exec vitest run src/campaign/__tests__/chat-history.test.ts src/routes/__tests__/chat.test.ts`
</verification>

<success_criteria>
- Legacy `POST /api/chat` is retired or hard-failed and can no longer stream narrator output.
- `/api/chat/lookup` and `/compare` replies persist through `chat_history.json` and reappear in `GET /api/chat/history`.
- Backend/shared regressions prove lookup does not create turn snapshots or emit narrated-turn SSE events.
</success_criteria>

<output>
After completion, create `.planning/phases/53-gameplay-route-convergence-and-reload-stable-research-log/53-01-SUMMARY.md`
</output>
