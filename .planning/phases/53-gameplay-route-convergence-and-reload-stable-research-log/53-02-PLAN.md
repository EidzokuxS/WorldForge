---
phase: 53-gameplay-route-convergence-and-reload-stable-research-log
plan: 02
type: execute
wave: 2
depends_on:
  - 53-01
files_modified:
  - frontend/lib/api.ts
  - frontend/lib/__tests__/api.test.ts
  - frontend/lib/gameplay-text.ts
  - frontend/components/game/narrative-log.tsx
  - frontend/components/game/__tests__/narrative-log.test.tsx
  - frontend/app/game/page.tsx
  - frontend/app/game/__tests__/page.test.tsx
autonomous: true
gap_closure: true
requirements:
  - SCEN-01
  - WRIT-01
  - RES-01
must_haves:
  truths:
    - "Reloaded `/game` sessions rehydrate lookup and compare replies from chat history as factual support blocks."
    - "Lookup/compare remain visibly distinct from ordinary narrated turns after reload."
    - "The route matrix explicitly proves `opening`, `action`, `retry`, and `lookup` behavior across live stream plus reload boundaries."
  artifacts:
    - path: "frontend/app/game/page.tsx"
      provides: "Reload-stable gameplay log hydration and lookup submission on `/game`"
    - path: "frontend/lib/gameplay-text.ts"
      provides: "Frontend message-kind derivation aligned to the shared lookup-log contract"
    - path: "frontend/app/game/__tests__/page.test.tsx"
      provides: "Stream-plus-reload route-matrix proof for `/game`"
  key_links:
    - from: "frontend/app/game/page.tsx"
      to: "frontend/lib/api.ts"
      via: "chatHistory, chatLookup, parseTurnSSE"
      pattern: "chatHistory|chatLookup|parseTurnSSE"
    - from: "frontend/lib/gameplay-text.ts"
      to: "shared/src/chat.ts"
      via: "shared lookup-log parser"
      pattern: "parseLookupLogEntry"
    - from: "frontend/components/game/narrative-log.tsx"
      to: "frontend/lib/gameplay-text.ts"
      via: "lookup/compare support-block rendering"
      pattern: "deriveGameMessageKind|stripLookupPrefix"
---

<objective>
Consume the persisted lookup-log contract in `/game` and close the frontend route matrix for stream plus reload behavior.

Purpose: Turn lookup/compare from a session-only overlay into a reload-stable part of the gameplay log without regressing Phase 45/47 turn presentation.
Output: Frontend helpers and rendering aligned to the shared lookup-log contract, plus route-matrix regressions for opening/action/retry/lookup across live stream and reload boundaries.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-gameplay-route-convergence-and-reload-stable-research-log/53-CONTEXT.md
@.planning/phases/53-gameplay-route-convergence-and-reload-stable-research-log/53-RESEARCH.md
@.planning/phases/53-gameplay-route-convergence-and-reload-stable-research-log/53-VALIDATION.md
@.planning/phases/53-gameplay-route-convergence-and-reload-stable-research-log/53-01-PLAN.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-03-SUMMARY.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-02-SUMMARY.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-03-SUMMARY.md
@frontend/lib/api.ts
@frontend/lib/gameplay-text.ts
@frontend/components/game/narrative-log.tsx
@frontend/app/game/page.tsx
@frontend/lib/__tests__/api.test.ts
@frontend/components/game/__tests__/narrative-log.test.tsx
@frontend/app/game/__tests__/page.test.tsx

<decisions>
D-01: `POST /api/chat` must no longer remain as a live alternate gameplay path that bypasses Phase 45 authoritative scene assembly and Phase 47 storyteller preset/guard logic; Phase 53 treats it as a `410 Gone` compatibility failure, not an alternate live path.
D-02: The product must have one truthful gameplay chat/runtime lane for visible narration behavior.
D-03: `/lookup` and `/compare` must behave like a real part of the game log, not a session-only transient overlay, and the persisted user-side slash command must survive reload as a normal player bubble.
D-04: Lookup persistence must preserve the distinction between factual lookup replies and ordinary narrated turns.
D-05: Phase 53 must explicitly prove route behavior for `action`, `retry`, `opening`, and `lookup` across both streaming and reload/history boundaries.
</decisions>

<interfaces>
Expected contract from Plan `53-01` in `shared/src/chat.ts`:
```ts
export function formatLookupLogEntry(kind: string, answer: string): string;
export function parseLookupLogEntry(
  content: string,
): { lookupKind: string; answer: string } | null;
```

Current frontend route and SSE contracts in `frontend/lib/api.ts`:
```ts
export type ChatHistoryResponse = {
  messages: ChatMessage[];
  premise: string;
  hasLiveTurnSnapshot: boolean;
};

export interface ChatLookupRequest {
  lookupKind: LookupKind;
  subject: string;
  compareAgainst?: string;
  question?: string;
}

export interface LookupResultEvent {
  lookupKind: LookupKind;
  subject: string;
  answer: string;
  citations: Array<{ kind?: string; label: string; excerpt: string }>;
  uncertaintyNotes: string[];
  sceneImpact: string;
}
```

Current frontend classification seam in `frontend/lib/gameplay-text.ts`:
```ts
export function deriveGameMessageKind(role: ChatRole, content: string): GameMessageKind;
export function stripLookupPrefix(content: string): string;
```
</interfaces>
</context>

<tasks>

<task type="analysis">
  <name>Task 0: Run GitNexus impact analysis before frontend route-matrix edits</name>
  <files>frontend/lib/api.ts, frontend/lib/gameplay-text.ts, frontend/components/game/narrative-log.tsx, frontend/app/game/page.tsx</files>
  <read_first>
    - AGENTS.md
    - frontend/lib/api.ts
    - frontend/lib/gameplay-text.ts
    - frontend/components/game/narrative-log.tsx
    - frontend/app/game/page.tsx
  </read_first>
  <action>
    Before editing frontend symbols in this plan, run the GitNexus MCP impact tool (`gitnexus_impact`) for the `/game` reload and message-classification seams that will change. At minimum inspect blast radius for:
    - `frontend/app/game/page.tsx` reload hydration and lookup submission flow;
    - `frontend/lib/gameplay-text.ts` message-kind derivation;
    - `frontend/components/game/narrative-log.tsx` support-block rendering;
    - `frontend/lib/api.ts` chat-history and lookup transport helpers.

    If GitNexus reports `HIGH` or `CRITICAL` impact for a planned symbol/file change, stop and document the risk in the execution notes before continuing. Do not begin code edits until this review is complete.
  </action>
  <acceptance_criteria>
    - Execution notes or summary mention that `gitnexus_impact(...)` was run before frontend edits.
    - Any `HIGH` or `CRITICAL` impact result is explicitly called out before code modification proceeds.
  </acceptance_criteria>
  <verify>
    <automated>gitnexus_impact({ repo: "WorldForge", target: "chatHistory", direction: "upstream" }) + gitnexus_impact({ repo: "WorldForge", target: "deriveGameMessageKind", direction: "upstream" })</automated>
  </verify>
  <done>Frontend route-matrix edits begin only after an explicit GitNexus impact pass on the affected `/game` seams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Extend the frontend route-matrix regressions for reload-stable lookup history</name>
  <files>frontend/lib/__tests__/api.test.ts, frontend/components/game/__tests__/narrative-log.test.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <read_first>
    - .planning/phases/53-gameplay-route-convergence-and-reload-stable-research-log/53-01-PLAN.md
    - frontend/lib/api.ts
    - frontend/lib/gameplay-text.ts
    - frontend/components/game/narrative-log.tsx
    - frontend/app/game/page.tsx
    - frontend/lib/__tests__/api.test.ts
    - frontend/components/game/__tests__/narrative-log.test.tsx
    - frontend/app/game/__tests__/page.test.tsx
  </read_first>
  <behavior>
    - Test 1: per D-03 and D-04, persisted lookup and compare entries returned by `chatHistory()` both render as support blocks after reload, without raw `[Lookup:` leakage.
    - Test 2: per D-05, `/game` keeps `opening`, `action`, `retry`, and `lookup` behavior distinct across live stream plus reload boundaries.
    - Test 3: `parseTurnSSE()` keeps its existing `lookup_result` coverage cleanly while lookup history rendering uses the persisted contract instead of duplicated local formatting rules.
    - Test 4: persisted user-side `/lookup ...` or `/compare ...` commands reload as normal player bubbles rather than being dropped or misclassified.
  </behavior>
  <action>
    Add or extend the existing frontend regressions so the Phase 53 frontend contract is explicit:
    - `frontend/lib/__tests__/api.test.ts` should preserve the existing lookup SSE lane coverage while preserving the action/opening/retry parser semantics;
    - `frontend/components/game/__tests__/narrative-log.test.tsx` should prove persisted lookup and compare entries render as support blocks from stored message content;
    - `frontend/app/game/__tests__/page.test.tsx` should seed reload history with both persisted lookup/compare messages and the matching raw slash-command user messages, then prove `/game` restores them without local-only reconstruction.

    Reuse the existing mocked component and mocked API patterns. Do not add a new browser-only verification path or a second UI surface for research history.
  </action>
  <acceptance_criteria>
    - `page.test.tsx` contains explicit reload-history cases for both persisted lookup and compare messages, plus the matching user slash-command messages.
    - `narrative-log.test.tsx` proves persisted lookup and compare entries render without the raw prefix.
    - `api.test.ts` keeps `lookup_result` parsing covered alongside the existing turn SSE events.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend exec vitest run lib/__tests__/api.test.ts components/game/__tests__/narrative-log.test.tsx app/game/__tests__/page.test.tsx</automated>
  </verify>
  <done>The frontend tests fail for the current reload gap and explicitly describe the repaired stream-plus-reload route matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire `/game` to the shared lookup-log contract and keep narrated turns separate</name>
  <files>frontend/lib/api.ts, frontend/lib/gameplay-text.ts, frontend/components/game/narrative-log.tsx, frontend/app/game/page.tsx</files>
  <read_first>
    - frontend/lib/__tests__/api.test.ts
    - frontend/components/game/__tests__/narrative-log.test.tsx
    - frontend/app/game/__tests__/page.test.tsx
    - frontend/lib/api.ts
    - frontend/lib/gameplay-text.ts
    - frontend/components/game/narrative-log.tsx
    - frontend/app/game/page.tsx
    - shared/src/chat.ts
  </read_first>
  <behavior>
    - Make the Task 1 regressions pass using the shared lookup-log helper from Plan `53-01`.
    - Keep slash-command parsing local to `/game`, but derive persisted lookup rendering and prefix stripping from the shared contract.
    - Preserve existing opening, action, retry, progress, and retry/undo readiness behavior.
  </behavior>
  <action>
    Implement the frontend half of D-03 through D-05 with the minimum surface-area change:
    - import and use the shared lookup-log parser/formatter from `@worldforge/shared` in `frontend/lib/gameplay-text.ts` and `frontend/app/game/page.tsx`;
    - replace the duplicated local lookup regex contract so both lookup detection and answer stripping delegate to the shared parser;
    - keep `NarrativeLog` rendering on the existing support-block lane, only changing the classification/parsing seam needed for persisted factual replies;
    - do not widen `ChatMessage`, do not add local storage, and do not invent a separate lookup/research panel.

    Preserve the Phase 45/47 reader-shell behavior: ordinary narrated turns remain narration, factual lookups remain support blocks, and retry/undo readiness still depends only on authoritative turn completion.
  </action>
  <acceptance_criteria>
    - `/game` renders persisted lookup and compare history correctly after reload using the same contract as live lookup submission.
    - `frontend/lib/gameplay-text.ts` no longer owns a private regex contract that can drift from backend persistence.
    - The frontend route-matrix regression command passes without regressing action/opening/retry behavior.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend exec vitest run lib/__tests__/api.test.ts components/game/__tests__/narrative-log.test.tsx app/game/__tests__/page.test.tsx</automated>
  </verify>
  <done>`/game` reloads lookup/compare as factual log entries, keeps them distinct from narration, and proves the Phase 53 stream-plus-reload contract in automated tests.</done>
</task>

</tasks>

<verification>
Run the combined Phase 53 route-matrix suite:

`npm --prefix shared exec vitest run src/__tests__/chat.test.ts && npm --prefix backend exec vitest run src/campaign/__tests__/chat-history.test.ts src/engine/__tests__/turn-processor.test.ts src/routes/__tests__/chat.test.ts && npm --prefix frontend exec vitest run lib/__tests__/api.test.ts components/game/__tests__/narrative-log.test.tsx app/game/__tests__/page.test.tsx`
</verification>

<success_criteria>
- Reloaded `/game` sessions render persisted lookup and compare messages as factual support blocks, not ordinary narration.
- Frontend rendering and reload hydration consume the same lookup-log contract as backend persistence.
- The automated route matrix explicitly covers `opening`, `action`, `retry`, and `lookup` across stream plus reload boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/53-gameplay-route-convergence-and-reload-stable-research-log/53-02-SUMMARY.md`
</output>
