---
phase: 71-repair-worldgen-research-authority-boundary-so-llm-owns-prem
plan: 04
type: execute
wave: 2
depends_on: [71-02, 71-03]
files_modified:
  - backend/src/routes/schemas.ts
  - backend/src/routes/worldgen.ts
  - backend/src/worldgen/types.ts
  - backend/src/routes/__tests__/worldgen.test.ts
autonomous: true
requirements: [P71-R4, P71-R5]
requirements_addressed: [P71-R4, P71-R5]
must_haves:
  truths:
    - "`suggest-seeds` returns `_researchArtifact` for automatic known-IP research."
    - "`generate` saves/loads and passes `researchArtifact` instead of rebuilding backend-owned research frames from `ipContext.franchise`."
    - "`regenerate-section` loads the artifact and passes it into worldgen request context."
    - "Worldbook/manual context continues to use legacy `ipContext` compatibility safely."
    - "The existing frontend wizard may omit `_researchArtifact`; backend behavior is explicit re-research or cached-artifact load, never legacy recanonicalization."
  artifacts:
    - path: "backend/src/routes/schemas.ts"
      provides: "Route payload schema for research artifacts"
    - path: "backend/src/routes/worldgen.ts"
      provides: "suggest/generate/regenerate artifact wiring"
    - path: "backend/src/worldgen/types.ts"
      provides: "GenerateScaffoldRequest artifact field"
    - path: "backend/src/routes/__tests__/worldgen.test.ts"
      provides: "Route regression coverage"
  key_links:
    - from: "backend/src/routes/worldgen.ts"
      to: "backend/src/campaign/manager.ts"
      via: "save/load worldgenResearchArtifact helpers"
      pattern: "saveWorldgenResearchArtifact|loadWorldgenResearchArtifact"
    - from: "backend/src/routes/worldgen.ts"
      to: "backend/src/worldgen/ip-researcher.ts"
      via: "researchWorldgenArtifact"
      pattern: "researchWorldgenArtifact"
---

<objective>
Wire the v2 research artifact through worldgen routes and request types.

Purpose: automatic known-IP/franchise worldgen must persist and pass the LLM-authored artifact, while backend routes stop turning `ipContext.franchise` into a new deterministic worldgen research frame.
Output: schemas, route wiring, request type support, and route tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-CONTEXT.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-RESEARCH.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-VALIDATION.md
@AGENTS.md
@backend/src/routes/worldgen.ts
@backend/src/routes/schemas.ts
@backend/src/worldgen/types.ts
@backend/src/routes/__tests__/worldgen.test.ts
@frontend/lib/api.ts
@frontend/components/title/use-new-campaign-wizard.ts

<interfaces>
Current route risk:
```typescript
function resolveWorldgenResearchFrame(
  campaignId: string,
  ipContext: IpResearchContext | null,
  premise: string,
  premiseDivergence: PremiseDivergence | null,
  seeds?: Partial<WorldSeeds>,
)
```

Target request field:
```typescript
export interface GenerateScaffoldRequest {
  researchArtifact?: WorldgenResearchArtifactV2 | null;
}
```

Frontend wizard decision for Phase 71:
- Do not edit frontend wizard files in this phase.
- `suggest-seeds` returns `_researchArtifact` for future pass-through/debug visibility, but the current browser flow is allowed to omit it when calling `generate`.
- When `generate` receives no body artifact, backend must first load a cached artifact by `campaignId`; if none exists, it explicitly re-runs `researchWorldgenArtifact`.
- This accepted re-research path must be covered by route tests and must not call `researchKnownIP`, `detectFranchise`, `resolveWorldgenResearchFrame`, or `saveWorldgenResearchFrame`.
- Do not synthesize a shadow `_ipContext` from the v2 artifact, because that would rebuild a legacy canonical-franchise shape from artifact facts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add route regressions for artifact handoff and persistence</name>
  <files>backend/src/routes/__tests__/worldgen.test.ts</files>
  <behavior>
    - `POST /api/worldgen/suggest-seeds` with automatic known-IP research returns `_researchArtifact` and does not need `_ipContext` for the automatic path.
    - `POST /api/worldgen/generate` accepts body `researchArtifact`, saves it to campaign config, loads cached artifact when omitted, and passes it into `generateWorldScaffold`.
    - `POST /api/worldgen/generate` with `{ ipContext: null, researchArtifact: v2 }` passes artifact through and does not crash or rebuild semantic frames.
    - `POST /api/worldgen/generate` with no body artifact and no cached artifact explicitly re-runs `researchWorldgenArtifact`; this documents the frontend wizard no-pass-through choice for Phase 71.
    - `POST /api/worldgen/regenerate-section` loads the cached artifact and passes it into section generation.
    - Selected worldbooks and explicit legacy `_ipContext` fixtures still work.
    - The fallback premise string `A world based on the ${ipContext.franchise} setting` is not used for v2 artifact flow.
  </behavior>
  <action>Add failing tests using the mocked campaign module and mocked `researchWorldgenArtifact`. Import the JJK/Naruto fixture from Plan 01 instead of duplicating it. Keep existing worldbook/manual tests intact. Include assertions that `researchKnownIP`, `resolveWorldgenResearchFrame`, and `saveWorldgenResearchFrame` are not called for v2 artifact flow, including the explicit re-research route when the frontend omits `_researchArtifact`.</action>
  <verify>
    <automated>npm --prefix backend run test -- src/routes/__tests__/worldgen.test.ts</automated>
  </verify>
  <done>Route tests fail only because schemas, request type, and route wiring do not yet support `researchArtifact`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement route and request wiring for v2 artifacts</name>
  <files>backend/src/routes/schemas.ts, backend/src/routes/worldgen.ts, backend/src/worldgen/types.ts</files>
  <behavior>
    - Route payloads accept the v2 artifact shape where needed.
    - Automatic research calls `researchWorldgenArtifact`.
    - `generateWorldScaffold` receives `researchArtifact`.
    - Legacy `ipContext` remains only for worldbook/manual/old-campaign compatibility.
  </behavior>
  <action>Before editing, run `npx gitnexus status`; if stale, run `npx gitnexus analyze`. Run GitNexus impact for `resolveWorldgenResearchFrame`, `generateWorldScaffold`, `suggestWorldSeeds`, `researchKnownIP`, and route file `backend/src/routes/worldgen.ts`; report any HIGH/CRITICAL result before edits. Add the artifact schema to route schemas by reusing Plan 01 validation; do not duplicate a second route-local artifact validator. Update `GenerateScaffoldRequest` with `researchArtifact?: WorldgenResearchArtifactV2 | null`. In `worldgen.ts`, import save/load artifact helpers and `researchWorldgenArtifact`; prefer artifact over legacy `ipContext` in automatic known-IP research; stop creating/saving `WorldgenResearchFrame` for v2 artifact flow; keep selected worldbook/manual `ipContext` flow unchanged. Do not edit frontend wizard files in Phase 71; document and test the explicit backend re-research behavior when the browser omits `_researchArtifact`.</action>
  <verify>
    <automated>npm --prefix backend run test -- src/routes/__tests__/worldgen.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <done>Route tests pass; typecheck passes; suggest/generate/regenerate all pass artifact through; legacy worldbook/manual context remains compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request -> route schema | Client-supplied artifact enters route layer. |
| route -> campaign config | Valid artifact becomes persisted local state. |
| route -> scaffold generation | Persisted/generated artifact becomes generation context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-04-01 | Tampering | route schemas | mitigate | Validate artifact shape and reject malformed payloads before persistence. |
| T-71-04-02 | Repudiation | route persistence | mitigate | Preserve artifact provenance and write through campaign helpers. |
| T-71-04-03 | Tampering | automatic research routing | mitigate | Tests prove backend does not rebuild semantic frame from `ipContext.franchise` for v2 flow. |
| T-71-04-04 | Denial of Service | route payload size | mitigate | Reuse capped artifact schema from Plan 01. |
</threat_model>

<verification>
- `npm --prefix backend run test -- src/routes/__tests__/worldgen.test.ts`
- `npm --prefix backend run typecheck`
- `rg "A world based on the .*franchise|saveWorldgenResearchFrame\\(|researchKnownIP\\(" backend/src/routes/worldgen.ts`
</verification>

<success_criteria>
- P71-R4 route handoff is implemented.
- P71-R5 compatibility remains intact.
- Automatic known-IP worldgen stores and passes v2 artifacts, not backend-owned research frames.
- Frontend wizard behavior is explicit: no pass-through work in Phase 71; backend re-research/cached-artifact path is tested and remains on v2 artifact authority.
</success_criteria>

<output>
After completion, create `.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-04-SUMMARY.md`.
</output>
