---
phase: 71-repair-worldgen-research-authority-boundary-so-llm-owns-prem
phase_number: 71
plan: 09
type: execute
wave: 7
depends_on: [71-04, 71-07, 71-08]
files_modified:
  - backend/src/routes/schemas.ts
  - backend/src/routes/worldgen.ts
  - backend/src/routes/__tests__/worldgen.test.ts
  - .planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-EVIDENCE-CAMPAIGN-HASHES.json
  - .planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [P71-R4, P71-R5]
requirements_addressed: [P71-R4, P71-R5]
must_haves:
  truths:
    - "Artifact-backed `/generate` and `/regenerate-section` do not pass legacy `ipContext`, `premiseDivergence`, or `researchFrame` downstream when `researchArtifact` exists."
    - "`/suggest-seed` accepts `researchArtifact` and passes it to `suggestSingleSeed`."
    - "`/save-edits` loads stored `worldgenResearchArtifact` first and passes artifact or legacy context into `extractLoreCards` without mixing lanes."
    - "Evidence campaign `cc851187-f6fd-4e9e-9071-933cb056374b` remains untouched: config/state SHA256 hashes match the pre-edit baseline, git diff is empty as supplemental evidence, and no repair or migration command runs."
  artifacts:
    - path: "backend/src/routes/schemas.ts"
      provides: "`suggestSeedSchema` researchArtifact payload acceptance"
    - path: "backend/src/routes/worldgen.ts"
      provides: "Artifact-first route orchestration for generate, regenerate-section, suggest-seed, and save-edits"
    - path: "backend/src/routes/__tests__/worldgen.test.ts"
      provides: "Route-level regressions for all three verification gaps"
    - path: ".planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-EVIDENCE-CAMPAIGN-HASHES.json"
      provides: "Pre-edit SHA256 baseline for ignored evidence campaign config/state files"
  key_links:
    - from: "backend/src/routes/worldgen.ts"
      to: "backend/src/worldgen/seed-suggester.ts"
      via: "suggestSingleSeed request object"
      pattern: "researchArtifact: result\\.data\\.researchArtifact"
    - from: "backend/src/routes/worldgen.ts"
      to: "backend/src/worldgen/scaffold-generator.ts"
      via: "generateWorldScaffold request object"
      pattern: "ipContext: null|premiseDivergence: null|researchFrame: null"
    - from: "backend/src/routes/worldgen.ts"
      to: "backend/src/worldgen/lore-extractor.ts"
      via: "extractLoreCards positional context args"
      pattern: "extractLoreCards\\(.*researchArtifact"
---

<objective>
Close Phase 71 verification gaps in worldgen route artifact handoff.

Purpose: the v2 research artifact lane is already built, but four route surfaces can still mix in or drop legacy research context. This plan locks route behavior so artifact-backed flows remain artifact-backed through seed regeneration, scaffold generation, section regeneration, and save-edits lore extraction.
Output: focused route tests, route/schema fixes, GitNexus scope proof, and `71-09-SUMMARY.md`.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-CONTEXT.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-RESEARCH.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-REVIEW.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-VERIFICATION.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-SUMMARY.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-07-SUMMARY.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-08-SUMMARY.md
@backend/src/routes/worldgen.ts
@backend/src/routes/schemas.ts
@backend/src/routes/__tests__/worldgen.test.ts
@backend/src/worldgen/seed-suggester.ts
@backend/src/worldgen/lore-extractor.ts

<gap_source_audit>
| Source | ID | Gap | Plan Task | Status |
|--------|----|-----|-----------|--------|
| VERIFICATION | G71-09-01 | Artifact-backed `/generate` and `/regenerate-section` can mix v2 artifact with cached legacy `ipContext`, `premiseDivergence`, or `researchFrame`. | Tasks 1, 2, 3 | COVERED |
| VERIFICATION | G71-09-02 | `/suggest-seed` schema/route omits `researchArtifact` even though `suggestSingleSeed` supports it. | Tasks 1, 2, 3 | COVERED |
| VERIFICATION | G71-09-03 | `/save-edits` re-extracts lore without stored artifact or legacy context. | Tasks 1, 2, 3 | COVERED |
| CONTEXT | evidence-campaign | Existing campaign `cc851187-f6fd-4e9e-9071-933cb056374b` must remain preserved; no silent repair. | Tasks 1, 3 | COVERED |
| CONTEXT | deferred-ui-review | Full UI review/edit surface for artifacts. | none | EXCLUDED - deferred in CONTEXT.md |
| CONTEXT | deferred-repair | Automatic repair of existing generated campaigns. | none | EXCLUDED - deferred in CONTEXT.md |
</gap_source_audit>

<interfaces>
Existing contract surfaces:
```typescript
// backend/src/routes/schemas.ts
const worldgenResearchArtifactPayloadSchema = worldgenResearchArtifactSchema.nullable().optional();

// backend/src/worldgen/seed-suggester.ts
export async function suggestSingleSeed(
  req: SuggestSeedsRequest & {
    category: SeedCategory;
    ipContext?: IpResearchContext | null;
    premiseDivergence?: PremiseDivergence | null;
  }
): Promise<string | string[]>;
// suggestSingleSeed already reads req.researchArtifact through SuggestSeedsRequest.

// backend/src/worldgen/lore-extractor.ts
export async function extractLoreCards(
  scaffold: WorldScaffold,
  role: ResolvedRole,
  ipContext?: IpResearchContext | null,
  premiseDivergence?: PremiseDivergence | null,
  onProgress?: (progress: GenerationProgress) => void,
  progressStep?: number,
  progressTotalSteps?: number,
  researchArtifact?: WorldgenResearchArtifactV2 | null,
): Promise<ExtractedLoreCard[]>;
```

Route-lane rule for this gap closure:
- If `researchArtifact` exists in the request body or campaign config, artifact lane wins.
- In artifact lane, set `ipContext = null`, `premiseDivergence = null`, and `researchFrame = null` before `resolvePremiseDivergence`, before artifact sufficiency, and before `generateWorldScaffold` or section regeneration.
- Only load or use legacy `ipContext`/`premiseDivergence`/`researchFrame` when no v2 artifact exists.
- Do not delete or rewrite stored legacy fields. Ignored is not migrated.
- Do not edit `generateWorldScaffold` in this plan. Route-level clearing is the minimal closure. If route tests cannot close the gap without a generator guard, stop and request a separate gap plan instead of expanding this one.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock route regressions for artifact lane isolation</name>
  <files>backend/src/routes/__tests__/worldgen.test.ts, .planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-EVIDENCE-CAMPAIGN-HASHES.json</files>
  <read_first>
    @AGENTS.md
    @.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-VERIFICATION.md
    @.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-REVIEW.md
    @backend/src/routes/worldgen.ts
    @backend/src/routes/schemas.ts
    @backend/src/routes/__tests__/worldgen.test.ts
    @backend/src/worldgen/__tests__/fixtures/jjk-naruto-artifact.ts
  </read_first>
  <behavior>
    - `/api/worldgen/generate` with stored v2 artifact plus stored legacy `ipContext` passes `researchArtifact` to `generateWorldScaffold` and passes `ipContext: null`, `premiseDivergence: null`, and `researchFrame: null`.
    - `/api/worldgen/generate` with body `researchArtifact` plus body or cached legacy context does not save or forward the legacy lane in that request.
    - `/api/worldgen/regenerate-section` with stored v2 artifact plus stored legacy context does not call legacy sufficiency, does not derive premise divergence from legacy `ipContext`, and passes null legacy fields to the section request.
    - `/api/worldgen/suggest-seed` accepts body `researchArtifact` and passes it into `suggestSingleSeed`.
    - `/api/worldgen/save-edits` loads stored `worldgenResearchArtifact` first and calls `extractLoreCards(scaffold, role, null, null, undefined, undefined, undefined, artifact)` when artifact exists.
    - `/api/worldgen/save-edits` with no artifact loads legacy `ipContext` and `premiseDivergence` and passes them into `extractLoreCards`.
  </behavior>
  <action>Before any test or source edit, create the ignored evidence-campaign SHA256 baseline exactly here:

```powershell
$paths = @(
  'campaigns/cc851187-f6fd-4e9e-9071-933cb056374b/config.json',
  'campaigns/cc851187-f6fd-4e9e-9071-933cb056374b/state.db'
)
$paths | ForEach-Object {
  if (!(Test-Path -LiteralPath $_)) { throw "Missing evidence file: $_" }
  [pscustomobject]@{
    path = $_
    sha256 = (Get-FileHash -Algorithm SHA256 -LiteralPath $_).Hash
  }
} | ConvertTo-Json | Set-Content -LiteralPath '.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-EVIDENCE-CAMPAIGN-HASHES.json'
```

Then add failing route tests only in `backend/src/routes/__tests__/worldgen.test.ts`. Reuse `cloneJjkNarutoArtifact()` and existing route mocks. Assert `interpretPremiseDivergence`, `evaluateResearchSufficiency`, `loadIpContext`, `loadPremiseDivergence`, `saveIpContext`, and `savePremiseDivergence` call behavior where it proves lane isolation. Preserve existing legacy no-artifact tests; add new artifact-lane cases beside current generate/regenerate/suggest-seed/save-edits describe blocks.</action>
  <verify>
    <automated>Test-Path -LiteralPath '.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-EVIDENCE-CAMPAIGN-HASHES.json'</automated>
    <automated>npm --prefix backend run test -- src/routes/__tests__/worldgen.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `71-09-EVIDENCE-CAMPAIGN-HASHES.json` exists before any edits to tests or source files and contains SHA256 entries for evidence campaign `config.json` and `state.db`.
    - New tests fail before implementation for the exact three verification gaps.
    - No production file is edited in this task.
    - Tests do not use hardcoded campaign repair data or mutate `campaigns/cc851187-f6fd-4e9e-9071-933cb056374b`.
  </acceptance_criteria>
  <done>Route tests express all missing behavior from `71-VERIFICATION.md` and fail only because route/schema wiring is not yet corrected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement artifact-first route and schema wiring</name>
  <files>backend/src/routes/schemas.ts, backend/src/routes/worldgen.ts</files>
  <read_first>
    @AGENTS.md
    @.agents/skills/gitnexus/gitnexus-impact-analysis/SKILL.md
    @.agents/skills/gitnexus/gitnexus-cli/SKILL.md
    @backend/src/routes/schemas.ts
    @backend/src/routes/worldgen.ts
    @backend/src/worldgen/seed-suggester.ts
    @backend/src/worldgen/lore-extractor.ts
    @backend/src/routes/__tests__/worldgen.test.ts
  </read_first>
  <behavior>
    - `suggestSeedSchema` accepts `researchArtifact: worldgenResearchArtifactPayloadSchema`.
    - `/suggest-seed` passes `researchArtifact: result.data.researchArtifact ?? undefined` into `suggestSingleSeed`.
    - `/generate` loads or accepts `researchArtifact` before resolving legacy context; once artifact exists, it clears/ignores legacy `ipContext`, `premiseDivergence`, and `researchFrame` before `resolvePremiseDivergence` and `generateWorldScaffold`.
    - `/regenerate-section` loads stored `researchArtifact` first; when artifact exists, it does not load or use legacy context for premise divergence, research frame, legacy sufficiency, or section request args.
    - `/save-edits` loads stored artifact first; only when no artifact exists does it load legacy `ipContext`/`premiseDivergence`; it passes the chosen context into `extractLoreCards`.
  </behavior>
  <action>Before editing, run `npx gitnexus status`; if stale, run `npx gitnexus analyze`. Then run GitNexus impact/API-impact for `backend/src/routes/worldgen.ts` and `backend/src/routes/schemas.ts`: use MCP `gitnexus_impact` when available for `suggestSeedSchema`, `suggestSingleSeed`, `generateWorldScaffold`, `extractLoreCards`, `resolvePremiseDivergence`, and the worldgen route handlers touched here. If MCP impact is unavailable, record the fallback in the summary and use `npx gitnexus status`, targeted `rg -n "app\\.post\\(\"/(generate|regenerate-section|suggest-seed|save-edits)" backend/src/routes/worldgen.ts`, and focused route tests as API-impact proof. If any impact result is HIGH or CRITICAL, stop and report before editing.

Implement the route/schema changes without editing `backend/src/worldgen/scaffold-generator.ts`, campaign manager, UI files, roadmap, requirements, or evidence campaign files. Do not remove legacy fields from config. Do not run a repair or migration command. Preserve legacy no-artifact behavior by gating legacy loads under `if (!researchArtifact)`.</action>
  <verify>
    <automated>npm --prefix backend run test -- src/routes/__tests__/worldgen.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Route tests from Task 1 pass.
    - Backend typecheck passes.
    - Artifact-backed route calls never pass both v2 artifact and legacy semantic context downstream.
    - Legacy no-artifact route tests still pass.
  </acceptance_criteria>
  <done>`/generate`, `/regenerate-section`, `/suggest-seed`, and `/save-edits` all preserve artifact authority boundaries while old no-artifact compatibility remains intact.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused closeout and write 71-09 summary</name>
  <files>.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-EVIDENCE-CAMPAIGN-HASHES.json, .planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-SUMMARY.md</files>
  <read_first>
    @AGENTS.md
    @.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-VERIFICATION.md
    @.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-SUMMARY.md
    @.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-PHASE-START.txt
    @.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-EVIDENCE-CAMPAIGN-HASHES.json
    @backend/src/routes/__tests__/worldgen.test.ts
  </read_first>
  <action>Run the focused route test, adjacent consumer tests, and typecheck. Then compare the ignored evidence-campaign files against the Task 1 SHA256 baseline exactly here, and fail on any mismatch:

```powershell
$baseline = Get-Content -Raw -LiteralPath '.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-EVIDENCE-CAMPAIGN-HASHES.json' | ConvertFrom-Json
foreach ($item in $baseline) {
  $current = (Get-FileHash -Algorithm SHA256 -LiteralPath $item.path).Hash
  if ($current -ne $item.sha256) { throw "Evidence campaign changed: $($item.path)" }
}
```

After the hash check passes, run `git diff -- campaigns/cc851187-f6fd-4e9e-9071-933cb056374b/config.json campaigns/cc851187-f6fd-4e9e-9071-933cb056374b/state.db` as supplemental evidence only; do not treat it as sufficient because `campaigns/` is gitignored. Run GitNexus change detection before commit when available: prefer MCP `gitnexus_detect_changes({scope: "staged"})` after staging only this plan's files. If MCP is unavailable, try `npx gitnexus detect_changes --repo WorldForge`; if the CLI returns `unknown command 'detect_changes'`, record that fallback and include `git diff --name-only -- backend/src/routes/schemas.ts backend/src/routes/worldgen.ts backend/src/routes/__tests__/worldgen.test.ts` plus `npx gitnexus status` as the local scope proof. Stop if unrelated files are staged.

Create `71-09-SUMMARY.md` with: gaps closed, files changed, tests run, GitNexus impact/detect fallback evidence, evidence-campaign SHA256 baseline path, closeout hash comparison result, supplemental no-diff proof, and explicit statement that no repair/migration command was run. Do not update `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, or phase-level `71-SUMMARY.md` in this plan.</action>
  <verify>
    <automated>npm --prefix backend run test -- src/routes/__tests__/worldgen.test.ts src/worldgen/__tests__/seed-suggester.test.ts src/worldgen/__tests__/lore-extractor.test.ts src/worldgen/__tests__/scaffold-resilience.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>
$baseline = Get-Content -Raw -LiteralPath '.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-EVIDENCE-CAMPAIGN-HASHES.json' | ConvertFrom-Json
foreach ($item in $baseline) {
  $current = (Get-FileHash -Algorithm SHA256 -LiteralPath $item.path).Hash
  if ($current -ne $item.sha256) { throw "Evidence campaign changed: $($item.path)" }
}
    </automated>
    <automated>git diff -- campaigns/cc851187-f6fd-4e9e-9071-933cb056374b/config.json campaigns/cc851187-f6fd-4e9e-9071-933cb056374b/state.db</automated>
  </verify>
  <acceptance_criteria>
    - Focused route and adjacent consumer tests pass.
    - Typecheck passes.
    - Evidence campaign config/state SHA256 hashes match `71-09-EVIDENCE-CAMPAIGN-HASHES.json`; any mismatch fails the task.
    - Evidence campaign diff command has no output as supplemental proof only.
    - `71-09-SUMMARY.md` records all three verification gaps as closed.
    - `71-09-SUMMARY.md` records the hash baseline file and closeout hash comparison result.
    - Summary records GitNexus impact and detect_changes result or the documented local fallback.
  </acceptance_criteria>
  <done>Gap closure evidence is recorded, evidence campaign hashes still match the pre-edit baseline, no evidence campaign repair occurred, and the plan can be handed to verification for P71-R4 re-check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request -> route schema | Client-supplied `researchArtifact`, `ipContext`, and `premiseDivergence` enter route orchestration. |
| campaign config -> route orchestration | Stored artifact and stored legacy fields can coexist. |
| route orchestration -> generator/lore prompts | Route-selected context becomes prompt-driving data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-09-01 | Tampering | `/generate` and `/regenerate-section` | mitigate | Artifact lane clears legacy context before divergence/frame/generator calls; route tests assert null legacy args. |
| T-71-09-02 | Tampering | `/suggest-seed` schema | mitigate | Reuse capped v2 artifact schema; malformed artifacts reject before `suggestSingleSeed`. |
| T-71-09-03 | Tampering | `/save-edits` lore extraction | mitigate | Stored artifact wins over legacy context; legacy context loads only when artifact absent. |
| T-71-09-04 | Repudiation | gap closure evidence | mitigate | `71-09-EVIDENCE-CAMPAIGN-HASHES.json` stores the pre-edit SHA256 baseline; `71-09-SUMMARY.md` records commands, GitNexus impact/detect fallback, hash comparison result, and supplemental no-diff proof. |
| T-71-09-05 | Information Disclosure | evidence campaign | accept | Plan only hashes and locally compares known local files; no secret or external data movement. |
</threat_model>

<verification>
Required:
- `npm --prefix backend run test -- src/routes/__tests__/worldgen.test.ts`
- `npm --prefix backend run test -- src/routes/__tests__/worldgen.test.ts src/worldgen/__tests__/seed-suggester.test.ts src/worldgen/__tests__/lore-extractor.test.ts src/worldgen/__tests__/scaffold-resilience.test.ts`
- `npm --prefix backend run typecheck`
- Create `.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-EVIDENCE-CAMPAIGN-HASHES.json` before any edits.
- Compare current evidence campaign SHA256 hashes against `.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-EVIDENCE-CAMPAIGN-HASHES.json` and fail if any hash changed.
- `git diff -- campaigns/cc851187-f6fd-4e9e-9071-933cb056374b/config.json campaigns/cc851187-f6fd-4e9e-9071-933cb056374b/state.db` as supplemental evidence only.
- GitNexus: impact before editing, detect_changes before commit when available, documented CLI/MCP fallback when unavailable.
</verification>

<success_criteria>
- All three gaps from `71-VERIFICATION.md` are covered by tests and implementation.
- No new plan touches UI, roadmap, requirements, phase-level summary, campaign repair, or migration behavior.
- Artifact-backed route flow never mixes v2 artifact with legacy `ipContext`, `premiseDivergence`, or `researchFrame`.
- Legacy no-artifact compatibility remains available and test-covered.
- Evidence campaign `cc851187-f6fd-4e9e-9071-933cb056374b` config/state SHA256 hashes match the pre-edit baseline, with empty config/state git diff recorded only as supplemental evidence.
</success_criteria>

<output>
During Task 1, create `.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-EVIDENCE-CAMPAIGN-HASHES.json`.
After completion, create `.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-09-SUMMARY.md`.
</output>
