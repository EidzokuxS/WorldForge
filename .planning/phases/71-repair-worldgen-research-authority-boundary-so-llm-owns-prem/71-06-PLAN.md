---
phase: 71-repair-worldgen-research-authority-boundary-so-llm-owns-prem
plan: 06
type: execute
wave: 4
depends_on: [71-05]
files_modified:
  - backend/src/worldgen/scaffold-steps/locations-step.ts
  - backend/src/worldgen/scaffold-steps/factions-step.ts
  - backend/src/worldgen/scaffold-steps/npcs-step.ts
  - backend/src/worldgen/__tests__/scaffold-resilience.test.ts
  - backend/src/worldgen/__tests__/npcs-step.test.ts
autonomous: true
requirements: [P71-R3, P71-R4]
requirements_addressed: [P71-R3, P71-R4]
must_haves:
  truths:
    - "Locations/factions/NPC generation consume artifact source usage rules."
    - "Naruto overlay mechanics do not become locations, factions, political structures, or cast in the JJK mixed-premise fixture."
    - "Canonical name lists from legacy `ipContext` are not applied as v2 artifact truth."
  artifacts:
    - path: "backend/src/worldgen/scaffold-steps/locations-step.ts"
      provides: "Artifact-aware location prompt"
    - path: "backend/src/worldgen/scaffold-steps/factions-step.ts"
      provides: "Artifact-aware faction prompt"
    - path: "backend/src/worldgen/scaffold-steps/npcs-step.ts"
      provides: "Artifact-aware NPC prompt"
  key_links:
    - from: "backend/src/worldgen/scaffold-steps/*-step.ts"
      to: "backend/src/worldgen/scaffold-steps/prompt-utils.ts"
      via: "buildWorldgenResearchContextBlock"
      pattern: "buildWorldgenResearchContextBlock"
---

<objective>
Rewire core scaffold entity steps to consume v2 research artifacts instead of legacy canonical franchise prompt blocks.

Purpose: the actual generated world structure must follow source usage rules, so a mechanics overlay cannot seed world geography, factions, or cast.
Output: artifact-aware locations, factions, and NPC step prompts with targeted regressions.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-CONTEXT.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-RESEARCH.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-VALIDATION.md
@AGENTS.md
@backend/src/worldgen/scaffold-steps/locations-step.ts
@backend/src/worldgen/scaffold-steps/factions-step.ts
@backend/src/worldgen/scaffold-steps/npcs-step.ts
@backend/src/worldgen/scaffold-steps/prompt-utils.ts
@backend/src/worldgen/__tests__/fixtures/jjk-naruto-artifact.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scaffold prompt leakage regressions</name>
  <files>backend/src/worldgen/__tests__/scaffold-resilience.test.ts, backend/src/worldgen/__tests__/npcs-step.test.ts</files>
  <behavior>
    - Location prompt for JJK/Naruto artifact asks for JJK locations/institutions and excludes Naruto geography/villages/nations.
    - Faction prompt asks for JJK institutions/factions and excludes Hidden Villages, Five Great Nations, Mizukage, Raikage, and Akatsuki unless artifact use rules allow factions.
    - NPC prompt asks for JJK characters/roles and excludes Naruto cast when Naruto is only `power_system`.
    - Legacy no-artifact scaffold prompts for locations/factions/NPCs with `{ ipContext, researchArtifact: null }` match golden/inline snapshots captured before production rewiring.
    - Null `ipContext` plus v2 artifact still emits artifact source rules for all three scaffold steps.
  </behavior>
  <action>Add failing tests by capturing mocked `safeGenerateObject` prompts or existing prompt test hooks. Import the JJK/Naruto fixture from Plan 01 instead of duplicating it. Add inline snapshots or golden strings for legacy `{ ipContext, researchArtifact: null }` prompts before changing production prompt code. Assertions must inspect prompt text and artifact rules, not final generated entity names only.</action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/scaffold-resilience.test.ts src/worldgen/__tests__/npcs-step.test.ts</automated>
  </verify>
  <done>Tests fail because scaffold steps still use legacy `ipContext` prompt blocks for v2 artifact flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire locations, factions, and NPC steps to artifact rules</name>
  <files>backend/src/worldgen/scaffold-steps/locations-step.ts, backend/src/worldgen/scaffold-steps/factions-step.ts, backend/src/worldgen/scaffold-steps/npcs-step.ts</files>
  <behavior>
    - Each step reads `req.researchArtifact`.
    - Each step emits `buildWorldgenResearchContextBlock({ researchArtifact: req.researchArtifact, ipContext, target })`.
    - Legacy `buildKnownIpGenerationContract` and `buildCanonicalList` are emitted only when no v2 artifact is present and legacy `ipContext` exists.
    - Legacy no-artifact prompt text remains snapshot-stable for worldbook/manual compatibility.
  </behavior>
  <action>Before editing, run `npx gitnexus status`; if stale, run `npx gitnexus analyze`. Run GitNexus impact for `generateLocationsStep`, `generateFactionsStep`, `generateNpcsStep`, `buildIpContextBlock`, and `buildKnownIpGenerationContract`; report any HIGH/CRITICAL result before edits. Update the three step files to prefer the Plan 05 helper for v2 artifacts and keep existing legacy behavior for worldbook/manual context. Do not add backend logic that decides which source is primary.</action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/scaffold-resilience.test.ts src/worldgen/__tests__/npcs-step.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <done>Scaffold step tests pass; prompt text for JJK/Naruto artifact excludes Naruto world-structure instructions and preserves JJK world-basis rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact prompt context -> generator LLM | Source usage rules direct generation of locations, factions, and NPCs. |
| legacy ipContext -> generator LLM | Old compatibility data remains prompt context for old/manual flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-06-01 | Tampering | entity step prompts | mitigate | Tests assert source usage roles control prompt wording for v2 artifact flow. |
| T-71-06-02 | Tampering | legacy compatibility branch | mitigate | Keep legacy branch gated on absence of v2 artifact. |
| T-71-06-03 | Denial of Service | prompt size | mitigate | Use bounded artifact formatter and existing token clamps. |
| T-71-06-04 | Repudiation | generated world structure | mitigate | Prompt includes artifact provenance/source labels so generated choices are traceable to artifact data. |
</threat_model>

<verification>
- `npm --prefix backend run test -- src/worldgen/__tests__/scaffold-resilience.test.ts src/worldgen/__tests__/npcs-step.test.ts`
- `npm --prefix backend run typecheck`
- `rg "FRANCHISE REFERENCE|Build the canonical world|This source IS the world|Canonical .* FROM" backend/src/worldgen/scaffold-steps/locations-step.ts backend/src/worldgen/scaffold-steps/factions-step.ts backend/src/worldgen/scaffold-steps/npcs-step.ts`
</verification>

<success_criteria>
- Locations/factions/NPC prompts use v2 artifact rules when present.
- Legacy worldbook/manual context remains compatible.
- Legacy no-artifact scaffold prompt goldens remain unchanged.
- Mixed JJK/Naruto artifact cannot instruct Naruto world geography/factions/cast for scaffold structure.
</success_criteria>

<output>
After completion, create `.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-06-SUMMARY.md`.
</output>
