---
phase: 71-repair-worldgen-research-authority-boundary-so-llm-owns-prem
plan: 07
type: execute
wave: 5
depends_on: [71-06]
files_modified:
  - backend/src/worldgen/scaffold-generator.ts
  - backend/src/worldgen/scaffold-steps/regen-helpers.ts
  - backend/src/worldgen/lore-extractor.ts
  - backend/src/worldgen/retrieval-intent.ts
  - backend/src/routes/worldgen.ts
  - backend/src/worldgen/__tests__/scaffold-resilience.test.ts
  - backend/src/worldgen/__tests__/lore-extractor.test.ts
  - backend/src/routes/__tests__/worldgen.test.ts
autonomous: true
requirements: [P71-R3, P71-R4]
requirements_addressed: [P71-R3, P71-R4]
must_haves:
  truths:
    - "Validation, regeneration, lore extraction, and sufficiency paths use the artifact where available."
    - "Regenerate-section persists artifact enrichment through the v2 artifact lane."
    - "Legacy `IpResearchContext` enrichment still works for old/worldbook flows without v2 artifact."
  artifacts:
    - path: "backend/src/worldgen/scaffold-generator.ts"
      provides: "Artifact-aware scaffold orchestration and validation context"
    - path: "backend/src/worldgen/scaffold-steps/regen-helpers.ts"
      provides: "Artifact-aware regenerate prompt helpers"
    - path: "backend/src/worldgen/lore-extractor.ts"
      provides: "Artifact-aware lore extraction prompt"
    - path: "backend/src/routes/worldgen.ts"
      provides: "Persist enriched artifact after generation/regeneration"
  key_links:
    - from: "backend/src/routes/worldgen.ts"
      to: "backend/src/worldgen/scaffold-generator.ts"
      via: "researchArtifact save-back"
      pattern: "researchArtifact"
    - from: "backend/src/worldgen/scaffold-generator.ts"
      to: "backend/src/worldgen/ip-researcher.ts"
      via: "evaluateResearchArtifactSufficiency"
      pattern: "evaluateResearchArtifactSufficiency"
---

<objective>
Complete artifact consumption across scaffold orchestration, regeneration, lore extraction, and sufficiency enrichment.

Purpose: source usage rules must survive beyond initial scaffold prompts into validation, section regeneration, lore extraction, and follow-up research so the old canonical-frame language cannot re-enter through adjacent paths.
Output: artifact-aware orchestration plus route save-back for enriched artifacts.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-CONTEXT.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-RESEARCH.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-VALIDATION.md
@AGENTS.md
@backend/src/worldgen/scaffold-generator.ts
@backend/src/worldgen/scaffold-steps/regen-helpers.ts
@backend/src/worldgen/lore-extractor.ts
@backend/src/routes/worldgen.ts
@backend/src/worldgen/retrieval-intent.ts
@backend/src/worldgen/__tests__/fixtures/jjk-naruto-artifact.ts

<legacy_authority_fate>
Final legacy decisions for Phase 71:
- `evaluateResearchSufficiency`: legacy-only for `IpResearchContext` when no v2 artifact exists.
- `buildWorldgenResearchPlan`: legacy-only helper for old single-source `IpResearchContext` enrichment; v2 artifact flow must not call it.
- `researchKnownIP`: must not be reachable from generate/regenerate when `researchArtifact` exists.
- Any retained legacy behavior is acceptable only for explicit user-selected single-source/manual/worldbook or old-campaign compatibility, never automatic mixed-premise v2 flow.
</legacy_authority_fate>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock regenerate, validation, and lore artifact regressions</name>
  <files>backend/src/worldgen/__tests__/scaffold-resilience.test.ts, backend/src/worldgen/__tests__/lore-extractor.test.ts, backend/src/routes/__tests__/worldgen.test.ts</files>
  <behavior>
    - Scaffold validation context uses artifact block and omits `Canonical subject`.
    - Regenerate helpers use artifact source usage rules and do not render legacy `FRANCHISE REFERENCE` for v2 flow.
    - Lore extraction receives artifact source rules and maps source/use categories to extraction categories.
    - Lore alignment: if JJK is only a `tone_overlay` and Naruto owns `power_system`, ability-card extraction may use Naruto power-system rules but must not create JJK ability cards from tone-only source data.
    - Route tests prove enriched artifact save-back for generate and regenerate-section.
    - Artifact roundtrip: enrich -> save -> reload -> enrich-again keeps the artifact valid/idempotent and does not duplicate identical search results/facts.
    - Legacy gate: v2 artifact flow does not call `evaluateResearchSufficiency` or `buildWorldgenResearchPlan`.
  </behavior>
  <action>Add failing prompt-capture and route tests. Import the JJK/Naruto fixture from Plan 01 instead of duplicating it, and add a second fixture variant where JJK is `tone_overlay` and Naruto is `mechanics_overlay` for `power_system`. Keep existing legacy `ipContext` sufficiency tests; add v2 artifact-specific assertions beside them.</action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/scaffold-resilience.test.ts src/worldgen/__tests__/lore-extractor.test.ts src/routes/__tests__/worldgen.test.ts</automated>
  </verify>
  <done>Tests fail only because artifact-aware orchestration/save-back is missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire artifact sufficiency and prompt consumers through orchestration</name>
  <files>backend/src/worldgen/scaffold-generator.ts, backend/src/worldgen/scaffold-steps/regen-helpers.ts, backend/src/worldgen/lore-extractor.ts, backend/src/worldgen/retrieval-intent.ts, backend/src/routes/worldgen.ts</files>
  <behavior>
    - `generateWorldScaffold` carries `researchArtifact` through validation and returns a single `researchArtifact` field containing the original or enriched artifact.
    - `regenerate-section` uses `evaluateResearchArtifactSufficiency` when artifact exists and saves the enriched artifact.
    - `regen-helpers` and `lore-extractor` use `buildWorldgenResearchContextBlock`.
    - Legacy `evaluateResearchSufficiency` and `buildWorldgenResearchPlan` remain for old `IpResearchContext` only and are gated off for v2 artifacts.
  </behavior>
  <action>Before editing, run `npx gitnexus status`; if stale, run `npx gitnexus analyze`. Run GitNexus impact for `generateWorldScaffold`, `evaluateResearchSufficiency`, `buildWorldgenResearchPlan`, `buildWorldgenResearchFrameBlock`, `extractLoreCards`, `regenerateLocationEntity`, `regenerateFactionEntity`, `regenerateNpcEntity`, and route file `backend/src/routes/worldgen.ts`; report any HIGH/CRITICAL result before edits. Prefer artifact sufficiency when `researchArtifact` exists. Keep legacy `ipContext`/`WorldgenResearchFrame` path only for old/worldbook/manual compatibility. Add or preserve a legacy-only annotation/gate in `retrieval-intent.ts` for `buildWorldgenResearchPlan`. Route save-back should persist the single returned `researchArtifact` when it differs from the loaded artifact; do not introduce a parallel `enrichedResearchArtifact` field. Tighten lore extraction prompt routing so ability-card extraction uses only sources with `useFor` containing `power_system`; tone-only sources can affect style/tone but not ability-card facts. Do not add repair logic for existing generated campaign `cc851187-f6fd-4e9e-9071-933cb056374b`.</action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/scaffold-resilience.test.ts src/worldgen/__tests__/lore-extractor.test.ts src/routes/__tests__/worldgen.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <done>Artifact-aware validation, regenerate, lore, sufficiency, legacy gate, and roundtrip tests pass; route save-back persists the single v2 artifact field; legacy enrichment tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact/search enrichment -> config | Additional search results/generated context are persisted after generation/regeneration. |
| artifact context -> validation/regenerate/lore prompts | Stored artifact data influences later generator calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-07-01 | Tampering | artifact sufficiency | mitigate | Use artifact source usage rules and schema validation; backend does not infer source meaning. |
| T-71-07-02 | Repudiation | enriched artifact save-back | mitigate | Preserve search job IDs, result URLs, provider/model provenance. |
| T-71-07-03 | Denial of Service | enrichment loop | mitigate | Cap follow-up jobs/results and keep existing search limits. |
| T-71-07-04 | Tampering | legacy frame path | mitigate | Gate legacy `WorldgenResearchFrame` usage to absence of v2 artifact and cover with tests. |
</threat_model>

<verification>
- `npm --prefix backend run test -- src/worldgen/__tests__/scaffold-resilience.test.ts src/worldgen/__tests__/lore-extractor.test.ts src/routes/__tests__/worldgen.test.ts`
- `npm --prefix backend run typecheck`
- `rg "Canonical subject|FRANCHISE REFERENCE|Build the canonical world|This source IS the world" backend/src/worldgen/scaffold-generator.ts backend/src/worldgen/scaffold-steps/regen-helpers.ts backend/src/worldgen/lore-extractor.ts backend/src/routes/worldgen.ts`
</verification>

<success_criteria>
- Artifact rules are consumed by validation, regenerate, lore, and sufficiency paths.
- Artifact enrichment is persisted.
- Legacy paths remain compatible and explicitly separate.
- V2 artifact flow cannot call legacy sufficiency or deterministic search-prefix planning.
- Lore extraction aligns source uses with extraction categories; tone-only JJK cannot create JJK ability cards when Naruto owns `power_system`.
- Enriched artifacts survive save/reload/enrich-again without duplication.
</success_criteria>

<output>
After completion, create `.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-07-SUMMARY.md`.
</output>
