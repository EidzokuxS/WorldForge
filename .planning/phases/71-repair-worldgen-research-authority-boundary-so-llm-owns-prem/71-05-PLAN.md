---
phase: 71-repair-worldgen-research-authority-boundary-so-llm-owns-prem
plan: 05
type: execute
wave: 3
depends_on: [71-04]
files_modified:
  - backend/src/worldgen/scaffold-steps/prompt-utils.ts
  - backend/src/worldgen/scaffold-steps/premise-step.ts
  - backend/src/worldgen/seed-suggester.ts
  - backend/src/worldgen/__tests__/seed-suggester.test.ts
  - backend/src/worldgen/__tests__/premise-divergence.test.ts
  - backend/src/worldgen/__tests__/research-artifact.test.ts
autonomous: true
requirements: [P71-R3, P71-R4]
requirements_addressed: [P71-R3, P71-R4]
must_haves:
  truths:
    - "Seed and premise generation consume the research artifact as source usage rules."
    - "Legacy `buildIpContextBlock` remains available for worldbook/manual compatibility but is not the v2 artifact formatter."
    - "Seed/premise prompts do not tell the model that backend selected a canonical franchise."
  artifacts:
    - path: "backend/src/worldgen/scaffold-steps/prompt-utils.ts"
      provides: "Artifact-aware prompt helper"
      exports: ["buildWorldgenResearchContextBlock"]
    - path: "backend/src/worldgen/seed-suggester.ts"
      provides: "Seed suggestion artifact consumption"
    - path: "backend/src/worldgen/scaffold-steps/premise-step.ts"
      provides: "Premise refinement artifact consumption"
  key_links:
    - from: "backend/src/worldgen/seed-suggester.ts"
      to: "backend/src/worldgen/scaffold-steps/prompt-utils.ts"
      via: "buildWorldgenResearchContextBlock"
      pattern: "buildWorldgenResearchContextBlock"
---

<objective>
Move seed and premise prompt consumption from canonical franchise wording to artifact-authored source usage rules.

Purpose: the first worldgen prompts must stop framing mixed premises as one backend-owned franchise, while old worldbook/manual context still works through compatibility helpers.
Output: artifact-aware prompt helper, seed/premise rewires, and prompt wording tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-CONTEXT.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-RESEARCH.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-VALIDATION.md
@AGENTS.md
@backend/src/worldgen/scaffold-steps/prompt-utils.ts
@backend/src/worldgen/seed-suggester.ts
@backend/src/worldgen/scaffold-steps/premise-step.ts
@backend/src/worldgen/__tests__/fixtures/jjk-naruto-artifact.ts

<interfaces>
High-risk existing helper:
```typescript
export function buildIpContextBlock(ipContext: IpResearchContext | null): string
```

Add:
```typescript
export function buildWorldgenResearchContextBlock(input: {
  researchArtifact?: WorldgenResearchArtifactV2 | null;
  ipContext?: IpResearchContext | null;
  target: string;
}): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock seed and premise prompt wording regressions</name>
  <files>backend/src/worldgen/__tests__/seed-suggester.test.ts, backend/src/worldgen/__tests__/premise-divergence.test.ts, backend/src/worldgen/__tests__/research-artifact.test.ts</files>
  <behavior>
    - Artifact formatter includes source usage rules for seed and premise targets.
    - `suggestWorldSeeds` with JJK/Naruto artifact does not render `This world is the Naruto universe`, `FRANCHISE REFERENCE`, or `Build the canonical world`.
    - Premise-step prompt uses artifact source rules and does not rely on `ipContext.franchise` when `researchArtifact` is present.
    - Legacy no-artifact seed prompt with `{ ipContext, researchArtifact: null }` matches a golden/inline snapshot captured before production prompt rewiring.
    - Legacy no-artifact premise prompt with `{ ipContext, researchArtifact: null }` matches a golden/inline snapshot captured before production prompt rewiring.
    - Null `ipContext` regression: `{ ipContext: null, researchArtifact: v2 }` works for seed/premise prompt assembly and does not call or require `ipContext.franchise`.
    - Divergence regression: `interpretPremiseDivergence(null, premise, role)` either returns the explicit no-context result or uses only raw premise/artifact-adjacent data without re-canonicalizing to a franchise string.
  </behavior>
  <action>Add failing assertions around prompt text using mocked generator calls. Import the JJK/Naruto fixture from Plan 01 instead of duplicating it. Add inline snapshots or golden strings for legacy `{ ipContext, researchArtifact: null }` seed and premise prompts before changing production prompt code. Keep the old `ipContext` tests for worldbook/manual compatibility unchanged.</action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/seed-suggester.test.ts src/worldgen/__tests__/premise-divergence.test.ts src/worldgen/__tests__/research-artifact.test.ts</automated>
  </verify>
  <done>Tests fail because seed/premise consumers still render legacy franchise wording.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add artifact-aware prompt helper and rewire seed/premise consumers</name>
  <files>backend/src/worldgen/scaffold-steps/prompt-utils.ts, backend/src/worldgen/scaffold-steps/premise-step.ts, backend/src/worldgen/seed-suggester.ts</files>
  <behavior>
    - `buildWorldgenResearchContextBlock` chooses v2 artifact formatting when `researchArtifact` exists.
    - `buildIpContextBlock` remains unchanged for legacy/manual/worldbook context.
    - `buildKnownIpGenerationContract` is not emitted for v2 artifact flow.
    - `buildWorldgenResearchContextBlock` handles `ipContext: null` when `researchArtifact` exists.
  </behavior>
  <action>Before editing, run `npx gitnexus status`; if stale, run `npx gitnexus analyze`. Run GitNexus impact for `buildIpContextBlock` and `interpretPremiseDivergence`; both are high-risk if touched, so report direct callers and affected processes before edits. Also run impact for `suggestWorldSeeds`, `suggestSingleSeed`, and `generateRefinedPremiseStep`. Add `buildWorldgenResearchContextBlock` in `prompt-utils.ts` that delegates to `formatWorldgenResearchArtifactBlock` when artifact exists, otherwise uses legacy `buildIpContextBlock` byte-for-byte for no-artifact flow. Rewire `seed-suggester.ts` and `premise-step.ts` to pass `req.researchArtifact`. Do not alter `interpretPremiseDivergence` unless unavoidable; if touched, update all d=1 dependents and keep the null-`ipContext` regression green.</action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/seed-suggester.test.ts src/worldgen/__tests__/premise-divergence.test.ts src/worldgen/__tests__/research-artifact.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <done>Seed and premise tests pass; v2 artifact prompts contain no backend-owned canonical subject wording; legacy ipContext tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted artifact -> prompt helper | Stored LLM/search artifact becomes prompt context. |
| prompt helper -> generator LLM | Prompt wording influences world DNA and premise generation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-05-01 | Tampering | `buildWorldgenResearchContextBlock` | mitigate | Prefer validated artifact; backend only formats source rules, not source meaning. |
| T-71-05-02 | Tampering | seed prompts | mitigate | Regression tests assert no canonical franchise wording for v2 artifact flow. |
| T-71-05-03 | Denial of Service | prompt helper | mitigate | Use Plan 01 bounded formatter; do not expand raw search results unbounded. |
| T-71-05-04 | Information Disclosure | prompt helper logs | mitigate | Do not log raw prompt bodies or raw hidden reasoning. |
</threat_model>

<verification>
- `npm --prefix backend run test -- src/worldgen/__tests__/seed-suggester.test.ts src/worldgen/__tests__/premise-divergence.test.ts src/worldgen/__tests__/research-artifact.test.ts`
- `npm --prefix backend run typecheck`
- `rg "This world is the .* universe|FRANCHISE REFERENCE|Build the canonical world|Canonical subject" backend/src/worldgen/seed-suggester.ts backend/src/worldgen/scaffold-steps/premise-step.ts backend/src/worldgen/scaffold-steps/prompt-utils.ts`
</verification>

<success_criteria>
- Seed and premise generation use v2 artifact rules when present.
- Legacy `ipContext` compatibility remains.
- Legacy no-artifact seed/premise prompts are protected by golden/snapshot tests.
- Null `ipContext` plus v2 artifact does not crash or re-canonicalize.
- High-risk prompt helper impact was reviewed before edits.
</success_criteria>

<output>
After completion, create `.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-05-SUMMARY.md`.
</output>
