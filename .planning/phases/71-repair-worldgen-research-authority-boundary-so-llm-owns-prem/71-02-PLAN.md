---
phase: 71-repair-worldgen-research-authority-boundary-so-llm-owns-prem
plan: 02
type: execute
wave: 1
depends_on: [71-01]
files_modified:
  - backend/src/campaign/manager.ts
  - backend/src/campaign/index.ts
  - backend/src/campaign/__tests__/manager.test.ts
autonomous: true
requirements: [P71-R5]
requirements_addressed: [P71-R5]
must_haves:
  truths:
    - "Campaign config can persist new v2 research artifacts."
    - "Existing configs with only legacy `ipContext` or `worldgenResearchFrame` still load without mutation."
    - "No existing campaign is silently repaired or rewritten."
  artifacts:
    - path: "backend/src/campaign/manager.ts"
      provides: "Read/write helpers for `worldgenResearchArtifact`"
      exports: ["saveWorldgenResearchArtifact", "loadWorldgenResearchArtifact"]
    - path: "backend/src/campaign/__tests__/manager.test.ts"
      provides: "Persistence compatibility regressions"
  key_links:
    - from: "backend/src/campaign/manager.ts"
      to: "backend/src/worldgen/research-artifact.ts"
      via: "WorldgenResearchArtifactV2 type and parser"
      pattern: "WorldgenResearchArtifactV2"
---

<objective>
Add compatibility-safe campaign persistence for `WorldgenResearchArtifactV2`.

Purpose: routes need a durable artifact lane that stores the user's raw premise, LLM-authored source plan, search results, generated context, and provenance without rewriting existing `ipContext` or `worldgenResearchFrame` data.
Output: campaign config field, save/load helpers, index exports, and manager tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-CONTEXT.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-RESEARCH.md
@.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-VALIDATION.md
@AGENTS.md
@backend/src/campaign/manager.ts
@backend/src/campaign/index.ts
@backend/src/campaign/__tests__/manager.test.ts
@backend/src/worldgen/research-artifact.ts
@backend/src/worldgen/__tests__/fixtures/jjk-naruto-artifact.ts

<interfaces>
Existing campaign config type includes:
```typescript
type CampaignConfigFile = {
  name: string;
  premise: string;
  seeds?: WorldSeeds;
  ipContext?: IpResearchContext;
  premiseDivergence?: PremiseDivergence;
  worldgenResearchFrame?: WorldgenResearchFrame;
  createdAt: number;
  updatedAt?: number;
};
```

Add:
```typescript
worldgenResearchArtifact?: WorldgenResearchArtifactV2;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock campaign artifact persistence and legacy compatibility tests</name>
  <files>backend/src/campaign/__tests__/manager.test.ts</files>
  <behavior>
    - Test 1: `readCampaignConfig` returns `worldgenResearchArtifact` when config contains a valid v2 artifact.
    - Test 2: `saveWorldgenResearchArtifact` writes the v2 artifact beside existing config fields without removing `ipContext`, `premiseDivergence`, or `worldgenResearchFrame`.
    - Test 3: `loadWorldgenResearchArtifact` returns `null` when only legacy fields exist.
    - Test 4: Reading a legacy config does not write to disk or mutate the file contents.
  </behavior>
  <action>Add failing tests importing `jjkWithNarutoPowerSystemArtifact` from `backend/src/worldgen/__tests__/fixtures/jjk-naruto-artifact.ts` created by Plan 01. Do not duplicate the mixed-premise fixture inline. Do not reference or mutate `campaigns/cc851187-f6fd-4e9e-9071-933cb056374b`; that campaign remains evidence only.</action>
  <verify>
    <automated>npm --prefix backend run test -- src/campaign/__tests__/manager.test.ts</automated>
  </verify>
  <done>Manager tests fail only because `worldgenResearchArtifact` helpers and config support are missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add read/write helpers without silent repair</name>
  <files>backend/src/campaign/manager.ts, backend/src/campaign/index.ts</files>
  <behavior>
    - `readCampaignConfig` preserves all legacy fields and returns `worldgenResearchArtifact` only when present.
    - `saveWorldgenResearchArtifact` updates config through the existing `updateCampaignConfig` path.
    - `loadWorldgenResearchArtifact` fail-closes to `null` on missing config or missing artifact.
  </behavior>
  <action>Before editing production symbols, run `npx gitnexus status`; if stale, run `npx gitnexus analyze`. Run GitNexus impact for `readCampaignConfig`, `updateCampaignConfig`, `createCampaign`, `saveWorldgenResearchFrame`, and `loadWorldgenResearchFrame`; report any HIGH/CRITICAL result before edits. Add `worldgenResearchArtifact?: WorldgenResearchArtifactV2` to `CampaignConfigFile`, return it from `readCampaignConfig`, add `saveWorldgenResearchArtifact` and `loadWorldgenResearchArtifact`, and export them from `backend/src/campaign/index.ts`. Do not add any migration, backfill, or repair code.</action>
  <verify>
    <automated>npm --prefix backend run test -- src/campaign/__tests__/manager.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <done>Campaign persistence tests pass; legacy config tests prove old fields still load; no code path rewrites existing campaign data while reading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem config -> backend reader | Existing and new campaign config JSON enters typed backend code. |
| route/worldgen code -> config writer | Generated research artifacts become durable local user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-02-01 | Tampering | `readCampaignConfig` | mitigate | Read new field only when present; preserve legacy fields; no read-time repair writes. |
| T-71-02-02 | Repudiation | `saveWorldgenResearchArtifact` | mitigate | Persist provenance inside artifact and keep `updatedAt` through existing update path. |
| T-71-02-03 | Information Disclosure | campaign config | accept | Local single-user app already stores campaign config as local JSON; no new external exposure. |
| T-71-02-04 | Denial of Service | artifact persistence | mitigate | Rely on Plan 01 schema/caps before routes save artifacts. |
</threat_model>

<verification>
- `npm --prefix backend run test -- src/campaign/__tests__/manager.test.ts`
- `npm --prefix backend run typecheck`
- Confirm `git diff -- campaigns/cc851187-f6fd-4e9e-9071-933cb056374b/config.json` is empty.
</verification>

<success_criteria>
- New v2 artifacts can be saved and loaded.
- Legacy `ipContext` and `worldgenResearchFrame` configs remain readable.
- No automatic repair or migration touches existing campaigns.
</success_criteria>

<output>
After completion, create `.planning/phases/71-repair-worldgen-research-authority-boundary-so-llm-owns-prem/71-02-SUMMARY.md`.
</output>
