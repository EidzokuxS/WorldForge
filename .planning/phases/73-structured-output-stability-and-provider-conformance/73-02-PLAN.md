---
phase: 73-structured-output-stability-and-provider-conformance
plan: 02
type: execute
wave: 2
depends_on: [73-01]
files_modified:
  - backend/src/ai/generate-object-safe.ts
  - backend/src/ai/__tests__/generate-object-safe.test.ts
autonomous: true
requirements: [P73-R2, P73-R3, P73-R6, P73-R7]
must_haves:
  truths:
    - "Existing callers still call `safeGenerateObject(opts)` and receive `{ object, trace }`."
    - "`safeGenerateObject` tries AI SDK `Output.object({ schema })` before text JSON extraction when capability allows it."
    - "Text fallback, repair, and retry remain explicit and trace-labeled."
  artifacts:
    - path: "backend/src/ai/generate-object-safe.ts"
      provides: "Native-first safe object generation with text fallback and strategy trace labels"
    - path: "backend/src/ai/__tests__/generate-object-safe.test.ts"
      provides: "Native success, native failure fallback, repair, retry, and Kimi/Mimo regression coverage"
  key_links:
    - from: "backend/src/ai/generate-object-safe.ts"
      to: "backend/src/ai/structured-output-capabilities.ts"
      via: "resolveStructuredOutputCapability"
      pattern: "resolveStructuredOutputCapability"
    - from: "safeGenerateObject"
      to: "AI SDK"
      via: "generateText({ output: Output.object({ schema }) })"
      pattern: "Output\\.object"
---

<objective>
Make the shared `safeGenerateObject` boundary native-first and backward-compatible.

Purpose: P73-R2 requires provider-native structured output where available, but Phase 73 must preserve existing text fallback and validation-aware repair because `safeGenerateObject` is CRITICAL.
Output: Strategy-aware `safeGenerateObject` implementation and focused tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-CONTEXT.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-PRD.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-RESEARCH.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-VALIDATION.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-PATTERNS.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-01-SUMMARY.md
@backend/src/ai/generate-object-safe.ts
@backend/src/ai/structured-output-capabilities.ts
@backend/src/ai/__tests__/generate-object-safe.test.ts
</context>

<source_audit>
GOAL: Native provider schema first, backend validation final, explicit fallback. Covered here for the shared object boundary.
REQ: P73-R2 implemented here; P73-R3 strategy trace labels implemented here; P73-R6 preserved by final Zod parse/repair; P73-R7 Kimi/Mimo regression remains covered here.
RESEARCH: Uses installed AI SDK v6 `Output.object` path and keeps text extraction/repair as secondary guardrail.
CONTEXT: Honors native-first, explicit text fallback, per-call trace labels, and no Zod removal. Deferred prose-quality ranking is not touched.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add native schema branch and strategy trace tests</name>
  <read_first>
    backend/src/ai/generate-object-safe.ts
    backend/src/ai/structured-output-capabilities.ts
    backend/src/ai/__tests__/generate-object-safe.test.ts
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-VALIDATION.md
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-PATTERNS.md
    AGENTS.md
    .claude/skills/gitnexus/gitnexus-impact-analysis/SKILL.md
  </read_first>
  <files>backend/src/ai/generate-object-safe.ts, backend/src/ai/__tests__/generate-object-safe.test.ts</files>
  <behavior>
    - Test 1: AI SDK native result with `output: { hp: 5 }` returns `{ object: { hp: 5 } }` and trace `strategy: "native_schema"`.
    - Test 2: native branch still runs `opts.schema.safeParse` before returning and fails/repairs if output violates Zod.
    - Test 3: existing text-only success tests still pass without caller changes.
  </behavior>
  <action>
    Run `gitnexus_impact({target: "safeGenerateObject", direction: "upstream"})`, `gitnexus_impact({target: "attemptGenerate", direction: "upstream"})`, and `gitnexus_impact({target: "toTraceFromGenerateTextResult", direction: "upstream"})` before editing. `safeGenerateObject` is expected CRITICAL by Phase 73; record direct callers and proceed only with backward-compatible changes. If any new unrelated HIGH/CRITICAL target appears, stop and report.

    In `generate-object-safe.ts`, import `Output` and `NoObjectGeneratedError` if available from `ai`, plus `resolveStructuredOutputCapability` and strategy types from `structured-output-capabilities.ts`. Keep `safeGenerateObject<T>(opts: SafeGenerateOpts<T>): Promise<SafeGenerateResult<T>>` unchanged. Extend `SafeGenerateTrace` with non-breaking fields: `requestedMode`, `strategy`, `primaryStrategy`, `fallbackStrategy`, `fallbackReason`, and `capability`.

    Split the old text path into a named helper such as `attemptTextFallbackGenerate`. Add a native helper such as `attemptNativeStructuredGenerate` that calls `generateText({ ...callOpts, output: Output.object({ schema: opts.schema }) })`. Read the installed AI SDK type definitions if the exact result field is unclear, then use the returned object output field from the SDK result. Always validate the candidate with `opts.schema.safeParse` before returning.

    Strategy labels must be exact strings: `native_schema`, `native_json`, `tool_mode`, `text_fallback`, `repair`, `full_retry`. The normal auto path should use `native_schema` first when resolver primary strategy is `native_schema`. Keep `mode?: "json" | "tool"` accepted; `mode: "tool"` may resolve to `tool_mode` but must not remove text fallback.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/ai/__tests__/generate-object-safe.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>
    `Select-String -Path backend/src/ai/generate-object-safe.ts -Pattern "Output.object|native_schema|text_fallback|strategy|resolveStructuredOutputCapability"` finds all terms.
    `Select-String -Path backend/src/ai/__tests__/generate-object-safe.test.ts -Pattern "native_schema|strategy|Output.object"` finds native branch tests.
    Existing caller signature remains: `Select-String -Path backend/src/ai/generate-object-safe.ts -Pattern "export async function safeGenerateObject<T>\\(opts: SafeGenerateOpts<T>\\)"` returns a match.
  </acceptance_criteria>
  <done>Native structured output is the primary branch without breaking existing callers or skipping Zod validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve explicit text fallback, repair, retry, and Kimi/Mimo coverage</name>
  <read_first>
    backend/src/ai/generate-object-safe.ts
    backend/src/ai/__tests__/generate-object-safe.test.ts
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-PRD.md
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-VALIDATION.md
    .planning/phases/72-worldgen-authority-propagation-regression-audit/72-02-SUMMARY.md
  </read_first>
  <files>backend/src/ai/generate-object-safe.ts, backend/src/ai/__tests__/generate-object-safe.test.ts</files>
  <behavior>
    - Test 1: simulated `NoObjectGeneratedError` or native schema rejection falls back to text extraction in the same attempt and returns trace `strategy: "text_fallback"` with `fallbackReason`.
    - Test 2: invalid text JSON triggers repair with nested trace `repair.strategy: "repair"` and keeps Kimi/Mimo `citations` and `canonicalNames` repair.
    - Test 3: when native, text fallback, and repair all fail, the outer retry log/trace path labels the failure as `full_retry` before final throw.
    - Test 4: `isSafeGenerateObjectError` recognizes both existing `safeGenerateObject:` failures and new structured-output failure messages.
  </behavior>
  <action>
    Keep repair prompt behavior from the existing implementation, including the explicit `citations` and `canonicalNames` repair rules. Do not delete `coerceToSchema`, balanced JSON extraction, bare-array wrapping, or existing fallback error recognition.

    Add tests that mock native failure, text fallback success, repair success, and full retry exhaustion. The fallback branch must call the old text JSON path with the JSON-only suffix. The trace must show which strategy actually returned the object. The log event payload for `llm.attempt` must include `strategy`, `primaryStrategy`, `fallbackStrategy`, `fallbackReason`, `responseModel`, `usage`, and bounded `error`; it must not include API keys or prompt bodies.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/ai/__tests__/generate-object-safe.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>
    `Select-String -Path backend/src/ai/__tests__/generate-object-safe.test.ts -Pattern "NoObjectGeneratedError|text_fallback|repair|full_retry|canonicalNames|citations"` finds all terms.
    `Select-String -Path backend/src/ai/generate-object-safe.ts -Pattern "llm.attempt|fallbackReason|repair:|full_retry|isSafeGenerateObjectError"` finds all terms.
    Targeted tests and typecheck exit 0.
  </acceptance_criteria>
  <done>`safeGenerateObject` remains compatible, native-first, observable, repair-capable, and regression-locked for observed Kimi/Mimo shape drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI provider -> backend schema | Provider output is untrusted even when native structured output is used. |
| fallback logs -> developer diagnostics | Logs need strategy/error metadata without leaking prompts/secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T73-02-01 | Tampering | safeGenerateObject native branch | mitigate | Always run final Zod safeParse; native output does not bypass coerce/repair/validation authority. |
| T73-02-02 | Denial of Service | retry/fallback loop | mitigate | Bounded retries remain; native rejection falls to explicit text fallback once per attempt, then repair, then full retry. |
| T73-02-03 | Information Disclosure | llm.attempt trace | mitigate | Log strategy/capability/usage/error type only; no prompt bodies, API keys, or hidden reasoning beyond existing bounded reasoning length. |
</threat_model>

<verification>
Run:
`npm --prefix backend run test -- src/ai/__tests__/generate-object-safe.test.ts`
`npm --prefix backend run typecheck`
</verification>

<success_criteria>
`safeGenerateObject` is native-first through AI SDK `Output.object`, keeps explicit text fallback and repair, preserves caller compatibility, and emits exact strategy labels for P73-R2/P73-R3/P73-R6/P73-R7.
</success_criteria>

<output>
After completion, create `.planning/phases/73-structured-output-stability-and-provider-conformance/73-02-SUMMARY.md`.
</output>
