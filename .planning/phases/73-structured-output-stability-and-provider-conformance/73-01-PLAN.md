---
phase: 73-structured-output-stability-and-provider-conformance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/ai/provider-registry.ts
  - backend/src/ai/structured-output-capabilities.ts
  - backend/src/ai/__tests__/structured-output-boundary.test.ts
  - backend/src/ai/__tests__/structured-output-capabilities.test.ts
  - .planning/phases/73-structured-output-stability-and-provider-conformance/73-STRUCTURED-OUTPUT-INVENTORY.md
autonomous: true
requirements: [P73-R1, P73-R3, P73-R5]
must_haves:
  truths:
    - "Executor can see every production object-generation seam and its intended strategy before changing the shared generator."
    - "Provider/model/transport identity is available to strategy logs without changing existing caller signatures."
    - "Capability decisions are keyed by provider/model/protocol/transport instead of a hidden global assumption."
  artifacts:
    - path: "backend/src/ai/structured-output-capabilities.ts"
      provides: "Provider/model/transport capability keying and strategy decision helpers"
    - path: "backend/src/ai/__tests__/structured-output-capabilities.test.ts"
      provides: "Mocked capability keying and strategy-label coverage"
    - path: ".planning/phases/73-structured-output-stability-and-provider-conformance/73-STRUCTURED-OUTPUT-INVENTORY.md"
      provides: "Classified production object-generation inventory"
  key_links:
    - from: "backend/src/ai/provider-registry.ts"
      to: "backend/src/ai/structured-output-capabilities.ts"
      via: "model metadata registration in createModel"
      pattern: "rememberStructuredOutputModelMetadata"
    - from: "backend/src/ai/__tests__/structured-output-boundary.test.ts"
      to: "73-STRUCTURED-OUTPUT-INVENTORY.md"
      via: "static inventory assertion"
      pattern: "STRUCTURED-OUTPUT-INVENTORY"
---

<objective>
Create the Phase 73 foundation: object-generation inventory, provider/model capability identity, and tests that prevent hidden structured-output seams from drifting outside `safeGenerateObject`.

Purpose: P73-R1 and P73-R3 require inventory and observable strategy identity before the CRITICAL `safeGenerateObject` boundary is changed.
Output: Capability helper module, inventory artifact, and static/unit tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-CONTEXT.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-PRD.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-RESEARCH.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-VALIDATION.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-PATTERNS.md
@backend/src/ai/provider-registry.ts
@backend/src/ai/__tests__/provider-registry.test.ts
@backend/src/ai/__tests__/structured-output-boundary.test.ts
</context>

<source_audit>
GOAL: Stable structured-output calls through native/provider-aware strategy, backend validation authority, and early conformance gates. Covered by plans 01-05.
REQ: P73-R1 -> plan 01; P73-R2 -> plan 02; P73-R3 -> plans 01,02,04; P73-R4 -> plan 03; P73-R5 -> plans 01,04; P73-R6 -> plans 02,03,05; P73-R7 -> plans 02,03,04,05.
RESEARCH: Capability resolver, inventory, AI SDK Output.object path, semantic ScenePlan, non-mutating conformance, and worldgen regressions are all planned.
CONTEXT: Native-first, explicit fallback, per provider/model/transport observability, backend validation authority, and deferred prose quality ranking are honored. No deferred idea is implemented.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inventory every production object-generation boundary</name>
  <read_first>
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-CONTEXT.md
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-PRD.md
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-VALIDATION.md
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-PATTERNS.md
    backend/src/ai/__tests__/structured-output-boundary.test.ts
    backend/src/ai/generate-object-safe.ts
  </read_first>
  <files>backend/src/ai/__tests__/structured-output-boundary.test.ts, .planning/phases/73-structured-output-stability-and-provider-conformance/73-STRUCTURED-OUTPUT-INVENTORY.md</files>
  <behavior>
    - Test 1: static scan fails if production code imports `generateObject` from `ai` outside `backend/src/ai/generate-object-safe.ts`.
    - Test 2: static scan fails if a production `safeGenerateObject` caller is absent from `73-STRUCTURED-OUTPUT-INVENTORY.md`.
    - Test 3: inventory rows classify each boundary as one of `native_schema`, `native_json`, `tool_mode`, `text_fallback`, or `unstructured_prose`.
  </behavior>
  <action>
    Run `npx gitnexus status`; if stale, run `npx gitnexus analyze`. For the existing test helper, run `gitnexus_impact({target: "collectSourceFiles", direction: "upstream"})`; if the helper is not indexed, record that in the summary and proceed.

    Expand `backend/src/ai/__tests__/structured-output-boundary.test.ts` so it scans `backend/src` excluding `__tests__` and checks the inventory artifact. Create `73-STRUCTURED-OUTPUT-INVENTORY.md` with a table containing at least: `file`, `line/anchor`, `caller`, `schema/domain`, `classification`, `required_strategy`, `fallback_allowed`, and `notes`.

    Inventory the current production seams from `rg -n "safeGenerateObject|generateText\\(|streamText\\(" backend/src`. Required classifications: all `safeGenerateObject` object calls default to `native_schema` with `text_fallback` allowed unless the row explicitly says `tool_mode`; direct prose/streaming calls such as `storyteller.ts`, `turn-processor.ts` final narration, `npc-agent.ts`, `reflection-agent.ts`, `world-engine.ts`, `archetype-researcher.ts`, `routes/ai.ts`, `ai/test-connection.ts`, and `scaffold-steps/premise-step.ts` direct `generateText` fallback prose are `unstructured_prose` or `text_fallback` as applicable. Do not mark final narration prose as structured output.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/ai/__tests__/structured-output-boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `Select-String -Path backend/src/ai/__tests__/structured-output-boundary.test.ts -Pattern "73-STRUCTURED-OUTPUT-INVENTORY|native_schema|text_fallback|unstructured_prose"` finds all terms.
    `Select-String -Path .planning/phases/73-structured-output-stability-and-provider-conformance/73-STRUCTURED-OUTPUT-INVENTORY.md -Pattern "backend/src/engine/scene-planner.ts|backend/src/worldgen/ip-researcher.ts|backend/src/ai/storyteller.ts|backend/src/engine/turn-processor.ts"` finds all terms.
    Targeted Vitest exits 0.
  </acceptance_criteria>
  <done>All production object-generation seams are inventoried and classified; the static boundary test protects inventory drift for P73-R1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add provider/model capability identity without caller churn</name>
  <read_first>
    backend/src/ai/provider-registry.ts
    backend/src/ai/__tests__/provider-registry.test.ts
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-RESEARCH.md
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-PATTERNS.md
    AGENTS.md
    .claude/skills/gitnexus/gitnexus-impact-analysis/SKILL.md
  </read_first>
  <files>backend/src/ai/provider-registry.ts, backend/src/ai/structured-output-capabilities.ts, backend/src/ai/__tests__/structured-output-capabilities.test.ts</files>
  <behavior>
    - Test 1: OpenAI-compatible model metadata contains provider id/name, model, normalized base URL family, protocol `openai-compatible`, and transport `chat-completions`.
    - Test 2: Anthropic-compatible model metadata contains protocol `anthropic-compatible` and transport `anthropic-messages`.
    - Test 3: capability resolver returns a decision with `primaryStrategy`, `fallbackStrategy: "text_fallback"`, `requestedMode`, `actualMode`, and `reason`.
    - Test 4: no API key or bearer token appears in metadata or capability keys.
  </behavior>
  <action>
    Run GitNexus impact before source edits: `gitnexus_impact({target: "createModel", direction: "upstream"})` and `gitnexus_impact({target: "resolveProviderProtocol", direction: "upstream"})`. If either returns HIGH/CRITICAL, stop and report unless the only risk is the already expected provider-model creation surface; otherwise proceed and record the blast radius.

    Create `backend/src/ai/structured-output-capabilities.ts`. Export exact string unions:
    `StructuredOutputPrimaryStrategy = "native_schema" | "native_json" | "tool_mode" | "text_fallback"`;
    `StructuredOutputTraceStrategy = StructuredOutputPrimaryStrategy | "repair" | "full_retry"`;
    `StructuredOutputRequestedMode = "auto" | "json" | "tool" | "native_schema" | "native_json" | "tool_mode" | "text_fallback"`.

    Export `StructuredOutputModelMetadata`, `StructuredOutputCapabilityDecision`, `rememberStructuredOutputModelMetadata(model, metadata)`, `getStructuredOutputModelMetadata(model)`, and `resolveStructuredOutputCapability(input)`. Use a `WeakMap<object, StructuredOutputModelMetadata>` for model identity. Never store `apiKey`, headers, prompt text, or raw settings. Capability keys must include provider id/name when available, model, protocol, normalized baseUrl family, and transport.

    Modify `createModel` in `provider-registry.ts` to call `rememberStructuredOutputModelMetadata` on returned models. For wrapped reasoning models, register the same metadata on the wrapped return value. Keep the public `createModel(config, options)` signature unchanged.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/ai/__tests__/provider-registry.test.ts src/ai/__tests__/structured-output-capabilities.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>
    `Select-String -Path backend/src/ai/structured-output-capabilities.ts -Pattern "StructuredOutputTraceStrategy|native_schema|native_json|tool_mode|text_fallback|WeakMap|apiKey"` proves types and keying exist while tests prove secrets are excluded.
    `Select-String -Path backend/src/ai/provider-registry.ts -Pattern "rememberStructuredOutputModelMetadata|transport"` finds provider metadata registration.
    Targeted tests and typecheck exit 0.
  </acceptance_criteria>
  <done>Provider/model/transport capability identity exists without breaking existing callers, enabling P73-R3 and P73-R5 observability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider settings -> capability metadata | Provider config contains secrets but logs/tests need non-secret identity. |
| source tree -> inventory artifact | Static inventory must reflect production source without trusting manual memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T73-01-01 | Information Disclosure | structured-output-capabilities.ts | mitigate | Store provider/model/protocol/transport only; tests assert `apiKey` and bearer strings never appear in metadata/report keys. |
| T73-01-02 | Tampering | 73-STRUCTURED-OUTPUT-INVENTORY.md | mitigate | Static test compares production callers against inventory so hidden object-generation seams cannot be omitted silently. |
| T73-01-03 | Denial of Service | capability resolver | mitigate | Resolver returns explicit fallback strategy instead of throwing for unknown providers. |
</threat_model>

<verification>
Run:
`npm --prefix backend run test -- src/ai/__tests__/structured-output-boundary.test.ts src/ai/__tests__/provider-registry.test.ts src/ai/__tests__/structured-output-capabilities.test.ts`
`npm --prefix backend run typecheck`
</verification>

<success_criteria>
P73-R1 inventory exists and is enforced by test. Provider/model/transport metadata and capability decisions are non-secret, typed, and test-covered for P73-R3/P73-R5.
</success_criteria>

<output>
After completion, create `.planning/phases/73-structured-output-stability-and-provider-conformance/73-01-SUMMARY.md`.
</output>
