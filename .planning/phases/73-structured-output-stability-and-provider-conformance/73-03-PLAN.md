---
phase: 73-structured-output-stability-and-provider-conformance
plan: 03
type: execute
wave: 2
depends_on: [73-01]
files_modified:
  - backend/src/engine/semantic-scene-plan-schema.ts
  - backend/src/engine/scene-plan-schema.ts
  - backend/src/engine/scene-planner.ts
  - backend/src/engine/__tests__/scene-planner.test.ts
  - backend/src/engine/__tests__/scene-plan-validator.test.ts
  - backend/src/engine/__tests__/turn-processor.scene-plan.test.ts
autonomous: true
requirements: [P73-R4, P73-R6, P73-R7]
must_haves:
  truths:
    - "ScenePlan model output no longer needs backend-generated event/action/response/narrator UUIDs."
    - "Backend maps semantic choices into strict `scenePlanSchema` before validation or execution."
    - "Payload alias, omitted repeated actor IDs, and missing tool selection are covered by tests."
  artifacts:
    - path: "backend/src/engine/semantic-scene-plan-schema.ts"
      provides: "Model-facing semantic ScenePlan schema and deterministic mapper"
    - path: "backend/src/engine/scene-planner.ts"
      provides: "Semantic ScenePlan generation path"
    - path: "backend/src/engine/__tests__/scene-planner.test.ts"
      provides: "Semantic mapping and observed ScenePlan failure regressions"
  key_links:
    - from: "backend/src/engine/scene-planner.ts"
      to: "backend/src/engine/semantic-scene-plan-schema.ts"
      via: "semanticScenePlanToStrictPlan"
      pattern: "semanticScenePlan"
    - from: "backend/src/engine/semantic-scene-plan-schema.ts"
      to: "backend/src/engine/scene-plan-schema.ts"
      via: "final strict parse"
      pattern: "scenePlanSchema.safeParse"
---

<objective>
Replace the model-facing strict ScenePlan object graph with a semantic ScenePlan contract while preserving strict backend `scenePlanSchema` as final authority.

Purpose: P73-R4 requires the LLM to choose semantic actor/tool intent, not fabricate backend-owned IDs and reference graphs.
Output: Semantic schema/mapper, `runScenePlanner` wiring, and regression tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-CONTEXT.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-PRD.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-RESEARCH.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-VALIDATION.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-PATTERNS.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-03-SUMMARY.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-04-SUMMARY.md
@backend/src/engine/scene-plan-schema.ts
@backend/src/engine/scene-planner.ts
@backend/src/engine/scene-plan-validator.ts
@backend/src/engine/tool-schemas.ts
@backend/src/engine/__tests__/scene-planner.test.ts
</context>

<source_audit>
GOAL: Stable structured-output calls and backend authority. This plan covers the highest-risk ScenePlan contract redesign.
REQ: P73-R4 implemented here; P73-R6 strict schema/tool validation remains final authority; P73-R7 ScenePlan payload/missing actor/tool regressions covered here.
RESEARCH: Follows semantic model-facing schema plus deterministic mapper recommendation; keeps strict `scenePlanSchema` and validator.
CONTEXT: Backend generates action IDs, event/response IDs, narrator references, and executable tool actions. No gameplay loop replacement.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define semantic ScenePlan schema and strict mapper</name>
  <read_first>
    backend/src/engine/scene-plan-schema.ts
    backend/src/engine/scene-plan-validator.ts
    backend/src/engine/tool-schemas.ts
    backend/src/engine/__tests__/scene-planner.test.ts
    backend/src/engine/__tests__/scene-plan-validator.test.ts
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-CONTEXT.md
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-PATTERNS.md
    AGENTS.md
  </read_first>
  <files>backend/src/engine/semantic-scene-plan-schema.ts, backend/src/engine/scene-plan-schema.ts, backend/src/engine/__tests__/scene-planner.test.ts, backend/src/engine/__tests__/scene-plan-validator.test.ts</files>
  <behavior>
    - Test 1: semantic output with actor labels/ids maps to strict `ScenePlan` with backend-generated UUIDs for anchor event, responses, planned actions, deferred hooks, and narrator facts.
    - Test 2: semantic action may omit repeated `actorId`; mapper derives it from `actionInterpretation.actorRef` or explicit action `actorRef`.
    - Test 3: semantic action may use `payload` as a compatibility alias for `input`, but strict returned plan always uses `input`.
    - Test 4: missing `toolName`/tool intent fails with a machine-readable semantic mapping issue before `executeToolCall` can run.
  </behavior>
  <action>
    Run GitNexus impact before source edits: `gitnexus_impact({target: "scenePlanSchema", direction: "upstream"})`, `gitnexus_impact({target: "sanitizeScenePlanCandidate", direction: "upstream"})`, and `gitnexus_impact({target: "validateScenePlan", direction: "upstream"})`. If schema helper symbols are not indexed, record target-not-found and proceed with source/test evidence.

    Create `backend/src/engine/semantic-scene-plan-schema.ts` with `semanticScenePlanSchema`, `SemanticScenePlan`, `SemanticScenePlanMappingError`, and `semanticScenePlanToStrictPlan(input, frame, opts?)`. Keep the model-facing fields semantic:
    `actionInterpretation.actorRef`, `intent`, `method?`, `targetRefs[]`;
    `primaryResponse.actorRef`, `responseKind`, `visibleToPlayer`, `targetRefs?`;
    `supportResponses[]` with same semantic refs;
    `plannedActions[]` with `actorRef?`, `toolName`, `input` and compatibility alias `payload`;
    `deferredHooks[]` with `hookType`, `subjectRefs[]`, `reason`;
    `hiddenRationale`.

    Do not ask the model for `id`, `eventId`, `actionId`, `responseId`, `anchorEventId`, `eventIds`, `responseIds`, `actionIds`, or `toolResultRefs`. Mapper owns those using `crypto.randomUUID()` by default and an injectable `idFactory` in tests. Actor refs resolve only against `SceneFrame` allowed active/support actor ids, `actorId`, and labels; background/forbidden actors remain rejected. Final return must pass `scenePlanSchema.safeParse` and the existing validator path.

    If `scene-plan-schema.ts` needs exports for shared constants/tool names, add narrow exports only. Do not weaken the strict `scenePlanSchema`.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/engine/__tests__/scene-planner.test.ts src/engine/__tests__/scene-plan-validator.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>
    `Select-String -Path backend/src/engine/semantic-scene-plan-schema.ts -Pattern "semanticScenePlanSchema|semanticScenePlanToStrictPlan|payload|crypto.randomUUID|scenePlanSchema.safeParse"` finds all terms.
    `Select-String -Path backend/src/engine/__tests__/scene-planner.test.ts -Pattern "backend-generated|payload|missing toolName|semantic"` finds corresponding tests.
    Targeted tests and typecheck exit 0.
  </acceptance_criteria>
  <done>Semantic ScenePlan mapping exists and strict backend ScenePlan remains final authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire runScenePlanner to semantic output and preserve rollback path regressions</name>
  <read_first>
    backend/src/engine/scene-planner.ts
    backend/src/engine/semantic-scene-plan-schema.ts
    backend/src/engine/scene-plan-schema.ts
    backend/src/engine/__tests__/scene-planner.test.ts
    backend/src/engine/__tests__/turn-processor.scene-plan.test.ts
    .planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-08-SUMMARY.md
  </read_first>
  <files>backend/src/engine/scene-planner.ts, backend/src/engine/__tests__/scene-planner.test.ts, backend/src/engine/__tests__/turn-processor.scene-plan.test.ts</files>
  <behavior>
    - Test 1: `runScenePlanner` calls `safeGenerateObject` with `semanticScenePlanSchema`, not `scenePlanLooseSchema`, for the normal first pass.
    - Test 2: prompt tells the model to output actor refs/semantic tool intent and explicitly says backend generates IDs and narrator references.
    - Test 3: repair call repairs semantic output once, then mapper/strict parse either succeeds or throws before mutation.
    - Test 4: turn processor ScenePlan regression still proves validate -> execute -> packet -> final narration ordering and rollback behavior.
  </behavior>
  <action>
    Run GitNexus impact before source edits: `gitnexus_impact({target: "runScenePlanner", direction: "upstream"})`, `gitnexus_impact({target: "buildDefaultScenePlannerPrompt", direction: "upstream"})`, and `gitnexus_impact({target: "buildDefaultScenePlannerSystem", direction: "upstream"})`.

    Update `runScenePlanner` to request `semanticScenePlanSchema` from `safeGenerateObject`. Replace prompt contract language that demands final strict object IDs with exact semantic language: "Return actorRef values from allowed actor ids or labels; return toolName from ALLOWED TOOLS; backend will generate event/action/response/narrator IDs; do not output id/eventId/actionId/responseId/narratorFacts reference arrays." Then call `semanticScenePlanToStrictPlan(result.object, args.frame)` and return only a strict `ScenePlan`.

    Keep one repair pass. The repair prompt must repair semantic object shape, not backend UUID graph shape. Existing loose sanitizer may remain exported for legacy tests, but normal `runScenePlanner` success must not rely on model-authored backend IDs.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/engine/__tests__/scene-planner.test.ts src/engine/__tests__/scene-plan-validator.test.ts src/engine/__tests__/turn-processor.scene-plan.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>
    `Select-String -Path backend/src/engine/scene-planner.ts -Pattern "semanticScenePlanSchema|semanticScenePlanToStrictPlan|backend will generate|actorRef"` finds all terms.
    `Select-String -Path backend/src/engine/scene-planner.ts -Pattern "schema: scenePlanLooseSchema"` returns no normal first-pass schema match; if retained, it is only in an explicitly named legacy helper/test path.
    Targeted tests and typecheck exit 0.
  </acceptance_criteria>
  <done>`runScenePlanner` consumes semantic model output and emits strict backend-owned ScenePlan objects without model-authored mechanical IDs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| judge model -> semantic ScenePlan | Model chooses semantic local intent but cannot own backend IDs or executable authority. |
| semantic mapper -> strict ScenePlan | Backend derives references and validates against strict Zod/tool schemas before mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T73-03-01 | Tampering | semantic-scene-plan-schema.ts | mitigate | Resolve actor refs only through allowed frame actors; reject background/forbidden/missing refs. |
| T73-03-02 | Elevation of Privilege | plannedActions mapper | mitigate | Validate mapped action inputs through strict `scenePlanSchema` and existing validator before execution. |
| T73-03-03 | Repudiation | runScenePlanner repair path | mitigate | Throw validation/mapping issues before mutation; tests prove failure path and rollback-order regressions. |
</threat_model>

<verification>
Run:
`npm --prefix backend run test -- src/engine/__tests__/scene-planner.test.ts src/engine/__tests__/scene-plan-validator.test.ts src/engine/__tests__/turn-processor.scene-plan.test.ts`
`npm --prefix backend run typecheck`
</verification>

<success_criteria>
P73-R4 is satisfied: model output is semantic, backend derives mechanical IDs/references/actions, strict schema and validators remain final authority, and observed ScenePlan failures are regression-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/73-structured-output-stability-and-provider-conformance/73-03-SUMMARY.md`.
</output>
