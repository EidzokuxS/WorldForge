---
phase: 73-structured-output-stability-and-provider-conformance
plan: 04
type: execute
wave: 3
depends_on: [73-02, 73-03]
files_modified:
  - backend/src/ai/structured-output-conformance.ts
  - backend/src/ai/__tests__/structured-output-conformance.test.ts
  - backend/src/scripts/structured-output-conformance.ts
  - backend/package.json
autonomous: true
requirements: [P73-R3, P73-R5, P73-R6, P73-R7]
must_haves:
  truths:
    - "Executor can run a mocked non-mutating provider conformance suite without live credentials."
    - "Live provider conformance is environment-gated and never mutates campaigns or executes runtime tools."
    - "Reports include provider/model/schema/mode/strategy/latency/usage/error/repair/semantic pass-fail fields."
  artifacts:
    - path: "backend/src/ai/structured-output-conformance.ts"
      provides: "Representative structured-output conformance cases and report runner"
    - path: "backend/src/ai/__tests__/structured-output-conformance.test.ts"
      provides: "Mocked conformance tests, no-secret assertions, non-mutation assertions"
    - path: "backend/src/scripts/structured-output-conformance.ts"
      provides: "Environment-gated live/local CLI harness"
  key_links:
    - from: "backend/src/ai/structured-output-conformance.ts"
      to: "backend/src/ai/generate-object-safe.ts"
      via: "safeGenerateObject"
      pattern: "safeGenerateObject"
    - from: "backend/src/scripts/structured-output-conformance.ts"
      to: "backend/package.json"
      via: "npm script"
      pattern: "structured-output:conformance"
---

<objective>
Build the provider conformance harness required before expensive long-running flows trust configured models.

Purpose: P73-R5 requires local provider/model/schema conformance reporting, with mocked unit tests mandatory and live providers opt-in.
Output: Non-mutating conformance module, CLI script, tests, and npm script.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-CONTEXT.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-PRD.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-RESEARCH.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-VALIDATION.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-PATTERNS.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-02-SUMMARY.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-03-SUMMARY.md
@backend/src/ai/test-connection.ts
@backend/src/ai/provider-registry.ts
@backend/src/ai/generate-object-safe.ts
@backend/src/engine/semantic-scene-plan-schema.ts
@backend/src/worldgen/research-artifact.ts
@backend/package.json
</context>

<source_audit>
GOAL: Provider conformance is checked before long flows trust structured output. This plan creates the harness.
REQ: P73-R5 implemented here; P73-R3 report fields verified here; P73-R6 non-mutating validator-only behavior preserved; P73-R7 representative failure cases included.
RESEARCH: Uses mocked Vitest first; live credentials are not assumed. Results are reported by provider/model/schema/mode/latency/usage/error/repair/semantic pass-fail.
CONTEXT: Harness is local, non-mutating, provider/model/transport keyed, and does not rank prose quality.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement non-mutating conformance cases and report runner</name>
  <read_first>
    backend/src/ai/generate-object-safe.ts
    backend/src/ai/structured-output-capabilities.ts
    backend/src/ai/test-connection.ts
    backend/src/ai/provider-registry.ts
    backend/src/engine/semantic-scene-plan-schema.ts
    backend/src/worldgen/research-artifact.ts
    backend/src/ai/__tests__/generate-object-safe.test.ts
    backend/src/ai/__tests__/provider-registry.test.ts
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-VALIDATION.md
  </read_first>
  <files>backend/src/ai/structured-output-conformance.ts, backend/src/ai/__tests__/structured-output-conformance.test.ts</files>
  <behavior>
    - Test 1: mocked provider run reports `providerId`, `providerName`, `protocol`, `model`, `schemaId`, `requestedMode`, `strategy`, `latencyMs`, `usage`, `errorType`, `repairUsed`, `semanticPass`, and `success`.
    - Test 2: representative cases include `generated_context_citations_canonicalNames`, `semantic_scene_plan_actions`, `external_metadata_caps`, `enum_tool_selection`, and `id_reference_mapping`.
    - Test 3: failed semantic validation reports `semanticPass: false` without mutating campaign state.
    - Test 4: source/import scan in the test fails if conformance module imports `getDb`, `executeToolCall`, campaign managers, or route handlers.
  </behavior>
  <action>
    Run GitNexus impact for the shared call boundary before source edits: `gitnexus_impact({target: "safeGenerateObject", direction: "upstream"})`. This plan may call the function but must not change it. If source edits require touching an existing function, run impact for that symbol first.

    Create `backend/src/ai/structured-output-conformance.ts`. Export `StructuredOutputConformanceCase`, `StructuredOutputConformanceResult`, `StructuredOutputConformanceReport`, `buildDefaultStructuredOutputConformanceCases()`, and `runStructuredOutputConformance(input)`.

    Use representative schemas/prompts only. Do not import database, campaign, route, or runtime tool executor modules. Cases must cover:
    `generated_context_citations_canonicalNames` for Kimi/Mimo string-to-object drift;
    `semantic_scene_plan_actions` for ScenePlan semantic actions and payload/input shape;
    `external_metadata_caps` for overlong metadata as data/cap validation;
    `enum_tool_selection` for allowed tool names;
    `id_reference_mapping` for backend-derived IDs/references.

    Each case should call `safeGenerateObject` with a schema and then run a `semanticCheck(object)` function. Result rows must include strategy/repair fields from trace and compact error type/message slices only. Do not log raw prompts, hidden reasoning, API keys, or full provider settings.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/ai/__tests__/structured-output-conformance.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>
    `Select-String -Path backend/src/ai/structured-output-conformance.ts -Pattern "generated_context_citations_canonicalNames|semantic_scene_plan_actions|external_metadata_caps|enum_tool_selection|id_reference_mapping|semanticPass|repairUsed"` finds all terms.
    `Select-String -Path backend/src/ai/__tests__/structured-output-conformance.test.ts -Pattern "getDb|executeToolCall|apiKey|semanticPass"` proves non-mutation/no-secret tests exist.
    Targeted test and typecheck exit 0.
  </acceptance_criteria>
  <done>Mocked conformance harness exists and proves report shape plus non-mutating behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add environment-gated CLI harness for configured providers</name>
  <read_first>
    backend/src/ai/structured-output-conformance.ts
    backend/src/ai/provider-registry.ts
    backend/src/settings/manager.ts
    backend/package.json
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-CONTEXT.md
    .planning/phases/73-structured-output-stability-and-provider-conformance/73-VALIDATION.md
  </read_first>
  <files>backend/src/scripts/structured-output-conformance.ts, backend/package.json, backend/src/ai/__tests__/structured-output-conformance.test.ts</files>
  <behavior>
    - Test 1: CLI exits 0 with `{ skipped: true }` when `WORLDFORGE_LIVE_PROVIDER_CONFORMANCE` is not `1`.
    - Test 2: CLI reads configured providers only in live mode and strips/masks `apiKey` from output.
    - Test 3: npm script `structured-output:conformance` exists and uses `tsx src/scripts/structured-output-conformance.ts`.
  </behavior>
  <action>
    Add `backend/src/scripts/structured-output-conformance.ts`. It must be live-provider gated: unless `process.env.WORLDFORGE_LIVE_PROVIDER_CONFORMANCE === "1"`, print a compact skipped JSON report and exit 0. In live mode, read existing provider settings without creating or mutating `settings.json`; if using `settings/manager.ts` would write defaults, do not use it. Prefer `node:fs` read-only JSON parsing of existing `settings.json` and mask secrets.

    Add backend npm script `"structured-output:conformance": "tsx src/scripts/structured-output-conformance.ts"`. The CLI should accept no campaign id and must not touch `campaigns/`, SQLite, vectors, route handlers, or tool executors.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/ai/__tests__/structured-output-conformance.test.ts</automated>
    <automated>npm --prefix backend run structured-output:conformance</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>
    `Select-String -Path backend/src/scripts/structured-output-conformance.ts -Pattern "WORLDFORGE_LIVE_PROVIDER_CONFORMANCE|skipped|apiKey|readFileSync"` finds all terms.
    `Select-String -Path backend/package.json -Pattern "structured-output:conformance"` finds the script.
    Script without live env exits 0 and reports skipped; targeted tests/typecheck exit 0.
  </acceptance_criteria>
  <done>Provider conformance harness is available locally, mocked by default, and live-provider safe by environment gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configured providers -> conformance harness | Optional live providers may be external and credentialed. |
| conformance report -> logs/stdout | Reports must be diagnostic without secret or prompt leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T73-04-01 | Denial of Service | live conformance script | mitigate | Live calls are opt-in via `WORLDFORGE_LIVE_PROVIDER_CONFORMANCE=1`; default script exits skipped. |
| T73-04-02 | Information Disclosure | conformance reports | mitigate | Tests assert API keys/prompts are not included; CLI masks settings. |
| T73-04-03 | Tampering | conformance cases | mitigate | Module imports no DB/campaign/tool executor code and tests scan for forbidden imports. |
</threat_model>

<verification>
Run:
`npm --prefix backend run test -- src/ai/__tests__/structured-output-conformance.test.ts`
`npm --prefix backend run structured-output:conformance`
`npm --prefix backend run typecheck`
</verification>

<success_criteria>
P73-R5 is satisfied by a mocked, non-mutating conformance harness and opt-in live script that reports provider/model/schema/mode behavior without campaign mutation.
</success_criteria>

<output>
After completion, create `.planning/phases/73-structured-output-stability-and-provider-conformance/73-04-SUMMARY.md`.
</output>
