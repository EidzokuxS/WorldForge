---
phase: 74-structured-prompt-contracts-and-model-facing-schema-hardenin
plan: 12
type: execute
wave: 1
depends_on:
  - "74-02"
  - "74-10"
files_modified:
  - backend/src/engine/prompt-contracts.ts
  - backend/src/engine/__tests__/scene-planner.test.ts
autonomous: true
gap_closure: true
requirements:
  - P74-R2
  - P74-R3
  - P74-R4
  - P74-R5
must_haves:
  truths:
    - "Runtime tool contract examples respect selectedToolNames."
    - "A log_event-only contract does not show offer_quick_actions as a valid or invalid example."
    - "ScenePlanner and hidden adjudication still get exact runtime tool input shapes before repair."
  artifacts:
    - path: "backend/src/engine/prompt-contracts.ts"
      provides: "Selected-tool-specific runtime tool examples"
      exports:
        - "buildRuntimeToolInputContract"
    - path: "backend/src/engine/__tests__/scene-planner.test.ts"
      provides: "Regression coverage for selected runtime tool examples"
      contains: "buildRuntimeToolInputContract"
  key_links:
    - from: "backend/src/engine/prompt-contracts.ts"
      to: "backend/src/engine/tool-schemas.ts"
      via: "runtimeToolInputSchemas-derived selected tool names"
      pattern: "runtimeToolInputSchemas"
    - from: "backend/src/engine/scene-planner.ts"
      to: "backend/src/engine/prompt-contracts.ts"
      via: "buildScenePlannerPromptContract passes frame.allowedTools"
      pattern: "buildScenePlannerPromptContract"
---

<objective>
Close the verifier gap where filtered runtime tool contracts still show an out-of-scope `offer_quick_actions` example.

Purpose: Prompt clarity comes first. The model-facing contract must demonstrate only tools selected for the current call, while backend validation remains deterministic and repair stays bounded by Phase 74 policy.
Output: Selected-tool-specific runtime tool examples and tests that fail on the current hardcoded `offer_quick_actions` example.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-VERIFICATION.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REVIEW.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REPAIR-POLICY.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-02-SUMMARY.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-10-SUMMARY.md
@backend/src/engine/prompt-contracts.ts
@backend/src/engine/__tests__/scene-planner.test.ts

<interfaces>
From `backend/src/engine/prompt-contracts.ts`:
```typescript
export function buildRuntimeToolInputContract(options?: {
  toolNames?: readonly RuntimeToolName[];
}): string;
export function buildScenePlannerPromptContract(options?: {
  allowedTools?: readonly RuntimeToolName[];
}): string;
export function buildHiddenAdjudicationPromptContract(options?: {
  allowedTools?: readonly RuntimeToolName[];
}): string;
```

GitNexus pre-plan impact already found `buildRuntimeToolInputContract` risk LOW: direct callers are `buildScenePlannerPromptContract` and `buildHiddenAdjudicationPromptContract`; affected process is `runScenePlanner`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make runtime tool examples selected-tool-specific</name>
  <files>backend/src/engine/prompt-contracts.ts, backend/src/engine/__tests__/scene-planner.test.ts</files>
  <behavior>
    - Test 1: `buildRuntimeToolInputContract({ toolNames: ["log_event"] })` contains the `log_event` input shape and no `offer_quick_actions` valid or invalid example text.
    - Test 2: `buildRuntimeToolInputContract({ toolNames: ["offer_quick_actions"] })` contains the quick-action shape and missing `actions[].action` invalid example.
    - Test 3: `buildRuntimeToolInputContract({ toolNames: [] })` says no runtime tools are allowed and contains no specific tool examples.
  </behavior>
  <action>
    Before editing, run `gitnexus_impact({ repo: "WorldForge", target: "buildRuntimeToolInputContract", direction: "upstream", maxDepth: 2, includeTests: true })` and record the blast radius in the summary. If risk is HIGH or CRITICAL, stop and report before editing.

    Add the tests first; they must fail against the current code because it hardcodes `offer_quick_actions` examples for a `log_event`-only contract. Then replace the hardcoded compact/invalid tool examples in `buildRuntimeToolInputContract()` with selected-tool-specific examples derived from `selectedToolNames`. Do not add a generic Zod-to-prompt generator. Keep the existing human-authored tool shape snippets and backend authority text. Move caller-level no-action examples out of the runtime-tool helper if needed; ScenePlanner and hidden adjudication already own their own minimal top-level outputs.

    Before each commit, run `gitnexus_detect_changes({ repo: "WorldForge", scope: "staged" })` and confirm only the expected engine prompt-contract/test scope is affected.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/engine/__tests__/scene-planner.test.ts src/engine/__tests__/hidden-adjudication.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <done>`log_event`-only runtime contracts no longer mention `offer_quick_actions`; quick-action contracts still show quick-action examples; all engine prompt contract tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM prompt contract -> model output | Model uses contract examples to choose runtime tool names and nested input shape. |
| Model output -> backend runtime validators | Untrusted structured output crosses into deterministic runtime tool validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-12-01 | Tampering | `buildRuntimeToolInputContract` | mitigate | Show only selected tool examples so prompt text cannot encourage unsupported tool names. |
| T-74-12-02 | Denial of Service | `runScenePlanner` repair loop | mitigate | Regression-lock log_event-only contract to avoid out-of-scope quick-action examples that trigger semantic mapping failure. |
| T-74-12-03 | Information Disclosure | prompt examples | accept | Examples contain synthetic non-secret labels only and no campaign data. |
</threat_model>

<verification>
Run focused engine prompt tests and backend typecheck. Confirm GitNexus staged detect_changes matches only `backend/src/engine/prompt-contracts.ts` and `backend/src/engine/__tests__/scene-planner.test.ts`.
</verification>

<success_criteria>
- `buildRuntimeToolInputContract({ toolNames: ["log_event"] })` does not contain `offer_quick_actions`.
- `buildRuntimeToolInputContract({ toolNames: ["offer_quick_actions"] })` still documents `actions[].action`.
- No runtime schema, semantic mapper, or executor behavior is weakened.
</success_criteria>

<output>
After completion, create `.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-12-SUMMARY.md`.
</output>
