---
phase: 74-structured-prompt-contracts-and-model-facing-schema-hardenin
plan: 03
type: execute
wave: 3
depends_on:
  - "74-02"
files_modified:
  - backend/src/engine/prompt-contracts.ts
  - backend/src/engine/world-brain.ts
  - backend/src/engine/oracle.ts
  - backend/src/engine/target-context.ts
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/__tests__/world-brain.test.ts
  - backend/src/engine/__tests__/oracle.test.ts
  - backend/src/engine/__tests__/target-context.test.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
autonomous: true
requirements:
  - P74-R2
  - P74-R3
  - P74-R4
  - P74-R5
must_haves:
  truths:
    - "World-brain, oracle, target, and movement model calls receive exact field, enum, nullability, and cap contracts."
    - "Gameplay classifier prompts distinguish model proposal from backend authority."
    - "Regression tests fail if compact structured-output contracts disappear from these P0 gameplay prompts."
  artifacts:
    - path: "backend/src/engine/prompt-contracts.ts"
      provides: "Additional P0 gameplay prompt-contract helpers"
      exports:
        - "buildWorldBrainPromptContract"
        - "buildOraclePromptContract"
        - "buildTargetContextPromptContract"
        - "buildMovementDetectionPromptContract"
    - path: "backend/src/engine/world-brain.ts"
      provides: "World-brain prompt contract"
      contains: "STRUCTURED_OUTPUT_CONTRACT: world-brain.v1"
    - path: "backend/src/engine/oracle.ts"
      provides: "Oracle prompt contract"
      contains: "STRUCTURED_OUTPUT_CONTRACT: oracle.v1"
    - path: "backend/src/engine/target-context.ts"
      provides: "Target classifier prompt contract"
      contains: "STRUCTURED_OUTPUT_CONTRACT: target-context.v1"
    - path: "backend/src/engine/turn-processor.ts"
      provides: "Movement classifier prompt contract"
      contains: "STRUCTURED_OUTPUT_CONTRACT: movement-detection.v1"
  key_links:
    - from: "backend/src/engine/world-brain.ts"
      to: "backend/src/engine/prompt-contracts.ts"
      via: "world-brain contract helper"
      pattern: "buildWorldBrainPromptContract"
    - from: "backend/src/engine/oracle.ts"
      to: "backend/src/engine/prompt-contracts.ts"
      via: "oracle contract helper"
      pattern: "buildOraclePromptContract"
    - from: "backend/src/engine/turn-processor.ts"
      to: "backend/src/ai/generate-object-safe.ts"
      via: "safeGenerateObject alias remains guarded"
      pattern: "safeGenerateObject"
---

<objective>
Complete P0 gameplay prompt contracts for world-brain and lightweight gameplay classifiers.

Purpose: P0 gameplay calls beyond ScenePlanner still need exact model-facing contracts, especially caps, enum/null rules, and authority boundaries.
Output: Engine contract helpers applied to world-brain, oracle, target context, and movement detection with prompt regression tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-STRUCTURED-PROMPT-AUDIT.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-CONTEXT.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-PATTERNS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REVIEWS.md
@backend/src/engine/prompt-contracts.ts
@backend/src/engine/world-brain.ts
@backend/src/engine/oracle.ts
@backend/src/engine/target-context.ts
@backend/src/engine/turn-processor.ts
@backend/src/engine/__tests__/world-brain.test.ts
@backend/src/engine/__tests__/oracle.test.ts
@backend/src/engine/__tests__/target-context.test.ts
@backend/src/engine/__tests__/turn-processor.test.ts
</context>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Exact schema/tool shape before repair | Adds compact contracts to remaining P0 gameplay model calls |
| REQ | P74-R2 | Field lists, caps, enum values, null rules, and examples are model-facing |
| REQ | P74-R3 | Reuses engine contract helper from 74-02 rather than per-prompt drift |
| REQ | P74-R4 | Adds prompt regression tests for marker and field/cap content |
| REQ | P74-R5 | Contracts say model proposes only from provided scene/state and backend validation decides |
| CONTEXT | "world-brain/oracle/target/movement" | Directly covers these P0 gameplay seams |
| RESEARCH | Validation Map 74-02 P0 gameplay | Completes P0 gameplay after nested runtime tool plan |
| PATTERNS | `buildWorldBrainPrompt` lacks exact field list/caps/example | Adds exact prompt contract to that gap |
| REVIEWS | HIGH marker tests prove markers, not contracts | Tests must assert examples, caps, nullability, invalid cases, and minimal outputs for these classifiers |
| REVIEWS | MEDIUM overlong rationale class | Oracle contract/test must include a reasoning cap and invalid overlong rationale example |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add remaining P0 gameplay contract helpers</name>
  <read_first>
    @backend/src/engine/prompt-contracts.ts
    @backend/src/engine/world-brain.ts
    @backend/src/engine/oracle.ts
    @backend/src/engine/target-context.ts
    @backend/src/engine/turn-processor.ts
  </read_first>
  <files>backend/src/engine/prompt-contracts.ts</files>
  <behavior>
    - Helper exports include world-brain, oracle, target-context, and movement-detection contracts.
    - Each helper returns a compact marker block with exact output shape, one small example, one minimal valid output, and one invalid/anti-pattern example.
    - Contracts include optional/null behavior and string/list caps where schemas define them.
  </behavior>
  <action>
    Before editing symbols, run GitNexus impact analysis for `buildRuntimeToolInputContract` and the new helper names after selecting names. If adding exports changes upstream imports, report any HIGH or CRITICAL risk.

    Extend `backend/src/engine/prompt-contracts.ts` from 74-02. Add pure string helpers with markers: `STRUCTURED_OUTPUT_CONTRACT: world-brain.v1`, `oracle.v1`, `target-context.v1`, and `movement-detection.v1`. Keep helpers data-free and deterministic. Do not move schema validation into prompt helper code.

    Addresses review concern: marker-only tests. Helper tests must assert required field names, caps, nullable/optional rules, compact valid examples, minimal valid outputs, and invalid examples for each helper.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- backend/src/engine/__tests__/world-brain.test.ts backend/src/engine/__tests__/oracle.test.ts backend/src/engine/__tests__/target-context.test.ts backend/src/engine/__tests__/turn-processor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Contract helpers exist and tests assert semantic adequacy strings without calling a provider.
  </acceptance_criteria>
  <done>Remaining P0 gameplay contract helper exports are available for prompt builders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden world-brain prompt contract</name>
  <read_first>
    @backend/src/engine/world-brain.ts
    @backend/src/engine/prompt-contracts.ts
    @backend/src/engine/__tests__/world-brain.test.ts
  </read_first>
  <files>backend/src/engine/world-brain.ts, backend/src/engine/__tests__/world-brain.test.ts</files>
  <behavior>
    - World-brain prompt includes `STRUCTURED_OUTPUT_CONTRACT: world-brain.v1`.
    - Prompt lists exact fields, caps, and enum/null behavior from the existing schema.
    - Prompt includes compact valid and invalid examples and says backend may trim/cap/drop invalid optional output but must not invent semantic facts.
  </behavior>
  <action>
    Before editing symbols, run GitNexus impact analysis for `buildWorldBrainPrompt`, `runWorldBrainSceneDirection`, and any exported world-brain schema constants touched.

    Insert the world-brain contract helper into the model-facing prompt before scene/world data. Preserve existing schema caps and repair prompt behavior. Do not loosen validation to fit prompts and do not add backend fallback that fabricates scene direction, oracle meaning, source roles, or canonical truth.

    Tests must prove more than marker presence: required world-brain fields, list/string caps, nullable/empty-list behavior, minimal valid output, and an invalid overlong/missing-field example.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- backend/src/engine/__tests__/world-brain.test.ts</automated>
  </verify>
  <acceptance_criteria>
    World-brain tests assert marker, required fields, caps, nullable/empty behavior, compact valid example, minimal valid output, invalid example, and final-validator language.
  </acceptance_criteria>
  <done>World-brain model call has an explicit schema contract before repair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden oracle, target, and movement classifier contracts</name>
  <read_first>
    @backend/src/engine/oracle.ts
    @backend/src/engine/target-context.ts
    @backend/src/engine/turn-processor.ts
    @backend/src/engine/prompt-contracts.ts
    @backend/src/engine/__tests__/oracle.test.ts
    @backend/src/engine/__tests__/target-context.test.ts
    @backend/src/engine/__tests__/turn-processor.test.ts
  </read_first>
  <files>backend/src/engine/oracle.ts, backend/src/engine/target-context.ts, backend/src/engine/turn-processor.ts, backend/src/engine/__tests__/oracle.test.ts, backend/src/engine/__tests__/target-context.test.ts, backend/src/engine/__tests__/turn-processor.test.ts</files>
  <behavior>
    - Oracle prompt asks for `{ chance: integer 1-99, reasoning: string }` and states the reasoning string cap.
    - Target prompt asks for nullable targetName and allowed targetType enum values.
    - Movement prompt asks for `{ isMovement: boolean, destination: string | null }`.
    - Tests assert markers and no invented target/destination fallback when model output is absent or invalid.
  </behavior>
  <action>
    Before editing symbols, run GitNexus impact analysis for `callOracle`, `executeOracleCall`, `detectCandidateByClassifier`, `detectActionTargetCandidate`, and `detectMovement`.

    Apply the new helper blocks to oracle, target context, and movement detection prompts. Keep examples minimal and shape-focused. Preserve existing validators and error handling. The backend may normalize strings and reject invalid classifier output, but must not invent targets, destinations, chance meaning, or movement intent.

    Addresses review concern: overlong rationale. Oracle tests must assert the prompt includes the exact reasoning cap and an invalid overlong-rationale anti-pattern. Target/movement tests must assert nullable rules and no-invention fail-closed language.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- backend/src/engine/__tests__/oracle.test.ts backend/src/engine/__tests__/target-context.test.ts backend/src/engine/__tests__/turn-processor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Classifier tests prove model-facing contracts include exact shapes, caps, nullability, minimal valid outputs, invalid examples, and existing invalid-output behavior remains deterministic.
  </acceptance_criteria>
  <done>Oracle, target, and movement structured prompts ask for exact shapes before validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model -> world-brain schema | Untrusted direction candidate crosses into deterministic validation |
| model -> classifier schemas | Untrusted chance, target, or movement classifier output crosses into gameplay decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T74-03-01 | Tampering | world-brain structured output | mitigate | Prompt lists exact fields/caps and schema remains final authority |
| T74-03-02 | Spoofing | target/movement classifier | mitigate | Nullable contracts and tests prevent backend from inventing missing targets or destinations |
| T74-03-03 | Repudiation | oracle reasoning | mitigate | Oracle contract requires bounded reasoning and existing logs/trace keep deterministic errors |
| T74-03-04 | Denial of Service | overlong strings/lists | mitigate | Prompt caps mirror schema caps and validators still reject or trim according to existing policy |
</threat_model>

<verification>
Run targeted P0 gameplay tests:

```bash
npm --prefix backend run test -- backend/src/engine/__tests__/world-brain.test.ts backend/src/engine/__tests__/oracle.test.ts backend/src/engine/__tests__/target-context.test.ts backend/src/engine/__tests__/turn-processor.test.ts
```
</verification>

<success_criteria>
P74-R2 through P74-R5 are satisfied for remaining P0 gameplay calls when each prompt includes a marker, exact shape/cap/nullability contract, and backend authority language with regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-03-SUMMARY.md`.
</output>
