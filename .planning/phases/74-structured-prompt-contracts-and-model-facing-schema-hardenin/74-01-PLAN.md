---
phase: 74-structured-prompt-contracts-and-model-facing-schema-hardenin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-STRUCTURED-PROMPT-AUDIT.md
  - backend/src/ai/__tests__/structured-prompt-contract-audit.test.ts
autonomous: true
requirements:
  - P74-R1
must_haves:
  truths:
    - "Every production structured-output model seam is listed in the Phase 74 prompt-contract audit before prompt code is changed."
    - "Static tests fail when a P0 or P1 structured-output seam is missing from the audit."
    - "The audit records which later plan owns each contract and which deterministic schema remains authoritative."
  artifacts:
    - path: ".planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-STRUCTURED-PROMPT-AUDIT.md"
      provides: "Locked prompt-contract coverage inventory"
      contains: "Plan owner"
    - path: "backend/src/ai/__tests__/structured-prompt-contract-audit.test.ts"
      provides: "Static coverage regression for the prompt-contract audit"
      exports: []
  key_links:
    - from: "backend/src/ai/__tests__/structured-prompt-contract-audit.test.ts"
      to: ".planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-STRUCTURED-PROMPT-AUDIT.md"
      via: "filesystem read"
      pattern: "74-STRUCTURED-PROMPT-AUDIT"
---

<objective>
Create the locked Phase 74 audit and static guardrails that implementation plans use as the source of truth.

Purpose: Prevent Phase 74 from fixing only the obvious stack frame. The audit must enumerate all structured-output calls and assign each to an implementation plan before prompts are changed.
Output: Updated audit table plus a targeted static test that proves coverage remains explicit.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-CONTEXT.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-STRUCTURED-PROMPT-AUDIT.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-RESEARCH.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-PATTERNS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REVIEWS.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-STRUCTURED-OUTPUT-INVENTORY.md
@backend/src/ai/__tests__/structured-output-boundary.test.ts
</context>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Audit every structured-output model call and make prompt contracts explicit before repair | Creates the locked audit input used by all later plans |
| REQ | P74-R1 | Refreshes and locks prompt-contract audit plus static coverage |
| CONTEXT | "This is a class problem, not one stack frame" | Audit must include all production safeGenerateObject callers and JSON/tool-like generateText seams |
| CONTEXT | "Use Phase 73 inventory but refresh into prompt-contract audit" | Starts from 73-STRUCTURED-OUTPUT-INVENTORY and records Phase 74 plan owners |
| REVIEWS | HIGH coverage gaps remain | Adds source-level ownership lock for seed-suggester, lore-extractor, starting-location, premise-divergence, premise-step, validation, npc-offscreen, and prompt-assembler.compressContext |
| REVIEWS | HIGH marker tests prove markers, not semantic adequacy | Adds audit fields requiring exact semantic-contract checks, not marker-only tests |
| RESEARCH | Validation Map 74-01 | Implements audit/static coverage first as Wave 1 foundation |
| PATTERNS | Static boundary test pattern | Adds a dedicated audit coverage test beside existing boundary tests |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Refresh and lock the prompt-contract audit</name>
  <read_first>
    @.planning/phases/73-structured-output-stability-and-provider-conformance/73-STRUCTURED-OUTPUT-INVENTORY.md
    @.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-CONTEXT.md
    @.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-RESEARCH.md
    @.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-PATTERNS.md
  </read_first>
  <files>.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-STRUCTURED-PROMPT-AUDIT.md</files>
  <behavior>
    - P0/P1/P2 rows cover every production structured output listed in Phase 73 inventory.
    - Each row records: priority, source file, call or prompt builder, schema/tool source, missing model-facing contract, required contract marker, deterministic authority, failure class, Phase 74 plan owner, and semantic adequacy test owner.
    - Source-level lock covers `seed-suggester.ts`, `lore-extractor.ts`, `starting-location.ts`, `premise-divergence.ts`, `scaffold-steps/premise-step.ts`, `scaffold-steps/validation.ts`, `npc-offscreen.ts`, and `prompt-assembler.ts` `compressContext`.
    - Audit distinguishes required P0/P1 source failures from P2 seams and records whether each marker is versioned.
    - Direct prose generation is excluded unless the model is asked for JSON, enum selection, or tool-like output.
  </behavior>
  <action>
    Refresh the audit table into a locked implementation checklist. Include all production safeGenerateObject callers, aliases such as safeGenerateObject as generateObject, and JSON/tool-like generateText seams. Do not add implementation tasks for deferred or out-of-phase work. Record plan ownership using 74-02 through 74-11 so later executors can prove full coverage without rediscovering scope.

    Addresses review concern: coverage gaps remain. Add a source-level ownership section that lists every required source file and the exact plan/marker/test owner. The section must include the eight review-named files and must treat `prompt-assembler.ts` ownership as `compressContext`, not the final storyteller prompt path.

    Addresses review concern: marker tests are insufficient. Add an explicit semantic adequacy checklist for non-trivial contracts: required fields, nested object/array shapes, caps, nullable/optional rules, compact valid example, minimal valid output, and invalid/anti-pattern examples.
  </action>
  <verify>
    <automated>rg -n "scene-planner|hidden-adjudication|world-brain|oracle|target-context|turn-processor|ip-researcher|generatedContext|known-ip-worldgen-research|worldbook|Plan owner" .planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-STRUCTURED-PROMPT-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    The audit includes P0 gameplay, P0 worldgen research artifact, P1 worldgen scaffold/regeneration, P1 character/power, P1 background simulation/compression, and P2 worldbook/import/script seams with explicit plan owners, including `backend/src/scripts/backfill-personality.ts`. `rg -n "seed-suggester|lore-extractor|starting-location|premise-divergence|premise-step|validation.ts|npc-offscreen|prompt-assembler.ts" 74-STRUCTURED-PROMPT-AUDIT.md` finds each file with a plan owner.
  </acceptance_criteria>
  <done>Audit is a complete Phase 74 coverage source and no row asks the backend to invent semantic content.</done>
</task>

<task type="auto">
  <name>Task 2: Add static audit coverage regression</name>
  <read_first>
    @backend/src/ai/__tests__/structured-output-boundary.test.ts
    @.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-STRUCTURED-PROMPT-AUDIT.md
  </read_first>
  <files>backend/src/ai/__tests__/structured-prompt-contract-audit.test.ts</files>
  <behavior>
    - Test fails if a required P0/P1 structured-output file is missing from the Phase 74 audit.
    - Test fails if a required audit row lacks a Phase 74 plan owner, versioned contract marker where applicable, semantic adequacy checklist, or test owner.
    - Test fails if required source files only appear in a broad glob row and not as concrete source-level ownership rows.
    - Test does not inspect raw prompts, provider secrets, live APIs, or runtime databases.
  </behavior>
  <action>
    Create a Vitest static test that reads the Phase 74 audit markdown and checks a curated required-file list from the Phase 73 inventory. Include P0 files: scene-planner.ts, hidden-adjudication.ts, world-brain.ts, oracle.ts, target-context.ts, turn-processor.ts, ip-researcher.ts, and research-artifact.ts. Include P1 breadth files for scaffold, character, known IP power, ingestion, NPC generation, seed-suggester, lore-extractor, starting-location, premise-divergence, premise-step, scaffold validation, npc-offscreen, and prompt-assembler compression. Include P2 worldbook composition/import and backfill-personality as explicitly owned seams. Keep the test deterministic and filesystem-only.

    The test must not be marker-only: for each non-trivial row, assert the audit row contains a marker plus semantic-check labels for required fields, caps, nullability, compact valid example, minimal valid output, and invalid example.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- backend/src/ai/__tests__/structured-prompt-contract-audit.test.ts backend/src/ai/__tests__/structured-output-boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    The new test proves the Phase 74 audit covers required files, source-level ownership metadata, versioned marker metadata, and semantic adequacy test metadata. Existing boundary tests still pass.
  </acceptance_criteria>
  <done>Static audit coverage is enforced by a targeted Vitest test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs -> implementation plans | Audit data drives later code edits and must not silently omit structured-output seams |
| repository filesystem -> static test | Test reads local files only; no model/provider input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T74-01-01 | Tampering | 74-STRUCTURED-PROMPT-AUDIT.md | mitigate | Static test requires required P0/P1 files, plan owners, and contract markers |
| T74-01-02 | Information Disclosure | structured-prompt-contract-audit.test.ts | mitigate | Test must not read .env, provider logs, raw model prompts, or live API output |
| T74-01-03 | Repudiation | Phase 74 coverage | mitigate | Audit rows include source file and plan owner for traceable closeout |
</threat_model>

<verification>
Run the targeted static tests:

```bash
npm --prefix backend run test -- backend/src/ai/__tests__/structured-prompt-contract-audit.test.ts backend/src/ai/__tests__/structured-output-boundary.test.ts
```
</verification>

<success_criteria>
P74-R1 is complete when the audit is the locked source for Phase 74 implementation and static coverage fails on missing high-priority structured-output seams.
</success_criteria>

<output>
After completion, create `.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-01-SUMMARY.md`.
</output>
