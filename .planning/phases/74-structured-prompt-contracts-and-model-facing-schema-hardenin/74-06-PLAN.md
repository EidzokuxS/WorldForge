---
phase: 74-structured-prompt-contracts-and-model-facing-schema-hardenin
plan: 06
type: execute
wave: 2
depends_on:
  - "74-01"
files_modified:
  - backend/src/worldbook-library/composition.ts
  - backend/src/worldgen/worldbook-importer.ts
  - backend/src/scripts/backfill-personality.ts
  - backend/src/worldgen/__tests__/worldbook-composition.test.ts
  - backend/src/worldgen/__tests__/worldbook-importer.test.ts
  - backend/src/scripts/__tests__/backfill-personality.test.ts
autonomous: true
requirements:
  - P74-R2
  - P74-R3
  - P74-R4
  - P74-R5
must_haves:
  truths:
    - "Worldbook composition and import structured prompts include exact output contracts."
    - "The supported backfill-personality script seam includes an explicit model-facing personality contract instead of remaining implicit."
    - "P2 seams are either marker-tested in this plan or explicitly documented as non-production exclusions; `backfill-personality.ts` is included, not excluded."
  artifacts:
    - path: "backend/src/worldbook-library/composition.ts"
      provides: "Worldbook composition prompt contracts"
      contains: "STRUCTURED_OUTPUT_CONTRACT: worldbook-composition.v1"
    - path: "backend/src/worldgen/worldbook-importer.ts"
      provides: "Worldbook import prompt contracts"
      contains: "STRUCTURED_OUTPUT_CONTRACT: worldbook-import.v1"
    - path: "backend/src/scripts/backfill-personality.ts"
      provides: "Operational script prompt contract"
      contains: "STRUCTURED_OUTPUT_CONTRACT: backfill-personality.v1"
  key_links:
    - from: "backend/src/worldbook-library/composition.ts"
      to: "backend/src/worldgen/__tests__/worldbook-composition.test.ts"
      via: "worldbook shape regression tests"
      pattern: "primarySource"
    - from: "backend/src/worldgen/worldbook-importer.ts"
      to: "backend/src/worldgen/__tests__/worldbook-importer.test.ts"
      via: "classification prompt assertions"
      pattern: "buildClassificationPrompt"
    - from: "backend/src/scripts/backfill-personality.ts"
      to: "backend/src/scripts/__tests__/backfill-personality.test.ts"
      via: "script prompt contract regression"
      pattern: "buildPrompt"
---

<objective>
Harden P2 worldbook, import, and supported script structured prompt seams.

Purpose: Checker feedback requires worldbook/import/script seams to be separated from character work and script handling to be explicit. This plan includes `backfill-personality.ts` rather than relying on a non-production exception.
Output: Marker-tested worldbook composition/import contracts and a backfill script prompt contract.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-STRUCTURED-PROMPT-AUDIT.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-CONTEXT.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-RESEARCH.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-PATTERNS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REVIEWS.md
@backend/src/worldbook-library/composition.ts
@backend/src/worldgen/worldbook-importer.ts
@backend/src/scripts/backfill-personality.ts
@backend/src/worldgen/__tests__/worldbook-composition.test.ts
@backend/src/worldgen/__tests__/worldbook-importer.test.ts
@backend/src/scripts/__tests__/backfill-personality.test.ts
</context>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Exact schema/tool shape before repair | Adds contracts to P2 worldbook/import/script structured prompts |
| REQ | P74-R2 | Contracts include exact output fields, list caps, optional/null behavior, and compact examples |
| REQ | P74-R3 | Uses local colocated snippets or existing character contract helpers rather than free-floating prose |
| REQ | P74-R4 | Adds regression fixtures for worldbook selection/import and script personality output shapes |
| REQ | P74-R5 | Contracts preserve provided entries/raw records as authority and forbid backend semantic invention |
| CONTEXT | "worldbook composition/import paths" | Directly covers worldbook P2 seams |
| CHECKER | "script seams such as backfill-personality have no concrete task" | Includes `backend/src/scripts/backfill-personality.ts` and its existing test file |
| RESEARCH | Open Questions (RESOLVED) P2 handling | Chooses inclusion for `backfill-personality.ts`; no implicit script exception remains |
| REVIEWS | LOW versioned marker consistency | Uses `worldbook-composition.v1`, `worldbook-import.v1`, and `backfill-personality.v1` markers |
| REVIEWS | HIGH marker tests prove markers, not contracts | Tests assert exact shapes, caps, valid/minimal/invalid examples, and source authority |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add worldbook composition and import contracts</name>
  <read_first>
    @backend/src/worldbook-library/composition.ts
    @backend/src/worldgen/worldbook-importer.ts
    @backend/src/worldgen/__tests__/worldbook-composition.test.ts
    @backend/src/worldgen/__tests__/worldbook-importer.test.ts
  </read_first>
  <files>backend/src/worldbook-library/composition.ts, backend/src/worldgen/worldbook-importer.ts, backend/src/worldgen/__tests__/worldbook-composition.test.ts, backend/src/worldgen/__tests__/worldbook-importer.test.ts</files>
  <behavior>
    - Worldbook composition prompts include versioned contract markers for primary-source detection and relevant-entry filtering.
    - Contracts ask for exact `{ sourceName }` and `{ relevantIndices: number[] }`-style shapes used by validators.
    - Importer prompts include exact structured output snippets and cap/authority text.
    - Tests assert compact valid examples, minimal valid outputs, and invalid examples for wrong source identity or list shape.
  </behavior>
  <action>
    Before editing symbols, run GitNexus impact analysis for `detectPrimarySource`, `filterRelevantEntries`, `composeWorldbookLibraryRecords`, `composeSelectedWorldbooks`, `buildClassificationPrompt`, `classifyBatchEntries`, `classifyEntries`, and `importClassifiedEntries`. If any exact importer helper is not indexed, run `gitnexus_query({query: "worldbook importer classification prompt safeGenerateObject"})` and `gitnexus_context` on the returned symbols before impact.

    Add compact contract snippets directly in worldbook modules or via a narrow local helper if repeated. Use versioned markers exactly: `STRUCTURED_OUTPUT_CONTRACT: worldbook-composition.v1` and `STRUCTURED_OUTPUT_CONTRACT: worldbook-import.v1`. Do not broaden Phase 74 into worldbook quality tuning. Keep validators as authority and ensure model contracts do not ask the backend to infer source identity or canonical entry meaning beyond provided data.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- backend/src/worldgen/__tests__/worldbook-composition.test.ts backend/src/worldgen/__tests__/worldbook-importer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Worldbook tests fail if primary-source, relevant-entry, or importer structured prompts lose versioned contract markers, exact output shapes, valid/minimal/invalid examples, caps, or source-authority language.
  </acceptance_criteria>
  <done>Worldbook structured prompts have explicit model-facing contracts and deterministic validation remains final authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add backfill-personality script contract</name>
  <read_first>
    @backend/src/scripts/backfill-personality.ts
    @backend/src/scripts/__tests__/backfill-personality.test.ts
    @backend/src/character/prompt-contract.ts
  </read_first>
  <files>backend/src/scripts/backfill-personality.ts, backend/src/scripts/__tests__/backfill-personality.test.ts</files>
  <behavior>
    - Backfill prompt includes `STRUCTURED_OUTPUT_CONTRACT: backfill-personality.v1`.
    - Prompt shows the exact personality object shape expected by the script.
    - Prompt includes compact valid example, minimal valid output, and invalid examples for missing required personality fields.
    - Prompt states source record text is authority and backend/script may reject or retry but must not invent unsupported canon/power/source truth.
  </behavior>
  <action>
    Before editing symbols, run GitNexus impact analysis for `buildPrompt`, `processRow`, and `runBackfill`. If private `buildPrompt` is not indexed, run `gitnexus_query({query: "backfill personality buildPrompt safeGenerateObject script"})` and use `gitnexus_context` on the returned script symbols before impact.

    Add a compact contract marker and schema-shaped example to the script prompt. Use the versioned marker `STRUCTURED_OUTPUT_CONTRACT: backfill-personality.v1`. Reuse `character/prompt-contract.ts` constants only if that does not create a circular dependency or broaden runtime behavior; otherwise keep a local script-only snippet. Preserve existing dry-run, retry, backup, and re-read-before-write behavior.

    Addresses review concern: backfill script scope. Treat `backfill-personality.ts` as an included supported operational seam, not an excluded one-time migration. Its tests must prove the contract exists and safety behavior remains unchanged.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- backend/src/scripts/__tests__/backfill-personality.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Script tests assert the versioned marker, exact personality fields, valid/minimal/invalid examples, no-invention language, and existing idempotency/safety behavior.
  </acceptance_criteria>
  <done>`backfill-personality.ts` is explicitly handled as a supported structured-output script seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model -> worldbook selection schemas | Untrusted source/filter selections can affect worldbook composition |
| model -> backfill script schema | Untrusted model output updates existing character personality records through an operational script |
| raw worldbook/record data -> model prompt | Source entries and character records enter prompts and must remain data, not instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T74-06-01 | Tampering | worldbook source/filter outputs | mitigate | Exact contracts plus validators for primary-source and relevant-entry shapes |
| T74-06-02 | Spoofing | imported source identity | mitigate | Contracts forbid backend/model inference beyond provided entries and validation |
| T74-06-03 | Tampering | backfill-personality output | mitigate | Script prompt contract and existing dry-run/retry/re-read safeguards remain in place |
| T74-06-04 | Information Disclosure | tests and logs | mitigate | Tests use synthetic fixtures and do not snapshot raw provider payloads or secrets |
</threat_model>

<verification>
Run targeted P2 tests:

```bash
npm --prefix backend run test -- backend/src/worldgen/__tests__/worldbook-composition.test.ts backend/src/worldgen/__tests__/worldbook-importer.test.ts backend/src/scripts/__tests__/backfill-personality.test.ts
```
</verification>

<success_criteria>
P74-R2 through P74-R5 are satisfied for worldbook/import/script seams when exact contracts are marker-tested and `backfill-personality.ts` is explicitly included in Phase 74 script seam hardening.
</success_criteria>

<output>
After completion, create `.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-06-SUMMARY.md`.
</output>
