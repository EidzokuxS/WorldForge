---
phase: 74-structured-prompt-contracts-and-model-facing-schema-hardenin
plan: 13
type: execute
wave: 1
depends_on:
  - "74-05"
  - "74-10"
files_modified:
  - backend/src/character/known-ip-worldgen-research.ts
  - backend/src/character/__tests__/known-ip-worldgen-research.test.ts
autonomous: true
gap_closure: true
requirements:
  - P74-R3
  - P74-R4
  - P74-R5
must_haves:
  truths:
    - "Missing power-stat rank values do not become invented rank 5 facts."
    - "Non-numeric, NaN, and 0 rank values fail strict parsing instead of passing through normalization."
    - "Power-stat repair/fail-closed behavior remains the only path for malformed required rank data."
  artifacts:
    - path: "backend/src/character/known-ip-worldgen-research.ts"
      provides: "Strict power-stat rank parsing before schema parse"
      exports:
        - "normalizeLlmPowerStats"
    - path: "backend/src/character/__tests__/known-ip-worldgen-research.test.ts"
      provides: "Regression coverage for invalid/missing rank inputs"
      contains: "normalizeLlmPowerStats"
  key_links:
    - from: "backend/src/character/known-ip-worldgen-research.ts"
      to: "backend/src/character/prompt-contract.ts"
      via: "power-stats contract remains evidence-bound"
      pattern: "buildPowerStatsPromptContract"
    - from: "backend/src/character/known-ip-worldgen-research.ts"
      to: "powerStatsLlmSchema"
      via: "strict schema parse rejects undefined or invalid rank"
      pattern: "powerStatsLlmSchema.parse"
---

<objective>
Close the verifier gap where missing or invalid power-stat ranks are silently normalized to rank 5.

Purpose: Backend validation must remain the deterministic authority. Normalization may coerce valid represented ranks, but it must not invent a mid-rank power fact when required rank semantics are missing.
Output: Strict rank parser plus tests for missing, non-numeric, NaN, and 0 ranks.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-VERIFICATION.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REVIEW.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REPAIR-POLICY.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-05-SUMMARY.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-10-SUMMARY.md
@backend/src/character/known-ip-worldgen-research.ts
@backend/src/character/__tests__/known-ip-worldgen-research.test.ts

<interfaces>
From `backend/src/character/known-ip-worldgen-research.ts`:
```typescript
export function normalizeLlmPowerStats(raw: Record<string, unknown>): PowerStats;
export async function repairPowerStats(opts: {
  rawObject: Record<string, unknown>;
  failures: string[];
  draft: CharacterDraft;
  franchise: string;
  role: ResolvedRole;
  premise: string;
  premiseDivergence?: PremiseDivergence | null;
  searchDigest: string;
  overrideText?: string;
}): Promise<PowerStats>;
```

GitNexus pre-plan impact already found `normalizeLlmPowerStats` risk LOW: direct callers are `repairPowerStats`, `enrichKnownIpWorldgenNpcDraft`, and `assessOriginalCharacterPowerStats`; affected processes are known-IP enrichment and original power assessment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace rank fallback with strict rank parsing</name>
  <files>backend/src/character/known-ip-worldgen-research.ts, backend/src/character/__tests__/known-ip-worldgen-research.test.ts</files>
  <behavior>
    - Test 1: `normalizeLlmPowerStats()` throws when any axis has a valid tier but missing `rank`.
    - Test 2: `normalizeLlmPowerStats()` throws for rank values `"unknown"`, `Number.NaN`, and `0`.
    - Test 3: `normalizeLlmPowerStats()` accepts valid integer ranks 1 through 10, including numeric strings with surrounding whitespace.
  </behavior>
  <action>
    Before editing, run `gitnexus_impact({ repo: "WorldForge", target: "normalizeLlmPowerStats", direction: "upstream", maxDepth: 2, includeTests: true })` and record the blast radius in the summary. If risk is HIGH or CRITICAL, stop and report before editing.

    Add regression tests first; they must fail against current `Number(raw*.rank) || 5` behavior. Implement a local strict parser, for example `rankFromUnknown(value: unknown): number | undefined`, that returns an integer only when the value is a finite integer between 1 and 10. Do not default missing, non-numeric, NaN, or 0 ranks. Let `powerStatsLlmSchema.parse(normalized)` fail so existing `repairPowerStats()` or fail-closed behavior owns malformed required semantics. Preserve tier coercion and hax/vulnerability handling.

    Before each commit, run `gitnexus_detect_changes({ repo: "WorldForge", scope: "staged" })` and confirm changed symbols match the known-IP power-stat normalization/test scope.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/character/__tests__/known-ip-worldgen-research.test.ts src/character/ingestion/__tests__/assess-original.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <done>Invalid/missing rank inputs no longer pass as rank 5; valid ranks still parse; power-stat repair remains bounded and evidence-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM power assessment -> normalization | Untrusted model output enters deterministic shape normalization. |
| Normalized power stats -> character draft | Parsed stats become player-visible and gameplay-relevant character facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-13-01 | Tampering | `normalizeLlmPowerStats` | mitigate | Reject missing/invalid rank values instead of manufacturing rank 5. |
| T-74-13-02 | Repudiation | power-stat repair evidence | mitigate | Preserve Zod issue reporting so summaries show repair/fail-closed cause. |
| T-74-13-03 | Information Disclosure | power-stat tests | accept | Tests use synthetic character data and no secrets. |
</threat_model>

<verification>
Run focused known-IP and original power-stat tests plus backend typecheck. Confirm staged GitNexus detect_changes reports expected character/power-stat impact only.
</verification>

<success_criteria>
- Missing ranks, non-numeric ranks, NaN ranks, and 0 ranks fail strict parse.
- No rank fallback creates `5`.
- Repair prompt remains evidence-only and fail-closed when required power facts are absent.
</success_criteria>

<output>
After completion, create `.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-13-SUMMARY.md`.
</output>
