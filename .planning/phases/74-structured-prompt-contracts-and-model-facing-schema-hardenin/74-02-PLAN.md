---
phase: 74-structured-prompt-contracts-and-model-facing-schema-hardenin
plan: 02
type: execute
wave: 2
depends_on:
  - "74-01"
files_modified:
  - backend/src/engine/prompt-contracts.ts
  - backend/src/engine/tool-schemas.ts
  - backend/src/engine/scene-planner.ts
  - backend/src/engine/hidden-adjudication.ts
  - backend/src/engine/__tests__/scene-planner.test.ts
  - backend/src/engine/__tests__/hidden-adjudication.test.ts
autonomous: true
requirements:
  - P74-R2
  - P74-R3
  - P74-R4
  - P74-R5
must_haves:
  truths:
    - "ScenePlanner prompts ask the model for semantic scene plan fields and exact nested runtime tool input shapes before repair."
    - "Hidden adjudication prompts ask the model for the exact actions[] tool-call shape before validation."
    - "Backend validation remains the final authority and backend code does not invent actor intent, targets, lore, or tool actions."
  artifacts:
    - path: "backend/src/engine/prompt-contracts.ts"
      provides: "Reusable gameplay prompt-contract snippets"
      exports:
        - "buildRuntimeToolInputContract"
        - "buildScenePlannerPromptContract"
        - "buildHiddenAdjudicationPromptContract"
    - path: "backend/src/engine/scene-planner.ts"
      provides: "ScenePlanner model-facing contract"
      contains: "STRUCTURED_OUTPUT_CONTRACT: scene-planner.v1"
    - path: "backend/src/engine/hidden-adjudication.ts"
      provides: "Hidden adjudication model-facing contract"
      contains: "STRUCTURED_OUTPUT_CONTRACT: hidden-adjudication.v1"
  key_links:
    - from: "backend/src/engine/prompt-contracts.ts"
      to: "backend/src/engine/tool-schemas.ts"
      via: "runtime tool schema imports"
      pattern: "runtimeToolInputSchemas"
    - from: "backend/src/engine/scene-planner.ts"
      to: "backend/src/engine/semantic-scene-plan-schema.ts"
      via: "semanticScenePlanSchema remains deterministic validator"
      pattern: "semanticScenePlanSchema"
    - from: "backend/src/engine/hidden-adjudication.ts"
      to: "backend/src/engine/tool-schemas.ts"
      via: "adjudication action input schemas"
      pattern: "runtimeToolInputSchemas"
---

<objective>
Make P0 gameplay contracts explicit for ScenePlanner and hidden adjudication, especially nested runtime tool inputs.

Purpose: Models must be asked for the exact semantic plan and tool-call shape before repair, while existing schema validation and backend-owned IDs remain authoritative.
Output: Engine prompt-contract helper plus ScenePlanner and hidden adjudication prompt updates with regression tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-STRUCTURED-PROMPT-AUDIT.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-CONTEXT.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-PATTERNS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REVIEWS.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-03-PLAN.md
@backend/src/engine/scene-planner.ts
@backend/src/engine/semantic-scene-plan-schema.ts
@backend/src/engine/tool-schemas.ts
@backend/src/engine/hidden-adjudication.ts
@backend/src/engine/__tests__/scene-planner.test.ts
@backend/src/engine/__tests__/hidden-adjudication.test.ts

<interfaces>
Existing authoritative schemas and helpers to preserve:

```typescript
export const runtimeToolInputSchemas = {
  offer_quick_actions: z.object({
    actions: z.array(
      z.object({
        label: z.string().min(1).max(80),
        action: z.string().min(1).max(220),
      }),
    ).min(3).max(5),
  }),
  // other runtime tool input schemas...
} as const;

export type RuntimeToolName = keyof typeof runtimeToolInputSchemas;
export const semanticScenePlanSchema = z.object({ /* semantic model-facing plan */ });
```
</interfaces>
</context>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Exact schema/tool shape before repair | Adds explicit contract markers and nested tool input examples before model calls |
| REQ | P74-R2 | ScenePlanner and hidden adjudication prompt contracts list required fields and nested shapes |
| REQ | P74-R3 | Contract helper is derived from backend schema/tool names instead of duplicated semantics |
| REQ | P74-R4 | Tests assert exact prompt contract presence and backend-owned ID boundaries |
| REQ | P74-R5 | Prompt text forbids backend/model semantic invention outside provided facts and current state |
| CONTEXT | P0 gameplay contracts, especially ScenePlanner/hidden adjudication and nested runtime tool inputs | Directly covers these P0 seams |
| RESEARCH | Validation Map 74-02 | Implements P0 gameplay nested tool contracts after audit lock |
| PATTERNS | ScenePlanner backend-generated IDs and payload alias compatibility | Contract asks for input, permits payload only as compatibility, and forbids model-generated IDs |
| REVIEWS | HIGH marker tests prove markers, not contracts | Tests must assert required nested examples, caps, nullable rules, invalid examples, and minimal valid outputs |
| REVIEWS | MEDIUM schema/contract drift | Runtime tool examples may use a narrow helper sourced from `runtimeToolInputSchemas`; no broad generic Zod-to-contract generator |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create engine prompt-contract helper from runtime tool schemas</name>
  <read_first>
    @backend/src/engine/tool-schemas.ts
    @backend/src/engine/semantic-scene-plan-schema.ts
    @.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-PATTERNS.md
  </read_first>
  <files>backend/src/engine/prompt-contracts.ts, backend/src/engine/tool-schemas.ts</files>
  <behavior>
    - `buildRuntimeToolInputContract()` includes every `RuntimeToolName` from `runtimeToolInputSchemas`.
    - `offer_quick_actions` is documented as `{ "actions": [{ "label": string, "action": string }] }` with 3-5 actions.
    - Helper output includes a compact valid example, a minimal valid output, and invalid examples for missing `actions[].action`, `payload` in primary output, and unsupported `toolName`.
    - Contracts state that backend owns IDs, reference resolution, trimming, and final validation.
  </behavior>
  <action>
    Before editing symbols, run GitNexus impact analysis for `runtimeToolInputSchemas` and any new exported helper names: `gitnexus_impact({target: "runtimeToolInputSchemas", direction: "upstream"})`. If risk is HIGH or CRITICAL, report it before editing.

    Add `backend/src/engine/prompt-contracts.ts` with pure string builders. Export `ENGINE_CONTRACT_MARKER_PREFIX`, `buildRuntimeToolInputContract`, `buildScenePlannerPromptContract`, and `buildHiddenAdjudicationPromptContract`. Build the runtime tool section from the existing schema registry names, not from a hand-maintained alternate enum. If `tool-schemas.ts` needs a tiny helper export for stable tool names, add it without changing validation semantics.

    Addresses review concern: schema/contract drift. A narrow helper that renders runtime tool names/examples from `runtimeToolInputSchemas` is allowed. Do not create a public generic Zod-to-contract formatter, do not import `generateSchemaExample`, and do not dump full Zod internals into prompts.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- backend/src/engine/__tests__/scene-planner.test.ts backend/src/engine/__tests__/hidden-adjudication.test.ts</automated>
  </verify>
  <acceptance_criteria>
    A reusable helper exists, includes all runtime tool names, includes valid/minimal/invalid examples, and can be tested without live providers or database access.
  </acceptance_criteria>
  <done>Engine prompt contracts are generated from authoritative tool schema names and include exact nested input examples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden ScenePlanner prompt contract</name>
  <read_first>
    @backend/src/engine/scene-planner.ts
    @backend/src/engine/semantic-scene-plan-schema.ts
    @backend/src/engine/prompt-contracts.ts
    @backend/src/engine/__tests__/scene-planner.test.ts
  </read_first>
  <files>backend/src/engine/scene-planner.ts, backend/src/engine/__tests__/scene-planner.test.ts</files>
  <behavior>
    - ScenePlanner prompt includes `STRUCTURED_OUTPUT_CONTRACT: scene-planner.v1`.
    - Prompt lists semantic scene plan top-level fields and planned action `{ toolName, input }` shape.
    - Prompt shows nested runtime tool input examples including quick-action `actions[].action`.
    - Prompt includes minimal valid output and invalid/anti-pattern examples: missing nested `actions[].action`, unsupported `toolName`, `payload` instead of `input`, and model-generated backend IDs.
    - Prompt states backend generates IDs and validates with `semanticScenePlanSchema`.
  </behavior>
  <action>
    Before editing symbols, run GitNexus impact analysis for `buildDefaultScenePlannerPrompt`, `buildDefaultScenePlannerSystem`, `runScenePlanner`, and `semanticScenePlanToStrictPlan`.

    Import the engine contract helper into `scene-planner.ts` and insert the ScenePlanner contract before campaign/scene data. Keep existing Phase 73 semantic authority: the model proposes semantic refs and tool intentions; backend mapping, IDs, caps, alias handling, and validation remain deterministic. Do not ask the model to output backend IDs. Do not add backend semantic fallback that invents quick action labels, targets, actor intent, or lore.

    Addresses review concern: marker tests are insufficient. Extend tests so they fail when the prompt contains the marker but omits required nested examples, minimal valid output, caps, invalid anti-pattern examples, or backend-owned ID language.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- backend/src/engine/__tests__/scene-planner.test.ts backend/src/engine/__tests__/turn-processor.scene-plan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    ScenePlanner tests assert the marker, nested tool input contract, quick-action action strings, caps, minimal valid output, invalid anti-pattern examples, and backend-owned ID language.
  </acceptance_criteria>
  <done>ScenePlanner primary prompt is contract-complete before repair and strict validation remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden hidden adjudication action contract</name>
  <read_first>
    @backend/src/engine/hidden-adjudication.ts
    @backend/src/engine/tool-schemas.ts
    @backend/src/engine/prompt-contracts.ts
    @backend/src/engine/__tests__/hidden-adjudication.test.ts
  </read_first>
  <files>backend/src/engine/hidden-adjudication.ts, backend/src/engine/__tests__/hidden-adjudication.test.ts</files>
  <behavior>
    - Hidden adjudication prompt includes `STRUCTURED_OUTPUT_CONTRACT: hidden-adjudication.v1`.
    - Prompt asks for `{ "rationale": string, "actions": [{ "toolName": RuntimeToolName, "input": object }] }`.
    - Prompt includes nested input examples for runtime tools and exact quick-action shape.
    - Prompt includes minimal valid output and invalid examples for missing `input`, missing `actions[].action`, unsupported `toolName`, and payload-vs-input aliasing.
    - Prompt forbids unsupported toolName values, missing input, payload-only primary output, and invented source truth.
  </behavior>
  <action>
    Before editing symbols, run GitNexus impact analysis for `buildJudgeAdjudicationContract`, `runHiddenAdjudicationPlan`, `executeAdjudicationPlan`, and `adjudicationPlanSchema`.

    Replace the current broad hidden-adjudication contract text with the shared engine prompt-contract helper. Preserve the existing discriminated union schema as the final authority. The primary prompt should request `input`; compatibility aliases may remain in validation only if already present, but the model-facing contract must not ask for `payload` as the preferred shape.

    Addresses review concern: provider-specific payload/input failures. Tests must include a fixture or assertion for `payload` as a non-primary anti-pattern while preserving deterministic compatibility mapping only where the schema already supports it.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- backend/src/engine/__tests__/hidden-adjudication.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Tests fail if hidden adjudication prompt loses marker, action array shape, allowed tool names, nested quick-action input contract, minimal valid output, invalid examples, or payload-vs-input warning.
  </acceptance_criteria>
  <done>Hidden adjudication prompts request exact runtime tool calls before deterministic schema validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model -> ScenePlanner schema | Untrusted model output becomes semantic scene plan candidate |
| model -> hidden adjudication schema | Untrusted model output becomes hidden action plan candidate |
| schema registry -> prompt helper | Authoritative backend tool schemas are rendered into model-facing text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T74-02-01 | Tampering | runtime tool input contract | mitigate | Derive allowed tool names from `runtimeToolInputSchemas` and keep schemas as final validators |
| T74-02-02 | Elevation of Privilege | hidden adjudication toolName | mitigate | Prompt lists allowed tool names and schema rejects unsupported tools |
| T74-02-03 | Information Disclosure | prompt tests | mitigate | Tests assert contract snippets only and must not snapshot secrets, raw provider responses, or campaign logs |
| T74-02-04 | Spoofing | model-generated IDs | mitigate | Contract explicitly says backend generates IDs and resolves refs |
</threat_model>

<verification>
Run targeted engine tests:

```bash
npm --prefix backend run test -- backend/src/engine/__tests__/scene-planner.test.ts backend/src/engine/__tests__/turn-processor.scene-plan.test.ts backend/src/engine/__tests__/hidden-adjudication.test.ts
```
</verification>

<success_criteria>
P74-R2 through P74-R5 are satisfied for ScenePlanner and hidden adjudication when model-facing prompts contain exact contract markers, nested runtime tool input shapes, and explicit backend-authority language while existing schema validation remains deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-02-SUMMARY.md`.
</output>
