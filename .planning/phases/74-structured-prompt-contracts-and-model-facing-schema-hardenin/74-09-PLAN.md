---
phase: 74-structured-prompt-contracts-and-model-facing-schema-hardenin
plan: 09
type: execute
wave: 4
depends_on:
  - "74-03"
files_modified:
  - backend/src/engine/prompt-contracts.ts
  - backend/src/engine/npc-offscreen.ts
  - backend/src/engine/prompt-assembler.ts
  - backend/src/engine/__tests__/npc-offscreen.test.ts
  - backend/src/engine/__tests__/context-compression.test.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
autonomous: true
requirements:
  - P74-R2
  - P74-R3
  - P74-R4
  - P74-R5
must_haves:
  truths:
    - "Offscreen NPC structured updates include exact model-facing contracts before backend persistence."
    - "Context compression asks for indexed selections with exact caps and cannot fabricate memory/lore content."
    - "Engine P1/P2 support seams are source-owned and marker-tested instead of hidden under P0 gameplay coverage."
  artifacts:
    - path: "backend/src/engine/prompt-contracts.ts"
      provides: "NPC offscreen and context-compression prompt-contract helpers"
      exports:
        - "buildNpcOffscreenPromptContract"
        - "buildContextCompressionPromptContract"
    - path: "backend/src/engine/npc-offscreen.ts"
      provides: "Offscreen NPC structured prompt contract"
      contains: "STRUCTURED_OUTPUT_CONTRACT: npc-offscreen.v1"
    - path: "backend/src/engine/prompt-assembler.ts"
      provides: "Context compression prompt contract"
      contains: "STRUCTURED_OUTPUT_CONTRACT: context-compression.v1"
  key_links:
    - from: "backend/src/engine/npc-offscreen.ts"
      to: "backend/src/engine/prompt-contracts.ts"
      via: "offscreen update contract helper"
      pattern: "buildNpcOffscreenPromptContract"
    - from: "backend/src/engine/prompt-assembler.ts"
      to: "backend/src/engine/prompt-contracts.ts"
      via: "compression contract helper"
      pattern: "buildContextCompressionPromptContract"
---

<objective>
Own background simulation and context-compression structured-output seams.

Purpose: Reviews flagged `npc-offscreen.ts` and `prompt-assembler.compressContext` as unowned. Both return backend-consumed structured data and need explicit contracts.
Output: Engine helper extensions plus prompt/tests for offscreen updates and context compression.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-STRUCTURED-PROMPT-AUDIT.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-CONTEXT.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-PATTERNS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REVIEWS.md
@.planning/phases/73-structured-output-stability-and-provider-conformance/73-STRUCTURED-OUTPUT-INVENTORY.md
@backend/src/engine/prompt-contracts.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/__tests__/npc-offscreen.test.ts
@backend/src/engine/__tests__/context-compression.test.ts
@backend/src/engine/__tests__/prompt-assembler.test.ts
</context>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Audit every structured-output model call | Owns engine P1/P2 seams not in the original plan set |
| REQ | P74-R2 | Adds exact fields, nested shapes, caps, nullable/optional rules, valid/minimal/invalid examples |
| REQ | P74-R3 | Reuses engine prompt-contract helpers from 74-02/74-03 |
| REQ | P74-R4 | Tests cover offscreen update shape and context-compression indexed selections |
| REQ | P74-R5 | Fail-closed/no-invention policy for missing offscreen semantics and fabricated memory/lore selections |
| REVIEWS | HIGH `npc-offscreen.ts` and `prompt-assembler.compressContext` have no owner | Directly covers both files |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add offscreen and compression contract helpers</name>
  <read_first>
    @backend/src/engine/prompt-contracts.ts
    @backend/src/engine/npc-offscreen.ts
    @backend/src/engine/prompt-assembler.ts
    @.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REVIEWS.md
  </read_first>
  <files>backend/src/engine/prompt-contracts.ts</files>
  <behavior>
    - Helper exports include `STRUCTURED_OUTPUT_CONTRACT: npc-offscreen.v1` and `context-compression.v1`.
    - Offscreen helper lists update fields, location/summary caps, nullable/optional rules, valid/minimal/invalid examples.
    - Compression helper lists indexed selection shape, max selections, no fabricated memory/lore content, valid/minimal/invalid examples.
  </behavior>
  <action>
    Before editing symbols, run GitNexus impact analysis for `buildWorldBrainPromptContract` and any new helper names after selecting names.

    Extend engine prompt-contract helpers from 74-03. Keep helpers pure and deterministic. Do not alter offscreen simulation behavior or prompt assembly algorithms in this task.
  </action>
  <verify>
    <automated>rg -n "npc-offscreen.v1|context-compression.v1|VALID MINIMAL|INVALID" backend/src/engine/prompt-contracts.ts</automated>
  </verify>
  <acceptance_criteria>
    Engine helper file contains both versioned markers and semantic adequacy text for valid/minimal/invalid examples, caps, nullability, and no-invention boundaries.
  </acceptance_criteria>
  <done>Engine prompt-contract helpers cover offscreen NPC updates and context compression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden npc-offscreen structured update contract</name>
  <read_first>
    @backend/src/engine/npc-offscreen.ts
    @backend/src/engine/prompt-contracts.ts
    @backend/src/engine/__tests__/npc-offscreen.test.ts
  </read_first>
  <files>backend/src/engine/npc-offscreen.ts, backend/src/engine/__tests__/npc-offscreen.test.ts</files>
  <behavior>
    - `runNpcOffscreen` prompt includes `STRUCTURED_OUTPUT_CONTRACT: npc-offscreen.v1`.
    - Prompt asks for exact offscreen update array/object shape and caps.
    - Prompt says backend may reject invalid updates but must not invent summaries, locations, goals, or relationship facts.
  </behavior>
  <action>
    Before editing symbols, run GitNexus impact analysis for `runNpcOffscreen`, `applyOffscreenUpdate`, and any private prompt builder symbols found by `gitnexus_query({query: "npc offscreen structured update safeGenerateObject prompt"})`.

    Insert the offscreen contract helper before NPC/world data. Preserve existing DB write behavior and validators. Missing required offscreen update semantics must fail closed or be omitted according to existing optional-output policy; backend must not fabricate state updates to make the schema pass.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- backend/src/engine/__tests__/npc-offscreen.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Tests assert marker, exact update shape, caps, minimal valid output, invalid examples, and no-invention/fail-closed language.
  </acceptance_criteria>
  <done>`npc-offscreen.ts` is source-owned and prompt-contract tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden context-compression contract</name>
  <read_first>
    @backend/src/engine/prompt-assembler.ts
    @backend/src/engine/prompt-contracts.ts
    @backend/src/engine/__tests__/context-compression.test.ts
    @backend/src/engine/__tests__/prompt-assembler.test.ts
  </read_first>
  <files>backend/src/engine/prompt-assembler.ts, backend/src/engine/__tests__/context-compression.test.ts, backend/src/engine/__tests__/prompt-assembler.test.ts</files>
  <behavior>
    - `compressContext` prompt includes `STRUCTURED_OUTPUT_CONTRACT: context-compression.v1`.
    - Prompt asks for indexed selections only, with max selection caps and no invented facts.
    - Tests prove final prose prompt assembly remains outside structured-output scope.
  </behavior>
  <action>
    Before editing symbols, run GitNexus impact analysis for `compressContext` and any private compression prompt builder symbols found by `gitnexus_query({query: "prompt assembler compressContext safeGenerateObject"})`.

    Insert the context-compression contract helper into the compression prompt. The model may select and summarize from provided indexed items only; backend must not accept fabricated memory/lore indices or invented content as compression success. Do not alter final storyteller prose prompts.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- backend/src/engine/__tests__/context-compression.test.ts backend/src/engine/__tests__/prompt-assembler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Tests assert marker, indexed selection shape, caps, valid/minimal/invalid examples, no fabricated content language, and no accidental marker requirement on final narration.
  </acceptance_criteria>
  <done>`prompt-assembler.compressContext` is source-owned and prompt-contract tested without changing final narration scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model -> offscreen update schema | Untrusted offscreen plans can persist background state changes |
| model -> compression schema | Untrusted compression selections can affect later prompt context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T74-09-01 | Tampering | npc-offscreen structured updates | mitigate | Exact update contract and fail-closed no-invention tests |
| T74-09-02 | Spoofing | context-compression selections | mitigate | Contract allows only indexed provided items and rejects fabricated content |
| T74-09-03 | Information Disclosure | compression tests | mitigate | Tests use synthetic items and do not snapshot raw campaign/provider data |
</threat_model>

<verification>
Run targeted engine support tests:

```bash
npm --prefix backend run test -- backend/src/engine/__tests__/npc-offscreen.test.ts backend/src/engine/__tests__/context-compression.test.ts backend/src/engine/__tests__/prompt-assembler.test.ts
```
</verification>

<success_criteria>
The engine support seams are complete when `npc-offscreen.ts` and `prompt-assembler.compressContext` have source-level owners, exact prompt contracts, and tests proving semantic adequacy plus deterministic no-invention behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-09-SUMMARY.md`.
</output>
