---
phase: 74-structured-prompt-contracts-and-model-facing-schema-hardenin
plan: 04
type: execute
wave: 2
depends_on:
  - "74-01"
files_modified:
  - backend/src/worldgen/prompt-contracts.ts
  - backend/src/worldgen/ip-researcher.ts
  - backend/src/worldgen/research-artifact.ts
  - backend/src/worldgen/__tests__/ip-researcher.test.ts
  - backend/src/worldgen/__tests__/research-artifact.test.ts
autonomous: true
requirements:
  - P74-R2
  - P74-R3
  - P74-R4
  - P74-R5
must_haves:
  truths:
    - "Worldgen research artifact prompts ask for exact generatedContext citations and canonicalNames shapes before repair."
    - "Private generatedContext prompt access is resolved explicitly so tests inspect the real prompt builder."
    - "Research artifact repair preserves source-rule authority and never asks backend code to infer canon/source roles from strings."
  artifacts:
    - path: "backend/src/worldgen/prompt-contracts.ts"
      provides: "Worldgen research prompt-contract snippets"
      exports:
        - "buildGeneratedContextPromptContract"
        - "buildResearchArtifactPromptContract"
        - "buildWorldgenSourceRuleAuthorityContract"
    - path: "backend/src/worldgen/ip-researcher.ts"
      provides: "Generated context and artifact research prompt contracts"
      contains: "STRUCTURED_OUTPUT_CONTRACT: generated-context.v1"
    - path: "backend/src/worldgen/research-artifact.ts"
      provides: "Worldgen artifact contract constants"
      contains: "STRUCTURED_OUTPUT_CONTRACT: worldgen-research-artifact.v1"
  key_links:
    - from: "backend/src/worldgen/ip-researcher.ts"
      to: "backend/src/worldgen/research-artifact.ts"
      via: "worldgenResearchArtifactSchema and generatedContext shape"
      pattern: "worldgenResearchArtifactSchema"
    - from: "backend/src/worldgen/ip-researcher.ts"
      to: "backend/src/worldgen/__tests__/ip-researcher.test.ts"
      via: "direct named export of buildGeneratedContextPrompt for prompt-contract tests"
      pattern: "buildGeneratedContextPrompt"
---

<objective>
Harden worldgen research artifact and generatedContext model-facing contracts.

Purpose: Generated context failures were observed as citations/canonicalNames string shapes. This plan makes those target shapes visible before repair and resolves private prompt-builder test access.
Output: Worldgen research contract helper, prompt updates, and regression tests for generatedContext/artifact prompts.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-STRUCTURED-PROMPT-AUDIT.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-CONTEXT.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-PATTERNS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REVIEWS.md
@.planning/phases/72-worldgen-authority-propagation-regression-audit/72-SUMMARY.md
@backend/src/worldgen/ip-researcher.ts
@backend/src/worldgen/research-artifact.ts
@backend/src/worldgen/__tests__/ip-researcher.test.ts
@backend/src/worldgen/__tests__/research-artifact.test.ts

<interfaces>
Worldgen generatedContext contract shape to preserve:

```typescript
generatedContext: {
  keyFacts: string[];
  tonalNotes: string[];
  citations: Array<{ jobId?: string; url?: string; note: string }>;
  canonicalNames: {
    locations?: string[];
    factions?: string[];
    characters?: string[];
  };
}
```
</interfaces>
</context>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Exact schema/tool shape before repair | Adds worldgen research model-facing contracts for generatedContext, artifact, sufficiency, and extraction outputs |
| REQ | P74-R2 | Prompts list required fields, nested shapes, optional/null rules, caps, valid examples, minimal outputs, and invalid examples |
| REQ | P74-R3 | Contracts reference `worldgenResearchArtifactSchema` shape and shared worldgen helpers; no generic Zod-to-contract generator |
| REQ | P74-R4 | Tests assert citations and canonicalNames contract snippets and source-rule text |
| REQ | P74-R5 | Source-rule authority is explicit; backend may normalize shape but not invent canon/source roles |
| CONTEXT | "citations string vs array, canonicalNames string vs object" | Directly targets these failure classes |
| REVIEWS | LOW private prompt builder strategy unresolved | Direct named export strategy for `buildGeneratedContextPrompt` is part of the task |
| PATTERNS | `known-ip-worldgen-research.ts` exact JSON target-shape pattern | Reuses exact-shape prompt style for generatedContext and artifact prompts |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create worldgen research prompt-contract helpers</name>
  <read_first>
    @backend/src/worldgen/research-artifact.ts
    @backend/src/worldgen/ip-researcher.ts
    @backend/src/worldgen/__tests__/research-artifact.test.ts
    @.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-PATTERNS.md
    @.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REVIEWS.md
  </read_first>
  <files>backend/src/worldgen/prompt-contracts.ts, backend/src/worldgen/research-artifact.ts, backend/src/worldgen/__tests__/research-artifact.test.ts</files>
  <behavior>
    - Helper exports include generatedContext, research artifact brief, sufficiency, fact extraction, and source-rule authority contracts.
    - generatedContext contract shows citations as array of objects and canonicalNames as grouped object.
    - Helpers include compact valid example, minimal valid output, invalid examples for citations-as-string and canonicalNames-as-string, and cap/nullability rules.
    - Helpers state backend can trim/cap/drop invalid optional data but cannot invent source roles, premise canon, or canonical truth.
  </behavior>
  <action>
    Before editing symbols, run GitNexus impact analysis for `worldgenResearchArtifactSchema`, `formatWorldgenResearchArtifactBlock`, and the new worldgen helper names after selecting them.

    Add `backend/src/worldgen/prompt-contracts.ts` with pure string builders. Keep contract examples compact and schema-shaped. If exposing a small generatedContext shape constant from `research-artifact.ts` helps tests, export it without changing parsing semantics. Do not add runtime dependencies. Do not create a broad generic Zod-to-contract generator.

    Addresses review concern: marker tests are insufficient. Tests must assert required nested shapes, caps, nullability/optional rules, compact valid example, minimal valid output, and invalid citations/canonicalNames anti-pattern examples.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- backend/src/worldgen/__tests__/research-artifact.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Contract helpers include exact generatedContext nested shape, valid/minimal/invalid examples, source-rule authority text, and no semantic backend invention language.
  </acceptance_criteria>
  <done>Worldgen research prompt-contract helpers exist and are semantically testable without live providers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden research artifact prompts and export generatedContext builder</name>
  <read_first>
    @backend/src/worldgen/ip-researcher.ts
    @backend/src/worldgen/research-artifact.ts
    @backend/src/worldgen/prompt-contracts.ts
    @backend/src/worldgen/__tests__/ip-researcher.test.ts
    @.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REVIEWS.md
  </read_first>
  <files>backend/src/worldgen/ip-researcher.ts, backend/src/worldgen/__tests__/ip-researcher.test.ts</files>
  <behavior>
    - `buildGeneratedContextPrompt` is exported as a named pure prompt builder for direct tests.
    - `buildGeneratedContextPrompt` includes `STRUCTURED_OUTPUT_CONTRACT: generated-context.v1`.
    - Research artifact prompts include exact citations and canonicalNames shapes before repair.
    - Artifact sufficiency and fact extraction prompts include exact return shapes and source-rule authority text.
    - Tests cover citations string vs array and canonicalNames string vs object prompt-contract regressions.
  </behavior>
  <action>
    Before editing symbols, run GitNexus impact analysis for `researchWorldgenArtifact`, `buildGeneratedContextPrompt`, `buildResearchArtifactBriefPrompt`, and `evaluateResearchArtifactSufficiency`.

    Insert the worldgen contract helper blocks into `ip-researcher.ts` prompt builders before raw research payloads. Export `buildGeneratedContextPrompt` as a normal named export so tests can inspect the real builder directly; do not use an `_test` namespace and do not rely only on mocked call-position access. Add a short JSDoc noting the export is for contract reuse/testing and has no side effects.

    Keep Phase 72 authority intact: `WorldgenResearchArtifactV2` and source rules decide canon/source role; backend repair can normalize shape and enforce caps but must not infer canonical truth, source roles, or premise validity from strings.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- backend/src/worldgen/__tests__/ip-researcher.test.ts backend/src/worldgen/__tests__/research-artifact.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Tests fail if generatedContext contract loses citations array-object shape, canonicalNames object shape, valid/minimal/invalid examples, source-rule authority language, or direct `buildGeneratedContextPrompt` test access.
  </acceptance_criteria>
  <done>Research artifact model-facing prompts request exact generatedContext and artifact shapes before backend validation/repair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model -> research artifact schema | Untrusted model output becomes candidate `WorldgenResearchArtifactV2` data |
| research/source data -> prompts | External source summaries are shown to model and must not become backend-invented canon |
| prompt-builder export -> tests | Previously private builder becomes a named export for deterministic contract tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T74-04-01 | Tampering | generatedContext citations/canonicalNames | mitigate | Prompt contract shows exact nested shapes and tests assert valid/minimal/invalid examples |
| T74-04-02 | Spoofing | source-rule authority | mitigate | Contracts state source roles/canon come only from source rules and artifacts, not backend inference |
| T74-04-03 | Information Disclosure | prompt regression tests | mitigate | Tests assert contract text only; no raw search payloads, provider logs, or secrets in snapshots |
| T74-04-04 | Denial of Service | overlong generated metadata | mitigate | Contract includes caps and validators remain final authority |
</threat_model>

<verification>
Run targeted worldgen research tests:

```bash
npm --prefix backend run test -- backend/src/worldgen/__tests__/ip-researcher.test.ts backend/src/worldgen/__tests__/research-artifact.test.ts
```
</verification>

<success_criteria>
P74-R2 through P74-R5 are satisfied for worldgen research when generatedContext/artifact prompts have exact model-facing contracts, private prompt-builder access is explicit, and source-rule authority remains outside backend semantic invention.
</success_criteria>

<output>
After completion, create `.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-04-SUMMARY.md`.
</output>
