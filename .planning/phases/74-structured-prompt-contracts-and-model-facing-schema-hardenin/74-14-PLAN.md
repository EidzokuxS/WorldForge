---
phase: 74-structured-prompt-contracts-and-model-facing-schema-hardenin
plan: 14
type: execute
wave: 1
depends_on:
  - "74-09"
  - "74-10"
files_modified:
  - backend/src/engine/npc-offscreen.ts
  - backend/src/engine/__tests__/npc-offscreen.test.ts
autonomous: true
gap_closure: true
requirements:
  - P74-R2
  - P74-R3
  - P74-R4
  - P74-R5
must_haves:
  truths:
    - "NPC offscreen schema enforces the caps advertised in the prompt contract."
    - "NPC offscreen update count is capped by the number of listed offscreen NPCs."
    - "Invalid overlong or excess NPC updates fail before persistence."
  artifacts:
    - path: "backend/src/engine/npc-offscreen.ts"
      provides: "Capped NPC offscreen update schema and dynamic batch cap"
      exports:
        - "parseOffscreenUpdates"
        - "simulateOffscreenNpcs"
    - path: "backend/src/engine/__tests__/npc-offscreen.test.ts"
      provides: "Regression coverage for offscreen caps and listed-NPC update limit"
      contains: "offscreen"
  key_links:
    - from: "backend/src/engine/npc-offscreen.ts"
      to: "backend/src/engine/prompt-contracts.ts"
      via: "prompt caps match schema caps"
      pattern: "buildNpcOffscreenPromptContract"
    - from: "backend/src/engine/npc-offscreen.ts"
      to: "applyOffscreenUpdate"
      via: "schema parse must succeed before updates are applied"
      pattern: "parseOffscreenUpdates"
---

<objective>
Close the verifier gap where NPC offscreen prompts advertise caps that the backend schema does not enforce.

Purpose: Prompt contracts must match deterministic backend validation. The model is told exact caps, and Zod must reject outputs that violate them before persistence.
Output: Exact `.max()` constraints, dynamic listed-NPC update cap, and tests for overlong fields and excess updates.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-VERIFICATION.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REVIEW.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-REPAIR-POLICY.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-09-SUMMARY.md
@.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-10-SUMMARY.md
@backend/src/engine/npc-offscreen.ts
@backend/src/engine/__tests__/npc-offscreen.test.ts

<interfaces>
From `backend/src/engine/npc-offscreen.ts`:
```typescript
export interface OffscreenUpdate {
  npcName: string;
  newLocation: string | null;
  actionSummary: string;
  goalProgress: string | null;
}
export function parseOffscreenUpdates(raw: Array<{
  npcName: string;
  newLocation: string | null;
  actionSummary: string;
  goalProgress: string | null;
}>): OffscreenUpdate[];
export async function simulateOffscreenNpcs(...): Promise<AppliedOffscreenUpdate[]>;
```

GitNexus pre-plan impact already found `simulateOffscreenNpcs` risk LOW: direct caller is `runRollbackCriticalPostTurn`. `parseOffscreenUpdates` risk LOW: direct caller is `simulateOffscreenNpcsInternal`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce NPC offscreen schema caps and dynamic update count</name>
  <files>backend/src/engine/npc-offscreen.ts, backend/src/engine/__tests__/npc-offscreen.test.ts</files>
  <behavior>
    - Test 1: schema parsing rejects `npcName` longer than 120 chars.
    - Test 2: schema parsing rejects `newLocation` longer than 120 chars, `actionSummary` longer than 260 chars, and `goalProgress` longer than 180 chars.
    - Test 3: schema parsing rejects an `updates` array longer than the listed NPC count.
  </behavior>
  <action>
    Before editing, run `gitnexus_impact({ repo: "WorldForge", target: "simulateOffscreenNpcs", direction: "upstream", maxDepth: 2, includeTests: true })` and `gitnexus_impact({ repo: "WorldForge", target: "parseOffscreenUpdates", direction: "upstream", maxDepth: 2, includeTests: true })`; record both blast-radius results in the summary. If either risk is HIGH or CRITICAL, stop and report before editing.

    Add tests first; they must fail against the current unconstrained `offscreenUpdateSchema`. Implement exact caps matching `buildNpcOffscreenPromptContract()`: `npcName.max(120)`, `newLocation.max(120).nullable()`, `actionSummary.max(260)`, and `goalProgress.max(180).nullable()`. Add a dynamic schema or post-parse guard so `updates.length <= offscreenKeyNpcs.length` before `parseOffscreenUpdates()` and before any `applyOffscreenUpdate()` calls.

    Avoid generic Zod patching or broad sanitizer behavior. Do not trim or invent summaries, locations, goals, relationship facts, source roles, or canonical truth. Invalid required semantics must reject or enter existing bounded repair/fail-closed behavior before persistence.

    Before each commit, run `gitnexus_detect_changes({ repo: "WorldForge", scope: "staged" })` and confirm changed symbols match the NPC offscreen validation/test scope.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/engine/__tests__/npc-offscreen.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <done>NPC offscreen outputs violating prompt caps or listed-NPC update count fail before DB writes or episodic event persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM offscreen update batch -> Zod schema | Untrusted structured update batch crosses into backend validation. |
| Validated offscreen update -> DB/vector writes | Accepted update can change NPC state and episodic memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-14-01 | Tampering | `offscreenUpdateSchema` | mitigate | Enforce exact caps and update-count limit before persistence. |
| T-74-14-02 | Denial of Service | offscreen batch updates | mitigate | Reject update arrays larger than listed NPC count to prevent oversized batch processing. |
| T-74-14-03 | Information Disclosure | offscreen prompt/tests | accept | Tests use synthetic NPC data only and do not store raw prompts or provider envelopes. |
</threat_model>

<verification>
Run focused NPC offscreen tests and backend typecheck. Confirm staged GitNexus detect_changes reports expected engine offscreen impact only.
</verification>

<success_criteria>
- Overlong `npcName`, `newLocation`, `actionSummary`, and `goalProgress` fail validation.
- More updates than listed NPC contexts fail before persistence.
- Prompt caps and backend caps match exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/74-structured-prompt-contracts-and-model-facing-schema-hardenin/74-14-SUMMARY.md`.
</output>
