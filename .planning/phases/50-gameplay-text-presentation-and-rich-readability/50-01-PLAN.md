---
phase: 50-gameplay-text-presentation-and-rich-readability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - package-lock.json
  - frontend/app/globals.css
  - frontend/lib/gameplay-text.ts
  - frontend/components/game/rich-text-message.tsx
  - frontend/components/game/special-message-block.tsx
  - frontend/components/game/narrative-log.tsx
  - frontend/components/game/__tests__/rich-text-message.test.tsx
  - frontend/components/game/__tests__/narrative-log.test.tsx
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "Assistant narration renders as a reader-oriented surface instead of one plain pre-wrapped text block."
    - "Submitted player actions render as their own faster-scanning message treatment and still honor lightweight RP markup."
    - "Lookup, compare/power-profile, system, and progress/status messages render as compact support blocks rather than looking like ordinary narration."
    - "Streaming assistant text stays readable while partial `*` or `**` markers remain plain until they close."
  artifacts:
    - path: "frontend/lib/gameplay-text.ts"
      provides: "bounded render-time message classification and paragraph/dialogue helpers"
      contains: "GameMessageKind|deriveGameMessageKind|isDialogueParagraph"
    - path: "frontend/components/game/rich-text-message.tsx"
      provides: "safe bounded RP renderer for narration and player messages"
      contains: "react-markdown|remark-gfm"
    - path: "frontend/components/game/special-message-block.tsx"
      provides: "compact support-block renderer with a dedicated compare/power-profile path alongside lookup/system/progress/mechanical messages"
      contains: "compare|lookup|system|progress|mechanical|power_profile"
    - path: "frontend/components/game/narrative-log.tsx"
      provides: "role-first message rendering that uses the new reader and support-block surfaces"
      contains: "RichTextMessage|SpecialMessageBlock"
  key_links:
    - from: "frontend/lib/gameplay-text.ts"
      to: "frontend/components/game/rich-text-message.tsx"
      via: "render-time helpers classify narration, dialogue, and RP emphasis without changing stored message text"
      pattern: "deriveGameMessageKind|isDialogueParagraph"
    - from: "frontend/components/game/rich-text-message.tsx"
      to: "frontend/components/game/narrative-log.tsx"
      via: "assistant and player messages must share the same bounded RP renderer while keeping different wrappers"
      pattern: "RichTextMessage"
    - from: "frontend/components/game/special-message-block.tsx"
      to: "frontend/components/game/narrative-log.tsx"
      via: "lookup/compare/system/progress blocks must stay outside the prose surface, with `power_profile` preserved as compare output instead of a generic lookup label"
      pattern: "SpecialMessageBlock|compare|lookup|system|progress|power_profile"
---

<objective>
Build the bounded rich-text and message-role rendering foundation for Phase 50.

Purpose: implement D-01 through D-06, D-10 through D-14, and D-18 through D-20 by replacing the flat gameplay log treatment with safe reader prose plus compact support blocks.
Output: shared gameplay text helpers, safe RP renderer, support-block component, and a rewired `NarrativeLog`.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-CONTEXT.md
@.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-RESEARCH.md
@.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-VALIDATION.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-03-SUMMARY.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-03-SUMMARY.md
@docs/ui_concept_hybrid.html
@frontend/package.json
@frontend/app/globals.css
@frontend/components/game/narrative-log.tsx
@frontend/components/game/__tests__/narrative-log.test.tsx

<interfaces>
From `@worldforge/shared` / `frontend/components/game/narrative-log.tsx`:
```ts
export type ChatRole = "user" | "assistant" | "system";

export interface ChatMessage {
  role: ChatRole;
  content: string;
}

interface NarrativeLogProps {
  messages: ChatMessage[];
  premise: string;
  isStreaming: boolean;
  turnPhase?: "idle" | "streaming" | "finalizing";
  sceneProgress?: "opening" | "scene-settling" | null;
}
```

From `frontend/app/globals.css` and `docs/ui_concept_hybrid.html`:
```css
#09090b   /* shell background */
#18181b   /* panel surface */
#27272a   /* hover / input surface */
#3f3f46   /* strong border */
#a1a1aa   /* muted text */
#f4f4f5   /* bright text */
#f8f9fa   /* bone narrative text */
#e63e00   /* blood accent */
#431407   /* threat background */
#3b82f6   /* mystic accent */
```
</interfaces>
</context>

<gitnexus_safety>
Before editing existing symbols in this plan, run `gitnexus_impact({ repo: "WorldForge", target: "NarrativeLog", direction: "upstream" })` and report the d=1 callers, affected processes, and risk level. If implementation expands beyond `NarrativeLog` into any other existing named symbol, run the same impact check for that symbol before editing it.
Before closeout, run `gitnexus_detect_changes({ repo: "WorldForge", scope: "all" })` and confirm only the expected gameplay-text rendering files, symbols, and execution flows for Plan `50-01` changed.
</gitnexus_safety>

<tasks>

<task type="auto" tdd="true">
  <name>Task 50-01-01: Add the bounded RP renderer and message-classification helpers</name>
  <files>frontend/package.json, package-lock.json, frontend/app/globals.css, frontend/lib/gameplay-text.ts, frontend/components/game/rich-text-message.tsx, frontend/components/game/special-message-block.tsx, frontend/components/game/__tests__/rich-text-message.test.tsx</files>
  <read_first>.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-CONTEXT.md, .planning/phases/50-gameplay-text-presentation-and-rich-readability/50-RESEARCH.md, docs/ui_concept_hybrid.html, frontend/package.json, frontend/app/globals.css, frontend/components/game/narrative-log.tsx</read_first>
  <behavior>
    - Test 1: narration and player text can render the locked RP subset only: plain paragraphs/line breaks, literal `"quoted speech"`, `*action*`, and `**strong emphasis**`, all at display time without storing HTML.
    - Test 2: unsupported markdown/GFM syntax including links, bullet/numbered lists, tables, task lists, headings, blockquotes, code spans/fences, and raw HTML is not upgraded into richer UI; it stays plain text or is ignored by the bounded renderer.
    - Test 3: lookup, compare/power_profile, system, progress, and future mechanical blocks are classified into compact non-prose surfaces.
    - Test 4: unfinished streaming markers such as `*unclosed` remain visible as plain text until closed instead of corrupting the render tree.
  </behavior>
  <action>Start by checking the existing `frontend/app/globals.css` tokens before adding anything new: this repo already defines `--color-bone`, `--color-blood`, `--color-mystic`, and the shell palette, so reuse or extend those variables instead of duplicating the concept colors under a second namespace. Then install the required renderer dependencies from the repo root with the exact workspace command `npm install --workspace frontend react-markdown@10.1.0 remark-gfm@4.0.1 @tailwindcss/typography@0.5.19`; because this repo uses npm workspaces, that command must update `frontend/package.json` and the root `package-lock.json` together. After install, verify the Tailwind v4 path immediately by registering `@plugin "@tailwindcss/typography";` in `frontend/app/globals.css` and keeping that registration only if the existing frontend CSS/build path accepts it cleanly; if the plugin import is rejected by the repo's current Tailwind setup, stop and keep the narration typography scoped in component-level utility classes while still using the existing gameplay tokens. Create `frontend/lib/gameplay-text.ts` with concrete helpers `deriveGameMessageKind(role, content)`, `stripLookupPrefix(content)`, `splitGameplayParagraphs(content)`, and `isDialogueParagraph(paragraph)`; the render kinds must be `"narration"`, `"player"`, `"system"`, `"lookup"`, `"compare"`, `"mechanical"`, and `"progress"`. Keep lookup detection keyed off the persisted Phase 49 prefix format `[Lookup: ...]`, but explicitly preserve `[Lookup: power_profile]` as `compare` so compare output keeps its own special-block path instead of falling through generic lookup handling. Create `frontend/components/game/rich-text-message.tsx` that uses `react-markdown` with `remark-gfm` only as a bounded parser backend for the locked RP subset from D-03 through D-06: allow paragraph separation/newlines plus only `em` and `strong` formatting for `*action*` and `**strong emphasis**`, while literal double-quoted speech remains ordinary text styled through paragraph/dialogue treatment instead of markdown syntax. Enforce the bounded subset explicitly at the renderer layer by whitelisting only paragraph/break/emphasis elements and unwrapping or flattening everything else so `remark-gfm` never upgrades links, autolinks, headings, blockquotes, ordered or unordered lists, tables, task lists, images, inline code, fenced code, or raw HTML into richer UI. Override `p`, `em`, and `strong` so narration reads like a digital reader surface: `p` gets prose spacing and `text-bone`, `em` is rendered as non-italic highlighted action emphasis, and `strong` is brighter semibold emphasis. Create `frontend/components/game/special-message-block.tsx` that renders compact labeled blocks for `compare`, `lookup`, `system`, `mechanical`, and `progress` using the same reused palette, but never applies `prose` typography to those surfaces; the compare variant must visibly read as a compare/power-profile result, not a generic lookup badge. Add a focused component test file that locks the exact allowed RP subset, proves unsupported markdown/GFM examples such as `[link](https://example.com)`, `- list`, `1. list`, pipe tables, and `- [ ] task` do not render as upgraded rich markdown, and proves the partial-marker behavior plus dedicated compare labeling contract; do not add a general Markdown editor, HTML renderer, or free-form formatting language.</action>
  <verify>
    <automated>npm ls --workspace frontend react-markdown remark-gfm @tailwindcss/typography && npx vitest run frontend/components/game/__tests__/rich-text-message.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\"react-markdown\"|\"remark-gfm\"|\"@tailwindcss/typography\"" frontend/package.json` returns matches.
    - `rg -n "\"react-markdown\"|\"remark-gfm\"|\"@tailwindcss/typography\"" package-lock.json` returns matches, proving the workspace lockfile was updated.
    - `npm ls --workspace frontend react-markdown remark-gfm @tailwindcss/typography` exits `0`.
    - `rg -n "@plugin \"@tailwindcss/typography\"|--color-bone|--color-blood|--color-mystic" frontend/app/globals.css` returns matches.
    - `rg -n "deriveGameMessageKind|stripLookupPrefix|isDialogueParagraph|power_profile|\"compare\"" frontend/lib/gameplay-text.ts` returns matches.
    - `rg -n "react-markdown|remark-gfm" frontend/components/game/rich-text-message.tsx` returns matches.
    - `rg -n "compare|power_profile|lookup" frontend/components/game/special-message-block.tsx frontend/components/game/__tests__/rich-text-message.test.tsx` returns matches.
    - `npx vitest run frontend/components/game/__tests__/rich-text-message.test.tsx` passes.
    - Existing gameplay tokens in `frontend/app/globals.css` are reused or extended; the task does not create a second duplicate color namespace for the same concept palette.
    - Unsupported GFM constructs are explicitly flattened or unwrapped at the renderer layer instead of being left to `remark-gfm` defaults.
    - The focused component tests prove only paragraphs, `*action*`, and `**strong emphasis**` are formatted, while unsupported markdown/GFM such as links, lists, tables, and task lists stays plain or is ignored.
    - The focused component tests prove `[Lookup: power_profile]` renders through the dedicated compare path and does not fall back to narration or a generic `Lookup` label.
  </acceptance_criteria>
  <done>The project has a safe bounded RP-subset renderer and shared classification helpers that can power richer gameplay text without changing stored chat content or accidentally enabling broader markdown/GFM features.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 50-01-02: Rewire `NarrativeLog` to the new reader and support-block surfaces</name>
  <files>frontend/components/game/narrative-log.tsx, frontend/components/game/__tests__/narrative-log.test.tsx</files>
  <read_first>.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-03-SUMMARY.md, .planning/phases/49-search-grounding-and-in-game-research-semantics/49-03-SUMMARY.md, frontend/components/game/narrative-log.tsx, frontend/components/game/rich-text-message.tsx, frontend/components/game/special-message-block.tsx, frontend/lib/gameplay-text.ts, frontend/components/game/__tests__/narrative-log.test.tsx</read_first>
  <behavior>
    - Test 1: assistant narration uses the reader surface, user messages use a distinct player block, and system/lookup/compare/progress messages no longer share one plain text wrapper.
    - Test 2: lookup entries stored as `[Lookup: kind] answer` render with a compact label and stripped body text, while `[Lookup: power_profile]` uses a dedicated compare label rather than a generic lookup badge.
    - Test 3: streaming/opening/finalizing statuses stay outside the story prose and still show the current Phase 45 wording.
  </behavior>
  <action>Replace the flat message rendering in `frontend/components/game/narrative-log.tsx` with role-first surfaces built on the new helpers. Assistant messages must render through `RichTextMessage` inside a centered reader article. User messages must render as a separate visually distinct block on the right side and still pass through `RichTextMessage` so submitted `"speech"`, `*action*`, and `**emphasis**` markup shows after submission per D-08 and D-09. System messages must render through `SpecialMessageBlock` with `kind="system"`. Persisted lookup messages must be detected from the literal `[Lookup: ...]` prefix introduced in Phase 49, stripped of the prefix body, and rendered through `SpecialMessageBlock`; ordinary lookup kinds use `kind="lookup"`, but `[Lookup: power_profile]` must be preserved as a dedicated compare/power-profile block path such as `kind="compare"` with its own compact label. Add test coverage that proves compare entries do not fall back to narration and do not display a generic `Lookup` label. Leave retry/undo/edit affordances intact, but keep all streaming/opening/finalizing copy outside the prose blocks by rendering those statuses as `kind="progress"` support blocks using the exact existing Phase 45 text. Do not change `GamePage` message persistence, do not introduce HTML storage, and do not turn ordinary statuses into assistant messages.</action>
  <verify>
    <automated>npx vitest run frontend/components/game/__tests__/narrative-log.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run frontend/components/game/__tests__/narrative-log.test.tsx` passes.
    - `rg -n "RichTextMessage|SpecialMessageBlock" frontend/components/game/narrative-log.tsx` returns matches.
    - `rg -n "\\[Lookup:|power_profile|compare" frontend/components/game/narrative-log.tsx frontend/components/game/__tests__/narrative-log.test.tsx` returns matches.
    - `rg -n "The storyteller is weaving the scene|The opening scene is taking shape|The world is still resolving" frontend/components/game/narrative-log.tsx` returns matches.
    - The narrative-log tests prove compare/power-profile entries render as a compact compare block and never fall back to narration or a generic `Lookup` label.
  </acceptance_criteria>
  <done>`NarrativeLog` now renders narration, player text, and support messages as distinct surfaces while preserving the existing Phase 45 streaming/finalization truth.</done>
</task>

</tasks>

<verification>
Run the focused renderer and narrative-log tests. Then run `gitnexus_detect_changes({ repo: "WorldForge", scope: "all" })` and confirm the diff is limited to the new gameplay text helpers/renderers plus the expected `NarrativeLog` wiring. Confirm the gameplay log now has bounded RP formatting, distinct player blocks, compact lookup/compare/system/progress blocks, explicit compare labeling for `power_profile`, and no HTML or editor creep.
</verification>

<success_criteria>
Phase 50 has a safe rendering foundation: narration feels like a reader surface, player text and support messages are visually distinct, and stream-time partial markup stays stable.
</success_criteria>

<output>
After completion, create `.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-01-SUMMARY.md`
</output>
