---
phase: 50-gameplay-text-presentation-and-rich-readability
plan: 04
type: execute
wave: 3
depends_on:
  - 50-01
  - 50-02
  - 50-03
files_modified:
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/routes/chat.ts
  - backend/src/routes/__tests__/chat.test.ts
  - frontend/lib/api.ts
  - frontend/lib/use-settings.ts
  - frontend/lib/__tests__/api.test.ts
  - frontend/app/game/page.tsx
  - frontend/components/game/narrative-log.tsx
  - frontend/components/game/__tests__/narrative-log.test.tsx
  - frontend/app/game/__tests__/page.test.tsx
autonomous: false
requirements:
  - UX-01
must_haves:
  truths:
    - "If the storyteller provider returns separate reasoning text, the runtime transports it on its own non-canonical SSE lane."
    - "If no separate reasoning exists, no reasoning event or disclosure block appears."
    - "The `/game` UI only shows a `Raw reasoning` disclosure when persisted settings allow it and the current assistant message has reasoning text."
    - "Reasoning stays out of canonical narration and is not persisted into ordinary chat history."
  artifacts:
    - path: "backend/src/engine/turn-processor.ts"
      provides: "separate `reasoning` turn event emission without polluting visible narration"
      contains: "\"reasoning\"|reasoningText"
    - path: "backend/src/routes/chat.ts"
      provides: "SSE transport for the new `reasoning` event"
      contains: "event: reasoning"
    - path: "frontend/lib/api.ts"
      provides: "frontend SSE parser support for reasoning payloads"
      contains: "onReasoning|case \"reasoning\""
    - path: "frontend/components/game/narrative-log.tsx"
      provides: "optional assistant-only `Raw reasoning` disclosure surface"
      contains: "Raw reasoning|details|summary"
  key_links:
    - from: "backend/src/engine/turn-processor.ts"
      to: "backend/src/routes/chat.ts"
      via: "reasoning must remain a distinct SSE event instead of appended narration"
      pattern: "\"reasoning\"|event: reasoning"
    - from: "backend/src/routes/chat.ts"
      to: "frontend/lib/api.ts"
      via: "the frontend parser must consume the new event without disturbing existing narrative, lookup, or done handling"
      pattern: "reasoning|onReasoning"
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/narrative-log.tsx"
      via: "page-level assistant metadata must gate the disclosure with persisted `showRawReasoning` settings"
      pattern: "showRawReasoning|debugReasoning|Raw reasoning"
---

<objective>
Finish Phase 50 by transporting provider reasoning separately and rendering it as an optional `/game` disclosure.

Purpose: implement D-21 through D-23 on top of the persisted D-24 settings gate, while keeping reasoning non-canonical and transport-safe.
Output: separate reasoning SSE transport plus assistant-only `/game` disclosure rendering that respects the global settings toggle.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-CONTEXT.md
@.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-RESEARCH.md
@.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-VALIDATION.md
@backend/src/engine/turn-processor.ts
@backend/src/engine/__tests__/turn-processor.test.ts
@backend/src/routes/chat.ts
@backend/src/routes/__tests__/chat.test.ts
@frontend/lib/api.ts
@frontend/lib/use-settings.ts
@frontend/lib/__tests__/api.test.ts
@frontend/app/game/page.tsx
@frontend/components/game/narrative-log.tsx
@frontend/components/game/__tests__/narrative-log.test.tsx
@frontend/app/game/__tests__/page.test.tsx
@.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-03-SUMMARY.md

<interfaces>
From `backend/src/engine/turn-processor.ts`:
```ts
export interface TurnEvent {
  type:
    | "oracle_result"
    | "scene-settling"
    | "narrative"
    | "state_update"
    | "quick_actions"
    | "auto_checkpoint"
    | "finalizing_turn"
    | "done"
    | "error";
  data: unknown;
}
```

From `frontend/lib/api.ts`:
```ts
export interface TurnSSEHandlers {
  onNarrative: (text: string) => void;
  onOracleResult: (result: { chance: number; roll: number; outcome: string; reasoning: string }) => void;
  onStateUpdate: (update: { tool: string; args: unknown; result: unknown }) => void;
  onQuickActions: (actions: Array<{ label: string; action: string }>) => void;
  onDone: () => void;
  onError: (error: string) => void;
}
```
</interfaces>
</context>

<gitnexus_safety>
Before editing existing symbols in this plan, run `gitnexus_impact({ repo: "WorldForge", target: "TurnEvent", direction: "upstream" })`, `gitnexus_impact({ repo: "WorldForge", target: "parseTurnSSE", direction: "upstream" })`, `gitnexus_impact({ repo: "WorldForge", target: "useSettings", direction: "upstream" })`, `gitnexus_impact({ repo: "WorldForge", target: "GamePage", direction: "upstream" })`, and `gitnexus_impact({ repo: "WorldForge", target: "NarrativeLog", direction: "upstream" })`. If implementation requires touching any additional named helper inside `backend/src/routes/chat.ts`, resolve that symbol first with GitNexus and run `gitnexus_impact` before editing it. Report the d=1 callers, affected processes, and risk level before changing those symbols.
Before closeout, run `gitnexus_detect_changes({ repo: "WorldForge", scope: "all" })` and confirm only the expected reasoning transport and `/game` disclosure symbols/processes for Plan `50-04` changed.
</gitnexus_safety>

<tasks>

<task type="auto" tdd="true">
  <name>Task 50-04-01: Add separate reasoning transport through the turn processor and SSE parser</name>
  <files>backend/src/engine/turn-processor.ts, backend/src/engine/__tests__/turn-processor.test.ts, backend/src/routes/chat.ts, backend/src/routes/__tests__/chat.test.ts, frontend/lib/api.ts, frontend/lib/__tests__/api.test.ts</files>
  <read_first>.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-RESEARCH.md, backend/src/engine/turn-processor.ts, backend/src/engine/__tests__/turn-processor.test.ts, backend/src/routes/chat.ts, backend/src/routes/__tests__/chat.test.ts, frontend/lib/api.ts, frontend/lib/__tests__/api.test.ts</read_first>
  <behavior>
    - Test 1: the turn-processor seam locks the exact installed `generateText(...)` reasoning field and timing model used by this repo's current `ai` package / provider path.
    - Test 2: when the storyteller provider returns separate reasoning text, the stream emits a distinct `reasoning` event without polluting `narrative`.
    - Test 3: when no reasoning exists, no `reasoning` event is emitted.
    - Test 4: the frontend SSE parser handles the new event without regressing `narrative`, `lookup_result`, or `done`.
  </behavior>
  <action>Per D-21 through D-23, add a non-canonical reasoning lane instead of stuffing reasoning into `ChatMessage.content`. Start in `backend/src/engine/__tests__/turn-processor.test.ts`: inspect the installed `ai` package result shape already used by this repo's storyteller `generateText(...)` calls and lock the exact separate-reasoning field name there before changing transport code. Then, in `backend/src/engine/turn-processor.ts`, extend `TurnEvent.type` with `"reasoning"` and read that exact field from the storyteller result. If the installed provider/SDK path exposes reasoning only after completion rather than incrementally, emit a single `reasoning` event after the visible narration decision is finalized and before `done`; do not invent token-by-token reasoning streaming. When a non-empty reasoning string exists, yield `type: "reasoning"` with payload `{ text: "<raw reasoning>" }`. Do not append that reasoning string through `appendChatMessages`, do not merge it into `narrativeText`, and do not persist it to `chat_history.json`. In `backend/src/routes/chat.ts`, stream the new SSE event literally as `event: reasoning`. In `frontend/lib/api.ts`, add `onReasoning?: (payload: { text: string }) => void` to `TurnSSEHandlers` and handle `case "reasoning"` inside `parseTurnSSE`. Update the engine, backend, and frontend parser tests to lock the exact field/timing seam plus the separate-lane behavior. Do not widen the canonical shared chat-history contract just to carry reasoning metadata.</action>
  <verify>
    <automated>npx vitest run backend/src/engine/__tests__/turn-processor.test.ts backend/src/routes/__tests__/chat.test.ts frontend/lib/__tests__/api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run backend/src/engine/__tests__/turn-processor.test.ts backend/src/routes/__tests__/chat.test.ts frontend/lib/__tests__/api.test.ts` passes.
    - `backend/src/engine/__tests__/turn-processor.test.ts` names the concrete installed storyteller reasoning field/shape being consumed instead of leaving the extraction seam implicit.
    - `rg -n "\"reasoning\"|reasoningText|event: reasoning" backend/src/engine/turn-processor.ts backend/src/routes/chat.ts` returns matches.
    - `rg -n "onReasoning|case \"reasoning\"" frontend/lib/api.ts` returns matches.
    - Tests prove no reasoning event is emitted when the provider returns no separate reasoning text.
  </acceptance_criteria>
  <done>The runtime transports provider reasoning on its own SSE lane without contaminating canonical narration or stored history.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 50-04-01A: Verify reasoning transport before `/game` disclosure wiring</name>
  <files>backend/src/engine/turn-processor.ts, backend/src/engine/__tests__/turn-processor.test.ts, backend/src/routes/chat.ts, backend/src/routes/__tests__/chat.test.ts, frontend/lib/api.ts, frontend/lib/__tests__/api.test.ts</files>
  <action>Pause after Task 50-04-01 and prove the separate reasoning lane is stable before any `/game` disclosure or settings-gated UI wiring begins. This checkpoint keeps the merged plan, but forces transport/parser correctness to be accepted as its own slice before frontend disclosure work expands the diff.</action>
  <action>This checkpoint is the hard scope boundary for Plan `50-04`: after Task `50-04-01`, execution must either stop for approval or continue only into Task `50-04-02` within the existing `files_modified` lists already declared in this plan. Do not add backend or frontend files beyond the current Task `50-04-01` and Task `50-04-02` file lists during execution; if extra files become necessary, stop and revise the plan first.</action>
  <what-built>Separate backend/frontend reasoning transport is in place through `TurnEvent`, `event: reasoning`, and `parseTurnSSE`, but `/game` disclosure wiring has not started yet.</what-built>
  <how-to-verify>
    1. Run `npx vitest run backend/src/engine/__tests__/turn-processor.test.ts backend/src/routes/__tests__/chat.test.ts frontend/lib/__tests__/api.test.ts`.
    2. Confirm the engine + transport/parser tests pass before any `/game` UI files are edited for reasoning disclosure.
    3. Inspect the current diff and confirm it is limited to `backend/src/engine/turn-processor.ts`, `backend/src/engine/__tests__/turn-processor.test.ts`, `backend/src/routes/chat.ts`, `backend/src/routes/__tests__/chat.test.ts`, `frontend/lib/api.ts`, and `frontend/lib/__tests__/api.test.ts`.
    4. Resume only if the reasoning lane is separate from canonical narration, the concrete installed reasoning field is covered by the engine test, and no `/game` disclosure wiring has been mixed into the transport change set.
  </how-to-verify>
  <verify>
    <automated>npx vitest run backend/src/engine/__tests__/turn-processor.test.ts backend/src/routes/__tests__/chat.test.ts frontend/lib/__tests__/api.test.ts</automated>
  </verify>
  <done>Reasoning transport/parsing is approved as a standalone slice, with a clean diff limited to the backend SSE lane and frontend parser files, before `/game` disclosure wiring starts.</done>
  <resume-signal>Type `approved` to continue with Task 50-04-02, or describe the transport/parser issue that must be corrected first.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 50-04-02: Gate assistant-only `/game` reasoning disclosure behind the persisted settings toggle</name>
  <files>frontend/lib/use-settings.ts, frontend/app/game/page.tsx, frontend/components/game/narrative-log.tsx, frontend/components/game/__tests__/narrative-log.test.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <read_first>.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-CONTEXT.md, frontend/lib/use-settings.ts, frontend/app/game/page.tsx, frontend/components/game/narrative-log.tsx, frontend/components/game/__tests__/narrative-log.test.tsx, frontend/app/game/__tests__/page.test.tsx</read_first>
  <behavior>
    - Test 1: `/game` only shows a disclosure labeled `Raw reasoning` when `ui.showRawReasoning === true` and the current assistant message has non-empty reasoning text.
    - Test 2: user, system, lookup, and progress messages never show a reasoning disclosure.
    - Test 3: when the toggle is off or reasoning text is absent, no empty placeholder disclosure is rendered.
  </behavior>
  <action>Per D-21 through D-24, keep reasoning as debug-only assistant metadata. In `frontend/lib/use-settings.ts` and `frontend/app/game/page.tsx`, load the persisted settings once through the existing settings client/hook and keep a local `showRawReasoning` boolean. Maintain a parallel assistant-message metadata lane such as `debugReasoningByMessage` or an additive local view model field like `debugReasoning?: string | null`, but do not widen the shared/persisted `ChatMessage` contract. When a `reasoning` event arrives, attach it only to the currently streaming or most recent assistant message. In `frontend/components/game/narrative-log.tsx`, render a native `<details>` disclosure labeled `Raw reasoning` under assistant narration only when `showRawReasoning` is true and that assistant message has non-empty `debugReasoning`; render the body inside a compact monospaced block with a bounded height/scroll treatment so very long provider reasoning stays inspectable without overwhelming the log. Update `narrative-log` and `/game` tests so they prove the disclosure appears only when enabled and present. Never render a reasoning block for user, system, lookup, or progress messages, and never show an empty disclosure placeholder.</action>
  <verify>
    <automated>npx vitest run frontend/components/game/__tests__/narrative-log.test.tsx frontend/app/game/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run frontend/components/game/__tests__/narrative-log.test.tsx frontend/app/game/__tests__/page.test.tsx` passes.
    - `rg -n "showRawReasoning|Raw reasoning|details|debugReasoning" frontend/lib/use-settings.ts frontend/app/game/page.tsx frontend/components/game/narrative-log.tsx` returns matches.
    - The UI tests prove no reasoning block is shown when the toggle is false or when the provider returns no reasoning payload.
  </acceptance_criteria>
  <done>`/game` can show optional raw reasoning as a debug disclosure under assistant messages only, with the persisted settings gate enforced.</done>
</task>

</tasks>

<verification>
Run the chat-route, SSE-parser, narrative-log, and `/game` page suites. Then run `gitnexus_detect_changes({ repo: "WorldForge", scope: "all" })` and confirm the diff is limited to the expected reasoning transport and assistant-disclosure surfaces, with no file growth beyond the existing Task `50-04-01` and Task `50-04-02` file lists.
</verification>

<success_criteria>
Phase 50 closes with a transport-safe, settings-gated raw reasoning disclosure that stays outside canonical narration and only appears when separate provider reasoning is actually available.
</success_criteria>

<output>
After completion, create `.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-04-SUMMARY.md`
</output>
