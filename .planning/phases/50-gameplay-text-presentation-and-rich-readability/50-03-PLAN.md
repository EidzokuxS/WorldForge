---
phase: 50-gameplay-text-presentation-and-rich-readability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/types.ts
  - shared/src/settings.ts
  - backend/src/settings/manager.ts
  - backend/src/routes/schemas.ts
  - backend/src/routes/__tests__/settings.test.ts
  - frontend/app/(non-game)/settings/page.tsx
  - frontend/app/(non-game)/settings/__tests__/page.test.tsx
  - frontend/components/settings/gameplay-tab.tsx
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "A persisted global settings toggle controls whether raw provider reasoning may appear in `/game`."
    - "Legacy or missing settings files default the toggle to `false` instead of dropping or inventing the field."
    - "The Settings screen exposes a dedicated Gameplay tab with a debug-only `Show raw reasoning` switch."
  artifacts:
    - path: "shared/src/types.ts"
      provides: "shared UI settings contract for reasoning visibility"
      contains: "UiConfig|showRawReasoning|ui:"
    - path: "shared/src/settings.ts"
      provides: "default settings with reasoning visibility disabled"
      contains: "showRawReasoning: false"
    - path: "backend/src/settings/manager.ts"
      provides: "normalized persisted settings contract for the new UI field"
      contains: "normalizeSettings|saveSettings|showRawReasoning"
    - path: "frontend/components/settings/gameplay-tab.tsx"
      provides: "dedicated gameplay settings surface for the toggle"
      contains: "Show raw reasoning"
  key_links:
    - from: "shared/src/types.ts"
      to: "backend/src/settings/manager.ts"
      via: "the shared `ui.showRawReasoning` contract must normalize and persist through the backend settings authority"
      pattern: "UiConfig|showRawReasoning|normalizeSettings|saveSettings"
    - from: "backend/src/routes/schemas.ts"
      to: "backend/src/routes/__tests__/settings.test.ts"
      via: "settings route payload validation must round-trip the new `ui.showRawReasoning` field"
      pattern: "settingsPayloadSchema|showRawReasoning"
    - from: "frontend/components/settings/gameplay-tab.tsx"
      to: "frontend/app/(non-game)/settings/page.tsx"
      via: "the new Gameplay tab must mount the persisted toggle in the existing settings load/save flow so `settings.ui.showRawReasoning` survives save and reload"
      pattern: "Gameplay|Show raw reasoning|ui.showRawReasoning"
---

<objective>
Create the persisted settings gate for Phase 50 raw-reasoning visibility before transport or `/game` disclosure work begins.

Purpose: implement D-24 and the persisted contract required for D-21 through D-23 without mixing it with runtime transport/rendering scope.
Output: shared/backend `ui.showRawReasoning` settings contract plus a dedicated Gameplay settings tab with the global toggle.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-CONTEXT.md
@.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-RESEARCH.md
@.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-VALIDATION.md
@shared/src/types.ts
@shared/src/settings.ts
@backend/src/settings/manager.ts
@backend/src/routes/schemas.ts
@backend/src/routes/__tests__/settings.test.ts
@frontend/app/(non-game)/settings/page.tsx
@frontend/app/(non-game)/settings/__tests__/page.test.tsx
@frontend/components/settings/research-tab.tsx

<interfaces>
From `shared/src/types.ts`:
```ts
export interface Settings {
  providers: Provider[];
  judge: RoleConfig;
  storyteller: RoleConfig;
  generator: RoleConfig;
  embedder: RoleConfig;
  fallback: FallbackConfig;
  images: ImageConfig;
  research: ResearchConfig;
}
```

From `shared/src/settings.ts`:
```ts
export function createDefaultSettings(): Settings;
```

From `frontend/app/(non-game)/settings/page.tsx`:
```ts
export default function SettingsPage();
```
</interfaces>
</context>

<gitnexus_safety>
Before editing existing symbols in this plan, run `gitnexus_impact({ repo: "WorldForge", target: "Settings", direction: "upstream" })`, `gitnexus_impact({ repo: "WorldForge", target: "createDefaultSettings", direction: "upstream" })`, `gitnexus_impact({ repo: "WorldForge", target: "normalizeSettings", direction: "upstream" })`, `gitnexus_impact({ repo: "WorldForge", target: "saveSettings", direction: "upstream" })`, `gitnexus_impact({ repo: "WorldForge", target: "settingsPayloadSchema", direction: "upstream" })`, and `gitnexus_impact({ repo: "WorldForge", target: "SettingsPage", direction: "upstream" })`. Report the d=1 callers, affected processes, and risk level before editing.
Before closeout, run `gitnexus_detect_changes({ repo: "WorldForge", scope: "all" })` and confirm only the expected settings-contract and settings-UI symbols/processes for Plan `50-03` changed.
</gitnexus_safety>

<tasks>

<task type="auto" tdd="true">
  <name>Task 50-03-01: Extend the shared and backend settings contract for raw reasoning visibility</name>
  <files>shared/src/types.ts, shared/src/settings.ts, backend/src/settings/manager.ts, backend/src/routes/schemas.ts, backend/src/routes/__tests__/settings.test.ts</files>
  <read_first>.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-CONTEXT.md, shared/src/types.ts, shared/src/settings.ts, backend/src/settings/manager.ts, backend/src/routes/schemas.ts, backend/src/routes/__tests__/settings.test.ts</read_first>
  <behavior>
    - Test 1: the shared settings contract persists `ui.showRawReasoning` with default `false`.
    - Test 2: the settings route accepts and returns `ui.showRawReasoning` without dropping it on save.
    - Test 3: legacy or partial settings payloads normalize the new field back to `false`.
  </behavior>
  <action>Per D-24, extend the persisted settings contract instead of creating a local-only flag. In `shared/src/types.ts`, add `export interface UiConfig { showRawReasoning: boolean; }` and add `ui: UiConfig` to `Settings`; keep `UiConfig` as the dedicated namespace for future gameplay/debug toggles rather than adding another root boolean later. In `shared/src/settings.ts`, set the default to `ui: { showRawReasoning: false }`. In `backend/src/settings/manager.ts`, normalize, rebind, and save the new field so missing or legacy settings files fall back to `false`. In `backend/src/routes/schemas.ts`, extend `settingsPayloadSchema` with a top-level `ui` object containing `showRawReasoning: z.boolean()`. Update `backend/src/routes/__tests__/settings.test.ts` so GET/POST assertions prove the field survives the full route round-trip. Keep the frontend on the shared `Settings` contract; do not create or widen a separate frontend-only settings mirror unless execution finds one already present and actively used in this repo. Do not hide this flag inside `research`, `storyteller`, or provider config, and do not make it frontend-only.</action>
  <verify>
    <automated>npx vitest run backend/src/routes/__tests__/settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run backend/src/routes/__tests__/settings.test.ts` passes.
    - `rg -n "UiConfig|showRawReasoning|ui:" shared/src/types.ts shared/src/settings.ts backend/src/settings/manager.ts backend/src/routes/schemas.ts` returns matches.
    - The backend settings tests prove legacy payloads and saved payloads both resolve `ui.showRawReasoning`.
  </acceptance_criteria>
  <done>The repo has one authoritative persisted settings contract for raw reasoning visibility, with default `false` and backend round-trip coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 50-03-02: Add the dedicated Gameplay settings tab and reasoning toggle UI</name>
  <files>frontend/app/(non-game)/settings/page.tsx, frontend/app/(non-game)/settings/__tests__/page.test.tsx, frontend/components/settings/gameplay-tab.tsx</files>
  <read_first>.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-CONTEXT.md, frontend/app/(non-game)/settings/page.tsx, frontend/app/(non-game)/settings/__tests__/page.test.tsx, frontend/components/settings/research-tab.tsx</read_first>
  <behavior>
    - Test 1: the Settings screen exposes a dedicated `Gameplay` tab rather than burying the toggle inside research/provider forms.
    - Test 2: the `Show raw reasoning` switch is bound to `settings.ui.showRawReasoning` through the existing settings page state and save flow, not a local-only component flag.
    - Test 3: the page test proves the toggle is labeled as debug-only, is hidden by default, and persists across save plus reload/rehydration.
  </behavior>
  <action>Per D-24 and D-22, add a dedicated gameplay-facing control instead of overloading the research tab. Create `frontend/components/settings/gameplay-tab.tsx` with one switch labeled `Show raw reasoning` and help text that states the block is debug-only, hidden by default, and never changes canonical narration. Wire that component into `frontend/app/(non-game)/settings/page.tsx` as a new `Gameplay` tab by binding the switch directly to the existing page-level settings object at `settings.ui.showRawReasoning`, using the current settings fetch/save path instead of local component state or local storage. Update `frontend/app/(non-game)/settings/__tests__/page.test.tsx` so the tab and switch are explicitly asserted, the save request proves `ui.showRawReasoning` is written through the normal settings submit flow, and a follow-up reload/rehydration assertion proves the saved value remains selected after the page loads settings again. Do not stash this preference in local storage and do not bury it under provider or research configuration.</action>
  <verify>
    <automated>npx vitest run frontend/app/(non-game)/settings/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run frontend/app/(non-game)/settings/__tests__/page.test.tsx` passes.
    - `rg -n "Gameplay|Show raw reasoning" frontend/app/(non-game)/settings/page.tsx frontend/components/settings/gameplay-tab.tsx frontend/app/(non-game)/settings/__tests__/page.test.tsx` returns matches.
    - The UI copy explicitly says the feature is debug-only and does not alter canonical narration.
    - The page test proves the save payload includes `ui.showRawReasoning` and that a save-then-reload path preserves the toggle state.
  </acceptance_criteria>
  <done>The Settings UI exposes the persisted raw-reasoning toggle in a dedicated Gameplay tab, wired through the existing save flow so `settings.ui.showRawReasoning` survives reload instead of living in local-only UI state.</done>
</task>

</tasks>

<verification>
Run the backend settings test and the frontend settings page test. Then run `gitnexus_detect_changes({ repo: "WorldForge", scope: "all" })` and confirm the diff is limited to the expected shared/backend settings contract plus the new settings UI surface.
</verification>

<success_criteria>
Phase 50 has a real persisted gate for raw reasoning visibility, and the Settings screen exposes it clearly without mixing it into unrelated research/provider controls.
</success_criteria>

<output>
After completion, create `.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-03-SUMMARY.md`
</output>
