---
phase: 50-gameplay-text-presentation-and-rich-readability
plan: 02
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - frontend/app/game/page.tsx
  - frontend/components/game/action-bar.tsx
  - frontend/components/game/location-panel.tsx
  - frontend/components/game/character-panel.tsx
  - frontend/components/game/quick-actions.tsx
  - frontend/components/game/__tests__/action-bar.test.tsx
  - frontend/components/game/__tests__/location-panel.test.tsx
  - frontend/app/game/__tests__/page.test.tsx
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "The `/game` shell reads like one intentional gameplay surface rather than a plain app layout with a text log in the middle."
    - "Player input remains a normal textarea, but it is sticky, easier to scan, and clearly advertises the lightweight RP markup contract."
    - "Location, character, and quick-action panels are easier to scan and remain visually aligned with the central narration surface."
    - "Presentation polish does not break busy-state locking, streaming, travel feedback, or current-scene panel behavior."
  artifacts:
    - path: "frontend/app/game/page.tsx"
      provides: "reader-centered shell layout with sticky input region and aligned rails"
      contains: "LocationPanel|NarrativeLog|CharacterPanel|QuickActions|ActionBar"
    - path: "frontend/components/game/action-bar.tsx"
      provides: "plain-text action input with RP hint copy and upgraded sticky treatment"
      contains: "Detail your next action|\"speech\", *action*, **emphasis**"
    - path: "frontend/components/game/location-panel.tsx"
      provides: "scan-friendly location rail aligned to the hybrid concept without changing scene semantics"
      contains: "Recent Happenings|Nearby Signs|People Here|Paths"
    - path: "frontend/components/game/character-panel.tsx"
      provides: "scan-friendly character rail with clearer equipment and inventory hierarchy"
      contains: "Character|Equipment|Inventory"
    - path: "frontend/components/game/quick-actions.tsx"
      provides: "higher-contrast quick-action rail that still unlocks only after authoritative completion"
      contains: "onAction|disabled"
  key_links:
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/action-bar.tsx"
      via: "the page keeps the sticky input mounted below the reader surface while preserving busy-state control"
      pattern: "ActionBar|turnPhase|sceneProgress"
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/location-panel.tsx"
      via: "the current-scene and travel contracts from Phase 46 must survive the new presentation layer"
      pattern: "currentScene|connectedPaths|travelFeedback"
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/quick-actions.tsx"
      via: "quick actions remain buffered until authoritative completion, even after the new shell styling"
      pattern: "QuickActions|quickActions|disabled"
---

<objective>
Apply the concept-driven shell, panel, and input treatment to `/game` without changing gameplay logic.

Purpose: implement D-07 through D-09 and D-15 through D-20 by pulling the page layout, sticky input, and side rails toward `docs/ui_concept_hybrid.html`.
Output: a more readable central reader shell, improved sticky action bar, and clearer side-panel/action-chip hierarchy.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-CONTEXT.md
@.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-RESEARCH.md
@.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-VALIDATION.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-04-SUMMARY.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-03-SUMMARY.md
@docs/ui_concept_hybrid.html
@frontend/app/game/page.tsx
@frontend/components/game/action-bar.tsx
@frontend/components/game/location-panel.tsx
@frontend/components/game/character-panel.tsx
@frontend/components/game/quick-actions.tsx
@frontend/components/game/__tests__/action-bar.test.tsx
@frontend/components/game/__tests__/location-panel.test.tsx
@frontend/app/game/__tests__/page.test.tsx

<interfaces>
From `frontend/components/game/action-bar.tsx`:
```ts
interface ActionBarProps {
  value: string;
  disabled?: boolean;
  isLoading: boolean;
  turnPhase?: "idle" | "streaming" | "finalizing";
  onChange: (value: string) => void;
  onSubmit: () => void;
}
```

From `frontend/components/game/location-panel.tsx`:
```ts
interface LocationPanelProps {
  scene?: {
    id: string | null;
    name: string | null;
    broadLocationName: string | null;
    hintSignals?: string[];
  } | null;
  connectedPaths: ConnectedPathView[];
  npcsHere: Array<{ id: string; name: string; tier: string }>;
  itemsHere: Array<{ id: string; name: string }>;
}
```
</interfaces>
</context>

<gitnexus_safety>
Before editing existing symbols in this plan, run `gitnexus_impact({ repo: "WorldForge", target: "GamePage", direction: "upstream" })`, `gitnexus_impact({ repo: "WorldForge", target: "ActionBar", direction: "upstream" })`, `gitnexus_impact({ repo: "WorldForge", target: "LocationPanel", direction: "upstream" })`, `gitnexus_impact({ repo: "WorldForge", target: "CharacterPanel", direction: "upstream" })`, and `gitnexus_impact({ repo: "WorldForge", target: "QuickActions", direction: "upstream" })`. Report the d=1 callers, affected processes, and risk level before changing those symbols.
Before closeout, run `gitnexus_detect_changes({ repo: "WorldForge", scope: "all" })` and confirm only the expected `/game` shell, panel, and quick-action symbols/processes for Plan `50-02` changed.
</gitnexus_safety>

<tasks>

<task type="auto" tdd="true">
  <name>Task 50-02-01: Rebuild the central gameplay shell and sticky input presentation</name>
  <files>frontend/app/game/page.tsx, frontend/components/game/action-bar.tsx, frontend/components/game/__tests__/action-bar.test.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <read_first>.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-CONTEXT.md, docs/ui_concept_hybrid.html, frontend/app/game/page.tsx, frontend/components/game/action-bar.tsx, frontend/components/game/__tests__/action-bar.test.tsx, frontend/app/game/__tests__/page.test.tsx</read_first>
  <behavior>
    - Test 1: `/game` keeps the same transport and busy-state logic, but the central layout becomes a reader-centered shell with a sticky action region.
    - Test 2: the action bar stays a plain textarea, preserves Enter-to-submit and finalizing lockouts, and exposes the RP markup hint text.
    - Test 3: travel feedback, toolbar actions, and scene/finalizing states still render after the shell changes.
  </behavior>
  <action>Update `frontend/app/game/page.tsx` and `frontend/components/game/action-bar.tsx` to match the hybrid concept shell without changing gameplay flow. In `page.tsx`, keep the existing toolbar buttons, travel feedback strip, and three-column composition, but present them inside a more deliberate shell: dark backdrop, centered reader column, side rails that visually read as `320px` concept-style panels on large screens, and a bottom fade/sticky input region rather than a flat border-top footer. Keep `NarrativeLog`, `LocationPanel`, `CharacterPanel`, `QuickActions`, and `ActionBar` in the same data flow order. In `action-bar.tsx`, keep the control a normal `Textarea` only; do not add formatting buttons, live previews, or WYSIWYG behavior. Change the placeholder to `Detail your next action...`, add explicit helper copy `RP markup: "speech", *action*, **emphasis**`, keep the existing finalizing warning, and use utilitarian input typography instead of narrative serif italics. Preserve the current character counter, submit button behavior, Enter/Shift+Enter handling, and busy-state disabling.</action>
  <verify>
    <automated>npx vitest run frontend/components/game/__tests__/action-bar.test.tsx frontend/app/game/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run frontend/components/game/__tests__/action-bar.test.tsx frontend/app/game/__tests__/page.test.tsx` passes.
    - `rg -n "Detail your next action|RP markup: \"speech\", \\*action\\*, \\*\\*emphasis\\*\\*" frontend/components/game/action-bar.tsx` returns matches.
    - `rg -n "LocationPanel|NarrativeLog|CharacterPanel|QuickActions|ActionBar" frontend/app/game/page.tsx` returns matches.
    - `rg -n "finalizing|travelFeedback|Settings|Saves" frontend/app/game/page.tsx` returns matches.
  </acceptance_criteria>
  <done>The center gameplay shell and sticky input now feel deliberate and readable while remaining a plain-text, transport-safe action surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 50-02-02: Upgrade panel and quick-action readability without changing scene semantics</name>
  <files>frontend/components/game/location-panel.tsx, frontend/components/game/character-panel.tsx, frontend/components/game/quick-actions.tsx, frontend/components/game/__tests__/location-panel.test.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <read_first>.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-04-SUMMARY.md, docs/ui_concept_hybrid.html, frontend/components/game/location-panel.tsx, frontend/components/game/character-panel.tsx, frontend/components/game/quick-actions.tsx, frontend/components/game/__tests__/location-panel.test.tsx, frontend/app/game/__tests__/page.test.tsx</read_first>
  <behavior>
    - Test 1: location and character rails become easier to scan through stronger section hierarchy and clearer list/card treatment.
    - Test 2: the broad-location plus immediate-scene contract from Phase 46 remains intact.
    - Test 3: quick actions keep their authoritative post-turn unlock behavior while gaining clearer chip-style presentation.
  </behavior>
  <action>Upgrade `frontend/components/game/location-panel.tsx`, `frontend/components/game/character-panel.tsx`, and `frontend/components/game/quick-actions.tsx` to match the hybrid concept hierarchy with concrete surface rules. Use small uppercase section labels, clearer spacing, and stronger list/card grouping for `Recent Happenings`, `Nearby Signs`, `People Here`, `Paths`, `Equipment`, and `Inventory`. Keep the location panel’s broad-location description as the main place text and the immediate-scene label as a separate bounded layer exactly as Phase 46 established; do not collapse the scene label into the location name or reintroduce broad-location NPC rendering. In `quick-actions.tsx`, keep the existing `actions`, `onAction`, and `disabled` contract, but render the actions as higher-contrast chips/tiles that read as support actions under the narrative surface rather than generic app buttons. Do not change what data is shown, when buttons unlock, or how travel/path interactions work.</action>
  <verify>
    <automated>npx vitest run frontend/components/game/__tests__/location-panel.test.tsx frontend/app/game/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run frontend/components/game/__tests__/location-panel.test.tsx frontend/app/game/__tests__/page.test.tsx` passes.
    - `rg -n "Recent Happenings|Nearby Signs|People Here|Paths" frontend/components/game/location-panel.tsx` returns matches.
    - `rg -n "Equipment|Inventory|Character" frontend/components/game/character-panel.tsx` returns matches.
    - `rg -n "actions|disabled|onAction" frontend/components/game/quick-actions.tsx` returns matches.
  </acceptance_criteria>
  <done>The side rails and quick-action row are materially easier to scan and still preserve the repaired scene-truth and authoritative-completion contracts.</done>
</task>

</tasks>

<verification>
Run the page, action-bar, and location-panel suites. Then run `gitnexus_detect_changes({ repo: "WorldForge", scope: "all" })` and confirm the diff is limited to the expected `/game` shell, rail, and action-input surfaces. Confirm the shell feels more like the concept reference, the input remains plain text, and Phase 45/46 transport and scene contracts still hold.
</verification>

<success_criteria>
`/game` now has an intentional reader shell, a scan-friendly sticky input, and clearer side rails without reopening any gameplay or scene-authority logic.
</success_criteria>

<output>
After completion, create `.planning/phases/50-gameplay-text-presentation-and-rich-readability/50-02-SUMMARY.md`
</output>
