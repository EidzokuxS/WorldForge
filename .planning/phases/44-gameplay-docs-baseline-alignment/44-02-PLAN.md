---
phase: 44-gameplay-docs-baseline-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/mechanics.md
autonomous: true
requirements:
  - DOCA-01
  - DOCA-02
must_haves:
  truths:
    - "`docs/mechanics.md` now treats canonical structured records as authoritative and derived tags as shorthand/compatibility output per D-16."
    - "The gameplay baseline describes supported target-aware Oracle behavior, bounded opening-state mechanics, live reflection semantics, and repaired travel/location-state contracts without overstating unresolved seams."
    - "The docs explicitly tighten world-information-flow wording into a bounded contract built around prompt-visible proximity, faction context, recent happenings, and elapsed-time cues instead of vague omniscience."
    - "The gameplay tool tables in `docs/mechanics.md` are audited and updated where needed so movement, event logging, and reveal-location behavior match the live runtime."
    - "Stale mechanics claims such as targetTags always existing implicitly, reflection threshold 15, or tags-as-only-ontology are rewritten or explicitly deprecated."
  artifacts:
    - path: "docs/mechanics.md"
      provides: "normative gameplay mechanics baseline aligned with Phases 29, 30, 40, 42, and 43"
      contains: "canonical"
  key_links:
    - from: "docs/mechanics.md"
      to: "backend/src/engine/target-context.ts"
      via: "Oracle target-support language matches the supported character/item/location-object runtime seam"
      pattern: "character|item|location/object|fallback"
    - from: "docs/mechanics.md"
      to: "backend/src/engine/start-condition-runtime.ts"
      via: "opening-state wording matches bounded status-flag-backed mechanics and expiry rules"
      pattern: "three-tick|status flags|companions|immediate situation"
    - from: "docs/mechanics.md"
      to: "backend/src/engine/reflection-agent.ts"
      via: "reflection wording matches live threshold and structured-state-first outcomes"
      pattern: "10|belief|goal|relationship"
    - from: "docs/mechanics.md"
      to: "backend/src/engine/prompt-assembler.ts"
      via: "world-information-flow wording stays aligned with live prompt-visible context instead of implying a complete hidden-knowledge simulation"
      pattern: "proximity|faction|recent happenings|elapsed time"
    - from: "docs/mechanics.md"
      to: "backend/src/engine/tool-executor.ts"
      via: "tool tables and movement/event semantics match live validation, travel-cost, and reveal-location behavior"
      pattern: "move_to|log_event|reveal_location|importance|travelCost"
---

<objective>
Rewrite `docs/mechanics.md` into the normative gameplay baseline for the repaired runtime.

Purpose: satisfy DOCA-02 and the mechanics-heavy portions of DOCA-01 by making gameplay semantics truthful for canonical character state, target-aware Oracle input, start-condition runtime effects, reflection, world simulation, and travel/location behavior.
Output: one mechanics document that future planning can trust without re-reading old pre-repair assumptions.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-gameplay-docs-baseline-alignment/44-CONTEXT.md
@.planning/phases/44-gameplay-docs-baseline-alignment/44-RESEARCH.md
@.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-01-SUMMARY.md
@.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-02-SUMMARY.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-04-SUMMARY.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-05-SUMMARY.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-06-SUMMARY.md
@.planning/phases/40-live-reflection-progression-triggers/40-02-SUMMARY.md
@docs/mechanics.md
@backend/src/character/record-adapters.ts
@backend/src/character/runtime-tags.ts
@backend/src/engine/target-context.ts
@backend/src/engine/start-condition-runtime.ts
@backend/src/engine/reflection-agent.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/location-graph.ts
@backend/src/engine/tool-executor.ts

<interfaces>
From `backend/src/character/record-adapters.ts`:
```ts
export function projectPlayerRecord(record: CharacterRecord): PlayerRecordProjection
```

From `backend/src/engine/target-context.ts`:
```ts
export async function resolveActionTargetContext(...): Promise<{
  targetType: SupportedActionTargetType | "none";
  targetTags: string[];
  fallbackReason: string | null;
}>
```

From `backend/src/engine/start-condition-runtime.ts`:
```ts
const OPENING_EFFECT_TICK_CEILING = 3;
```

From `backend/src/engine/location-graph.ts`:
```ts
export type ResolvedTravelPath = {
  destinationId: string;
  locationIds: string[];
  edgeIds: string[];
  totalTravelCost: number;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the core mechanics contract around canonical state, Oracle targets, and opening-state effects</name>
  <read_first>
@docs/mechanics.md
@.planning/phases/44-gameplay-docs-baseline-alignment/44-CONTEXT.md
@.planning/phases/44-gameplay-docs-baseline-alignment/44-RESEARCH.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-CONTEXT.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-CONTEXT.md
@.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-01-SUMMARY.md
@.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-02-SUMMARY.md
@backend/src/character/record-adapters.ts
@backend/src/character/runtime-tags.ts
@backend/src/engine/target-context.ts
@backend/src/engine/start-condition-runtime.ts
  </read_first>
  <files>docs/mechanics.md</files>
  <acceptance_criteria>
    - `rg -n "canonical|structured record|derived tags|compatibility output" docs/mechanics.md` finds canonical-record-first wording.
    - `rg -n "character, item, and location/object|fallback|target-aware" docs/mechanics.md` finds explicit supported-target Oracle boundaries.
    - `rg -n "arrivalMode|startingVisibility|entryPressure|companions|immediateSituation|three-tick" docs/mechanics.md` finds bounded start-condition runtime-effect wording.
  </acceptance_criteria>
  <action>Rewrite the opening sections of `docs/mechanics.md` per D-02, D-07, D-08, D-09, D-16, D-17, and D-18. Replace the old `Tag-Based System` framing that implies flat tags are the whole ontology with explicit canonical-record-first language: structured character/runtime records are authoritative, and derived tags remain shorthand or compatibility output for prompts, Oracle context, and older surfaces. In the Oracle sections, keep the five-step explanation but replace vague target-tag language with the live supported set: target-aware rulings are implemented for supported `character`, `item`, and `location/object` targets, and unsupported or unresolved targets fall back honestly to non-targeted evaluation. In the start-condition sections, document the live bounded mechanics for `arrivalMode`, `startingVisibility`, `entryPressure`, `companions`, and `immediateSituation`, including status-flag-backed opening effects, prompt-visible constraints, and deterministic expiry on location change, explicit resolution, or the three-tick ceiling. Do not claim a free-form rules engine, generic faction-targeting vertical, or fully solved inventory authority.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n 'canonical|structured record|derived tags|character, item, and location/object|fallback|arrivalMode|startingVisibility|entryPressure|companions|immediateSituation|three-tick' docs/mechanics.md; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx vitest run backend/src/character/__tests__/record-adapters.test.ts backend/src/routes/__tests__/character.test.ts backend/src/engine/__tests__/turn-processor.test.ts backend/src/engine/__tests__/oracle.test.ts"</automated>
  </verify>
  <done>The first half of `docs/mechanics.md` now describes the live character-state, Oracle, and opening-state contracts accurately enough to serve as the normative gameplay baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Align reflection, tool contracts, and travel/location semantics with the repaired runtime</name>
  <read_first>
@docs/mechanics.md
@.planning/phases/44-gameplay-docs-baseline-alignment/44-CONTEXT.md
@.planning/phases/40-live-reflection-progression-triggers/40-01-SUMMARY.md
@.planning/phases/40-live-reflection-progression-triggers/40-02-SUMMARY.md
@.planning/phases/40-live-reflection-progression-triggers/40-03-SUMMARY.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-04-SUMMARY.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-05-SUMMARY.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-06-SUMMARY.md
@backend/src/engine/reflection-agent.ts
@backend/src/engine/location-graph.ts
@backend/src/engine/tool-executor.ts
@backend/src/routes/campaigns.ts
@backend/src/engine/prompt-assembler.ts
  </read_first>
  <files>docs/mechanics.md</files>
  <acceptance_criteria>
    - `rg -n "threshold .*10|sum >= 10|belief|goal|relationship" docs/mechanics.md` finds corrected live reflection semantics instead of threshold 15 wording.
    - `rg -n "travel cost|connectedPaths|recent happenings|ephemeral|persistent sublocation|macro location|move_to|log_event|reveal_location|no-op|caller-supplied importance" docs/mechanics.md` finds the repaired travel/location contract and audited gameplay tool-table semantics.
    - `rg -n "proximity|faction|recent happenings|elapsed time|world-information-flow|knowledge model|does not guarantee|omniscience" docs/mechanics.md` finds explicit bounded wording for world-information flow instead of vague realism claims.
    - `rg -n "Phase 38|inventory authority|pending" docs/mechanics.md` finds an explicit bounded pending note rather than solved-authority language.
  </acceptance_criteria>
  <action>Rewrite the remaining mechanics sections so they match the repaired runtime and explicit deprecation policy. In NPC/reflection/world-simulation sections, replace the stale `sum >= 15` reflection threshold with the live threshold `10`, and describe reflection as evidence-driven structured-state updates that usually change beliefs, goals, or relationships before wealth/skill progression. Explicitly audit and update the gameplay tool tables while you are in `docs/mechanics.md`: `move_to` should describe location-graph pathing, travel-cost semantics, and zero-cost self-travel/no-op behavior from Phase 43-06; `log_event` should describe caller-supplied importance rather than an LLM-scored importance contract; and `reveal_location` should note the ephemeral-scene plus bidirectional-edge implications where that affects gameplay understanding. Add an explicit world-information-flow rewrite that explains the bounded live contract without claim IDs: the runtime gives the prompt proximity/location context, faction/world state, recent happenings, and elapsed-time cues, but does not guarantee a fully simulated per-actor knowledge model or perfect omniscience boundaries. Update world and location semantics to describe macro locations, persistent sublocations, and ephemeral scene locations in one graph, with `connectedPaths`, travel cost in ticks, and per-location recent happenings as live player-visible semantics grounded in `location-graph.ts` and the current world payloads. Add an explicit bounded note that inventory/equipment authority still has a Phase 38 seam: live behavior uses items tables plus canonical records, but the docs must not present authority as fully closed. Do not leave the old tag-universal language, threshold-15 language, flat instant-travel implications, or vague "the prompt probably knows enough" world-knowledge language anywhere in the file.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n 'threshold .*10|sum >= 10|belief|goal|relationship|travel cost|connectedPaths|recent happenings|ephemeral|persistent sublocation|macro location|move_to|log_event|reveal_location|no-op|caller-supplied importance|proximity|faction|elapsed time|world-information-flow|knowledge model|does not guarantee|omniscience|Phase 38|inventory authority|pending' docs/mechanics.md; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx vitest run backend/src/engine/__tests__/reflection-budget.test.ts backend/src/engine/__tests__/reflection-agent.test.ts backend/src/engine/__tests__/prompt-assembler.test.ts backend/src/routes/__tests__/campaigns.test.ts frontend/app/game/__tests__/page.test.tsx"</automated>
  </verify>
  <done>`docs/mechanics.md` now carries the repaired live mechanics baseline for reflection, gameplay tool tables, travel, local history, bounded world-information-flow semantics, and unresolved seams instead of stale pre-v1.1 claims.</done>
</task>

</tasks>

<verification>
Read `docs/mechanics.md` front to back after the rewrite. Confirm every gameplay-significant claim now follows the repaired runtime: canonical records first, honest target support boundaries, bounded opening effects, reflection threshold 10, and travel/local-history semantics that match Phase 43.
</verification>

<success_criteria>
`docs/mechanics.md` becomes the single normative gameplay baseline for character state, Oracle semantics, start conditions, reflection, and travel/location behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/44-gameplay-docs-baseline-alignment/44-02-SUMMARY.md`
</output>
