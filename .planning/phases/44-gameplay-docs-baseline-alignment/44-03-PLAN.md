---
phase: 44-gameplay-docs-baseline-alignment
plan: 03
type: execute
wave: 2
depends_on:
  - 44-01
  - 44-02
files_modified:
  - docs/memory.md
  - .planning/phases/44-gameplay-docs-baseline-alignment/44-CLAIM-RESOLUTION.md
autonomous: true
requirements:
  - DOCA-01
  - DOCA-03
must_haves:
  truths:
    - "`docs/memory.md` accurately describes the live SQLite/vector/prompt contract, including vector-only lore retrieval, top-3 lore, top-5 episodic memory, caller-supplied event importance, and checkpoint-complete restore."
    - "The docs explicitly preserve a bounded pending note for Phase 38 inventory/equipment authority instead of claiming a fully solved inventory truth model."
    - "Phase 36 Group B and Group C claims have a traceable resolution artifact that points to implemented behavior, deprecation, or explicit pending notes, with a brief rationale for why Group D is excluded."
  artifacts:
    - path: "docs/memory.md"
      provides: "normative runtime, retrieval, prompt, and save/restore baseline"
      contains: "EPISODIC MEMORY"
    - path: ".planning/phases/44-gameplay-docs-baseline-alignment/44-CLAIM-RESOLUTION.md"
      provides: "claim-by-claim resolution map for Phase 36 Group B and Group C carry-forward work"
      contains: "B1"
  key_links:
    - from: "docs/memory.md"
      to: "backend/src/engine/prompt-assembler.ts"
      via: "retrieval counts and prompt block names match the live prompt assembly contract"
      pattern: "EPISODIC MEMORY|LORE CONTEXT|top 3|top 5"
    - from: "docs/memory.md"
      to: "backend/src/campaign/restore-bundle.ts"
      via: "save/load wording matches the config-inclusive authoritative bundle contract"
      pattern: "config.json|checkpoint|bundle"
    - from: ".planning/phases/44-gameplay-docs-baseline-alignment/44-CLAIM-RESOLUTION.md"
      to: ".planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-HANDOFF.md"
      via: "resolution checklist preserves traceability from every Group B/C item to the final docs"
      pattern: "B1|B2|B3|B4|B5|B6|C1|C2|C3|C4|C5"
---

<objective>
Finish the normative runtime-doc rewrite and leave behind a traceable proof that every elevated claim was resolved honestly.

Purpose: satisfy DOCA-03 and close DOCA-01 verification by rewriting `docs/memory.md` around the real retrieval/memory/prompt/save contract and recording a claim-resolution map tied back to Phase 36 Group B/C.
Output: corrected `docs/memory.md` plus a phase-local claim-resolution checklist for auditability.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-gameplay-docs-baseline-alignment/44-CONTEXT.md
@.planning/phases/44-gameplay-docs-baseline-alignment/44-RESEARCH.md
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-HANDOFF.md
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-RUNTIME-MATRIX.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-01-SUMMARY.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-02-SUMMARY.md
@.planning/phases/40-live-reflection-progression-triggers/40-03-SUMMARY.md
@docs/memory.md
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/reflection-agent.ts
@backend/src/engine/tool-executor.ts
@backend/src/routes/chat.ts
@backend/src/campaign/checkpoints.ts
@backend/src/campaign/restore-bundle.ts

<interfaces>
From `backend/src/engine/prompt-assembler.ts`:
```ts
const loreCards = await searchLoreCards(queryVector, 3);
const events = await searchEpisodicEvents(queryVector, currentTick, 5);
```

From `backend/src/engine/reflection-agent.ts`:
```ts
export const REFLECTION_THRESHOLD = 10;
```

From `frontend/lib/api.ts`:
```ts
case "finalizing_turn": handlers.onFinalizing?.(); break;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite `docs/memory.md` around the live retrieval, prompt, and restore contracts</name>
  <read_first>
@docs/memory.md
@.planning/phases/44-gameplay-docs-baseline-alignment/44-CONTEXT.md
@.planning/phases/44-gameplay-docs-baseline-alignment/44-RESEARCH.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-01-SUMMARY.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-02-SUMMARY.md
@.planning/phases/40-live-reflection-progression-triggers/40-03-SUMMARY.md
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/reflection-agent.ts
@backend/src/engine/tool-executor.ts
@backend/src/routes/chat.ts
@backend/src/campaign/checkpoints.ts
@backend/src/campaign/restore-bundle.ts
  </read_first>
  <files>docs/memory.md</files>
  <acceptance_criteria>
    - `rg -n "EPISODIC MEMORY|NPC STATES|top 3|top 5|vector-only|caller-supplied importance|config.json|tool call|error results" docs/memory.md` finds the corrected live retrieval, prompt-template, validation, and restore contract.
    - `rg -n "RETRIEVED MEMORIES|NPC STATE|keyword \\+ vector|Wiki scraper|sum >= 15" docs/memory.md` returns no stale wording.
    - `rg -n "Phase 38|inventory authority|pending" docs/memory.md` finds an explicit bounded unresolved-seam note.
  </acceptance_criteria>
  <action>Rewrite `docs/memory.md` per D-02, D-04, D-05, D-09, D-13, D-14, and D-15 so it becomes the normative runtime/retrieval baseline. Keep SQLite as the source of truth, but rewrite inventory/equipment language into bounded honest wording: live behavior uses the `items` table plus canonical records, yet Phase 38 still tracks a remaining authority seam. Verify the live inventory-validation/item-reference contract against `backend/src/engine/tool-executor.ts` before rewriting that section: document the narrower truthful rule that tool-mediated entity, item, and location references are validated when specific tool calls resolve names and invalid references return error results, but do not present the backend as a blanket narration filter that rejects every hypothetical nonexistent item mention. Rewrite episodic-memory sections so importance is caller-supplied, reflection triggers at threshold 10, same-turn committed evidence can be read before embeddings exist, and retrieval uses composite scoring with a top-5 episodic window. Rewrite lore retrieval to say vector-only lore search returns the top 3 cards. Rewrite prompt assembly to use the real block names, especially `[EPISODIC MEMORY]` instead of `[RETRIEVED MEMORIES]`, and fix prompt-template naming drift such as `[NPC STATES]` where needed so the docs mirror the live prompt assembler. Rewrite save/load/checkpoint sections so the authoritative bundle includes `state.db`, `config.json`, `chat_history.json`, and vectors only where appropriate. Do not preserve wiki-scraper, keyword+vector, threshold-15, or checkpoint-omits-config language anywhere in the file.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n 'EPISODIC MEMORY|NPC STATES|top 3|top 5|vector-only|caller-supplied importance|config.json|tool call|error results|Phase 38|inventory authority|pending' docs/memory.md; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx vitest run backend/src/engine/__tests__/prompt-assembler.test.ts backend/src/vectors/__tests__/episodic-events.test.ts backend/src/campaign/__tests__/checkpoints.test.ts backend/src/engine/__tests__/reflection-budget.test.ts backend/src/engine/__tests__/reflection-agent.test.ts"</automated>
  </verify>
  <done>`docs/memory.md` now matches the live retrieval, prompt, reflection, and restore contracts closely enough to serve as the normative runtime baseline for future phases.</done>
</task>

<task type="auto">
  <name>Task 2: Record claim-by-claim resolution for every elevated Phase 36 Group B/C item</name>
  <read_first>
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-HANDOFF.md
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-RUNTIME-MATRIX.md
@.planning/phases/44-gameplay-docs-baseline-alignment/44-CONTEXT.md
@docs/concept.md
@docs/mechanics.md
@docs/memory.md
@docs/tech_stack.md
  </read_first>
  <files>.planning/phases/44-gameplay-docs-baseline-alignment/44-CLAIM-RESOLUTION.md</files>
  <acceptance_criteria>
    - `rg -n "^## Group B|^## Group C|B1|B2|B3|B4|B5|B6|C1|C2|C3|C4|C5" .planning/phases/44-gameplay-docs-baseline-alignment/44-CLAIM-RESOLUTION.md` finds every elevated handoff item.
    - `rg -n "TURN-19|STATE-14|STATE-18|STATE-19|MEM-17|CHAR-04|MEM-09|TURN-02|WORLD-12|WORLD-13|STATE-01|MEM-11|CHAR-05" .planning/phases/44-gameplay-docs-baseline-alignment/44-CLAIM-RESOLUTION.md` finds the underlying claim IDs anchored to final resolution notes.
    - `rg -n "Group D|out of scope|deferred" .planning/phases/44-gameplay-docs-baseline-alignment/44-CLAIM-RESOLUTION.md` finds a brief rationale for why Group D is not part of this Phase 44 artifact.
  </acceptance_criteria>
  <action>Create `.planning/phases/44-gameplay-docs-baseline-alignment/44-CLAIM-RESOLUTION.md` as the execution-time proof artifact for DOCA-01. Use two sections, `Group B` and `Group C`, and list every elevated handoff item `B1` through `B6` and `C1` through `C5`. For each item, include: the original claim IDs from `36-HANDOFF.md`; whether the final Phase 44 resolution is `implemented behavior`, `explicit deprecation/replacement`, or `explicit bounded pending note`; and the exact final doc locations that carry that resolution, using concrete file-and-section references in `docs/concept.md`, `docs/mechanics.md`, `docs/memory.md`, or `docs/tech_stack.md`. Include the specific already-known drift areas from D-05 where relevant: wiki/Fandom ingest, Solid Slate layout, scaffold counts, `[RETRIEVED MEMORIES]`, reflection-threshold wording, transport descriptions, and any prompt-template naming corrections that Phase 44 actually touched. Add a brief note explaining that Group D is intentionally excluded because the Phase 36 handoff marked it as later/deferred work outside DOCA-01 scope. Do not write a vague narrative summary; make this a concise checklist/table that a future verifier can scan line by line.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n '^## Group B|^## Group C|B1|B2|B3|B4|B5|B6|C1|C2|C3|C4|C5|TURN-19|STATE-14|STATE-18|STATE-19|MEM-17|CHAR-04|MEM-09|TURN-02|WORLD-12|WORLD-13|STATE-01|MEM-11|CHAR-05|Group D|out of scope|deferred' .planning/phases/44-gameplay-docs-baseline-alignment/44-CLAIM-RESOLUTION.md; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx vitest run backend/src/character/__tests__/record-adapters.test.ts backend/src/engine/__tests__/prompt-assembler.test.ts backend/src/routes/__tests__/campaigns.test.ts frontend/lib/__tests__/api.test.ts"</automated>
  </verify>
  <done>Phase 44 has a traceable claim-resolution artifact that proves every elevated Phase 36 Group B/C item was resolved as implemented, deprecated, or explicitly bounded pending, and explains why Group D is intentionally excluded.</done>
</task>

</tasks>

<verification>
Read `docs/memory.md` and the claim-resolution artifact together. Confirm runtime contract language is truthful and that every elevated Group B/C item is explicitly mapped to its final documentation outcome.
</verification>

<success_criteria>
Future planners can use `docs/memory.md` plus the claim-resolution artifact as a trustworthy runtime baseline without re-running the Phase 36 reconciliation audit.
</success_criteria>

<output>
After completion, create `.planning/phases/44-gameplay-docs-baseline-alignment/44-03-SUMMARY.md`
</output>
