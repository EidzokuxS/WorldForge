# 81-05 PLAN: Settled Packet And Narration Handoff

## Scope

Ensure narrator sees only settled post-execution truth plus compact GM guardrails. Planned but failed/skipped effects must not be narrated as completed.

## Requirements Covered

P81-R6, P81-R7.

## Dependencies

Depends on: 81-04.
Blocks: 81-06.

## Read First

- `81-CONTEXT.md`
- `81-SPEC.md`
- `backend/src/engine/narrator-packet.ts`
- `backend/src/engine/prompt-assembler.ts`
- `backend/src/engine/visible-narration-output-guard.ts`
- `frontend/app/game/page.tsx`

## File Ownership

- `backend/src/engine/narrator-packet.ts`
- `backend/src/engine/prompt-assembler.ts`
- `backend/src/engine/__tests__/narrator-packet.test.ts`
- frontend status copy only if stage names change

## GitNexus Impact Targets Before Source Edits

```text
gitnexus_impact({ target: "buildNarratorPacket", direction: "upstream" })
gitnexus_impact({ target: "assembleFinalNarrationPrompt", direction: "upstream" })
gitnexus_impact({ target: "assembleAuthoritativeScene", direction: "upstream" })
gitnexus_impact({ target: "runVisibleNarrationWithPacketGuard", direction: "upstream" })
```

## Tasks

1. Add tests where checklist planned an effect but tool step failed/skipped.
2. Ensure narrator packet includes successful visible effects and skip/failure only when player-visible.
3. Keep private forecast terms and hidden effects out.
4. Feed GM Read guardrails as compact constraints, not second plan.
5. Decouple narrator packet types from `ScenePlan` and `gm-beat-plan` projection types where Phase 81 output no longer uses them.
6. Add deterministic failed-effect-leak fixture: failed tool effect summary must be absent from narrator packet and final-visible narration.
7. Update UI stage labels/tests for `gm-read`, `gm-action-checklist`, `tool-step`, `settled-packet`, and `final-narration`.
8. Test migration: enumerate narrator-packet, prompt-assembler, visible-narration, and frontend stage tests that survive/change.

## Verify

```bash
npm --prefix backend exec vitest run src/engine/__tests__/narrator-packet.test.ts src/engine/__tests__/prompt-assembler.test.ts
npm --prefix backend run typecheck
npm --prefix frontend run typecheck
```

## Acceptance Criteria

- Narrator never describes unexecuted effects as real.
- Final prose remains grounded in settled truth.
- Player-visible stage copy remains understandable.
- Private/offscreen terms are excluded after direct, mutating, and failed-step paths.
