---
phase: 18-character-creation-and-game-start-e2e
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified: []
autonomous: true
requirements: [CHAR-BROWSER-PARSE, CHAR-BROWSER-GENERATE, CHAR-BROWSER-IMPORT, CHAR-BROWSER-SAVE, CHAR-BROWSER-GAMESTART]

must_haves:
  truths:
    - "User can navigate from world review to character creation page"
    - "User can create a character by typing a free-text description and clicking Parse"
    - "User can create a character by selecting AI Generate and entering a concept"
    - "User can import a V2/V3 SillyTavern card file and see parsed character"
    - "User can save character and get redirected to the game page"
    - "Game page loads with player character data visible in sidebar"
  artifacts:
    - path: "e2e/18-02-char-browser-e2e.ts"
      provides: "Browser E2E test script for character creation flow"
  key_links:
    - from: "frontend/app/campaign/[id]/character/page.tsx"
      to: "POST /api/worldgen/parse-character"
      via: "fetch on Parse button click"
      pattern: "fetch.*parse-character"
    - from: "frontend/app/campaign/[id]/character/page.tsx"
      to: "POST /api/worldgen/save-character"
      via: "fetch on Save button click"
      pattern: "fetch.*save-character"
    - from: "POST /api/worldgen/save-character"
      to: "frontend/app/game/page.tsx"
      via: "redirect after save completes"
      pattern: "router\\.push.*game"
---

<objective>
Verify the full character creation and game start flow through real browser interaction with Playwright MCP. Test all 3 creation modes (parse description, AI generate, import V2 card), character save, starting location resolution, redirect to game page, and first-turn readiness.

Purpose: End-to-end browser verification that the user experience works from world review through character creation to game start. API verification in Plan 01 confirms backend; this plan confirms frontend integration.
Output: Browser E2E test scripts + screenshots proving the flow works.
</objective>

<execution_context>
@C:/Users/robra/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-character-creation-and-game-start-e2e/18-01-SUMMARY.md
@.planning/phases/17-world-generation-pipeline-e2e/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Browser E2E — Parse Description mode + Save + Game Start</name>
  <files>e2e/18-02-char-browser-e2e.ts</files>
  <read_first>
    - frontend/app/campaign/[id]/character/page.tsx (character creation page component)
    - frontend/components/character-creation/CharacterForm.tsx (form component with 3 modes)
    - frontend/components/character-creation/CharacterCard.tsx (character preview card)
    - frontend/app/game/page.tsx (game page that loads after character save)
  </read_first>
  <action>
Using Playwright MCP browser, navigate through the full character creation flow:

1. **Navigate to character creation page** — Use a campaign created in Phase 17 (or create a new one via API). Navigate to `/campaign/{id}/character`. Verify the page loads with the character creation form visible.

2. **Test Parse Description mode:**
   - Enter a rich character description in the text area: "A battle-scarred orc shaman named Grukh who communicates with spirits of the dead. Tall and imposing with ritual scarification covering his arms. Carries a bone staff and wears pelts of animals he has hunted. Wise but feared by his own tribe."
   - Click the Parse/Submit button
   - Wait for GLM to process (may take 30-60 seconds)
   - Verify CharacterCard appears showing: name (Grukh or similar), race (orc), appearance details, tags
   - Screenshot the character card for quality assessment

3. **Save character and verify game start:**
   - Click the Save/Begin Adventure button
   - Wait for save-character + resolve-starting-location to complete
   - Verify redirect to `/game` page (or `/campaign/{id}/game`)
   - Verify game page loads with player name visible in sidebar
   - Verify chat area shows premise or is ready for first action
   - Screenshot the game page showing character in sidebar

Quality check: Character card must show coherent, thematically matching character. Game page must load without errors. Score >= 4.5/5.
  </action>
  <verify>
    <automated>curl -s http://localhost:3001/api/campaigns/active | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); const p=d.player||d.data?.player; console.log('player:', p?.name); process.exit(p?.name ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Character creation page renders with form visible at /campaign/{id}/character
    - Parse Description mode: text entry -> Parse -> CharacterCard with name, race, tags visible
    - Save button: character saved -> redirect to game page
    - Game page: player name visible in sidebar, chat area loaded
    - No JS errors in console during the flow
    - Quality >= 4.5/5 for generated character
  </acceptance_criteria>
  <done>Parse Description -> Save -> Game Start flow works end-to-end in real browser with GLM</done>
</task>

<task type="auto">
  <name>Task 2: Browser E2E — AI Generate mode + Import V2 Card mode</name>
  <files>e2e/18-02-char-browser-e2e.ts</files>
  <read_first>
    - frontend/components/character-creation/CharacterForm.tsx (mode switching, generate and import panels)
    - frontend/lib/v2-card-parser.ts (V2/V3 card parsing logic)
    - frontend/lib/api.ts (API helper functions)
  </read_first>
  <action>
Test the remaining 2 character creation modes via Playwright MCP browser. For each mode, use a fresh campaign (create via API or delete existing player):

1. **Test AI Generate mode:**
   - Navigate to character creation page for a campaign
   - Switch to the AI Generate tab/mode
   - Enter a concept prompt: "A cunning halfling merchant who secretly runs a smuggling ring"
   - Click Generate
   - Wait for GLM to process (30-60 seconds)
   - Verify CharacterCard appears with generated character: name, race (halfling), thematic tags
   - Screenshot for quality assessment
   - Do NOT save (need clean campaign for V2 import test, or use a different campaign)

2. **Test Import V2 Card mode:**
   - Navigate to character creation page for a campaign (fresh or different)
   - Switch to the Import V2 Card tab/mode
   - Create a minimal V2 card JSON in memory and upload it via file input. The V2 card structure:
     ```json
     {
       "spec": "chara_card_v2",
       "spec_version": "2.0",
       "data": {
         "name": "Aria Nightwhisper",
         "description": "A mysterious elven ranger who patrols the borders",
         "personality": "Stoic, observant, loyal to nature",
         "first_mes": "You spot a figure in green emerging from the treeline...",
         "tags": ["elf", "ranger", "nature"],
         "character_book": null
       }
     }
     ```
   - Upload the card file
   - Verify CharacterCard appears with imported data: name "Aria Nightwhisper", race/description parsed
   - Screenshot for quality assessment

3. **Verify mode switching works cleanly:**
   - Switch between all 3 modes and verify form resets/updates appropriately
   - No stale data from previous mode appears

Quality check: AI Generate must produce thematically appropriate character. V2 Import must correctly parse all card fields. Both modes >= 4.5/5.
  </action>
  <verify>
    <automated>curl -s -X POST http://localhost:3001/api/worldgen/generate-character -H "Content-Type: application/json" -d '{"role":"player","concept":"test rogue"}' | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log('name:', d.name); process.exit(d.name ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - AI Generate mode: concept entry -> Generate -> CharacterCard with thematically matching character
    - Import V2 mode: V2 card JSON uploaded -> CharacterCard with correct name and parsed fields
    - Mode switching works without stale data or errors
    - Both modes produce characters with name, race/description, and tags
    - Quality >= 4.5/5 for both generated and imported characters
  </acceptance_criteria>
  <done>All 3 character creation modes verified working in real browser — Parse, Generate, Import V2 all produce valid characters with GLM</done>
</task>

</tasks>

<verification>
Full character creation page works in browser with all 3 modes. The critical path (create character -> save -> game start) completes without errors. Game page loads with player data after character save.
</verification>

<success_criteria>
- All 3 character creation modes produce valid characters in browser
- Parse Description -> CharacterCard with name, race, tags
- AI Generate -> CharacterCard with thematically matching content
- Import V2 -> CharacterCard with imported card data
- Save -> redirect to game page with player in sidebar
- Quality >= 4.5/5 for all generated characters
- Screenshots captured for each mode as evidence
</success_criteria>

<output>
After completion, create `.planning/phases/18-character-creation-and-game-start-e2e/18-02-SUMMARY.md`
</output>
