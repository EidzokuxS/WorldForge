---
phase: 45-authoritative-scene-assembly-and-start-of-play-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
  - frontend/components/game/__tests__/narrative-log.test.tsx
  - frontend/app/game/__tests__/page.test.tsx
autonomous: true
requirements:
  - SCEN-01
must_haves:
  truths:
    - "Phase 45 starts from failing regressions that describe the intended opening-scene and single-pass scene-assembly contract before runtime changes begin."
    - "The repo has an explicit regression for the user-visible bug where `/game` renders `premise` as the opening scene when no messages exist."
    - "The repo has an explicit regression for the sequencing bug where visible narration can begin before authoritative scene settlement is complete."
    - "The repo has an explicit regression for repeated output blocks in final visible narration."
  artifacts:
    - path: "backend/src/engine/__tests__/turn-processor.test.ts"
      provides: "turn-ordering and duplicate-output contract coverage"
      contains: "defers visible narration until authoritative scene settlement|suppresses repeated narration blocks"
    - path: "backend/src/engine/__tests__/prompt-assembler.test.ts"
      provides: "final narration context contract coverage"
      contains: "scene effects|opening state|player-perceivable"
    - path: "frontend/components/game/__tests__/narrative-log.test.tsx"
      provides: "opening-surface regression coverage"
      contains: "does not render premise as opening narration"
    - path: "frontend/app/game/__tests__/page.test.tsx"
      provides: "live `/game` opening-surface regression coverage"
      contains: "premise|opening narration"
  key_links:
    - from: "backend/src/engine/__tests__/turn-processor.test.ts"
      to: "frontend/app/game/__tests__/page.test.tsx"
      via: "backend sequencing and frontend opening display jointly define SCEN-01"
      pattern: "single-pass|opening narration|premise"
---

<objective>
Lock the Phase 45 contract in failing tests before implementation: no premise-dump opening surface, no user-visible narration before authoritative scene settlement, and no repeated visible output blocks.

Purpose: turn the live gameplay complaints into executable regressions so later implementation cannot drift into a prompt-only or frontend-only fix.
Output: failing backend and frontend tests that pin SCEN-01.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-CONTEXT.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-RESEARCH.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-VALIDATION.md
@backend/src/engine/turn-processor.ts
@backend/src/engine/prompt-assembler.ts
@frontend/components/game/narrative-log.tsx
@frontend/app/game/page.tsx

<interfaces>
From `backend/src/engine/turn-processor.ts`:
```ts
export async function* processTurn(
  options: TurnOptions
): AsyncGenerator<TurnEvent>
```

From `frontend/components/game/narrative-log.tsx`:
```tsx
interface NarrativeLogProps {
  messages: ChatMessage[];
  premise: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing backend regressions for authoritative scene sequencing and duplicate suppression</name>
  <read_first>
    .planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-CONTEXT.md
    .planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-RESEARCH.md
    backend/src/engine/turn-processor.ts
    backend/src/engine/prompt-assembler.ts
    backend/src/engine/__tests__/turn-processor.test.ts
    backend/src/engine/__tests__/prompt-assembler.test.ts
  </read_first>
  <files>backend/src/engine/__tests__/turn-processor.test.ts, backend/src/engine/__tests__/prompt-assembler.test.ts</files>
  <acceptance_criteria>
    - `rg -n "defers visible narration until authoritative scene settlement|suppresses repeated narration blocks|player-perceivable" backend/src/engine/__tests__/turn-processor.test.ts backend/src/engine/__tests__/prompt-assembler.test.ts` finds new Phase 45 regressions.
    - `npm --prefix backend exec vitest run src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/prompt-assembler.test.ts` fails because the Phase 45 implementation is not wired yet.
  </acceptance_criteria>
  <action>Add failing backend regressions that force a runtime, not prompt-only, fix. In `turn-processor.test.ts`, add a case that proves visible `narrative` events must not be emitted from the hidden tool-driving pass before authoritative scene settlement is complete, and another case that repeated consecutive output blocks in the final visible narration are suppressed instead of being persisted verbatim. In `prompt-assembler.test.ts`, add a bounded contract case that final scene narration context must be built from authoritative opening/scene/perceivable-effect data rather than a premise-only fallback. Keep the tests implementation-agnostic enough to allow a dedicated `scene-assembly.ts` helper or equivalent, but specific enough that the current one-pass visible stream fails.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/prompt-assembler.test.ts</automated>
  </verify>
  <done>Backend regressions describe the required Phase 45 sequencing and duplicate-suppression contract clearly enough that implementation cannot hide behind prompt wording changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add failing frontend regressions for the no-premise opening surface</name>
  <read_first>
    .planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-CONTEXT.md
    .planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-RESEARCH.md
    frontend/components/game/narrative-log.tsx
    frontend/app/game/page.tsx
    frontend/components/game/__tests__/narrative-log.test.tsx
    frontend/app/game/__tests__/page.test.tsx
  </read_first>
  <files>frontend/components/game/__tests__/narrative-log.test.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <acceptance_criteria>
    - `rg -n "does not render premise as opening narration|neutral opening placeholder" frontend/components/game/__tests__/narrative-log.test.tsx frontend/app/game/__tests__/page.test.tsx` finds new Phase 45 regressions.
    - `npm --prefix frontend exec vitest run components/game/__tests__/narrative-log.test.tsx app/game/__tests__/page.test.tsx` fails because the current UI still renders premise as the opening scene surface.
  </acceptance_criteria>
  <action>Add failing frontend regressions that prove `/game` must not render `premise` as opening narration when there are no assistant messages yet. Cover both the leaf component (`NarrativeLog`) and the page-level initialization flow (`app/game/page.tsx`) so the eventual fix cannot pass by changing only one layer. Expect a neutral placeholder or loading/opening-state copy instead of premise text, and keep the tests grounded in the actual props/state shape already used by `/game`.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run components/game/__tests__/narrative-log.test.tsx app/game/__tests__/page.test.tsx</automated>
  </verify>
  <done>Frontend regressions pin the product contract that premise is background truth, not opening scene text.</done>
</task>

</tasks>

<verification>
Run both the backend and frontend targeted suites and confirm they fail for the right reasons: visible narration is still emitted too early, duplicate blocks are still possible, and `premise` is still rendered as the empty-state opening surface.
</verification>

<success_criteria>
Phase 45 starts from red tests that describe the real gameplay seams, not from vague prose complaints: scene text must be single-pass and runtime-grounded, and `/game` must stop treating premise as opening narration.
</success_criteria>

<output>
After completion, create `.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-01-SUMMARY.md`
</output>
