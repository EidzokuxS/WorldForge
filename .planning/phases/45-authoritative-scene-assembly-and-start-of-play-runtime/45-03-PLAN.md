---
phase: 45-authoritative-scene-assembly-and-start-of-play-runtime
plan: 03
type: execute
wave: 3
depends_on:
  - 45-02
files_modified:
  - frontend/components/game/narrative-log.tsx
  - frontend/app/game/page.tsx
  - frontend/components/game/__tests__/narrative-log.test.tsx
  - frontend/app/game/__tests__/page.test.tsx
autonomous: true
requirements:
  - SCEN-01
must_haves:
  truths:
    - "The `/game` opening surface no longer renders the campaign premise as if it were opening narration."
    - "Frontend scene presentation stays compatible with the new backend single-pass narration contract."
    - "Empty-state or pre-first-message UI uses neutral runtime-safe copy instead of lore-dump fallback."
    - "When a campaign has no assistant messages yet, `/game` actively requests the backend-generated opening scene instead of passively waiting forever."
    - "Frontend distinguishes hidden scene-settling/opening generation from post-narration finalization so quality-first backend work does not look like a frozen UI."
  artifacts:
    - path: "frontend/components/game/narrative-log.tsx"
      provides: "opening-surface contract aligned to Phase 45"
      contains: "Begin your adventure|opening"
    - path: "frontend/app/game/page.tsx"
      provides: "live `/game` state wiring aligned to backend single-pass narration and opening generation"
      contains: "NarrativeLog|turnPhase|opening"
    - path: "frontend/components/game/__tests__/narrative-log.test.tsx"
      provides: "leaf opening-surface regression coverage"
      contains: "premise as opening narration"
    - path: "frontend/app/game/__tests__/page.test.tsx"
      provides: "page-level Phase 45 presentation regression coverage"
      contains: "premise|opening narration"
  key_links:
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/narrative-log.tsx"
      via: "page and leaf component share the no-premise opening contract"
      pattern: "NarrativeLog|premise"
---

<objective>
Converge the `/game` frontend on the Phase 45 opening and single-pass scene contract after the backend runtime changes land.

Purpose: remove premise-dump opening behavior from the user-facing surface and keep frontend copy/loading states aligned with the new backend narration sequence.
Output: `/game` presentation that waits for runtime scene text instead of fabricating an opening from premise.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-CONTEXT.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-RESEARCH.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-VALIDATION.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-02-PLAN.md
@frontend/app/game/page.tsx
@frontend/components/game/narrative-log.tsx
@frontend/lib/api.ts

<interfaces>
From `frontend/components/game/narrative-log.tsx`:
```tsx
interface NarrativeLogProps {
  messages: ChatMessage[];
  premise: string;
  isStreaming: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove premise-as-opening behavior and align `/game` presentation with single-pass narration</name>
  <read_first>
    .planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-CONTEXT.md
    .planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-RESEARCH.md
    frontend/components/game/narrative-log.tsx
    frontend/app/game/page.tsx
    frontend/components/game/__tests__/narrative-log.test.tsx
    frontend/app/game/__tests__/page.test.tsx
  </read_first>
  <files>frontend/components/game/narrative-log.tsx, frontend/app/game/page.tsx, frontend/components/game/__tests__/narrative-log.test.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <acceptance_criteria>
    - `rg -n "premise \\|\\| \"Begin your adventure|premise as opening narration" frontend/components/game/narrative-log.tsx frontend/components/game/__tests__/narrative-log.test.tsx frontend/app/game/__tests__/page.test.tsx` no longer finds the old premise-fallback opening behavior.
    - `rg -n "opening|scene-settling|turnPhase" frontend/app/game/page.tsx frontend/app/game/__tests__/page.test.tsx` finds the explicit opening-request/progress handling path.
    - `npm --prefix frontend exec vitest run components/game/__tests__/narrative-log.test.tsx app/game/__tests__/page.test.tsx` passes.
  </acceptance_criteria>
  <action>Update the `/game` presentation layer so the opening scene surface is neutral until runtime scene text exists, but not passive forever. In `narrative-log.tsx`, remove the behavior that renders `premise` directly as the empty-state narrative. Replace it with neutral waiting/opening copy that does not masquerade as in-world scene text. In `page.tsx`, when the loaded campaign has zero assistant messages, explicitly trigger backend opening-scene generation (`POST /api/chat/opening` or the final execution equivalent) instead of relying on premise fallback or leaving the log empty. Surface a distinct opening/scene-settling state so a quality-first hidden pass can take time without looking like a broken UI. Keep whatever premise state is still needed for non-opening internal uses, but do not rely on it as a visible first scene fallback. Update the leaf and page tests to prove both the no-premise opening contract and the active opening-request path. Do not add rich-text styling or broader readability work here; keep this plan strictly about the no-premise opening and compatibility with the backend’s new single-pass narration sequence.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run components/game/__tests__/narrative-log.test.tsx app/game/__tests__/page.test.tsx</automated>
  </verify>
  <done>The frontend no longer turns premise into opening narration and stays aligned with backend-authored scene text as the sole opening source.</done>
</task>

</tasks>

<verification>
Run the frontend Phase 45 suite and confirm the empty-state surface is neutral while normal turns still render the backend-authored narration stream correctly.
</verification>

<success_criteria>
The first scene the player reads on `/game` now comes from runtime narration, not a UI fallback that dumps premise text into the narrative log.
</success_criteria>

<output>
After completion, create `.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-03-SUMMARY.md`
</output>
