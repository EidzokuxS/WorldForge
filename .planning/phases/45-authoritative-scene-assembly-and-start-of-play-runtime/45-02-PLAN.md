---
phase: 45-authoritative-scene-assembly-and-start-of-play-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
files_modified:
  - backend/src/engine/scene-assembly.ts
  - backend/src/engine/turn-processor.ts
  - backend/src/routes/chat.ts
  - backend/src/engine/storyteller-contract.ts
  - backend/src/engine/prompt-assembler.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
  - backend/src/routes/__tests__/chat.test.ts
autonomous: true
requirements:
  - SCEN-01
must_haves:
  truths:
    - "Visible narration now comes from a final settled-scene pass, not from the hidden tool-driving stream."
    - "Local present-scene settlement happens before visible narration, while off-screen/faction/reflection finalization remains rollback-critical post-narration work."
    - "The final visible narration is assembled from authoritative opening state, local scene facts, and player-perceivable same-turn effects."
    - "Repeated visible output blocks are prevented or collapsed before the assistant message is persisted."
    - "The hidden action-resolution pass is quality-first and may be non-streamed/slower, but the runtime must surface explicit scene-settling progress instead of leaving `/game` visually frozen."
    - "A typed scene-effects contract carries enough authoritative causal detail from hidden resolution into final narration so the second pass does not have to re-infer intent from raw state deltas."
    - "Opening-scene generation has an explicit backend trigger path instead of relying on premise fallback or an empty message log."
  artifacts:
    - path: "backend/src/engine/scene-assembly.ts"
      provides: "authoritative same-turn scene-effects assembly for final narration"
      contains: "perceivable|scene effects|opening"
    - path: "backend/src/engine/turn-processor.ts"
      provides: "two-stage storyteller sequencing and final visible narration pass"
      contains: "scene settlement|final narration|streamText"
    - path: "backend/src/routes/chat.ts"
      provides: "post-turn split between local pre-narration settlement and remaining rollback-critical finalization"
      contains: "tickPresentNpcs|simulateOffscreenNpcs|checkAndTriggerReflections"
    - path: "backend/src/engine/storyteller-contract.ts"
      provides: "bounded rules for hidden action-resolution pass and final visible narration pass"
      contains: "continuous narration pass|player-perceivable"
    - path: "backend/src/routes/chat.ts"
      provides: "opening-scene trigger and explicit scene-settling progress signal"
      contains: "opening|scene-settling|finalizing"
  key_links:
    - from: "backend/src/engine/scene-assembly.ts"
      to: "backend/src/engine/turn-processor.ts"
      via: "turn processor builds final narration input from settled scene effects"
      pattern: "scene effects|perceivable"
    - from: "backend/src/engine/turn-processor.ts"
      to: "backend/src/routes/chat.ts"
      via: "present-scene settlement moves ahead of final visible narration while off-screen finalization stays post-narration"
      pattern: "tickPresentNpcs|simulateOffscreenNpcs"
---

<objective>
Implement the backend Phase 45 runtime contract: hidden tool-driving resolution, authoritative local scene settlement, explicit opening-scene generation, final visible narration from settled state, and duplicate-block suppression.

Purpose: satisfy the causal-world and single-pass narration decisions from Phase 45 discussion without waiting for the later encounter-scope and writing-quality phases.
Output: backend turn flow and scene-assembly seam that make visible narration lag behind authoritative settlement by design, not by hope.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-CONTEXT.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-RESEARCH.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-VALIDATION.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-01-PLAN.md
@backend/src/engine/turn-processor.ts
@backend/src/routes/chat.ts
@backend/src/engine/storyteller-contract.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/npc-agent.ts
@backend/src/vectors/episodic-events.ts

<interfaces>
From `backend/src/engine/turn-processor.ts`:
```ts
export interface TurnSummary {
  tick: number;
  oracleResult: OracleResult;
  toolCalls: Array<{ tool: string; args: unknown; result: unknown }>;
  narrativeText: string;
}
```

From `backend/src/routes/chat.ts`:
```ts
function buildOnPostTurn(
  settings: Settings,
  campaignId: string,
  judgeProvider: ProviderConfig,
): ((summary: TurnSummary) => Promise<void>) | undefined
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an authoritative scene-assembly seam and split hidden resolution from visible narration</name>
  <read_first>
    .planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-CONTEXT.md
    .planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-RESEARCH.md
    backend/src/engine/turn-processor.ts
    backend/src/engine/prompt-assembler.ts
    backend/src/engine/storyteller-contract.ts
    backend/src/vectors/episodic-events.ts
  </read_first>
  <files>backend/src/engine/scene-assembly.ts, backend/src/engine/turn-processor.ts, backend/src/engine/storyteller-contract.ts, backend/src/engine/prompt-assembler.ts</files>
  <acceptance_criteria>
    - `rg -n "scene[- ]assembly|perceivable|final narration|hidden.*pass|tool-driving" backend/src/engine/scene-assembly.ts backend/src/engine/turn-processor.ts backend/src/engine/storyteller-contract.ts` finds the new Phase 45 seams.
    - `rg -n "player-perceivable|opening state|scene effects" backend/src/engine/prompt-assembler.ts backend/src/engine/scene-assembly.ts` finds explicit bounded final narration inputs instead of a premise-only fallback.
    - `rg -n "scene-settling|opening" backend/src/engine/turn-processor.ts backend/src/routes/chat.ts` finds explicit runtime progress/opening hooks for the hidden pass and start-of-play flow.
    - `npm --prefix backend exec vitest run src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/prompt-assembler.test.ts` passes.
  </acceptance_criteria>
  <action>Create a dedicated backend scene-assembly seam rather than continuing to stream user-visible prose from the tool-driving pass. Add `backend/src/engine/scene-assembly.ts` to assemble authoritative final-narration inputs from settled opening state, current location, local same-turn committed events, location-local recent context, and other player-perceivable scene effects. Make this seam explicit and typed enough to carry causal/intent metadata needed by the final narrator, not just raw deltas. In `turn-processor.ts`, split the current single Storyteller pass into two responsibilities: first, a hidden tool-driving/action-resolution pass that may still use tools but does not yield user-visible `narrative` events; second, a final visible narration pass that runs only after local scene settlement is complete. The hidden pass is quality-first; do not optimize it by downgrading fidelity just to chase latency. Instead, expose an explicit runtime progress signal (`scene-settling` or equivalent) so `/game` never looks frozen while the hidden pass and local settlement are running. Reuse existing prompt-assembly data where sensible, but do not require the final narrator to infer scene effects that the runtime can already assemble directly. Update `storyteller-contract.ts` so the hidden pass and final visible pass have distinct bounded rules. Suppress or collapse repeated output blocks before persisting the assistant message, using paragraph/block-aware normalization rather than naive raw-string equality. Add an explicit backend opening-scene generation path for campaigns with zero assistant messages, so removing premise fallback does not create an empty opening surface. Define the hidden-pass failure path clearly: if hidden resolution fails, no partial visible narration is persisted and the client receives a bounded recoverable error state. Do not solve broad encounter-scope, macro-location partitioning, or prose-style tuning in this plan.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/prompt-assembler.test.ts</automated>
  </verify>
  <done>The backend has a clean separation between hidden action resolution and final visible narration, and the latter is assembled from settled runtime state.</done>
</task>

<task type="auto">
  <name>Task 2: Move local present-scene settlement ahead of visible narration and leave global finalization post-narration</name>
  <read_first>
    .planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-CONTEXT.md
    .planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-RESEARCH.md
    backend/src/routes/chat.ts
    backend/src/engine/npc-agent.ts
    backend/src/engine/turn-processor.ts
    backend/src/routes/__tests__/chat.test.ts
    backend/src/engine/__tests__/turn-processor.test.ts
  </read_first>
  <files>backend/src/engine/turn-processor.ts, backend/src/routes/chat.ts, backend/src/routes/__tests__/chat.test.ts, backend/src/engine/__tests__/turn-processor.test.ts</files>
  <acceptance_criteria>
    - `rg -n "tickPresentNpcs" backend/src/engine/turn-processor.ts backend/src/routes/chat.ts` shows present-scene settlement owned before final narration instead of only in post-turn finalization.
    - `rg -n "simulateOffscreenNpcs|checkAndTriggerReflections|tickFactions" backend/src/routes/chat.ts` still shows rollback-critical post-narration world finalization for non-local work.
    - `rg -n "opening|scene-settling|SceneEffects" backend/src/engine/turn-processor.ts backend/src/routes/chat.ts backend/src/engine/scene-assembly.ts` finds the explicit opening/progress/typed-seam contract.
    - `npm --prefix backend exec vitest run src/engine/__tests__/turn-processor.test.ts src/routes/__tests__/chat.test.ts` passes.
  </acceptance_criteria>
  <action>Refactor the sequencing boundary so local present-scene NPC settlement happens before final visible narration, while global/off-screen/faction/reflection work remains rollback-critical post-narration finalization. In practice, remove `tickPresentNpcs(...)` from the old all-in-one post-turn bucket in `chat.ts` and make the authoritative local-settlement step run before the final narration pass in `turn-processor.ts` or a helper it owns. Keep `simulateOffscreenNpcs(...)`, `checkAndTriggerReflections(...)`, and `tickFactions(...)` in post-narration rollback-critical finalization unless they generate immediate player-perceivable effects already captured by the scene-assembly seam. Preserve Phase 39 done/finalizing semantics, but add a distinct hidden-pass/local-settlement progress state so the frontend can distinguish “story generation still settling the current scene” from “post-turn world finalization after visible narration.” Also include the backend opening-scene trigger in this sequencing design: campaigns with zero assistant messages must be able to request a first authoritative scene without abusing the normal user-action path. Do not introduce a second independent world state pipeline.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/turn-processor.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>The backend sequencing now matches the product contract: current-scene visible consequences settle before the visible message, while broader world finalization still completes before `done`.</done>
</task>

</tasks>

<verification>
Run the backend Phase 45 smoke suites and inspect event order. The user-visible `narrative` path should now be downstream of local settlement, and `finalizing_turn` should still guard the remaining rollback-critical post-narration work.
</verification>

<success_criteria>
Backend scene text is now causally downstream of settled local state. The player sees one final runtime-grounded scene, not a half-finished prose stream that later world logic tries to catch up with.
</success_criteria>

<output>
After completion, create `.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-02-SUMMARY.md`
</output>
