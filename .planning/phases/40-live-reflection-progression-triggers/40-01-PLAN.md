---
phase: 40-live-reflection-progression-triggers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/engine/reflection-budget.ts
  - backend/src/engine/index.ts
  - backend/src/engine/tool-executor.ts
  - backend/src/engine/npc-tools.ts
  - backend/src/engine/npc-offscreen.ts
  - backend/src/engine/__tests__/reflection-budget.test.ts
  - backend/src/engine/__tests__/tool-executor.test.ts
  - backend/src/engine/__tests__/npc-agent.test.ts
  - backend/src/engine/__tests__/npc-offscreen.test.ts
autonomous: true
requirements:
  - SIMF-01
must_haves:
  truths:
    - "Committed gameplay, present-NPC, and off-screen NPC events all add live reflection signal during ordinary runtime instead of relying on scripts or replay passes."
    - "Each authoritative event writer increments participating NPCs once per committed event, while players and unmatched names do not create shadow reflection state."
    - "Reflection signal accumulation stays inside the authoritative event-write path that Phase 39 already keeps inside the honest turn boundary."
  artifacts:
    - path: "backend/src/engine/reflection-budget.ts"
      provides: "campaign-scoped participant resolution and unprocessedImportance accumulation helper"
      contains: "accumulateReflectionBudget"
    - path: "backend/src/engine/tool-executor.ts"
      provides: "Storyteller log_event writes that accumulate reflection budget after committed episodic events"
      contains: "handleLogEvent"
    - path: "backend/src/engine/npc-tools.ts"
      provides: "present-NPC dialogue and action writers that feed the same accumulation seam"
      contains: "createNpcAgentTools"
    - path: "backend/src/engine/npc-offscreen.ts"
      provides: "off-screen NPC event writes that feed the same accumulation seam"
      contains: "applyOffscreenUpdate"
  key_links:
    - from: "backend/src/engine/tool-executor.ts"
      to: "backend/src/engine/reflection-budget.ts"
      via: "log_event committed episodic-event write followed by budget accumulation"
      pattern: "storeEpisodicEvent|accumulateReflectionBudget"
    - from: "backend/src/engine/npc-tools.ts"
      to: "backend/src/engine/reflection-budget.ts"
      via: "present-NPC speak path and act->log_event path reuse the same live accumulation seam"
      pattern: "storeEpisodicEvent|executeToolCall|accumulateReflectionBudget"
    - from: "backend/src/engine/npc-offscreen.ts"
      to: "backend/src/engine/reflection-budget.ts"
      via: "off-screen episodic-event write followed by budget accumulation"
      pattern: "storeEpisodicEvent|accumulateReflectionBudget"
---

<objective>
Make live runtime events accumulate reflection budget anywhere ordinary play already writes authoritative episodic events.

Purpose: satisfy the trigger half of `SIMF-01` by wiring reflection accumulation onto committed gameplay, present-NPC, and off-screen NPC event flow per `D-01`, `D-02`, and `D-03`.
Output: one shared accumulation helper plus backend tests proving the live event writers now increment `npcs.unprocessedImportance` without introducing a second pipeline or replay pass.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-live-reflection-progression-triggers/40-CONTEXT.md
@.planning/phases/40-live-reflection-progression-triggers/40-RESEARCH.md
@.planning/phases/40-live-reflection-progression-triggers/40-VALIDATION.md
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-HANDOFF.md
@.planning/phases/39-honest-turn-boundary-retry-undo/39-CONTEXT.md
@backend/src/engine/tool-executor.ts
@backend/src/engine/npc-tools.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/engine/reflection-agent.ts
@backend/src/engine/__tests__/tool-executor.test.ts
@backend/src/engine/__tests__/npc-agent.test.ts
@backend/src/engine/__tests__/npc-offscreen.test.ts

<interfaces>
From `backend/src/engine/tool-executor.ts`:
```ts
export interface ToolResult {
  success: boolean;
  result?: unknown;
  error?: string;
}

export async function executeToolCall(
  campaignId: string,
  toolName: string,
  args: Record<string, unknown>,
  tick: number,
  outcomeTier?: string,
): Promise<ToolResult>
```

From `backend/src/engine/npc-tools.ts`:
```ts
export function createNpcAgentTools(
  campaignId: string,
  npcId: string,
  tick: number,
  judgeProvider: ProviderConfig,
)
```

From `backend/src/engine/npc-offscreen.ts`:
```ts
export interface OffscreenUpdate {
  npcName: string;
  newLocation: string | null;
  actionSummary: string;
  goalProgress: string | null;
}

export async function applyOffscreenUpdate(
  campaignId: string,
  npcCtx: NpcContext,
  update: OffscreenUpdate,
  tick: number,
): Promise<AppliedOffscreenUpdate>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock live accumulation seams in backend tests</name>
  <read_first>
@.planning/phases/40-live-reflection-progression-triggers/40-CONTEXT.md
@.planning/phases/40-live-reflection-progression-triggers/40-RESEARCH.md
@backend/src/engine/tool-executor.ts
@backend/src/engine/npc-tools.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/engine/__tests__/tool-executor.test.ts
@backend/src/engine/__tests__/npc-agent.test.ts
@backend/src/engine/__tests__/npc-offscreen.test.ts
  </read_first>
  <files>backend/src/engine/__tests__/reflection-budget.test.ts, backend/src/engine/__tests__/tool-executor.test.ts, backend/src/engine/__tests__/npc-agent.test.ts, backend/src/engine/__tests__/npc-offscreen.test.ts</files>
  <acceptance_criteria>
    - `npm --prefix backend exec vitest run src/engine/__tests__/reflection-budget.test.ts src/engine/__tests__/tool-executor.test.ts src/engine/__tests__/npc-agent.test.ts src/engine/__tests__/npc-offscreen.test.ts` fails only because live accumulation wiring is still missing.
    - `rg -n "accumulates reflection budget|increments reflection budget|ignores unmatched participants" backend/src/engine/__tests__/reflection-budget.test.ts backend/src/engine/__tests__/tool-executor.test.ts backend/src/engine/__tests__/npc-agent.test.ts backend/src/engine/__tests__/npc-offscreen.test.ts` finds explicit regression coverage for every authoritative event writer in scope.
  </acceptance_criteria>
  <behavior>
    - Test 1: a dedicated helper-level spec proves participant resolution is campaign-scoped, case-insensitive, ignores players/unmatched names, and increments matched NPC rows by raw event importance once each.
    - Test 2: `executeToolCall(..., "log_event", ...)` stores the episodic event and then accumulates reflection budget on the participating NPCs.
    - Test 3: present-NPC dialogue or act-generated events increment reflection budget through the same committed live path without double-counting.
    - Test 4: off-screen NPC updates increment reflection budget after their episodic event is committed.
  </behavior>
  <action>Write failing tests first. Add a focused `reflection-budget.test.ts` for participant resolution and `unprocessedImportance` increments. Extend `tool-executor`, `npc-agent`, and `npc-offscreen` tests so they pin the exact live write paths from `D-01`, `D-02`, and `D-03`: no scripts, no replay scan, no second analytics queue, no debug-only counters. Use raw event importance as the first implementation target per `D-06`, keep the coverage centered on ordinary short-arc interaction viability per `D-04` and `D-05`, and avoid any checkpoint, UI, or docs-only assertions.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/reflection-budget.test.ts src/engine/__tests__/tool-executor.test.ts src/engine/__tests__/npc-agent.test.ts src/engine/__tests__/npc-offscreen.test.ts</automated>
  </verify>
  <done>The backend test suite explicitly fails on missing live accumulation behavior across all authoritative event writers, with no ambiguity about how NPC participants should be resolved or how many times a committed event may increment the budget.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement one shared reflection-budget helper and wire every authoritative event writer to it</name>
  <read_first>
@backend/src/engine/reflection-agent.ts
@backend/src/engine/tool-executor.ts
@backend/src/engine/npc-tools.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/engine/__tests__/reflection-budget.test.ts
@backend/src/engine/__tests__/tool-executor.test.ts
@backend/src/engine/__tests__/npc-agent.test.ts
@backend/src/engine/__tests__/npc-offscreen.test.ts
  </read_first>
  <files>backend/src/engine/reflection-budget.ts, backend/src/engine/index.ts, backend/src/engine/tool-executor.ts, backend/src/engine/npc-tools.ts, backend/src/engine/npc-offscreen.ts</files>
  <acceptance_criteria>
    - `rg -n "accumulateReflectionBudget" backend/src/engine/reflection-budget.ts backend/src/engine/tool-executor.ts backend/src/engine/npc-tools.ts backend/src/engine/npc-offscreen.ts` shows one shared accumulation seam wired from every authoritative event writer in phase scope.
    - `npm --prefix backend exec vitest run src/engine/__tests__/reflection-budget.test.ts src/engine/__tests__/tool-executor.test.ts src/engine/__tests__/npc-agent.test.ts src/engine/__tests__/npc-offscreen.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: committed `log_event` calls increment each participating NPC once by raw importance.
    - Test 2: present-NPC `speak` writes increment reflection budget directly, while present-NPC `act` piggybacks through `log_event` and does not increment twice.
    - Test 3: off-screen NPC event writes increment reflection budget after the event is stored.
    - Test 4: unmatched or non-NPC participants leave `unprocessedImportance` untouched.
  </behavior>
  <action>Create `backend/src/engine/reflection-budget.ts` with a campaign-scoped helper that resolves participant names against `npcs`, deduplicates names case-insensitively, ignores players and unmatched names, and increments `npcs.unprocessedImportance` by the committed event importance once per matched NPC. Call it immediately after successful `storeEpisodicEvent(...)` in `handleLogEvent`, present-NPC `speak`, and off-screen `applyOffscreenUpdate`. Keep present-NPC `act` piggybacking through `log_event` per `D-02` so it does not double-count. Export the helper through `backend/src/engine/index.ts` only if the new tests or later plans need it. Do not add replay scans, background repair jobs, separate event mirrors, or any state outside authoritative SQLite per `D-09`, `D-12`, and `D-13`.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/reflection-budget.test.ts src/engine/__tests__/tool-executor.test.ts src/engine/__tests__/npc-agent.test.ts src/engine/__tests__/npc-offscreen.test.ts</automated>
  </verify>
  <done>Normal gameplay, present-NPC updates, and off-screen NPC updates now all feed the same live reflection-budget path exactly once per committed event, using authoritative campaign-scoped SQLite state and no parallel pipeline.</done>
</task>

</tasks>

<verification>
Run the targeted backend Vitest command for the helper and writer-path tests. Confirm every authoritative episodic-event writer in scope calls the same accumulation seam, that participant resolution is campaign-scoped and NPC-only, that `act` still piggybacks through `log_event` instead of double-counting, and that no replay or tooling-only path was introduced.
</verification>

<success_criteria>
Phase 40 has a real runtime trigger source: ordinary gameplay, present-NPC behavior, and off-screen NPC updates all accumulate `unprocessedImportance` through the same authoritative event-write seam, making reflection reachable without scripts or manual repair.
</success_criteria>

<output>
After completion, create `.planning/phases/40-live-reflection-progression-triggers/40-01-SUMMARY.md`
</output>
