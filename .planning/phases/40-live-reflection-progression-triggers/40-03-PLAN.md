---
phase: 40-live-reflection-progression-triggers
plan: 03
type: execute
wave: 3
depends_on:
  - 40-02
files_modified:
  - backend/src/vectors/episodic-events.ts
  - backend/src/engine/reflection-agent.ts
  - backend/src/routes/chat.ts
  - backend/src/vectors/__tests__/episodic-events.test.ts
  - backend/src/engine/__tests__/reflection-agent.test.ts
  - backend/src/routes/__tests__/chat.test.ts
autonomous: true
gap_closure: true
requirements:
  - SIMF-01
must_haves:
  truths:
    - "When reflection triggers during rollback-critical finalization, it can read the same-turn committed events that pushed an NPC over threshold before any auxiliary embedding runs."
    - "Present-NPC dialogue and off-screen NPC event writes enter the same live evidence handoff as storyteller `log_event` writes, so reflection reads one consistent committed-event path per D-02 and D-03."
    - "Auxiliary post-turn embedding drains the shared same-turn evidence queue after reflection finishes, so non-`log_event` writers are not stranded outside the later semantic-evidence path."
  artifacts:
    - path: "backend/src/vectors/episodic-events.ts"
      provides: "committed-event pending-evidence helpers layered on top of `storeEpisodicEvent()`"
      contains: "storeEpisodicEvent"
    - path: "backend/src/engine/reflection-agent.ts"
      provides: "reflection prompt assembly that merges same-turn committed evidence with semantic retrieval"
      contains: "runReflection"
    - path: "backend/src/routes/chat.ts"
      provides: "post-turn auxiliary drain that embeds queued committed events after reflection runs"
      contains: "queueAuxiliaryPostTurnWork"
  key_links:
    - from: "backend/src/vectors/episodic-events.ts"
      to: "backend/src/engine/reflection-agent.ts"
      via: "non-destructive read of same-turn committed events before `searchEpisodicEvents()` fallback"
      pattern: "storeEpisodicEvent|Recent evidence|searchEpisodicEvents"
    - from: "backend/src/vectors/episodic-events.ts"
      to: "backend/src/routes/chat.ts"
      via: "post-turn drain of queued committed events into `embedAndUpdateEvent()`"
      pattern: "drain|embedAndUpdateEvent"
    - from: "backend/src/engine/npc-tools.ts"
      to: "backend/src/vectors/episodic-events.ts"
      via: "present-NPC `speak` continues writing through `storeEpisodicEvent()` and now joins the shared evidence handoff"
      pattern: "storeEpisodicEvent"
    - from: "backend/src/engine/npc-offscreen.ts"
      to: "backend/src/vectors/episodic-events.ts"
      via: "off-screen updates continue writing through `storeEpisodicEvent()` and now join the shared evidence handoff"
      pattern: "storeEpisodicEvent"
---

<objective>
Close the verifier-reported same-turn evidence handoff gap so reflection reads the committed events that actually triggered it.

Purpose: finish the blocked half of `SIMF-01` without reopening checkpoint fidelity or broader progression design. This plan fixes the hollow seam identified in `40-VERIFICATION.md`: reflection currently depends on embedded vector search results that are not available yet, and non-`log_event` writers never join that path.
Output: targeted backend regressions plus one shared committed-event evidence handoff reused by reflection before it runs and by auxiliary embedding after it runs.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/40-live-reflection-progression-triggers/40-CONTEXT.md
@.planning/phases/40-live-reflection-progression-triggers/40-VERIFICATION.md
@.planning/phases/40-live-reflection-progression-triggers/40-01-SUMMARY.md
@.planning/phases/40-live-reflection-progression-triggers/40-02-SUMMARY.md
@backend/src/vectors/episodic-events.ts
@backend/src/engine/reflection-agent.ts
@backend/src/routes/chat.ts
@backend/src/engine/npc-tools.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/vectors/__tests__/episodic-events.test.ts
@backend/src/engine/__tests__/reflection-agent.test.ts
@backend/src/routes/__tests__/chat.test.ts

<interfaces>
From `backend/src/vectors/episodic-events.ts`:
```ts
export interface EpisodicEvent {
  id: string;
  text: string;
  tick: number;
  location: string;
  participants: string[];
  importance: number;
  type: string;
  vector: number[];
}

export async function storeEpisodicEvent(
  campaignId: string,
  event: Omit<EpisodicEvent, "id" | "vector">,
): Promise<string>

export async function embedAndUpdateEvent(
  eventId: string,
  text: string,
  provider: ResolvedRole["provider"],
): Promise<void>

export async function searchEpisodicEvents(
  queryVector: number[],
  currentTick: number,
  limit?: number,
): Promise<EpisodicEvent[]>
```

From `backend/src/engine/reflection-agent.ts`:
```ts
export async function runReflection(
  campaignId: string,
  npcId: string,
  tick: number,
  judgeProvider: ProviderConfig,
  embedderProvider?: ProviderConfig,
): Promise<ReflectionResult>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock same-turn committed-evidence handoff regressions</name>
  <read_first>
@.planning/phases/40-live-reflection-progression-triggers/40-VERIFICATION.md
@.planning/phases/40-live-reflection-progression-triggers/40-01-SUMMARY.md
@.planning/phases/40-live-reflection-progression-triggers/40-02-SUMMARY.md
@backend/src/vectors/episodic-events.ts
@backend/src/engine/reflection-agent.ts
@backend/src/routes/chat.ts
@backend/src/vectors/__tests__/episodic-events.test.ts
@backend/src/engine/__tests__/reflection-agent.test.ts
@backend/src/routes/__tests__/chat.test.ts
  </read_first>
  <files>backend/src/vectors/__tests__/episodic-events.test.ts, backend/src/engine/__tests__/reflection-agent.test.ts, backend/src/routes/__tests__/chat.test.ts</files>
  <acceptance_criteria>
    - `npm --prefix backend exec vitest run src/vectors/__tests__/episodic-events.test.ts src/engine/__tests__/reflection-agent.test.ts src/routes/__tests__/chat.test.ts` fails only because the shared same-turn evidence handoff is not implemented yet.
    - `rg -n "pending evidence|same-turn evidence|non-log_event|drains queued committed events|Recent evidence" backend/src/vectors/__tests__/episodic-events.test.ts backend/src/engine/__tests__/reflection-agent.test.ts backend/src/routes/__tests__/chat.test.ts` finds direct regressions for the verifier-reported handoff gap.
  </acceptance_criteria>
  <behavior>
    - Test 1: `storeEpisodicEvent()` queues a campaign-scoped same-turn committed-event record that can be read without requiring embeddings.
    - Test 2: `runReflection()` includes same-turn committed events for the qualifying NPC in `Recent evidence` even when vector search has not embedded them yet.
    - Test 3: `queueAuxiliaryPostTurnWork()` drains the queued committed events after reflection, including present-NPC `speak` and off-screen writes rather than only `log_event` tool calls.
    - Test 4: pending evidence reads are campaign-scoped and tick-scoped so unrelated campaigns or stale turns do not leak into the reflection prompt.
  </behavior>
  <action>Write failing tests first around the exact gap from `40-VERIFICATION.md`. In `backend/src/vectors/__tests__/episodic-events.test.ts`, define the contract for a lightweight same-turn committed-event queue piggybacking on `storeEpisodicEvent()` per D-01 and D-02. In `backend/src/engine/__tests__/reflection-agent.test.ts`, make `Recent evidence` stay populated from that queue when the qualifying events are not embedded yet, instead of falling through to "No recent events recorded." In `backend/src/routes/__tests__/chat.test.ts`, add a route-level regression showing rollback-critical reflection still runs before auxiliary embedding, but the auxiliary embed drain now includes non-`log_event` writers per D-03 and D-12. Keep the scope confined to the evidence handoff gap: no checkpoint restore work, no new UI/debug surface, and no Phase 41 durability behavior.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/vectors/__tests__/episodic-events.test.ts src/engine/__tests__/reflection-agent.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>The backend tests explicitly pin the missing same-turn evidence handoff: committed events must be visible to reflection before embeddings exist, and the same queue must later feed auxiliary embedding for `log_event`, `speak`, and off-screen writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement one shared same-turn evidence handoff for reflection and auxiliary embedding</name>
  <read_first>
@backend/src/vectors/episodic-events.ts
@backend/src/engine/reflection-agent.ts
@backend/src/routes/chat.ts
@backend/src/engine/npc-tools.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/vectors/__tests__/episodic-events.test.ts
@backend/src/engine/__tests__/reflection-agent.test.ts
@backend/src/routes/__tests__/chat.test.ts
  </read_first>
  <files>backend/src/vectors/episodic-events.ts, backend/src/engine/reflection-agent.ts, backend/src/routes/chat.ts</files>
  <acceptance_criteria>
    - `rg -n "pending|drain|same-turn|Recent evidence" backend/src/vectors/episodic-events.ts backend/src/engine/reflection-agent.ts backend/src/routes/chat.ts` shows one shared committed-event handoff reused by reflection and auxiliary embedding.
    - `npm --prefix backend exec vitest run src/vectors/__tests__/episodic-events.test.ts src/engine/__tests__/reflection-agent.test.ts src/routes/__tests__/chat.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: committed same-turn events become immediately readable evidence without synchronous embedding.
    - Test 2: reflection merges queued same-turn evidence with semantic search results and remains inside rollback-critical finalization.
    - Test 3: auxiliary post-turn embedding drains the same queue after reflection, so non-`log_event` writers join the later semantic evidence path.
    - Test 4: the queue is cleared after auxiliary handling so stale committed evidence does not leak into later turns.
  </behavior>
  <action>Add a lightweight in-memory committed-event handoff to `backend/src/vectors/episodic-events.ts`, keyed by campaign and scoped to the current tick, and populate it inside `storeEpisodicEvent()` so every existing writer automatically joins the same path per D-02 and D-03. Expose a non-destructive read helper for reflection and a drain helper for post-turn auxiliary work. Update `backend/src/engine/reflection-agent.ts` so `runReflection()` merges queued same-turn committed events relevant to the NPC into `Recent evidence` before relying on `searchEpisodicEvents()`, preferring direct committed text over making embeddings rollback-critical. Update `backend/src/routes/chat.ts` so `queueAuxiliaryPostTurnWork()` drains and embeds that shared queue after reflection completes, covering `log_event`, present-NPC `speak`, and off-screen writes while leaving image-generation behavior and the Phase 39 honest completion boundary intact. Do not introduce synchronous embedding, replay scans, new durable storage, checkpoint work, or broader reflection redesign per D-12, D-13, and D-14.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/vectors/__tests__/episodic-events.test.ts src/engine/__tests__/reflection-agent.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>The just-triggering committed events are available to reflection before it runs, and every writer that already calls `storeEpisodicEvent()` now shares the same post-turn evidence drain used for later embedding.</done>
</task>

</tasks>

<verification>
Run the targeted backend Vitest suite for episodic-events, reflection-agent, and chat. Confirm reflection sees same-turn committed evidence before embeddings exist, confirm auxiliary embedding drains the same shared queue after reflection, and confirm present-NPC and off-screen writers join that queue automatically through `storeEpisodicEvent()` with no synchronous embedding or checkpoint-scope changes.
</verification>

<success_criteria>
The Phase 40 blocker is closed: threshold-crossing events are visible to `runReflection()` during rollback-critical finalization, and non-`log_event` writers now share the same evidence path instead of bypassing both reflection grounding and later embedding.
</success_criteria>

<output>
After completion, create `.planning/phases/40-live-reflection-progression-triggers/40-03-SUMMARY.md`
</output>
