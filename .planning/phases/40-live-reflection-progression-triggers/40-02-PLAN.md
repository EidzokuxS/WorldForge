---
phase: 40-live-reflection-progression-triggers
plan: 02
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - backend/src/engine/reflection-agent.ts
  - backend/src/engine/__tests__/reflection-agent.test.ts
  - backend/src/engine/__tests__/reflection-progression.test.ts
  - backend/src/routes/__tests__/chat.test.ts
autonomous: true
requirements:
  - SIMF-01
must_haves:
  truths:
    - "Ordinary repeated play can cross the live reflection threshold during normal post-turn finalization, with no manual repair step or offline script."
    - "When reflection fires, the default observable outcomes are structured belief, goal, and relationship changes that later turns can read."
    - "Wealth-tier and skill-tier progression remain possible but require materially stronger evidence than ordinary belief, goal, or relationship drift."
  artifacts:
    - path: "backend/src/engine/reflection-agent.ts"
      provides: "threshold-triggered reflection runner with explicit outcome-priority and evidence guardrails"
      contains: "runReflection"
    - path: "backend/src/engine/__tests__/reflection-agent.test.ts"
      provides: "reflection prompt and structured-state regressions for live runtime outcomes"
      contains: "Recent evidence"
    - path: "backend/src/routes/__tests__/chat.test.ts"
      provides: "rollback-critical post-turn regression proving reflection is part of the ordinary gameplay loop before done"
      contains: "triggers reflection during post-turn finalization"
  key_links:
    - from: "backend/src/routes/chat.ts"
      to: "backend/src/engine/reflection-agent.ts"
      via: "rollback-critical post-turn finalization invokes reflection before done"
      pattern: "checkAndTriggerReflections|done"
    - from: "backend/src/engine/reflection-agent.ts"
      to: "backend/src/engine/reflection-tools.ts"
      via: "Judge-driven tool calls persist structured belief, goal, relationship, wealth, and skill changes"
      pattern: "createReflectionTools|set_belief|set_goal|set_relationship|upgrade_wealth|upgrade_skill"
    - from: "backend/src/engine/reflection-agent.ts"
      to: "backend/src/vectors/episodic-events.ts"
      via: "episodic evidence retrieval grounds reflection decisions in committed events"
      pattern: "searchEpisodicEvents"
---

<objective>
Make threshold-crossed reflection fire inside the normal gameplay loop and bias its outcomes toward durable structured NPC-state changes.

Purpose: finish `SIMF-01` by proving normal play can trigger reflection during Phase 39 finalization and by making belief, goal, and relationship drift the first-class live outcomes per `D-07` through `D-10` and `D-12`.
Output: backend tests and reflection-agent prompt/runtime changes that keep reflection evidence-driven, structured-state-first, and reachable through ordinary play while keeping wealth/skill progression secondary.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-live-reflection-progression-triggers/40-CONTEXT.md
@.planning/phases/40-live-reflection-progression-triggers/40-RESEARCH.md
@.planning/phases/40-live-reflection-progression-triggers/40-VALIDATION.md
@.planning/phases/07-reflection-progression/07-CONTEXT.md
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-HANDOFF.md
@.planning/phases/39-honest-turn-boundary-retry-undo/39-CONTEXT.md
@backend/src/routes/chat.ts
@backend/src/engine/reflection-agent.ts
@backend/src/engine/reflection-tools.ts
@backend/src/engine/__tests__/reflection-agent.test.ts
@backend/src/engine/__tests__/reflection-progression.test.ts
@backend/src/routes/__tests__/chat.test.ts

<interfaces>
From `backend/src/engine/reflection-agent.ts`:
```ts
export const REFLECTION_THRESHOLD = 10;

export interface ReflectionResult {
  npcId: string;
  npcName: string;
  toolCalls: Array<{ tool: string; args: unknown; result: unknown }>;
  error?: string;
}

export async function runReflection(
  campaignId: string,
  npcId: string,
  tick: number,
  judgeProvider: ProviderConfig,
  embedderProvider?: ProviderConfig,
): Promise<ReflectionResult>
```

From `backend/src/engine/reflection-tools.ts`:
```ts
export const WEALTH_TIERS = ["Destitute", "Poor", "Comfortable", "Wealthy", "Obscenely Rich"] as const;
export const SKILL_TIERS = ["Novice", "Skilled", "Master"] as const;
export const RELATIONSHIP_TAGS = ["Trusted Ally", "Friendly", "Neutral", "Suspicious", "Hostile", "Sworn Enemy"] as const;

export function createReflectionTools(campaignId: string, npcId: string)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the live trigger and structured-outcome contract in backend tests</name>
  <read_first>
@backend/src/routes/chat.ts
@backend/src/engine/reflection-agent.ts
@backend/src/engine/reflection-tools.ts
@backend/src/engine/__tests__/reflection-agent.test.ts
@backend/src/engine/__tests__/reflection-progression.test.ts
@backend/src/routes/__tests__/chat.test.ts
  </read_first>
  <files>backend/src/engine/__tests__/reflection-agent.test.ts, backend/src/engine/__tests__/reflection-progression.test.ts, backend/src/routes/__tests__/chat.test.ts</files>
  <acceptance_criteria>
    - `npm --prefix backend exec vitest run src/engine/__tests__/reflection-agent.test.ts src/engine/__tests__/reflection-progression.test.ts src/routes/__tests__/chat.test.ts` fails only because the live trigger or outcome-priority contract is not explicit enough yet.
    - `rg -n "triggers reflection during post-turn finalization|beliefs, goals, and relationships first|materially stronger evidence|significant trade/loot|3\\+ successful uses" backend/src/engine/__tests__/reflection-agent.test.ts backend/src/routes/__tests__/chat.test.ts` finds direct regressions for the Phase 40 decisions.
  </acceptance_criteria>
  <behavior>
    - Test 1: a normal `/api/chat/action` finalization path can reach `checkAndTriggerReflections` after live accumulation and before `done`.
    - Test 2: reflection prompts and tool expectations make belief, goal, and relationship changes the primary live outcomes.
    - Test 3: wealth and skill upgrades remain allowed only with materially stronger evidence than ordinary interaction arcs.
    - Test 4: successful reflection resets `unprocessedImportance` only after running the evidence-driven reflection flow.
  </behavior>
  <action>Write failing tests first. Extend `reflection-agent.test.ts` so the prompt and expected tool behavior explicitly encode `D-07`, `D-08`, `D-09`, and `D-10`: belief, goal, and relationship drift are first-class; wealth and skill upgrades stay secondary and need materially stronger evidence. Extend `chat.test.ts` with a regression that proves reflection remains inside the rollback-critical post-turn loop from Phase 39 per `D-12`, with `finalizing_turn` preceding any eventual `done`. If the route-level mock shape blocks honest coverage, narrow the mocking only for the new case instead of adding a separate manual test. Do not add popup/UI assertions, checkpoint durability checks, or a knowledge-propagation engine.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/reflection-agent.test.ts src/engine/__tests__/reflection-progression.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>The backend test suite now encodes both halves of Phase 40: ordinary play can trigger reflection during post-turn finalization, and the preferred outcomes are durable structured-state changes instead of noisy progression jumps or meta-only signaling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden the reflection-agent runtime contract around evidence-driven structured-state changes</name>
  <read_first>
@backend/src/engine/reflection-agent.ts
@backend/src/engine/reflection-tools.ts
@backend/src/routes/chat.ts
@backend/src/engine/__tests__/reflection-agent.test.ts
@backend/src/engine/__tests__/reflection-progression.test.ts
@backend/src/routes/__tests__/chat.test.ts
  </read_first>
  <files>backend/src/engine/reflection-agent.ts</files>
  <acceptance_criteria>
    - `rg -n "beliefs, goals, and relationships|materially stronger evidence|significant trade/loot|3\\+ successful uses|choose not to call any tools" backend/src/engine/reflection-agent.ts` shows explicit outcome-priority guardrails in the live reflection prompt.
    - `npm --prefix backend exec vitest run src/engine/__tests__/reflection-agent.test.ts src/engine/__tests__/reflection-progression.test.ts src/routes/__tests__/chat.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: reflection remains threshold-triggered and evidence-driven.
    - Test 2: primary outcomes are belief, goal, and relationship updates written into structured NPC state.
    - Test 3: wealth and skill progression require stronger evidence and do not become the default output for ordinary scenes.
    - Test 4: no reflection is a valid result when evidence is insufficient.
  </behavior>
  <action>Update `runReflection()` so the live prompt contract is explicit about Phase 40 priorities: ordinary short arcs should be able to trigger reflection per `D-04` and `D-05`, but once reflection runs it should primarily produce structured belief, goal, and relationship changes per `D-07` and `D-09`. Keep wealth-tier and skill-tier progression in scope but subordinate per `D-08`, with materially stronger evidence requirements called out plainly in the prompt. Preserve the Phase 39 contract by keeping reflection inside post-turn finalization and by leaving `checkAndTriggerReflections()` threshold-based and reset-on-success. Reuse `createReflectionTools()` and the existing structured record fields; do not add UI surfaces, manual repair hooks, checkpoint/restart durability work, or a new knowledge-propagation subsystem.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/reflection-agent.test.ts src/engine/__tests__/reflection-progression.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>Reflection is now a live, evidence-driven gameplay mechanic: ordinary play can cross the threshold, reflection fires during normal finalization, and the dominant outcomes are durable structured NPC-state changes that later runtime systems can consume.</done>
</task>

</tasks>

<verification>
Run the targeted backend Vitest suite for reflection-agent, reflection-progression, and chat. Confirm ordinary post-turn finalization can trigger reflection after live accumulation from Plan 40-01, that `done` still waits until the reflection step finishes, and that the live prompt/runtime contract prioritizes belief/goal/relationship drift while keeping wealth/skill progression secondary.
</verification>

<success_criteria>
Phase 40 is operational, not dormant: repeated normal play now produces enough runtime signal for reflection to fire, the reflection step lives inside the honest Phase 39 turn loop, and the resulting structured NPC-state changes are the primary downstream behavior drivers the player can observe in later turns.
</success_criteria>

<output>
After completion, create `.planning/phases/40-live-reflection-progression-triggers/40-02-SUMMARY.md`
</output>
