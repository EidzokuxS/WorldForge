---
phase: 57-power-scaling-character-profile-redesign
plan: 04
type: execute
wave: 3
depends_on: ["57-01", "57-02", "57-03"]
files_modified:
  - shared/src/types.ts
  - shared/src/index.ts
  - backend/src/character/grounded-character-profile.ts
  - backend/src/character/canonical-source-bundle.ts
  - frontend/components/world-review/character-record-inspector.tsx
  - frontend/components/world-review/npcs-section.tsx
  - frontend/lib/character-drafts.ts
autonomous: false
requirements:
  - SC-3
  - SC-4
  - SC-7
must_haves:
  truths:
    - "CharacterGroundingProfile, PowerProfile, SourceBundle, ContinuityPolicy types removed from shared"
    - "Legacy files grounded-character-profile.ts and canonical-source-bundle.ts deleted"
    - "Frontend character card shows power stats table, hax abilities, vulnerabilities"
    - "Characters without powerStats show 'No power assessment' in UI (not crash, not fake data)"
    - "No duplicate personality text across card sections"
    - "Old grounding, source bundle, and continuity sections removed from inspector"
    - "Frontend draft normalizer no longer references sourceBundle or continuity"
  artifacts:
    - path: "frontend/components/world-review/character-record-inspector.tsx"
      provides: "Redesigned inspector with Power Stats table, Hax, Vulnerabilities sections"
      contains: "powerStats"
    - path: "frontend/lib/character-drafts.ts"
      provides: "Clean draft normalizer without old type references"
  key_links:
    - from: "frontend/components/world-review/character-record-inspector.tsx"
      to: "shared/src/types.ts"
      via: "PowerStats, HaxAbility, CharacterVulnerability imports"
      pattern: "import.*PowerStats"
    - from: "frontend/components/world-review/character-record-inspector.tsx"
      to: "shared/src/power-tiers.ts"
      via: "formatTierRank for display"
      pattern: "formatTierRank"
---

<objective>
Delete legacy type definitions and files (safe now that all consumers are migrated), redesign the frontend character inspector for power stats display, clean up frontend draft normalizer, and run entry-path verification matrix.

Purpose: Final cleanup and user-facing redesign. Deletion is safe because Plans 02-03 already migrated all consumers. The inspector must reflect the new power system and handle absence gracefully.
Output: Clean shared types, deleted legacy files, redesigned inspector, entry-path verification proof.
</objective>

<execution_context>
@C:/Users/robra/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-power-scaling-character-profile-redesign/57-CONTEXT.md
@.planning/phases/57-power-scaling-character-profile-redesign/57-RESEARCH.md
@.planning/phases/57-power-scaling-character-profile-redesign/57-01-SUMMARY.md
@.planning/phases/57-power-scaling-character-profile-redesign/57-02-SUMMARY.md
@.planning/phases/57-power-scaling-character-profile-redesign/57-03-SUMMARY.md

<interfaces>
<!-- New types (shared/src/types.ts — after Plan 01): -->
```typescript
export interface TierRank<T extends string = string> {
  tier: T; rank: number; // 1-10
}
export interface HaxAbility {
  name: string; type: string; bypassTier: ApDurabilityTier | null; limitations: string[];
}
export interface CharacterVulnerability {
  description: string; severity: "minor" | "major" | "critical";
}
export interface PowerStats {
  attackPotency: TierRank<ApDurabilityTier>;
  speed: TierRank<SpeedTier>;
  durability: TierRank<ApDurabilityTier>;
  intelligence: TierRank<IntelligenceTier>;
  hax: HaxAbility[];
  vulnerabilities: CharacterVulnerability[];
}
// CharacterDraft and CharacterRecord have: powerStats?: PowerStats
```

<!-- Utility from shared/src/power-tiers.ts: -->
```typescript
export function formatTierRank<T extends string>(tr: TierRank<T>): string; // "City 7"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old types, legacy files, redesign inspector, clean frontend</name>
  <files>shared/src/types.ts, shared/src/index.ts, backend/src/character/grounded-character-profile.ts, backend/src/character/canonical-source-bundle.ts, frontend/components/world-review/character-record-inspector.tsx, frontend/components/world-review/npcs-section.tsx, frontend/lib/character-drafts.ts</files>
  <read_first>
    - shared/src/types.ts (find remaining old type definitions to remove: CharacterGroundingProfile, PowerProfile, CharacterSourceBundle, CharacterContinuityPolicy, CharacterContinuityInertia and old fields on CharacterDraft/Record/Patch)
    - shared/src/index.ts (find old exports to remove)
    - backend/src/character/grounded-character-profile.ts (confirm no remaining imports elsewhere before deletion)
    - backend/src/character/canonical-source-bundle.ts (confirm no remaining imports elsewhere before deletion)
    - frontend/components/world-review/character-record-inspector.tsx (full file — find old Grounding, Power Profile, Continuity, Sources sections)
    - frontend/components/world-review/npcs-section.tsx (check for grounding references)
    - frontend/lib/character-drafts.ts (find normalizeSourceBundle, normalizeContinuity)
  </read_first>
  <action>
**BEFORE any deletion, verify no remaining imports:**
Run `grep -r "grounded-character-profile" backend/src/ --include="*.ts" | grep -v __tests__ | grep -v ".test."` and `grep -r "canonical-source-bundle" backend/src/ --include="*.ts" | grep -v __tests__ | grep -v ".test."` to confirm Plans 02-03 removed all references. If any remain, fix them before proceeding.

**In shared/src/types.ts — NOW remove old types (all consumers migrated by Plans 02-03):**
1. DELETE type definitions: `CharacterSourceBundle`, `CharacterContinuityInertia`, `CharacterContinuityPolicy`, `PowerProfile`, `CharacterGroundingProfile`
2. On `CharacterDraft`: REMOVE `grounding?`, `sourceBundle?`, `continuity?` (keep `powerStats?: PowerStats` added in Plan 01)
3. On `CharacterRecord`: REMOVE `grounding?`, `sourceBundle?`, `continuity?` (keep `powerStats?: PowerStats`)
4. On `CharacterDraftPatch`: REMOVE `sourceBundle?` and `continuity?` partial fields (keep `powerStats?: Partial<PowerStats>`)

**In shared/src/index.ts:**
- REMOVE exports: `CharacterSourceBundle`, `CharacterContinuityInertia`, `CharacterContinuityPolicy`, `PowerProfile`, `CharacterGroundingProfile`

**Delete backend files:**
- DELETE `backend/src/character/grounded-character-profile.ts`
- DELETE `backend/src/character/canonical-source-bundle.ts`

**In character-record-inspector.tsx — REDESIGN (addresses review concerns 2 and 7):**

1. Remove imports of old types
2. Add imports of `PowerStats`, `HaxAbility`, `CharacterVulnerability`, `formatTierRank` from `@worldforge/shared`

3. REMOVE sections entirely:
   - Grounding section (summary, facts, abilities, constraints, signature moves)
   - Old Power Profile section (attack, speed, durability, range text strings)
   - Continuity section (identity inertia, protected core, mutable surface)
   - Sources section (canon sources, secondary sources, synthesis metadata)

4. ADD new sections inside the Advanced tab:

**Power Stats table** (when `record.powerStats` exists):
Render as a 2-column Tailwind-styled table:
| Axis | Rating |
|------|--------|
| Attack Potency | {formatTierRank(ps.attackPotency)} |
| Speed | {formatTierRank(ps.speed)} |
| Durability | {formatTierRank(ps.durability)} |
| Intelligence | {formatTierRank(ps.intelligence)} |

When `powerStats` is undefined: show muted "No power assessment" text. SC-7 is satisfied with a table (radar/spider chart is deferred per CONTEXT.md deferred ideas).

**Hax Abilities list** (when `ps.hax.length > 0`):
Each ability as a compact row: bold name, type in muted text, bypass tier badge if present ("Bypasses {tier}"), limitations in italic.

**Vulnerabilities list** (when `ps.vulnerabilities.length > 0`):
Each vulnerability with severity badge: minor=gray/zinc, major=amber/yellow, critical=red.

5. KEEP existing sections that remain: Identity (baseFacts, behavioralCore, liveDynamics), Profile, Social Context, Motivations, Capabilities, State, Loadout, Start Conditions, Provenance
6. Ensure personality text appears ONLY in identity.behavioralCore section — no duplication

**In npcs-section.tsx:**
- Remove any direct references to `grounding`, `sourceBundle`, `continuity` on NPC summary cards
- NPC cards show: name, role, faction, location, tier, canonical status

**In character-drafts.ts:**
1. Remove `normalizeSourceBundle()` function entirely
2. Remove `normalizeContinuity()` function entirely
3. Remove imports of `CharacterSourceBundle`, `CharacterContinuityPolicy`, `CharacterGroundingProfile`
4. Stop setting `sourceBundle` or `continuity` on normalized output
5. Pass through `powerStats` if present on input
  </action>
  <verify>
    <automated>npm --prefix R:/Projects/WorldForge/frontend run lint && npm --prefix R:/Projects/WorldForge/frontend run build && npm --prefix R:/Projects/WorldForge/backend run typecheck && npm --prefix R:/Projects/WorldForge/shared run build 2>/dev/null; npx tsc --noEmit -p R:/Projects/WorldForge/shared/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - shared/src/types.ts does NOT contain `interface CharacterGroundingProfile`
    - shared/src/types.ts does NOT contain `interface PowerProfile`
    - shared/src/types.ts does NOT contain `interface CharacterSourceBundle`
    - shared/src/types.ts does NOT contain `interface CharacterContinuityPolicy`
    - shared/src/types.ts CharacterDraft does NOT contain `grounding?` or `sourceBundle?` or `continuity?`
    - backend/src/character/grounded-character-profile.ts does NOT exist
    - backend/src/character/canonical-source-bundle.ts does NOT exist
    - character-record-inspector.tsx contains Power Stats table rendering
    - character-record-inspector.tsx contains `formatTierRank`
    - character-record-inspector.tsx does NOT contain `CharacterGroundingProfile`
    - character-record-inspector.tsx does NOT contain `protectedCore`
    - character-drafts.ts does NOT contain `normalizeSourceBundle`
    - character-drafts.ts does NOT contain `normalizeContinuity`
    - Frontend builds without errors
    - Backend typechecks without errors
    - Shared typechecks without errors
  </acceptance_criteria>
  <done>Old types and legacy files removed. Inspector redesigned with power stats table, hax, vulnerabilities. Frontend draft normalizer cleaned. Entire codebase compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Entry-path verification matrix and visual check</name>
  <files>frontend/components/world-review/character-record-inspector.tsx</files>
  <action>Human verifies the redesigned inspector and entry-path coverage. All automated work was completed in Task 1 and prior plans.</action>
  <verify>
    <automated>cd R:/Projects/WorldForge/backend && npx vitest run --reporter=verbose && cd R:/Projects/WorldForge/shared && npx vitest run --reporter=verbose && npm --prefix R:/Projects/WorldForge/frontend run build</automated>
  </verify>
  <what-built>Complete Phase 57: VS Battles power scaling system, redesigned character inspector, removed grounding/continuity/source types.</what-built>
  <how-to-verify>
**Visual check (SC-7):**
1. Start backend: `cd R:/Projects/WorldForge/backend && npm run dev`
2. Start frontend: `cd R:/Projects/WorldForge/frontend && npm run dev`
3. Open http://localhost:3000

**Entry-path verification matrix (addresses review concern 5):**

| Path | Steps | Expected |
|------|-------|----------|
| Old campaign load | Load a pre-Phase-57 campaign | Characters load without crash. Inspector shows "No power assessment" for characters without powerStats. |
| Known-IP worldgen | Create new campaign with known IP, run worldgen | Generated NPCs have powerStats with VS Battles tiers in inspector. |
| Archetype creation | Create character via archetype research | Character gets powerStats from research if available, or "No power assessment" if research did not produce stats. |
| V2 card import | Import a V2 card for NPC or player | Card imports without crash. powerStats may be undefined (import path does not produce power stats). |
| Save/load cycle | Save campaign, reload page, reopen | All power stats persist across save/load. |
| UI readback | Open NPC inspector in World Review | Power Stats table shows 4 axes. Hax and vulnerabilities display correctly. No old sections visible. |
| In-game compare | During gameplay, use compare command | Response references tier+rank data, not old text descriptions. Characters without power data get explicit absence message. |

4. Walk through each row in the matrix above
5. Verify no duplicate personality text in any inspector section
  </how-to-verify>
  <done>User confirms all entry paths work correctly and inspector displays properly.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
```bash
npm --prefix R:/Projects/WorldForge/backend run typecheck
npm --prefix R:/Projects/WorldForge/frontend run lint
npm --prefix R:/Projects/WorldForge/frontend run build
cd R:/Projects/WorldForge/backend && npx vitest run --reporter=verbose
cd R:/Projects/WorldForge/shared && npx vitest run --reporter=verbose
```
</verification>

<success_criteria>
- All 4 old types (CharacterGroundingProfile, PowerProfile, SourceBundle, ContinuityPolicy) removed from shared (SC-4)
- Two legacy files deleted
- Inspector shows power stats table (not spider/radar — that is deferred), hax, vulnerabilities (SC-7 as table)
- Characters without powerStats show "No power assessment" in UI and lookups (fail-closed)
- No duplicate personality text (SC-3)
- All entry paths verified: old campaign load, known-IP worldgen, archetype, V2 import, save/load, UI readback, in-game compare (addresses review concern 5)
- Full test suite green, frontend builds, backend typechecks
</success_criteria>

<output>
After completion, create `.planning/phases/57-power-scaling-character-profile-redesign/57-04-SUMMARY.md`
</output>
