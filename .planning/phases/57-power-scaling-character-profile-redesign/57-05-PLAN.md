---
phase: 57-power-scaling-character-profile-redesign
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/engine/prompt-assembler.ts
  - backend/src/engine/npc-agent.ts
  - backend/src/engine/npc-offscreen.ts
  - backend/src/engine/reflection-tools.ts
  - backend/src/character/record-adapters.ts
  - backend/src/character/__tests__/record-adapters.test.ts
autonomous: true
gap_closure: true
requirements: [SC-4, SC-6]

must_haves:
  truths:
    - "NPC agent prompts contain no continuity/identityInertia/protectedCore/mutableSurface/changePressureNotes fields"
    - "Reflection threshold is a flat constant, not branched on identityInertia"
    - "Backend compiles with zero Phase-57-related TS2339/TS2353 errors"
  artifacts:
    - path: "backend/src/engine/prompt-assembler.ts"
      provides: "buildRuntimeIdentityLines without includeContinuity option or continuity injection"
      contains: "buildRuntimeIdentityLines"
    - path: "backend/src/engine/npc-agent.ts"
      provides: "buildNpcIdentityPrompt without continuity block"
    - path: "backend/src/engine/npc-offscreen.ts"
      provides: "buildOffscreenIdentitySummary without continuity field"
    - path: "backend/src/engine/reflection-tools.ts"
      provides: "minimumEvidenceForPromotion returning flat constant 1"
    - path: "backend/src/character/record-adapters.ts"
      provides: "mergeEditableNpcDraft without sourceBundle/continuity access"
    - path: "backend/src/character/__tests__/record-adapters.test.ts"
      provides: "CharacterRecord fixture without continuity field"
  key_links:
    - from: "backend/src/engine/prompt-assembler.ts"
      to: "shared/src/types.ts"
      via: "CharacterRecord type (no continuity field)"
      pattern: "buildRuntimeIdentityLines"
    - from: "backend/src/engine/reflection-tools.ts"
      to: "shared/src/types.ts"
      via: "CharacterRecord type (no continuity field)"
      pattern: "minimumEvidenceForPromotion"
---

<objective>
Remove all remaining `.continuity` and `.sourceBundle` field accesses from engine and adapter files that were left behind when Plan 04 deleted the type definitions. This closes SC-6 (NPC agent and reflection prompts work without continuity) and the TS compile errors blocking SC-4.

Purpose: The types were removed but 7 consuming code sites still reference the deleted fields, producing TS2339/TS2353 errors and leaving dead continuity logic in prompts.
Output: Clean backend typecheck with zero Phase-57-related errors; all continuity prompt injection removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-power-scaling-character-profile-redesign/57-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove continuity from engine files (prompt-assembler, npc-agent, npc-offscreen, reflection-tools)</name>
  <files>
    backend/src/engine/prompt-assembler.ts,
    backend/src/engine/npc-agent.ts,
    backend/src/engine/npc-offscreen.ts,
    backend/src/engine/reflection-tools.ts
  </files>
  <read_first>
    backend/src/engine/prompt-assembler.ts,
    backend/src/engine/npc-agent.ts,
    backend/src/engine/npc-offscreen.ts,
    backend/src/engine/reflection-tools.ts,
    shared/src/types.ts (CharacterRecord — confirm no continuity field)
  </read_first>
  <action>
    **prompt-assembler.ts — buildRuntimeIdentityLines function (starts ~line 1355):**
    1. Remove `includeContinuity?: boolean` from the options type (line 1359). The options type becomes `{ indent?: string }`.
    2. Delete `const continuity = record.continuity;` (line 1366).
    3. Delete the entire `if (options.includeContinuity && continuity)` block (lines 1401-1413) that pushes continuity summary into formatted array.

    **prompt-assembler.ts — caller at ~line 500 (player identity):**
    1. Change the call from `buildRuntimeIdentityLines(effectivePlayerRecord, { includeContinuity: Boolean(effectivePlayerRecord.continuity) || effectivePlayerRecord.identity.tier === "key" })` to just `buildRuntimeIdentityLines(effectivePlayerRecord)`. Remove the second argument entirely since the only remaining option is `indent` which defaults to "".

    **prompt-assembler.ts — caller at ~line 817 (NPC identity):**
    1. Change the call from `buildRuntimeIdentityLines(npcRecord, { indent: "  ", includeContinuity: Boolean(npcRecord.continuity) || ... })` to `buildRuntimeIdentityLines(npcRecord, { indent: "  " })`. Remove only the `includeContinuity` property.

    **npc-agent.ts — buildNpcIdentityPrompt function (starts ~line 404):**
    1. Delete `const continuity = record.continuity;` (line 408).
    2. Delete the entire `if (continuity && ...)` block (lines 440-462) that pushes "Continuity / fidelity:" lines into the array. This removes 22 lines of dead code.

    **npc-offscreen.ts — buildOffscreenIdentitySummary function (starts ~line 77):**
    1. Delete `const continuity = record.continuity;` (line 81).
    2. Replace the ternary `continuity ? [...].join(" | ") : null` block (lines 107-116) with just `null` or remove it entirely from the array. The simplest fix: delete lines 107-116 entirely — the `.filter(Boolean)` on line 117 already handles null entries.

    **reflection-tools.ts — minimumEvidenceForPromotion function (line 230):**
    1. Replace the entire function body with `return 1;`. Remove the `const inertia = record.continuity?.identityInertia ?? "flexible"` line and the if-branches for 'strict' and 'anchored'. The function becomes:
    ```typescript
    function minimumEvidenceForPromotion(_record: CharacterRecord): number {
      return 1;
    }
    ```
    Prefix parameter with underscore since it is no longer read.
  </action>
  <verify>
    <automated>npm --prefix backend run typecheck 2>&1 | grep -c "continuity"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "record.continuity" backend/src/engine/prompt-assembler.ts returns zero matches
    - grep -n "record.continuity" backend/src/engine/npc-agent.ts returns zero matches
    - grep -n "record.continuity" backend/src/engine/npc-offscreen.ts returns zero matches
    - grep -n "record.continuity" backend/src/engine/reflection-tools.ts returns zero matches
    - grep -n "includeContinuity" backend/src/engine/prompt-assembler.ts returns zero matches
    - grep -n "identityInertia" backend/src/engine/reflection-tools.ts returns zero matches
    - grep -n "protectedCore\|mutableSurface\|changePressureNotes" backend/src/engine/prompt-assembler.ts returns zero matches
    - grep -n "protectedCore\|mutableSurface\|changePressureNotes" backend/src/engine/npc-agent.ts returns zero matches
  </acceptance_criteria>
  <done>All 4 engine files compile without .continuity references. buildRuntimeIdentityLines no longer accepts or uses includeContinuity. minimumEvidenceForPromotion returns flat 1.</done>
</task>

<task type="auto">
  <name>Task 2: Remove sourceBundle/continuity from record-adapters and fix test fixture</name>
  <files>
    backend/src/character/record-adapters.ts,
    backend/src/character/__tests__/record-adapters.test.ts
  </files>
  <read_first>
    backend/src/character/record-adapters.ts,
    backend/src/character/__tests__/record-adapters.test.ts,
    shared/src/types.ts (CharacterRecord and CharacterDraft — confirm no sourceBundle/continuity fields)
  </read_first>
  <action>
    **record-adapters.ts — mergeEditableNpcDraft function (~line 670):**
    1. Delete line 679: `sourceBundle: npc.draft.sourceBundle,`
    2. Delete line 680: `continuity: npc.draft.continuity,`
    These two fields no longer exist on CharacterDraft. The surrounding lines (powerStats at 678, closing paren at 681) stay intact.

    **record-adapters.test.ts — CharacterRecord fixture (~line 349):**
    1. Delete the `continuity` object (lines 349-354) from the CharacterRecord fixture:
    ```
    continuity: {
      identityInertia: "anchored",
      protectedCore: ["Will not abandon the station"],
      mutableSurface: ["Trust in the player"],
      changePressureNotes: ["Major defeats can force realignment."],
    },
    ```
    This field no longer exists on the CharacterRecord type.

    **After both edits, run backend typecheck to confirm zero TS2339/TS2353 errors related to continuity or sourceBundle.**
  </action>
  <verify>
    <automated>npm --prefix backend run typecheck 2>&1 | grep -E "TS2339|TS2353" | grep -ci "continuity\|sourceBundle"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "sourceBundle" backend/src/character/record-adapters.ts returns zero matches
    - grep -n "continuity" backend/src/character/record-adapters.ts returns zero matches
    - grep -n "continuity" backend/src/character/__tests__/record-adapters.test.ts returns zero matches
    - npm --prefix backend run typecheck exits with code 0 (or at minimum, zero TS2339/TS2353 errors mentioning continuity or sourceBundle)
  </acceptance_criteria>
  <done>record-adapters.ts and its test compile cleanly. No references to sourceBundle or continuity remain in adapter code. Backend typecheck passes with zero Phase-57-related type errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm --prefix backend run typecheck` — must exit 0 or show zero TS2339/TS2353 errors on continuity/sourceBundle
2. `grep -rn "record\.continuity\|draft\.continuity\|draft\.sourceBundle\|npcRecord\.continuity" backend/src/` — must return zero matches
3. `grep -rn "includeContinuity\|identityInertia" backend/src/engine/` — must return zero matches
</verification>

<success_criteria>
- Backend compiles without any Phase-57-related TypeScript errors
- No engine or adapter file references .continuity, .sourceBundle, identityInertia, protectedCore, mutableSurface, or changePressureNotes
- SC-6 is satisfied: NPC agent, offscreen, reflection, and prompt-assembler work without continuity fields
- SC-4 is fully satisfied: old types removed from both definitions AND consumers
</success_criteria>

<output>
After completion, create `.planning/phases/57-power-scaling-character-profile-redesign/57-05-SUMMARY.md`
</output>
