---
phase: 39-honest-turn-boundary-retry-undo
plan: 03
type: execute
wave: 3
depends_on:
  - 39-02
files_modified:
  - frontend/app/game/page.tsx
  - frontend/app/game/__tests__/page.test.tsx
autonomous: true
requirements:
  - RINT-02
  - SIMF-02
gap_closure: true
must_haves:
  truths:
    - "A backend-style SSE retry error returns `/game` to the same committed pre-turn UI boundary that thrown retry parser failures restore."
    - "Optimistic retry placeholder content, stale completion affordances, and buffered follow-up actions are cleared before controls return to idle."
    - "Frontend regression coverage simulates the real retry failure contract by driving `parseTurnSSE()` through `onError` instead of only rejecting the parser promise."
  artifacts:
    - path: "frontend/app/game/page.tsx"
      provides: "One retry rollback cleanup path shared by terminal SSE errors and thrown retry failures inside `/game`"
      contains: "handleRetry"
    - path: "frontend/app/game/__tests__/page.test.tsx"
      provides: "Regression coverage for backend-style retry SSE errors and honest pre-turn rollback recovery"
      contains: "restores the honest pre-turn boundary after a failed retry"
  key_links:
    - from: "frontend/lib/api.ts"
      to: "frontend/app/game/page.tsx"
      via: "`parseTurnSSE()` `onError` callback handled as a terminal retry failure without changing route contracts"
      pattern: "onError"
    - from: "frontend/app/game/__tests__/page.test.tsx"
      to: "frontend/app/game/page.tsx"
      via: "mocked retry stream invokes `onError` and asserts the same rollback cleanup path as thrown failures"
      pattern: "onError\\("
---

<objective>
Close the Phase 39 verification blocker at the frontend retry seam so backend-style SSE retry failures trigger the same rollback cleanup the page already performs for thrown parser failures.

Purpose: The verifier found that `/api/chat/retry` can fail honestly on the backend while `/game` still leaves optimistic retry UI state visible because only thrown parser failures enter the rollback path.
Output: One focused frontend fix plus regression coverage that proves real SSE `error` retry failures restore the honest pre-turn boundary.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-honest-turn-boundary-retry-undo/39-CONTEXT.md
@.planning/phases/39-honest-turn-boundary-retry-undo/39-RESEARCH.md
@.planning/phases/39-honest-turn-boundary-retry-undo/39-VERIFICATION.md
@.planning/phases/39-honest-turn-boundary-retry-undo/39-02-SUMMARY.md
@frontend/lib/api.ts
@frontend/app/game/page.tsx
@frontend/app/game/__tests__/page.test.tsx

<interfaces>
From `frontend/lib/api.ts`:
```ts
export interface TurnSSEHandlers {
  onFinalizing?: () => void;
  onDone: () => void;
  onError: (error: string) => void;
}

export async function parseTurnSSE(
  body: ReadableStream<Uint8Array>,
  handlers: TurnSSEHandlers,
): Promise<void>;
```

From `frontend/app/game/page.tsx`:
```ts
const handleRetry = async () => {
  // optimistic assistant placeholder is inserted before parseTurnSSE()
  await parseTurnSSE(response.body, {
    onError: (error) => {
      toast.error("Retry error", { description: error });
    },
  });
} catch (error) {
  // rollback cleanup lives here today
  setMessages(rolledBackMessages);
  setHasLiveTurnSnapshot(false);
  await restoreGameplayState(activeCampaign.id, activeCampaign.premise);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the real retry SSE failure path in `/game` regressions</name>
  <files>frontend/app/game/__tests__/page.test.tsx</files>
  <behavior>
    - Test 1: the retry failure regression drives `parseTurnSSE()` by calling `handlers.onError("Retry replay failed")` and then resolving normally, matching the backend SSE contract from the verification gap.
    - Test 2: after that retry failure, the optimistic assistant placeholder and abandoned last-turn pair disappear, the committed pre-turn boundary remains visible, and `Retry turn` is no longer rendered because `hasLiveTurnSnapshot` recomputes from restored history.
    - Test 3: buffered quick actions stay cleared and the action input returns to idle-ready so the player is not left in a stale completed-turn shell.
  </behavior>
  <action>Replace the current rejected-parser retry regression with one that simulates the production contract from `39-VERIFICATION.md`: `parseTurnSSE()` invokes `onError` during retry and resolves without throwing. Per D-07, D-08, and D-09, assert that `/game` reaches the same rollback outcome as the existing thrown-failure path: optimistic retry UI is removed, the restored committed boundary is visible, retry/undo gating reflects the refreshed `hasLiveTurnSnapshot`, and the action bar is usable again. Keep coverage strictly on the Phase 39 frontend seam; do not add backend-route, checkpoint, inventory-authority, or multi-step undo assertions.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run app/game/__tests__/page.test.tsx</automated>
  </verify>
  <done>The regression fails until `/game` treats backend-style retry SSE `error` as a rollback trigger instead of a toast-only branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route retry SSE errors through the existing rollback cleanup</name>
  <files>frontend/app/game/page.tsx</files>
  <behavior>
    - Test 1: terminal retry SSE `error` enters the same cleanup path as a thrown retry parser failure.
    - Test 2: `/game` clears optimistic retry UI, resets `hasLiveTurnSnapshot` from restored history, and returns to idle without stale completion affordances.
    - Test 3: the fix preserves the existing backend SSE and route contract; no new route payloads or parser API changes are required.
  </behavior>
  <action>Keep the fix inside `frontend/app/game/page.tsx`. Do not redesign backend routes, change SSE event names, or broaden inventory/checkpoint/state-authority scope. Per the verification gap and D-07/D-09, unify retry failure handling so terminal SSE `error` drives the same rollback cleanup already used in `catch`. The preferred shape is to capture the retry `onError` message and rethrow after `parseTurnSSE()` returns so the existing catch path runs once, but an equivalent single-path cleanup helper is acceptable if it yields the same result. Per D-08, clear optimistic retry placeholder content, stale quick actions, and stale completion affordances before controls return to idle, then recompute `hasLiveTurnSnapshot` and world state from `restoreGameplayState(...)`. Keep assistant edit behavior intact per D-13, and do not modify `frontend/lib/api.ts` unless the executor can justify that `/game` alone cannot satisfy the gap.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run app/game/__tests__/page.test.tsx</automated>
  </verify>
  <done>Backend-style retry SSE failures and thrown parser failures now converge on one honest rollback cleanup path in `/game`, leaving the player at the restored pre-turn boundary instead of an abandoned optimistic retry shell.</done>
</task>

</tasks>

<verification>
Run the targeted `/game` Vitest file. Confirm the retry failure regression now uses the real SSE `error` path, that the UI rolls back to the committed pre-turn boundary, and that no retry control or stale quick action survives after the failed replay.
</verification>

<success_criteria>
The lone Phase 39 blocker is removed without reopening backend or broader UI scope: `/game` treats backend retry SSE errors as terminal rollback events, the player only sees the restored pre-turn boundary after a failed retry, and the regression test encodes the production failure mode that the verifier reported.
</success_criteria>

<output>
After completion, create `.planning/phases/39-honest-turn-boundary-retry-undo/39-03-SUMMARY.md`
</output>
