---
phase: 39-honest-turn-boundary-retry-undo
plan: 02
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - frontend/lib/api.ts
  - frontend/lib/__tests__/api.test.ts
  - frontend/app/game/page.tsx
  - frontend/app/game/__tests__/page.test.tsx
  - frontend/components/game/narrative-log.tsx
  - frontend/components/game/action-bar.tsx
autonomous: true
requirements:
  - SIMF-02
  - RINT-02
must_haves:
  truths:
    - "The UI distinguishes narration still streaming from the turn still finalizing, with copy that tells the player the world is still resolving and controls unlock only after completion."
    - "Retry, undo, and quick actions appear only after the authoritative turn boundary is reached."
    - "A narrated response alone is not treated as a completed turn, and a failed retry returns the player to an honest pre-turn UI state instead of leaving stale completion affordances behind."
  artifacts:
    - path: "frontend/lib/api.ts"
      provides: "SSE parser support for the backend finalization signal with an interface-safe optional handler for existing callers"
      contains: "onFinalizing"
    - path: "frontend/app/game/page.tsx"
      provides: "explicit gameplay turn-readiness state, honest retry/undo gating, quick-action timing, and failed-retry recovery"
      contains: "hasLiveTurnSnapshot"
    - path: "frontend/components/game/narrative-log.tsx"
      provides: "player-visible separation between streaming and finalizing states with explicit non-technical finalization copy"
      contains: "world"
  key_links:
    - from: "frontend/lib/api.ts"
      to: "frontend/app/game/page.tsx"
      via: "parseTurnSSE finalization callback that stays backward-safe for existing callers"
      pattern: "onFinalizing|onDone"
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/narrative-log.tsx"
      via: "turn-phase props controlling status copy, retry/undo visibility, and quick-action timing"
      pattern: "canRetryUndo|turnPhase|quickActions"
---

<objective>
Consume the honest backend turn-boundary contract in `/game` so the UI never treats narration completion as full turn completion.

Purpose: satisfy the player-facing half of `SIMF-02` and keep `retry/undo` availability aligned with the authoritative last-turn boundary from Plan 39-01.
Output: frontend SSE parsing, page state, and status UI that distinguish streaming from finalizing, keep quick actions and retry/undo behind authoritative completion, and recover cleanly from failed retry attempts without broadening the route contract.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-honest-turn-boundary-retry-undo/39-CONTEXT.md
@.planning/phases/39-honest-turn-boundary-retry-undo/39-RESEARCH.md
@.planning/phases/37-campaign-loaded-gameplay-transport/37-02-SUMMARY.md
@frontend/lib/api.ts
@frontend/app/game/page.tsx
@frontend/components/game/narrative-log.tsx
@frontend/components/game/action-bar.tsx
@frontend/lib/__tests__/api.test.ts
@frontend/app/game/__tests__/page.test.tsx

<interfaces>
From `frontend/lib/api.ts`:
```ts
export type ChatHistoryResponse = {
  messages: ChatMessage[];
  premise: string;
  hasLiveTurnSnapshot: boolean;
};

export interface TurnSSEHandlers {
  onNarrative: (text: string) => void;
  onOracleResult: (result: { chance: number; roll: number; outcome: string; reasoning: string }) => void;
  onStateUpdate: (update: { tool: string; args: unknown; result: unknown }) => void;
  onQuickActions: (actions: Array<{ label: string; action: string }>) => void;
  onFinalizing?: () => void;
  onDone: () => void;
  onError: (error: string) => void;
}
```

From `frontend/app/game/page.tsx`:
```ts
const [isStreaming, setIsStreaming] = useState(false);
const [hasLiveTurnSnapshot, setHasLiveTurnSnapshot] = useState(false);

const canRetryUndo =
  hasLiveTurnSnapshot &&
  !isStreaming &&
  messages.length >= 2 &&
  messages[messages.length - 1]?.role === "assistant";
```

From `frontend/components/game/narrative-log.tsx`:
```ts
interface NarrativeLogProps {
  messages: ChatMessage[];
  premise: string;
  isStreaming: boolean;
  onRetry?: () => void;
  onUndo?: () => void;
  canRetryUndo?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the frontend SSE and readiness contract in tests</name>
  <files>frontend/lib/__tests__/api.test.ts, frontend/app/game/__tests__/page.test.tsx</files>
  <behavior>
    - Test 1: `parseTurnSSE()` dispatches a dedicated finalization callback when the backend emits `finalizing_turn`, and the parser remains safe when `onFinalizing` is omitted by another caller.
    - Test 2: `/game` keeps retry/undo, quick actions, and the input surface non-ready during finalization even when an assistant message already exists.
    - Test 3: `/game` sets `hasLiveTurnSnapshot` only on authoritative `done`, not on narrative completion, and only then reveals quick actions.
    - Test 4: failed retry removes optimistic placeholder UI, refreshes back to the restored pre-turn boundary, surfaces an error, and does not leave stale completion copy or controls behind.
  </behavior>
  <action>Write failing frontend tests first. Extend the API helper tests to cover the new SSE event and make `onFinalizing` optional so the parser interface stays safe for any existing or future caller. Update the page tests so they prove the UI stays in an intermediate not-done state during finalization per `D-07`, `D-08`, and `D-09`, that quick actions remain hidden until authoritative `done`, and that failed retry returns the page to the restored pre-turn boundary rather than leaving the optimistic retry shell on screen. Keep edit behavior intact and do not add tests for multi-step undo, checkpoint restore, route redesign, or broader inventory UI behavior per `D-06`, `D-11`, `D-12`, and `D-13`.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run lib/__tests__/api.test.ts app/game/__tests__/page.test.tsx</automated>
  </verify>
  <done>The frontend tests fail only because the explicit finalization/readiness behavior, quick-action timing, parser interface safety, and failed-retry recovery have not been implemented yet, and the UI contract is pinned to the backend event semantics from Plan 39-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit turn-readiness state in `/game` and gameplay components</name>
  <files>frontend/lib/api.ts, frontend/app/game/page.tsx, frontend/components/game/narrative-log.tsx, frontend/components/game/action-bar.tsx</files>
  <behavior>
    - Test 1: the UI differentiates "storyteller is streaming" from "turn is finalizing" with distinct player-facing copy.
    - Test 2: retry/undo controls and quick actions only appear after the backend emits authoritative `done`.
    - Test 3: action submission stays blocked until finalization completes, then returns to normal idle readiness.
    - Test 4: failed retry lands the page in an honest restored pre-turn state with stale optimistic UI removed and clear error feedback shown.
  </behavior>
  <action>Extend `parseTurnSSE()` with a backward-safe optional `finalizing_turn` handler. In `/game`, replace the single `isStreaming` boolean as the source of truth with an explicit turn-readiness phase such as `idle | streaming | finalizing`, while preserving the existing route contract and campaign-addressed helpers from Phase 37. `NarrativeLog` and `ActionBar` must use explicit, non-technical copy guidance: the streaming state should say the response is still being narrated, and the finalizing state should say the world is still resolving or settling and that retry/undo will unlock when the turn is complete; do not surface raw transport terms like `SSE` or `finalizing_turn` to players. Keep `hasLiveTurnSnapshot` false until authoritative `done` arrives, hide or buffer quick actions until that same `done`, and clear stale quick actions on undo, new submit, or retry failure. For failed retry, remove the optimistic placeholder assistant message, refresh history/world data from the backend so the page returns to the restored pre-turn boundary, surface a clear error toast, and leave the action bar idle-ready for another attempt without pretending the abandoned retry completed. Keep assistant-message edit working per `D-13`, and do not add multi-step history, checkpoint UI changes, route redesign, or inventory-authority semantics.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run lib/__tests__/api.test.ts app/game/__tests__/page.test.tsx</automated>
  </verify>
  <done>The gameplay UI exposes an honest readiness contract: narration completion is not full completion, retry/undo and quick actions only unlock after authoritative `done`, finalizing copy explains that the world is still resolving, and failed retry returns the screen to a clean restored pre-turn state instead of leaving stale completion affordances behind.</done>
</task>

</tasks>

<verification>
Run the targeted frontend Vitest suite above. Confirm that `/game` remains blocked during finalization, shows distinct in-world copy for streaming vs finalizing, keeps quick actions and retry/undo hidden until authoritative completion from Plan 39-01, and returns to a clean restored pre-turn UI after failed retry.
</verification>

<success_criteria>
The frontend no longer guesses turn readiness from narration or message shape alone. It consumes the explicit backend completion contract, keeps retry/undo and quick actions honest, uses a safe optional parser interface for the finalization event, and preserves existing single-step retry/undo scope without broadening the route or restore contract.
</success_criteria>

<output>
After completion, create `.planning/phases/39-honest-turn-boundary-retry-undo/39-02-SUMMARY.md`
</output>
