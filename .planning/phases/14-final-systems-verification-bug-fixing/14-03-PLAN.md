---
phase: 14-final-systems-verification-bug-fixing
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 5 known bugs verified fixed in live browser testing"
    - "All systems from docs/ work end-to-end with GLM-5 Turbo"
    - "Quality score >= 4.5/5 across gameplay, UX, UI, error handling"
  artifacts: []
  key_links: []
---

<objective>
Comprehensive E2E verification of all fixed bugs and all systems described in docs/. Live browser playtest with GLM-5 Turbo to verify 4.5+ quality.

Purpose: Final sign-off that all systems work correctly before marking v1 complete.
Output: Verification results confirming all bugs fixed and all systems functional.
</objective>

<execution_context>
@C:/Users/robra/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-final-systems-verification-bug-fixing/14-CONTEXT.md
@.planning/phases/14-final-systems-verification-bug-fixing/14-01-SUMMARY.md
@.planning/phases/14-final-systems-verification-bug-fixing/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all 5 bug fixes via API and browser testing</name>
  <files></files>
  <read_first>backend/src/vectors/episodic-events.ts, backend/src/lib/mcp-client.ts, backend/src/engine/turn-processor.ts, frontend/app/game/page.tsx, backend/src/worldgen/lore-extractor.ts</read_first>
  <action>
Start both servers (backend :3001, frontend :3000). Run through each of the 5 fixed bugs:

**Bug 1 — Episodic events vector type:**
- Create a new campaign, generate world, create character, start game
- Submit an action — this triggers storeEpisodicEvent in the post-turn hook
- Check backend logs for "Stored episodic event" without any LanceDB errors
- Verify no "Failed to infer data type for field vector" error in logs

**Bug 2 — LanceDB episodic search resilience:**
- After the first turn (events stored without vectors), verify the prompt assembler doesn't crash when searching episodic events
- Check logs for graceful handling (empty results, no errors)

**Bug 3 — MCP spawn:**
- If a Known IP campaign exists or can be created (e.g., Naruto premise), trigger IP research
- Check backend logs: should either connect to MCP successfully OR fall back to LLM with "MCP[...] failed ... using fallback" message (not "spawn npx ENOENT")
- If MCP still fails on the chosen provider, that's OK as long as fallback works without ENOENT

**Bug 4 — Location sidebar:**
- During gameplay, submit a movement action (e.g., "go to [location name]")
- Watch the location sidebar — it should update DURING the stream (when state_update fires), not just after
- Verify the sidebar shows the new location name, NPCs, and paths

**Bug 5 — Quick actions:**
- Submit 5 consecutive actions
- Count how many times quick action buttons appear below the narrative
- Should be 5/5 (100%). If Storyteller omits the tool call, fallback actions should appear.

Report findings for each bug with PASS/FAIL and any error output.
  </action>
  <verify>
    <automated>curl -s http://localhost:3001/api/health | grep -q "ok" && echo "Backend running"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 bugs produce PASS results
    - No "Failed to infer data type" errors in backend logs
    - No "spawn npx ENOENT" errors in backend logs
    - Quick actions appear on 5/5 consecutive turns
    - Location sidebar updates mid-stream on movement
  </acceptance_criteria>
  <done>All 5 known bugs verified fixed in live environment</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All 5 bug fixes applied and verified via automated testing. Systems verified:
  1. Episodic events store without vector type errors
  2. MCP spawn uses platform-aware npx path
  3. Quick actions have server-side fallback (100% appearance)
  4. Location sidebar updates on state_update events
  5. Lore extraction has retry + reduced-schema fallback

  Additionally, all docs/ systems tested end-to-end with GLM-5 Turbo.</what-built>
  <how-to-verify>
    1. Open http://localhost:3000 in browser
    2. Create a new campaign with a Known IP premise (e.g., "Naruto")
    3. Go through world generation pipeline — verify lore cards are produced (Lore tab in world review)
    4. Create a character and start the game
    5. Submit 3-5 actions — verify:
       a. Quick actions appear after EVERY turn
       b. Location sidebar updates when you move
       c. Oracle panel shows probability results
       d. Narrative quality is coherent (no metadata leaks, no section headers in prose)
    6. Check Settings page — verify GLM-5 Turbo is configured for all roles
    7. Test save/load checkpoint
    8. Rate overall quality: gameplay, UX, UI, error handling (target: 4.5/5)
  </how-to-verify>
  <resume-signal>Type "approved" with quality score, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- All 5 bug fixes confirmed working in live browser
- All docs/ systems verified end-to-end
- Quality score >= 4.5/5 from human verification
</verification>

<success_criteria>
Phase 14 complete: all known bugs fixed, all systems verified working with GLM-5 Turbo, quality >= 4.5/5.
</success_criteria>

<output>
After completion, create `.planning/phases/14-final-systems-verification-bug-fixing/14-03-SUMMARY.md`
</output>
