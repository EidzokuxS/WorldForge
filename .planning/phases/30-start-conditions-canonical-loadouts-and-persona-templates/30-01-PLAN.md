---
phase: 30-start-conditions-canonical-loadouts-and-persona-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/types.ts
  - shared/src/index.ts
  - backend/src/routes/schemas.ts
  - backend/src/routes/__tests__/schemas.test.ts
  - backend/src/character/persona-templates.ts
  - backend/src/character/loadout-deriver.ts
  - backend/src/character/__tests__/persona-templates.test.ts
  - backend/src/character/__tests__/loadout-deriver.test.ts
autonomous: true
requirements:
  - P30-03
  - P30-04
  - P30-06
must_haves:
  truths:
    - "Phase 30 adds one explicit contract surface for persona templates, start-resolution results, and canonical loadout specs without inventing a parallel character model, per D-01 and D-04."
    - "Later plans can extend typed helper modules for template application and loadout derivation instead of rediscovering or retyping those seams."
    - "Compatibility remains additive: legacy save payloads and the current Phase 29 draft seam still validate while new fields are introduced, per D-09."
  artifacts:
    - path: "shared/src/types.ts"
      provides: "Shared Phase 30 contracts for persona templates, resolved start state, and canonical loadout specs"
    - path: "backend/src/routes/schemas.ts"
      provides: "Zod validation for new Phase 30 payloads and additive compatibility aliases"
    - path: "backend/src/character/persona-templates.ts"
      provides: "Pure template-patch merge seam for player and NPC drafts"
    - path: "backend/src/character/loadout-deriver.ts"
      provides: "Pure canonical loadout derivation seam with audit output"
  key_links:
    - from: "shared/src/types.ts"
      to: "backend/src/routes/schemas.ts"
      via: "shared contract to route validation"
      pattern: "PersonaTemplate|ResolvedStartConditions|CanonicalLoadout"
    - from: "backend/src/character/persona-templates.ts"
      to: "shared/src/types.ts"
      via: "template patch application against CharacterDraft"
      pattern: "CharacterDraft|PersonaTemplate"
    - from: "backend/src/character/loadout-deriver.ts"
      to: "shared/src/types.ts"
      via: "deterministic loadout spec derivation from canonical draft fields"
      pattern: "CharacterDraft|CanonicalLoadout"
---

<objective>
Lock the Phase 30 contract surface before any route or UI wiring starts.

Purpose: Phase 30 should extend the current Phase 29 `CharacterDraft` seam, not reopen the character-model debate. This plan defines the exact shared contracts and pure helper seams that later backend and frontend plans will implement against.
Output: shared types, route schemas, and pure helper modules for persona templates and canonical loadout derivation.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-CONTEXT.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-RESEARCH.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-phase-29-30-handoff.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-01-SUMMARY.md
@shared/src/types.ts
@backend/src/routes/schemas.ts

<decisions>
- D-01: Build on the current Phase 29 worktree and shared `CharacterDraft` seam; do not invent a parallel character model.
- D-04: Persona templates must be reusable across player and NPC flows and feed the shared draft pipeline.
- D-05: Creation and review surfaces must expose the new fields through the existing draft seams.
- D-08: Prefer campaign-scoped persona templates unless broader scope is proven necessary.
- D-09: Compatibility work should cover existing saves/NPC rows only as much as Phase 30 needs.
</decisions>

<interfaces>
Current shared seam already available to extend:
```ts
export interface CharacterDraft {
  identity: CharacterIdentityDraft;
  profile: CharacterProfile;
  socialContext: CharacterSocialContext;
  motivations: CharacterMotivations;
  capabilities: CharacterCapabilities;
  state: CharacterState;
  loadout: CharacterLoadout;
  startConditions: CharacterStartConditions;
  provenance: CharacterProvenance;
}
```

Current provenance/start fields already present but underused:
```ts
export interface CharacterStartConditions {
  startLocationId?: string | null;
  arrivalMode?: string | null;
  immediateSituation?: string | null;
  entryPressure?: string[];
  companions?: string[];
  startingVisibility?: string | null;
  resolvedNarrative?: string | null;
  sourcePrompt?: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend shared Phase 30 contracts and route schemas without breaking the Phase 29 seam</name>
  <files>shared/src/types.ts, shared/src/index.ts, backend/src/routes/schemas.ts, backend/src/routes/__tests__/schemas.test.ts</files>
  <behavior>
    - Test 1: route schemas accept additive Phase 30 payloads for persona template CRUD/apply and richer start-resolution responses while legacy save aliases still validate.
    - Test 2: shared types expose one reusable `PersonaTemplate` / template-patch contract for both player and NPC drafts, per D-04.
    - Test 3: canonical loadout derivation and start-resolution outputs have explicit typed shapes so later plans do not invent ad hoc objects.
  </behavior>
  <action>Extend the shared type layer and route validation layer with the exact contracts Phase 30 needs: campaign-scoped persona templates, a template patch format that targets `CharacterDraft`, a structured start-resolution result, and a canonical loadout spec/audit shape. Keep `CharacterDraft` authoritative per D-01 and make the new types additive to the current Phase 29 seam instead of creating player-only or NPC-only variants per D-04. In `backend/src/routes/schemas.ts`, add Zod schemas for persona template list/create/update/apply payloads and for the enriched start-resolution response/body shape, but keep the current compatibility aliases alive so existing save flows still parse during the migration per D-09. Do not broaden this into runtime prompt rewrites or UI work.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/routes/__tests__/schemas.test.ts</automated>
  </verify>
  <done>Shared contracts and route schemas explicitly describe Phase 30 start/template/loadout data while preserving the current Phase 29 draft/save compatibility seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create pure helper seams for template patching and canonical loadout derivation</name>
  <files>backend/src/character/persona-templates.ts, backend/src/character/loadout-deriver.ts, backend/src/character/__tests__/persona-templates.test.ts, backend/src/character/__tests__/loadout-deriver.test.ts</files>
  <behavior>
    - Test 1: applying a persona template patch to either a player or NPC draft preserves role/tier identity and only mutates the intended grouped fields.
    - Test 2: the loadout deriver returns a deterministic normalized spec plus audit metadata even when start-state inputs are partial.
    - Test 3: both helper modules stay pure and compile without campaign I/O so wave 2 plans can extend them safely.
  </behavior>
  <action>Create two pure backend helper modules that later plans will extend instead of reimplementing: `persona-templates.ts` for draft-patch merge behavior and `loadout-deriver.ts` for deterministic scenario-to-loadout derivation. Make the persona merge helper apply to the shared `CharacterDraft` seam for both players and NPCs per D-04, and make the loadout helper return a typed spec/audit structure rather than raw string arrays. Keep these helpers side-effect free in this plan; no campaign storage, route registration, or item writes yet. The point of this plan is to lock contracts and merge rules early so wave 2 can implement behavior against stable exports.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/character/__tests__/persona-templates.test.ts src/character/__tests__/loadout-deriver.test.ts</automated>
  </verify>
  <done>The repo has stable pure helper seams for persona template application and canonical loadout derivation, ready for backend route/storage work in later plans.</done>
</task>

</tasks>

<verification>
Run the schema and helper-module regressions and confirm Phase 30 now has one typed contract surface for start conditions, canonical loadout derivation, and persona templates.
</verification>

<success_criteria>
Later Phase 30 plans can work against explicit contracts and pure helper exports instead of exploring or improvising new models, while the current Phase 29 compatibility seam remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-01-SUMMARY.md`
</output>
