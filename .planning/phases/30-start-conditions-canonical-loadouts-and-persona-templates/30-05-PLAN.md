---
phase: 30-start-conditions-canonical-loadouts-and-persona-templates
plan: 05
type: execute
wave: 4
depends_on:
  - "30-04"
files_modified:
  - frontend/app/character-creation/page.tsx
  - frontend/app/character-creation/__tests__/page.test.tsx
  - frontend/app/campaign/[id]/character/page.tsx
  - frontend/app/campaign/[id]/character/__tests__/page.test.tsx
  - frontend/components/character-creation/character-card.tsx
  - frontend/components/character-creation/__tests__/character-card.test.tsx
autonomous: true
requirements:
  - P30-01
  - P30-02
  - P30-03
  - P30-04
  - P30-05
must_haves:
  truths:
    - "The current player-facing character editors expose structured start conditions, persona-template application, and backend-driven canonical loadout preview through the existing draft seam, per D-05."
    - "Users can resolve start state and inspect the authoritative starting equipment before save without waiting for a full persistence cycle."
    - "The player-facing UI remains within the current Phase 29 shell instead of drifting into the Phase 32 redesign."
  artifacts:
    - path: "frontend/components/character-creation/character-card.tsx"
      provides: "Shared player editor controls for start conditions, template selection, and canonical loadout preview"
    - path: "frontend/app/character-creation/page.tsx"
      provides: "New-character page wiring for structured start/template/loadout flows"
    - path: "frontend/app/campaign/[id]/character/page.tsx"
      provides: "Existing-campaign character page wiring for the same flows"
  key_links:
    - from: "frontend/components/character-creation/character-card.tsx"
      to: "frontend/lib/api.ts"
      via: "resolve-start and preview-loadout actions"
      pattern: "resolveStartingLocation|preview.*loadout"
    - from: "frontend/app/character-creation/page.tsx"
      to: "frontend/components/character-creation/character-card.tsx"
      via: "shared draft state passed into the current character editor shell"
      pattern: "CharacterDraft|personaTemplates"
    - from: "frontend/app/campaign/[id]/character/page.tsx"
      to: "frontend/components/character-creation/character-card.tsx"
      via: "existing campaign character flow reusing the same shared editor"
      pattern: "draft|startConditions|loadout"
---

<objective>
Expose Phase 30 start/template/loadout workflows in the player-facing character editors.

Purpose: After the client/data seam is stable, the player-facing pages can stay focused on one concern: letting the user edit structured start conditions, apply personas, and preview canonical loadouts in the current character editor shell.
Output: updated player character pages and shared `CharacterCard` controls wired to backend-owned preview and template flows.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-CONTEXT.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-RESEARCH.md
@frontend/lib/api.ts
@frontend/lib/character-drafts.ts
@frontend/app/character-creation/page.tsx
@frontend/app/campaign/[id]/character/page.tsx
@frontend/components/character-creation/character-card.tsx

<decisions>
- D-01: Build on the current Phase 29 worktree and shared `CharacterDraft` seam.
- D-04: Persona templates must be reusable across player and NPC flows and feed the shared draft pipeline.
- D-05: Creation surfaces must expose the new fields through existing draft seams; do not create alternate frontend data models.
- D-07: Keep this phase scoped away from the Phase 32 UI overhaul.
</decisions>

<interfaces>
The player-facing pages already share one editor surface:
```ts
type CharacterCardProps = {
  draft: CharacterDraft;
  personaTemplates: PersonaTemplateSummary[];
  onDraftChange: (next: CharacterDraft) => void;
};
```

The API/helper layer now provides backend-owned preview/apply calls:
```ts
previewCanonicalLoadout(draft: CharacterDraft): Promise<CanonicalLoadoutPreview>;
applyPersonaTemplate(templateId: string, draft: CharacterDraft): Promise<CharacterDraft>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 30 controls to the shared player character editor</name>
  <files>frontend/components/character-creation/character-card.tsx, frontend/components/character-creation/__tests__/character-card.test.tsx</files>
  <behavior>
    - Test 1: `CharacterCard` exposes structured start-condition fields instead of only a free-text prompt and detached location selector.
    - Test 2: the editor can list/apply campaign persona templates while keeping `CharacterDraft` as the source of truth.
    - Test 3: the editor can request a backend-owned canonical loadout preview and render the result before save.
  </behavior>
  <action>Update `frontend/components/character-creation/character-card.tsx` so the shared player editor surface exposes the Phase 30 controls within the existing component shell: richer start-condition fields, campaign persona-template selection/application, and canonical loadout preview actions that call the backend-owned preview seam. Keep the component draft-backed per D-01 and D-05; do not introduce a separate template editor object or local preview derivation logic.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run components/character-creation/__tests__/character-card.test.tsx</automated>
  </verify>
  <done>The shared player editor surface can edit structured start state, apply personas, and show authoritative loadout previews without leaving the current component shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire both player character pages to the shared Phase 30 editor flow</name>
  <files>frontend/app/character-creation/page.tsx, frontend/app/character-creation/__tests__/page.test.tsx, frontend/app/campaign/[id]/character/page.tsx, frontend/app/campaign/[id]/character/__tests__/page.test.tsx</files>
  <behavior>
    - Test 1: the new-character page resolves start conditions, previews canonical loadout, and saves through the shared draft-backed flow.
    - Test 2: the in-campaign character page exposes the same Phase 30 actions without regressing the current Phase 29 editing path.
    - Test 3: page structure stays recognizably within the current shell rather than introducing Phase 32 redesign work.
  </behavior>
  <action>Wire `frontend/app/character-creation/page.tsx` and `frontend/app/campaign/[id]/character/page.tsx` to the updated `CharacterCard` and helper-layer APIs so both player-facing flows expose the same Phase 30 functionality. Reuse the current draft state, save path, and page layout per D-05 and D-07. Keep the page work focused on orchestration and error handling, not new layout architecture.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run app/character-creation/__tests__/page.test.tsx app/campaign/[id]/character/__tests__/page.test.tsx</automated>
  </verify>
  <done>Both player character pages surface Phase 30 behavior cleanly through the existing draft-backed editor flow.</done>
</task>

</tasks>

<verification>
Run the player editor and page regressions, then visually inspect the changed character pages to confirm the new controls were layered into the current shell rather than a new UI architecture.
</verification>

<success_criteria>
Users can edit structured start conditions, apply campaign persona templates, and preview canonical starting equipment from both player-facing character pages before saving.
</success_criteria>

<output>
After completion, create `.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-05-SUMMARY.md`
</output>
