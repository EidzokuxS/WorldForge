---
phase: 30-start-conditions-canonical-loadouts-and-persona-templates
plan: 06
type: execute
wave: 4
depends_on:
  - "30-04"
files_modified:
  - frontend/app/campaign/[id]/review/page.tsx
  - frontend/app/campaign/[id]/review/__tests__/page.test.tsx
  - frontend/components/world-review/npcs-section.tsx
  - frontend/components/world-review/__tests__/npcs-section.test.tsx
autonomous: true
requirements:
  - P30-04
  - P30-05
must_haves:
  truths:
    - "World review NPC editing can list and apply campaign persona templates through the same shared draft seam used by player flows, per D-04 and D-05."
    - "NPC review surfaces can display and refresh structured start/loadout context without inventing a separate NPC-only editor model."
    - "The review flow remains within the existing Phase 29 review shell; this plan is scoped to editor behavior, not UI overhaul."
  artifacts:
    - path: "frontend/app/campaign/[id]/review/page.tsx"
      provides: "Review-page data plumbing for persona templates and canonical draft metadata"
    - path: "frontend/components/world-review/npcs-section.tsx"
      provides: "NPC editor controls for template application plus start/loadout visibility"
  key_links:
    - from: "frontend/app/campaign/[id]/review/page.tsx"
      to: "frontend/components/world-review/npcs-section.tsx"
      via: "review page passing persona-template and draft data into NPC editing"
      pattern: "personaTemplates|draft"
    - from: "frontend/components/world-review/npcs-section.tsx"
      to: "frontend/lib/character-drafts.ts"
      via: "NPC draft round-tripping through the shared helper seam"
      pattern: "CharacterDraft|startConditions|loadout"
---

<objective>
Finish Phase 30 UI wiring for NPC review without keeping it bundled to the player-facing character pages.

Purpose: NPC review is a separate concern with its own files and tests. Splitting it out keeps the old 30-04 frontend work within context budget and lets player-facing and NPC-facing UI execution run in parallel once the shared frontend data seam lands.
Output: review-page and NPC-section integration for campaign persona templates plus canonical start/loadout visibility on shared NPC drafts.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-CONTEXT.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-RESEARCH.md
@frontend/lib/api.ts
@frontend/lib/character-drafts.ts
@frontend/app/campaign/[id]/review/page.tsx
@frontend/components/world-review/npcs-section.tsx

<decisions>
- D-01: Build on the current Phase 29 worktree and shared `CharacterDraft` seam.
- D-04: Persona templates must be reusable across player and NPC flows and feed the shared draft pipeline.
- D-05: Review surfaces must expose the new fields through existing draft seams; do not create alternate frontend data models.
- D-07: Keep this phase scoped away from the Phase 32 UI overhaul.
</decisions>

<interfaces>
The review flow already round-trips NPCs through shared draft helpers:
```ts
function withNpcDraft(npc: ScaffoldNpc, draft: CharacterDraft): ScaffoldNpc {
  return {
    ...npc,
    ...characterDraftToScaffoldNpc(draft),
    draft,
  };
}
```

The page-level world payload now carries template data:
```ts
type WorldData = {
  personaTemplates: PersonaTemplateSummary[];
  npcs: Array<{ draft?: CharacterDraft | null }>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plumb campaign persona templates into the review page state</name>
  <files>frontend/app/campaign/[id]/review/page.tsx, frontend/app/campaign/[id]/review/__tests__/page.test.tsx</files>
  <behavior>
    - Test 1: the review page loads/stores campaign persona templates alongside the current world payload and passes them into NPC editing state.
    - Test 2: review-page state updates remain additive and do not regress the current Phase 29 review workflow.
  </behavior>
  <action>Update `frontend/app/campaign/[id]/review/page.tsx` to carry the campaign persona-template collection and richer canonical NPC draft data from the normalized world payload into the existing review workflow. Keep the review page focused on data plumbing and orchestration; do not rebuild page layout or introduce new review shells.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run app/campaign/[id]/review/__tests__/page.test.tsx</automated>
  </verify>
  <done>The review page exposes the Phase 30 template/draft data needed for NPC editing without altering the overall review-shell architecture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose template application and canonical start/loadout visibility in NPC review editing</name>
  <files>frontend/components/world-review/npcs-section.tsx, frontend/components/world-review/__tests__/npcs-section.test.tsx</files>
  <behavior>
    - Test 1: NPC review can list/apply campaign persona templates against shared NPC drafts.
    - Test 2: NPC editing can display or refresh structured start/loadout context through the same shared draft model instead of a separate NPC-only object.
    - Test 3: existing NPC review edits still round-trip through the scaffold compatibility bridge.
  </behavior>
  <action>Update `frontend/components/world-review/npcs-section.tsx` so NPC review exposes campaign persona-template actions and canonical start/loadout visibility using the same draft-backed seam as player flows. Reuse `CharacterDraft` plus the existing scaffold projection helpers per D-01 and D-05. Keep the work scoped to editing behavior and data wiring, not Phase 32 visual redesign.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run components/world-review/__tests__/npcs-section.test.tsx</automated>
  </verify>
  <done>NPC review editing now participates in the same Phase 30 persona/start/loadout model as player creation and editing.</done>
</task>

</tasks>

<verification>
Run the review-page and NPC-section regressions and confirm NPC editing now consumes the shared Phase 30 draft/template/loadout data through the existing review shell.
</verification>

<success_criteria>
World review NPC editing can use campaign persona templates and see canonical start/loadout context without forking away from the shared draft model.
</success_criteria>

<output>
After completion, create `.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-06-SUMMARY.md`
</output>
