---
phase: 30-start-conditions-canonical-loadouts-and-persona-templates
plan: 03
type: execute
wave: 2
depends_on:
  - "30-01"
files_modified:
  - backend/src/campaign/manager.ts
  - backend/src/character/persona-templates.ts
  - backend/src/routes/persona-templates.ts
  - backend/src/routes/campaigns.ts
  - backend/src/index.ts
  - backend/src/character/__tests__/persona-templates.test.ts
  - backend/src/routes/__tests__/persona-templates.test.ts
  - backend/src/routes/__tests__/campaigns.test.ts
autonomous: true
requirements:
  - P30-04
  - P30-05
  - P30-06
must_haves:
  truths:
    - "Persona templates are campaign-scoped, storable, selectable, and reusable for both player and NPC drafts without creating separate template models, per D-04 and D-08."
    - "Older campaigns load safely with an empty template list by default; Phase 30 does not require an eager backfill or new global library, per D-09."
    - "Current campaign/world payloads expose available persona templates so existing editor surfaces can consume them in a later UI plan."
  artifacts:
    - path: "backend/src/campaign/manager.ts"
      provides: "Campaign config persistence helpers for persona templates"
    - path: "backend/src/character/persona-templates.ts"
      provides: "Template storage/apply logic against CharacterDraft"
    - path: "backend/src/routes/persona-templates.ts"
      provides: "Campaign-scoped persona template CRUD and apply endpoints"
    - path: "backend/src/routes/campaigns.ts"
      provides: "World payload exposure of campaign persona templates"
  key_links:
    - from: "backend/src/campaign/manager.ts"
      to: "backend/src/character/persona-templates.ts"
      via: "campaign config storage feeding template application"
      pattern: "personaTemplates|config"
    - from: "backend/src/routes/persona-templates.ts"
      to: "backend/src/character/persona-templates.ts"
      via: "CRUD and apply endpoints using shared draft merge logic"
      pattern: "applyPersona|template"
    - from: "backend/src/routes/campaigns.ts"
      to: "backend/src/routes/persona-templates.ts"
      via: "world payload exposing selectable templates for current editor surfaces"
      pattern: "personaTemplates"
---

<objective>
Add campaign-scoped persona template storage and backend application paths on top of the shared draft seam.

Purpose: Persona templates are a first-class Phase 30 deliverable, but they should stay campaign-local and draft-based instead of growing into a separate character system. This plan adds storage, CRUD/apply routes, and world-payload exposure without expanding into Phase 32 UI or a global library.
Output: campaign-local persona template persistence, apply endpoints, and additive world payload wiring.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-CONTEXT.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-RESEARCH.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-05-SUMMARY.md
@backend/src/campaign/manager.ts
@backend/src/routes/campaigns.ts
@backend/src/index.ts
@backend/src/character/persona-templates.ts

<decisions>
- D-01: Build on the current Phase 29 worktree and shared `CharacterDraft` seam.
- D-04: Persona templates must be reusable across player and NPC flows and feed the shared draft pipeline.
- D-05: Existing creation/review surfaces should consume the new data through the current draft seams.
- D-08: Prefer campaign-scoped persona templates unless broader scope is proven necessary now.
- D-09: Keep migration/backfill work minimal for existing saves and configs.
</decisions>

<interfaces>
Current campaign config shape does not yet carry persona templates:
```ts
type CampaignConfigFile = {
  name: string;
  premise: string;
  seeds?: WorldSeeds;
  ipContext?: IpResearchContext;
  premiseDivergence?: PremiseDivergence;
  worldbookSelection?: CampaignWorldbookSelection[];
  generationComplete?: boolean;
  currentTick?: number;
  createdAt: number;
  updatedAt?: number;
};
```

Current world payload stops at player/NPC canonical data:
```ts
return c.json({
  locations,
  npcs,
  factions,
  relationships,
  player,
  items,
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist campaign-local persona templates and expose CRUD/apply endpoints</name>
  <files>backend/src/campaign/manager.ts, backend/src/character/persona-templates.ts, backend/src/routes/persona-templates.ts, backend/src/index.ts, backend/src/character/__tests__/persona-templates.test.ts, backend/src/routes/__tests__/persona-templates.test.ts</files>
  <behavior>
    - Test 1: campaign configs without persona templates load with `[]` and preserve all existing fields after template writes.
    - Test 2: template CRUD stays campaign-scoped and never leaks or requires a cross-campaign/global library.
    - Test 3: applying a template to either a player or NPC draft returns one merged `CharacterDraft` plus compatibility aliases instead of separate player/NPC template models.
  </behavior>
  <action>Implement campaign-local persona template persistence in `backend/src/campaign/manager.ts` and `backend/src/character/persona-templates.ts`, using the campaign config as the narrowest Phase 30 storage surface per D-08 and D-09. The research leaned toward SQLite, but for this phase use config-backed storage because it avoids extra DB migration churn and still satisfies the campaign-scoped requirement. Add a dedicated `backend/src/routes/persona-templates.ts` module with list/create/update/delete/apply endpoints nested under the campaign namespace, register it in `backend/src/index.ts`, and have the apply endpoint merge onto the shared `CharacterDraft` seam for both players and NPCs per D-04. Do not create player-only or NPC-only template contracts.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/character/__tests__/persona-templates.test.ts src/routes/__tests__/persona-templates.test.ts</automated>
  </verify>
  <done>Campaigns can store and manage persona templates locally, and backend apply endpoints can merge those templates onto shared player/NPC drafts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose campaign persona templates through world payloads and shared route outputs</name>
  <files>backend/src/routes/campaigns.ts, backend/src/routes/persona-templates.ts, backend/src/routes/__tests__/campaigns.test.ts, backend/src/routes/__tests__/persona-templates.test.ts</files>
  <behavior>
    - Test 1: `GET /api/campaigns/:id/world` returns a `personaTemplates` collection alongside the existing player/NPC payload without regressing legacy fields.
    - Test 2: template apply responses return shared draft data plus the same compatibility aliases the current frontend already expects.
    - Test 3: missing template ids return clear 404 responses instead of silently no-oping.
  </behavior>
  <action>Update the campaign world payload and the new persona-template route outputs so the current editor surfaces can list, select, and apply templates without inventing another data fetch model per D-05. Keep the route contracts additive: include `personaTemplates` beside the existing world payload and return merged shared-draft results with compatibility aliases for current callers. Limit migration work to safe defaults for old configs and missing template ids; do not add any global registry or Phase 32 UI concerns.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/routes/__tests__/campaigns.test.ts src/routes/__tests__/persona-templates.test.ts</automated>
  </verify>
  <done>Current campaign payloads and backend route outputs surface persona templates cleanly enough for the existing editor flows to consume in the frontend plan.</done>
</task>

</tasks>

<verification>
Run the persona-template and campaigns route regressions and confirm templates are now campaign-local backend resources exposed through the shared draft seam.
</verification>

<success_criteria>
Phase 30 has a real backend persona-template system: campaign-scoped storage, CRUD/apply routes, and additive world-payload exposure, all without introducing a second character model or a global library.
</success_criteria>

<output>
After completion, create `.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-03-SUMMARY.md`
</output>
