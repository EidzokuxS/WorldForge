---
phase: 30-start-conditions-canonical-loadouts-and-persona-templates
plan: 02
type: execute
wave: 2
depends_on:
  - "30-01"
files_modified:
  - backend/src/worldgen/starting-location.ts
  - backend/src/routes/character.ts
  - backend/src/character/loadout-deriver.ts
  - backend/src/engine/prompt-assembler.ts
  - backend/src/worldgen/__tests__/starting-location.test.ts
  - backend/src/routes/__tests__/character.test.ts
  - backend/src/character/__tests__/loadout-deriver.test.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
autonomous: true
requirements:
  - P30-01
  - P30-02
  - P30-03
  - P30-05
  - P30-06
must_haves:
  truths:
    - "Start selection resolves and persists as structured `startConditions`, not just a one-off location+narrative result, per D-02."
    - "Pre-save canonical loadout preview is explicitly owned by a backend route/service seam so the frontend reads authoritative derived equipment before save, per D-03 and D-05."
    - "Starting equipment is derived deterministically from canonical scenario state, then materialized into real item rows on save instead of staying a detached string list."
    - "Prompt/runtime readers receive concise structured start/loadout context without turning this plan into the broader Phase 31 prompt harmonization."
  artifacts:
    - path: "backend/src/worldgen/starting-location.ts"
      provides: "Structured start-condition resolution service"
    - path: "backend/src/routes/character.ts"
      provides: "Persisted start-condition flow plus backend-owned pre-save loadout preview/save endpoints"
    - path: "backend/src/character/loadout-deriver.ts"
      provides: "Deterministic scenario-to-loadout rule implementation shared by preview and save paths"
    - path: "backend/src/engine/prompt-assembler.ts"
      provides: "Compact prompt-facing formatting for start and loadout context"
  key_links:
    - from: "backend/src/routes/character.ts"
      to: "backend/src/worldgen/starting-location.ts"
      via: "route alias calling the structured resolver"
      pattern: "resolveStartingLocation|startConditions"
    - from: "backend/src/routes/character.ts"
      to: "backend/src/character/loadout-deriver.ts"
      via: "backend-owned preview plus save-time canonical loadout derivation and item materialization"
      pattern: "preview.*loadout|loadout|items|ownerId"
    - from: "backend/src/engine/prompt-assembler.ts"
      to: "backend/src/routes/character.ts"
      via: "prompt sections consuming persisted canonical start/loadout fields"
      pattern: "startConditions|equippedItemRefs|signatureItems"
---

<objective>
Make the player start state and starting equipment canonical, persisted, and usable by runtime readers.

Purpose: Phase 30 fails if `startConditions` stays inert or if loadout remains a detached vibe list. This plan upgrades the existing route/service seam so start state becomes authoritative and loadout becomes deterministic, materialized state.
Output: structured start-condition resolution, a backend-owned canonical loadout preview seam, save-time loadout materialization, and prompt-facing compact formatting.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-CONTEXT.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-RESEARCH.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-03-SUMMARY.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-05-SUMMARY.md
@backend/src/worldgen/starting-location.ts
@backend/src/routes/character.ts
@backend/src/character/loadout-deriver.ts
@backend/src/engine/prompt-assembler.ts

<decisions>
- D-01: Build on the current Phase 29 worktree and shared `CharacterDraft` seam.
- D-02: Replace loose starting-location resolution with persisted `startConditions` rather than another transient helper layer.
- D-03: Canonical loadout derivation must come from structured scenario/context state and end at real item materialization.
- D-05: Keep exposing new behavior through existing draft seams.
- D-06: Keep Phase 30 scoped away from Phase 31 prompt harmonization.
- D-09: Address migration for existing saves/NPC rows only as much as Phase 30 needs.
</decisions>

<interfaces>
Current start-resolution seam to upgrade:
```ts
export interface StartingLocationResult {
  locationName: string;
  narrative: string;
}
```

Current save boundary already persists a canonical record:
```ts
const characterRecord = createCharacterRecordFromDraft(
  {
    ...draft,
    socialContext: {
      ...draft.socialContext,
      currentLocationId: matchedLocation.id,
      currentLocationName: matchedLocation.name,
    },
  },
  { id: playerId, campaignId },
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade start resolution into a persisted structured start-condition flow</name>
  <files>backend/src/worldgen/starting-location.ts, backend/src/routes/character.ts, backend/src/worldgen/__tests__/starting-location.test.ts, backend/src/routes/__tests__/character.test.ts</files>
  <behavior>
    - Test 1: the start-resolution service returns a structured start-condition result including resolved location id/name plus arrival and immediate-situation fields, while the compatibility route still returns legacy `locationId/locationName/narrative` aliases.
    - Test 2: `save-character` persists `draft.startConditions` and reconciles `socialContext.currentLocationId/currentLocationName` from the resolved start state instead of treating the narrative as transient.
    - Test 3: older drafts that only carry `currentLocationName` still save successfully through a compatibility repair path per D-09.
  </behavior>
  <action>Refactor `backend/src/worldgen/starting-location.ts` so the internal resolver produces a structured `startConditions` object instead of only `{ locationName, narrative }`, then keep `/api/worldgen/resolve-starting-location` in `backend/src/routes/character.ts` as a migration-friendly shell that exposes both the new structured payload and the old alias fields per D-02. On the save path, persist resolved `startConditions` onto the canonical draft/record and use the resolved location id/name as the authoritative bridge into `socialContext`. Do not add a second frontend-only state model; keep the route working through the existing draft seam per D-01 and D-05.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldgen/__tests__/starting-location.test.ts src/routes/__tests__/character.test.ts</automated>
  </verify>
  <done>Start resolution is now a persisted structured scenario flow, and the old location-only contract survives only as an additive compatibility shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Own the backend preview seam and materialize canonical loadouts while keeping runtime summaries compact</name>
  <files>backend/src/character/loadout-deriver.ts, backend/src/routes/character.ts, backend/src/engine/prompt-assembler.ts, backend/src/character/__tests__/loadout-deriver.test.ts, backend/src/routes/__tests__/character.test.ts, backend/src/engine/__tests__/prompt-assembler.test.ts</files>
  <behavior>
    - Test 1: a backend preview endpoint derives the canonical loadout from draft plus structured scenario fields before save and returns deterministic preview/audit data.
    - Test 2: saving a character materializes the same derived loadout into real `items` rows and keeps `characterRecord.loadout`, `players.equippedItems`, and owned items synchronized.
    - Test 3: prompt assembly renders concise start/loadout context from canonical fields without broad wording rewrites outside this plan's scope.
  </behavior>
  <action>Implement the real rule stack in `backend/src/character/loadout-deriver.ts` so canonical loadouts come from structured scenario/context inputs per D-03, not from loose editor strings. In `backend/src/routes/character.ts`, explicitly own the pre-save preview seam by adding a backend route/API handler that accepts the current shared draft plus structured start/template context and returns the deterministic canonical loadout preview/audit payload for the UI; do not leave preview logic as a frontend-only guess. Then wire the save path to the same deriver, materialize or replace starting item rows transactionally, and keep the legacy `players.equippedItems` bridge synchronized for runtime compatibility. In `backend/src/engine/prompt-assembler.ts`, add minimal compact-view formatting for persisted `startConditions` and canonical loadout summaries so runtime prompts can reason about the new fields per D-06 without turning this into the broader Phase 31 prompt audit. Handle legacy campaigns narrowly: when old saves still lack item rows, fall back to the canonical record's loadout snapshot instead of introducing a repo-wide backfill sweep.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/character/__tests__/loadout-deriver.test.ts src/routes/__tests__/character.test.ts src/engine/__tests__/prompt-assembler.test.ts</automated>
  </verify>
  <done>The backend owns one authoritative canonical loadout derivation seam for both preview and save, and starting equipment is saved as real item state exposed to runtime readers through compact canonical summaries.</done>
</task>

</tasks>

<verification>
Run the start-resolution, character-route, loadout-deriver, and prompt-assembler regressions and verify the authoritative path is now backend-owned `CharacterDraft.startConditions/loadout` preview/save logic, not detached helper strings.
</verification>

<success_criteria>
Players now enter the world through a persisted scenario model, and their canonical starting equipment is previewed and saved through one backend-owned derivation path that runtime prompts can describe consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-02-SUMMARY.md`
</output>
