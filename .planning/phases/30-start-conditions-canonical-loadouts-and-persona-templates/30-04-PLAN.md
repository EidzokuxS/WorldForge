---
phase: 30-start-conditions-canonical-loadouts-and-persona-templates
plan: 04
type: execute
wave: 3
depends_on:
  - "30-02"
  - "30-03"
files_modified:
  - frontend/lib/api-types.ts
  - frontend/lib/api.ts
  - frontend/lib/character-drafts.ts
  - frontend/lib/world-data-helpers.ts
  - frontend/lib/__tests__/api.test.ts
  - frontend/lib/__tests__/world-data-helpers.test.ts
autonomous: true
requirements:
  - P30-01
  - P30-03
  - P30-04
  - P30-05
must_haves:
  truths:
    - "The frontend client layer understands the richer backend contracts for structured start resolution, backend-owned loadout preview, and campaign persona templates."
    - "Player and NPC editors still round-trip one shared `CharacterDraft` model instead of inventing alternate frontend DTOs for start or template flows, per D-01 and D-04."
    - "Later UI plans can consume normalized world/template/loadout data through helpers instead of reimplementing API response shaping in pages."
  artifacts:
    - path: "frontend/lib/api-types.ts"
      provides: "Typed frontend contracts for enriched start-resolution, loadout preview, and persona-template payloads"
    - path: "frontend/lib/api.ts"
      provides: "Client helpers for backend preview/apply/list flows"
    - path: "frontend/lib/character-drafts.ts"
      provides: "Shared draft normalization and merge helpers aware of template/start/loadout data"
    - path: "frontend/lib/world-data-helpers.ts"
      provides: "World payload normalization that carries persona templates and canonical draft metadata"
  key_links:
    - from: "frontend/lib/api.ts"
      to: "backend/src/routes/character.ts"
      via: "structured start-resolution and backend-owned preview route calls"
      pattern: "resolveStartingLocation|preview.*loadout|startConditions"
    - from: "frontend/lib/api.ts"
      to: "backend/src/routes/persona-templates.ts"
      via: "campaign persona-template CRUD/apply client calls"
      pattern: "personaTemplates|apply"
    - from: "frontend/lib/world-data-helpers.ts"
      to: "frontend/lib/character-drafts.ts"
      via: "normalized world payload feeding one shared draft seam"
      pattern: "CharacterDraft|personaTemplates|loadout"
---

<objective>
Prepare the frontend data layer for Phase 30 without bundling it together with page-level UI work.

Purpose: The old 30-04 plan was too large because it mixed contract normalization, API wiring, and two distinct UI surfaces. This plan isolates the client/data seam so later player and NPC UI plans can stay small and parallel-friendly.
Output: updated frontend API/types plus shared draft/world normalization helpers for structured start conditions, backend-owned loadout preview, and campaign persona templates.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-CONTEXT.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-RESEARCH.md
@frontend/lib/api-types.ts
@frontend/lib/api.ts
@frontend/lib/character-drafts.ts
@frontend/lib/world-data-helpers.ts

<decisions>
- D-01: Build on the current Phase 29 worktree and shared `CharacterDraft` seam.
- D-04: Persona templates must be reusable across player and NPC flows and feed the shared draft pipeline.
- D-05: Existing creation/review surfaces should consume the new data through the current draft seams.
- D-07: Keep this phase scoped away from the Phase 32 UI overhaul.
- D-08: Persona templates stay campaign-scoped in this phase.
</decisions>

<interfaces>
Current frontend world payload already centers canonical drafts:
```ts
export interface WorldData {
  locations: LocationRecord[];
  npcs: Array<{
    characterRecord?: CharacterRecord | null;
    draft?: CharacterDraft | null;
    npc?: ScaffoldNpc | null;
  }>;
  player: {
    characterRecord?: CharacterRecord | null;
    draft?: CharacterDraft | null;
    character?: ParsedCharacter | null;
  } | null;
}
```

The backend plans now own these new seams:
```ts
type ResolvedStartConditions = {
  startConditions: CharacterStartConditions;
  locationId: string;
  locationName: string;
};

type CanonicalLoadoutPreview = {
  equippedItemRefs: string[];
  signatureItems: string[];
  audit: string[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend frontend API contracts and calls for structured start/template/loadout flows</name>
  <files>frontend/lib/api-types.ts, frontend/lib/api.ts, frontend/lib/__tests__/api.test.ts</files>
  <behavior>
    - Test 1: frontend API helpers can call the enriched start-resolution route, the backend-owned loadout preview route, and the campaign persona-template CRUD/apply routes.
    - Test 2: typed responses preserve additive compatibility aliases so partially migrated callers do not break during Phase 30 execution.
    - Test 3: API helpers expose one shared draft-centric result shape rather than separate player/NPC template or preview models.
  </behavior>
  <action>Extend `frontend/lib/api-types.ts` and `frontend/lib/api.ts` to understand the Phase 30 backend surface introduced by 30-02 and 30-03: richer start-resolution responses, backend-owned canonical loadout preview responses, and campaign persona-template CRUD/apply endpoints. Keep the client contracts additive so the current Phase 29 worktree baseline still parses legacy aliases while the new fields come online. Do not push response shaping into pages; this plan owns the reusable client seam.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run lib/__tests__/api.test.ts</automated>
  </verify>
  <done>The frontend has typed, reusable API helpers for all Phase 30 backend seams without introducing player-only or NPC-only contract forks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize world payloads and draft helpers around one shared Phase 30 data model</name>
  <files>frontend/lib/character-drafts.ts, frontend/lib/world-data-helpers.ts, frontend/lib/__tests__/world-data-helpers.test.ts</files>
  <behavior>
    - Test 1: world payload normalization carries persona templates plus richer canonical player/NPC draft data without regressing existing aliases.
    - Test 2: draft helper projections preserve one shared `CharacterDraft` seam even after template metadata and preview loadout state are introduced.
    - Test 3: downstream pages/components can consume normalized data without reimplementing payload merges.
  </behavior>
  <action>Refine `frontend/lib/character-drafts.ts` and `frontend/lib/world-data-helpers.ts` so both player and NPC flows continue to round-trip through one shared draft contract per D-01 and D-04. Normalize persona-template collections, richer start-condition data, and canonical loadout preview metadata into helper-layer outputs that later UI plans can consume directly. Keep backwards-compatible normalization in place until the backend migration is fully closed.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run lib/__tests__/world-data-helpers.test.ts</automated>
  </verify>
  <done>The frontend helper layer exposes one normalized Phase 30 data seam for later page/component wiring.</done>
</task>

</tasks>

<verification>
Run the frontend API/helper regressions and confirm the page-level plans can consume Phase 30 data through reusable helpers instead of page-local response shaping.
</verification>

<success_criteria>
The former 30-04 frontend data concerns are isolated into one moderate-sized plan, and all later Phase 30 UI work can build on a stable shared client seam.
</success_criteria>

<output>
After completion, create `.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-04-SUMMARY.md`
</output>
