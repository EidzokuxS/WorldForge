---
phase: 47-storyteller-output-quality-and-anti-slop-prompting
plan: 02
type: execute
wave: 2
depends_on:
  - 47-01
files_modified:
  - backend/src/engine/prompt-assembler.ts
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/routes/__tests__/chat.test.ts
autonomous: true
requirements:
  - WRIT-01
must_haves:
  truths:
    - "Both hidden and final visible storyteller passes receive the same baseline-plus-GLM writing contract through the live runtime path."
    - "Prompting adapts by scene mode using runtime facts, not a vague always-literary voice, per D-01 and D-02."
    - "Response-length and anti-slop guidance stay bounded so GLM quality does not collapse under prompt bloat."
    - "The runtime still prioritizes engine truth, action clarity, and perception boundaries while improving prose quality."
    - "If Phase 47 introduces a minimal storyteller profile/settings seam, runtime prompt assembly and storyteller model selection read the same authoritative setting."
  artifacts:
    - path: "backend/src/engine/prompt-assembler.ts"
      provides: "scene-adaptive prompt sections and bounded storyteller system text"
      contains: "scene mode|baseline|glm|length"
    - path: "backend/src/engine/turn-processor.ts"
      provides: "live hidden/final narration calls using the storyteller-aware model creation seam"
      contains: "createModel|final-visible|hidden-tool-driving"
    - path: "backend/src/engine/__tests__/prompt-assembler.test.ts"
      provides: "regressions for scene-adaptive prompt assembly and bounded anti-slop rules"
      contains: "dialogue|combat|quiet|glm"
  key_links:
    - from: "backend/src/engine/storyteller-presets.ts"
      to: "backend/src/engine/prompt-assembler.ts"
      via: "prompt assembly selects the appropriate baseline and scene-mode overlays"
      pattern: "scene mode|preset|overlay"
    - from: "backend/src/ai/provider-registry.ts"
      to: "backend/src/engine/turn-processor.ts"
      via: "live storyteller passes opt into bounded GLM-specific behavior"
      pattern: "createModel\\(.*storyteller"
---

<objective>
Wire the new preset and model foundation into the live storyteller prompt path.

Purpose: make better first-pass prose the default live behavior for hidden and visible narration without reopening engine truth or adding a rewrite stack.
Output: scene-adaptive prompt assembly and runtime storyteller calls that actually use the Phase 47 preset layer.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-CONTEXT.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-RESEARCH.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-VALIDATION.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-01-PLAN.md
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/turn-processor.ts
@backend/src/engine/storyteller-presets.ts
@backend/src/engine/storyteller-contract.ts
@backend/src/engine/__tests__/prompt-assembler.test.ts
@backend/src/engine/__tests__/turn-processor.test.ts

<interfaces>
Expected from `backend/src/engine/storyteller-presets.ts` after Plan 47-01:
```ts
export type StorytellerSceneMode = "dialogue" | "combat" | "quiet" | "horror" | "default";

export function buildStorytellerPresetBlocks(options: {
  pass: "hidden-tool-driving" | "final-visible";
  sceneMode: StorytellerSceneMode;
  family: "baseline" | "glm";
}): string[];
```

Expected from `backend/src/ai/provider-registry.ts` after Plan 47-01:
```ts
export function createModel(
  config: ProviderConfig,
  options?: { role?: "storyteller" | "judge" | "generator" | "embedder"; familyHint?: "baseline" | "glm" },
): LanguageModel;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 47-02-01: Make prompt assembly scene-adaptive, bounded, and preset-driven</name>
  <files>backend/src/engine/prompt-assembler.ts, backend/src/engine/__tests__/prompt-assembler.test.ts</files>
  <behavior>
    - Test 1: combat, dialogue, quiet, and eerie scenes select different scene-mode guidance while keeping the same anti-slop/agency/perception baseline.
    - Test 2: hidden and final-visible passes both receive bounded preset-derived instructions without duplicating or bloating the prompt.
    - Test 3: prompt assembly keeps response-length guidance explicit and runtime-derived rather than forcing one always-literary voice.
  </behavior>
  <action>Update `prompt-assembler.ts` to derive a conservative `StorytellerSceneMode` from runtime facts already in hand: scene effects, action result, current scene tags, presence/awareness hints, and opening-state pressure. Per D-01 and D-02, encode adaptive scene guidance so combat reads tighter and more physical, quiet or dialogue scenes can breathe, and eerie scenes can become atmospheric without drifting into melodrama. Keep the baseline and GLM overlay concise to avoid the prompt-bloat pitfall from research. Do not add classifiers, second opinion models, or narrative doctrine outside the preset module. The output should stay backend-owned and explicit: prompt sections should show where the style contract comes from and keep length/pacing instructions bounded.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/prompt-assembler.test.ts</automated>
  </verify>
  <done>Prompt assembly now feeds live storyteller passes with scene-adaptive, preset-derived guidance instead of a single generic prose voice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 47-02-02: Route live storyteller calls through the new role-aware model seam without adding a rewrite stack</name>
  <files>backend/src/engine/turn-processor.ts, backend/src/engine/__tests__/turn-processor.test.ts, backend/src/routes/__tests__/chat.test.ts</files>
  <behavior>
    - Test 1: hidden-tool-driving, final-visible, and opening-scene narration all call the storyteller-aware model path and preserve existing turn/event ordering.
    - Test 2: non-storyteller behavior is unchanged; the new overlay affects only storyteller calls.
    - Test 3: routes still emit `scene-settling`, `narrative`, `finalizing_turn`, and `done` correctly while using the improved first-pass prompt contract.
  </behavior>
  <action>Update `turn-processor.ts` so the hidden pass, final visible pass, and opening-scene generation opt into the storyteller-aware `createModel(...)` seam from Plan 47-01. This runtime wiring must intentionally use the storyteller GLM behavior chosen in `47-01`, not accidentally fall back to a prose-flattening reasoning path. Keep D-06 and D-07 explicit: improve the first visible pass itself, do not bolt on a mandatory rewrite model or a second creative stage. Preserve all Phase 39/45/46 truth boundaries: no regression to duplicated scene assembly, no player-action leakage, no omniscient visible narration, and no route-contract changes beyond using the better storyteller model path. If `47-01` added a minimal storyteller profile setting, read that same setting here for prompt/model selection instead of duplicating another config seam. Extend route-level tests only where they prove the action/opening SSE path still honors the improved storyteller integration.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/turn-processor.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>The live gameplay storyteller path now uses the Phase 47 preset and GLM overlay foundation end-to-end without changing the engine contract.</done>
</task>

</tasks>

<verification>
Run the runtime smoke for prompt assembly and turn processing. Confirm that the preset layer is now live in hidden/final/opening narration and that the SSE turn contract still matches Phase 39/45/46 expectations.
</verification>

<success_criteria>
Phase 47 writing improvements are now real runtime behavior: scene-adaptive prompting, bounded GLM tuning, and no drift away from deterministic engine truth.
</success_criteria>

<output>
After completion, create `.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-02-SUMMARY.md`
</output>
