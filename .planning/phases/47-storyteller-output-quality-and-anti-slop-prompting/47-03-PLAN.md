---
phase: 47-storyteller-output-quality-and-anti-slop-prompting
plan: 03
type: execute
wave: 3
depends_on:
  - 47-02
files_modified:
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/routes/__tests__/chat.test.ts
  - .planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-SMOKE-CHECKLIST.md
autonomous: false
requirements:
  - WRIT-01
must_haves:
  truths:
    - "First-pass prompting remains the primary quality mechanism; any post-generation guard is narrow, evidence-backed, and at most one retry."
    - "Visible narration cannot leak section headers/tool syntax, restart the same lead paragraph, or keep obvious slop clusters once the phase is complete."
    - "Phase 47 closes only after live GLM smoke across multiple scene types confirms better RP writing without truth regressions."
  artifacts:
    - path: "backend/src/engine/turn-processor.ts"
      provides: "bounded final-visible quality guard with no heavy rewrite stack"
      contains: "sanitize|retry|slop|lead"
    - path: ".planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-SMOKE-CHECKLIST.md"
      provides: "manual GLM verification rubric for dialogue, combat, quiet, and eerie scenes"
      contains: "combat|dialogue|quiet|horror|approved"
    - path: "backend/src/engine/__tests__/turn-processor.test.ts"
      provides: "regressions for repeated-lead, leak, and slop-cluster handling"
      contains: "retry|sanitize|slop"
  key_links:
    - from: "backend/src/engine/turn-processor.ts"
      to: "backend/src/routes/chat.ts"
      via: "route-visible narration remains single-pass even if a bounded retry is triggered once"
      pattern: "narrative|finalizing_turn|done"
    - from: ".planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-SMOKE-CHECKLIST.md"
      to: "backend/src/engine/turn-processor.ts"
      via: "manual verification judges the exact failure modes guarded in code"
      pattern: "slop|repeat|omniscience"
---

<objective>
Close Phase 47 with the smallest quality guard justified by evidence and a live GLM smoke pass.

Purpose: keep Phase 47 honest to D-06 and D-07 by adding no more than the narrow guard the runtime actually needs, then verify the result on real scene outputs.
Output: an evidence-backed final-quality seam and a phase-local smoke checklist for sign-off.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-CONTEXT.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-RESEARCH.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-VALIDATION.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-02-PLAN.md
@backend/src/engine/turn-processor.ts
@backend/src/routes/chat.ts
@backend/src/engine/__tests__/turn-processor.test.ts
@backend/src/routes/__tests__/chat.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 47-03-01: Add only the bounded final-visible guard justified by evidence and write the live smoke checklist</name>
  <files>backend/src/engine/turn-processor.ts, backend/src/engine/__tests__/turn-processor.test.ts, backend/src/routes/__tests__/chat.test.ts, .planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-SMOKE-CHECKLIST.md</files>
  <behavior>
    - Test 1: repeated lead paragraphs, surviving header/tool leaks, and obvious banned slop clusters are either handled by existing sanitization or corrected by at most one retry.
    - Test 2: if the prompt-only path already resolves those failures, the code does not introduce an unnecessary rewrite loop.
    - Test 3: route-visible SSE behavior stays single-pass and bounded even when the guard fires.
  </behavior>
  <action>Implement the smallest evidence-backed quality guard in `turn-processor.ts`. Start from the research-backed failure list only: repeated opening/lead paragraph, section-header or tool-call leakage that survives existing sanitizers, and a short banned slop-cluster set derived from the preset matrix. If these failures are already prevented by the Plan 47-01/02 contract changes plus `sanitizeNarrative()` and `collapseRepeatedNarrationBlocks()`, keep the post-generation guard as detection-only and document in `47-SMOKE-CHECKLIST.md` that no retry was necessary. If one or more failure modes still reproduce in tests or captured fixtures, add exactly one tightened final-visible retry and no more. Do not add a second creative pipeline, regex paraphraser, or giant rewrite pass, per D-06 and D-07. Write `47-SMOKE-CHECKLIST.md` with explicit before/after quality checks for combat, dialogue, quiet scene-setting, and eerie/horror-adjacent scenes.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/turn-processor.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>The runtime has either no retry or one narrow retry, and the phase-local smoke checklist exists with the exact quality failures it is meant to catch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 47-03-02: Verify live GLM storyteller quality against the phase checklist</name>
  <files>.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-SMOKE-CHECKLIST.md</files>
  <action>Run the final backend smoke suite, then use the checklist to judge live GLM output quality across the required scene modes before phase closeout. This checkpoint exists because WRIT-01 needs human product judgment, not because Claude should defer automatable work.</action>
  <what-built>Preset-driven storyteller quality tuning is live in hidden/final/opening narration, with at most one bounded final-visible guard and a phase-local smoke checklist.</what-built>
  <how-to-verify>
    1. Start the app with the configured storyteller model that Phase 47 targets (`GLM-5.1` if available, otherwise the current `GLM-5` fallback).
    2. Use one existing campaign and run four short checks using `.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-SMOKE-CHECKLIST.md`:
       - combat/action-heavy turn
       - quiet scene-setting or travel-adjacent turn
       - dialogue/negotiation turn
       - eerie or tension-heavy turn
    3. Confirm each sample materially reduces purple prose, generic assistant smell, and repeated emotional restatement while keeping:
       - no player-action impersonation
       - no omniscient scene leakage
       - no repeated opening paragraph
       - clear action/consequence readability
    4. If the checklist finds regressions, record the failing scenario and stop phase closeout until the guard/prompt contract is corrected.
  </how-to-verify>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/storyteller-contract.test.ts src/engine/__tests__/storyteller-presets.test.ts src/engine/__tests__/prompt-assembler.test.ts src/engine/__tests__/turn-processor.test.ts src/ai/__tests__/provider-registry.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>Live GLM quality is either approved against the checklist or returned with specific failing scenarios for follow-up work.</done>
  <resume-signal>Type "approved" if the live GLM smoke passes, or reply with the failing checklist items.</resume-signal>
</task>

</tasks>

<verification>
Run the Phase 47 full smoke suite before the human checkpoint, then use the checklist to verify real live outputs across multiple scene modes on the target GLM path.
</verification>

<success_criteria>
WorldForge storyteller output now feels materially more playable in live RP: less slop, less inflation, no truth regressions, and no hidden heavy rewrite system sneaking in under the hood.
</success_criteria>

<output>
After completion, create `.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-03-SUMMARY.md`
</output>
