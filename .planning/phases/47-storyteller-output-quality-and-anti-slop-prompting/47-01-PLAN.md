---
phase: 47-storyteller-output-quality-and-anti-slop-prompting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/engine/storyteller-presets.ts
  - backend/src/engine/storyteller-contract.ts
  - backend/src/ai/provider-registry.ts
  - backend/src/settings/manager.ts
  - shared/src/settings.ts
  - shared/src/types.ts
  - backend/src/engine/__tests__/storyteller-presets.test.ts
  - backend/src/engine/__tests__/storyteller-contract.test.ts
  - backend/src/ai/__tests__/provider-registry.test.ts
autonomous: true
requirements:
  - WRIT-01
must_haves:
  truths:
    - "WorldForge has one backend-owned storyteller preset seam that emits a portable baseline plus a GLM overlay instead of scattering style text across prompts."
    - "Portable preset motifs are explicitly adapted into runtime rules, while ST macros, jailbreak text, and persona shells are rejected."
    - "Hidden and final narration contracts both assemble from the same preset source, so writing-quality changes do not fork into separate doctrines."
    - "GLM-targeted tuning stays an overlay on the standard model path instead of creating a provider-by-provider preset tree."
    - "The storyteller GLM path explicitly handles reasoning-vs-temperature compatibility instead of silently inheriting a prose-flattening middleware path."
  artifacts:
    - path: "backend/src/engine/storyteller-presets.ts"
      provides: "portable preset normalization and baseline plus GLM overlay builders"
      contains: "baseline|glm|portable|reject"
    - path: "backend/src/engine/storyteller-contract.ts"
      provides: "pass-specific storyteller contract assembled from the preset seam"
      contains: "hidden-tool-driving|final-visible"
    - path: "backend/src/ai/provider-registry.ts"
      provides: "storyteller-aware model creation hook for GLM overlay behavior and bounded reasoning/sampler compatibility"
      contains: "storyteller|glm|reasoning|temperature"
    - path: "backend/src/settings/manager.ts"
      provides: "optional minimal storyteller profile/settings seam if live tuning needs a stable user-selectable surface"
      contains: "storyteller|profile"
  key_links:
    - from: "backend/src/engine/storyteller-presets.ts"
      to: "backend/src/engine/storyteller-contract.ts"
      via: "contract builder imports normalized preset blocks instead of inline ad hoc prose"
      pattern: "build.*preset|baseline|overlay"
    - from: "backend/src/ai/provider-registry.ts"
      to: "backend/src/engine/turn-processor.ts"
      via: "storyteller model creation can later opt into bounded GLM-specific handling without changing other roles"
      pattern: "createModel|storyteller"
---

<objective>
Create the backend-owned preset and contract foundation for storyteller quality tuning.

Purpose: turn Phase 47 into explicit runtime contracts and bounded model behavior instead of vague prompt tinkering.
Output: a new preset module, a reassembled storyteller contract, and a role-aware GLM overlay seam ready for runtime integration.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-CONTEXT.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-RESEARCH.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-VALIDATION.md
@backend/src/engine/storyteller-contract.ts
@backend/src/ai/provider-registry.ts
@backend/src/worldgen/scaffold-steps/prompt-utils.ts
@backend/src/engine/__tests__/storyteller-contract.test.ts
@backend/src/ai/__tests__/provider-registry.test.ts

<interfaces>
From `backend/src/engine/storyteller-contract.ts`:
```ts
export type StorytellerPass = "hidden-tool-driving" | "final-visible";

export function buildStorytellerContract(
  options?: {
    pass?: StorytellerPass;
    includeWorldRules?: boolean;
    includeContextRules?: boolean;
    includeToolSupportRules?: boolean;
  },
): string;
```

From `backend/src/ai/provider-registry.ts`:
```ts
export interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  apiKey: string;
  model: string;
  protocol?: ProviderProtocol;
}

export function createModel(
  config: ProviderConfig,
  options?: { role?: "storyteller" | "judge" | "generator" | "embedder"; familyHint?: "baseline" | "glm" },
): LanguageModel;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 47-01-01: Build the portable preset matrix and export baseline plus GLM overlay blocks</name>
  <files>backend/src/engine/storyteller-presets.ts, backend/src/engine/__tests__/storyteller-presets.test.ts, backend/src/engine/__tests__/storyteller-contract.test.ts</files>
  <behavior>
    - Test 1: baseline preset output includes simulation-first RP rules, anti-impersonation, anti-omniscience, anti-repetition, and bounded length guidance derived from portable motifs.
    - Test 2: GLM overlay is smaller and additive, aimed at repetition/overthinking control rather than replacing the baseline.
    - Test 3: rejected motifs such as ST macros, jailbreak text, or persona shells never appear in exported preset blocks.
  </behavior>
  <action>Per D-03, D-04, D-05, and D-10, create `backend/src/engine/storyteller-presets.ts` as the only runtime-owned preset normalization seam. Encode an explicit motif matrix that adapts portable community patterns into WorldForge rules: simulation-first framing, player-agency boundary, perception-bounded knowledge, anti-drama/anti-echo, concrete language, and bounded response length. Add a small scene-adaptive vocabulary layer so later plans can vary combat, dialogue, horror, and quiet scenes without inventing a new doctrine. Reject and omit SillyTavern macros, placeholder variables, jailbreak/safety-bypass text, and persona-shell framing. Keep the module data-driven and testable rather than pasting opaque prompt blobs. Extend `storyteller-contract.test.ts` only enough to assert that the contract will consume the new preset builders; do not wire runtime callers yet.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/storyteller-presets.test.ts src/engine/__tests__/storyteller-contract.test.ts</automated>
  </verify>
  <done>The repo has an explicit, tested preset module that captures portable anti-slop and RP-writing motifs without importing ST baggage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 47-01-02: Reassemble the storyteller contract around the preset seam and add a storyteller-aware GLM model hook</name>
  <files>backend/src/engine/storyteller-contract.ts, backend/src/ai/provider-registry.ts, backend/src/engine/__tests__/storyteller-contract.test.ts, backend/src/ai/__tests__/provider-registry.test.ts</files>
  <behavior>
    - Test 1: `buildStorytellerContract()` composes pass rules with baseline preset text and adds GLM overlay text only when explicitly requested.
    - Test 2: hidden-tool-driving and final-visible contracts share the same preset source while preserving pass-specific responsibilities.
    - Test 3: provider-registry exposes a storyteller-aware model path for GLM-family models without changing non-storyteller roles or creating provider-specific preset trees.
    - Test 4: the storyteller GLM path does not silently inherit a reasoning middleware combination that strips the sampler behavior needed for prose quality.
    - Test 5: existing `createModel(config)` call sites remain valid and behavior-compatible unless they explicitly opt into storyteller options.
  </behavior>
  <action>Update `storyteller-contract.ts` so both passes are assembled from the new preset module instead of standalone string piles. Preserve runtime-truth constraints from Phase 45/46 while rewriting the style layer around the preset outputs. In `provider-registry.ts`, add a bounded storyteller-aware creation seam that later runtime code can opt into via `createModel(config, options?)`, where `options?: { role?: "storyteller" | "judge" | "generator" | "embedder"; familyHint?: "baseline" | "glm" }`. Keep this backward-compatible: `createModel(config)` must remain the default valid call shape for all existing callers, with unchanged behavior unless `options` are explicitly passed. As part of that seam, make the storyteller GLM path explicitly decide whether it should bypass the current reasoning/temperature-stripping behavior or otherwise preserve the sampler behavior needed for prose quality, instead of inheriting it by accident. Keep D-06, D-07, D-08, and D-09 explicit: no heavy rewrite pipeline, no provider explosion, no sampler-dashboard sprawl. The GLM handling should remain one overlay on the existing OpenAI-compatible path and must not regress Anthropic/OpenAI compatibility. If execution shows that the overlay needs a stable user-visible switch, add only one minimal storyteller profile enum through `shared/src/types.ts`, `shared/src/settings.ts`, and `backend/src/settings/manager.ts`; do not add a sampler dashboard.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/storyteller-contract.test.ts src/ai/__tests__/provider-registry.test.ts</automated>
  </verify>
  <done>The contract and model factory now expose one explicit baseline-plus-GLM foundation that downstream runtime plans can consume directly.</done>
</task>

</tasks>

<verification>
Run the contract smoke and confirm the new foundation is explicit and backend-owned: preset normalization, contract assembly, and GLM storyteller overlay are all tested independently before runtime wiring begins.
</verification>

<success_criteria>
Phase 47 no longer depends on hand-wavy prompt prose. WorldForge has a concrete preset layer and a bounded storyteller GLM hook that future plans can wire into the live turn path.
</success_criteria>

<output>
After completion, create `.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-01-SUMMARY.md`
</output>
