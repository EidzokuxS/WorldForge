---
phase: 27-lore-card-editing-and-deletion
plan: 03
type: execute
wave: 3
depends_on:
  - "27-01"
  - "27-02"
files_modified:
  - backend/src/routes/__tests__/lore.test.ts
  - backend/src/vectors/__tests__/lore-cards.test.ts
  - frontend/lib/__tests__/api.test.ts
  - frontend/components/world-review/__tests__/lore-section.test.tsx
  - .planning/phases/27-lore-card-editing-and-deletion/27-03-smoke-target.md
autonomous: false
requirements:
  - P27-01
  - P27-02
  - P27-03
  - P27-04
  - P27-05
  - P27-06
must_haves:
  truths:
    - "Automated tests fail if lore edit/delete success, validation, not-found, or vector-refresh behavior regresses."
    - "Frontend tests fail if LoreSection stops showing card actions, pending state, or refresh-after-mutation behavior."
    - "Before the human checkpoint starts, the repo already has a running dev app plus one concrete campaign id and review URL with at least two lore cards."
    - "A human can follow one short smoke script against the prepared review URL and confirm edit, search refresh, delete, and persistence through reload."
  artifacts:
    - path: "backend/src/routes/__tests__/lore.test.ts"
      provides: "Route regression coverage for lore item edit/delete HTTP behavior"
    - path: "backend/src/vectors/__tests__/lore-cards.test.ts"
      provides: "Vector-layer coverage for stable-id rewrite and delete-by-id helpers"
    - path: "frontend/lib/__tests__/api.test.ts"
      provides: "Frontend client helper coverage for lore item requests and error propagation"
    - path: "frontend/components/world-review/__tests__/lore-section.test.tsx"
      provides: "Component coverage for edit/delete UI state and refresh clearing"
    - path: ".planning/phases/27-lore-card-editing-and-deletion/27-03-smoke-target.md"
      provides: "Prepared campaign id, ready-to-open review URL, and named lore cards for the blocking smoke pass"
  key_links:
    - from: "backend/src/routes/__tests__/lore.test.ts"
      to: "backend/src/routes/lore.ts"
      via: "Hono request contract assertions"
      pattern: "PUT|DELETE"
    - from: "backend/src/vectors/__tests__/lore-cards.test.ts"
      to: "backend/src/vectors/lore-cards.ts"
      via: "stable-id mutation helper assertions"
      pattern: "updateLoreCard|deleteLoreCard"
    - from: "frontend/components/world-review/__tests__/lore-section.test.tsx"
      to: "frontend/components/world-review/lore-section.tsx"
      via: "interaction and refresh tests"
      pattern: "onRefresh|setResults"
    - from: ".planning/phases/27-lore-card-editing-and-deletion/27-03-smoke-target.md"
      to: "frontend/app/campaign/[id]/review/page.tsx"
      via: "recorded concrete smoke URL and selected campaign id"
      pattern: "reviewUrl: http://localhost:3000/campaign/.+/review"
---

<objective>
Lock Phase 27 behavior behind regression tests and finish with a world-review smoke pass.

Purpose: The backend and UI implementation ownership lives in Plans 27-01 and 27-02. This plan is the regression-and-smoke gate that proves those implementations hold together, closes remaining coverage gaps, and ends with one real interaction pass.
Output: focused route/vector/frontend tests for Phase 27 plus a prepared smoke-target note and deterministic checklist for the implemented lore edit/delete flow.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-lore-card-editing-and-deletion/27-CONTEXT.md
@.planning/phases/27-lore-card-editing-and-deletion/27-RESEARCH.md
@backend/src/routes/__tests__/lore.test.ts
@backend/src/vectors/__tests__/lore-cards.test.ts
@frontend/lib/__tests__/api.test.ts
@frontend/components/world-review/__tests__/lore-section.test.tsx
@frontend/components/world-review/lore-section.tsx
@frontend/lib/api.ts

<interfaces>
Phase 27 required coverage from context:
```md
- successful edit
- successful delete
- validation failure
- not-found handling
```

Current gaps from research:
```md
- lore route tests still use stale title/content card shapes
- vector tests do not cover stable-id edit/delete helpers
- lore-section tests only cover read-only rendering
- frontend vitest should run from frontend/ workdir
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 27 regression coverage across route, vector, API, and component seams</name>
  <files>backend/src/routes/__tests__/lore.test.ts, backend/src/vectors/__tests__/lore-cards.test.ts, frontend/lib/__tests__/api.test.ts, frontend/components/world-review/__tests__/lore-section.test.tsx</files>
  <behavior>
    - Test 1: `PUT /api/campaigns/:id/lore/:cardId` succeeds with real `term/definition/category` payloads and returns 404 for missing cards.
    - Test 2: invalid lore edit payloads fail validation instead of reaching the vector helper.
    - Test 3: vector helper tests prove edits preserve ids and re-embed rewritten lore content while delete-by-id removes only the target row.
    - Test 4: frontend API tests cover PUT/DELETE request methods plus backend error propagation.
    - Test 5: LoreSection tests cover opening edit/delete flows, per-card pending state, clearing search results after success, and surfacing mutation failure.
  </behavior>
  <action>Treat this plan as the final regression matrix, not the primary implementation owner. Broaden the targeted tests created in Plans 27-01 and 27-02 into a release gate that exercises the full Phase 27 contract together. In `backend/src/routes/__tests__/lore.test.ts`, keep real `term/definition/category` fixtures and close any remaining edit/delete success, validation-failure, and 404 gaps. In `backend/src/vectors/__tests__/lore-cards.test.ts`, keep stable-id edit and targeted delete coverage aligned with the route expectations and the vector-refresh behavior from Plan 27-01. In `frontend/lib/__tests__/api.test.ts`, keep fetch-mocking coverage aligned with the backend item-route contract and error messaging. In `frontend/components/world-review/__tests__/lore-section.test.tsx`, keep the component regression focused on edit/delete interactions, one-card-at-a-time pending state, `setResults(null)` refresh behavior, and visible mutation errors. Do not move implementation ownership for vector freshness or world-review behavior into this plan; use it to confirm those earlier plans stay correct together.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/routes/__tests__/lore.test.ts src/vectors/__tests__/lore-cards.test.ts && npm --prefix frontend exec vitest run lib/__tests__/api.test.ts components/world-review/__tests__/lore-section.test.tsx</automated>
  </verify>
  <done>Phase 27 behaviors are locked by targeted automated tests on the backend route/vector seams and the frontend client/component seams.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare a concrete smoke target with running dev services and known lore cards</name>
  <files>.planning/phases/27-lore-card-editing-and-deletion/27-03-smoke-target.md</files>
  <action>Make the blocking human checkpoint execution-ready instead of handing it setup work. After Task 1 passes, verify backend and frontend dev availability first. If `http://localhost:3001/api/health` or `http://localhost:3000` is not reachable, start the repo-root `npm run dev` command in a detached/background process and wait until both endpoints respond. Then prepare a known campaign with lore cards: list campaigns via `GET /api/campaigns`, load candidates via `POST /api/campaigns/{id}/load`, and choose the first campaign whose `GET /api/campaigns/{id}/lore` response contains at least two cards. If no such campaign exists, create a dedicated smoke campaign via `POST /api/campaigns`, load it, and seed two deterministic lore cards through a repo-local one-off script that uses the existing backend campaign/vector modules so the checkpoint does not depend on manual world generation. Before handoff, confirm the chosen campaign opens in review and that lore search is operational enough for the smoke flow by hitting `GET /api/campaigns/{id}/lore/search?q={existingTerm}`. If search or health checks still fail, stop here with the concrete blocker instead of entering the human checkpoint. Write `.planning/phases/27-lore-card-editing-and-deletion/27-03-smoke-target.md` with exact fields for `campaignId`, `reviewUrl`, `editCard` (`id` + current `term`), `deleteCard` (`id` + current `term`), and the timestamped backend/frontend health checks. The human checkpoint must be able to open that URL directly and know which two cards to operate on without any additional setup.</action>
  <verify>
    <automated>$p = '.planning/phases/27-lore-card-editing-and-deletion/27-03-smoke-target.md'; if (-not (Test-Path $p)) { throw 'Missing smoke-target artifact.' }; $t = Get-Content -Raw $p; if ($t -notmatch 'campaignId:' -or $t -notmatch 'reviewUrl: http://localhost:3000/campaign/.+/review' -or $t -notmatch 'editCard:' -or $t -notmatch 'deleteCard:') { throw 'Smoke-target artifact is missing required fields.' }; Invoke-RestMethod 'http://localhost:3001/api/health' | Out-Null; Invoke-WebRequest 'http://localhost:3000' -UseBasicParsing | Out-Null; Write-Output 'PASS: smoke target prepared'</automated>
  </verify>
  <done>The plan has a running local app and a recorded campaign id/review URL with two known lore cards ready for the manual smoke pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human smoke verification for world-review lore mutations</name>
  <files>.planning/phases/27-lore-card-editing-and-deletion/27-03-smoke-target.md, backend/src/routes/lore.ts, frontend/components/world-review/lore-section.tsx</files>
  <action>Run the short browser smoke flow against the prepared world-review lore target after Tasks 1 and 2 pass. Use the exact campaign id and review URL recorded in `.planning/phases/27-lore-card-editing-and-deletion/27-03-smoke-target.md` so the checkpoint only verifies behavior, not environment setup.</action>
  <what-built>Per-card lore edit/delete flow in world review, plus Phase 27 regression coverage.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/27-lore-card-editing-and-deletion/27-03-smoke-target.md` and copy the recorded `reviewUrl`, `editCard`, and `deleteCard` values.
    2. Open the recorded `reviewUrl` in the browser and switch to the Lore tab.
    3. Edit the recorded `editCard` and change its term to `Phase 27 Smoke Edited Term`, save it, and confirm the updated text appears immediately.
    4. Search for `Phase 27 Smoke Edited Term` and confirm the edited card is returned with the new content.
    5. Delete the recorded `deleteCard`, confirm it disappears, then refresh the page and confirm it is still gone.
    6. Reopen the edit dialog for the remaining card, try saving an invalid edit such as a blank term or blank definition, and confirm the UI surfaces an error instead of silently succeeding.
  </how-to-verify>
  <verify>
    <automated>MISSING — run only after Task 2 writes the concrete smoke target and health checks pass in this same plan.</automated>
  </verify>
  <done>Human smoke verification approves the live world-review lore flow or records the concrete mismatch for follow-up.</done>
  <resume-signal>Type "approved" if the smoke pass matches the expected behavior, or describe the mismatch.</resume-signal>
</task>

</tasks>

<verification>
Phase gate:
1. `npm --prefix backend exec vitest run src/routes/__tests__/lore.test.ts src/vectors/__tests__/lore-cards.test.ts`
2. `npm --prefix frontend exec vitest run lib/__tests__/api.test.ts components/world-review/__tests__/lore-section.test.tsx`
3. Prepare `.planning/phases/27-lore-card-editing-and-deletion/27-03-smoke-target.md` with a live campaign id, review URL, and two named lore cards.
4. Complete the blocking human smoke checklist above.
</verification>

<success_criteria>
Phase 27 ships with explicit automated coverage for edit/delete/validation/not-found/vector freshness, a prepared live smoke target, and one successful manual smoke pass proving the world-review flow works against the running app.
</success_criteria>

<output>
After completion, create `.planning/phases/27-lore-card-editing-and-deletion/27-03-SUMMARY.md`
</output>
