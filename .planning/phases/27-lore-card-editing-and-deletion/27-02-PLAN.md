---
phase: 27-lore-card-editing-and-deletion
plan: 02
type: execute
wave: 2
depends_on:
  - "27-01"
files_modified:
  - frontend/lib/api-types.ts
  - frontend/lib/api.ts
  - frontend/components/world-review/lore-section.tsx
  - frontend/lib/__tests__/api.test.ts
  - frontend/components/world-review/__tests__/lore-section.test.tsx
autonomous: true
requirements:
  - P27-06
must_haves:
  truths:
    - "A user can start editing or deleting an individual lore card from the world review tab."
    - "The edit surface lets the user change term, definition, and category only."
    - "Save/delete actions show per-card pending and error state instead of freezing the whole lore tab."
    - "After a successful mutation, the visible lore list refreshes from the server and stale search results are cleared."
  artifacts:
    - path: "frontend/lib/api.ts"
      provides: "Client helpers for lore item update/delete requests"
    - path: "frontend/lib/api-types.ts"
      provides: "Frontend lore edit payload/category typing"
    - path: "frontend/components/world-review/lore-section.tsx"
      provides: "Per-card edit and delete controls in world review"
  key_links:
    - from: "frontend/components/world-review/lore-section.tsx"
      to: "frontend/lib/api.ts"
      via: "save/delete handlers"
      pattern: "updateLoreCard|deleteLoreCardById"
    - from: "frontend/components/world-review/lore-section.tsx"
      to: "frontend/app/campaign/[id]/review/page.tsx"
      via: "server-authoritative refresh seam"
      pattern: "onRefresh"
    - from: "frontend/components/world-review/lore-section.tsx"
      to: "results ?? initialCards"
      via: "clear local search overlay after successful mutation"
      pattern: "setResults\\(null\\)"
---

<objective>
Add world-review lore management UI on top of the new backend item routes.

Purpose: Users need a clear per-card editing/deletion surface, but the parent review page must remain the source of truth. This plan adds the client helpers and the card-level UI behavior without inventing optimistic cache logic.
Output: frontend lore item API helpers, an edit dialog, a delete confirmation flow, and per-card pending/error handling in `LoreSection`.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-lore-card-editing-and-deletion/27-CONTEXT.md
@.planning/phases/27-lore-card-editing-and-deletion/27-RESEARCH.md
@frontend/lib/api-types.ts
@frontend/lib/api.ts
@frontend/components/world-review/lore-section.tsx
@frontend/components/world-review/regenerate-dialog.tsx
@frontend/components/ui/alert-dialog.tsx
@frontend/app/campaign/[id]/review/page.tsx

<interfaces>
From frontend/lib/api-types.ts:
```ts
export interface LoreCardItem {
  id: string;
  term: string;
  definition: string;
  category: string;
}
```

From frontend/lib/api.ts:
```ts
export async function getLoreCards(campaignId: string): Promise<LoreCardItem[]>;
export async function searchLore(campaignId: string, query: string, limit?: number): Promise<LoreCardItem[]>;
export async function deleteLore(campaignId: string): Promise<void>;
```

From frontend/components/world-review/lore-section.tsx:
```ts
interface LoreSectionProps {
  cards: LoreCardItem[];
  campaignId: string;
  onRefresh?: () => void;
}

const displayCards = results ?? initialCards;
```

From frontend/app/campaign/[id]/review/page.tsx:
```ts
<LoreSection
  cards={loreCards}
  campaignId={campaignId}
  onRefresh={async () => {
    const fresh = await getLoreCards(campaignId);
    setLoreCards(fresh);
  }}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lore item client helpers and frontend typing</name>
  <files>frontend/lib/api-types.ts, frontend/lib/api.ts, frontend/lib/__tests__/api.test.ts</files>
  <behavior>
    - Test 1: `updateLoreCard()` sends a PUT request with the exact lore edit payload and returns parsed card data.
    - Test 2: `deleteLoreCardById()` sends DELETE to the item endpoint.
    - Test 3: backend validation and not-found errors propagate usable messages to the caller.
  </behavior>
  <action>Extend the frontend API surface for the Phase 27 item routes. Add a narrow lore-edit payload type and a frontend category option source in `frontend/lib/api-types.ts` if needed so `LoreSection` does not hand-roll request shapes. Then add a reusable `apiPut()` primitive in `frontend/lib/api.ts` and expose focused lore helpers: one to update a lore card by id and one to delete a lore card by id. Reuse the existing `readErrorMessage()` error path so backend validation and not-found responses surface usable messages in the UI. Extend `frontend/lib/__tests__/api.test.ts` in the same plan so the request method, URL, payload, and error propagation are verified immediately. Keep the old collection-level helpers intact because worldbook import and full-collection deletion remain separate flows.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run lib/__tests__/api.test.ts -t "updateLoreCard sends PUT|deleteLoreCardById sends DELETE|propagates lore item API errors"</automated>
  </verify>
  <done>The frontend has typed helpers for lore item update/delete requests and can surface backend error text without inline fetch duplication in the component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build per-card edit/delete UX in LoreSection</name>
  <files>frontend/components/world-review/lore-section.tsx, frontend/lib/api.ts, frontend/lib/api-types.ts, frontend/components/world-review/__tests__/lore-section.test.tsx</files>
  <behavior>
    - Test 1: a user can open the edit dialog, change `term`, `definition`, and `category`, save, and trigger refresh.
    - Test 2: deleting one lore card clears stale search results and refreshes the canonical list.
    - Test 3: pending state is scoped to the target card and mutation failures remain visible in the UI.
  </behavior>
  <action>Upgrade `frontend/components/world-review/lore-section.tsx` from read-only cards to per-card management. Add edit and delete affordances on each card using existing UI primitives only: a `Dialog`-based edit form for `term`, `definition`, and `category`, plus an `AlertDialog` confirmation for delete. Track pending state per target card so other cards stay usable while one mutation is in flight. On successful edit or delete, clear `results` with `setResults(null)` before awaiting `onRefresh?.()` so search overlays do not show stale items. Keep the dialog open on edit failure and surface the backend error inline or near the actions; do not silently swallow failures. Extend `frontend/components/world-review/__tests__/lore-section.test.tsx` in the same plan so edit, delete, pending-state, and refresh-after-mutation behavior are locked at the component seam. Do not wire lore edits into `saveWorldEdits()` or any scaffold rewrite path, and do not add in-game lore editing or batch actions.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run components/world-review/__tests__/lore-section.test.tsx -t "edits a lore card and refreshes|deletes a lore card and clears search results|shows per-card pending state|surfaces lore mutation failures"</automated>
  </verify>
  <done>The world-review lore tab now supports per-card edit and delete flows with clear pending/error states and server-authoritative refresh behavior.</done>
</task>

</tasks>

<verification>
Run the targeted frontend API and component tests from this plan after each task. Use `.planning/phases/27-lore-card-editing-and-deletion/27-VALIDATION.md` as the Phase 27 sampling contract, then run frontend lint/type checks as secondary safety nets before handing off to Plan 27-03.
</verification>

<success_criteria>
Users can manage lore cards directly from world review: edit and delete actions are available per card, only the allowed fields are editable, backend errors are visible, and successful mutations reload the authoritative lore list without stale search state.
</success_criteria>

<output>
After completion, create `.planning/phases/27-lore-card-editing-and-deletion/27-02-SUMMARY.md`
</output>
