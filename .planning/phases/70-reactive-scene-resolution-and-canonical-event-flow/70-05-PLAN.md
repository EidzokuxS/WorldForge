---
phase: 70-reactive-scene-resolution-and-canonical-event-flow
plan: 05
type: execute
wave: 3
depends_on: [70-04]
files_modified:
  - backend/src/engine/narrator-packet.ts
  - backend/src/engine/visible-narration-output-guard.ts
  - backend/src/engine/prompt-assembler.ts
  - backend/src/engine/__tests__/scene-turn-packet.test.ts
  - backend/src/engine/__tests__/visible-narration-output-guard.test.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
autonomous: true
requirements: [P70-R6, P70-R7]
must_haves:
  truths:
    - "Storyteller final-visible prompt consumes a NarratorPacket built from committed player-perceivable facts."
    - "Raw ScenePlan, hiddenRationale, hidden actors, broad world rows, and rejected tool plans are not narrator inputs."
    - "Exact hidden actor names with awareness `none` are unavailable to final prompt assembly."
    - "Hint-band actor names are included in backend-only `forbiddenActorNames`; hint identity stays obfuscated in prompt text."
    - "`narratorFacts` cannot become an LLM-authored prose channel; packet projection uses backend event IDs/tool-result refs and pre-prompt guard scanning."
    - "Storyteller final prose is scanned after generation and before persistence; forbidden exact names/fact markers retry once and then fail before assistant message persistence per T70-09."
    - "No `narrative` SSE/TurnEvent is emitted until `runVisibleNarrationWithPacketGuard` has validated the complete final prose."
  artifacts:
    - path: backend/src/engine/narrator-packet.ts
      provides: "CanonicalTurnPacket to NarratorPacket projection and prompt formatter."
      contains: "buildNarratorPacket|formatNarratorPacketForPrompt|allowedVisibleActorNames|forbiddenActorNames|forbiddenFactMarkers"
    - path: backend/src/engine/visible-narration-output-guard.ts
      provides: "Backend-only post-generation Storyteller output guard and retry helper."
      contains: "validateVisibleNarrationAgainstPacket|runVisibleNarrationWithPacketGuard|VISIBLE_NARRATION_PACKET_GUARD_RETRY_LIMIT"
    - path: backend/src/engine/prompt-assembler.ts
      provides: "Final narration prompt path accepting NarratorPacket."
      contains: "narratorPacket|FINAL NARRATION TASK"
    - path: backend/src/engine/__tests__/scene-turn-packet.test.ts
      provides: "Hidden leakage and visible packet tests."
      contains: "hiddenRationale|allowedVisibleActorNames|hint"
  key_links:
    - from: backend/src/engine/narrator-packet.ts
      to: backend/src/engine/prompt-assembler.ts
      via: "formatNarratorPacketForPrompt"
      pattern: "formatNarratorPacketForPrompt"
    - from: backend/src/engine/narrator-packet.ts
      to: backend/src/engine/scene-presence.ts
      via: "awareness filtering"
      pattern: "AwarenessBand|clear|hint|none"
    - from: backend/src/engine/visible-narration-output-guard.ts
      to: backend/src/engine/narrator-packet.ts
      via: "backend-only forbiddenActorNames/forbiddenFactMarkers scan"
      pattern: "forbiddenActorNames|forbiddenFactMarkers"
---

<objective>
Create the narrator-facing committed packet and wire final prompt assembly to consume that packet instead of raw hidden planning material.

Purpose: Make final prose downstream of committed visible facts per D-07 and preserve Phase 69 narrator-only ownership.
Output: `narrator-packet.ts`, prompt assembler integration, and leakage tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-CONTEXT.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-RESEARCH.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-PATTERNS.md
@backend/src/engine/narrator-packet.ts
@backend/src/engine/visible-narration-output-guard.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/storyteller-contract.ts
@backend/src/engine/scene-assembly.ts
@backend/src/engine/scene-presence.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build NarratorPacket from canonical committed facts</name>
  <files>backend/src/engine/narrator-packet.ts, backend/src/engine/__tests__/scene-turn-packet.test.ts</files>
  <read_first>
    <file>backend/src/engine/narrator-packet.ts</file>
    <file>backend/src/engine/scene-frame.ts</file>
    <file>backend/src/engine/scene-plan-schema.ts</file>
    <file>backend/src/engine/scene-plan-executor.ts</file>
    <file>backend/src/engine/scene-presence.ts</file>
    <file>backend/src/engine/scene-assembly.ts</file>
    <file>.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-VALIDATION.md</file>
  </read_first>
  <behavior>
    - `NarratorPacket.allowedVisibleActorNames` includes player plus clear-aware actors only.
    - Hint awareness contributes `hintSignals`, not hidden actor identity, and hint actor exact names are included in backend-only `forbiddenActorNames`.
    - Packet excludes `hiddenRationale`, `ScenePlan.plannedActions`, invalid/rejected actions, raw DB rows, and hidden actor names.
    - Packet carries backend-only `forbiddenActorNames` and `forbiddenFactMarkers` guard metadata that is never formatted into the Storyteller prompt.
    - Packet facts are projected from `CanonicalTurnPacket` IDs and full `ToolResult` metadata, not from LLM prose in `ScenePlan.narratorFacts`.
  </behavior>
  <action>Implement `CanonicalTurnPacket` and `NarratorPacket` types plus `buildNarratorPacket(args: { frame; canonicalTurnPacket; sceneAssembly? }): NarratorPacket`. Include fields `playerAction`, `oracleOutcome`, `anchorEvent`, `perceivableEvents`, `perceivableResponses`, `perceivableEffects`, `visibleActors`, `hintSignals`, `guardrails`, `controlReturnReason`, `allowedVisibleActorNames`, `forbiddenActorNames`, and `forbiddenFactMarkers`. `perceivableEvents` and related fields must be projected from backend canonical event IDs, action IDs, and full `ToolResult` metadata in `CanonicalTurnPacket`; never copy free prose from `ScenePlan.narratorFacts`. `forbiddenActorNames` must include exact hidden actor names, awareness-`none` actor names, and hint-band actor names from the frame; `forbiddenFactMarkers` must include packet-specific hidden fact markers generated by the backend, not terms invented by the Storyteller prompt. Implement `assertNarratorPacketPromptSafe(packet)` and call it before formatting so any remaining prose fields in backend event/effect summaries are scanned against forbidden actor names and fact markers before `formatNarratorPacketForPrompt(packet)` runs. Implement `formatNarratorPacketForPrompt(packet): string` with sections `[NARRATOR PACKET]`, `VISIBLE ACTORS`, `COMMITTED EVENTS`, `PERCEIVABLE RESPONSES`, `GUARDRAILS`, and `CONTROL RETURN`. The formatter must never include `forbiddenActorNames`, `forbiddenFactMarkers`, hidden actor names, hint actor names, or hidden fact marker values. Tests must seed hidden and hint actors in frame and assert exact name absence from packet-visible fields and formatted prompt text while guard metadata remains available to backend code.</action>
  <verify>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>cd backend && npx vitest run src/engine/__tests__/scene-turn-packet.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "export interface NarratorPacket|buildNarratorPacket|assertNarratorPacketPromptSafe|formatNarratorPacketForPrompt|allowedVisibleActorNames|forbiddenActorNames|forbiddenFactMarkers|ToolResult" backend/src/engine/narrator-packet.ts` returns matches.</criterion>
    <criterion>`rg "hiddenRationale|plannedActions" backend/src/engine/narrator-packet.ts` returns no positive packet inclusion path.</criterion>
    <criterion>`rg "hidden actor|hint actor|allowedVisibleActorNames|hintSignals|narratorFacts|ToolResult" backend/src/engine/__tests__/scene-turn-packet.test.ts` returns matches.</criterion>
    <criterion>Typecheck and targeted Vitest command exit 0.</criterion>
  </acceptance_criteria>
  <done>NarratorPacket gives final prose only visible committed facts and a hard visible actor allow-list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire final prompt assembly to NarratorPacket</name>
  <files>backend/src/engine/prompt-assembler.ts, backend/src/engine/__tests__/prompt-assembler.test.ts</files>
  <read_first>
    <file>backend/src/engine/prompt-assembler.ts</file>
    <file>backend/src/engine/narrator-packet.ts</file>
    <file>backend/src/engine/storyteller-contract.ts</file>
    <file>backend/src/engine/__tests__/prompt-assembler.test.ts</file>
    <file>.planning/phases/69-judge-owned-hidden-pass-migration-and-narrator-only-runtime/69-SUMMARY.md</file>
  </read_first>
  <behavior>
    - `assembleFinalNarrationPrompt` accepts optional `narratorPacket`.
    - When packet exists, final prompt includes formatted packet and does not include raw ScenePlan or hidden rationale.
    - Forbidden actor names and forbidden fact markers are never written into the prompt; they remain backend-only guard metadata for post-generation scanning.
    - Prompt assembly runs the packet pre-prompt guard before `formatNarratorPacketForPrompt` so backend prose summaries cannot carry forbidden names into the prompt.
    - Existing opening scene and no-packet callers remain compatible.
  </behavior>
  <action>Run `gitnexus_impact({target: "assembleFinalNarrationPrompt", direction: "upstream", repo: "WorldForge"})` before editing. Add an optional `narratorPacket?: NarratorPacket` parameter to `assembleFinalNarrationPrompt(...)`. When supplied, call `assertNarratorPacketPromptSafe(narratorPacket)` before `formatNarratorPacketForPrompt(narratorPacket)`, append the formatted packet, and update `[FINAL NARRATION TASK]` text to say `Use the NarratorPacket as the authoritative committed packet.` Keep existing SceneAssembly sections for compatibility, but tests must prove `hiddenRationale`, raw `plannedActions`, hidden actor names, hint actor names, `forbiddenActorNames`, and `forbiddenFactMarkers` are absent from the final prompt when a packet is supplied; tests must also prove prompt assembly throws before prompt return if packet prose contains a forbidden actor name or fact marker.</action>
  <verify>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>cd backend && npx vitest run src/engine/__tests__/scene-turn-packet.test.ts src/engine/__tests__/prompt-assembler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "narratorPacket|assertNarratorPacketPromptSafe|formatNarratorPacketForPrompt|NarratorPacket" backend/src/engine/prompt-assembler.ts` returns matches.</criterion>
    <criterion>`rg "hiddenRationale|plannedActions|NarratorPacket|hint actor|forbiddenActorNames|forbiddenFactMarkers|pre-prompt" backend/src/engine/__tests__/prompt-assembler.test.ts` returns matches proving exclusion/inclusion behavior.</criterion>
    <criterion>Targeted Vitest and backend typecheck exit 0.</criterion>
  </acceptance_criteria>
  <done>Final-visible prompt assembly can consume a NarratorPacket while keeping existing callers operational.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement post-generation Storyteller output guard</name>
  <files>backend/src/engine/visible-narration-output-guard.ts, backend/src/engine/__tests__/visible-narration-output-guard.test.ts, backend/src/engine/narrator-packet.ts</files>
  <read_first>
    <file>backend/src/engine/turn-processor.ts</file>
    <file>backend/src/engine/narrator-packet.ts</file>
    <file>backend/src/engine/prompt-assembler.ts</file>
    <file>.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-RESEARCH.md</file>
    <file>.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-VALIDATION.md</file>
  </read_first>
  <behavior>
    - `validateVisibleNarrationAgainstPacket` scans final prose after generation for exact `forbiddenActorNames` and exact `forbiddenFactMarkers`.
    - `runVisibleNarrationWithPacketGuard` retries once on the first violation with a generic guard addendum that does not reveal the forbidden name or fact marker.
    - Second violation throws before assistant message persistence, so existing route snapshot restore handles rollback when Plan 70-06 wires the helper into `processTurn`.
    - Forbidden terms are never sent in the Storyteller prompt or retry addendum.
    - Final visible narration is generated as a complete non-streamed string, or buffered internally, until packet guard validation passes; no `narrative` SSE/TurnEvent can reach the client first.
  </behavior>
  <action>Create `backend/src/engine/visible-narration-output-guard.ts` exporting `VISIBLE_NARRATION_PACKET_GUARD_RETRY_LIMIT = 1`, `validateVisibleNarrationAgainstPacket(args: { packet: NarratorPacket; text: string }): VisibleNarrationPacketGuardResult`, and `runVisibleNarrationWithPacketGuard(args)`. The validator must perform exact substring scans against `packet.forbiddenActorNames` and `packet.forbiddenFactMarkers` after final prose generation; do not use fuzzy matching or semantic inference in Phase 70A. The runner must call a supplied non-streaming narration function once, validate the complete output string, retry once with a generic non-leaking addendum such as `Revise to stay within the visible packet and omit any non-visible identities or facts.`, validate again, and throw a typed error if unsafe output remains. If the implementation must adapt an existing streaming helper, it must buffer the full stream inside `runVisibleNarrationWithPacketGuard` and return only after validation; it must not yield `narrative` TurnEvents or SSE chunks during this helper. Do not call `appendChatMessages()` in this module; Plan 70-06 must call `runVisibleNarrationWithPacketGuard` before any `appendChatMessages()` assistant write and before any `narrative` SSE/TurnEvent emission. Add `visible-narration-output-guard.test.ts` covering clean output, retry-on-first-violation, second-violation throw, generic addendum absence of forbidden terms, exact actor-name plus exact fact-marker detection, non-streaming/buffered final output, and no `narrative` SSE before guard pass.</action>
  <verify>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>cd backend && npx vitest run src/engine/__tests__/visible-narration-output-guard.test.ts src/engine/__tests__/scene-turn-packet.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "VISIBLE_NARRATION_PACKET_GUARD_RETRY_LIMIT|validateVisibleNarrationAgainstPacket|runVisibleNarrationWithPacketGuard" backend/src/engine/visible-narration-output-guard.ts` returns matches.</criterion>
    <criterion>`rg "forbiddenActorNames|forbiddenFactMarkers|retry once|second violation|generic|narrative|SSE|non-streaming|buffer" backend/src/engine/__tests__/visible-narration-output-guard.test.ts` returns matches.</criterion>
    <criterion>Tests assert retry prompts/addenda do not contain the forbidden actor name or forbidden fact marker.</criterion>
    <criterion>Targeted Vitest and backend typecheck exit 0.</criterion>
  </acceptance_criteria>
  <done>Backend has a tested post-generation Storyteller output guard ready to run before `appendChatMessages()` in Plan 70-06.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CanonicalTurnPacket -> NarratorPacket | Committed facts are filtered by player visibility before prose generation. |
| NarratorPacket -> Storyteller prompt | Final LLM receives only player-perceivable packet content. |
| Storyteller output -> assistant persistence | Generated prose is untrusted until exact forbidden-name/fact scanning passes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-05-01 | Information Disclosure | `buildNarratorPacket` | mitigate | Filter by player awareness and assert hidden actor exact names are absent. |
| T-70-05-02 | Tampering | `assembleFinalNarrationPrompt` | mitigate | Treat packet as authoritative committed source; no raw ScenePlan input. |
| T-70-05-03 | Repudiation | final prompt evidence | mitigate | Tests assert included sections and excluded hidden fields. |
| T70-09 | Information Disclosure | `runVisibleNarrationWithPacketGuard` | mitigate | Scan final prose after generation and before persistence; retry once with a generic non-leaking addendum, then throw before `appendChatMessages()` if still unsafe. |
| T-70-05-04 | Information Disclosure | hint-band actor identity | mitigate | Include hint-band exact actor names in backend-only `forbiddenActorNames`; prompt receives hint signals only. |
| T-70-05-05 | Information Disclosure | final narration streaming | mitigate | Generate or buffer final prose until `runVisibleNarrationWithPacketGuard` passes; emit no `narrative` SSE before validation. |
</threat_model>

<verification>
Run backend typecheck plus `cd backend && npx vitest run src/engine/__tests__/scene-turn-packet.test.ts src/engine/__tests__/prompt-assembler.test.ts src/engine/__tests__/visible-narration-output-guard.test.ts`.
</verification>

<success_criteria>
Final prose is downstream of player-perceivable committed facts, not hidden planning state, and unsafe generated prose cannot be persisted after two exact guard violations.
</success_criteria>

<output>
After completion, create `.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-05-SUMMARY.md`.
</output>
