---
phase: 70-reactive-scene-resolution-and-canonical-event-flow
plan: 06
type: execute
wave: 4
depends_on: [70-02, 70-03, 70-04, 70-05]
files_modified:
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/target-context.ts
  - backend/src/engine/__tests__/turn-processor.scene-plan.test.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/engine/__tests__/turn-processor.observability.test.ts
autonomous: true
requirements: [P70-R2, P70-R3, P70-R4, P70-R5, P70-R6, P70-R7]
must_haves:
  truths:
    - "Normal player turns use SceneFrame -> Oracle -> ScenePlan -> validate -> execute -> NarratorPacket -> final prose."
    - "`buildSceneFrame` runs before `callOracle` and before any ScenePlan LLM call; deterministic state loading and candidate gathering may happen first, but movement mutation must wait for validated ScenePlan execution."
    - "WorldBrainSceneDirection and HiddenAdjudicationPlan are bypassed on the normal visible-turn ScenePlan path."
    - "Storyteller final prose runs through `runVisibleNarrationWithPacketGuard` before any assistant message persistence."
    - "No `narrative` TurnEvent/SSE leaves the processor or route before `runVisibleNarrationWithPacketGuard` validates complete prose."
    - "`backend/src/engine/target-context.ts` classifier (`detectCandidateByClassifier`, source `classifier`) and LLM `detectMovement` do not run before `buildSceneFrame`; they are either bypassed by ScenePlan ownership or run only after SceneFrame from frame-owned candidates."
    - "`SCENE_PLAN_ENABLED` defaults true; explicit `false` isolates the legacy path as a temporary rollback flag without mixing ScenePlan and legacy stages, and Phase 70 closeout must either remove it or record a dated follow-up with the failing evidence that still requires it."
    - "Opening scenes keep the existing Phase 68 path."
  artifacts:
    - path: backend/src/engine/turn-processor.ts
      provides: "ScenePlan pipeline orchestration inside processTurn."
      contains: "buildSceneFrame|callOracle|runScenePlanner|validateScenePlan|executeScenePlan|buildNarratorPacket|runVisibleNarrationWithPacketGuard"
    - path: backend/src/engine/__tests__/turn-processor.scene-plan.test.ts
      provides: "Canonical ordering, Oracle separation, hidden leak, and no mini-round regressions."
      contains: "ScenePlan|Oracle|tickPresentNpcs|NarratorPacket"
  key_links:
    - from: backend/src/engine/turn-processor.ts
      to: backend/src/engine/scene-frame.ts
      via: "buildSceneFrame before callOracle and ScenePlan"
      pattern: "buildSceneFrame"
    - from: backend/src/engine/turn-processor.ts
      to: backend/src/engine/visible-narration-output-guard.ts
      via: "post-generation Storyteller guard before appendChatMessages"
      pattern: "runVisibleNarrationWithPacketGuard"
    - from: backend/src/engine/turn-processor.ts
      to: backend/src/engine/prompt-assembler.ts
      via: "assembleFinalNarrationPrompt with narratorPacket"
      pattern: "narratorPacket"
---

<objective>
Migrate `processTurn(...)` to the Scene Planner of Record pipeline for normal player-visible turns while preserving Oracle, rollback semantics, streaming stages, and opening-scene behavior.

Purpose: Collapse fragmented local visible-turn authority into one structured ScenePlan path per D-02, D-05, and D-08.
Output: Turn processor integration plus focused regression coverage.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-CONTEXT.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-RESEARCH.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-PATTERNS.md
@.planning/phases/69-judge-owned-hidden-pass-migration-and-narrator-only-runtime/69-SUMMARY.md
@backend/src/engine/turn-processor.ts
@backend/src/engine/scene-frame.ts
@backend/src/engine/scene-planner.ts
@backend/src/engine/scene-plan-validator.ts
@backend/src/engine/scene-plan-executor.ts
@backend/src/engine/narrator-packet.ts
@backend/src/engine/visible-narration-output-guard.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/target-context.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Replace normal turn hidden stages with ScenePlan pipeline</name>
  <files>backend/src/engine/turn-processor.ts, backend/src/engine/target-context.ts, backend/src/engine/__tests__/turn-processor.scene-plan.test.ts, backend/src/engine/__tests__/turn-processor.test.ts</files>
  <read_first>
    <file>backend/src/engine/turn-processor.ts</file>
    <file>backend/src/engine/__tests__/turn-processor.test.ts</file>
    <file>backend/src/engine/__tests__/turn-processor.scene-plan.test.ts</file>
    <file>backend/src/engine/scene-frame.ts</file>
    <file>backend/src/engine/scene-planner.ts</file>
    <file>backend/src/engine/scene-plan-validator.ts</file>
    <file>backend/src/engine/scene-plan-executor.ts</file>
    <file>backend/src/engine/narrator-packet.ts</file>
    <file>backend/src/engine/world-brain.ts</file>
    <file>backend/src/engine/hidden-adjudication.ts</file>
    <file>backend/src/engine/target-context.ts</file>
  </read_first>
  <behavior>
    - `buildSceneFrame` runs before `callOracle` and before `runScenePlanner`.
    - Oracle call remains after SceneFrame and before ScenePlan generation.
    - LLM `detectMovement()` and the `target-context.ts` LLM target classifier path (`detectCandidateByClassifier`, `source: "classifier"`) do not run before `buildSceneFrame`; they run only after SceneFrame from frame-owned candidate IDs or are bypassed entirely by ScenePlan-owned movement interpretation. Actual movement goes through validated `ScenePlan` `move_to` or existing tool execution.
    - `runWorldBrainSceneDirection` and `runHiddenAdjudicationPlan` are not called on normal player turn when ScenePlan path is active.
    - `assembleFinalNarrationPrompt` receives `narratorPacket`.
    - Final prose is produced through non-streaming/buffered `runVisibleNarrationWithPacketGuard`; only after it passes may code persist with `appendChatMessages` and yield a `narrative` TurnEvent/SSE.
    - `SCENE_PLAN_ENABLED` is a temporary rollout flag that defaults true; only an explicit string value `false` uses the isolated legacy path, and it has a closeout sunset: remove it once focused Phase 70 route/typecheck tests pass, or leave it only with a documented follow-up issue naming the failing rollback evidence.
  </behavior>
  <action>Run `gitnexus_impact({target: "processTurn", direction: "upstream", repo: "WorldForge"})`, `gitnexus_impact({target: "detectMovement", direction: "upstream", repo: "WorldForge"})`, and `gitnexus_impact({target: "resolveActionTargetContext", direction: "upstream", repo: "WorldForge"})` before editing. In `processTurn(...)`, keep deterministic state loading and candidate gathering first, then call `buildSceneFrame(...)` before `callOracle(...)`. Do not run LLM `detectMovement()` or mutate movement before `buildSceneFrame`; do not call `target-context.ts` `detectCandidateByClassifier` / `source: "classifier"` before `buildSceneFrame` on normal action/retry ScenePlan path. Only deterministic pre-LLM movement hints and candidate IDs may be included in the frame; any remaining LLM movement/classifier interpretation must run after SceneFrame from frame-owned candidates or be bypassed by ScenePlan ownership. Actual movement must happen only through validated `ScenePlan` actions and existing `move_to`/tool execution. Add `SCENE_PLAN_ENABLED` as a temporary environment rollout flag inside the processor seam: default true when unset, use the legacy Phase 69 path only when the value is exactly `false`, ensure flag-off execution does not call ScenePlan modules, and define removal criteria in comments/docs: delete the flag once focused Phase 70 route/typecheck tests pass, or keep it only with a dated follow-up naming the failing evidence that prevents removal. After `buildSceneFrame(...)`, call `callOracle(...)`, then `runScenePlanner(...)`, `validateScenePlan(...)`, `executeScenePlan(...)`, `assembleAuthoritativeScene(...)`, `buildNarratorPacket(...)`, `assembleFinalNarrationPrompt(..., narratorPacket)`, and non-streaming/buffered `runVisibleNarrationWithPacketGuard(...)` before `appendChatMessages(...)` and before yielding any `narrative` TurnEvent. Remove normal player-turn calls to `runWorldBrainSceneDirection(...)`, `assembleJudgeAdjudicationPrompt(...)`, `runHiddenAdjudicationPlan(...)`, and `executeAdjudicationPlan(...)` from the ScenePlan path. Keep `processOpeningScene(...)` on the existing Phase 68 world-brain path. Preserve existing `TurnEvent` event names `oracle_result`, `scene-settling`, `narrative`, `finalizing_turn`, and `done`; add compact `scene-plan` observability only with counts/ids/durations, not raw prompts, hidden rationale, forbidden actor names, or forbidden fact markers.</action>
  <verify>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>cd backend && npx vitest run src/engine/__tests__/turn-processor.scene-plan.test.ts src/engine/__tests__/turn-processor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "buildSceneFrame|callOracle|runScenePlanner|validateScenePlan|executeScenePlan|buildNarratorPacket|narratorPacket|runVisibleNarrationWithPacketGuard" backend/src/engine/turn-processor.ts` returns matches.</criterion>
    <criterion>`rg "buildSceneFrame.*before.*callOracle|callOracle.*before.*runScenePlanner|detectMovement.*not.*before.*buildSceneFrame|after.*SceneFrame|detectCandidateByClassifier|source.*classifier|move_to|SCENE_PLAN_ENABLED|remove.*flag|dated follow-up" backend/src/engine/__tests__/turn-processor.scene-plan.test.ts backend/src/engine/turn-processor.ts .planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70A-MIGRATION-PLAN.md` returns matches proving ordering, movement/target classifier gating, flag coverage, and rollback-flag sunset.</criterion>
    <criterion>`rg "runWorldBrainSceneDirection|runHiddenAdjudicationPlan|executeAdjudicationPlan" backend/src/engine/__tests__/turn-processor.scene-plan.test.ts` returns matches proving non-call assertions.</criterion>
    <criterion>`rg "processOpeningScene" backend/src/engine/__tests__/turn-processor.test.ts` still has opening-scene non-regression coverage.</criterion>
    <criterion>Typecheck and targeted Vitest command exit 0.</criterion>
  </acceptance_criteria>
  <done>Normal player turns use SceneFrame -> Oracle -> ScenePlan pipeline, final prose passes the packet output guard before persistence, and opening scenes remain on existing Phase 68 path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve ordering, rollback, and observability semantics</name>
  <files>backend/src/engine/turn-processor.ts, backend/src/engine/__tests__/turn-processor.scene-plan.test.ts, backend/src/engine/__tests__/turn-processor.observability.test.ts</files>
  <read_first>
    <file>backend/src/engine/turn-processor.ts</file>
    <file>backend/src/engine/__tests__/turn-processor.scene-plan.test.ts</file>
    <file>backend/src/engine/__tests__/turn-processor.observability.test.ts</file>
    <file>backend/src/engine/__tests__/fixtures/expected-seams.ts</file>
    <file>.planning/phases/69-judge-owned-hidden-pass-migration-and-narrator-only-runtime/69-VALIDATION.md</file>
  </read_first>
  <behavior>
    - Validation failure happens before execution and before final narration.
    - Execution failure yields an error through the existing thrown-error route path.
    - Storyteller output guard failure yields an error before assistant persistence and before `done`.
    - Storyteller output guard pass happens before any `narrative` TurnEvent/SSE emission.
    - Tick advance and `onPostTurn` remain after visible narration, not before.
    - `SCENE_PLAN_ENABLED=false` runs the legacy path without invoking ScenePlan modules, default/unset runs the ScenePlan path, and the flag has explicit cleanup criteria.
  </behavior>
  <action>Extend `turn-processor.scene-plan.test.ts` to assert exact call order: `buildSceneFrame` before `callOracle`, `callOracle` before `runScenePlanner`, `validateScenePlan` before `executeScenePlan`, `executeScenePlan` before `buildNarratorPacket`, `buildNarratorPacket` before `assembleFinalNarrationPrompt`, `assembleFinalNarrationPrompt` before `runVisibleNarrationWithPacketGuard`, `runVisibleNarrationWithPacketGuard` before `appendChatMessages`, `runVisibleNarrationWithPacketGuard` before the first yielded `narrative` TurnEvent/SSE payload, and final narration before tick advance plus `onPostTurn`. Add validation-failure, execution-failure, and Storyteller output-guard failure tests that assert no unsafe assistant message is persisted, no `narrative` or `done` event is yielded after an unsafe guard failure, and route-owned snapshot restore remains the failure boundary. Add flag tests proving `SCENE_PLAN_ENABLED` unset/default uses ScenePlan, `SCENE_PLAN_ENABLED=false` isolates the legacy path without ScenePlan module calls, and the flag is named temporary with cleanup criteria in comments/docs. Update observability fixture expectations to include compact events `scene.frame`, `judge.scene-plan`, `scene.plan.validation`, `scene.plan.execution`, `scene.packet`, and `visible-narration.packet-guard` with no raw prompt, hidden rationale, forbidden actor names, or forbidden fact marker fields.</action>
  <verify>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>cd backend && npx vitest run src/engine/__tests__/turn-processor.scene-plan.test.ts src/engine/__tests__/turn-processor.observability.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "scene.frame|judge.scene-plan|scene.plan.validation|scene.plan.execution|scene.packet|visible-narration.packet-guard" backend/src/engine/turn-processor.ts backend/src/engine/__tests__/turn-processor.observability.test.ts backend/src/engine/__tests__/fixtures/expected-seams.ts` returns matches.</criterion>
    <criterion>`rg "before.*callOracle|before.*runScenePlanner|before.*executeScenePlan|before.*assembleFinalNarrationPrompt|runVisibleNarrationWithPacketGuard.*before.*appendChatMessages|runVisibleNarrationWithPacketGuard.*before.*narrative|SSE|SCENE_PLAN_ENABLED|legacy path|cleanup criteria|no.*done" backend/src/engine/__tests__/turn-processor.scene-plan.test.ts backend/src/engine/turn-processor.ts .planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70A-MIGRATION-PLAN.md` returns matches.</criterion>
    <criterion>Targeted Vitest and backend typecheck exit 0.</criterion>
  </acceptance_criteria>
  <done>ScenePlan path preserves current turn ordering and error semantics while adding bounded observability.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ScenePlan pipeline -> final narration | Only validated, executed, packetized facts reach storyteller. |
| Processor errors -> route rollback | Thrown validation/execution errors must flow to existing snapshot restore path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-06-01 | Tampering | `processTurn` ordering | mitigate | Tests assert `buildSceneFrame` before `callOracle`, `callOracle` before `runScenePlanner`, validation before execution, and packet before final narration. |
| T-70-06-02 | Information Disclosure | observability | mitigate | Log only counts/ids/durations; no prompt body, hidden rationale, forbidden actor names, or forbidden fact markers. |
| T-70-06-03 | Denial of Service | ScenePlan failures | mitigate | Fail loudly through existing route restore path; no silent fallback loop. |
| T70-09 | Information Disclosure | Storyteller output guard | mitigate | Run `runVisibleNarrationWithPacketGuard` before `appendChatMessages`; retry once with a generic addendum, then throw before persistence if unsafe output remains. |
| T-70-06-04 | Information Disclosure | final narration SSE | mitigate | Tests assert no `narrative` TurnEvent/SSE is emitted before non-streaming/buffered packet guard validation passes. |
| T-70-06-05 | Tampering | movement and target classifier ordering | mitigate | `detectMovement` and `target-context.ts` classifier path cannot run before `buildSceneFrame` on normal action/retry ScenePlan path; they run after SceneFrame from frame-owned candidates or are bypassed by ScenePlan ownership. |
| T-70-06-06 | Rollback | `SCENE_PLAN_ENABLED` flag | mitigate | Default true; explicit false isolates legacy path as temporary rollback without mixing ScenePlan and legacy stages; closeout removes it after focused Phase 70 route/typecheck tests pass or records a dated follow-up with failing evidence. |
</threat_model>

<verification>
Run backend typecheck plus `cd backend && npx vitest run src/engine/__tests__/turn-processor.scene-plan.test.ts src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/turn-processor.observability.test.ts`.
</verification>

<success_criteria>
Normal turn processor path has one local planner of record and still honors Oracle, final narration, tick, retry, and rollback boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-06-SUMMARY.md`.
</output>
