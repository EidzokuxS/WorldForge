---
phase: 70-reactive-scene-resolution-and-canonical-event-flow
plan: 07
type: execute
wave: 5
depends_on: [70-06]
files_modified:
  - backend/src/routes/chat.ts
  - backend/src/routes/__tests__/chat.test.ts
  - backend/src/routes/__tests__/chat.scene-plan.test.ts
autonomous: true
requirements: [P70-R4, P70-R7]
must_haves:
  truths:
    - "Normal `/api/chat/action` and `/api/chat/retry` visible paths do not inject `tickPresentNpcs()` before final narration."
    - "Turn lock, snapshot capture, restore on error, retry, undo, and post-turn background work remain intact."
    - "`tickPresentNpcs` remains available for background/offscreen or future non-critical use, but normal route imports and action/retry critical path no longer use it."
    - "ScenePlan execution failure after earlier validated actions restores route snapshot and persists no unsafe assistant message."
  artifacts:
    - path: backend/src/routes/chat.ts
      provides: "Route-level removal of pre-visible present-NPC mini-round for normal turns."
      contains: "buildOnPostTurn|tryBeginTurn|captureSnapshot|restoreSnapshot"
    - path: backend/src/routes/__tests__/chat.scene-plan.test.ts
      provides: "Route regression proving no independent present-NPC critical-path call and rollback parity."
      contains: "tickPresentNpcs|retry|restoreSnapshot"
  key_links:
    - from: backend/src/routes/chat.ts
      to: backend/src/engine/turn-processor.ts
      via: "processTurn options no longer include onBeforeVisibleNarration for normal action/retry"
      pattern: "processTurn"
    - from: backend/src/routes/chat.ts
      to: backend/src/engine/npc-agent.ts
      via: "tickPresentNpcs no longer imported for critical path"
      pattern: "tickPresentNpcs"
---

<objective>
Remove route-level independent present-NPC mini-rounds from the normal visible-turn critical path while preserving route safety controls.

Purpose: Make ScenePlan the one local planner of record for player-visible action/retry per D-05 and D-06.
Output: Chat route cutover plus route-level action/retry regression tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-CONTEXT.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-RESEARCH.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-PATTERNS.md
@backend/src/routes/chat.ts
@backend/src/routes/__tests__/chat.test.ts
@backend/src/engine/turn-processor.ts
@backend/src/engine/npc-agent.ts
@backend/src/engine/state-snapshot.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Remove onBeforeVisibleNarration mini-round injection</name>
  <files>backend/src/routes/chat.ts, backend/src/routes/__tests__/chat.scene-plan.test.ts</files>
  <read_first>
    <file>backend/src/routes/chat.ts</file>
    <file>backend/src/routes/__tests__/chat.test.ts</file>
    <file>backend/src/engine/turn-processor.ts</file>
    <file>backend/src/engine/npc-agent.ts</file>
    <file>.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-CONTEXT.md</file>
  </read_first>
  <behavior>
    - `tickPresentNpcs` is not imported or called by the normal action/retry route critical path.
    - `processTurn` still receives `onPostTurn` for background work after visible response.
    - Existing turn lock and snapshot code remains in place.
    - `tickPresentNpcs` is intentionally retained in `backend/src/engine/npc-agent.ts` for background/offscreen/future callers unless impact analysis proves no remaining consumer; this plan removes normal route use only.
  </behavior>
  <action>Run `gitnexus_impact({target: "buildOnBeforeVisibleNarration", direction: "upstream", repo: "WorldForge"})` and `gitnexus_impact({target: "tickPresentNpcs", direction: "upstream", repo: "WorldForge"})` before editing. Remove `buildOnBeforeVisibleNarration(...)` and `runLocalPresentSceneSettlement(...)` from `backend/src/routes/chat.ts` if they become unused. Remove the `tickPresentNpcs` import from `chat.ts` unless another non-critical route-level path still uses it. Do not delete `tickPresentNpcs` from `backend/src/engine/npc-agent.ts` in this plan; explicit rationale is that it may remain for background/offscreen or future non-critical autonomy paths while Phase 70 removes it from the normal visible action/retry path. Ensure both `/action` and `/retry` pass no `onBeforeVisibleNarration` callback into `processTurn(...)`. Preserve `tryBeginTurn`, `captureSnapshot`, `restoreSnapshot`, `setLastTurnSnapshot`, `clearLastTurnSnapshot`, `endTurn`, SSE streaming, and `buildOnPostTurn(...)`.</action>
  <verify>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>cd backend && npx vitest run src/routes/__tests__/chat.scene-plan.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "buildOnBeforeVisibleNarration|runLocalPresentSceneSettlement" backend/src/routes/chat.ts` returns no matches.</criterion>
    <criterion>`rg "tickPresentNpcs" backend/src/routes/chat.ts` returns no matches.</criterion>
    <criterion>`rg "export async function tickPresentNpcs|tickPresentNpcs" backend/src/engine/npc-agent.ts` returns matches, with rationale captured in `70A-MIGRATION-PLAN.md` by Plan 70-08.</criterion>
    <criterion>`rg "buildOnPostTurn|captureSnapshot|restoreSnapshot|tryBeginTurn|endTurn" backend/src/routes/chat.ts` returns matches.</criterion>
    <criterion>Route tests and backend typecheck exit 0.</criterion>
  </acceptance_criteria>
  <done>Normal route critical path no longer launches independent present NPC LLM/tool ticks before final narration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove retry and rollback parity on ScenePlan path</name>
  <files>backend/src/routes/__tests__/chat.test.ts, backend/src/routes/__tests__/chat.scene-plan.test.ts</files>
  <read_first>
    <file>backend/src/routes/__tests__/chat.test.ts</file>
    <file>backend/src/routes/chat.ts</file>
    <file>backend/src/engine/state-snapshot.ts</file>
    <file>.planning/phases/69-judge-owned-hidden-pass-migration-and-narrator-only-runtime/69-VALIDATION.md</file>
  </read_first>
  <behavior>
    - `/api/chat/retry` reuses the same ScenePlan route options shape as `/api/chat/action`.
    - ScenePlan validation/execution error restores pre-turn snapshot and does not emit `done`.
    - ScenePlan execution failure after earlier successful action N-1 restores pre-turn snapshot, removes partial mutations, and persists no unsafe assistant message.
    - Undo remains based on the stored last-turn snapshot.
  </behavior>
  <action>Add `backend/src/routes/__tests__/chat.scene-plan.test.ts` if a dedicated route test file is clearer than expanding `chat.test.ts`. Use a mocked `processTurn` generator that emits `oracle_result`, `scene-settling`, simulates `executeScenePlan` failure after an earlier validated action/action N-1, and then throws before any safe final narration. Assert `restoreSnapshot(campaignId, snapshot)` is called, `clearLastTurnSnapshot(campaignId)` is called, `endTurn(campaignId)` is called, the SSE body contains `event: error`, the SSE body does not contain `event: done`, the SSE body does not contain unsafe `event: narrative`, and `appendChatMessages` does not persist an assistant message from the failed turn. Where the route test can inspect DB or mocked snapshot state, prove restore removes partial mutations from the earlier action; otherwise assert the route calls the exact snapshot restore seam after the action-N failure and add a TODO-free test name `execution failure after action N restores snapshot`.</action>
  <verify>
    <automated>cd backend && npx vitest run src/routes/__tests__/chat.scene-plan.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "onBeforeVisibleNarration|onPostTurn|restoreSnapshot|event: error|event: done|event: narrative|appendChatMessages|action N|partial mutations" backend/src/routes/__tests__/chat.scene-plan.test.ts backend/src/routes/__tests__/chat.test.ts` returns matches.</criterion>
    <criterion>Tests assert `onBeforeVisibleNarration` is `undefined` or absent on the `processTurn` call options for action and retry.</criterion>
    <criterion>Targeted route Vitest exits 0.</criterion>
  </acceptance_criteria>
  <done>Route tests prove ScenePlan failures keep current rollback/retry safety and no present-NPC critical-path hook remains.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route -> turn processor | Route config determines whether independent NPC autonomy can enter visible critical path. |
| Turn processor error -> snapshot restore | Thrown errors from ScenePlan validation/execution must restore route snapshot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-07-01 | Elevation of Privilege | `chat.ts` present-NPC hook | mitigate | Remove `tickPresentNpcs` from normal action/retry critical path. |
| T-70-07-02 | Tampering | route failure handling | mitigate | Tests assert restoreSnapshot and no `done` on ScenePlan error. |
| T-70-07-03 | Denial of Service | turn lock | mitigate | Preserve `tryBeginTurn` and `endTurn` in finally path. |
| T-70-07-04 | Tampering | partial ScenePlan execution failure | mitigate | Route tests cover action-N failure after earlier mutations and assert snapshot restore plus no unsafe assistant persistence. |
| T-70-07-05 | Elevation of Privilege | `tickPresentNpcs` orphan path | mitigate | Retain function only outside normal route critical path with explicit background/offscreen rationale. |
</threat_model>

<verification>
Run backend typecheck plus `cd backend && npx vitest run src/routes/__tests__/chat.scene-plan.test.ts src/routes/__tests__/chat.test.ts`.
</verification>

<success_criteria>
The route no longer authorizes a second visible-turn NPC mini-round, and rollback/retry behavior remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-07-SUMMARY.md`.
</output>
