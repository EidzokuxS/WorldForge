---
phase: 70-reactive-scene-resolution-and-canonical-event-flow
plan: 03
type: execute
wave: 1
depends_on: [70-01]
files_modified:
  - backend/src/engine/scene-plan-schema.ts
  - backend/src/engine/scene-planner.ts
  - backend/src/engine/__tests__/scene-planner.test.ts
autonomous: true
requirements: [P70-R2, P70-R5, P70-R7]
must_haves:
  truths:
    - "Judge returns one strict ScenePlan object, not prose and not runtime tool execution."
    - "Scene Planner receives Oracle result as input and does not replace Oracle outcome authority in Phase 70."
    - "Malformed or over-broad ScenePlan output repairs once and then fails loudly."
    - "ScenePlan planned actions identify actors by SceneFrame actor IDs and `narratorFacts` uses backend references instead of LLM prose."
  artifacts:
    - path: backend/src/engine/scene-plan-schema.ts
      provides: "Strict ScenePlan schema, caps, action union, and formatting helpers."
      contains: "scenePlanSchema|scenePlanLooseSchema|formatScenePlanValidationIssues"
    - path: backend/src/engine/scene-planner.ts
      provides: "Judge-lane safeGenerateObject ScenePlan call and repair pass."
      contains: "runScenePlanner|safeGenerateObject|withRole"
    - path: backend/src/engine/__tests__/scene-planner.test.ts
      provides: "Schema and repair behavior tests."
      contains: "repair|Oracle|strict"
  key_links:
    - from: backend/src/engine/scene-planner.ts
      to: backend/src/ai/generate-object-safe.ts
      via: "safeGenerateObject"
      pattern: "safeGenerateObject"
    - from: backend/src/engine/scene-planner.ts
      to: backend/src/engine/scene-plan-schema.ts
      via: "strict parse after loose candidate"
      pattern: "scenePlanSchema"
---

<objective>
Implement the strict ScenePlan model-call module using the existing judge-lane `safeGenerateObject` and repair pattern.

Purpose: Replace separate world-brain direction and hidden adjudication planning with one structured ScenePlan decision while preserving Oracle as the bounded outcome authority per D-01 and D-02.
Output: `scene-plan-schema.ts`, `scene-planner.ts`, and focused planner tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-CONTEXT.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-RESEARCH.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-PATTERNS.md
@backend/src/engine/scene-plan-schema.ts
@backend/src/engine/scene-planner.ts
@backend/src/engine/world-brain.ts
@backend/src/engine/hidden-adjudication.ts
@backend/src/ai/generate-object-safe.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement ScenePlan schema normalization and repair support</name>
  <files>backend/src/engine/scene-plan-schema.ts, backend/src/engine/__tests__/scene-planner.test.ts</files>
  <read_first>
    <file>backend/src/engine/scene-plan-schema.ts</file>
    <file>backend/src/engine/world-brain.ts</file>
    <file>backend/src/engine/hidden-adjudication.ts</file>
    <file>backend/src/engine/tool-schemas.ts</file>
    <file>.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Loose candidate object can be sanitized into strict ScenePlan only through explicit normalization.
    - Unknown actor/tool names are not silently accepted by schema; validator handles legality after strict shape parse.
    - Schema caps match consensus: plannedActions max 8, supportResponses max 2, deferredHooks max 4, hiddenRationale max 280.
    - `narratorFacts` allows only event/action/response references and rejects free prose fields that could become an unguarded hidden-fact channel.
  </behavior>
  <action>Extend `scene-plan-schema.ts` with `scenePlanLooseSchema`, `sanitizeScenePlanCandidate(candidate, frame)`, `formatScenePlanValidationIssues(issues)`, and `buildScenePlanContract()`. Normalization may trim strings and clamp empty arrays to `[]`; it must not invent actor IDs, convert display names into actor IDs without a SceneFrame roster match, convert hidden actors into visible facts, accept free prose in `narratorFacts`, or turn invalid tool names into no-op actions. `narratorFacts` must be represented as references such as `eventIds`, `responseIds`, `actionIds`, and `toolResultRefs`; reject fields named `summary`, `description`, `text`, `prose`, or equivalent free-text fact bodies. Add tests that overlong strings, extra keys, too many actions, too many support responses, display-name actor references, prose-bearing `narratorFacts`, and malformed runtime tool inputs are rejected by strict parse.</action>
  <verify>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>cd backend && npx vitest run src/engine/__tests__/scene-planner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "scenePlanLooseSchema|sanitizeScenePlanCandidate|buildScenePlanContract|formatScenePlanValidationIssues|narratorFacts" backend/src/engine/scene-plan-schema.ts` returns matches.</criterion>
    <criterion>`rg "SCENE_PLAN_ACTION_LIMIT|SCENE_PLAN_SUPPORT_RESPONSE_LIMIT|SCENE_PLAN_DEFERRED_HOOK_LIMIT|SCENE_PLAN_HIDDEN_RATIONALE_MAX" backend/src/engine/scene-plan-schema.ts` returns matches.</criterion>
    <criterion>Typecheck and targeted Vitest command exit 0.</criterion>
  </acceptance_criteria>
  <done>ScenePlan schema layer is strict, bounded, test-covered, and ready for model-call integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement judge-lane ScenePlan generation</name>
  <files>backend/src/engine/scene-planner.ts, backend/src/engine/__tests__/scene-planner.test.ts</files>
  <read_first>
    <file>backend/src/engine/scene-planner.ts</file>
    <file>backend/src/engine/world-brain.ts</file>
    <file>backend/src/engine/scene-plan-schema.ts</file>
    <file>backend/src/ai/generate-object-safe.ts</file>
    <file>backend/src/ai/role-context.ts</file>
    <file>backend/src/engine/scene-frame.ts</file>
    <file>.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-RESEARCH.md</file>
  </read_first>
  <behavior>
    - `runScenePlanner` calls `safeGenerateObject` with role `judge`, temperature 0, and SceneFrame plus Oracle result in prompt text.
    - Prompt tells the judge to use actor IDs from the SceneFrame roster for planned actions and response references; display names are labels only.
    - First invalid strict parse triggers one repair pass with validation issues.
    - Repair failure throws; no fallback narration or empty plan is returned.
  </behavior>
  <action>Run `gitnexus_impact({target: "runWorldBrainSceneDirection", direction: "upstream", repo: "WorldForge"})` before editing because this module copies its repair pattern. Implement `runScenePlanner(args: { provider; frame; playerAction; oracleResult; outcomeBounds?; recentConversation?; maxOutputTokens? }): Promise&lt;ScenePlan&gt;`. Use `createModel`, `withRole("judge", ...)`, `safeGenerateObject`, `scenePlanLooseSchema`, `sanitizeScenePlanCandidate`, strict `scenePlanSchema.safeParse`, and a repair prompt built from `formatScenePlanValidationIssues`. The initial prompt must include strings `SCENE FRAME`, `ORACLE RESULT`, `ALLOWED ACTORS`, `ALLOWED TOOLS`, `USE ACTOR IDS`, and `NARRATOR FACT REFERENCES ONLY`. The `ALLOWED ACTORS` section may show labels for readability, but every planned actor reference must use the UUID/id from the SceneFrame roster. Tests must mock `safeGenerateObject` for success, repair success, repair failure, display-name actor output, and prose-bearing `narratorFacts` output.</action>
  <verify>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>cd backend && npx vitest run src/engine/__tests__/scene-planner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "withRole\\(\"judge\"|safeGenerateObject|SCENE FRAME|ORACLE RESULT|ALLOWED TOOLS|USE ACTOR IDS|NARRATOR FACT REFERENCES ONLY" backend/src/engine/scene-planner.ts` returns matches.</criterion>
    <criterion>`rg "repair|strict-parse|Validation issues" backend/src/engine/scene-planner.ts backend/src/engine/__tests__/scene-planner.test.ts` returns matches.</criterion>
    <criterion>Targeted Vitest tests assert `safeGenerateObject` call count 1 on valid output and 2 on repair success.</criterion>
  </acceptance_criteria>
  <done>`runScenePlanner(...)` returns only a strict ScenePlan or throws a concrete error after repair budget is exhausted.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SceneFrame + player action -> Scene Planner | Trusted frame and untrusted player text are packaged for judge model reasoning. |
| Model object -> strict ScenePlan | Model output is parsed, sanitized, repaired once, and strict-parsed before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-03-01 | Tampering | `runScenePlanner` | mitigate | Strict schema parse plus one repair pass; throw after repair failure. |
| T-70-03-02 | Information Disclosure | ScenePlan prompt/repair logs | mitigate | Do not log full frame, hidden rationale, or prompt body outside existing explicit prompt-dump controls. |
| T-70-03-03 | Repudiation | ScenePlan repair | mitigate | Tests assert repair path includes formatted validation issues. |
| T-70-03-04 | Information Disclosure | `narratorFacts` | mitigate | Require reference-only `narratorFacts`; reject LLM-authored prose before it can reach NarratorPacket or prompt formatting. |
</threat_model>

<verification>
Run backend typecheck and `cd backend && npx vitest run src/engine/__tests__/scene-planner.test.ts`.
</verification>

<success_criteria>
The judge-lane ScenePlan call is strict, bounded, repairable, and independent from Oracle outcome calculation.
</success_criteria>

<output>
After completion, create `.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-03-SUMMARY.md`.
</output>
