---
phase: 70-reactive-scene-resolution-and-canonical-event-flow
plan: 02
type: execute
wave: 1
depends_on: [70-01]
files_modified:
  - backend/src/engine/scene-frame.ts
  - backend/src/engine/__tests__/scene-frame.test.ts
autonomous: true
requirements: [P70-R1, P70-R5, P70-R7]
must_haves:
  truths:
    - "SceneFrame is deterministic from stored campaign state and existing scene/presence seams."
    - "SceneFrame includes current tick, player, scene scope, roster buckets, awareness, recent events, target candidates, movement candidates, Oracle context, combat envelope candidate, and allowed tools."
    - "Every roster actor and candidate exposes a stable backend actor/location/item ID so later ScenePlan actions never depend on display names."
    - "SceneFrame does not widen into global simulation or add persistence schema."
  artifacts:
    - path: backend/src/engine/scene-frame.ts
      provides: "Deterministic per-turn situation packet builder."
      contains: "buildSceneFrame|resolveScenePresence|allowedTools"
    - path: backend/src/engine/__tests__/scene-frame.test.ts
      provides: "SceneFrame behavior tests for roster, awareness, recent events, candidates, and allowed tools."
      contains: "active|support|background|movementCandidates|targetCandidates"
  key_links:
    - from: backend/src/engine/scene-frame.ts
      to: backend/src/engine/scene-presence.ts
      via: "resolveScenePresence and awareness helpers"
      pattern: "resolveScenePresence|getObserverAwareness"
    - from: backend/src/engine/scene-frame.ts
      to: backend/src/engine/tool-schemas.ts
      via: "allowed runtime tool list"
      pattern: "runtimeToolInputSchemas"
---

<objective>
Implement `buildSceneFrame(...)` as the deterministic situation packet that replaces loose runtime context assembly for ScenePlan input.

Purpose: Give the judge one factual local scene frame while keeping engine ownership over presence, awareness, target/movement candidates, Oracle context, and tool allow-lists per D-03.
Output: Working `scene-frame.ts` plus focused unit coverage.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-CONTEXT.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-RESEARCH.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-PATTERNS.md
@backend/src/engine/scene-frame.ts
@backend/src/engine/scene-presence.ts
@backend/src/engine/scene-assembly.ts
@backend/src/engine/target-context.ts
@backend/src/engine/combat-envelope.ts
@backend/src/engine/tool-schemas.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build deterministic SceneFrame projection</name>
  <files>backend/src/engine/scene-frame.ts, backend/src/engine/__tests__/scene-frame.test.ts</files>
  <read_first>
    <file>backend/src/engine/scene-frame.ts</file>
    <file>backend/src/engine/scene-presence.ts</file>
    <file>backend/src/engine/scene-assembly.ts</file>
    <file>backend/src/engine/target-context.ts</file>
    <file>backend/src/engine/combat-envelope.ts</file>
    <file>backend/src/engine/tool-schemas.ts</file>
    <file>backend/src/db/schema.ts</file>
    <file>.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Clear-present actors become `roster.active`.
    - Hint/broad-location actors become `roster.support` or `roster.background` without leaking hidden identities to player-visible fields.
    - Actor display names are labels only; all planner/action references use roster actor IDs.
    - `allowedTools` is derived from `runtimeToolInputSchemas` names and excludes tools not supported by `executeToolCall`.
  </behavior>
  <action>Run `gitnexus_impact({target: "assembleAuthoritativeScene", direction: "upstream", repo: "WorldForge"})` and `gitnexus_impact({target: "resolveScenePresence", direction: "upstream", repo: "WorldForge"})` before editing. Implement `buildSceneFrame(options)` by reading the current player row, current location, current scene scope, NPC rows in current broad location, recent local committed events, movement candidates from connected location state, and target candidates from player-visible or scene-present actors/items. Reuse `resolveScenePresence`, `getObserverAwareness`, `getObserverKnowledgeBasis`, `inferPresenceVisibility`, and `runtimeToolInputSchemas`. Ensure `roster.active`, `roster.support`, `roster.background`, `targetCandidates`, and `movementCandidates` include stable backend IDs (`actorId`, `locationId`, `itemId`, or equivalent) plus optional display labels; labels must not be the primary key for downstream planned actions. Include the Oracle input context as a field named `oracleContext` and a nullable `combatEnvelope` field; do not call Oracle or any LLM from `buildSceneFrame`. Keep all data derived from existing DB/state/tool paths; do not add Drizzle migrations.</action>
  <verify>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>cd backend && npx vitest run src/engine/__tests__/scene-frame.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "resolveScenePresence|getObserverAwareness|getObserverKnowledgeBasis" backend/src/engine/scene-frame.ts` returns matches.</criterion>
    <criterion>`rg "allowedTools.*runtimeToolInputSchemas|Object.keys\\(runtimeToolInputSchemas\\)" backend/src/engine/scene-frame.ts` returns a match.</criterion>
    <criterion>`rg "active|support|background|actorId|locationId|recentEvents|targetCandidates|movementCandidates|oracleContext|combatEnvelope" backend/src/engine/__tests__/scene-frame.test.ts` returns matches.</criterion>
    <criterion>Targeted Vitest and backend typecheck exit 0.</criterion>
  </acceptance_criteria>
  <done>`buildSceneFrame(...)` returns a deterministic local situation packet with no LLM call and no persistence mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock hidden and broad-location filtering behavior</name>
  <files>backend/src/engine/scene-frame.ts, backend/src/engine/__tests__/scene-frame.test.ts</files>
  <read_first>
    <file>backend/src/engine/scene-frame.ts</file>
    <file>backend/src/engine/scene-presence.ts</file>
    <file>backend/src/engine/__tests__/scene-presence.test.ts</file>
    <file>.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-VALIDATION.md</file>
  </read_first>
  <behavior>
    - Actor with player awareness `none` can be available to validator/planner only as hidden context, not as narrator-visible identity.
    - Broad-location actor outside current scene scope cannot be `active` unless a deterministic channel or scene-scope rule justifies it.
  </behavior>
  <action>Add tests for `clear`, `hint`, and `none` awareness bands using deterministic fixtures. Assert hidden actor names are absent from player-facing frame labels, hint actors are represented through hint text, and broad-location actors outside scene scope land in `roster.background` unless an explicit channel candidate marks support eligibility. Add a helper such as `getSceneFrameVisibleActorNames(frame)` only if tests need a compact assertion target.</action>
  <verify>
    <automated>cd backend && npx vitest run src/engine/__tests__/scene-frame.test.ts src/engine/__tests__/scene-presence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "awareness.*none|hint|background" backend/src/engine/__tests__/scene-frame.test.ts` returns matches.</criterion>
    <criterion>`rg "hidden.*not.*visible|visibleActorNames|playerHints" backend/src/engine/__tests__/scene-frame.test.ts` returns matches.</criterion>
    <criterion>Targeted Vitest command exits 0.</criterion>
  </acceptance_criteria>
  <done>SceneFrame eligibility and visibility tests prove local scene scope is not broad-location membership.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB state -> SceneFrame | Stored state becomes model input; only deterministic filters may decide scene eligibility. |
| Hidden presence -> planner context | Hidden actors may inform planning but cannot appear as player-visible names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-02-01 | Tampering | `buildSceneFrame` | mitigate | Build roster from DB rows plus `resolveScenePresence`, not player claims. |
| T-70-02-02 | Information Disclosure | `SceneFrame` visible labels | mitigate | Tests assert awareness `none` actor names do not appear in player-facing fields. |
| T-70-02-03 | Elevation of Privilege | broad-location actors | mitigate | Background actors are not active unless scene scope/channel rules qualify them. |
| T-70-02-04 | Tampering | actor identity references | mitigate | SceneFrame exposes stable IDs for actor/action mapping; display names remain labels only. |
</threat_model>

<verification>
Run `npm --prefix backend run typecheck` and `cd backend && npx vitest run src/engine/__tests__/scene-frame.test.ts src/engine/__tests__/scene-presence.test.ts`.
</verification>

<success_criteria>
SceneFrame is a deterministic, local, bounded packet ready for Oracle and ScenePlan input.
</success_criteria>

<output>
After completion, create `.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-02-SUMMARY.md`.
</output>
