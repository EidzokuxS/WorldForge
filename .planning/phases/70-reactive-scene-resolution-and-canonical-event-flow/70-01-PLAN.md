---
phase: 70-reactive-scene-resolution-and-canonical-event-flow
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/engine/scene-frame.ts
  - backend/src/engine/scene-plan-schema.ts
  - backend/src/engine/scene-planner.ts
  - backend/src/engine/scene-plan-validator.ts
  - backend/src/engine/scene-plan-executor.ts
  - backend/src/engine/narrator-packet.ts
  - backend/src/engine/__tests__/scene-frame.test.ts
  - backend/src/engine/__tests__/scene-planner.test.ts
  - backend/src/engine/__tests__/scene-plan-validator.test.ts
  - backend/src/engine/__tests__/scene-turn-packet.test.ts
  - backend/src/engine/__tests__/turn-processor.scene-plan.test.ts
autonomous: true
requirements: [P70-R1, P70-R2, P70-R3, P70-R4, P70-R5, P70-R6, P70-R7]
must_haves:
  truths:
    - "Executor has concrete backend-local contracts for SceneFrame, ScenePlan, validation, execution, NarratorPacket, and turn ordering before runtime wiring starts."
    - "ScenePlan action schemas reuse runtimeToolInputSchemas and keep Oracle separate per D-01."
    - "ScenePlan actor references use SceneFrame actor IDs, not display names, and `narratorFacts` is an ID/reference contract, not free prose."
    - "Wave 0 tests exist for the five validation files named in 70-VALIDATION.md and may go green because they prove contract surfaces, not live runtime behavior."
    - "Later live-wiring plans remain responsible for red/green behavior tests before changing the normal turn path."
  artifacts:
    - path: backend/src/engine/scene-frame.ts
      provides: "SceneFrame types, caps, and builder signature."
      contains: "SceneFrame|buildSceneFrame|SCENE_FRAME"
    - path: backend/src/engine/scene-plan-schema.ts
      provides: "Strict ScenePlan Zod schema and tool-action union."
      contains: "scenePlanSchema|SCENE_PLAN_ACTION_LIMIT|hiddenRationale"
    - path: backend/src/engine/__tests__/turn-processor.scene-plan.test.ts
      provides: "Canonical ordering regression surface."
      contains: "ScenePlan|tickPresentNpcs|final narration"
  key_links:
    - from: backend/src/engine/scene-plan-schema.ts
      to: backend/src/engine/tool-schemas.ts
      via: "imports runtimeToolInputSchemas"
      pattern: "runtimeToolInputSchemas"
    - from: backend/src/engine/__tests__/turn-processor.scene-plan.test.ts
      to: backend/src/engine/turn-processor.ts
      via: "processTurn ordering assertions"
      pattern: "processTurn|scene-plan"
---

<objective>
Create the Phase 70 Wave 0 contract and test surface for a local Scene Planner of Record without changing the live turn path.

Purpose: Give every downstream executor exact exported names, schema keys, caps, and test files so implementation does not invent parallel shapes.
Output: Backend-local contract modules plus contract-first Wave 0 test files from `70-VALIDATION.md`; these tests may go green immediately because live behavior is wired in later TDD plans.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-CONTEXT.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-RESEARCH.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-PATTERNS.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-VALIDATION.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-WORLD-SIMULATION-DATA-FLOW-CONSENSUS.md
@.planning/phases/69-judge-owned-hidden-pass-migration-and-narrator-only-runtime/69-SUMMARY.md
@backend/src/engine/tool-schemas.ts
@backend/src/engine/hidden-adjudication.ts
@backend/src/engine/world-brain.ts
@backend/src/engine/scene-presence.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define SceneFrame and ScenePlan contracts</name>
  <files>backend/src/engine/scene-frame.ts, backend/src/engine/scene-plan-schema.ts, backend/src/engine/__tests__/scene-frame.test.ts, backend/src/engine/__tests__/scene-planner.test.ts</files>
  <read_first>
    <file>.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-CONTEXT.md</file>
    <file>.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-RESEARCH.md</file>
    <file>.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-PATTERNS.md</file>
    <file>backend/src/engine/hidden-adjudication.ts</file>
    <file>backend/src/engine/tool-schemas.ts</file>
    <file>backend/src/engine/scene-presence.ts</file>
    <file>backend/src/engine/combat-envelope.ts</file>
  </read_first>
  <behavior>
    - SceneFrame exports exact roster buckets `active`, `support`, and `background`.
    - ScenePlan schema is strict, bounded, and uses `RuntimeToolName` plus `runtimeToolInputSchemas`.
    - ScenePlan planned actions and response references use actor IDs from the SceneFrame roster; display names are context only and are rejected as actor references.
    - `narratorFacts` contains backend-resolvable event/action/response references only, not LLM-authored prose that could bypass hidden-fact guards.
    - Oracle remains a separate input to ScenePlan per D-01; ScenePlan does not own outcome-tier selection.
    - This Wave 0 task is contract-first: tests may be green once exported types/schemas compile; runtime red/green behavior is enforced by later live-wiring tasks.
  </behavior>
  <action>Create `backend/src/engine/scene-frame.ts` exporting `SceneActor`, `SceneFrame`, `SceneFrameRoster`, `SceneFramePerception`, `SceneFrameRecentEvent`, `SceneFrameTargetCandidate`, `SceneFrameMovementCandidate`, `SceneFrameDeferredHook`, `SceneFrameBuildOptions`, `SCENE_FRAME_RECENT_EVENT_LIMIT = 12`, `SCENE_FRAME_TARGET_CANDIDATE_LIMIT = 12`, `SCENE_FRAME_MOVEMENT_CANDIDATE_LIMIT = 12`, and the signature `export async function buildSceneFrame(options: SceneFrameBuildOptions): Promise&lt;SceneFrame&gt;`. Every `SceneActor` and target/movement candidate must carry stable backend IDs; labels are display-only. Create `backend/src/engine/scene-plan-schema.ts` exporting `SCENE_PLAN_ACTION_LIMIT = 8`, `SCENE_PLAN_HIDDEN_RATIONALE_MAX = 280`, `SCENE_PLAN_SUPPORT_RESPONSE_LIMIT = 2`, `SCENE_PLAN_DEFERRED_HOOK_LIMIT = 4`, `scenePlanActionSchema`, `sceneResponseSchema`, `scenePlanSchema`, and inferred types `ScenePlanAction`, `SceneResponse`, `ScenePlan`. The schema keys must be exactly `actionInterpretation`, `anchorEvent`, `primaryResponse`, `supportResponses`, `plannedActions`, `deferredHooks`, `narratorFacts`, and `hiddenRationale`; every object schema must be `.strict()`. `narratorFacts` must be shaped as backend-resolvable references such as `anchorEventId`, `eventIds`, `responseIds`, and `toolResultRefs`; it must not accept free prose fields such as `summary`, `description`, or `text`. Add focused tests that parse a valid plan, reject an extra top-level key, reject more than 8 actions, reject more than 2 support responses, reject display-name actor references, reject prose-bearing `narratorFacts`, and reject invalid tool inputs. Do not add DB migrations or shared-package exports; Phase 70 contracts stay backend-local unless code proves a frontend consumer.</action>
  <verify>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>cd backend && npx vitest run src/engine/__tests__/scene-frame.test.ts src/engine/__tests__/scene-planner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "export interface SceneFrame|export async function buildSceneFrame|SCENE_FRAME_RECENT_EVENT_LIMIT" backend/src/engine/scene-frame.ts` returns matches.</criterion>
    <criterion>`rg "scenePlanSchema|SCENE_PLAN_ACTION_LIMIT|runtimeToolInputSchemas|hiddenRationale" backend/src/engine/scene-plan-schema.ts` returns matches.</criterion>
    <criterion>`rg "rejects extra|support response|invalid tool|actor ID|narratorFacts.*prose" backend/src/engine/__tests__/scene-planner.test.ts` returns matches.</criterion>
    <criterion>Typecheck and targeted Vitest command exit 0.</criterion>
  </acceptance_criteria>
  <done>SceneFrame and ScenePlan contracts compile, schema caps are tested, and no runtime turn path imports the new modules yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define validator, executor, packet, and ordering test surfaces</name>
  <files>backend/src/engine/scene-planner.ts, backend/src/engine/scene-plan-validator.ts, backend/src/engine/scene-plan-executor.ts, backend/src/engine/narrator-packet.ts, backend/src/engine/__tests__/scene-plan-validator.test.ts, backend/src/engine/__tests__/scene-turn-packet.test.ts, backend/src/engine/__tests__/turn-processor.scene-plan.test.ts</files>
  <read_first>
    <file>backend/src/engine/turn-processor.ts</file>
    <file>backend/src/engine/prompt-assembler.ts</file>
    <file>backend/src/engine/scene-assembly.ts</file>
    <file>backend/src/engine/scene-presence.ts</file>
    <file>backend/src/engine/tool-executor.ts</file>
    <file>backend/src/engine/hidden-adjudication.ts</file>
    <file>.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-VALIDATION.md</file>
  </read_first>
  <behavior>
    - Validator contract rejects invalid actors, invalid tools, hidden narrator facts, and Oracle/outcome contradictions before execution.
    - Executor contract commits only validated actions through existing deterministic tool execution per D-03.
    - Executor contract returns full per-action `ToolResult` metadata needed to project `CanonicalTurnPacket` and `NarratorPacket`; it does not collapse results into prose summaries.
    - NarratorPacket contract exposes only player-perceivable committed facts per D-07.
    - Ordering tests created here pin names and call-order assertions for later runtime red/green work; they do not require live turn behavior to change in Wave 0.
  </behavior>
  <action>Create module contracts with concrete exported names: `runScenePlanner(args)`, `validateScenePlan(args)`, `executeScenePlan(args)`, `buildNarratorPacket(args)`, `formatNarratorPacketForPrompt(packet)`, `ScenePlanValidationIssue`, `ValidatedScenePlan`, `ExecutedScenePlan`, `CanonicalTurnPacket`, and `NarratorPacket`. `ExecutedScenePlan` must retain full `ToolResult` metadata per action, including action order, action id/ref, `toolName`, input, and raw `ToolResult`, so packet projection never has to trust LLM prose. Add Wave 0 test files named exactly by `70-VALIDATION.md`: `scene-plan-validator.test.ts`, `scene-turn-packet.test.ts`, and `turn-processor.scene-plan.test.ts`. The tests must import the exported contracts and pin exact behavior names for actor legality, UUID/actor-ID mapping, tool legality, full ToolResult projection, narratorFacts-as-references, outcome-bound preservation, hidden fact exclusion, canonical ordering, retry/rollback, and no independent `tickPresentNpcs()` critical-path call. Keep runtime wiring out of this task; this task only creates contract surfaces and executable tests for the surfaces.</action>
  <verify>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>cd backend && npx vitest run src/engine/__tests__/scene-plan-validator.test.ts src/engine/__tests__/scene-turn-packet.test.ts src/engine/__tests__/turn-processor.scene-plan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "runScenePlanner|validateScenePlan|executeScenePlan|buildNarratorPacket" backend/src/engine` returns matches in the new modules.</criterion>
    <criterion>`rg "actor legality|actor ID|ToolResult|narratorFacts|hidden fact|tickPresentNpcs|retry|rollback|canonical" backend/src/engine/__tests__/scene-plan-validator.test.ts backend/src/engine/__tests__/scene-turn-packet.test.ts backend/src/engine/__tests__/turn-processor.scene-plan.test.ts` returns matches.</criterion>
    <criterion>Typecheck and targeted Vitest command exit 0.</criterion>
  </acceptance_criteria>
  <done>All Phase 70 module names and Wave 0 contract test filenames exist and compile, with no runtime route or processor behavior changed.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player input -> Scene Planner | Freeform player text enters judge prompts but cannot define actors, visibility, tools, or legality. |
| ScenePlan model output -> backend contracts | Structured model output is untrusted until Zod parsing and validator checks pass. |
| Hidden state -> NarratorPacket | Hidden actors/facts may be planner inputs but cannot cross into final-visible prompt inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01-01 | Tampering | `scene-plan-schema.ts` | mitigate | Use `.strict()` schemas, hard caps, and `runtimeToolInputSchemas` instead of ad hoc action shapes. |
| T-70-01-02 | Information Disclosure | `narrator-packet.ts` | mitigate | Contract exposes `NarratorPacket` without `hiddenRationale`, raw ScenePlan, broad world rows, or hidden actor names. |
| T-70-01-03 | Elevation of Privilege | `turn-processor.scene-plan.test.ts` | mitigate | Test surface names the forbidden independent `tickPresentNpcs()` critical-path call per D-05. |
| T-70-01-04 | Information Disclosure | `scene-plan-schema.ts` `narratorFacts` | mitigate | Contract requires backend IDs/references and rejects LLM-authored prose that could carry hidden facts around packet filtering. |
</threat_model>

<verification>
Run `npm --prefix backend run typecheck` and the five-file Wave 0 command from `70-VALIDATION.md`. Green Wave 0 means contract modules compile and schema/fixture expectations are executable; it does not replace later red/green live-behavior tests in Plans 70-02 through 70-08.
</verification>

<success_criteria>
Wave 0 contract modules and test files exist; schema caps are executable; later plans can import stable contract names without inventing new names; live behavior remains unchanged until the later TDD plans wire it.
</success_criteria>

<output>
After completion, create `.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-01-SUMMARY.md`.
</output>
