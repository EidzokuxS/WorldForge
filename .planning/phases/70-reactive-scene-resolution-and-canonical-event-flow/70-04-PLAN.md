---
phase: 70-reactive-scene-resolution-and-canonical-event-flow
plan: 04
type: execute
wave: 2
depends_on: [70-02, 70-03]
files_modified:
  - backend/src/engine/scene-plan-validator.ts
  - backend/src/engine/scene-plan-executor.ts
  - backend/src/engine/__tests__/scene-plan-validator.test.ts
autonomous: true
requirements: [P70-R3, P70-R5, P70-R7]
must_haves:
  truths:
    - "Invalid actors, hidden actor exposure, invalid tools, malformed tool args, and Oracle/outcome contradictions fail before mutation."
    - "Validated ScenePlan actions execute through existing deterministic tool services."
    - "Execution failure after action N throws and preserves route-owned snapshot restore behavior."
    - "ExecutedScenePlan carries full ToolResult metadata for CanonicalTurnPacket and NarratorPacket projection."
  artifacts:
    - path: backend/src/engine/scene-plan-validator.ts
      provides: "Pure validation gate before planned actions mutate state."
      contains: "validateScenePlan|ScenePlanValidationIssue|ValidatedScenePlan"
    - path: backend/src/engine/scene-plan-executor.ts
      provides: "Ordered execution bridge through executeToolCall."
      contains: "executeScenePlan|executeToolCall|ExecutedScenePlan"
    - path: backend/src/engine/__tests__/scene-plan-validator.test.ts
      provides: "Actor legality, tool legality, outcome contradiction, and fail-loud coverage."
      contains: "unknown actor|invalid tool|outcome|before mutation"
  key_links:
    - from: backend/src/engine/scene-plan-validator.ts
      to: backend/src/engine/scene-frame.ts
      via: "frame allow-lists"
      pattern: "SceneFrame"
    - from: backend/src/engine/scene-plan-executor.ts
      to: backend/src/engine/tool-executor.ts
      via: "executeToolCall"
      pattern: "executeToolCall"
---

<objective>
Implement the backend gate that turns a model-proposed ScenePlan into either a validated ordered action set or a loud failure.

Purpose: Preserve the core value that LLMs propose meaning while backend validates legality and commits state per D-03.
Output: `validateScenePlan(...)`, `executeScenePlan(...)`, and focused validation/execution tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-CONTEXT.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-RESEARCH.md
@.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-PATTERNS.md
@backend/src/engine/scene-frame.ts
@backend/src/engine/scene-plan-schema.ts
@backend/src/engine/scene-plan-validator.ts
@backend/src/engine/scene-plan-executor.ts
@backend/src/engine/hidden-adjudication.ts
@backend/src/engine/tool-executor.ts
@backend/src/engine/tool-schemas.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement strict ScenePlan preflight validator</name>
  <files>backend/src/engine/scene-plan-validator.ts, backend/src/engine/__tests__/scene-plan-validator.test.ts</files>
  <read_first>
    <file>backend/src/engine/scene-plan-validator.ts</file>
    <file>backend/src/engine/scene-frame.ts</file>
    <file>backend/src/engine/scene-plan-schema.ts</file>
    <file>backend/src/engine/tool-schemas.ts</file>
    <file>backend/src/engine/combat-bounds.ts</file>
    <file>backend/src/engine/combat-envelope.ts</file>
    <file>.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-VALIDATION.md</file>
  </read_first>
  <behavior>
    - Unknown actor IDs fail with issue code `unknown_actor`.
    - Background actors cannot own scene-changing planned actions.
    - Tool name and tool input validation happens before `executeToolCall`.
    - ScenePlan cannot contradict Oracle outcome bounds passed into validation.
    - Actor references must be IDs from SceneFrame roster, not display names.
    - `narratorFacts` must reference event/action/response IDs only; free prose or hidden actor identity in narrator facts fails validation.
  </behavior>
  <action>Run `gitnexus_impact({target: "executeToolCall", direction: "upstream", repo: "WorldForge"})` before editing because this validator protects that mutation path. Implement `validateScenePlan(args: { frame: SceneFrame; plan: ScenePlan; oracleResult; outcomeBounds? }): ValidatedScenePlanResult`, where the result is `{ ok: true; plan: ValidatedScenePlan } | { ok: false; issues: ScenePlanValidationIssue[] }`. Add issue codes `unknown_actor`, `display_name_actor_reference`, `inactive_primary_actor`, `background_actor_action`, `hidden_actor_visible_fact`, `narrator_fact_prose`, `unsupported_tool`, `invalid_tool_input`, `outcome_contradiction`, and `too_many_primary_scene_changers`. Validate every actor reference from actionInterpretation, anchorEvent, responses, deferredHooks, narratorFacts references, and each planned action input with the matching `runtimeToolInputSchemas[toolName].safeParse(...)`. Actor references must resolve by SceneFrame actor ID/UUID, never by display name. `narratorFacts` must contain only backend-resolvable references to frame/canonical packet items; reject free text or hidden actor names even if the schema layer missed them. Do not mutate DB state in validation.</action>
  <verify>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>cd backend && npx vitest run src/engine/__tests__/scene-plan-validator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "unknown_actor|display_name_actor_reference|invalid_tool_input|hidden_actor_visible_fact|narrator_fact_prose|outcome_contradiction" backend/src/engine/scene-plan-validator.ts backend/src/engine/__tests__/scene-plan-validator.test.ts` returns matches.</criterion>
    <criterion>`rg "runtimeToolInputSchemas\\[|safeParse" backend/src/engine/scene-plan-validator.ts` returns matches.</criterion>
    <criterion>Tests assert `executeToolCall` is not called by validation.</criterion>
    <criterion>Typecheck and targeted Vitest command exit 0.</criterion>
  </acceptance_criteria>
  <done>Invalid ScenePlans fail before mutation with machine-readable issues.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute validated ScenePlan actions through tool executor</name>
  <files>backend/src/engine/scene-plan-executor.ts, backend/src/engine/__tests__/scene-plan-validator.test.ts</files>
  <read_first>
    <file>backend/src/engine/scene-plan-executor.ts</file>
    <file>backend/src/engine/hidden-adjudication.ts</file>
    <file>backend/src/engine/tool-executor.ts</file>
    <file>backend/src/engine/tool-schemas.ts</file>
    <file>backend/src/engine/scene-plan-validator.ts</file>
  </read_first>
  <behavior>
    - Ordered plannedActions execute through `executeToolCall`.
    - First tool failure throws `ScenePlan action failed: {toolName}`.
    - Quick actions, state updates, emitted events, successful travel, and full per-action `ToolResult` metadata are projected in the same shape expected by current turn processor and packet code.
    - A failure on action N preserves evidence for successful earlier action results in memory and throws before final narration; route-level tests in 70-07 prove snapshot restore removes partial mutations.
  </behavior>
  <action>Implement `executeScenePlan(args: { campaignId: string; tick: number; outcomeTier?: string; plan: ValidatedScenePlan }): Promise&lt;ExecutedScenePlan&gt;` by adapting the `executeAdjudicationPlan` loop rather than duplicating tool logic. Preserve fields `toolCallResults`, `emittedEvents`, `quickActionsEmitted`, and `successfulTravel`; each `toolCallResults` entry must include action order, ScenePlan action id/ref when present, actor id when present, `toolName`, input args, and the full `ToolResult` object returned by `executeToolCall`. Add `canonicalEvents` only if built from successful executed actions and backend tool result metadata, not LLM `narratorFacts` prose. Throw on any `ToolResult.success === false`. Tests must mock `executeToolCall` and prove ordered execution, throw-on-action-N after earlier success, quick action projection, successful travel projection, and full `ToolResult` projection for packet builders.</action>
  <verify>
    <automated>npm --prefix backend run typecheck</automated>
    <automated>cd backend && npx vitest run src/engine/__tests__/scene-plan-validator.test.ts src/engine/__tests__/tool-executor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "executeScenePlan|executeToolCall|ScenePlan action failed|quickActionsEmitted|successfulTravel|ToolResult" backend/src/engine/scene-plan-executor.ts` returns matches.</criterion>
    <criterion>`rg "ordered execution|action N|throws.*failed|quick action|successful travel|full ToolResult" backend/src/engine/__tests__/scene-plan-validator.test.ts` returns matches.</criterion>
    <criterion>Targeted Vitest and backend typecheck exit 0.</criterion>
  </acceptance_criteria>
  <done>Only validated ScenePlan actions can reach existing runtime tool execution, and failed execution remains rollback-safe through thrown errors.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ScenePlan -> validator | Untrusted model output enters backend legality checks. |
| Validated plan -> tool executor | Trusted subset of model proposal reaches mutation-capable services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-04-01 | Tampering | `validateScenePlan` | mitigate | Validate actor allow-list, tool allow-list, tool inputs, and outcome bounds before mutation. |
| T-70-04-02 | Tampering | `executeScenePlan` | mitigate | Execute only `ValidatedScenePlan`, throw on first failed tool result. |
| T-70-04-03 | Repudiation | execution results | mitigate | Preserve full `ToolResult` metadata, emitted events, and successful travel projection for turn event and packet evidence. |
| T-70-04-04 | Information Disclosure | `validateScenePlan` `narratorFacts` | mitigate | Reject free prose and hidden actor names in narrator facts before packet projection. |
</threat_model>

<verification>
Run backend typecheck plus `cd backend && npx vitest run src/engine/__tests__/scene-plan-validator.test.ts src/engine/__tests__/tool-executor.test.ts`.
</verification>

<success_criteria>
ScenePlan validation rejects illegal proposals before mutation and execution uses only existing deterministic tool services.
</success_criteria>

<output>
After completion, create `.planning/phases/70-reactive-scene-resolution-and-canonical-event-flow/70-04-SUMMARY.md`.
</output>
