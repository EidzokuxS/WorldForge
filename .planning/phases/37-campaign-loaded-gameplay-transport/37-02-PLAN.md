---
phase: 37-campaign-loaded-gameplay-transport
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - frontend/lib/api.ts
  - frontend/lib/__tests__/api.test.ts
  - frontend/app/game/page.tsx
  - frontend/app/game/__tests__/page.test.tsx
autonomous: true
requirements:
  - RINT-01
must_haves:
  truths:
    - "The `/game` page fetches gameplay history by explicit campaignId after load or reload."
    - "Submitting an action, retrying, undoing, and editing on `/game` all send the loaded campaignId instead of relying on hidden session state."
    - "The `/game` page still remains the canonical gameplay page for this phase."
  artifacts:
    - path: "frontend/lib/api.ts"
      provides: "Campaign-explicit gameplay helper signatures and request payloads"
    - path: "frontend/lib/__tests__/api.test.ts"
      provides: "API helper regressions for gameplay transport payloads"
    - path: "frontend/app/game/page.tsx"
      provides: "Loaded-campaign gameplay wiring on `/game`"
    - path: "frontend/app/game/__tests__/page.test.tsx"
      provides: "Page regressions for explicit campaign gameplay calls after initialization"
  key_links:
    - from: "frontend/app/game/page.tsx"
      to: "frontend/lib/api.ts"
      via: "history/action/retry/undo/edit helpers"
      pattern: "campaignId"
    - from: "frontend/lib/api.ts"
      to: "/api/chat/*"
      via: "query/body payloads"
      pattern: "/api/chat"
    - from: "frontend/app/game/__tests__/page.test.tsx"
      to: "frontend/app/game/page.tsx"
      via: "mocked helper signatures"
      pattern: "campaignId"
---

<objective>
Wire the `/game` page and frontend gameplay helpers to the new explicit campaign-addressed backend transport contract from Plan 37-01.

Purpose: complete `RINT-01` end-to-end after reload while preserving `/game` as the canonical gameplay page for this phase.
Output: frontend API helpers that require `campaignId`, `/game` handlers that pass the loaded campaign id on every gameplay request, and targeted frontend regressions for those calls.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-campaign-loaded-gameplay-transport/37-CONTEXT.md
@.planning/phases/37-campaign-loaded-gameplay-transport/37-01-PLAN.md
@frontend/lib/api.ts
@frontend/lib/__tests__/api.test.ts
@frontend/app/game/page.tsx
@frontend/app/game/__tests__/page.test.tsx

<interfaces>
From frontend/lib/api.ts:
```typescript
export function chatRetry(): Promise<Response>;
export function chatUndo(): Promise<{ success: boolean; messagesRemoved: number }>;
export function chatEdit(
  messageIndex: number,
  newContent: string,
): Promise<{ success: boolean }>;
```

From frontend/app/game/page.tsx:
```typescript
const [activeCampaign, setActiveCampaign] = useState<CampaignMeta | null>(null);

const [history, world] = await Promise.all([
  apiGet<ChatHistoryResponse>("/api/chat/history"),
  getWorldData(campaign.id),
]);

const response = await apiStreamPost("/api/chat/action", {
  playerAction: actionText,
  intent: actionText,
  method: "",
});

const response = await chatRetry();
await chatUndo();
await chatEdit(messageIndex, newContent);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require campaignId in frontend gameplay helper signatures</name>
  <read_first>
    .planning/phases/37-campaign-loaded-gameplay-transport/37-CONTEXT.md
    .planning/phases/37-campaign-loaded-gameplay-transport/37-01-PLAN.md
    frontend/lib/api.ts
    frontend/lib/__tests__/api.test.ts
  </read_first>
  <files>frontend/lib/api.ts, frontend/lib/__tests__/api.test.ts</files>
  <behavior>
    - Test 1: `chatRetry`, `chatUndo`, and `chatEdit` send `campaignId` in the request body.
    - Test 2: history fetch helper path used by `/game` can encode explicit `campaignId` for the backend contract.
    - Test 3: helper signatures make the hidden-session gameplay transport impossible from the typed frontend API.
  </behavior>
  <action>Per D-08 and D-09, update `frontend/lib/api.ts` so the gameplay helper surface requires `campaignId` for `history`, `action`, `retry`, `undo`, and `edit`. Reuse existing API helper patterns instead of scattering raw fetch calls. Add or extend targeted `frontend/lib/__tests__/api.test.ts` assertions so the changed helpers prove the exact request path/body contract expected by Plan 37-01. Keep `/api/chat/*` unchanged externally per D-01 and do not introduce `/campaign/[id]/game` or any new route family (D-07).</action>
  <acceptance_criteria>
    - `frontend/lib/api.ts` exposes campaign-explicit helper signatures for the Phase 37 gameplay routes.
    - The helper implementations send `campaignId` in the query/body positions required by the backend plan.
    - `frontend/lib/__tests__/api.test.ts` asserts the concrete payload/path contract for the updated gameplay helpers.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend exec vitest run "lib/__tests__/api.test.ts"</automated>
  </verify>
  <done>Frontend gameplay helpers no longer permit session-coupled calls; every targeted helper requires and transmits the loaded campaign id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire `/game` to use explicit campaign gameplay calls after reload</name>
  <read_first>
    .planning/phases/37-campaign-loaded-gameplay-transport/37-CONTEXT.md
    .planning/phases/37-campaign-loaded-gameplay-transport/37-01-PLAN.md
    frontend/app/game/page.tsx
    frontend/app/game/__tests__/page.test.tsx
    frontend/lib/api.ts
  </read_first>
  <files>frontend/app/game/page.tsx, frontend/app/game/__tests__/page.test.tsx, frontend/lib/api.ts</files>
  <behavior>
    - Test 1: initialization fetches history with the resolved `campaign.id`, even when that campaign came from remembered state after reload.
    - Test 2: action, retry, undo, and edit handlers pass `activeCampaign.id` into the updated helpers.
    - Test 3: `/game` stays the canonical page and still falls back to remembered campaign loading before issuing gameplay requests.
  </behavior>
  <action>Per D-07, D-08, and D-09, keep `/game` as the page, preserve the current active-campaign-then-remembered-campaign bootstrap, and change the gameplay fetch/submit handlers so every call into the backend transport uses the resolved explicit `campaignId`. Update `frontend/app/game/page.tsx` to stop calling `/api/chat/history` and `/api/chat/action` as hidden session-only requests. Extend `frontend/app/game/__tests__/page.test.tsx` so initialization and gameplay handlers assert campaign-explicit helper usage. Do not widen this into URL route churn, checkpoint behavior, or unrelated shell changes.</action>
  <acceptance_criteria>
    - `frontend/app/game/page.tsx` only issues targeted gameplay transport calls after a concrete campaign id is available.
    - The remembered-campaign bootstrap remains intact, but the subsequent history/action/retry/undo/edit calls all carry explicit `campaignId`.
    - `frontend/app/game/__tests__/page.test.tsx` covers both initialization and at least one handler path that would fail if the page regressed to session-coupled calls.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/game/__tests__/page.test.tsx" "lib/__tests__/api.test.ts"</automated>
  </verify>
  <done>The `/game` page can reload, resolve a campaign, and issue all targeted gameplay requests against that explicit campaign identity without relying on hidden backend session state.</done>
</task>

</tasks>

<verification>
- Frontend targeted tests pass for updated API helpers and `/game` page wiring.
- Manual spot-check by reading `frontend/app/game/page.tsx` shows initialization still uses `/game` with active/remembered campaign fallback, but all gameplay transport calls use explicit `campaignId`.
- Manual spot-check by reading `frontend/lib/api.ts` shows `campaignId` is required in helper signatures for the targeted gameplay routes.
</verification>

<success_criteria>
- The frontend issues no targeted gameplay request without an explicit campaign id after initialization.
- `/game` remains the canonical gameplay route in this phase, with no `/campaign/[id]/game` expansion.
- The backend/frontend transport seam now matches the Phase 37 context exactly: same route family, explicit campaign identity, no hidden session assumptions.
</success_criteria>

<output>
After completion, create `.planning/phases/37-campaign-loaded-gameplay-transport/37-02-SUMMARY.md`
</output>
