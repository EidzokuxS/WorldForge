---
phase: 37-campaign-loaded-gameplay-transport
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - frontend/lib/api.ts
  - frontend/lib/__tests__/api.test.ts
  - frontend/app/game/page.tsx
  - frontend/app/game/__tests__/page.test.tsx
autonomous: true
requirements:
  - RINT-01
must_haves:
  truths:
    - "The `/game` page fetches gameplay history by explicit `campaignId` after load or reload."
    - "Submitting an action, retrying, undoing, and editing on `/game` all send the loaded `campaignId` instead of relying on hidden session state."
    - "The `/game` page still remains the canonical gameplay page for Phase 37."
  artifacts:
    - path: "frontend/lib/api.ts"
      provides: "Explicit gameplay helper signatures with query/body payloads and correct streaming-vs-JSON wrappers"
    - path: "frontend/lib/__tests__/api.test.ts"
      provides: "API helper regressions for `chatHistory`, `chatAction`, `chatRetry`, `chatUndo`, and `chatEdit`"
    - path: "frontend/app/game/page.tsx"
      provides: "Loaded-campaign gameplay wiring on `/game`"
    - path: "frontend/app/game/__tests__/page.test.tsx"
      provides: "Page regressions for remembered-campaign reload and explicit-campaign gameplay handlers"
  key_links:
    - from: "frontend/app/game/page.tsx"
      to: "frontend/lib/api.ts"
      via: "named gameplay helpers"
      pattern: "chat(History|Action|Retry|Undo|Edit)"
    - from: "frontend/lib/api.ts"
      to: "/api/chat/*"
      via: "query/body payloads"
      pattern: "/api/chat/(history|action|retry|undo|edit)"
    - from: "frontend/app/game/__tests__/page.test.tsx"
      to: "frontend/app/game/page.tsx"
      via: "explicit campaign id assertions"
      pattern: "campaignId"
---

<objective>
Wire the `/game` page and frontend gameplay helpers to the explicit campaign-addressed backend transport contract from Plan 37-01.

Purpose: complete `RINT-01` end-to-end after reload while preserving `/game` as the canonical gameplay page for this phase.
Output: dedicated frontend gameplay helpers that require `campaignId`, `/game` handlers that pass the loaded campaign id on every gameplay request, and targeted frontend regressions for both helper payloads and page wiring.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-campaign-loaded-gameplay-transport/37-CONTEXT.md
@.planning/phases/37-campaign-loaded-gameplay-transport/37-REVIEWS.md
@.planning/phases/37-campaign-loaded-gameplay-transport/37-01-PLAN.md
@frontend/lib/api.ts
@frontend/lib/__tests__/api.test.ts
@frontend/app/game/page.tsx
@frontend/app/game/__tests__/page.test.tsx

<interfaces>
From frontend/lib/api.ts:
```typescript
export async function apiGet<T>(path: string): Promise<T>;
export async function apiStreamPost(path: string, body: unknown): Promise<Response>;
export async function apiPost<T>(path: string, body?: unknown): Promise<T>;

export function chatRetry(): Promise<Response>;
export function chatUndo(): Promise<{ success: boolean; messagesRemoved: number }>;
export function chatEdit(
  messageIndex: number,
  newContent: string,
): Promise<{ success: boolean }>;
```

From frontend/app/game/page.tsx:
```typescript
type ChatHistoryResponse = {
  messages: ChatMessage[];
  premise: string;
};

const [history, world] = await Promise.all([
  apiGet<ChatHistoryResponse>("/api/chat/history"),
  getWorldData(campaign.id),
]);

const response = await apiStreamPost("/api/chat/action", {
  playerAction: actionText,
  intent: actionText,
  method: "",
});

const response = await chatRetry();
await chatUndo();
await chatEdit(messageIndex, newContent);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create explicit frontend gameplay helpers for query-based history and body-based action control</name>
  <read_first>
    .planning/phases/37-campaign-loaded-gameplay-transport/37-CONTEXT.md
    .planning/phases/37-campaign-loaded-gameplay-transport/37-REVIEWS.md
    .planning/phases/37-campaign-loaded-gameplay-transport/37-01-PLAN.md
    frontend/lib/api.ts
    frontend/lib/__tests__/api.test.ts
  </read_first>
  <files>frontend/lib/api.ts, frontend/lib/__tests__/api.test.ts</files>
  <behavior>
    - Test 1: `chatHistory(campaignId)` sends `GET /api/chat/history?campaignId=<encoded-id>`.
    - Test 2: `chatAction(campaignId, playerAction, intent, method)` and `chatRetry(campaignId)` use `apiStreamPost` and return `Promise<Response>` for SSE handling.
    - Test 3: `chatUndo(campaignId)` and `chatEdit(campaignId, messageIndex, newContent)` use `apiPost` and return parsed JSON results.
  </behavior>
  <action>Per D-08 and D-09, stop exposing a hidden-session gameplay surface from `frontend/lib/api.ts`. Add `export type ChatHistoryResponse = { messages: ChatMessage[]; premise: string }` to `frontend/lib/api.ts` so `/game` can import the shared response type instead of redefining it locally. Add `chatHistory(campaignId: string): Promise<ChatHistoryResponse>` that calls `apiGet<ChatHistoryResponse>(\`/api/chat/history?campaignId=\${encodeURIComponent(campaignId)}\`)`. Add `chatAction(campaignId: string, playerAction: string, intent: string, method: string): Promise<Response>` that calls `apiStreamPost("/api/chat/action", { campaignId, playerAction, intent, method })`. Change `chatRetry` to `chatRetry(campaignId: string): Promise<Response>` and have it call `apiStreamPost("/api/chat/retry", { campaignId })`. Change `chatUndo` to `chatUndo(campaignId: string): Promise<{ success: boolean; messagesRemoved: number }>` and have it call `apiPost("/api/chat/undo", { campaignId })`. Change `chatEdit` to `chatEdit(campaignId: string, messageIndex: number, newContent: string): Promise<{ success: boolean }>` and have it call `apiPost("/api/chat/edit", { campaignId, messageIndex, newContent })`. Keep the generic `apiGet`, `apiStreamPost`, and `apiPost` utilities exported, but make the gameplay contract explicit through these named helpers. Extend `frontend/lib/__tests__/api.test.ts` to assert the exact fetch URL/payload for all five helpers, and call out the streaming-vs-JSON distinction explicitly in test names or assertions: `chatAction` and `chatRetry` are streaming helpers, while `chatUndo` and `chatEdit` are JSON helpers.</action>
  <acceptance_criteria>
    - `frontend/lib/api.ts` exports `ChatHistoryResponse`, `chatHistory`, and `chatAction`.
    - `frontend/lib/api.ts` contains the exact strings `/api/chat/history?campaignId=${encodeURIComponent(campaignId)}`, `apiStreamPost("/api/chat/action", { campaignId, playerAction, intent, method })`, and `apiStreamPost("/api/chat/retry", { campaignId })`.
    - `frontend/lib/api.ts` contains `apiPost("/api/chat/undo", { campaignId })` and `apiPost("/api/chat/edit", { campaignId, messageIndex, newContent })`.
    - `frontend/lib/__tests__/api.test.ts` contains helper-specific assertions for `chatHistory`, `chatAction`, `chatRetry`, `chatUndo`, and `chatEdit`.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend exec vitest run "lib/__tests__/api.test.ts"</automated>
  </verify>
  <done>The frontend gameplay helper surface is explicit and typed: history uses a query param, action/retry remain streaming helpers, undo/edit remain JSON helpers, and every targeted helper requires `campaignId`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire `/game` to call the explicit campaign helpers after active-or-remembered bootstrap</name>
  <read_first>
    .planning/phases/37-campaign-loaded-gameplay-transport/37-CONTEXT.md
    .planning/phases/37-campaign-loaded-gameplay-transport/37-REVIEWS.md
    .planning/phases/37-campaign-loaded-gameplay-transport/37-01-PLAN.md
    frontend/app/game/page.tsx
    frontend/app/game/__tests__/page.test.tsx
    frontend/lib/api.ts
  </read_first>
  <files>frontend/app/game/page.tsx, frontend/app/game/__tests__/page.test.tsx, frontend/lib/api.ts</files>
  <behavior>
    - Test 1: when `getActiveCampaign()` returns `null` but `getRememberedCampaignId()` returns a valid id, `/game` calls `loadCampaign(rememberedId)`, then `chatHistory(rememberedId)`, then `getWorldData(rememberedId)`.
    - Test 2: `submitAction`, `handleRetry`, `handleUndo`, and `handleEdit` each pass `activeCampaign.id` into the named gameplay helpers instead of calling generic transport utilities directly.
    - Test 3: `/game` remains the canonical page; bootstrap still uses active-campaign-first with remembered-campaign fallback before any gameplay request fires.
  </behavior>
  <action>Per D-07, D-08, and D-09, keep `/game` as the canonical page and preserve the current bootstrap order: `getActiveCampaign()` first, then `getRememberedCampaignId()`, then `loadCampaign(rememberedCampaignId)` when needed. In `frontend/app/game/page.tsx`, replace the local `ChatHistoryResponse` type with the exported type from `@/lib/api`. Replace the direct `apiGet("/api/chat/history")` call in `initGame()` with `chatHistory(campaign.id)`. Replace the direct `apiStreamPost("/api/chat/action", { playerAction, intent, method })` call in `submitAction()` with `chatAction(activeCampaign.id, actionText, actionText, "")`, and add an early return if `activeCampaign` is missing. Update `handleRetry()` to call `chatRetry(activeCampaign.id)`, `handleUndo()` to call `chatUndo(activeCampaign.id)`, and `handleEdit()` to call `chatEdit(activeCampaign.id, messageIndex, newContent)`, each with an early return when `activeCampaign` is unavailable. Keep `getWorldData(campaign.id)` unchanged. Remove gameplay-page use of generic `apiGet` / `apiStreamPost` for the targeted chat routes; they should remain only in the shared helper layer. Extend `frontend/app/game/__tests__/page.test.tsx` so the mocked `@/lib/api` module exposes `chatHistory`, `chatAction`, `chatRetry`, `chatUndo`, `chatEdit`, `getRememberedCampaignId`, and `loadCampaign`. Add one reload-path test proving remembered-campaign bootstrap reaches `chatHistory(campaign.id)`, and add one handler-path test using a stubbed child component button or callback so the test fails unless `chatAction` or `chatRetry` receives the explicit `fakeCampaign.id`.</action>
  <acceptance_criteria>
    - `frontend/app/game/page.tsx` imports `chatHistory` and `chatAction`, and no longer calls `apiGet<ChatHistoryResponse>("/api/chat/history")` or `apiStreamPost("/api/chat/action", ...)`.
    - `frontend/app/game/page.tsx` contains `await chatHistory(campaign.id)`, `await chatAction(activeCampaign.id, actionText, actionText, "")`, `await chatRetry(activeCampaign.id)`, `await chatUndo(activeCampaign.id)`, and `await chatEdit(activeCampaign.id, messageIndex, newContent)`.
    - `frontend/app/game/__tests__/page.test.tsx` mocks `getRememberedCampaignId` and `loadCampaign`, includes a remembered-campaign bootstrap assertion, and includes at least one explicit helper-call assertion for `chatAction` or `chatRetry`.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/game/__tests__/page.test.tsx" "lib/__tests__/api.test.ts"</automated>
  </verify>
  <done>The `/game` page can reload, resolve a campaign, and issue all targeted gameplay requests against that explicit campaign identity without relying on hidden backend session state.</done>
</task>

</tasks>

<verification>
- Frontend targeted tests pass for updated API helpers and `/game` page wiring.
- Reading `frontend/lib/api.ts` shows dedicated `chatHistory(campaignId)` and `chatAction(campaignId, ...)` helpers, plus explicit streaming-vs-JSON distinctions for the remaining helpers.
- Reading `frontend/app/game/page.tsx` shows the page still bootstraps active-or-remembered campaign state, but every targeted gameplay transport call uses `activeCampaign.id` explicitly.
</verification>

<success_criteria>
- The frontend issues no targeted gameplay request without an explicit `campaignId` after initialization.
- `/game` remains the canonical gameplay route in Phase 37, with no `/campaign/[id]/game` expansion.
- The frontend/backend transport seam now matches the Phase 37 contract exactly: `chatHistory(campaignId)` for query-param history, `chatAction(campaignId, ...)` / `chatRetry(campaignId)` for streaming requests, and `chatUndo(campaignId)` / `chatEdit(campaignId, ...)` for JSON requests.
</success_criteria>

<output>
After completion, create `.planning/phases/37-campaign-loaded-gameplay-transport/37-02-SUMMARY.md`
</output>
