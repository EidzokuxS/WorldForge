---
phase: 37-campaign-loaded-gameplay-transport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/chat.ts
  - backend/src/routes/schemas.ts
  - backend/src/routes/__tests__/chat.test.ts
autonomous: true
requirements:
  - RINT-01
must_haves:
  truths:
    - "GET /api/chat/history returns the loaded campaign history after reload when the request names `campaignId` explicitly in the query string."
    - "POST /api/chat/action, /api/chat/retry, /api/chat/undo, and /api/chat/edit resolve against the requested campaign instead of requiring a pre-active in-memory session."
    - "Retry and undo snapshot state cannot cross campaigns inside one backend process because snapshots are keyed by `campaignId`."
  artifacts:
    - path: "backend/src/routes/chat.ts"
      provides: "Campaign-addressed gameplay transport, async history loading, and campaign-scoped snapshot state"
    - path: "backend/src/routes/schemas.ts"
      provides: "Explicit `campaignId` validation for targeted Phase 37 gameplay routes"
    - path: "backend/src/routes/__tests__/chat.test.ts"
      provides: "Backend regressions for explicit-campaign gameplay transport"
  key_links:
    - from: "backend/src/routes/chat.ts"
      to: "backend/src/routes/schemas.ts"
      via: "query/body validation"
      pattern: "chatHistoryQuerySchema.safeParse\\(c.req.query\\(\\)\\)"
    - from: "backend/src/routes/chat.ts"
      to: "backend/src/routes/helpers.ts"
      via: "loaded campaign resolver"
      pattern: "await requireLoadedCampaign\\(c, campaignId\\)"
    - from: "backend/src/routes/chat.ts"
      to: "backend/src/routes/chat.ts"
      via: "campaign-scoped snapshot map"
      pattern: "lastTurnSnapshots\\.(get|set|delete)\\(campaignId"
---

<objective>
Make the live `/api/chat/*` gameplay transport campaign-addressable on the backend so reload no longer depends on an already-active in-memory session.

Purpose: satisfy `RINT-01` at the transport seam identified by Phase 36 Group A2 without renaming routes, widening into checkpoint/rollback fidelity, or removing the remaining legacy `POST /api/chat` path.
Output: explicit request validation, `requireLoadedCampaign()` authority for the targeted gameplay routes, an async `GET /api/chat/history` handler, and campaign-scoped in-memory last-turn snapshot state.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-campaign-loaded-gameplay-transport/37-CONTEXT.md
@.planning/phases/37-campaign-loaded-gameplay-transport/37-REVIEWS.md
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-HANDOFF.md
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-RUNTIME-MATRIX.md
@backend/src/routes/chat.ts
@backend/src/routes/helpers.ts
@backend/src/routes/schemas.ts
@backend/src/routes/__tests__/chat.test.ts

<interfaces>
From backend/src/routes/helpers.ts:
```typescript
export async function requireLoadedCampaign(
  c: Context,
  campaignId: string,
): Promise<CampaignMeta | Response>;
```

From backend/src/routes/schemas.ts:
```typescript
export const chatActionBodySchema = z.object({
  playerAction: z.string().min(1).max(2000),
  intent: z.string().min(1).max(2000),
  method: z.string().max(500).default(""),
});

export const chatEditBodySchema = z.object({
  messageIndex: z.number().int().min(0),
  newContent: z.string().min(1),
});
```

From backend/src/routes/chat.ts:
```typescript
let lastTurnSnapshot: TurnSnapshot | null = null;

app.get("/history", (c) => { ... });
app.post("/action", async (c) => { ... });
app.post("/retry", async (c) => { ... });
app.post("/undo", async (c) => { ... });
app.post("/edit", async (c) => { ... });
app.post("/", async (c) => { ... }); // legacy plain-text route
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit `campaignId` validation for every targeted gameplay route</name>
  <read_first>
    .planning/phases/37-campaign-loaded-gameplay-transport/37-CONTEXT.md
    .planning/phases/37-campaign-loaded-gameplay-transport/37-REVIEWS.md
    backend/src/routes/schemas.ts
    backend/src/routes/chat.ts
    backend/src/routes/__tests__/chat.test.ts
  </read_first>
  <files>backend/src/routes/schemas.ts, backend/src/routes/chat.ts, backend/src/routes/__tests__/chat.test.ts</files>
  <behavior>
    - Test 1: `GET /chat/history` rejects a missing `campaignId` query string and accepts `/chat/history?campaignId=campaign-42`.
    - Test 2: `POST /chat/action`, `POST /chat/retry`, `POST /chat/undo`, and `POST /chat/edit` reject request bodies that omit `campaignId`.
    - Test 3: route tests pin the Phase 37 request contract so the targeted gameplay routes cannot regress back to hidden session-only identity.
  </behavior>
  <action>Per D-01, D-02, and D-03, keep the endpoint family under `/api/chat/*` and make campaign identity explicit on every targeted gameplay route. In `backend/src/routes/schemas.ts`, introduce a reusable trimmed `campaignId` string validator with the exact validation message `campaignId is required.`. Export `chatHistoryQuerySchema = z.object({ campaignId: campaignIdSchema })`, extend `chatActionBodySchema` so it requires `campaignId`, export `chatRetryBodySchema = z.object({ campaignId: campaignIdSchema })`, export `chatUndoBodySchema = z.object({ campaignId: campaignIdSchema })`, and extend `chatEditBodySchema` so it requires `campaignId` alongside `messageIndex` and `newContent`. In `backend/src/routes/chat.ts`, validate `GET /history` with `chatHistoryQuerySchema.safeParse(c.req.query())` so history is explicitly query-param based (`/api/chat/history?campaignId=<id>`), and validate `retry` / `undo` with `parseBody(c, chatRetryBodySchema)` and `parseBody(c, chatUndoBodySchema)` before any route logic runs. Extend `backend/src/routes/__tests__/chat.test.ts` to assert 400 responses for missing `campaignId` on `history`, `action`, `retry`, `undo`, and `edit`, plus one 200 success case for `GET /chat/history?campaignId=campaign-42`. Address the review concern directly: do not use a GET body for history.</action>
  <acceptance_criteria>
    - `backend/src/routes/schemas.ts` exports `chatHistoryQuerySchema`, `chatRetryBodySchema`, and `chatUndoBodySchema`.
    - `backend/src/routes/chat.ts` contains `chatHistoryQuerySchema.safeParse(c.req.query())`, `parseBody(c, chatRetryBodySchema)`, and `parseBody(c, chatUndoBodySchema)`.
    - `backend/src/routes/__tests__/chat.test.ts` includes requests to `/chat/history?campaignId=${CAMPAIGN_ID}` and missing-`campaignId` assertions for `action`, `retry`, `undo`, and `edit`.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix backend exec vitest run "src/routes/__tests__/chat.test.ts"</automated>
  </verify>
  <done>The backend gameplay transport contract is explicit: history reads `campaignId` from the query string, the targeted POST routes require `campaignId` in JSON, and tests lock that contract in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve targeted gameplay routes through `requireLoadedCampaign()` and isolate snapshots by `campaignId`</name>
  <read_first>
    .planning/phases/37-campaign-loaded-gameplay-transport/37-CONTEXT.md
    .planning/phases/37-campaign-loaded-gameplay-transport/37-REVIEWS.md
    backend/src/routes/chat.ts
    backend/src/routes/helpers.ts
    backend/src/campaign/manager.ts
    backend/src/routes/__tests__/chat.test.ts
  </read_first>
  <files>backend/src/routes/chat.ts, backend/src/routes/__tests__/chat.test.ts</files>
  <behavior>
    - Test 1: `GET /chat/history?campaignId=campaign-42` succeeds when no campaign is already active but `loadCampaign("campaign-42")` can resolve it.
    - Test 2: `POST /chat/action`, `POST /chat/retry`, `POST /chat/undo`, and `POST /chat/edit` await `requireLoadedCampaign(c, campaignId)` instead of using `getActiveCampaign()` as their authority.
    - Test 3: retry/undo read and write snapshot state through a `Map<string, TurnSnapshot>` keyed by `campaignId`, so one campaign cannot consume another campaign’s snapshot.
  </behavior>
  <action>Per D-04, D-05, D-06, and D-11, keep `requireLoadedCampaign()` as the authoritative resolver for the targeted gameplay routes only, while leaving the legacy plain-text `app.post("/")` route untouched on `getActiveCampaign()`. In `backend/src/routes/chat.ts`, make `app.get("/history", async (c) => { ... })` and read `campaignId` from the parsed query object. For `history`, `action`, `retry`, `undo`, and `edit`, use the exact pattern `const campaign = await requireLoadedCampaign(c, campaignId); if (campaign instanceof Response) return campaign;` before route work begins. Preserve the `getActiveCampaign` import and the current `app.post("/")` logic because that legacy route still depends on it for compile/runtime health. Replace `let lastTurnSnapshot: TurnSnapshot | null = null;` with `const lastTurnSnapshots = new Map<string, TurnSnapshot>();`. On action success call `lastTurnSnapshots.set(campaignId, snapshot)`. On retry load `const previousSnapshot = lastTurnSnapshots.get(campaignId)`, return `c.json({ error: "Nothing to retry." }, 400)` when missing, restore that snapshot, and overwrite the map with the fresh rerun snapshot. On undo load `const previousSnapshot = lastTurnSnapshots.get(campaignId)`, return `c.json({ error: "Nothing to undo." }, 400)` when missing, restore it, pop the last two chat messages, and then call `lastTurnSnapshots.delete(campaignId)`. Extend `backend/src/routes/__tests__/chat.test.ts` to mock `loadCampaign` from `../../campaign/index.js`, prove history works when `getActiveCampaign()` returns `null` but `loadCampaign(campaignId)` succeeds, and add at least one regression that would fail if the code accidentally reused a single `lastTurnSnapshot` across campaigns. Address the review concern directly: history becomes async in this task because `requireLoadedCampaign()` is async.</action>
  <acceptance_criteria>
    - `backend/src/routes/chat.ts` declares `app.get("/history", async (c) =>` and awaits `requireLoadedCampaign(c, campaignId)` inside `history`, `action`, `retry`, `undo`, and `edit`.
    - `backend/src/routes/chat.ts` declares `const lastTurnSnapshots = new Map<string, TurnSnapshot>()` and uses `.get(campaignId)`, `.set(campaignId, ...)`, and `.delete(campaignId)`.
    - `backend/src/routes/chat.ts` still contains the legacy plain-text handler with `app.post("/", async (c) =>` and `const activeCampaign = getActiveCampaign();`.
    - `backend/src/routes/__tests__/chat.test.ts` mocks `loadCampaign` and includes a no-active-session history success assertion driven by explicit `campaignId`.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix backend exec vitest run "src/routes/__tests__/chat.test.ts"</automated>
  </verify>
  <done>The targeted backend gameplay routes now resolve the requested loaded campaign explicitly, history is async and query-param based, snapshots are isolated per campaign, and the legacy `POST /api/chat` route remains intact.</done>
</task>

</tasks>

<verification>
- Backend targeted regression tests pass for the explicit `campaignId` contract and the loaded-campaign resolver path.
- Reading `backend/src/routes/chat.ts` shows `GET /history` is async and query-param based, while `POST /` still uses `getActiveCampaign()`.
- Reading `backend/src/routes/chat.ts` shows snapshot state is stored in a `Map<string, TurnSnapshot>` keyed by campaign id rather than a single process-global singleton.
</verification>

<success_criteria>
- A reloaded campaign can hit `history`, `action`, `retry`, `undo`, and `edit` by explicit `campaignId` without a hidden active-session precondition.
- The backend transport seam stays inside `/api/chat/*`, keeps the legacy plain-text `POST /api/chat` path healthy, and does not widen into checkpoint fidelity or singleton-removal cleanup.
- Plan 37-02 receives a precise backend contract: `GET /api/chat/history?campaignId=<id>`, targeted POST bodies carrying `campaignId`, and campaign-scoped snapshot state.
</success_criteria>

<output>
After completion, create `.planning/phases/37-campaign-loaded-gameplay-transport/37-01-SUMMARY.md`
</output>
