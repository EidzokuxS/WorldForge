---
phase: 37-campaign-loaded-gameplay-transport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/chat.ts
  - backend/src/routes/schemas.ts
  - backend/src/routes/helpers.ts
  - backend/src/routes/__tests__/chat.test.ts
autonomous: true
requirements:
  - RINT-01
must_haves:
  truths:
    - "GET /api/chat/history returns the loaded campaign history after reload when the request names the campaign explicitly."
    - "POST /api/chat/action, /api/chat/retry, /api/chat/undo, and /api/chat/edit resolve against the requested campaign instead of requiring a pre-active in-memory session."
    - "Retry and undo snapshot state cannot cross campaigns inside one backend process because snapshots are keyed by campaignId."
  artifacts:
    - path: "backend/src/routes/chat.ts"
      provides: "Campaign-addressed gameplay transport and campaign-scoped snapshot state"
    - path: "backend/src/routes/schemas.ts"
      provides: "Explicit campaignId validation for Phase 37 gameplay routes"
    - path: "backend/src/routes/helpers.ts"
      provides: "requireLoadedCampaign resolver used by gameplay routes"
    - path: "backend/src/routes/__tests__/chat.test.ts"
      provides: "Route regressions for explicit campaign gameplay transport"
  key_links:
    - from: "backend/src/routes/chat.ts"
      to: "backend/src/routes/helpers.ts"
      via: "requireLoadedCampaign"
      pattern: "requireLoadedCampaign"
    - from: "backend/src/routes/chat.ts"
      to: "backend/src/routes/schemas.ts"
      via: "parseBody\\(c, .*Schema\\)"
      pattern: "campaignId"
    - from: "backend/src/routes/chat.ts"
      to: "backend/src/routes/__tests__/chat.test.ts"
      via: "route contract assertions"
      pattern: "campaignId"
---

<objective>
Make the live `/api/chat/*` gameplay transport campaign-addressable on the backend so reload no longer depends on an already-active in-memory session.

Purpose: satisfy `RINT-01` at the transport seam identified by Phase 36 Group A2 without redesigning routes or widening into rollback/checkpoint fidelity.
Output: explicit request validation, `requireLoadedCampaign()` authority for the targeted gameplay routes, and campaign-scoped in-memory last-turn snapshot state.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-campaign-loaded-gameplay-transport/37-CONTEXT.md
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-HANDOFF.md
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-RUNTIME-MATRIX.md
@backend/src/routes/chat.ts
@backend/src/routes/helpers.ts
@backend/src/routes/schemas.ts
@backend/src/routes/__tests__/chat.test.ts

<interfaces>
From backend/src/routes/helpers.ts:
```typescript
export function requireActiveCampaign(
  c: Context,
  campaignId: string
): CampaignMeta | Response;

export async function requireLoadedCampaign(
  c: Context,
  campaignId: string,
): Promise<CampaignMeta | Response>;
```

From backend/src/routes/schemas.ts:
```typescript
export const chatActionBodySchema = z.object({
  playerAction: z.string().min(1).max(2000),
  intent: z.string().min(1).max(2000),
  method: z.string().max(500).default(""),
});

export const chatEditBodySchema = z.object({
  messageIndex: z.number().int().min(0),
  newContent: z.string().min(1),
});
```

From backend/src/routes/chat.ts:
```typescript
let lastTurnSnapshot: TurnSnapshot | null = null;

app.get("/history", (c) => { ... });
app.post("/action", async (c) => { ... });
app.post("/retry", async (c) => { ... });
app.post("/undo", async (c) => { ... });
app.post("/edit", async (c) => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten gameplay route contracts around explicit campaign identity</name>
  <read_first>
    .planning/phases/37-campaign-loaded-gameplay-transport/37-CONTEXT.md
    .planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-HANDOFF.md
    backend/src/routes/schemas.ts
    backend/src/routes/chat.ts
    backend/src/routes/__tests__/chat.test.ts
  </read_first>
  <files>backend/src/routes/schemas.ts, backend/src/routes/chat.ts, backend/src/routes/__tests__/chat.test.ts</files>
  <behavior>
    - Test 1: `GET /chat/history` rejects missing `campaignId` and returns campaign history for the named campaign.
    - Test 2: `POST /chat/action`, `POST /chat/retry`, `POST /chat/undo`, and `POST /chat/edit` reject missing `campaignId`.
    - Test 3: gameplay route tests prove the chat transport no longer assumes an already active session for request identity.
  </behavior>
  <action>Per D-01, D-02, and D-03, add explicit Phase 37 request schemas for gameplay transport without renaming the `/api/chat/*` family. `history` must accept an explicit `campaignId` request parameter in a testable form, and `action`, `retry`, `undo`, and `edit` must require `campaignId` in the request body. Update `backend/src/routes/chat.ts` to parse those schemas before route work begins and remove the current silent session-only contract. Extend `backend/src/routes/__tests__/chat.test.ts` to cover the new request validation and the success path for explicit campaign-addressed history. Do not widen this task into the legacy plain-text `POST /api/chat` route beyond compile/test health (D-11).</action>
  <acceptance_criteria>
    - `backend/src/routes/schemas.ts` contains dedicated gameplay route schemas that require `campaignId` for Phase 37 targeted routes.
    - `backend/src/routes/chat.ts` reads explicit `campaignId` for `history`, `action`, `retry`, `undo`, and `edit`.
    - `backend/src/routes/__tests__/chat.test.ts` contains failing-then-passing assertions for missing-`campaignId` rejection and explicit history success.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix backend exec vitest run "src/routes/__tests__/chat.test.ts"</automated>
  </verify>
  <done>Gameplay transport has an explicit campaign-addressed contract on the backend, and regression tests lock the request shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move gameplay route authority to requireLoadedCampaign and scope snapshots by campaign</name>
  <read_first>
    .planning/phases/37-campaign-loaded-gameplay-transport/37-CONTEXT.md
    backend/src/routes/chat.ts
    backend/src/routes/helpers.ts
    backend/src/campaign/manager.ts
    backend/src/routes/__tests__/chat.test.ts
  </read_first>
  <files>backend/src/routes/chat.ts, backend/src/routes/helpers.ts, backend/src/routes/__tests__/chat.test.ts</files>
  <behavior>
    - Test 1: targeted gameplay routes resolve through `requireLoadedCampaign()` semantics instead of `getActiveCampaign()` authority.
    - Test 2: retry/undo/edit operate on snapshot state stored under the requested `campaignId`, not one process-global singleton.
    - Test 3: action/retry auto-checkpoint and post-turn hooks continue using the resolved campaign id after the resolver change.
  </behavior>
  <action>Per D-04, D-05, and D-06, replace `getActiveCampaign()` as the authoritative gameplay resolver in `backend/src/routes/chat.ts` for `history`, `action`, `retry`, `undo`, and `edit` with `requireLoadedCampaign()` semantics. Keep `loadCampaign(campaignId)` side effects acceptable for this phase, but route correctness must no longer depend on a preloaded active session. Replace the single `lastTurnSnapshot` binding with campaign-scoped in-memory snapshot storage keyed by `campaignId`, then thread reads/writes for action, retry, and undo through that map so one loaded campaign cannot consume another campaign’s snapshot. Extend `backend/src/routes/__tests__/chat.test.ts` for the new resolver/snapshot behavior or create the minimal helper coverage inside the same test file if route mocking is simpler. Do not change checkpoint persistence, rollback breadth, or turn-boundary semantics here (D-10).</action>
  <acceptance_criteria>
    - `backend/src/routes/chat.ts` uses `requireLoadedCampaign()` for the targeted gameplay routes named in D-04.
    - No module-level `TurnSnapshot | null` singleton remains for gameplay transport; snapshot state is keyed by `campaignId`.
    - Route tests cover explicit-campaign retry/undo/edit expectations strongly enough that a session-only regression would fail.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix backend exec vitest run "src/routes/__tests__/chat.test.ts"</automated>
  </verify>
  <done>Gameplay backend transport resolves against the requested loaded campaign and keeps in-memory rollback snapshot state isolated per campaign.</done>
</task>

</tasks>

<verification>
- Backend targeted regression suite passes for the updated gameplay route contract.
- Manual spot-check by reading `backend/src/routes/chat.ts` shows no targeted gameplay route still relies on `getActiveCampaign()` for authority.
- Manual spot-check by reading `backend/src/routes/chat.ts` shows snapshot storage is campaign-scoped, not one singleton.
</verification>

<success_criteria>
- A reloaded campaign can hit `history`, `action`, `retry`, `undo`, and `edit` by explicit `campaignId` without a hidden active-session precondition.
- The backend transport seam remains inside `/api/chat/*` and does not expand into route-tree redesign or checkpoint-fidelity work.
- The executor can hand the frontend plan a stable backend contract: explicit `campaignId` everywhere and campaign-scoped snapshot state.
</success_criteria>

<output>
After completion, create `.planning/phases/37-campaign-loaded-gameplay-transport/37-01-SUMMARY.md`
</output>
