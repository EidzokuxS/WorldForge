---
phase: 32-desktop-first-non-game-ui-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - 32-00
  - 32-01
files_modified:
  - frontend/app/page.tsx
  - frontend/app/(non-game)/page.tsx
  - frontend/app/(non-game)/__tests__/page.test.tsx
  - frontend/app/(non-game)/campaign/new/layout.tsx
  - frontend/app/(non-game)/campaign/new/page.tsx
  - frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx
  - frontend/app/(non-game)/campaign/new/dna/page.tsx
  - frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx
  - frontend/components/campaign-new/flow-provider.tsx
  - frontend/components/campaign-new/concept-workspace.tsx
  - frontend/components/campaign-new/dna-workspace.tsx
  - frontend/components/title/use-new-campaign-wizard.ts
  - frontend/components/title/new-campaign-dialog.tsx
autonomous: true
requirements:
  - P32-02
  - P32-06
must_haves:
  truths:
    - "The launcher sends users into routed campaign creation instead of opening a modal-only wizard."
    - "Concept, source-selection, IP/divergence, and DNA state persist between `/campaign/new` and `/campaign/new/dna`."
    - "Campaign creation still reaches the canonical next route after world generation without flattening Phase 29/30 seams."
  artifacts:
    - path: "frontend/app/(non-game)/page.tsx"
      provides: "Launcher workspace in the shared shell"
    - path: "frontend/app/(non-game)/campaign/new/layout.tsx"
      provides: "Route-level state preservation for creation flow"
    - path: "frontend/app/(non-game)/campaign/new/page.tsx"
      provides: "Concept and source-selection workspace"
    - path: "frontend/app/(non-game)/campaign/new/dna/page.tsx"
      provides: "DNA editing workspace"
    - path: "frontend/components/campaign-new/flow-provider.tsx"
      provides: "Shared creation-flow state across routed steps"
    - path: "frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx"
      provides: "Regression coverage for routed concept flow"
  key_links:
    - from: "frontend/app/(non-game)/campaign/new/layout.tsx"
      to: "frontend/components/campaign-new/flow-provider.tsx"
      via: "shared creation-state provider"
      pattern: "CampaignNewFlowProvider|FlowProvider"
    - from: "frontend/components/campaign-new/flow-provider.tsx"
      to: "frontend/components/title/use-new-campaign-wizard.ts"
      via: "reused campaign creation/generation actions"
      pattern: "useNewCampaignWizard"
    - from: "frontend/app/(non-game)/page.tsx"
      to: "frontend/app/(non-game)/campaign/new/page.tsx"
      via: "launcher entry link"
      pattern: "/campaign/new"
---

<objective>
Replace modal campaign creation with routed launcher and creation workspaces that preserve wizard state across steps.

Purpose: remove the most obvious workflow bottleneck in the current UI without changing the backend campaign/world-generation seams.
Output: routed `/campaign/new` flow, shell-owned launcher at `/`, and test coverage that protects state continuity through concept and DNA routes.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-desktop-first-non-game-ui-overhaul/32-RESEARCH.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-desktop-ui-workspace-spec.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-phase-32-33-handoff.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-04-SUMMARY.md
@frontend/app/page.tsx
@frontend/components/title/new-campaign-dialog.tsx
@frontend/components/title/use-new-campaign-wizard.ts
@frontend/lib/api.ts

<interfaces>
From `frontend/components/title/use-new-campaign-wizard.ts`:
```typescript
const [dnaState, setDnaState] = useState<DnaState | null>(null);
const [ipContext, setIpContext] = useState<IpContext | null>(null);
const [premiseDivergence, setPremiseDivergence] = useState<PremiseDivergence | null>(null);
const [selectedWorldbooks, setSelectedWorldbooks] = useState<WorldbookLibraryItem[]>([]);
```

From `frontend/lib/api.ts`:
```typescript
export function suggestSeeds(
  premise: string,
  opts?: {
    name?: string;
    franchise?: string;
    research?: boolean;
    selectedWorldbooks?: CampaignWorldbookSelection[];
  }
): Promise<WorldSeeds & { _ipContext?: IpContext | null; _premiseDivergence?: PremiseDivergence | null }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write routed creation and launcher regression tests first</name>
  <files>frontend/app/(non-game)/__tests__/page.test.tsx, frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx, frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx, frontend/components/campaign-new/flow-provider.tsx</files>
  <behavior>
    - Test 1: the shell-owned launcher exposes a routed "new campaign" entry instead of opening the old modal as the primary path.
    - Test 2: `/campaign/new` and `/campaign/new/dna` preserve concept, worldbook selection, `ipContext`, `premiseDivergence`, and DNA edits across route transitions.
    - Test 3: the routed flow can still surface generation progress and canonical next-route intent after create/generate completes.
  </behavior>
  <action>Create the routed creation regression harness before moving UI. Add a route-level `flow-provider` mounted from `frontend/app/(non-game)/campaign/new/layout.tsx` and test it against the exact continuity risks called out in `32-RESEARCH.md`. Treat the current `useNewCampaignWizard` logic as the source of truth for generation actions and campaign payload shaping; do not introduce query-string state, a new global store, or a second creation state machine.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/(non-game)/__tests__/page.test.tsx" "app/(non-game)/campaign/new/__tests__/page.test.tsx" "app/(non-game)/campaign/new/dna/__tests__/page.test.tsx"</automated>
  </verify>
  <done>The routed launcher/creation tests fail until the new creation pages and state-preserving layout exist, and the flow-provider contract is explicit instead of implicit dialog state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement routed concept and DNA workspaces on the existing wizard seam</name>
  <files>frontend/app/(non-game)/campaign/new/layout.tsx, frontend/app/(non-game)/campaign/new/page.tsx, frontend/app/(non-game)/campaign/new/dna/page.tsx, frontend/components/campaign-new/concept-workspace.tsx, frontend/components/campaign-new/dna-workspace.tsx, frontend/components/title/use-new-campaign-wizard.ts</files>
  <behavior>
    - Test 1: the concept workspace shows campaign name/premise, research toggle, and selected-source context in-shell.
    - Test 2: the DNA workspace reuses the current suggestion/edit/create behavior without losing custom values or generation progress wiring.
    - Test 3: creation payloads still include `worldbookSelection`, `ipContext`, and `premiseDivergence` when present.
  </behavior>
  <action>Build `frontend/app/(non-game)/campaign/new/page.tsx` and `frontend/app/(non-game)/campaign/new/dna/page.tsx` inside the shared shell, backed by `flow-provider.tsx` and the current wizard actions. Reuse the existing seed suggestion, worldbook-library selection, and generation-progress behavior instead of rewriting the business logic. The concept workspace should own source/research state; the DNA workspace should own dense editing and create actions. Keep everything Tailwind/shadcn-only and preserve the post-create redirect into canonical campaign routes.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/(non-game)/campaign/new/__tests__/page.test.tsx" "app/(non-game)/campaign/new/dna/__tests__/page.test.tsx"</automated>
  </verify>
  <done>Routed concept and DNA pages exist, keep flow state alive through the route-group layout, and continue to drive the same backend creation/generation seams used by the modal wizard.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the launcher to the shell and retire modal-first creation</name>
  <files>frontend/app/page.tsx, frontend/app/(non-game)/page.tsx, frontend/components/title/new-campaign-dialog.tsx</files>
  <action>Move the `/` launcher into the shared shell by replacing `frontend/app/page.tsx` with `frontend/app/(non-game)/page.tsx`. Redesign it into a desktop launcher workspace with recent/resume actions, routed entry into `/campaign/new`, and clear links toward settings/library per D-01 and D-02. Remove `NewCampaignDialog` from the primary path; if any of its substructure remains useful, demote it to internal helper status rather than keeping modal-first architecture alive.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/(non-game)/__tests__/page.test.tsx" "app/(non-game)/campaign/new/__tests__/page.test.tsx" "app/(non-game)/campaign/new/dna/__tests__/page.test.tsx"</automated>
  </verify>
  <done>The canonical launcher is shell-owned, "New Campaign" enters the routed flow, and modal-first creation is no longer the primary authored UX.</done>
</task>

</tasks>

<verification>
Phase 32 creation routing is ready when `/` launches into `/campaign/new`, state survives `/campaign/new` -> `/campaign/new/dna`, and the routed flow still reaches canonical campaign review/character destinations.
</verification>

<success_criteria>
Campaign creation is no longer dialog-bound, the routed creation steps preserve all current Phase 26/30 context artifacts, and launcher behavior is protected by focused page tests.
</success_criteria>

<output>
After completion, create `.planning/phases/32-desktop-first-non-game-ui-overhaul/32-02-SUMMARY.md`
</output>
