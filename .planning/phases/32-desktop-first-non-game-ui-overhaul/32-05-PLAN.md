---
phase: 32-desktop-first-non-game-ui-overhaul
plan: 05
type: execute
wave: 3
depends_on:
  - 32-01
files_modified:
  - frontend/app/campaign/[id]/character/page.tsx
  - frontend/app/(non-game)/campaign/[id]/character/page.tsx
  - frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx
  - frontend/app/character-creation/page.tsx
  - frontend/app/character-creation/__tests__/page.test.tsx
  - frontend/app/game/__tests__/page.test.tsx
  - frontend/components/character-creation/character-workspace.tsx
  - frontend/components/character-creation/character-card.tsx
  - frontend/components/character-creation/character-form.tsx
  - frontend/components/character-creation/__tests__/character-workspace.test.tsx
autonomous: true
requirements:
  - P32-04
  - P32-06
must_haves:
  truths:
    - "Character creation becomes a desktop authoring workspace with clear input methods, structured draft editing, live summary context, and sticky save/begin actions."
    - "Persona templates, start-condition resolution, and canonical loadout preview remain first-class parts of the canonical draft workflow."
    - "Legacy `/character-creation` stops being a second authored page, and `/game` remains structurally separate except for the final handoff."
  artifacts:
    - path: "frontend/app/(non-game)/campaign/[id]/character/page.tsx"
      provides: "Shell-owned canonical character authoring route"
    - path: "frontend/components/character-creation/character-workspace.tsx"
      provides: "Desktop workspace container for input modes, editor, summary rail, and sticky actions"
    - path: "frontend/components/character-creation/character-card.tsx"
      provides: "Canonical structured draft editor inside the new workspace"
    - path: "frontend/app/character-creation/page.tsx"
      provides: "Redirect-only legacy compatibility stub"
    - path: "frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx"
      provides: "Regression coverage for canonical character workspace"
    - path: "frontend/app/game/__tests__/page.test.tsx"
      provides: "Smoke coverage that `/game` stays outside the non-game shell"
  key_links:
    - from: "frontend/app/(non-game)/campaign/[id]/character/page.tsx"
      to: "frontend/components/character-creation/character-workspace.tsx"
      via: "shell-owned page composition"
      pattern: "character-workspace"
    - from: "frontend/components/character-creation/character-workspace.tsx"
      to: "frontend/components/character-creation/character-card.tsx"
      via: "structured draft editor composition"
      pattern: "CharacterCard"
    - from: "frontend/app/character-creation/page.tsx"
      to: "frontend/app/(non-game)/campaign/[id]/character/page.tsx"
      via: "legacy redirect to canonical route"
      pattern: "redirect|router"
---

<objective>
Turn character creation into the canonical desktop authoring workspace and remove the standalone legacy page.

Purpose: make the new ontology, persona-template, start-condition, and loadout seams readable and actionable instead of burying them inside a stacked single-page form.
Output: shell-owned canonical character route, workspace container, upgraded route tests, and compatibility coverage proving legacy routing and `/game` handoff still work.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-desktop-first-non-game-ui-overhaul/32-RESEARCH.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-05-SUMMARY.md
@frontend/app/campaign/[id]/character/page.tsx
@frontend/app/character-creation/page.tsx
@frontend/components/character-creation/character-card.tsx
@frontend/components/character-creation/character-form.tsx
@frontend/lib/character-drafts.ts
@shared/src/types.ts

<interfaces>
From `shared/src/types.ts`:
```typescript
export interface CharacterDraft {
  identity: CharacterIdentityDraft;
  profile: CharacterProfile;
  socialContext: CharacterSocialContext;
  motivations: CharacterMotivations;
  capabilities: CharacterCapabilities;
  state: CharacterState;
  loadout: CharacterLoadout;
  startConditions: CharacterStartConditions;
  provenance: CharacterProvenance;
}
```

From `frontend/components/character-creation/character-card.tsx`:
```typescript
interface CharacterCardProps {
  draft: CharacterDraft;
  locationNames: string[];
  personaTemplates?: PersonaTemplateSummary[];
  previewLoadout?: LoadoutPreviewResult | null;
  onResolveStartingLocation: () => void;
  onPreviewLoadout?: () => void;
  onApplyPersonaTemplate?: (templateId: string) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical character workspace, legacy redirect, and `/game` smoke tests</name>
  <files>frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx, frontend/components/character-creation/__tests__/character-workspace.test.tsx, frontend/app/character-creation/__tests__/page.test.tsx, frontend/app/game/__tests__/page.test.tsx, frontend/components/character-creation/character-workspace.tsx</files>
  <behavior>
    - Test 1: the canonical character route renders the shared-shell workspace with input-method region, main draft editor, summary context, and sticky action.
    - Test 2: persona-template apply, start-condition resolve, and loadout preview affordances remain visible in the workspace contract.
    - Test 3: legacy `/character-creation` redirects to the canonical route and `/game` does not inherit non-game shell framing.
  </behavior>
  <action>Write the route/workspace/compatibility tests first and define `character-workspace.tsx` as the container they expect. This is the guardrail for the pending Phase 29/30 closeout risk in `STATE.md`: the tests should assert the current draft/persona/start/loadout seams stay visible after the page move instead of being flattened by the redesign.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx" "components/character-creation/__tests__/character-workspace.test.tsx" "app/character-creation/__tests__/page.test.tsx" "app/game/__tests__/page.test.tsx"</automated>
  </verify>
  <done>The canonical character workspace, legacy redirect, and `/game` isolation are all protected by explicit tests before page implementation changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the desktop character workspace on the existing draft/start/loadout seam</name>
  <files>frontend/app/campaign/[id]/character/page.tsx, frontend/app/(non-game)/campaign/[id]/character/page.tsx, frontend/components/character-creation/character-workspace.tsx, frontend/components/character-creation/character-card.tsx, frontend/components/character-creation/character-form.tsx</files>
  <behavior>
    - Test 1: the page still loads locations/persona templates from `getWorldData`, mutates `CharacterDraft`, and saves through `saveCharacter`.
    - Test 2: the workspace makes persona input modes, structured start conditions, and canonical loadout preview visually first-class.
    - Test 3: the redesign keeps one canonical editor surface instead of introducing a second incompatible character model.
  </behavior>
  <action>Move the canonical character page into `frontend/app/(non-game)/campaign/[id]/character/page.tsx` and rebuild it around `character-workspace.tsx`. Use the left rail for entry methods/progress, the main canvas for `CharacterCard` editing, the right rail for live summary/loadout/start completeness context, and a sticky primary action for save/begin. Reuse the existing API calls and `CharacterDraft` seam from Phases 29-30; do not create a new editor model, do not regress `CharacterCard` back to flat tags, and do not redesign `/game`.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx" "components/character-creation/__tests__/character-card.test.tsx" "components/character-creation/__tests__/character-workspace.test.tsx"</automated>
  </verify>
  <done>The canonical character route uses the shared shell, exposes draft/persona/start/loadout semantics clearly, and still saves the same `CharacterDraft` payload into the current runtime handoff.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the legacy character page with a redirect-only stub and keep `/game` handoff clean</name>
  <files>frontend/app/character-creation/page.tsx, frontend/app/character-creation/__tests__/page.test.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <action>Turn `frontend/app/character-creation/page.tsx` into a redirect-only compatibility stub that forwards to `/campaign/[id]/character`. Update route tests so D-06 is enforced now, and keep the post-save navigation entering `/game` exactly as a minimal compatibility handoff. Do not pull the gameplay page into the non-game shell and do not add gameplay UI redesign work here.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/character-creation/__tests__/page.test.tsx" "app/game/__tests__/page.test.tsx" "app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx"</automated>
  </verify>
  <done>The only authored character-creation experience is the canonical campaign route, and the flow still hands off cleanly to the unchanged gameplay surface.</done>
</task>

</tasks>

<verification>
Character work is complete when canonical route tests, workspace/component tests, redirect tests, and the `/game` smoke test all pass together.
</verification>

<success_criteria>
Character creation reads as a desktop authoring tool for the current draft/persona/start/loadout seams, legacy route drift is removed, and gameplay remains structurally separate.
</success_criteria>

<output>
After completion, create `.planning/phases/32-desktop-first-non-game-ui-overhaul/32-05-SUMMARY.md`
</output>
