---
phase: 32-desktop-first-non-game-ui-overhaul
plan: 04
type: execute
wave: 3
depends_on:
  - 32-01
files_modified:
  - frontend/app/campaign/[id]/review/page.tsx
  - frontend/app/(non-game)/campaign/[id]/review/page.tsx
  - frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx
  - frontend/app/world-review/page.tsx
  - frontend/app/world-review/__tests__/page.test.tsx
  - frontend/components/world-review/review-workspace.tsx
  - frontend/components/world-review/npcs-section.tsx
  - frontend/components/world-review/lore-section.tsx
autonomous: true
requirements:
  - P32-03
  - P32-06
must_haves:
  truths:
    - "World review becomes a desktop workspace with visible section navigation, dense editing surface, summary/validation context, and sticky actions."
    - "NPC and lore editing still operate on the current scaffold/draft/persona seams rather than a new review data model."
    - "Legacy `/world-review` stops being a parallel authored route and redirects to the canonical campaign review URL."
  artifacts:
    - path: "frontend/app/(non-game)/campaign/[id]/review/page.tsx"
      provides: "Shell-owned canonical review route"
    - path: "frontend/components/world-review/review-workspace.tsx"
      provides: "Desktop review container with section nav, main editor, and inspector/action regions"
    - path: "frontend/components/world-review/npcs-section.tsx"
      provides: "NPC review surface compatible with persona/start/loadout context"
    - path: "frontend/components/world-review/lore-section.tsx"
      provides: "Lore editing/search surface integrated into the workspace"
    - path: "frontend/app/world-review/page.tsx"
      provides: "Redirect-only legacy compatibility stub"
    - path: "frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx"
      provides: "Regression coverage for canonical review workspace"
  key_links:
    - from: "frontend/app/(non-game)/campaign/[id]/review/page.tsx"
      to: "frontend/lib/world-data-helpers.ts"
      via: "editable scaffold normalization"
      pattern: "toEditableScaffold"
    - from: "frontend/components/world-review/review-workspace.tsx"
      to: "frontend/components/world-review/npcs-section.tsx"
      via: "shared scaffold editor composition"
      pattern: "NpcsSection"
    - from: "frontend/app/world-review/page.tsx"
      to: "frontend/app/(non-game)/campaign/[id]/review/page.tsx"
      via: "legacy redirect to canonical route"
      pattern: "redirect|router"
---

<objective>
Turn world review into the canonical desktop editor workspace and remove legacy route drift.

Purpose: give the pre-game world-editing flow real desktop hierarchy and density while preserving the current scaffold, lore, and NPC semantics introduced in Phases 29-30.
Output: shell-owned review route, dense review workspace container, updated NPC/lore integration, and redirect coverage for the legacy review route.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-desktop-first-non-game-ui-overhaul/32-RESEARCH.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-06-SUMMARY.md
@frontend/app/campaign/[id]/review/page.tsx
@frontend/app/world-review/page.tsx
@frontend/components/world-review/npcs-section.tsx
@frontend/components/world-review/lore-section.tsx
@frontend/lib/world-data-helpers.ts
@frontend/lib/api-types.ts

<interfaces>
From `frontend/lib/api-types.ts`:
```typescript
export interface EditableScaffold {
  refinedPremise: string;
  locations: ScaffoldLocation[];
  factions: ScaffoldFaction[];
  npcs: ScaffoldNpc[];
  loreCards: ScaffoldLoreCard[];
  personaTemplates?: PersonaTemplateSummary[];
}
```

From `frontend/components/world-review/npcs-section.tsx`:
```typescript
interface NpcsSectionProps {
  npcs: ScaffoldNpc[];
  campaignId: string;
  locationNames: string[];
  factionNames: string[];
  personaTemplates?: PersonaTemplateSummary[];
  onChange: (npcs: ScaffoldNpc[]) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical review workspace and legacy-redirect tests</name>
  <files>frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx, frontend/app/world-review/__tests__/page.test.tsx, frontend/components/world-review/review-workspace.tsx</files>
  <behavior>
    - Test 1: the canonical review route renders desktop section navigation, main editor content, and sticky save/continue actions inside the shared shell.
    - Test 2: summary/validation context remains visible without duplicating the full editor in the inspector rail.
    - Test 3: legacy `/world-review` no longer renders a standalone editor and redirects to the canonical campaign review route.
  </behavior>
  <action>Create review-page regression coverage before moving files. Add a `review-workspace.tsx` container contract that the tests exercise and use it to lock the intended list/detail/inspector behavior from D-03. Keep these tests focused on composition and route ownership; section-specific editing logic stays covered by the existing world-review component tests.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx" "app/world-review/__tests__/page.test.tsx"</automated>
  </verify>
  <done>The canonical review route and legacy redirect are protected by focused tests, and the workspace container contract exists before the page move.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the desktop review workspace on the current scaffold seam</name>
  <files>frontend/app/campaign/[id]/review/page.tsx, frontend/app/(non-game)/campaign/[id]/review/page.tsx, frontend/components/world-review/review-workspace.tsx, frontend/components/world-review/npcs-section.tsx, frontend/components/world-review/lore-section.tsx</files>
  <behavior>
    - Test 1: save/regenerate/load behavior still uses the current scaffold + lore APIs.
    - Test 2: NPC editing continues to surface persona templates and start/loadout context introduced in Phase 30.
    - Test 3: lore search/edit/delete/import remain reachable inside the denser workspace.
  </behavior>
  <action>Move the canonical review page into `frontend/app/(non-game)/campaign/[id]/review/page.tsx` and recast it around `review-workspace.tsx`. Use shell slots, section navigation, and sticky primary actions instead of tab-only desktop editing. Keep `EditableScaffold`, `toEditableScaffold`, `saveWorldEdits`, `regenerateSection`, lore refresh, and NPC persona-template plumbing intact; this is a UI/layout refactor, not a review-model rewrite. Adjust `npcs-section.tsx` and `lore-section.tsx` only as needed to fit the new density and hierarchy.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx" "components/world-review/__tests__/npcs-section.test.tsx" "components/world-review/__tests__/lore-section.test.tsx"</automated>
  </verify>
  <done>The canonical review route uses the shared shell, renders as a dense desktop workspace, and preserves the current NPC/lore/persona semantics without introducing a second data shape.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the legacy review page with a redirect-only compatibility stub</name>
  <files>frontend/app/world-review/page.tsx, frontend/app/world-review/__tests__/page.test.tsx</files>
  <action>Turn `frontend/app/world-review/page.tsx` into a redirect-only compatibility stub that forwards to `/campaign/[id]/review` using the existing query/campaign context. Remove any standalone editor UI from the legacy route so D-06 is enforced during Phase 32 instead of deferred to Phase 33. Keep the continue flow aimed at the canonical character route, not back to the legacy pages.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/world-review/__tests__/page.test.tsx" "app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx"</automated>
  </verify>
  <done>The only authored review experience is the canonical campaign route, and the legacy route exists solely for compatibility redirection.</done>
</task>

</tasks>

<verification>
World review is complete when the canonical page test, NPC/lore component tests, and legacy redirect test all pass together.
</verification>

<success_criteria>
Review editing is visibly desktop-first, retains current scaffold/NPC/lore semantics, and no longer shares ownership with the old `/world-review` page.
</success_criteria>

<output>
After completion, create `.planning/phases/32-desktop-first-non-game-ui-overhaul/32-04-SUMMARY.md`
</output>
