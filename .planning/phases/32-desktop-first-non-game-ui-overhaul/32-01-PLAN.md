---
phase: 32-desktop-first-non-game-ui-overhaul
plan: 01
type: execute
wave: 1
depends_on:
  - 32-00
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/components/ui/sidebar.tsx
  - frontend/components/ui/separator.tsx
  - frontend/components/ui/resizable.tsx
  - frontend/components/non-game-shell/app-shell.tsx
  - frontend/components/non-game-shell/app-sidebar.tsx
  - frontend/components/non-game-shell/page-header.tsx
  - frontend/components/non-game-shell/inspector-rail.tsx
  - frontend/components/non-game-shell/sticky-action-bar.tsx
  - frontend/components/non-game-shell/__tests__/app-shell.test.tsx
  - frontend/app/(non-game)/layout.tsx
  - frontend/app/(non-game)/__tests__/layout.test.tsx
autonomous: true
requirements:
  - P32-01
  - P32-06
must_haves:
  truths:
    - "A shared non-game shell exists with persistent header, left navigation, main canvas, inspector rail, and sticky action regions."
    - "Non-game routes have one App Router route-group layout while `/game` remains outside that shell."
    - "Shell primitives rely on Tailwind, shadcn, and compatible wrappers only, with no new custom CSS files."
  artifacts:
    - path: "frontend/app/(non-game)/layout.tsx"
      provides: "Shared route-group layout for non-game pages"
    - path: "frontend/components/non-game-shell/app-shell.tsx"
      provides: "Desktop shell composition contract for launcher, creation, review, character, settings, and library pages"
    - path: "frontend/components/ui/sidebar.tsx"
      provides: "shadcn-compatible sidebar primitive for persistent navigation"
    - path: "frontend/components/ui/resizable.tsx"
      provides: "shadcn-compatible resizable primitive for review and character workspaces"
    - path: "frontend/components/non-game-shell/__tests__/app-shell.test.tsx"
      provides: "Regression coverage for shell slot composition"
  key_links:
    - from: "frontend/app/(non-game)/layout.tsx"
      to: "frontend/components/non-game-shell/app-shell.tsx"
      via: "shared shell wrapper import"
      pattern: "app-shell"
    - from: "frontend/components/non-game-shell/app-shell.tsx"
      to: "frontend/components/ui/sidebar.tsx"
      via: "persistent left-rail navigation"
      pattern: "Sidebar|SidebarProvider"
    - from: "frontend/components/non-game-shell/app-shell.tsx"
      to: "frontend/components/ui/resizable.tsx"
      via: "desktop-pane composition seam for later plans"
      pattern: "Resizable"
---

<objective>
Create the shared shell and route-group foundation that every Phase 32 surface will adopt after the Wave 0 baseline gate clears.

Purpose: land one desktop interaction grammar before moving launcher, creation, review, character, settings, and library pages into it.
Output: shadcn-compatible shell primitives, route-group layout, and targeted tests that lock the shell contract before page migrations begin.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-desktop-first-non-game-ui-overhaul/32-VALIDATION.md
@.planning/phases/32-desktop-first-non-game-ui-overhaul/32-RESEARCH.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-desktop-ui-workspace-spec.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-phase-32-33-handoff.md
@docs/ui_concept_hybrid.html
@frontend/app/globals.css
@frontend/components.json
@frontend/package.json

<interfaces>
From `frontend/app/globals.css`:
```css
@theme inline {
  --font-sans: var(--font-inter);
  --font-serif: var(--font-playfair);
  --color-bone: #f8f9fa;
  --color-blood: #e63e00;
  --color-mystic: #3b82f6;
}
```

From `frontend/components.json`:
```json
{
  "style": "new-york",
  "tailwind": {
    "css": "app/globals.css",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "ui": "@/components/ui",
    "lib": "@/lib"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the non-game shell contract in tests before page adoption</name>
  <files>frontend/components/non-game-shell/app-shell.tsx, frontend/components/non-game-shell/app-sidebar.tsx, frontend/components/non-game-shell/page-header.tsx, frontend/components/non-game-shell/inspector-rail.tsx, frontend/components/non-game-shell/sticky-action-bar.tsx, frontend/components/non-game-shell/__tests__/app-shell.test.tsx, frontend/app/(non-game)/layout.tsx, frontend/app/(non-game)/__tests__/layout.test.tsx</files>
  <behavior>
    - Test 1: the shell renders left navigation, main content, optional inspector content, and sticky actions without collapsing them into one stacked column.
    - Test 2: the route-group layout composes the shell and keeps a dedicated child slot for later launcher/settings/review/character pages.
    - Test 3: the shell contract exposes route context and action labels without baking page-specific forms into the foundation.
  </behavior>
  <action>Create the `frontend/components/non-game-shell/` foundation per D-01 through D-04 in `32-RESEARCH.md`: `app-shell`, `app-sidebar`, `page-header`, `inspector-rail`, and `sticky-action-bar`. Add targeted tests first so later plans can rely on stable shell slots instead of re-inventing layout chrome. Also add `frontend/app/(non-game)/layout.tsx` as the shared App Router route-group layout that wraps future non-game pages. Keep the shell visual language anchored to `docs/ui_concept_hybrid.html` through Tailwind classes and existing theme tokens only; do not introduce a new CSS file, page-local shell copies, or `/game` coupling.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "components/non-game-shell/__tests__/app-shell.test.tsx" "app/(non-game)/__tests__/layout.test.tsx"</automated>
  </verify>
  <done>The shell contract is test-protected, the `(non-game)` layout exists, and downstream page plans can render inside one shared desktop frame instead of creating new page-local wrappers.</done>
</task>

<task type="auto">
  <name>Task 2: Add the required shadcn-compatible shell primitives and dependency seam</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/components/ui/sidebar.tsx, frontend/components/ui/separator.tsx, frontend/components/ui/resizable.tsx</files>
  <action>Add the missing shadcn-compatible primitives the research calls out: `sidebar`, `separator`, and `resizable`, including the `react-resizable-panels` dependency path that `shadcn` wires for `Resizable`. Generate or adapt them through the existing shadcn/Tailwind setup in `frontend/components.json`; do not hand-roll drag handles, custom sidebar state, or bespoke styling. Keep the resulting UI wrappers ready for review/character workspaces while leaving actual resizable usage to later plans.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "components/non-game-shell/__tests__/app-shell.test.tsx" "app/(non-game)/__tests__/layout.test.tsx"</automated>
  </verify>
  <done>The repo contains shadcn-compatible sidebar/resizable/separator wrappers and package metadata that later plans can consume without extra dependency churn.</done>
</task>

</tasks>

<verification>
Foundation is complete when the shell tests pass and the only shared layout introduced for non-game work is `frontend/app/(non-game)/layout.tsx`.
</verification>

<success_criteria>
Every later Phase 32 page can mount inside one tested shell foundation, and the repo now has the shadcn-compatible primitives required for persistent navigation and desktop pane composition.
</success_criteria>

<output>
After completion, create `.planning/phases/32-desktop-first-non-game-ui-overhaul/32-01-SUMMARY.md`
</output>
