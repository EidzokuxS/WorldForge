---
phase: 32-desktop-first-non-game-ui-overhaul
plan: 03
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - frontend/app/settings/page.tsx
  - frontend/app/(non-game)/settings/page.tsx
  - frontend/app/(non-game)/settings/__tests__/page.test.tsx
  - frontend/app/(non-game)/library/page.tsx
  - frontend/app/(non-game)/library/__tests__/page.test.tsx
  - frontend/components/library/library-workspace.tsx
autonomous: true
requirements:
  - P32-05
must_haves:
  truths:
    - "Settings uses the shared non-game shell and keeps save state, provider health context, and role bindings visible."
    - "Reusable worldbook management has a canonical `/library` workspace instead of living only inside creation UI."
    - "Settings and library look and behave like the same product surface rather than special-case mini-apps."
  artifacts:
    - path: "frontend/app/(non-game)/settings/page.tsx"
      provides: "Shell-owned settings page"
    - path: "frontend/app/(non-game)/library/page.tsx"
      provides: "Canonical reusable-library management route"
    - path: "frontend/components/library/library-workspace.tsx"
      provides: "Desktop library browse/import workspace"
    - path: "frontend/app/(non-game)/settings/__tests__/page.test.tsx"
      provides: "Regression coverage for shell-adopted settings"
    - path: "frontend/app/(non-game)/library/__tests__/page.test.tsx"
      provides: "Regression coverage for library workspace behavior"
  key_links:
    - from: "frontend/app/(non-game)/settings/page.tsx"
      to: "frontend/lib/use-settings.ts"
      via: "existing auto-save settings state"
      pattern: "useSettings"
    - from: "frontend/app/(non-game)/library/page.tsx"
      to: "frontend/lib/api.ts"
      via: "worldbook library list/import calls"
      pattern: "listWorldbookLibrary|importWorldbookLibrary"
    - from: "frontend/components/library/library-workspace.tsx"
      to: "frontend/app/(non-game)/layout.tsx"
      via: "shared shell slots"
      pattern: "page-header|inspector|sticky"
---

<objective>
Adopt the shared shell for settings and create a first-class reusable library workspace.

Purpose: stop treating provider settings and reusable worldbook management as disconnected utilities outside the main non-game product surface.
Output: shell-owned settings route, canonical `/library` page, and targeted tests proving provider/research/library behavior survives the UI overhaul.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-desktop-first-non-game-ui-overhaul/32-RESEARCH.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-desktop-ui-workspace-spec.md
@frontend/app/settings/page.tsx
@frontend/components/settings/providers-tab.tsx
@frontend/components/settings/roles-tab.tsx
@frontend/components/settings/images-tab.tsx
@frontend/components/settings/research-tab.tsx
@frontend/lib/use-settings.ts
@frontend/lib/api.ts

<interfaces>
From `frontend/lib/use-settings.ts` and `frontend/app/settings/page.tsx`:
```typescript
const { settings, setSettings, isLoading, isSaving, save } = useSettings();
```

From `frontend/lib/api.ts`:
```typescript
export function listWorldbookLibrary(): Promise<{ items: WorldbookLibraryItem[] }>;
export function importWorldbookLibrary(
  displayName: string,
  worldbook: object,
  originalFileName?: string,
): Promise<{ item: WorldbookLibraryItem; existed: boolean }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add settings and library shell-adoption page tests</name>
  <files>frontend/app/(non-game)/settings/__tests__/page.test.tsx, frontend/app/(non-game)/library/__tests__/page.test.tsx, frontend/components/library/library-workspace.tsx</files>
  <behavior>
    - Test 1: the settings page renders inside the shared shell with visible save state and contextual provider summary.
    - Test 2: the library page lists reusable sources, exposes import actions, and keeps creation-related summary context visible in-shell.
    - Test 3: neither page falls back to standalone centered-card layout or modal-only interactions.
  </behavior>
  <action>Write the page-level shell adoption tests first and sketch `library-workspace.tsx` as the focused desktop workspace container they exercise. The tests should lock the Phase 32 shell language for settings/library before the pages move, and they should prove that provider/research behavior and worldbook-library management remain accessible without reintroducing page-local chrome.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/(non-game)/settings/__tests__/page.test.tsx" "app/(non-game)/library/__tests__/page.test.tsx"</automated>
  </verify>
  <done>New page tests exist for both settings and library shell adoption, and the library workspace contract is explicit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move settings into the shared shell without changing its data behavior</name>
  <files>frontend/app/settings/page.tsx, frontend/app/(non-game)/settings/page.tsx, frontend/app/(non-game)/settings/__tests__/page.test.tsx</files>
  <behavior>
    - Test 1: providers, roles, images, and research tabs still render and mutate through `useSettings`.
    - Test 2: save status remains visible and understandable in the shell header/inspector regions.
    - Test 3: canonical `/settings` behavior is unchanged except for the new shell framing.
  </behavior>
  <action>Move the physical settings page into `frontend/app/(non-game)/settings/page.tsx` so it inherits the shell while keeping the canonical `/settings` URL stable. Preserve the existing tab internals, auto-save cadence, and summary/footer logic, but recast the page into shell slots with stronger header/status framing per D-04. Do not split settings into a new data model or introduce custom CSS.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/(non-game)/settings/__tests__/page.test.tsx"</automated>
  </verify>
  <done>Settings lives in the shared shell, keeps its current provider/research behavior, and communicates save/health state without feeling disconnected from the rest of the non-game UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create the canonical `/library` workspace on existing reusable-worldbook APIs</name>
  <files>frontend/app/(non-game)/library/page.tsx, frontend/components/library/library-workspace.tsx, frontend/app/(non-game)/library/__tests__/page.test.tsx</files>
  <behavior>
    - Test 1: the library page can list current reusable worldbooks and surface empty/loading/import states.
    - Test 2: import actions remain wired to the existing backend library endpoints rather than a new ad hoc flow.
    - Test 3: the workspace gives users a clear path back into campaign creation without moving library management back into the creation modal.
  </behavior>
  <action>Implement `/library` as the canonical reusable worldbook management workspace using `listWorldbookLibrary` and `importWorldbookLibrary` only. Focus on browse/select/import, metadata display, and shell-integrated summaries; do not invent new backend contracts or advanced search if the current API does not require it. Keep the visual language aligned with the shell and `ui_concept_hybrid` rather than making library a utility screen.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "app/(non-game)/library/__tests__/page.test.tsx"</automated>
  </verify>
  <done>A shell-owned `/library` route exists, manages reusable sources through current APIs, and no longer forces users back into campaign creation UI for basic library work.</done>
</task>

</tasks>

<verification>
Settings/library adoption is complete when both routes use the shared shell and their focused page tests prove current provider and library behavior remains intact.
</verification>

<success_criteria>
Settings and reusable library management are canonical, shell-owned surfaces that preserve current functionality and establish the same desktop editing grammar as the rest of Phase 32.
</success_criteria>

<output>
After completion, create `.planning/phases/32-desktop-first-non-game-ui-overhaul/32-03-SUMMARY.md`
</output>
