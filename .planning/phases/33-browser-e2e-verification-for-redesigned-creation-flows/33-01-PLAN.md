---
phase: 33-browser-e2e-verification-for-redesigned-creation-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/character-creation/page.tsx
  - frontend/app/character-creation/__tests__/page.test.tsx
  - frontend/app/world-review/page.tsx
  - frontend/app/world-review/__tests__/page.test.tsx
autonomous: true
requirements: [P33-01, P33-04]
must_haves:
  truths:
    - "Legacy /character-creation route no longer exists as a file"
    - "Legacy /world-review route no longer exists as a file"
    - "Launcher page renders inside the shared shell at /"
    - "Shell sidebar navigation links are visible and clickable"
    - "Navigating to /campaign/new from launcher loads the campaign creation page inside the shell"
  artifacts:
    - path: "frontend/app/(non-game)/page.tsx"
      provides: "Launcher page inside shared shell"
    - path: "frontend/components/non-game-shell/app-shell.tsx"
      provides: "Shared desktop shell with sidebar, header, inspector"
  key_links:
    - from: "launcher page"
      to: "/campaign/new"
      via: "New Campaign button click"
      pattern: "href.*campaign/new"
    - from: "shell sidebar"
      to: "all non-game routes"
      via: "sidebar navigation links"
---

<objective>
Delete legacy route files and verify the shared non-game shell renders correctly in a real browser via PinchTab.

Purpose: Clean prerequisite -- remove dead routes (per D-09) before E2E verification of canonical routes. Confirm the shell foundation from Phase 32 actually works in a browser.
Output: Legacy routes deleted, shell + launcher verified rendering in browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md
@frontend/app/character-creation/page.tsx
@frontend/app/world-review/page.tsx
@frontend/components/non-game-shell/app-shell.tsx
@frontend/app/(non-game)/page.tsx
@frontend/app/(non-game)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy route files and verify build</name>
  <files>
    frontend/app/character-creation/page.tsx,
    frontend/app/character-creation/__tests__/page.test.tsx,
    frontend/app/world-review/page.tsx,
    frontend/app/world-review/__tests__/page.test.tsx
  </files>
  <read_first>
    frontend/app/character-creation/page.tsx,
    frontend/app/character-creation/__tests__/page.test.tsx,
    frontend/app/world-review/page.tsx,
    frontend/app/world-review/__tests__/page.test.tsx,
    frontend/app/(non-game)/campaign/[id]/character/page.tsx,
    frontend/app/(non-game)/campaign/[id]/review/page.tsx
  </read_first>
  <action>
    Per D-09: Delete these 4 files entirely (no redirects, just remove):
    - `frontend/app/character-creation/page.tsx`
    - `frontend/app/character-creation/__tests__/page.test.tsx`
    - `frontend/app/world-review/page.tsx`
    - `frontend/app/world-review/__tests__/page.test.tsx`

    Then delete the now-empty directories:
    - `frontend/app/character-creation/__tests__/`
    - `frontend/app/character-creation/`
    - `frontend/app/world-review/__tests__/`
    - `frontend/app/world-review/`

    After deletion, run `npm --prefix frontend run build` to confirm the Next.js build succeeds without these files. If any other file imports from these deleted paths, fix the import (it should not happen since Phase 32 made these redirect-only stubs).

    Also run `npm --prefix frontend run lint` to confirm no lint errors from dangling references.
  </action>
  <verify>
    <automated>ls frontend/app/character-creation/page.tsx 2>/dev/null && echo "FAIL: legacy file exists" || echo "PASS: deleted" ; ls frontend/app/world-review/page.tsx 2>/dev/null && echo "FAIL: legacy file exists" || echo "PASS: deleted" ; npm --prefix frontend run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/character-creation/` directory does not exist
    - `frontend/app/world-review/` directory does not exist
    - `npm --prefix frontend run build` exits 0
    - `npm --prefix frontend run lint` exits 0
  </acceptance_criteria>
  <done>Legacy route files and their test files are deleted. Next.js build succeeds without them.</done>
</task>

<task type="auto">
  <name>Task 2: PinchTab browser verification of shell + launcher + navigation</name>
  <files></files>
  <read_first>
    frontend/components/non-game-shell/app-shell.tsx,
    frontend/components/non-game-shell/app-sidebar.tsx,
    frontend/app/(non-game)/page.tsx,
    frontend/app/(non-game)/layout.tsx
  </read_first>
  <action>
    Start PinchTab in headless mode: `BRIDGE_HEADLESS=true pinchtab &`
    Ensure both dev servers are running: backend on :3001, frontend on :3000.

    Using PinchTab HTTP API (port 9867), execute the following verification sequence:

    1. **Navigate to launcher** (`http://localhost:3000/`):
       - Confirm the page loads (accessibility tree contains "Campaign Launchpad" text)
       - Confirm shell sidebar is visible (contains navigation links: "Launch", "Settings", "Library" or similar)
       - Confirm "New Campaign" button exists in the launcher content
       - Confirm "Open Library" and "Open Settings" shortcut buttons exist

    2. **Navigate to /campaign/new**:
       - Click "New Campaign" button via PinchTab `/evaluate` (use `var btn = document.querySelector('a[href="/campaign/new"]'); btn && btn.click();`)
       - Confirm the page loads inside the shell (shell sidebar still visible)
       - Confirm page header shows "Campaign Creation" or similar eyebrow text
       - Navigate back to launcher

    3. **Navigate to /settings**:
       - Click "Open Settings" or navigate directly to `http://localhost:3000/settings`
       - Confirm settings page renders inside the shell
       - Confirm shell sidebar still visible

    4. **Navigate to /library**:
       - Navigate to `http://localhost:3000/library`
       - Confirm library page renders inside the shell

    5. **Verify legacy routes return 404**:
       - Navigate to `http://localhost:3000/character-creation`
       - Confirm 404 or Next.js error page (NOT a redirect, NOT the old page content)
       - Navigate to `http://localhost:3000/world-review`
       - Confirm 404 or Next.js error page

    Per D-07: If any step reveals a bug, fix it immediately and re-run that step.
    Per D-11: Shell elements (sidebar, header, inspector rail) verified as part of each navigation step.

    PinchTab workarounds:
    - Use `/evaluate` with `var` (not const/let) for JS execution
    - For buttons with lucide icons, use programmatic JS click instead of ref-click
    - Avoid `!` character in evaluate expressions
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/ | grep -q "Campaign Launchpad" && echo "PASS: launcher renders" || echo "FAIL: launcher missing"</automated>
  </verify>
  <acceptance_criteria>
    - PinchTab accessibility tree at `/` contains text "Campaign Launchpad"
    - PinchTab accessibility tree at `/` contains a link to "/campaign/new"
    - PinchTab accessibility tree at `/campaign/new` contains text "Campaign Creation" or "Create"
    - PinchTab accessibility tree at `/settings` contains text "Settings"
    - PinchTab accessibility tree at `/library` contains text "Library"
    - Navigation to `/character-creation` does NOT show the old character creation page content
    - Navigation to `/world-review` does NOT show the old world review page content
    - Shell sidebar is visible on all 4 canonical pages (/, /campaign/new, /settings, /library)
  </acceptance_criteria>
  <done>Shell renders correctly on all canonical routes. Legacy routes are confirmed dead. Navigation between shell pages works via sidebar and buttons.</done>
</task>

</tasks>

<verification>
- Legacy route files deleted from filesystem
- Next.js build succeeds
- Shell renders on all canonical non-game routes in real browser
- Legacy routes return 404 in browser
- No regressions in navigation
</verification>

<success_criteria>
- Zero legacy route files remain
- Build passes
- All 4 canonical routes (/, /campaign/new, /settings, /library) render inside the shell in PinchTab
- Legacy /character-creation and /world-review URLs no longer serve content
</success_criteria>

<output>
After completion, create `.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-01-SUMMARY.md`
</output>
