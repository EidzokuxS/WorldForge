---
phase: 33-browser-e2e-verification-for-redesigned-creation-flows
plan: 07
type: execute
wave: 2
depends_on: [33-05]
files_modified:
  - frontend/components/non-game-shell/campaign-status-provider.tsx
  - frontend/app/(non-game)/layout.tsx
  - frontend/components/non-game-shell/app-shell.tsx
  - frontend/components/non-game-shell/app-sidebar.tsx
  - frontend/components/title/load-campaign-dialog.tsx
  - frontend/components/title/__tests__/load-campaign-dialog.test.tsx
  - frontend/app/(non-game)/campaign/[id]/review/page.tsx
  - frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx
  - frontend/app/(non-game)/campaign/[id]/character/page.tsx
  - frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx
  - backend/src/routes/helpers.ts
  - backend/src/routes/campaigns.ts
  - backend/src/routes/__tests__/campaigns.test.ts
autonomous: true
requirements: [P33-01, P33-02, P33-04]
gap_closure: true
must_haves:
  truths:
    - "World Review and Character are not reachable for campaigns that have not completed world generation."
    - "Load dialog cards no longer load a campaign from a whole-card click; only the Load button triggers load."
    - "The shared shell does not show a redundant 'Open Creation Flow' CTA while the user is already inside the routed creation flow."
  artifacts:
    - path: "frontend/components/non-game-shell/campaign-status-provider.tsx"
      provides: "Shared non-game campaign readiness state for shell navigation and route guards"
    - path: "frontend/app/(non-game)/campaign/[id]/review/page.tsx"
      provides: "Review readiness guard before scaffold editor loads"
    - path: "backend/src/routes/helpers.ts"
      provides: "Backend readiness guard for generated-world routes"
  key_links:
    - from: "frontend/components/non-game-shell/campaign-status-provider.tsx"
      to: "frontend/components/non-game-shell/app-sidebar.tsx"
      via: "campaign readiness context"
      pattern: "useCampaignStatus"
    - from: "backend/src/routes/helpers.ts"
      to: "backend/src/routes/campaigns.ts"
      via: "generated-world route guard"
      pattern: "requireGeneratedCampaign"
    - from: "frontend/components/title/load-campaign-dialog.tsx"
      to: "handleLoadCampaign"
      via: "Load button click only"
      pattern: "onClick=\\{\\(event"
---

<objective>
Close the readiness/navigation half of UAT Gap 2 by enforcing generated-world guards, removing misleading shell navigation/CTAs, and fixing campaign loading so only explicit Load actions activate a campaign.

Purpose: Even after the routed DNA flow works again, the user can still hit broken states if review/character routes and shell links are available before the world is ready. This plan enforces readiness across the shell, pages, and backend route boundary.
Output: Readiness-aware shell navigation, guarded review/character routes, precise load behavior, and no redundant shell CTA on creation routes.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-UAT.md
@.planning/debug/phase-33-uat-gap-2.md
@frontend/app/(non-game)/layout.tsx
@frontend/components/non-game-shell/app-shell.tsx
@frontend/components/non-game-shell/app-sidebar.tsx
@frontend/components/title/load-campaign-dialog.tsx
@frontend/app/(non-game)/campaign/[id]/review/page.tsx
@frontend/app/(non-game)/campaign/[id]/character/page.tsx
@frontend/lib/api.ts
@backend/src/routes/helpers.ts
@backend/src/routes/campaigns.ts

<interfaces>
From shared/src/types.ts:
```typescript
export interface CampaignMeta {
  id: string;
  name: string;
  premise: string;
  createdAt: number;
  updatedAt: number;
  seeds?: WorldSeeds;
  generationComplete?: boolean;
}
```

From frontend/lib/api.ts:
```typescript
export async function loadCampaign(campaignId: string): Promise<CampaignMeta>;
export async function getWorldData(campaignId: string): Promise<WorldData>;
export async function getActiveCampaign(): Promise<CampaignMeta | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add readiness and campaign-loading regressions</name>
  <files>
    frontend/components/title/__tests__/load-campaign-dialog.test.tsx,
    frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx,
    frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx,
    backend/src/routes/__tests__/campaigns.test.ts
  </files>
  <read_first>
    .planning/debug/phase-33-uat-gap-2.md,
    frontend/components/title/__tests__/load-campaign-dialog.test.tsx,
    frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx,
    frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx,
    backend/src/routes/__tests__/campaigns.test.ts,
    frontend/components/title/load-campaign-dialog.tsx,
    frontend/app/(non-game)/campaign/[id]/review/page.tsx,
    frontend/app/(non-game)/campaign/[id]/character/page.tsx,
    backend/src/routes/helpers.ts,
    backend/src/routes/campaigns.ts
  </read_first>
  <behavior>
    - Test 1: Clicking a campaign card body does not load; only the explicit Load button triggers `handleLoadCampaign`.
    - Test 2: Review and character routes do not render their editors when `generationComplete` is false or the backend rejects the world as not ready.
    - Test 3: Generated-world API requests return a clear not-ready response instead of silently serving an empty world.
  </behavior>
  <action>
    Add frontend and backend regressions that pin the readiness and loading behavior required by UAT Gap 2.

    Per D-04 and D-11, the tests must cover broken-state prevention, not just happy-path rendering. Keep scope on the routed creation/review/character flow; do not add library scenarios (D-02).
  </action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "frontend/components/title/__tests__/load-campaign-dialog.test.tsx" "frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx" && npm --prefix backend exec vitest run "src/routes/__tests__/campaigns.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - The load-dialog regression explicitly proves whole-card clicks no longer call `handleLoadCampaign`.
    - Review and character page tests cover a not-ready campaign response path, not just the ready state.
    - Backend route tests assert a clear generated-world guard response for not-ready campaigns.
  </acceptance_criteria>
  <done>Regression coverage exists for the three remaining broken behaviors: whole-card load clicks, unguarded review/character access, and backend not-ready world responses.</done>
</task>

<task type="auto">
  <name>Task 2: Implement readiness-aware shell navigation and guarded campaign routes</name>
  <files>
    frontend/components/non-game-shell/campaign-status-provider.tsx,
    frontend/app/(non-game)/layout.tsx,
    frontend/components/non-game-shell/app-shell.tsx,
    frontend/components/non-game-shell/app-sidebar.tsx,
    frontend/components/title/load-campaign-dialog.tsx,
    frontend/app/(non-game)/campaign/[id]/review/page.tsx,
    frontend/app/(non-game)/campaign/[id]/character/page.tsx,
    backend/src/routes/helpers.ts,
    backend/src/routes/campaigns.ts
  </files>
  <read_first>
    .planning/debug/phase-33-uat-gap-2.md,
    frontend/app/(non-game)/layout.tsx,
    frontend/components/non-game-shell/app-shell.tsx,
    frontend/components/non-game-shell/app-sidebar.tsx,
    frontend/components/title/load-campaign-dialog.tsx,
    frontend/app/(non-game)/campaign/[id]/review/page.tsx,
    frontend/app/(non-game)/campaign/[id]/character/page.tsx,
    frontend/lib/api.ts,
    backend/src/routes/helpers.ts,
    backend/src/routes/campaigns.ts
  </read_first>
  <action>
    Implement the remaining Gap 2 fixes with one shared readiness contract:

    - Add `frontend/components/non-game-shell/campaign-status-provider.tsx` and mount it from `frontend/app/(non-game)/layout.tsx`. It should derive current campaign readiness from the route campaign id plus `loadCampaign()` / `getActiveCampaign()` and expose whether review/character are actually available.
    - Update `AppSidebar` to hide or disable review/character navigation when the current campaign is not ready. Do not derive readiness from URL shape alone anymore.
    - Update `AppShell` so creation routes do not render the fallback sticky `Open Creation Flow` CTA that duplicates the sidebar and bounces the user backward through the flow.
    - Fix `LoadCampaignDialog` so only the explicit Load button triggers loading; card-body clicks must be inert/selectable but not destructive or navigational.
    - Add frontend guards in review and character pages: if the campaign is not generation-ready, redirect back into the routed creation flow or show a clear generation-required callout instead of loading broken editors.
    - Add a backend helper such as `requireGeneratedCampaign` and use it on `/api/campaigns/:id/world` so not-ready campaigns fail clearly at the API boundary rather than returning an empty pseudo-world.

    This task implements the "review/character/sidebar gated by readiness" and "load cards only on explicit Load click" items in UAT Gap 2.
  </action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "frontend/components/title/__tests__/load-campaign-dialog.test.tsx" "frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx" && npm --prefix backend exec vitest run "src/routes/__tests__/campaigns.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/components/title/load-campaign-dialog.tsx` no longer calls `handleLoadCampaign` from the card wrapper.
    - The shell does not expose review/character navigation for an ungenerated campaign.
    - Creation routes no longer show the redundant fallback "Open Creation Flow" sticky action.
    - `GET /api/campaigns/:id/world` returns a clear not-ready response for campaigns with `generationComplete=false`.
    - Browser-verifiable outcome: direct hits to review/character before world readiness no longer land on broken editors.
  </acceptance_criteria>
  <done>The shell, review/character routes, load dialog, and backend world route all enforce real campaign readiness instead of URL-shape assumptions.</done>
</task>

</tasks>

<verification>
- Readiness/loading regressions pass in frontend and backend suites.
- Sidebar/review/character behavior is keyed off campaign readiness, not pathname alone.
- Whole-card campaign load clicks are eliminated.
- The redundant creation-route sticky fallback is gone.
</verification>

<success_criteria>
- Users cannot enter review or character before the world is ready.
- Load dialog behavior matches the visible affordance: only the Load button loads.
- The shell no longer advertises misleading or duplicate navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-07-SUMMARY.md`
</output>
