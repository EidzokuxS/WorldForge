---
phase: 33-browser-e2e-verification-for-redesigned-creation-flows
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/non-game-shell/__tests__/app-shell.test.tsx
  - frontend/app/(non-game)/__tests__/layout.test.tsx
  - frontend/app/(non-game)/__tests__/page.test.tsx
  - frontend/app/(non-game)/library/__tests__/page.test.tsx
  - frontend/app/(non-game)/settings/__tests__/page.test.tsx
  - frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx
  - frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx
  - frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx
  - frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx
  - frontend/app/campaign/[id]/character/__tests__/page.test.tsx
  - frontend/app/settings/__tests__/page.test.tsx
  - backend/src/campaign/__tests__/manager.test.ts
  - shared/src/__tests__/settings.test.ts
autonomous: true
requirements: [P33-04]
gap_closure: true

must_haves:
  truths:
    - "npm --prefix frontend exec vitest run exits with 0 test failures in the main project"
    - "All test assertions match actual component markup after the flat-layout rewrite"
    - "No orphaned test files exist for deleted page routes"
  artifacts:
    - path: "frontend/components/non-game-shell/__tests__/app-shell.test.tsx"
      provides: "Shell contract tests aligned with flat layout"
    - path: "frontend/app/(non-game)/__tests__/page.test.tsx"
      provides: "Launcher test aligned with flat layout (no data-shell-surface)"
    - path: "backend/src/campaign/__tests__/manager.test.ts"
      provides: "premiseDivergence tests with personaTemplates field"
    - path: "shared/src/__tests__/settings.test.ts"
      provides: "settings default searchProvider=brave"
  key_links:
    - from: "frontend/components/non-game-shell/__tests__/app-shell.test.tsx"
      to: "frontend/components/non-game-shell/app-shell.tsx"
      via: "test assertions match rendered markup"
      pattern: "data-shell-region"
---

<objective>
Realign all failing test files to match the production markup after three post-approval flat-layout rewrite commits (4622090, f4dfc61, 0a09e34). Delete orphaned legacy test directories.

Purpose: Close the final VERIFICATION.md gap (criterion 4: "UX stable without blocking regressions") by making the full Vitest suite pass.
Output: 0 test failures in `npm --prefix frontend exec vitest run` for the main project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Realign 9 frontend test files to match flat-layout production markup</name>
  <files>
    frontend/components/non-game-shell/__tests__/app-shell.test.tsx,
    frontend/app/(non-game)/__tests__/layout.test.tsx,
    frontend/app/(non-game)/__tests__/page.test.tsx,
    frontend/app/(non-game)/library/__tests__/page.test.tsx,
    frontend/app/(non-game)/settings/__tests__/page.test.tsx,
    frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx,
    frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx,
    frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx,
    frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx
  </files>
  <read_first>
    frontend/components/non-game-shell/app-shell.tsx,
    frontend/components/non-game-shell/shell-primitives.tsx,
    frontend/components/non-game-shell/page-header.tsx,
    frontend/components/non-game-shell/app-sidebar.tsx,
    frontend/components/non-game-shell/campaign-status-provider.tsx,
    frontend/app/(non-game)/layout.tsx,
    frontend/app/(non-game)/page.tsx,
    frontend/app/(non-game)/settings/page.tsx,
    frontend/app/(non-game)/library/page.tsx,
    frontend/app/(non-game)/campaign/new/page.tsx,
    frontend/app/(non-game)/campaign/new/dna/page.tsx,
    frontend/app/(non-game)/campaign/[id]/character/page.tsx,
    frontend/app/(non-game)/campaign/[id]/review/page.tsx,
    frontend/components/campaign-new/concept-workspace.tsx,
    frontend/components/campaign-new/dna-workspace.tsx,
    frontend/components/library/library-workspace.tsx,
    frontend/components/character-creation/character-workspace.tsx,
    frontend/components/world-review/review-workspace.tsx
  </read_first>
  <action>
Rewrite each test file so every assertion matches the actual current production markup. The production code is correct; only tests need updating. Specific changes per file:

**1. `app-shell.test.tsx` (2 failing tests)**

The AppShell no longer renders `data-shell-region` attributes directly on a grid div, an inspector slot, a sticky action tray, eyebrow, description, or status props. The flat-layout AppShell renders:
- `ShellFrame` with `data-shell-region="outer-frame"`
- `ShellNavigationRail` with `data-shell-region="navigation-rail"`
- `ShellMainPanel` with `data-shell-region="main-panel"`
- `PageHeader` with just a title (derived from `titleForRoute(pathname)`) and optional `headerActions`
- A `<main role="main">` element containing children

The `inspector`, `stickyActions`, `stickyTitle`, `stickyDescription`, `eyebrow`, `description`, `status` props are all deprecated and IGNORED in the flat layout.

Rewrite test 1 ("renders Gap 1 shell hooks"):
- Mock `usePathname` to return `/settings` (already done)
- Mock `SidebarProvider`, `AppSidebar`, and `CampaignStatusProvider` or mock their dependencies
- The AppShell mock needs `SidebarProvider` from `@/components/ui/sidebar` to be available. Mock it as a passthrough.
- Assert: `screen.getByRole("main")` exists and contains "Settings body"
- Assert: `screen.getByRole("main").closest("[data-shell-region='main-panel']")` is not null (ShellMainPanel still has this)
- Assert: document contains element with `data-shell-region="outer-frame"` (ShellFrame still has this)
- Assert: document contains element with `data-shell-region="navigation-rail"` (ShellNavigationRail still has this)
- Remove assertions for `data-shell-region="action-tray"` (no action tray in flat layout)
- Remove assertions for `data-shell-region="frame"` and `data-shell-region="nav-rail"` and `data-shell-region="main"` (old names)
- Remove assertions for inspector text, grid cols, "Workspace" heading, "Provider and Role Settings" description
- Assert the page header renders "Settings" as the title (from `titleForRoute("/settings")`)
- Remove `getByTestId("shell-grid")` assertion (no such testid in flat layout)

Rewrite test 2 ("exposes route context and action labels"):
- The flat-layout AppShell ignores `eyebrow`, `description`, and `status` props entirely (they are deprecated)
- Assert: `screen.getByText("Provider Settings")` exists when `title="Provider Settings"` is passed (overrides route-derived title)
- Assert: `screen.getByRole("button", { name: "Reconnect" })` exists when passed as `headerActions`
- Assert: `screen.getByRole("main")` contains "Child slot"
- Remove assertions for "Route Context" (eyebrow), "Route-owned shell frame" (description), "Autosave active" (status) since those props are ignored
- Remove the `queryByLabelText(/campaign name/i)` assertion (unnecessary, still valid but keep it if desired)

Note: AppShell imports `SidebarProvider` from `@/components/ui/sidebar` and `AppSidebar` from `@/components/non-game-shell/app-sidebar`. AppSidebar needs `useCampaignStatus` from `campaign-status-provider`. Mock `@/components/non-game-shell/campaign-status-provider` to return `{ useCampaignStatus: () => ({ loading: false, generationReady: false, campaignId: null, campaign: null, reviewAvailable: false, characterAvailable: false }) }`. Mock `next/navigation` to provide both `usePathname` and `useRouter`.

**2. `layout.test.tsx` (1 failing test)**

The layout now wraps children in `CampaignStatusProvider` > `AppShell`. The test asserts "Campaign Launchpad" and 2x "New Campaign" which no longer exist in the flat layout. The AppShell for pathname `/` renders PageHeader with title "Launchpad" (from `titleForRoute("/")`), and the sidebar renders "New Campaign" once as a nav link.

Mock `@/components/non-game-shell/campaign-status-provider` as passthrough: `CampaignStatusProvider: ({ children }: { children: React.ReactNode }) => <>{children}</>` and `useCampaignStatus: () => ({ loading: false, generationReady: false, campaignId: null, campaign: null, reviewAvailable: false, characterAvailable: false })`.
Mock `next/navigation` to return `usePathname: () => "/"` AND `useRouter: () => ({ push: vi.fn() })`.
Mock `@/lib/api` to provide `getActiveCampaign: vi.fn().mockResolvedValue(null)` and `loadCampaign: vi.fn()`.

Update assertions:
- Assert `screen.getByText("Launchpad")` exists (PageHeader title for `/`)
- Assert `screen.getByRole("main")` contains "Launchpad child"
- Remove assertion for "Campaign Launchpad" (not rendered)
- Assert sidebar contains "New Campaign" link (only 1 instance now, not 2)

**3. `page.test.tsx` (LauncherPage - 1 failing test)**

The LauncherPage no longer renders `data-shell-surface="panel"` panels or text "Primary flow", "Resume flow", "Shell Shortcuts". It renders a flat list with "New Campaign" button-link, "Load Campaign" button, "Recent Campaigns" section label, and a campaign list.

Mock `@/lib/api` to provide `apiGet: vi.fn().mockResolvedValue([])`, `apiDelete: vi.fn()`, `loadCampaign: vi.fn()`.
Mock `next/navigation` to provide `useRouter: () => ({ push: vi.fn() })`.
Mock `sonner` as `{ toast: { success: vi.fn(), error: vi.fn() } }`.
Remove the LoadCampaignDialog mock (no longer imported by LauncherPage).

Update assertions:
- Assert `screen.getByRole("link", { name: "New Campaign" })` with `href="/campaign/new"` (still present)
- Assert `screen.getByText("Recent Campaigns")` exists (section label)
- Remove all `data-shell-surface="panel"` assertions
- Remove assertions for "Primary flow", "Resume flow", "Shell Shortcuts", "Load Campaign Dialog"

**4. `library/__tests__/page.test.tsx` (1 failing test)**

LibraryPage now renders `<LibraryWorkspace />` which does NOT have a "Back to Campaign Creation" link. It has an "Import Worldbook" button and a worldbook table.

Mock `@/lib/api` to provide `listWorldbookLibrary` and `importWorldbookLibrary`.
Mock `sonner`.

Update assertions:
- Assert `screen.getByText("Alpha Codex")` exists (worldbook name in table)
- Remove assertion for `getByRole("link", { name: "Back to Campaign Creation" })` (does not exist in LibraryWorkspace)
- Assert something representative of the new UI, e.g. `screen.getByText("Import Worldbook")` exists

**5. `settings/__tests__/page.test.tsx` (non-game route, 1 failing test)**

SettingsPage no longer renders "Changes are saved automatically." or "Active providers: 1". It now shows a green status dot with "Saved" (or "Saving...") and tab triggers.

Update assertions:
- Replace `getByText("Changes are saved automatically.")` with `getByText("Saved")` (the auto-save indicator)
- Replace `getByText(/Active providers: 1/)` with checking tabs exist: `getByText("Providers")` is present
- Keep `getByText("Providers")` assertion

**6. `campaign/new/__tests__/page.test.tsx` (1 failing test)**

CampaignConceptPage renders `<ConceptWorkspace />` which is mocked via `useCampaignNewFlow`. The test assertion `screen.getByText("Source Library")` fails because the workspace now renders "Sources" (not "Source Library").

Update the failing assertion:
- Replace `screen.getByText("Source Library")` with `screen.getByText("Sources")`

The other 2 tests in this file (wizard guarded DNA startup, validation message) should still pass since ConceptWorkspace renders "Continue to DNA" button and the validation message text is unchanged. Verify by running the tests.

**7. `campaign/new/dna/__tests__/page.test.tsx` (2 failing tests)**

DnaPage renders `<DnaWorkspace />` which calls `useCampaignNewFlow()`. The mock is missing `handleSeedToggle` and `handlePrepareManualDna` methods that the DnaWorkspace calls.

Test "renders routed DNA editing on persisted flow state" fails because:
- DnaWorkspace renders the heading text "World DNA" only as a category label, not as a standalone heading. It does NOT render a literal "World DNA" text. Check actual DnaWorkspace output. The WORLD_DNA_CARDS labels are the category headings (Geography, Political Structure, etc.). There is no standalone "World DNA" text in DnaWorkspace. But the test expects `screen.getByText("World DNA")`. This may come from the page header rendered by AppShell (title "New Campaign" for the `/campaign/new/dna` route). But DnaPage does NOT render inside AppShell in tests (it's imported directly). So the test needs to assert on what DnaWorkspace actually renders.

Fix: Add missing mock methods `handleSeedToggle: vi.fn()` and `handlePrepareManualDna: vi.fn()` to the flowState mock. Also add `handleSeedToggle` type: `handleSeedToggle: vi.fn()` and `handlePrepareManualDna: vi.fn()`.

Update test "renders routed DNA editing on persisted flow state":
- Replace `screen.getByText("World DNA")` with `screen.getByText("Geography")` (first WORLD_DNA_CARDS label) or another category label that proves DNA grid is rendering
- Keep `screen.getByText(/Storm coast/)` assertion (still rendered in textarea)
- Keep `screen.getByRole("button", { name: "Create World" })` assertion (button text is "Create World" when not busy)

Update test "replaces the dead fallback with actionable recovery guidance when DNA is still empty":
- Replace the long expected message `"World DNA has not been prepared yet. Go back to concept, continue into DNA, or start with at least one manual seed before generating."` with what actually renders: `"World DNA has not been prepared yet."` and `"Go back to concept, continue into DNA, or start with manual seeds."` (two separate elements). Use: `screen.getByText("World DNA has not been prepared yet.")` to verify presence.
- Keep the `queryByText("DNA suggestions are not ready yet...")` negative assertion (still valid)
- Add `screen.getByRole("button", { name: "Start With Manual DNA" })` assertion to verify the recovery CTA

**8. `campaign/[id]/character/__tests__/page.test.tsx` (non-game route, 1 failing test)**

The character page no longer renders "Input Methods", "Draft Summary", `data-shell-surface="rail"`, `data-shell-surface="panel"`, or `data-shell-region="action-tray"`. It renders CharacterWorkspace > CharacterForm + CharacterCard + a footer bar with "Back to Review" and "Save & Begin Adventure" buttons.

Rewrite test "pins Gap 1 character navigation, summary, and action tray to shared shell primitives":
- After load, assert `screen.getByTestId("character-form")` exists (CharacterForm mock renders this)
- Assert `screen.getByText("Use the entry methods above to parse, generate, or import a character.")` exists (no-draft message)
- Assert `screen.getByRole("button", { name: "Save & Begin Adventure" })` exists and is disabled (no characterDraft)
- Assert `screen.getByRole("link", { name: "Back to Review" })` exists
- Remove assertions for "Input Methods", "Draft Summary", "Awaiting Draft", "Begin Adventure", `data-shell-surface`, `data-shell-region`

Keep test "blocks character creation when the backend reports world generation is not ready" but update:
- Replace `queryByRole("button", { name: "Begin Adventure" })` with `queryByRole("button", { name: /Save.*Begin Adventure/ })`

**9. `campaign/[id]/review/__tests__/page.test.tsx` (1 failing test)**

The review page no longer renders "Sections", "Review Summary", `data-shell-surface="rail"`, `data-shell-surface="panel"`, or `data-shell-region="action-tray"`. It renders ReviewWorkspace > Tabs (Premise, Locations, Factions, NPCs, Lore) + footer with "Launchpad" link and "Continue to Character" button.

Rewrite test "pins Gap 1 review navigation, summary, and action tray to shared shell primitives":
- After load, assert `screen.getByText("Premise Section")` exists (mocked PremiseSection)
- Assert tab triggers exist: `screen.getByText(/Premise/)`, `screen.getByText(/Locations/)`, etc.
- Assert `screen.getByRole("button", { name: "Continue to Character" })` exists (renamed from "Continue to Character Creation")
- Assert `screen.getByRole("link", { name: "Launchpad" })` exists
- Remove assertions for "Sections", "Review Summary", `data-shell-surface`, `data-shell-region`

Keep test "blocks world review when the campaign is not generation-ready" as-is (this test should still pass since the generationRequired flow is unchanged).
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix frontend exec vitest run -- --reporter=verbose frontend/components/non-game-shell/__tests__/app-shell.test.tsx frontend/app/\(non-game\)/__tests__/layout.test.tsx frontend/app/\(non-game\)/__tests__/page.test.tsx frontend/app/\(non-game\)/library/__tests__/page.test.tsx frontend/app/\(non-game\)/settings/__tests__/page.test.tsx frontend/app/\(non-game\)/campaign/new/__tests__/page.test.tsx frontend/app/\(non-game\)/campaign/new/dna/__tests__/page.test.tsx "frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 9 test files pass: 0 FAIL lines in output for these paths
    - app-shell.test.tsx contains `data-shell-region="outer-frame"` assertion (verifies ShellFrame renders)
    - app-shell.test.tsx contains `data-shell-region="navigation-rail"` assertion (verifies ShellNavigationRail renders)
    - app-shell.test.tsx contains `data-shell-region="main-panel"` assertion (verifies ShellMainPanel renders)
    - app-shell.test.tsx does NOT contain `data-shell-region="action-tray"` assertion (removed in flat layout)
    - app-shell.test.tsx does NOT contain `getByTestId("shell-grid")` assertion
    - page.test.tsx (launcher) does NOT contain `data-shell-surface="panel"` assertion
    - page.test.tsx (launcher) contains `getByText("Recent Campaigns")` or `getByRole("link", { name: "New Campaign" })`
    - character page test contains `Save & Begin Adventure` (not "Begin Adventure")
    - review page test contains `Continue to Character` (not "Continue to Character Creation")
    - dna page test mock contains `handleSeedToggle: vi.fn()` and `handlePrepareManualDna: vi.fn()`
    - concept page test contains `getByText("Sources")` (not "Source Library")
  </acceptance_criteria>
  <done>All 9 non-game frontend test files pass with assertions that match the current flat-layout production markup</done>
</task>

<task type="auto">
  <name>Task 2: Fix backend/shared test failures and delete orphaned legacy test directories</name>
  <files>
    backend/src/campaign/__tests__/manager.test.ts,
    shared/src/__tests__/settings.test.ts,
    frontend/app/campaign/[id]/character/__tests__/page.test.tsx,
    frontend/app/settings/__tests__/page.test.tsx
  </files>
  <read_first>
    backend/src/campaign/manager.ts,
    backend/src/campaign/__tests__/manager.test.ts,
    shared/src/__tests__/settings.test.ts,
    shared/src/settings.ts,
    frontend/app/campaign/[id]/character/__tests__/page.test.tsx,
    frontend/app/settings/__tests__/page.test.tsx
  </read_first>
  <action>
**1. Fix `backend/src/campaign/__tests__/manager.test.ts` (2 failing tests)**

`readCampaignConfig` now returns `personaTemplates: []` and `worldbookSelection: undefined` as additional properties. The tests use `.toEqual()` which requires exact match.

Test "readCampaignConfig preserves legacy excludedCharacters beside cached premiseDivergence" (line ~685):
- Add `personaTemplates: []` and `worldbookSelection: undefined` to the expected object:
```typescript
expect(readCampaignConfig("test-id")).toEqual({
  name: "Test",
  premise: "P",
  createdAt: 1000,
  updatedAt: 1000,
  generationComplete: false,
  ipContext: {
    ...cachedIpContext,
    excludedCharacters: ["Dr. Kel"],
  },
  premiseDivergence,
  personaTemplates: [],
  worldbookSelection: undefined,
  currentTick: undefined,
  seeds: undefined,
});
```

Test "readCampaignConfig preserves worldbookSelection beside ipContext and premiseDivergence" (line ~725):
- Add `personaTemplates: []` to the expected object:
```typescript
expect(readCampaignConfig("test-id")).toEqual({
  name: "Test",
  premise: "",
  createdAt: 1000,
  updatedAt: 1000,
  generationComplete: false,
  ipContext: cachedIpContext,
  premiseDivergence,
  worldbookSelection,
  personaTemplates: [],
  currentTick: undefined,
  seeds: undefined,
});
```

**2. Fix `shared/src/__tests__/settings.test.ts` (1 failing test)**

The default `searchProvider` changed from `"duckduckgo"` to `"brave"` in `shared/src/settings.ts` line 93.

Update the test "defaults searchProvider to 'duckduckgo'" (line ~339):
- Change test name to "defaults searchProvider to 'brave'"
- Change assertion from `expect(settings.research.searchProvider).toBe("duckduckgo")` to `expect(settings.research.searchProvider).toBe("brave")`

**3. Delete orphaned legacy test directories**

Two test directories exist for page routes that no longer have a `page.tsx`:
- `frontend/app/campaign/[id]/character/__tests__/` — the legacy character route page was deleted; only the test directory remains
- `frontend/app/settings/__tests__/` — the legacy settings route page was deleted; only the test directory remains

Delete both directories entirely:
```bash
rm -rf "frontend/app/campaign/[id]/character/__tests__"
rm -rf "frontend/app/settings/__tests__"
```

Also clean up empty parent directories if they become empty after deletion:
- Check if `frontend/app/campaign/[id]/character/` is empty after removing `__tests__/` — if so, delete up through `frontend/app/campaign/[id]/character/`
- Check if `frontend/app/settings/` is empty after removing `__tests__/` — if so, delete `frontend/app/settings/`
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix frontend exec vitest run -- --reporter=verbose backend/src/campaign/__tests__/manager.test.ts shared/src/__tests__/settings.test.ts 2>&1 | tail -10 && echo "---" && ls "frontend/app/campaign/[id]/character/__tests__/" 2>&1 && ls "frontend/app/settings/__tests__/" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - manager.test.ts contains `personaTemplates: []` in both premiseDivergence persistence expected objects
    - settings.test.ts contains `.toBe("brave")` (not "duckduckgo")
    - `frontend/app/campaign/[id]/character/__tests__/` directory does not exist
    - `frontend/app/settings/__tests__/` directory does not exist
    - Both backend test files pass with 0 failures
  </acceptance_criteria>
  <done>Backend/shared tests pass, legacy orphaned test directories deleted, zero test failures remain</done>
</task>

</tasks>

<verification>
Run the full frontend Vitest suite scoped to the main project (exclude worktree paths):
```bash
cd R:/Projects/WorldForge && npm --prefix frontend exec vitest run 2>&1 | grep "FAIL " | grep -v worktrees
```
Expected: zero FAIL lines from the main project.

Also verify the full suite summary:
```bash
cd R:/Projects/WorldForge && npm --prefix frontend exec vitest run 2>&1 | grep "Test Files"
```
Expected: The number of failed test files drops from 57 to 41 (the 16 main-project failures are resolved; worktree failures are not our concern).
</verification>

<success_criteria>
1. `npm --prefix frontend exec vitest run` produces 0 FAIL lines for main-project test files (filtering out `.claude/worktrees/` paths)
2. All 9 rewritten frontend test files pass
3. Both backend test files (manager, settings) pass
4. Orphaned `frontend/app/campaign/[id]/character/__tests__/` and `frontend/app/settings/__tests__/` directories are deleted
5. No production code was changed — only test files modified or deleted
</success_criteria>

<output>
After completion, create `.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-13-SUMMARY.md`
</output>
