---
phase: 33-browser-e2e-verification-for-redesigned-creation-flows
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/world-review/__tests__/lore-section.test.tsx
  - .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-12-SUMMARY.md
autonomous: true
requirements: [P33-04]
gap_closure: true
must_haves:
  truths:
    - "`npm --prefix frontend run lint` exits 0 for the current Phase 33 baseline."
    - "The pre-existing `lore-section.test.tsx` lint debt is removed without changing world-review behavior."
    - "The lore-section test file still runs after the typing cleanup."
  artifacts:
    - path: "frontend/components/world-review/__tests__/lore-section.test.tsx"
      provides: "Typed lore-section test doubles and assertions without `no-explicit-any` violations"
    - path: ".planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-12-SUMMARY.md"
      provides: "Recorded lint rerun proof for the remaining Phase 33 verification gap"
  key_links:
    - from: "frontend/components/world-review/__tests__/lore-section.test.tsx"
      to: "npm --prefix frontend run lint"
      via: "eslint no-explicit-any rule on the Phase 33 baseline"
      pattern: "any"
---

<objective>
Close the remaining verification gap from `33-VERIFICATION.md` by removing the `no-explicit-any` debt in `lore-section.test.tsx` and rerunning frontend lint to green.

Purpose: Phase 33 cannot close cleanly while its own verification report still records an unresolved lint failure. This plan fixes only that pre-existing test-file debt and re-proves lint, without reopening unrelated frontend work.
Output: Typed lore-section test doubles plus a clean frontend lint rerun recorded in `33-12-SUMMARY.md`.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md
@frontend/components/world-review/__tests__/lore-section.test.tsx
@frontend/components/world-review/lore-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the `no-explicit-any` debt from lore-section.test.tsx</name>
  <files>
    frontend/components/world-review/__tests__/lore-section.test.tsx
  </files>
  <read_first>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md,
    frontend/components/world-review/__tests__/lore-section.test.tsx,
    frontend/components/world-review/lore-section.tsx
  </read_first>
  <action>
    Fix only the unresolved lint artifact called out in `33-VERIFICATION.md`: replace the `any`-typed mocks, fixtures, and helper casts in `frontend/components/world-review/__tests__/lore-section.test.tsx` with proper typed test doubles or narrowly scoped `unknown` conversions.

    Constraints:
    - Preserve the existing behavioral intent of the lore-section tests; this is a typing cleanup, not a feature rewrite.
    - Keep the fix local to the test file unless the component's exported types provide a clearly better test contract.
    - Do not suppress the lint rule, add eslint-disable comments, or loosen project-wide lint configuration.
  </action>
  <verify>
    <automated>npm --prefix frontend run lint -- "frontend/components/world-review/__tests__/lore-section.test.tsx" && npm --prefix frontend exec vitest run "frontend/components/world-review/__tests__/lore-section.test.tsx"</automated>
  </verify>
  <done>The lore-section test file is fully typed enough to satisfy eslint without changing its behavioral coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run frontend lint and record closure of the Phase 33 verification gap</name>
  <files>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-12-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md,
    frontend/components/world-review/__tests__/lore-section.test.tsx
  </read_first>
  <action>
    Re-run the relevant lint proof after Task 1 is green and capture the result in `33-12-SUMMARY.md`.

    The summary must state:
    - that the unresolved `33-VERIFICATION.md` lint gap is closed
    - the exact frontend lint command used and its exit status
    - whether the targeted lore-section test still passes after the typing cleanup

    If lint still fails elsewhere, stop with that concrete remaining file list instead of pretending the gap is closed.
  </action>
  <verify>
    <automated>npm --prefix frontend run lint && npm --prefix frontend exec vitest run "frontend/components/world-review/__tests__/lore-section.test.tsx"</automated>
  </verify>
  <done>The phase has a recorded green frontend lint rerun, so the last verification gap is no longer hanging open.</done>
</task>

</tasks>

<verification>
- `frontend/components/world-review/__tests__/lore-section.test.tsx` no longer triggers `no-explicit-any`.
- `npm --prefix frontend run lint` exits 0 on the current baseline.
- The targeted lore-section test still passes after the typing cleanup.
</verification>

<success_criteria>
- The remaining `33-VERIFICATION.md` gap is closed explicitly, not ignored.
- Frontend lint is green before final Phase 33 approval.
- The fix stays local to the pre-existing test debt and does not widen into unrelated frontend changes.
</success_criteria>

<output>
After completion, create `.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-12-SUMMARY.md`
</output>
