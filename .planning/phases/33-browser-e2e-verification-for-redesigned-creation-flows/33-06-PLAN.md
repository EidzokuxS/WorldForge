---
phase: 33-browser-e2e-verification-for-redesigned-creation-flows
plan: 06
type: execute
wave: 2
depends_on: [33-05]
files_modified:
  - frontend/components/campaign-new/flow-session.ts
  - frontend/components/campaign-new/flow-provider.tsx
  - frontend/components/title/use-new-campaign-wizard.ts
  - frontend/app/(non-game)/campaign/new/page.tsx
  - frontend/components/campaign-new/concept-workspace.tsx
  - frontend/components/campaign-new/dna-workspace.tsx
  - frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx
  - frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx
  - frontend/components/campaign-new/__tests__/flow-provider.test.tsx
autonomous: true
requirements: [P33-02, P33-04]
gap_closure: true
must_haves:
  truths:
    - "Leaving and returning to the routed `/campaign/new` flow does not wipe concept or DNA state."
    - "Continue from concept triggers the wizard's DNA suggestion startup instead of routing into a dead 'not ready yet' screen."
    - "World DNA and world generation progress are visible while work is in flight."
  artifacts:
    - path: "frontend/components/campaign-new/flow-session.ts"
      provides: "Durable routed creation-flow persistence outside the route subtree"
    - path: "frontend/components/campaign-new/flow-provider.tsx"
      provides: "Hydrated routed wizard state backed by persistence"
    - path: "frontend/components/campaign-new/dna-workspace.tsx"
      provides: "Visible suggestion and generation progress states"
  key_links:
    - from: "frontend/app/(non-game)/campaign/new/page.tsx"
      to: "frontend/components/title/use-new-campaign-wizard.ts"
      via: "handleNextToDna instead of raw route push"
      pattern: "handleNextToDna"
    - from: "frontend/components/campaign-new/flow-provider.tsx"
      to: "frontend/components/campaign-new/flow-session.ts"
      via: "session hydrate/persist cycle"
      pattern: "flow-session"
    - from: "frontend/components/title/use-new-campaign-wizard.ts"
      to: "frontend/components/campaign-new/dna-workspace.tsx"
      via: "generationProgress and phase state rendered in routed UI"
      pattern: "generationProgress"
---

<objective>
Close the routed concept/DNA orchestration half of UAT Gap 2 by making creation-flow state durable, reusing the wizard's guarded handlers, and rendering visible suggestion/generation progress on the routed pages.

Purpose: The routed creation flow currently bypasses `handleNextToDna`, stores concept state only in route-local React memory, and tracks progress without ever rendering it. This plan restores the original wizard orchestration while keeping the routed Phase 32 UX.
Output: Persisted routed creation flow with guarded DNA startup and visible progress/loading states.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-UAT.md
@.planning/debug/phase-33-uat-gap-2.md
@frontend/components/campaign-new/flow-provider.tsx
@frontend/components/title/use-new-campaign-wizard.ts
@frontend/app/(non-game)/campaign/new/page.tsx
@frontend/components/campaign-new/concept-workspace.tsx
@frontend/components/campaign-new/dna-workspace.tsx

<interfaces>
From shared/src/types.ts:
```typescript
export interface CampaignMeta {
  id: string;
  name: string;
  premise: string;
  createdAt: number;
  updatedAt: number;
  seeds?: WorldSeeds;
  generationComplete?: boolean;
}
```

From frontend/components/title/use-new-campaign-wizard.ts:
```typescript
async function handleNextToDna(): Promise<void>;
async function handleCreateWithSeeds(seeds?: Partial<WorldSeeds>): Promise<void>;
async function handleCreateWithDna(): Promise<void>;
const generationProgress: GenerationProgress | null;
const creatingCampaign: boolean;
const isGenerating: boolean;
const isSuggesting: boolean;
const canCreate: boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add routed creation-flow regressions for persistence and guarded DNA startup</name>
  <files>
    frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx,
    frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx,
    frontend/components/campaign-new/__tests__/flow-provider.test.tsx
  </files>
  <read_first>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-UAT.md,
    .planning/debug/phase-33-uat-gap-2.md,
    frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx,
    frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx,
    frontend/components/campaign-new/flow-provider.tsx,
    frontend/components/title/use-new-campaign-wizard.ts,
    frontend/app/(non-game)/campaign/new/page.tsx,
    frontend/components/campaign-new/concept-workspace.tsx,
    frontend/components/campaign-new/dna-workspace.tsx
  </read_first>
  <behavior>
    - Test 1: Clicking Continue on the routed concept page calls the wizard's guarded DNA startup path instead of a bare `router.push("/campaign/new/dna")`.
    - Test 2: Flow provider rehydrates previously entered concept/DNA state from durable storage when the subtree remounts.
    - Test 3: Routed DNA UI shows an in-flight suggestion/generation state rather than the dead-end "DNA suggestions are not ready yet" fallback when work is active or state was persisted.
    - Test 4: Browser back/forward navigation between `/campaign/new` and `/campaign/new/dna` preserves the routed flow state instead of wiping concept or DNA inputs.
    - Test 5: Invalid concept inputs and an empty/manual DNA state surface actionable validation instead of allowing a dead-end continue/generate path.
  </behavior>
  <action>
    Add focused frontend regressions that pin the routed-creation behavior required by UAT Gap 2.

    Per D-01 and D-04, the tests must cover the concept-to-DNA handoff, persistence across leaving/returning plus browser back navigation, visible progress states, and invalid/empty-input recovery. Keep them restricted to the routed creation surface; do not add library coverage (D-02).
  </action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx" "frontend/components/campaign-new/__tests__/flow-provider.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail against the current bare-route-push implementation and pass after the fix.
    - At least one test asserts durable flow rehydration after provider remount.
    - At least one test asserts the routed DNA screen renders loading/progress UI instead of the dead fallback during in-flight work.
    - At least one test asserts browser back/forward navigation does not wipe routed creation state.
    - At least one test asserts invalid concept input or empty DNA blocks progress with clear validation.
  </acceptance_criteria>
  <done>Frontend regressions describe the exact routed creation-flow failures recorded in UAT before implementation changes begin.</done>
</task>

<task type="auto">
  <name>Task 2: Restore durable routed wizard orchestration and render progress</name>
  <files>
    frontend/components/campaign-new/flow-session.ts,
    frontend/components/campaign-new/flow-provider.tsx,
    frontend/components/title/use-new-campaign-wizard.ts,
    frontend/app/(non-game)/campaign/new/page.tsx,
    frontend/components/campaign-new/concept-workspace.tsx,
    frontend/components/campaign-new/dna-workspace.tsx
  </files>
  <read_first>
    .planning/debug/phase-33-uat-gap-2.md,
    frontend/components/campaign-new/flow-provider.tsx,
    frontend/components/title/use-new-campaign-wizard.ts,
    frontend/app/(non-game)/campaign/new/page.tsx,
    frontend/components/campaign-new/concept-workspace.tsx,
    frontend/components/campaign-new/dna-workspace.tsx
  </read_first>
  <action>
    Implement the routed-creation fix from Gap 2 without inventing a second wizard:

    - Add `frontend/components/campaign-new/flow-session.ts` (or equivalent) to persist the routed concept/DNA state outside the `/campaign/new` subtree. Persist at minimum: campaign name, premise, franchise, research flag, selected worldbooks, `dnaState`, current phase/step, and any in-flight progress snapshot needed to restore the UI after route churn.
    - Update `CampaignNewFlowProvider` to hydrate from that storage on mount, persist relevant state changes, and clear the persisted session only after a successful completion/reset.
    - Replace the routed concept page's bare `router.push("/campaign/new/dna")` with the wizard's guarded `handleNextToDna()` flow. Only navigate when the routed wizard actually enters the DNA step or has a recoverable fallback state ready.
    - Update `ConceptWorkspace` and `DnaWorkspace` so the routed UI surfaces `isSuggesting`, `creatingCampaign`, `isGenerating`, and `generationProgress` with visible loader/progress/status copy. Buttons must disable during in-flight work and communicate the active step.
    - Preserve routed state when the user navigates backward/forward between concept and DNA, and make invalid concept submissions or empty/manual DNA states fail with clear UI validation instead of a dead empty page. This is explicit D-04 coverage, not optional polish.
    - Keep the existing `useNewCampaignWizard` orchestration authoritative. Do not fork a second state machine for routed pages.

    This task is specifically the persistence/progress/orchestration fix for UAT Gap 2, per D-01 and D-11.
  </action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx" "frontend/components/campaign-new/__tests__/flow-provider.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Leaving `/campaign/new` and returning restores the last entered concept/DNA state instead of resetting to blank fields.
    - `frontend/app/(non-game)/campaign/new/page.tsx` no longer contains a raw `router.push("/campaign/new/dna")` handoff.
    - Routed concept and DNA pages render visible loading/progress states while suggestions or world generation are running.
    - Browser back/forward navigation between concept and DNA preserves the routed flow state.
    - Invalid concept submissions or empty/manual DNA states show actionable validation and do not strand the user on a dead DNA screen.
    - Browser-verifiable outcome: Continue to DNA shows a loader/progress state instead of dropping the user onto a dead "not ready" page.
  </acceptance_criteria>
  <done>The routed concept and DNA pages reuse the original wizard's guarded orchestration, preserve state across route churn, and show visible in-flight progress.</done>
</task>

</tasks>

<verification>
- Routed creation regressions pass.
- The concept page no longer bypasses `handleNextToDna`.
- Durable flow storage exists and is used by the provider.
- DNA/generation progress is visible in the routed UI.
</verification>

<success_criteria>
- The routed creation flow preserves user input across leaving and returning.
- Continue to DNA actually starts DNA suggestion work and exposes visible progress.
- The routed concept/DNA UX is usable again before any review/character handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-06-SUMMARY.md`
</output>
