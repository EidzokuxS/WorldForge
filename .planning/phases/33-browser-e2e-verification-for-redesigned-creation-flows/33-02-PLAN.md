---
phase: 33-browser-e2e-verification-for-redesigned-creation-flows
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified: []
autonomous: true
requirements: [P33-01, P33-02, P33-03]
must_haves:
  truths:
    - "User can create a new original-world campaign from the shell launcher"
    - "User can enter a premise and proceed to World DNA step"
    - "User can generate or skip World DNA seeds"
    - "User can trigger scaffold generation and see progress"
    - "Generated world scaffold produces locations, factions, and NPCs"
    - "User is redirected to world review after generation completes"
  artifacts:
    - path: "frontend/app/(non-game)/campaign/new/page.tsx"
      provides: "Campaign creation concept step"
    - path: "frontend/app/(non-game)/campaign/new/dna/page.tsx"
      provides: "World DNA seed configuration step"
    - path: "frontend/app/(non-game)/campaign/[id]/review/page.tsx"
      provides: "World review page after generation"
  key_links:
    - from: "launcher /"
      to: "/campaign/new"
      via: "New Campaign button"
      pattern: "href.*campaign/new"
    - from: "/campaign/new"
      to: "/campaign/new/dna"
      via: "proceed after premise entry"
    - from: "/campaign/new/dna"
      to: "POST /api/worldgen/generate"
      via: "generate button triggers scaffold generation"
    - from: "scaffold generation"
      to: "/campaign/[id]/review"
      via: "redirect after generation completes"
---

<objective>
Verify the complete original-world campaign creation flow end-to-end in a real browser: launcher -> concept entry -> World DNA -> scaffold generation -> world review landing.

Purpose: Validate the primary creation pipeline (D-01 flow 1) works on the redesigned shell routes with real LLM calls. This covers the original-world smoke test (D-03).
Output: Original-world campaign created and generated through the full pipeline via PinchTab browser automation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md
@.planning/phases/32-desktop-first-non-game-ui-overhaul/32-02-SUMMARY.md
@frontend/app/(non-game)/campaign/new/page.tsx
@frontend/app/(non-game)/campaign/new/dna/page.tsx
@frontend/app/(non-game)/campaign/new/layout.tsx
@frontend/components/non-game-shell/app-shell.tsx
@frontend/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E campaign creation with original-world premise through World DNA</name>
  <files></files>
  <read_first>
    frontend/app/(non-game)/campaign/new/page.tsx,
    frontend/app/(non-game)/campaign/new/dna/page.tsx,
    frontend/app/(non-game)/campaign/new/layout.tsx,
    frontend/components/campaign-new/concept-step.tsx,
    frontend/lib/api.ts
  </read_first>
  <action>
    Ensure PinchTab is running (BRIDGE_HEADLESS=true pinchtab), backend on :3001, frontend on :3000.

    Using PinchTab, execute the original-world campaign creation flow:

    1. **Navigate to /campaign/new**:
       - Load `http://localhost:3000/campaign/new`
       - Confirm the campaign creation page renders inside the shell
       - Identify the premise/concept input field in the accessibility tree

    2. **Enter an original-world premise** (no known IP):
       - Type or set value: "A dying world where reality fractures into unstable pocket dimensions. Survivors scavenge between rifts while ancient machines slowly awaken."
       - Confirm the text appears in the input
       - Find and click the "Next" or "Continue" or "Proceed to DNA" button to advance

    3. **World DNA step**:
       - Confirm navigation to `/campaign/new/dna` or DNA step renders
       - Confirm DNA seed categories are visible (epoch, geography, culturalFlavor, etc.)
       - If "AI Suggest" buttons exist, click one to trigger AI seed generation (real LLM call via GLM)
       - Wait for AI suggestion to complete (up to 60s timeout per D-06 retry tolerance)
       - Confirm at least one DNA category has a populated value
       - If AI suggestion fails after retries, manually verify the seed UI renders correctly and proceed

    4. **Trigger scaffold generation**:
       - Find and click the "Generate World" or equivalent button
       - Confirm generation progress indicators appear (SSE progress events)
       - Wait for generation to complete (this involves multiple real LLM calls -- allow up to 5 minutes)
       - The generation pipeline is: premise -> locations -> factions -> NPCs -> lore extraction

    5. **Verify redirect to world review**:
       - After generation completes, confirm navigation to `/campaign/[id]/review`
       - Confirm the review page renders inside the shell
       - Confirm at least one location, one faction, and one NPC are visible in the review sections

    Per D-04: Also test error states:
    - If any step shows an error toast or broken UI state, document it, fix the code, and re-test
    - Verify that back navigation from DNA step returns to concept step without losing premise

    Per D-07: Fix bugs inline as found. Every bug discovered must be fixed and re-tested immediately.
    Per D-05: All LLM calls are real (GLM provider). No mocks.
    Per D-06: Retry up to 3 times on LLM failure before marking as failed.
  </action>
  <verify>
    <automated>curl -s http://localhost:3001/api/campaigns | grep -q "campaigns" && echo "PASS: campaigns API responds" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Campaign creation page at `/campaign/new` renders inside shell (shell sidebar visible)
    - Premise text can be entered into the concept input
    - DNA step renders with seed category controls
    - Scaffold generation triggers and shows progress
    - Generation completes without unhandled errors
    - Review page at `/campaign/[id]/review` shows generated locations, factions, and NPCs
    - At least 1 location, 1 faction, and 1 NPC visible in review after generation
    - Shell sidebar remains visible throughout the entire flow
  </acceptance_criteria>
  <done>Complete original-world campaign creation flow works end-to-end in browser: premise -> DNA -> generate -> review. All bugs found are fixed and re-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Edge case and error state verification for campaign creation</name>
  <files></files>
  <read_first>
    frontend/app/(non-game)/campaign/new/page.tsx,
    frontend/app/(non-game)/campaign/new/dna/page.tsx,
    frontend/lib/api.ts,
    backend/src/routes/campaigns.ts
  </read_first>
  <action>
    Using PinchTab, verify edge cases and error states per D-04:

    1. **Empty premise submission**:
       - Navigate to `/campaign/new`
       - Try to proceed without entering any premise text
       - Confirm the UI prevents advancement or shows a validation error
       - Should NOT create a campaign with empty premise

    2. **Back navigation from DNA**:
       - Enter a premise, proceed to DNA step
       - Navigate back (browser back or UI back button)
       - Confirm premise text is preserved (not lost)
       - Proceed forward again and confirm DNA step still works

    3. **Skip World DNA**:
       - If there is a "Skip DNA" or "Generate without DNA" option, test it
       - Confirm generation proceeds without DNA seeds
       - If no skip option exists, verify all DNA categories show defaults or are optional

    4. **Cancel mid-generation** (if applicable):
       - Start scaffold generation
       - If there is a cancel button, click it during generation
       - Confirm the UI returns to a usable state (not stuck in loading)

    5. **Load existing campaign from launcher**:
       - Return to launcher at `/`
       - If a "Load Campaign" dialog exists, open it
       - Confirm the previously created campaign appears in the list
       - Load it and confirm navigation to the correct route

    Per D-07: Fix all bugs found inline.
    Per D-08: ALL bugs must be fixed, zero tolerance for deferring.
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/campaign/new | grep -q "html" && echo "PASS: campaign/new serves HTML" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Empty premise does not create a campaign (validation prevents or error shown)
    - Back navigation from DNA preserves premise text
    - Generation can complete with or without DNA seeds
    - No broken UI states after any edge case interaction
    - Previously created campaign is loadable from launcher
  </acceptance_criteria>
  <done>Campaign creation edge cases verified. Error states handled gracefully. No stuck or broken UI states.</done>
</task>

</tasks>

<verification>
- Full original-world campaign creation pipeline works in browser
- Real LLM calls succeed (GLM provider)
- Edge cases don't break the UI
- Shell stays consistent throughout the flow
- At least one complete original-world campaign exists after this plan
</verification>

<success_criteria>
- One original-world campaign fully created and generated through browser (D-03 original-world)
- Campaign creation flow navigable: launcher -> concept -> DNA -> generate -> review (D-01 flow 1)
- All discovered bugs fixed and re-tested (D-07, D-08)
- Error states handled gracefully (D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-02-SUMMARY.md`
</output>
