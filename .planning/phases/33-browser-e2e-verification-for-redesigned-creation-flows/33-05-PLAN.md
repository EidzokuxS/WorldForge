---
phase: 33-browser-e2e-verification-for-redesigned-creation-flows
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/globals.css
  - frontend/components/non-game-shell/shell-primitives.tsx
  - frontend/components/non-game-shell/app-shell.tsx
  - frontend/components/non-game-shell/app-sidebar.tsx
  - frontend/app/(non-game)/page.tsx
  - frontend/components/world-review/review-workspace.tsx
  - frontend/components/character-creation/character-workspace.tsx
  - frontend/components/non-game-shell/__tests__/app-shell.test.tsx
  - frontend/app/(non-game)/__tests__/page.test.tsx
  - frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx
  - frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx
autonomous: true
requirements: [P33-01, P33-04]
gap_closure: true
must_haves:
  truths:
    - "Launcher, shell chrome, review workspace, and character workspace read as one desktop visual system instead of mixed radii and surfaces."
    - "The left navigation rail and the main canvas share coherent outer geometry; the shell no longer has a square rail beside a separately rounded content slab."
    - "Non-game workspace summary panels and action trays use one shell primitive layer instead of ad hoc `bg-card`, `bg-muted`, and one-off radius combinations."
  artifacts:
    - path: "frontend/app/globals.css"
      provides: "Dedicated non-game shell token contract for radii, surfaces, borders, and accents"
    - path: "frontend/components/non-game-shell/shell-primitives.tsx"
      provides: "Reusable shell panel/rail/action primitives used across non-game routes"
    - path: "frontend/components/non-game-shell/app-shell.tsx"
      provides: "Unified outer shell geometry and surface application"
  key_links:
    - from: "frontend/app/globals.css"
      to: "frontend/components/non-game-shell/shell-primitives.tsx"
      via: "CSS custom properties consumed by shell primitives"
      pattern: "var\\(--shell-"
    - from: "frontend/components/non-game-shell/shell-primitives.tsx"
      to: "frontend/app/(non-game)/page.tsx"
      via: "launcher and workspace panel imports"
      pattern: "from \"@/components/non-game-shell/shell-primitives\""
---

<objective>
Close UAT Gap 1 by replacing the fragmented non-game visual system with one shell token/primitives layer and migrating the launcher plus shared review/character workspace containers onto it.

Purpose: The current shell looks patched together because radius, surface, and border decisions are split across `AppShell`, the sidebar primitive, default cards, and page-local wrappers. This plan creates one reusable shell language without touching `/game` or adding bespoke CSS files.
Output: Dedicated shell tokens and primitives applied to the launcher, shell frame, review workspace, and character workspace.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-UAT.md
@.planning/debug/phase-33-uat-gap-1-shell-visuals.md
@docs/ui_concept_hybrid.html
@frontend/app/globals.css
@frontend/components/non-game-shell/app-shell.tsx
@frontend/components/non-game-shell/app-sidebar.tsx
@frontend/components/ui/sidebar.tsx
@frontend/app/(non-game)/page.tsx
@frontend/components/world-review/review-workspace.tsx
@frontend/components/character-creation/character-workspace.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the shell primitive contract in frontend regressions</name>
  <files>
    frontend/components/non-game-shell/__tests__/app-shell.test.tsx,
    frontend/app/(non-game)/__tests__/page.test.tsx,
    frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx,
    frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx
  </files>
  <read_first>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-UAT.md,
    .planning/debug/phase-33-uat-gap-1-shell-visuals.md,
    frontend/components/non-game-shell/__tests__/app-shell.test.tsx,
    frontend/app/(non-game)/__tests__/page.test.tsx,
    frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx,
    frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx,
    frontend/components/non-game-shell/app-shell.tsx,
    frontend/app/(non-game)/page.tsx,
    frontend/components/world-review/review-workspace.tsx,
    frontend/components/character-creation/character-workspace.tsx
  </read_first>
  <action>
    Add targeted frontend regressions that describe the shell contract required by UAT Gap 1. The tests must prove the non-game shell exposes reusable shell-surface slots instead of route-local ad hoc styling, and must reference the gap explicitly.

    Target state:
    - `AppShell` test asserts the shell renders shared primitive hooks for the outer frame, left rail, main panel, and sticky action tray instead of relying on one-off route classes.
    - Launcher test asserts the primary launcher cards render inside the new shell-panel primitive contract rather than bare `rounded-3xl` blocks.
    - Review and character workspace tests assert their section nav, summary rail, and action tray all use the same shell primitives as the launcher.

    Keep the tests narrow to the non-game shell surface contract per the diagnosed gap; do not expand into library or gameplay coverage (per D-02, deferred).
  </action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "frontend/components/non-game-shell/__tests__/app-shell.test.tsx" "frontend/app/(non-game)/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The targeted tests fail before the primitive migration and pass after it.
    - Test assertions mention shared shell frame/panel/action-tray hooks rather than route-specific cosmetic class strings only.
    - Browser-verifiable outcome is encoded: launcher, review, and character all expose the same shell regions for left rail, main canvas, summary panel, and action tray.
  </acceptance_criteria>
  <done>Frontend regressions pin the shell primitive contract that Gap 1 requires before implementation begins.</done>
</task>

<task type="auto">
  <name>Task 2: Implement one non-game shell token and primitive layer</name>
  <files>
    frontend/app/globals.css,
    frontend/components/non-game-shell/shell-primitives.tsx,
    frontend/components/non-game-shell/app-shell.tsx,
    frontend/components/non-game-shell/app-sidebar.tsx,
    frontend/app/(non-game)/page.tsx,
    frontend/components/world-review/review-workspace.tsx,
    frontend/components/character-creation/character-workspace.tsx
  </files>
  <read_first>
    docs/ui_concept_hybrid.html,
    .planning/debug/phase-33-uat-gap-1-shell-visuals.md,
    frontend/app/globals.css,
    frontend/components/non-game-shell/app-shell.tsx,
    frontend/components/non-game-shell/app-sidebar.tsx,
    frontend/components/ui/sidebar.tsx,
    frontend/app/(non-game)/page.tsx,
    frontend/components/world-review/review-workspace.tsx,
    frontend/components/character-creation/character-workspace.tsx
  </read_first>
  <action>
    Implement the Gap 1 fix by introducing one dedicated shell token/primitives layer and migrating the shared non-game shell surfaces onto it.

    Concrete target state:
    - In `frontend/app/globals.css`, add dedicated shell variables for outer background, panel surfaces, rail surface, border strength, highlight, and a single radius scale. Keep them inside the existing theme file; do not add a standalone CSS file.
    - Create `frontend/components/non-game-shell/shell-primitives.tsx` exporting reusable shell panel helpers for outer shell frame, content panels, side rails, and sticky action trays. These primitives should own the radius/border/shadow recipe so page files stop hand-rolling it.
    - Update `AppShell` and `AppSidebar` so the left rail and main canvas share coherent geometry. Remove the asymmetric `rounded-r-[32px]` shell treatment and stop relying on a square outer sidebar rail.
    - Update the launcher, review workspace, and character workspace to consume the shared shell primitives instead of mixing `bg-card`, `bg-muted`, `bg-background`, `rounded-2xl`, and `rounded-3xl` on each page.
    - Preserve the existing non-game route structure from Phase 32 and keep `/game` and generic shadcn primitives out of scope. This is a non-game shell fix, not a global card redesign.
  </action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "frontend/components/non-game-shell/__tests__/app-shell.test.tsx" "frontend/app/(non-game)/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/globals.css` defines dedicated non-game shell tokens such as shared shell surfaces/radii instead of relying only on generic shadcn theme buckets.
    - `frontend/components/non-game-shell/shell-primitives.tsx` exists and is imported by the shell and workspace files.
    - `frontend/components/non-game-shell/app-shell.tsx` no longer uses `rounded-r-[32px]`.
    - Launcher, review workspace, and character workspace all consume the new shell primitives rather than page-local rounded panel wrappers.
    - Browser-verifiable outcome: the launcher, left rail, summary panels, and sticky action trays look like one coherent desktop system with aligned radii and surfaces.
  </acceptance_criteria>
  <done>The non-game shell, launcher, and shared workspaces render from one dedicated shell token/primitives layer and no longer present the fragmented geometry/surface mix reported in UAT.</done>
</task>

</tasks>

<verification>
- Targeted frontend regressions for shell primitives pass.
- No `rounded-r-[32px]` shell frame remains in the non-game shell.
- Launcher, review, and character shared workspaces all consume the same shell primitives.
</verification>

<success_criteria>
- UAT Gap 1 root cause is structurally removed: shell visuals are governed by one token/primitives layer.
- The non-game shell no longer mixes square rails, one-sided shell radii, and unrelated surface buckets.
- Launcher, review, and character screens are ready for browser/UAT visual retesting.
</success_criteria>

<output>
After completion, create `.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-05-SUMMARY.md`
</output>
