---
phase: 33-browser-e2e-verification-for-redesigned-creation-flows
plan: 03
type: execute
wave: 2
depends_on: [33-01]
files_modified: []
autonomous: true
requirements: [P33-01, P33-02, P33-03]
must_haves:
  truths:
    - "User can create a known-IP campaign and generate a world scaffold from it"
    - "World review page shows generated locations with editable fields"
    - "User can edit a location name or tags in the review page"
    - "User can edit faction details in the review page"
    - "User can add or edit NPCs in the review page"
    - "User can view and search lore cards in the review page"
    - "Edits persist after save (not lost on navigation)"
  artifacts:
    - path: "frontend/app/(non-game)/campaign/[id]/review/page.tsx"
      provides: "World review page with scaffold editing"
    - path: "frontend/components/world-review/review-workspace.tsx"
      provides: "Review workspace composition"
  key_links:
    - from: "review page sections"
      to: "PUT /api/worldgen/save-edits"
      via: "save button triggers backend persist"
    - from: "NPC section"
      to: "POST /api/worldgen/parse-character or generate-character"
      via: "NPC creation modes"
    - from: "lore section"
      to: "GET /api/campaigns/:id/lore"
      via: "lore card display and search"
---

<objective>
Create a known-IP campaign via PinchTab and thoroughly verify the world review editing flow: locations, factions, NPCs, and lore sections.

Purpose: Cover D-01 flow 3 (world review editing) and D-03 (known-IP smoke test). Verify scaffold editing works on the redesigned shell route with real data.
Output: Known-IP campaign created, world review editing verified across all sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md
@.planning/phases/32-desktop-first-non-game-ui-overhaul/32-04-SUMMARY.md
@frontend/app/(non-game)/campaign/[id]/review/page.tsx
@frontend/components/world-review/review-workspace.tsx
@frontend/components/world-review/locations-section.tsx
@frontend/components/world-review/factions-section.tsx
@frontend/components/world-review/npcs-section.tsx
@frontend/components/world-review/lore-section.tsx
@frontend/components/world-review/premise-section.tsx
@frontend/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create known-IP campaign and verify world review sections render</name>
  <files></files>
  <read_first>
    frontend/app/(non-game)/campaign/new/page.tsx,
    frontend/app/(non-game)/campaign/[id]/review/page.tsx,
    frontend/components/world-review/review-workspace.tsx,
    frontend/components/world-review/locations-section.tsx,
    frontend/components/world-review/factions-section.tsx,
    frontend/components/world-review/npcs-section.tsx,
    frontend/components/world-review/lore-section.tsx,
    frontend/components/world-review/premise-section.tsx,
    frontend/lib/api.ts
  </read_first>
  <action>
    Ensure PinchTab is running, backend on :3001, frontend on :3000.

    Using PinchTab, create a known-IP campaign and navigate to review:

    1. **Create known-IP campaign**:
       - Navigate to `http://localhost:3000/campaign/new`
       - Enter a known-IP premise: "The world of Avatar: The Last Airbender, set 50 years after the Hundred Year War. The Fire Nation has reformed, but tensions rise as a new threat emerges from the Spirit World."
       - Proceed through DNA step (skip or accept defaults -- known IPs often skip DNA)
       - Trigger scaffold generation and wait for completion (up to 5 min for real LLM calls)
       - Confirm redirect to `/campaign/[id]/review`

    2. **Verify review page sections render**:
       - Confirm premise section shows the entered premise text
       - Confirm locations section shows at least 2-3 generated locations with names and tags
       - Confirm factions section shows at least 1-2 factions
       - Confirm NPCs section shows at least 3-5 NPCs (key + supporting tiers)
       - Confirm lore section shows generated lore cards (if lore extraction succeeded)
       - Confirm all sections render inside the shell (sidebar visible, page header shows "World Review" or "Review")

    3. **Verify section navigation**:
       - If sections are tabbed or collapsible, switch between them
       - Confirm each section loads its content correctly
       - Check that section counts/badges (if any) match visible items

    Per D-07: Fix bugs inline. If a section fails to render or shows errors, investigate, fix, and re-test.
    Per D-05: All generation uses real GLM LLM calls.
    Per D-11: Shell elements verified as part of each step.
  </action>
  <verify>
    <automated>curl -s http://localhost:3001/api/campaigns | grep -q "campaigns" && echo "PASS: API responds" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Known-IP campaign created successfully (premise references Avatar: The Last Airbender)
    - Scaffold generation completes without unhandled errors
    - Review page at `/campaign/[id]/review` renders inside the shell
    - Premise section displays the entered premise text
    - Locations section shows 2+ generated locations
    - Factions section shows 1+ generated factions
    - NPCs section shows 3+ generated NPCs
    - Lore section renders (even if lore extraction had partial failures, section itself must not crash)
    - Shell sidebar visible throughout
  </acceptance_criteria>
  <done>Known-IP campaign created and all world review sections render with generated content. D-03 known-IP smoke test satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: World review editing -- locations, factions, NPCs, lore</name>
  <files></files>
  <read_first>
    frontend/components/world-review/locations-section.tsx,
    frontend/components/world-review/factions-section.tsx,
    frontend/components/world-review/npcs-section.tsx,
    frontend/components/world-review/lore-section.tsx,
    frontend/components/world-review/helpers/tag-editor.tsx,
    frontend/components/world-review/helpers/string-list-editor.tsx,
    backend/src/routes/schemas.ts
  </read_first>
  <action>
    Using PinchTab on the known-IP campaign review page created in Task 1:

    1. **Edit a location**:
       - Find a location entry in the locations section
       - Click to edit (inline edit or modal -- depends on current UI)
       - Change the location name (e.g., append " - Modified" to the name)
       - Add or remove a tag on the location
       - Save the edit
       - Confirm the change persists in the UI after save
       - Refresh the page and confirm the edit is still there (persisted to backend)

    2. **Edit a faction**:
       - Find a faction entry in the factions section
       - Edit the faction description or goals
       - Save and verify persistence
       - Refresh page to confirm backend persistence

    3. **Edit or add an NPC**:
       - Find an NPC in the NPCs section
       - Edit the NPC's tags or description
       - Save and verify
       - If "Add NPC" functionality exists (3 modes: describe, import V2, AI generate per project history):
         - Test the "Describe" mode: enter a brief NPC description, submit
         - Confirm the new NPC appears in the list
       - Verify duplicate name warnings if applicable (add an NPC with same name as existing)

    4. **Lore card interaction**:
       - View lore cards in the lore section
       - If search field exists, type a search term and verify filtered results
       - If lore editing exists (Phase 27), edit a lore card term or definition
       - Save and verify

    5. **Regenerate a section** (if regenerate button exists):
       - Find "Regenerate" for one section (e.g., locations)
       - Click and confirm regeneration triggers (real LLM call)
       - Verify new content replaces old content after completion

    Per D-07: Fix all bugs inline. Common issues to watch for:
    - Edit fields not pre-populated with current values
    - Save not triggering backend persist (check network tab via PinchTab evaluate)
    - Refresh losing edits (backend save failing silently)
    - UI not updating after save (stale state)

    Per D-04: Test error states:
    - What happens if you try to save with empty required fields?
    - What happens if backend returns an error during save?
  </action>
  <verify>
    <automated>curl -s http://localhost:3001/api/campaigns | grep -q "campaigns" && echo "PASS: backend running" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Location name edit persists after page refresh
    - Faction edit persists after page refresh
    - NPC edit persists after page refresh
    - New NPC creation via "Describe" mode works (NPC appears in list)
    - Lore section renders and search works (if applicable)
    - No JavaScript errors in console during editing (verify via PinchTab evaluate: `var errs = window.__consoleErrors; errs ? errs.length : 0`)
    - Shell sidebar remains visible during all editing operations
  </acceptance_criteria>
  <done>World review editing verified: locations, factions, NPCs, and lore sections all support edit operations with backend persistence. All bugs found are fixed.</done>
</task>

</tasks>

<verification>
- Known-IP campaign (Avatar: The Last Airbender) created and generated successfully
- All 5 review sections render: premise, locations, factions, NPCs, lore
- Edit operations work and persist to backend for locations, factions, and NPCs
- Lore section renders and supports search
- Shell stays consistent throughout
</verification>

<success_criteria>
- Known-IP smoke test complete (D-03)
- World review editing flow verified (D-01 flow 3)
- All edits persist after page refresh
- All discovered bugs fixed and re-tested (D-07, D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-03-SUMMARY.md`
</output>
