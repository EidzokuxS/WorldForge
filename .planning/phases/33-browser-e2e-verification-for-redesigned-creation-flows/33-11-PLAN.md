---
phase: 33-browser-e2e-verification-for-redesigned-creation-flows
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/debug/phase-33-browser-transport.md
  - .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-11-SUMMARY.md
  - package.json
  - frontend/package.json
  - backend/package.json
autonomous: true
requirements: [P33-01, P33-02, P33-03, P33-04]
gap_closure: true
must_haves:
  truths:
    - "The executor has one named PinchTab browser path that can reach `http://localhost:3000` from this environment."
    - "Browser rerun plans no longer assume PinchTab will magically work; they can reuse a recorded PinchTab open/snapshot/evaluate path."
    - "Phase 33 stops cleanly on a concrete PinchTab blocker instead of silently downgrading to curl-only proof or another transport."
  artifacts:
    - path: ".planning/debug/phase-33-browser-transport.md"
      provides: "Recorded PinchTab launch commands, smoke-path commands, and localhost reachability evidence"
    - path: ".planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-11-SUMMARY.md"
      provides: "Reusable PinchTab-only prerequisite summary for downstream browser rerun plans"
  key_links:
    - from: "package.json"
      to: "http://localhost:3000"
      via: "`npm run dev` frontend launch path"
      pattern: "\"dev\": \"concurrently"
    - from: "package.json"
      to: "http://localhost:3001/api/health"
      via: "`npm run dev` backend launch path"
      pattern: "\"npm --prefix backend run dev\""
    - from: ".planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-11-SUMMARY.md"
      to: ".planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-PLAN.md"
      via: "documented PinchTab transport contract reused by browser reruns"
      pattern: "transport: PinchTab"
---

<objective>
Restore and validate the PinchTab browser E2E transport prerequisite for Phase 33 so downstream rerun plans can execute against localhost instead of repeating the earlier curl-only downgrade.

Purpose: The last execution failed the phase goal because browser proof was replaced with HTTP checks when PinchTab could not reach localhost. This plan makes the PinchTab seam explicit: fix it if the cause is project-local, otherwise stop on a named blocker before any browser rerun plan starts.
Output: A recorded PinchTab transport path, localhost smoke proof, and a reusable summary that downstream plans must follow.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md
@package.json
@frontend/package.json
@backend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore the named PinchTab localhost path for Phase 33</name>
  <files>
    .planning/debug/phase-33-browser-transport.md,
    package.json,
    frontend/package.json,
    backend/package.json
  </files>
  <read_first>
    .planning/codebase/TESTING.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md,
    package.json,
    frontend/package.json,
    backend/package.json
  </read_first>
  <action>
    Re-establish the PinchTab transport seam that Phase 33 depends on. Per the locked phase context and testing conventions, PinchTab is the only allowed browser E2E transport here. Do not substitute `agent-browser`, Playwright, curl, or any other browser path.

    Required sequence:
    - Verify the product is reachable first: backend on `http://localhost:3001/api/health`, frontend on `http://localhost:3000`. If either is down, start the repo-local dev path (`npm run dev` at repo root or the narrower frontend/backend scripts) and wait until both endpoints respond.
    - Start or reconnect to PinchTab (`BRIDGE_HEADLESS=true pinchtab` or the project's equivalent launch) and use the PinchTab CLI/HTTP API only.
    - Execute a real browser smoke path through PinchTab against localhost: open `http://localhost:3000/`, confirm the launcher shell and `New Campaign` are visible in the accessibility tree, programmatically click the launcher link into `/campaign/new`, and confirm the creation route renders there.
    - If PinchTab cannot reach localhost because of the known bridge/isolation issue, diagnose whether the blocker is project-local launch/origin/config behavior or an external PinchTab/browser problem.
    - Fix only project-local seams if needed. Limit changes to launch/config files already in the repo; do not invent a new test framework, do not switch to curl, and do not broaden into unrelated app code.
    - Write `.planning/debug/phase-33-browser-transport.md` with: transport name `PinchTab`, exact commands, whether PinchTab was usable, the localhost smoke steps and outputs, and any remaining non-project blocker if PinchTab still cannot be restored.

    This plan is a hard gate for all browser reruns. If PinchTab cannot complete the localhost smoke path after project-local fixes, stop here with the concrete blocker instead of allowing another curl-only fallback or alternate transport.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; Invoke-RestMethod 'http://localhost:3001/api/health' | Out-Null; Invoke-WebRequest 'http://localhost:3000' -UseBasicParsing | Out-Null; pinchtab health | Out-Null; pinchtab nav http://localhost:3000/ | Out-Null; Start-Sleep -Seconds 2; $launcher = pinchtab snap -i -c; if ($launcher -notmatch 'New Campaign') { throw 'PinchTab did not expose the launcher at /.' }; pinchtab eval 'var btn = document.querySelector("a[href=\"/campaign/new\"]"); if (btn === null) { throw new Error("Missing /campaign/new launcher link"); } btn.click(); window.location.pathname;' | Out-Null; Start-Sleep -Seconds 2; $path = (pinchtab eval 'window.location.pathname' | Out-String); if ($path -notmatch '/campaign/new') { throw "PinchTab click did not reach /campaign/new: $path" }; $campaign = pinchtab snap -i -c; if (($campaign -notmatch 'Continue to DNA') -and ($campaign -notmatch 'Campaign') -and ($campaign -notmatch 'World DNA')) { throw 'PinchTab reached /campaign/new but did not expose the creation UI.' }; Write-Output 'PASS: PinchTab smoke path reached / and /campaign/new on localhost.'</automated>
  </verify>
  <done>The PinchTab smoke path is proven against localhost, or the exact blocker is documented without downgrading to curl-only verification or another browser transport.</done>
</task>

<task type="auto">
  <name>Task 2: Record the reusable browser-transport contract for downstream reruns</name>
  <files>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-11-SUMMARY.md,
    .planning/debug/phase-33-browser-transport.md
  </files>
  <read_first>
    .planning/debug/phase-33-browser-transport.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md
  </read_first>
  <action>
    Convert the transport restoration work into a reusable prerequisite summary for Plans 33-08 through 33-10.

    The summary must include:
    - `transport: PinchTab` with the exact launch/connect path to use
    - frontend/backend readiness proof with timestamps or command outputs
    - the minimum PinchTab smoke sequence that downstream plans must reuse before starting flow-specific work: open `/`, verify the launcher shell renders, click `New Campaign`, confirm `/campaign/new`, then return/back as needed
    - any still-relevant operating rules from the transport fix, such as headed/headless mode, viewport, or launch command
    - an explicit blocked status if PinchTab could not be restored

    Downstream browser plans must be able to read this summary and know exactly which PinchTab path to reuse without re-diagnosing the transport problem.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; $summary = '.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-11-SUMMARY.md'; if (-not (Test-Path $summary)) { throw 'Missing 33-11 summary.' }; $text = Get-Content -LiteralPath $summary -Raw; foreach ($required in @('transport: PinchTab', 'frontend:', 'backend:', 'smoke:')) { if ($text -notmatch [regex]::Escape($required)) { throw \"33-11 summary missing $required\" } }; Write-Output 'PASS: 33-11 summary records the reusable PinchTab transport contract.'</automated>
  </verify>
  <done>The phase now has a reusable, PinchTab-only prerequisite summary instead of an implicit assumption that the bridge will work.</done>
</task>

</tasks>

<verification>
- A PinchTab browser path reaches `http://localhost:3000` from the current environment.
- The chosen PinchTab transport is written down as an explicit prerequisite for 33-08 through 33-10.
- The phase no longer permits another curl-only downgrade or alternate browser transport without a named blocker.
</verification>

<success_criteria>
- Browser rerun plans have a concrete PinchTab contract to follow.
- Localhost reachability is proven through a real PinchTab smoke path, not inferred from HTTP responses alone.
- Any remaining PinchTab failure is isolated and documented before browser reruns begin.
</success_criteria>

<output>
After completion, create `.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-11-SUMMARY.md`
</output>
