---
phase: 33-browser-e2e-verification-for-redesigned-creation-flows
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/debug/phase-33-browser-transport.md
  - .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-11-SUMMARY.md
  - package.json
  - frontend/package.json
  - backend/package.json
autonomous: true
requirements: [P33-01, P33-02, P33-03, P33-04]
gap_closure: true
must_haves:
  truths:
    - "The executor has one named real-browser transport path that can reach `http://localhost:3000` from this environment."
    - "Browser rerun plans no longer assume transport will magically work; they can reuse a recorded browser open/snapshot/click path."
    - "Phase 33 stops cleanly on a concrete transport blocker instead of silently downgrading to curl-only proof."
  artifacts:
    - path: ".planning/debug/phase-33-browser-transport.md"
      provides: "Recorded browser transport choice, commands, and localhost reachability evidence"
    - path: ".planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-11-SUMMARY.md"
      provides: "Reusable prerequisite summary for downstream browser rerun plans"
  key_links:
    - from: "package.json"
      to: "http://localhost:3000"
      via: "`npm run dev` frontend launch path"
      pattern: "\"dev\": \"concurrently"
    - from: "package.json"
      to: "http://localhost:3001/api/health"
      via: "`npm run dev` backend launch path"
      pattern: "\"npm --prefix backend run dev\""
    - from: ".planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-11-SUMMARY.md"
      to: ".planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-PLAN.md"
      via: "documented transport contract reused by browser reruns"
      pattern: "transport:"
---

<objective>
Restore and validate the real browser E2E transport prerequisite for Phase 33 so downstream rerun plans can execute against localhost instead of repeating the earlier curl-only downgrade.

Purpose: The last execution failed the phase goal because browser proof was replaced with HTTP checks when the browser bridge could not reach localhost. This plan makes the transport seam explicit: fix it if the cause is project-local, otherwise stop on a named blocker before any browser rerun plan starts.
Output: A recorded named browser transport path, localhost reachability proof, and a reusable summary that downstream plans must follow.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md
@package.json
@frontend/package.json
@backend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore one named real-browser localhost path for Phase 33</name>
  <files>
    .planning/debug/phase-33-browser-transport.md,
    package.json,
    frontend/package.json,
    backend/package.json
  </files>
  <read_first>
    .planning/codebase/TESTING.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md,
    package.json,
    frontend/package.json,
    backend/package.json
  </read_first>
  <action>
    Re-establish the real browser transport seam that Phase 33 depends on. Prefer PinchTab per the phase context and testing conventions, but if PinchTab remains the source of the localhost-reachability failure, establish one equivalent named browser path that can actually drive a real browser from this environment and record that choice explicitly.

    Required sequence:
    - Verify the product is reachable first: backend on `http://localhost:3001/api/health`, frontend on `http://localhost:3000`. If either is down, start the repo-local dev path (`npm run dev` at repo root or the narrower frontend/backend scripts) and wait until both endpoints respond.
    - Attempt the project-preferred browser path first. If it cannot reach localhost because of the known bridge/isolation issue, diagnose whether the blocker is project-local launch/origin/config behavior or an external browser-bridge problem.
    - Fix only project-local seams if needed. Limit changes to launch/config files already in the repo; do not invent a new test framework, do not switch to curl, and do not broaden into unrelated app code.
    - If PinchTab still cannot reach localhost after project-local fixes, establish one equivalent named browser path using the locally available browser tooling. It must be able to open `http://localhost:3000`, inspect interactive elements, click into `New Campaign`, and return cleanly. Record the chosen path and why it was used instead of PinchTab.
    - Write `.planning/debug/phase-33-browser-transport.md` with: chosen transport name, exact commands, whether PinchTab was usable, the localhost proof steps, and any remaining non-project blocker if transport still cannot be restored.

    This plan is a hard gate for all browser reruns. If no real browser path reaches localhost, stop here with the concrete blocker instead of allowing another curl-only fallback.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; Invoke-RestMethod 'http://localhost:3001/api/health' | Out-Null; Invoke-WebRequest 'http://localhost:3000' -UseBasicParsing | Out-Null; if (Get-Command agent-browser -ErrorAction SilentlyContinue) { agent-browser open http://localhost:3000 | Out-Null; $snapshot = agent-browser snapshot -i --json; if ($snapshot -notmatch 'New Campaign') { throw 'Browser transport reached localhost but did not expose launcher controls.' }; agent-browser close | Out-Null } elseif (Test-NetConnection 127.0.0.1 -Port 9867 -InformationLevel Quiet) { Write-Output 'PASS: PinchTab bridge reachable on 9867; record the exact transport commands in phase-33-browser-transport.md.' } else { throw 'No real browser transport verified: neither agent-browser proof nor PinchTab bridge reachability succeeded.' }</automated>
  </verify>
  <done>One named real-browser transport path is proven against localhost, or the exact blocker is documented without downgrading to curl-only verification.</done>
</task>

<task type="auto">
  <name>Task 2: Record the reusable browser-transport contract for downstream reruns</name>
  <files>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-11-SUMMARY.md,
    .planning/debug/phase-33-browser-transport.md
  </files>
  <read_first>
    .planning/debug/phase-33-browser-transport.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md
  </read_first>
  <action>
    Convert the transport restoration work into a reusable prerequisite summary for Plans 33-08 through 33-10.

    The summary must include:
    - `transport:` with the exact named path to use (`PinchTab`, `agent-browser`, or another explicitly named real-browser path justified in Task 1)
    - frontend/backend readiness proof with timestamps or command outputs
    - the minimum browser smoke sequence that downstream plans must reuse before starting flow-specific work: open `/`, verify the launcher shell renders, click `New Campaign`, confirm `/campaign/new`, then return/back as needed
    - any still-relevant operating rules from the transport fix, such as headed/headless mode, viewport, or launch command
    - an explicit blocked status if no real browser path could be restored

    Downstream browser plans must be able to read this summary and know exactly which browser path to reuse without re-diagnosing the transport problem.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; $summary = '.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-11-SUMMARY.md'; if (-not (Test-Path $summary)) { throw 'Missing 33-11 summary.' }; $text = Get-Content -LiteralPath $summary -Raw; foreach ($required in @('transport:', 'frontend:', 'backend:', 'smoke:')) { if ($text -notmatch [regex]::Escape($required)) { throw \"33-11 summary missing $required\" } }; Write-Output 'PASS: 33-11 summary records the reusable browser transport contract.'</automated>
  </verify>
  <done>The phase now has a reusable, named browser-transport prerequisite summary instead of an implicit assumption that the bridge will work.</done>
</task>

</tasks>

<verification>
- A real browser path reaches `http://localhost:3000` from the current environment.
- The chosen transport is written down as an explicit prerequisite for 33-08 through 33-10.
- The phase no longer permits another curl-only downgrade without a named blocker.
</verification>

<success_criteria>
- Browser rerun plans have a concrete transport contract to follow.
- Localhost reachability is proven through a real browser path, not inferred from HTTP responses alone.
- Any remaining transport failure is isolated and documented before browser reruns begin.
</success_criteria>

<output>
After completion, create `.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-11-SUMMARY.md`
</output>
