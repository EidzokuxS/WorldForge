---
phase: 33-browser-e2e-verification-for-redesigned-creation-flows
plan: 08
type: execute
wave: 3
depends_on: [33-05, 33-06, 33-07]
files_modified:
  - .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-SUMMARY.md
  - frontend/app/(non-game)/campaign/new/page.tsx
  - frontend/app/(non-game)/campaign/new/dna/page.tsx
  - frontend/app/(non-game)/campaign/[id]/review/page.tsx
  - frontend/app/(non-game)/campaign/[id]/character/page.tsx
  - frontend/components/campaign-new/concept-workspace.tsx
  - frontend/components/campaign-new/dna-workspace.tsx
  - frontend/components/world-review/review-workspace.tsx
  - frontend/components/world-review/locations-section.tsx
  - frontend/components/world-review/factions-section.tsx
  - frontend/components/world-review/npcs-section.tsx
  - frontend/components/world-review/lore-section.tsx
  - frontend/components/character-creation/character-workspace.tsx
  - frontend/components/character-creation/character-card.tsx
  - frontend/components/non-game-shell/app-shell.tsx
  - frontend/components/non-game-shell/app-sidebar.tsx
  - frontend/components/title/load-campaign-dialog.tsx
  - backend/src/routes/campaigns.ts
  - backend/src/routes/helpers.ts
  - backend/src/routes/worldgen.ts
autonomous: false
requirements: [P33-02, P33-03, P33-04]
gap_closure: true
must_haves:
  truths:
    - "The original-world routed flow is browser-tested end-to-end with visible DNA/world progress, durable inputs, and no premature review/character access."
    - "A known-IP flow is browser-tested again after the fixes, world-review edits persist after save plus reload, and the same shell carries the user into character creation."
    - "Character creation is browser-tested for parse/edit/save plus persona/start-condition seams, and saving hands off cleanly into `/game` without the non-game shell."
    - "Every regression discovered during the browser reruns is fixed inline and the exact failing flow is re-run in the same phase before approval."
  artifacts:
    - path: ".planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-SUMMARY.md"
      provides: "Recorded browser/UAT evidence for original-world and known-IP retests, including a browser-found bug/fix/rerun ledger"
    - path: "frontend/app/(non-game)/campaign/[id]/review/page.tsx"
      provides: "Browser-retested world-review route used for save/reload persistence verification"
    - path: "frontend/app/(non-game)/campaign/[id]/character/page.tsx"
      provides: "Browser-retested character route covering persona/start-condition seams and `/game` handoff"
  key_links:
    - from: "launcher /"
      to: "/campaign/new"
      via: "browser-tested creation entry"
      pattern: "/campaign/new"
    - from: "/campaign/new"
      to: "/campaign/[id]/review"
      via: "visible DNA + world-generation progress"
      pattern: "Generation progress"
    - from: "/campaign/[id]/review"
      to: "world-review save flow"
      via: "browser save action plus hard reload persistence retest"
      pattern: "save"
    - from: "/campaign/[id]/review"
      to: "/campaign/[id]/character"
      via: "ready-only handoff after world exists"
      pattern: "Continue to Character"
    - from: "/campaign/[id]/character"
      to: "/game"
      via: "browser save-character handoff"
      pattern: "/game"
---

<objective>
Re-run Phase 33 browser/UAT verification on the fixed UI/UX using real browser interactions, including world-review persistence and full character handoff coverage, and fix any browser-found regressions inline before pausing for final human approval.

Purpose: The previous phase was closed on curl/API evidence even though the user-reported problems were visual and interactional. This plan re-tests the actual desktop UI, closes blocked UAT tests 4-6 in the browser, and blocks completion until any browser-found regressions are fixed and re-run.
Output: Browser/UAT evidence for original-world and known-IP flows, world-review save/reload persistence, character/persona/start-condition/game-handoff coverage, a bug/fix/rerun ledger, and a final user approval checkpoint.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-UAT.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md
@.planning/debug/phase-33-uat-gap-1-shell-visuals.md
@.planning/debug/phase-33-uat-gap-2.md
@frontend/app/(non-game)/page.tsx
@frontend/app/(non-game)/campaign/new/page.tsx
@frontend/app/(non-game)/campaign/new/dna/page.tsx
@frontend/app/(non-game)/campaign/[id]/review/page.tsx
@frontend/app/(non-game)/campaign/[id]/character/page.tsx
@frontend/components/non-game-shell/app-shell.tsx
@frontend/components/title/load-campaign-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Browser re-verify the original-world routed creation flow with an inline fix-and-rerun loop</name>
  <files>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-SUMMARY.md,
    frontend/app/(non-game)/campaign/new/page.tsx,
    frontend/app/(non-game)/campaign/new/dna/page.tsx,
    frontend/components/campaign-new/concept-workspace.tsx,
    frontend/components/campaign-new/dna-workspace.tsx,
    frontend/components/non-game-shell/app-shell.tsx,
    frontend/components/non-game-shell/app-sidebar.tsx,
    frontend/components/title/load-campaign-dialog.tsx,
    frontend/app/(non-game)/campaign/[id]/review/page.tsx,
    frontend/app/(non-game)/campaign/[id]/character/page.tsx,
    backend/src/routes/campaigns.ts,
    backend/src/routes/helpers.ts
  </files>
  <read_first>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-UAT.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md,
    frontend/app/(non-game)/page.tsx,
    frontend/app/(non-game)/campaign/new/page.tsx,
    frontend/app/(non-game)/campaign/new/dna/page.tsx,
    frontend/app/(non-game)/campaign/[id]/review/page.tsx,
    frontend/app/(non-game)/campaign/[id]/character/page.tsx,
    frontend/components/title/load-campaign-dialog.tsx
  </read_first>
  <action>
    Use real browser automation against the running frontend/backend. Do not fall back to curl or API-only verification again. If PinchTab still cannot reach localhost, stop and surface that browser-transport blocker instead of downgrading the phase evidence.

    Per D-01, D-04, D-05, D-06, D-07, D-08, D-10, and D-11, re-test the original-world flow end-to-end:
    1. Open `/` at desktop resolution and confirm the shell, launcher cards, sidebar, and sticky/action regions share the unified visual language from Plan 05.
    2. Open `/campaign/new`, enter campaign name/premise, leave the route, come back, and confirm the inputs persist.
    3. Click Continue to DNA and confirm a visible DNA suggestion state appears before the DNA editor becomes interactive.
    4. Trigger world generation and confirm the UI shows live progress while generation is running.
    5. Confirm review/character links are not available on an ungenerated campaign and become available only after generation completes.
    6. Verify that the Load Campaign dialog only loads from the Load button, not from clicking the card body.
    7. Confirm the previous blocker cases now behave correctly: no dead DNA page, no wiped concept state, no redundant shell CTA, no premature review/character access.
    8. If any browser-visible regression appears at any step, fix it immediately in the implicated files, rerun the exact failing browser segment, and rerun the affected automated suites before continuing. Do not batch bugs for later; this plan must obey D-07 and D-08.

    Record the actual browser observations in the summary with concrete evidence, not curl proxies. Include a "browser-found regressions" section even if the answer is "none".
  </action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "frontend/components/non-game-shell/__tests__/app-shell.test.tsx" "frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx" "frontend/components/title/__tests__/load-campaign-dialog.test.tsx" "frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx" && npm --prefix backend exec vitest run "src/routes/__tests__/campaigns.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Original-world browser run uses real UI interactions, not curl/API shortcuts.
    - Campaign inputs survive leaving and returning to `/campaign/new`.
    - Continue to DNA shows visible suggestion progress instead of the dead fallback message.
    - World generation shows visible progress while running.
    - Review and character are inaccessible before readiness and accessible after generation completes.
    - Load dialog only loads via the Load button.
    - Any browser-found regression from this flow is fixed inline and the failing segment is re-run successfully before the task closes.
  </acceptance_criteria>
  <done>The original-world routed creation flow and its previously blocked edge cases are re-verified through actual browser/UI interactions, with any browser-found regressions fixed and re-run in the same task.</done>
</task>

<task type="auto">
  <name>Task 2: Browser re-verify known-IP review persistence plus character creation, persona/start-condition seams, save, and `/game` handoff</name>
  <files>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-SUMMARY.md,
    frontend/app/(non-game)/campaign/[id]/review/page.tsx,
    frontend/components/world-review/review-workspace.tsx,
    frontend/components/world-review/locations-section.tsx,
    frontend/components/world-review/factions-section.tsx,
    frontend/components/world-review/npcs-section.tsx,
    frontend/components/world-review/lore-section.tsx,
    frontend/app/(non-game)/campaign/[id]/character/page.tsx,
    frontend/components/character-creation/character-workspace.tsx,
    frontend/components/character-creation/character-card.tsx,
    backend/src/routes/worldgen.ts,
    backend/src/routes/campaigns.ts
  </files>
  <read_first>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-UAT.md,
    frontend/app/(non-game)/campaign/new/page.tsx,
    frontend/app/(non-game)/campaign/new/dna/page.tsx,
    frontend/app/(non-game)/campaign/[id]/review/page.tsx,
    frontend/app/(non-game)/campaign/[id]/character/page.tsx
  </read_first>
  <action>
    Per D-01, D-03, D-04, D-05, D-06, D-07, D-08, D-10, and D-11, use real browser interactions to close blocked UAT tests 4-6:
    1. Create or load a known-IP campaign through the canonical `/campaign/new` browser path and reach populated world review through the same shell flow. This keeps P33-03 intact while covering the richer post-generation surface.
    2. On the review route, perform real UI edits to at least one persisted world-review field (for example location, faction, NPC, or lore), save through the browser, hard reload the page, and confirm the edit persists. This is the required real-browser retest for UAT test 4.
    3. Continue from review into character creation and confirm the character workspace loads only after the world is ready.
    4. In the character workspace, verify the intended creation modes are present and browser-usable. Cover the parse-described-character path at minimum, then smoke the additional mode toggles so the UI does not regress into dead or hidden states.
    5. Explicitly verify the Phase 30 seams in the browser: persona template control presence/selection or explicit empty-state behavior, start-condition inputs/validation, and any canonical loadout/starting-state preview that the route exposes. Do not skip persona/start-condition coverage because a chosen campaign path happens not to need them.
    6. Edit the resulting draft in the browser, save the character, and confirm the handoff lands on `/game` without the non-game shell wrapping the game page. This is the required real-browser retest for UAT tests 5 and 6.
    7. If any browser-visible regression appears anywhere in review or character flow, fix it immediately in the implicated files, rerun the exact failing browser segment, and rerun the affected automated suites before continuing.

    Record the actual known-IP browser evidence in the summary so Phase 33 once again satisfies the "one original-world + one known-IP smoke test" requirement. The summary must include the edit/save/reload proof, character-save handoff proof, and a bug/fix/rerun ledger for this task.
  </action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - One known-IP browser flow is completed after the fixes.
    - Review edits are performed and persisted through real browser save + hard reload, not direct API calls.
    - Review and character routes are reached through real browser navigation, not direct API calls.
    - Character creation covers parse/edit/save plus persona/start-condition seams in the browser.
    - `/game` remains outside the non-game shell after character handoff.
    - Any browser-found regression from review or character flow is fixed inline and the failing segment is re-run successfully before the task closes.
  </acceptance_criteria>
  <done>A known-IP browser run proves the fixes hold beyond the original-world path, re-closes world-review persistence in the browser, and re-verifies character creation through `/game` handoff with inline bug fixing and reruns.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final desktop UI/UX approval on the fixed non-game flow</name>
  <files></files>
  <read_first>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-SUMMARY.md,
    docs/ui_concept_hybrid.html,
    .planning/debug/phase-33-uat-gap-1-shell-visuals.md,
    .planning/debug/phase-33-uat-gap-2.md
  </read_first>
  <action>
    After the automated browser retests pass, ask the user to inspect the fixed UI/UX at desktop resolution and confirm the shell now feels coherent and the base creation workflow is no longer broken.
  </action>
  <what-built>Fixed non-game shell visuals, routed creation persistence/progress, readiness-aware review/character gating, and browser-tested original-world + known-IP flows.</what-built>
  <how-to-verify>
    1. Open `/` and confirm the sidebar, launcher cards, and shell frame no longer fight each other on radii/surfaces.
    2. Open `/campaign/new`, enter concept data, leave and return, and confirm the inputs persist.
    3. Continue to DNA and confirm you see real progress/loading, not a dead empty state.
    4. Generate a world and verify review/character stay locked until the world is ready.
    5. In world review, edit a real field, save, reload, and confirm the change persists.
    6. Continue into character creation, confirm the persona/start-condition seams are usable, save a character, and verify `/game` opens without the non-game shell.
    7. Check the review and character pages for the same visual system as the launcher.
  </how-to-verify>
  <verify>
    <automated>npm --prefix frontend exec vitest run "frontend/components/non-game-shell/__tests__/app-shell.test.tsx" "frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx" "frontend/components/title/__tests__/load-campaign-dialog.test.tsx" "frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx" && npm --prefix backend exec vitest run "src/routes/__tests__/campaigns.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - The user confirms the non-game shell feels visually coherent at desktop resolution.
    - The user confirms world-review save/reload and character-save-to-`/game` now work in the browser.
    - The user confirms the creation flow is now usable enough to continue testing instead of blocking further UAT.
    - Any remaining issue is concrete and narrow rather than "basic workflow is broken."
  </acceptance_criteria>
  <resume-signal>Type "approved" if the desktop UI/UX is fixed enough to close the Phase 33 UAT gaps, or describe the remaining issue.</resume-signal>
  <done>User approval is captured after the automated browser/UAT evidence is reviewed.</done>
</task>

</tasks>

<verification>
- Frontend/backend regression suites pass before browser work.
- Original-world and known-IP flows are re-tested through real browser interactions.
- World-review edit/save/reload persistence is re-tested through the browser.
- Character creation, persona/start-condition seams, save, and `/game` handoff are re-tested through the browser.
- Any browser-found regression is fixed inline and the failing flow is re-run before the plan reaches the human checkpoint.
- Final human approval confirms the desktop UI/UX is no longer blocked by the original complaints.
</verification>

<success_criteria>
- Phase 33 gap closure is supported by real browser/UAT evidence rather than curl-only verification.
- Blocked UAT tests 4-6 are genuinely re-closed in the browser: review edits persist, character creation works, persona/start-condition seams behave, and `/game` handoff is clean.
- Both original-world and known-IP flows pass the fixed non-game creation/review/character path.
- Browser-found regressions are fixed and re-run inside this phase instead of being cataloged for later.
- The user can approve the desktop UI/UX as coherent and usable.
</success_criteria>

<output>
After completion, create `.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-SUMMARY.md`
</output>
