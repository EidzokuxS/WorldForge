---
phase: 33-browser-e2e-verification-for-redesigned-creation-flows
plan: 08
type: execute
wave: 3
depends_on: [33-05, 33-06, 33-07]
files_modified:
  - .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-SUMMARY.md
  - frontend/app/(non-game)/campaign/new/page.tsx
  - frontend/app/(non-game)/campaign/new/dna/page.tsx
  - frontend/components/campaign-new/concept-workspace.tsx
  - frontend/components/campaign-new/dna-workspace.tsx
  - frontend/components/non-game-shell/app-shell.tsx
  - frontend/components/non-game-shell/app-sidebar.tsx
  - frontend/components/title/load-campaign-dialog.tsx
  - frontend/app/(non-game)/settings/page.tsx
  - frontend/lib/api.ts
  - backend/src/routes/worldgen.ts
  - backend/src/routes/campaigns.ts
autonomous: true
requirements: [P33-01, P33-02, P33-04]
gap_closure: true
must_haves:
  truths:
    - "The original-world browser flow works through concept, DNA, and world generation with visible progress and no dead-end routed states."
    - "Edge cases from D-04 are browser-tested on the real UI: invalid concept input, browser back navigation, empty DNA, and mid-generation interruption do not corrupt the flow."
    - "Real LLM failure handling follows D-06 exactly: the failing step is retried up to 3 times, each retry is recorded, and the third failure reports a clear final error instead of silently stalling."
  artifacts:
    - path: ".planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-SUMMARY.md"
      provides: "Original-world browser evidence plus a retry ledger with attempt counts and final failure reporting"
    - path: "frontend/components/campaign-new/concept-workspace.tsx"
      provides: "Browser-retested concept route with validation and progress affordances"
    - path: "frontend/components/campaign-new/dna-workspace.tsx"
      provides: "Browser-retested DNA route with empty-state recovery and generation progress/failure handling"
  key_links:
    - from: "/campaign/new"
      to: "/campaign/new/dna"
      via: "guarded Continue to DNA flow with persisted state"
      pattern: "Continue to DNA"
    - from: "/campaign/new/dna"
      to: "real generator calls"
      via: "visible DNA/world generation progress plus retryable failure UI"
      pattern: "Generate New World|generationProgress|error"
    - from: "LLM-dependent browser action"
      to: ".planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-SUMMARY.md"
      via: "explicit retry ledger with attempts 1/3, 2/3, 3/3"
      pattern: "1/3|2/3|3/3"
---

<objective>
Re-run the original-world browser flow on the fixed routed creation UI, explicitly close the D-04 edge cases on the live product surface, and operationalize D-06 with a real 3-attempt LLM-failure protocol before moving on to the previously blocked review/character work.

Purpose: The blocked UAT path starts with the base original-world flow. This plan narrows the work to one executable browser slice: concept, DNA, generation, interruption recovery, and failure reporting. It must use real browser automation, not curl fallbacks.
Output: Original-world browser/UAT evidence, explicit D-04 edge-case coverage, and a D-06 retry/final-failure ledger in `33-08-SUMMARY.md`.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-UAT.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md
@.planning/debug/phase-33-uat-gap-2.md
@frontend/app/(non-game)/campaign/new/page.tsx
@frontend/app/(non-game)/campaign/new/dna/page.tsx
@frontend/components/campaign-new/concept-workspace.tsx
@frontend/components/campaign-new/dna-workspace.tsx
@frontend/components/non-game-shell/app-shell.tsx
@frontend/components/non-game-shell/app-sidebar.tsx
@frontend/components/title/load-campaign-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Browser re-verify the original-world routed creation flow and explicit D-04 edge cases</name>
  <files>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-SUMMARY.md,
    frontend/app/(non-game)/campaign/new/page.tsx,
    frontend/app/(non-game)/campaign/new/dna/page.tsx,
    frontend/components/campaign-new/concept-workspace.tsx,
    frontend/components/campaign-new/dna-workspace.tsx,
    frontend/components/non-game-shell/app-shell.tsx,
    frontend/components/non-game-shell/app-sidebar.tsx,
    frontend/components/title/load-campaign-dialog.tsx,
    backend/src/routes/campaigns.ts
  </files>
  <read_first>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-UAT.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-VERIFICATION.md,
    frontend/app/(non-game)/campaign/new/page.tsx,
    frontend/app/(non-game)/campaign/new/dna/page.tsx,
    frontend/components/campaign-new/concept-workspace.tsx,
    frontend/components/campaign-new/dna-workspace.tsx,
    frontend/components/title/load-campaign-dialog.tsx
  </read_first>
  <action>
    Use real browser automation against the running frontend/backend. Do not fall back to curl or API-only evidence. If the browser transport cannot reach localhost, stop and surface that blocker instead of silently downgrading the proof.

    Per D-01, D-04, D-07, D-08, D-10, and D-11, re-run the original-world flow at desktop resolution and close the specific edge cases the checker called out:
    1. Open `/` and confirm the shared shell remains coherent while entering the flow.
    2. On `/campaign/new`, exercise invalid inputs first: blank or otherwise invalid required concept data must block progress with clear validation instead of silently routing onward.
    3. Enter valid original-world concept data, continue to DNA, and confirm visible suggestion/loading state appears before the DNA editor becomes interactive.
    4. Use browser back navigation from `/campaign/new/dna` to `/campaign/new`, then forward back into DNA, and confirm the routed flow state persists instead of resetting.
    5. Exercise the empty-world-DNA edge case explicitly: if suggestion output is empty, or if the UI allows clearing DNA inputs to empty, `Generate New World` must remain blocked or recoverable with clear validation instead of a dead "not ready" page.
    6. Start world generation, then interrupt the flow mid-run by navigating away and returning, or by using any cancel/abort affordance if one exists. The UI must recover to either a resumed in-flight state or a clear cancelled state without wiping the session.
    7. Confirm review/character links stay unavailable before the world is ready and appear only after generation completes.
    8. Re-check the load-dialog bug in the browser: only the explicit Load button may load a campaign.

    If any browser-visible regression appears at any step, fix it immediately in the implicated files, rerun the exact failing browser segment, and rerun the affected automated suites before continuing. Keep a browser-found bug/fix/rerun ledger in the summary even if the answer is "none".
  </action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx" "frontend/components/campaign-new/__tests__/flow-provider.test.tsx" "frontend/components/title/__tests__/load-campaign-dialog.test.tsx" "frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx" && npm --prefix backend exec vitest run "src/routes/__tests__/campaigns.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - The original-world run uses real browser interactions, not curl or direct API-only verification.
    - Invalid concept input is tested explicitly and blocked with clear UI feedback.
    - Browser back/forward navigation between concept and DNA is tested explicitly and preserves flow state.
    - The empty-world-DNA case is tested explicitly and does not strand the user on a dead page.
    - Mid-generation interruption is tested explicitly and returns to a resumed or clearly cancelled state without losing the flow.
    - Review/character remain gated until world readiness is real.
    - Any browser-found regression is fixed inline and the failing segment is re-run before the task closes.
  </acceptance_criteria>
  <done>The original-world routed creation flow and the specific D-04 edge cases are re-verified through the browser with inline bug fixing and reruns.</done>
</task>

<task type="auto">
  <name>Task 2: Exercise D-06 LLM failure handling with an explicit 3-retry ceiling and final failure reporting</name>
  <files>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-SUMMARY.md,
    frontend/app/(non-game)/campaign/new/page.tsx,
    frontend/app/(non-game)/campaign/new/dna/page.tsx,
    frontend/components/campaign-new/concept-workspace.tsx,
    frontend/components/campaign-new/dna-workspace.tsx,
    frontend/app/(non-game)/settings/page.tsx,
    frontend/lib/api.ts,
    backend/src/routes/worldgen.ts
  </files>
  <read_first>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-UAT.md,
    frontend/app/(non-game)/settings/page.tsx,
    frontend/lib/api.ts,
    frontend/components/campaign-new/concept-workspace.tsx,
    frontend/components/campaign-new/dna-workspace.tsx,
    backend/src/routes/worldgen.ts
  </read_first>
  <action>
    Operationalize D-06 exactly and record it concretely. Use a real connection failure path, not a mock:

    - Temporarily induce a genuine generator-facing failure through the existing provider/settings seam so the browser flow hits a real LLM connection error during DNA suggestion or world generation.
    - Retry the exact failing browser action up to 3 times total, recording attempts `1/3`, `2/3`, and `3/3` in `33-08-SUMMARY.md` with the failing step, visible UI message, and any backend error surface.
    - On retries 1 and 2, confirm the UI remains recoverable: the inputs/session stay intact, the action can be retried, and the page does not dead-end or corrupt navigation.
    - After the third consecutive failure, require clear final failure reporting in the browser UI and summary. If the UI only spins, disappears silently, or reports something ambiguous, fix that inline and re-run the failure sequence until the reporting is explicit.
    - Restore valid provider settings and rerun the same step successfully with real LLM calls before closing the task.

    This task implements D-04's LLM-connection-failure edge case and D-06's retry ceiling/final failure reporting. Preserve D-07 and D-08: any bug found here is fixed immediately and the failing step is re-run before moving on.
  </action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx" "frontend/components/campaign-new/__tests__/flow-provider.test.tsx" && npm --prefix backend exec vitest run "src/routes/__tests__/campaigns.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - The failure drill uses a real connection/config failure, not mocked network responses.
    - The failing browser step is retried exactly up to 3 times and every attempt is logged as `1/3`, `2/3`, `3/3`.
    - Retries 1 and 2 leave the UI in a recoverable state with preserved flow data.
    - After the third consecutive failure, the browser UI reports a clear final failure instead of a silent spinner or dead state.
    - After restoring valid provider settings, the same step succeeds with real LLM calls.
  </acceptance_criteria>
  <done>D-06 is executable and evidenced: 3 retries maximum, explicit final failure reporting, and successful recovery after restoring valid provider settings.</done>
</task>

</tasks>

<verification>
- Original-world creation is re-tested through real browser interactions.
- D-04 edge cases are exercised explicitly: invalid inputs, back navigation, empty DNA, mid-generation interruption, and LLM connection failure.
- D-06 is recorded concretely with a 3-attempt retry ledger and final failure reporting.
- Any browser-found regression is fixed inline and the failing segment is re-run before the plan closes.
</verification>

<success_criteria>
- The base original-world flow no longer blocks the rest of the phase.
- The checker's D-04 and D-06 gaps are operationalized in executable browser tasks, not left as general intent.
- The resulting summary provides concrete browser evidence for the base flow and the retry/failure protocol before the blocked UAT 4-6 path resumes.
</success_criteria>

<output>
After completion, create `.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-SUMMARY.md`
</output>
