---
phase: 33-browser-e2e-verification-for-redesigned-creation-flows
plan: 10
type: execute
wave: 5
depends_on: [33-09, 33-12]
files_modified:
  - .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-10-SUMMARY.md
  - frontend/app/(non-game)/campaign/[id]/character/page.tsx
  - frontend/components/character-creation/character-workspace.tsx
  - frontend/components/character-creation/character-card.tsx
  - frontend/components/non-game-shell/app-shell.tsx
  - frontend/components/non-game-shell/app-sidebar.tsx
  - frontend/lib/api.ts
  - backend/src/routes/worldgen.ts
  - backend/src/routes/campaigns.ts
autonomous: false
requirements: [P33-01, P33-03, P33-04]
gap_closure: true
must_haves:
  truths:
    - "Character creation is browser-tested from the fixed review route through parse/edit/save and clean handoff into `/game`."
    - "Persona-template, start-condition, and canonical-loadout seams are exercised in the browser, including invalid-input handling where the UI exposes it."
    - "The final desktop UX approval happens only after the original-world, known-IP review, and character/browser handoff evidence exists."
  artifacts:
    - path: ".planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-10-SUMMARY.md"
      provides: "Character browser evidence, persona/start-condition/canonical-loadout coverage, `/game` handoff proof, and final approval notes"
    - path: "frontend/app/(non-game)/campaign/[id]/character/page.tsx"
      provides: "Browser-retested character route entered from review"
    - path: "frontend/components/character-creation/character-workspace.tsx"
      provides: "Browser-retested character-authoring UI for parse/edit/save and seam validation"
  key_links:
    - from: "/campaign/[id]/review"
      to: "/campaign/[id]/character"
      via: "ready-only continue flow after review"
      pattern: "Continue to Character"
    - from: "character workspace"
      to: "/game"
      via: "browser save-character handoff"
      pattern: "/game"
    - from: "final approval"
      to: ".planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-10-SUMMARY.md"
      via: "review of browser evidence from 33-08 through 33-10"
      pattern: "approved"
---

<objective>
Close the blocked character-creation half of Phase 33 through browser verification, including persona/start-condition seams, canonical loadout coverage, and the save-to-`/game` handoff, then pause for final human approval.

Purpose: UAT tests 5 and 6 were blocked behind the broken creation flow. This plan keeps the final step narrow: character route only, browser-only evidence, inline fixes/reruns, then desktop approval.
Output: Character browser/UAT evidence and final approval checkpoint in `33-10-SUMMARY.md`.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-UAT.md
@.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-12-SUMMARY.md
@frontend/app/(non-game)/campaign/[id]/character/page.tsx
@frontend/components/character-creation/character-workspace.tsx
@frontend/components/character-creation/character-card.tsx
@frontend/components/non-game-shell/app-shell.tsx
@frontend/components/non-game-shell/app-sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Browser re-verify character creation, persona/start-condition seams, and `/game` handoff</name>
  <files>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-10-SUMMARY.md,
    frontend/app/(non-game)/campaign/[id]/character/page.tsx,
    frontend/components/character-creation/character-workspace.tsx,
    frontend/components/character-creation/character-card.tsx,
    frontend/components/non-game-shell/app-shell.tsx,
    frontend/components/non-game-shell/app-sidebar.tsx,
    frontend/lib/api.ts,
    backend/src/routes/worldgen.ts,
    backend/src/routes/campaigns.ts
  </files>
  <read_first>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-CONTEXT.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-UAT.md,
    frontend/app/(non-game)/campaign/[id]/character/page.tsx,
    frontend/components/character-creation/character-workspace.tsx,
    frontend/components/character-creation/character-card.tsx
  </read_first>
  <action>
    Per D-01, D-03, D-04, D-07, D-08, D-10, and D-11, use real browser interactions to close blocked UAT tests 5 and 6:

    1. Continue from the already-fixed review route into character creation only after the world is genuinely ready.
    2. Exercise the parse-described-character path in the browser, then edit the resulting draft through the redesigned character UI.
    3. Explicitly verify the Phase 30 seams in the browser: persona-template control presence/selection or explicit empty-state behavior, start-condition inputs plus validation, and the canonical loadout path tied to the chosen setup.
    4. Exercise the canonical loadout flow explicitly: confirm the UI exposes the canonical loadout for the chosen character path, verify it can be selected or confirmed without hidden/dead controls, and verify adjacent edits do not silently clear that loadout choice.
    5. Exercise one invalid-input path in the character flow where the UI exposes it, such as empty parse text or invalid start-condition submission, and confirm the failure is explicit instead of silently dead-ending.
    6. Smoke the additional creation-mode toggles so they remain reachable and do not collapse into hidden/dead UI states.
    7. Save the character and confirm the handoff lands on `/game` without the non-game shell wrapping the game page.
    8. If any browser-visible regression appears, fix it immediately, rerun the exact failing browser segment, and rerun the affected automated suites before continuing.
    9. If any LLM-dependent character step fails, reuse the D-06 protocol already established in 33-08: up to 3 retries total with explicit attempt logging and clear final failure reporting before declaring the task blocked.

    Record concrete browser evidence for parse/edit/save, persona/start-condition/canonical-loadout coverage, invalid-input behavior, and `/game` handoff in `33-10-SUMMARY.md`.
  </action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx" && npm --prefix backend exec vitest run "src/routes/__tests__/campaigns.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Character creation is reached through browser navigation from review, not direct API calls.
    - Parse/edit/save is completed in the browser.
    - Persona/start-condition/canonical-loadout seams are exercised explicitly, including an invalid-input path where available.
    - `/game` opens without the non-game shell after character save.
    - Any browser-found regression is fixed inline and the exact failing segment is re-run.
    - If an LLM failure occurs, the same 3-attempt ceiling and final-failure reporting from 33-08 is followed.
  </acceptance_criteria>
  <done>Blocked UAT tests 5 and 6 are re-closed with browser evidence for character creation, canonical loadout coverage, and `/game` handoff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final desktop approval on the fixed non-game creation, review, and character flows</name>
  <files></files>
  <read_first>
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-08-SUMMARY.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-09-SUMMARY.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-10-SUMMARY.md,
    .planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-12-SUMMARY.md,
    docs/ui_concept_hybrid.html,
    .planning/debug/phase-33-uat-gap-1-shell-visuals.md,
    .planning/debug/phase-33-uat-gap-2.md
  </read_first>
  <action>
    After the automated browser reruns for 33-08 through 33-10 pass, ask the user to inspect the fixed desktop UI/UX and confirm the redesigned non-game flow is coherent and no longer blocked by the original complaints.
  </action>
  <what-built>Real browser re-verification of the original-world flow, D-04/D-06 recovery handling, known-IP review persistence, and character creation through `/game` handoff.</what-built>
  <how-to-verify>
    1. Open `/` and confirm the shell visuals remain coherent.
    2. Open `/campaign/new`, verify invalid inputs are handled clearly, enter concept data, and confirm back navigation does not wipe the flow.
    3. Continue to DNA, confirm visible progress, and verify empty-DNA or interruption cases no longer strand the UI.
    4. Run one known-IP flow into world review, edit a field, save, reload, and confirm the edit persists.
    5. Continue into character creation, verify persona/start-condition/canonical-loadout seams feel usable, save a character, and confirm `/game` opens without the non-game shell.
    6. Review the retry ledger for any LLM failure encountered and confirm the final reporting was clear.
  </how-to-verify>
  <verify>
    <automated>npm --prefix frontend run lint && npm --prefix frontend exec vitest run "frontend/app/(non-game)/campaign/new/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/new/dna/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx" "frontend/app/(non-game)/campaign/[id]/character/__tests__/page.test.tsx" && npm --prefix backend exec vitest run "src/routes/__tests__/campaigns.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - The user confirms the non-game shell and flow feel coherent at desktop resolution.
    - The user confirms review save/reload persistence and character-save-to-`/game` now work in the browser.
    - The user confirms any observed LLM failure reporting is explicit enough to debug rather than a silent dead-end.
    - Any remaining issue is narrow and concrete, not "the basic workflow is broken."
  </acceptance_criteria>
  <resume-signal>Type "approved" if the desktop UI/UX is fixed enough to close the Phase 33 UAT gaps, or describe the remaining issue.</resume-signal>
  <done>Final human approval is captured after the browser evidence for 33-08 through 33-10 is reviewed.</done>
</task>

</tasks>

<verification>
- Character creation is re-tested through real browser interactions on the fixed routes.
- Persona/start-condition/canonical-loadout seams and one invalid-input path are exercised explicitly.
- `/game` handoff is re-tested without the non-game shell.
- Frontend lint is green before final desktop approval is requested.
- Final approval happens only after the browser evidence from 33-08 through 33-10 exists.
</verification>

<success_criteria>
- Blocked UAT tests 5 and 6 are re-closed with real browser evidence.
- The full gap-closure path is preserved in order: 33-08 fixes the base flow, 33-09 re-closes review, 33-10 re-closes character plus final approval.
- The previously recorded frontend lint gap is closed before the phase asks for final approval.
- The user can approve the desktop UI/UX as usable again after reviewing concrete browser evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/33-browser-e2e-verification-for-redesigned-creation-flows/33-10-SUMMARY.md`
</output>
