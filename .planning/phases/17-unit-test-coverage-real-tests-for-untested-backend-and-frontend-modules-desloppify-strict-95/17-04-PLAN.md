---
phase: 17-unit-test-coverage
plan: 04
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified:
  - backend/src/routes/__tests__/ai.test.ts
  - backend/src/routes/__tests__/images.test.ts
  - backend/src/routes/__tests__/character.test.ts
autonomous: true
requirements:
  - TEST-ROUTES-1
must_haves:
  truths:
    - "ai route test verifies /providers/test calls testProviderConnection and returns result"
    - "ai route test verifies /ai/test-role resolves role model and calls generateText"
    - "images route test verifies GET /:campaignId/:type/:filename serves cached PNG with correct headers"
    - "images route test verifies GET rejects invalid type and invalid filename"
    - "images route test verifies POST /generate calls generateImage and cacheImage"
    - "character route test verifies /parse-character delegates to parseCharacterDescription or parseNpcDescription based on role"
    - "character route test verifies /save-character writes player to DB and returns playerId"
    - "character route test verifies /save-character rejects unknown location with 400"
  artifacts:
    - path: "backend/src/routes/__tests__/ai.test.ts"
      provides: "Unit tests for AI routes"
      min_lines: 60
    - path: "backend/src/routes/__tests__/images.test.ts"
      provides: "Unit tests for image routes"
      min_lines: 80
    - path: "backend/src/routes/__tests__/character.test.ts"
      provides: "Unit tests for character routes"
      min_lines: 100
  key_links:
    - from: "backend/src/routes/__tests__/ai.test.ts"
      to: "backend/src/routes/ai.ts"
      via: "import default app"
      pattern: "import.*from.*ai\\.js"
    - from: "backend/src/routes/__tests__/images.test.ts"
      to: "backend/src/routes/images.ts"
      via: "import default app"
      pattern: "import.*from.*images\\.js"
    - from: "backend/src/routes/__tests__/character.test.ts"
      to: "backend/src/routes/character.ts"
      via: "import default app"
      pattern: "import.*from.*character\\.js"
---

<objective>
Write real unit tests for three route modules: ai.ts, images.ts, character.ts.

Purpose: Route tests verify HTTP contract (status codes, response shapes, validation) by mounting routes on a Hono test app and using app.request(). All external dependencies (AI SDK, FS, DB) are mocked. Tests verify request validation, error handling, and correct delegation to service functions.
Output: Three test files following the established route test pattern from campaigns.test.ts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/routes/ai.ts
@backend/src/routes/images.ts
@backend/src/routes/character.ts
@backend/src/routes/__tests__/campaigns.test.ts
@backend/src/routes/schemas.ts
</context>

<interfaces>
From backend/src/routes/__tests__/campaigns.test.ts (route test pattern):
```typescript
// Setup: create Hono app, mount route, use app.request()
const app = new Hono();
app.route("/api/campaigns", campaignRoutes);

// Test: send request, check response
const res = await app.request("/api/campaigns", { method: "GET" });
expect(res.status).toBe(200);
const body = await res.json();
expect(body).toEqual([...]);
```

From backend/src/routes/schemas.ts (validation schemas used by routes):
```typescript
export const testProviderSchema = z.object({ baseUrl, model, apiKey });
export const testRoleSchema = z.object({ role, providers, roles });
export const imageGenerateSchema = z.object({ campaignId, type, entityId, prompt });
export const parseCharacterSchema = z.object({ campaignId, concept, role?, locationNames?, factionNames? });
export const saveCharacterSchema = z.object({ campaignId, character });
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test routes/ai.ts + routes/images.ts</name>
  <files>backend/src/routes/__tests__/ai.test.ts, backend/src/routes/__tests__/images.test.ts</files>
  <read_first>
    - backend/src/routes/ai.ts
    - backend/src/routes/images.ts
    - backend/src/routes/__tests__/campaigns.test.ts (test pattern)
    - backend/src/routes/schemas.ts (validation schemas)
    - backend/src/routes/helpers.ts (parseBody pattern)
  </read_first>
  <behavior>
    AI route tests:
    - Test 1: POST /providers/test with valid body calls testProviderConnection and returns result
    - Test 2: POST /providers/test with invalid body returns 400
    - Test 3: POST /ai/test-role with valid body resolves role model and calls generateText
    - Test 4: POST /ai/test-role with missing role config returns 400
    - Test 5: POST /ai/test-role with missing providerId returns 400
    - Test 6: POST /ai/test-role returns success:false on LLM error

    Image route tests:
    - Test 1: GET /:campaignId/:type/:filename serves PNG with correct Content-Type header
    - Test 2: GET with invalid type returns 400
    - Test 3: GET with invalid filename (path traversal) returns 400
    - Test 4: GET with nonexistent file returns 404
    - Test 5: POST /generate with valid body generates and caches image
    - Test 6: POST /generate with no image provider returns 400
    - Test 7: POST /generate with invalid body returns 400
  </behavior>
  <action>
    Create two test files.

    **ai.test.ts:** Mount ai routes at `/api`. Mock `ai` (generateText), `../../ai/index.js` (createModel, resolveRoleModel, testProviderConnection), `../../lib/index.js` (getErrorMessage, getErrorStatus, clampTokens), `../helpers.js` or let parseBody work with real Zod validation.

    For /providers/test: send POST with `{baseUrl:"http://test",model:"m",apiKey:"k"}`. Mock testProviderConnection to return `{ok:true}`. Verify 200.

    For /ai/test-role: send POST with `{role:"judge",providers:[{id:"p1",...}],roles:{judge:{providerId:"p1",model:"m"}}}`. Mock resolveRoleModel and generateText. Verify response includes success, role, model, response, latencyMs.

    **images.test.ts:** Mount images routes at `/api/images`. Mock `node:fs` (existsSync, readFileSync), `../../campaign/paths.js` (assertSafeId, getImagesDir), `../../images/index.js` (generateImage, resolveImageProvider, cacheImage), `../../settings/index.js` (loadSettings), `../../lib/index.js`.

    For GET: mock existsSync->true, readFileSync->Buffer.from("fake-png"). Verify Content-Type is image/png. Test invalid type ("videos") returns 400. Test invalid filename ("../etc/passwd") returns 400.

    For POST /generate: mock resolveImageProvider to return provider, generateImage to return Buffer. Verify cacheImage called with correct args.
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix backend run test -- --run src/routes/__tests__/ai.test.ts src/routes/__tests__/images.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" backend/src/routes/__tests__/ai.test.ts returns 5+
    - grep -c "describe\|it(" backend/src/routes/__tests__/images.test.ts returns 6+
    - grep "testProviderConnection\|generateText\|resolveRoleModel" backend/src/routes/__tests__/ai.test.ts returns matches
    - grep "Content-Type\|image/png\|cacheImage" backend/src/routes/__tests__/images.test.ts returns matches
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>ai.test.ts and images.test.ts pass with combined 11+ test cases covering all endpoints and error paths</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test routes/character.ts</name>
  <files>backend/src/routes/__tests__/character.test.ts</files>
  <read_first>
    - backend/src/routes/character.ts
    - backend/src/routes/__tests__/campaigns.test.ts (test pattern)
    - backend/src/routes/helpers.ts (setupCharacterEndpoint, requireActiveCampaign, resolveGenerator)
    - backend/src/routes/schemas.ts
  </read_first>
  <behavior>
    - Test 1: POST /parse-character with role=player calls parseCharacterDescription
    - Test 2: POST /parse-character with role=key calls parseNpcDescription
    - Test 3: POST /generate-character with role=player calls generateCharacter
    - Test 4: POST /generate-character with role=key calls generateNpcFromArchetype
    - Test 5: POST /import-v2-card with role=player calls mapV2CardToCharacter
    - Test 6: POST /import-v2-card with role=key calls mapV2CardToNpc
    - Test 7: POST /save-character saves player to DB and returns playerId
    - Test 8: POST /save-character rejects unknown location with 400
    - Test 9: POST /save-character deletes existing player before inserting new one
    - Test 10: POST /resolve-starting-location returns first isStarting location when no prompt
    - Test 11: POST /resolve-starting-location with prompt calls resolveStartingLocation
  </behavior>
  <action>
    Create test file at `backend/src/routes/__tests__/character.test.ts`.

    Mock all character service functions (`../../character/index.js`), DB (`../../db/index.js`), settings (`../../settings/index.js`), worldgen (`../../worldgen/index.js`), images (`../../images/index.js`), campaign (`../../campaign/index.js`), lib, and helpers.

    The key challenge is `setupCharacterEndpoint` and `requireActiveCampaign` from helpers.js. Two approaches:
    1. Mock `../helpers.js` entirely -- return mock context objects
    2. Mock underlying dependencies and let helpers run

    Prefer approach 1 for isolation. Mock `parseBody` to return parsed data, mock `setupCharacterEndpoint` to return `{campaign:{premise:"test"}, gen:{provider:mockProvider, temperature:0.7, maxTokens:4096}, names:{locationNames:["Tavern"], factionNames:["Guild"]}, settings:{...}}`, mock `requireActiveCampaign` to return campaign meta.

    For /save-character: mock getDb to return object with select/insert/delete chains. Mock select chain to return locations list `[{id:"loc-1",name:"Tavern"}]`. Verify delete(players) called before insert(players).

    For /resolve-starting-location: mock getDb select to return locations with isStarting flags.
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix backend run test -- --run src/routes/__tests__/character.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" backend/src/routes/__tests__/character.test.ts returns 10+
    - grep "parseCharacterDescription\|parseNpcDescription\|save-character\|resolve-starting-location" backend/src/routes/__tests__/character.test.ts returns matches
    - grep "400\|playerId" backend/src/routes/__tests__/character.test.ts returns matches
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>character.test.ts passes with 10+ test cases covering all 6 endpoints with role differentiation and validation</done>
</task>

</tasks>

<verification>
cd R:/Projects/WorldForge && npm --prefix backend run test -- --run src/routes/__tests__/ai.test.ts src/routes/__tests__/images.test.ts src/routes/__tests__/character.test.ts
</verification>

<success_criteria>
- All three test files exist and pass
- AI route tests verify provider test and role test endpoints
- Image route tests verify serving, validation, and generation
- Character route tests verify all 6 endpoints with player/key role differentiation
- Tests follow established Hono test pattern (mount route, app.request, check status/body)
</success_criteria>

<output>
After completion, create `.planning/phases/17-unit-test-coverage-real-tests-for-untested-backend-and-frontend-modules-desloppify-strict-95/17-04-SUMMARY.md`
</output>
