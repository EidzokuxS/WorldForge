---
phase: 17-unit-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/worldgen/__tests__/scaffold-saver.test.ts
  - backend/src/worldgen/__tests__/worldbook-importer.test.ts
autonomous: true
requirements:
  - TEST-WORLDGEN
must_haves:
  truths:
    - "scaffold-saver test verifies clearExistingScaffold deletes old data"
    - "scaffold-saver test verifies insertLocations, insertFactions, insertNpcs create correct DB rows"
    - "scaffold-saver test verifies updateAdjacency creates bidirectional connections"
    - "scaffold-saver test verifies membership and territory relationships are inserted"
    - "worldbook-importer test verifies parseWorldBook extracts unique named entries with HTML stripped"
    - "worldbook-importer test verifies classifyEntries calls generateObject and returns classified entries"
    - "worldbook-importer test verifies importClassifiedEntries routes entries to correct DB tables and lore cards"
  artifacts:
    - path: "backend/src/worldgen/__tests__/scaffold-saver.test.ts"
      provides: "Unit tests for scaffold-saver.ts"
      min_lines: 100
    - path: "backend/src/worldgen/__tests__/worldbook-importer.test.ts"
      provides: "Unit tests for worldbook-importer.ts"
      min_lines: 80
  key_links:
    - from: "backend/src/worldgen/__tests__/scaffold-saver.test.ts"
      to: "backend/src/worldgen/scaffold-saver.ts"
      via: "import saveScaffoldToDb"
      pattern: "import.*saveScaffoldToDb"
    - from: "backend/src/worldgen/__tests__/worldbook-importer.test.ts"
      to: "backend/src/worldgen/worldbook-importer.ts"
      via: "import parseWorldBook, classifyEntries, importClassifiedEntries"
      pattern: "import.*parseWorldBook"
---

<objective>
Write real unit tests for scaffold-saver.ts and worldbook-importer.ts.

Purpose: These worldgen modules handle DB writes (scaffold saving) and content parsing/classification (worldbook import). Tests verify actual logic -- transaction behavior, data mapping, HTML stripping, entry routing.
Output: Two test files with comprehensive coverage of all exported functions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/worldgen/scaffold-saver.ts
@backend/src/worldgen/worldbook-importer.ts
@backend/src/engine/__tests__/oracle.test.ts
</context>

<interfaces>
<!-- Existing mock patterns from the codebase -->

From backend/src/engine/__tests__/oracle.test.ts:
```typescript
vi.mock("ai", () => ({ generateObject: vi.fn() }));
vi.mock("../../ai/provider-registry.js", () => ({ createModel: vi.fn().mockReturnValue("mock-model") }));
```

From backend/src/routes/__tests__/campaigns.test.ts:
```typescript
vi.mock("../../db/index.js", () => ({ getDb: vi.fn() }));
vi.mock("../../lib/index.js", () => ({
  getErrorMessage: vi.fn((_err, fallback) => fallback),
  getErrorStatus: vi.fn(() => 500),
  createLogger: vi.fn(() => ({ info: vi.fn(), warn: vi.fn(), error: vi.fn() })),
}));
```

From backend/src/worldgen/scaffold-saver.ts:
```typescript
export function saveScaffoldToDb(campaignId: string, scaffold: WorldScaffold): void;
```

From backend/src/worldgen/worldbook-importer.ts:
```typescript
export function parseWorldBook(json: unknown): WorldBookEntry[];
export async function classifyEntries(entries: WorldBookEntry[], role: ResolvedRole): Promise<ClassifiedEntry[]>;
export async function importClassifiedEntries(campaignId: string, entries: ClassifiedEntry[], embedderResult: ResolveResult): Promise<ImportResult>;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffold-saver.ts</name>
  <files>backend/src/worldgen/__tests__/scaffold-saver.test.ts</files>
  <read_first>
    - backend/src/worldgen/scaffold-saver.ts
    - backend/src/worldgen/types.ts
    - backend/src/engine/__tests__/oracle.test.ts (mock patterns)
  </read_first>
  <behavior>
    - Test 1: saveScaffoldToDb calls db.transaction with a function
    - Test 2: Inside transaction, clearExistingScaffold deletes relationships, npcs, factions, locations for the campaignId
    - Test 3: insertLocations creates one row per location with correct fields (id, campaignId, name, description, tags as JSON, isStarting, connectedTo as "[]")
    - Test 4: updateAdjacency creates bidirectional connections -- if A connects to B, both A.connectedTo includes B and B.connectedTo includes A
    - Test 5: insertFactions creates rows with name, tags/goals/assets as JSON
    - Test 6: insertNpcs sets tier to "key", maps locationName to locationId via the map, stores goals as {short_term, long_term}
    - Test 7: insertMembershipRelationships creates relationship with ["Member"] tag for NPCs with factionName
    - Test 8: insertTerritoryRelationships creates relationship with ["Controls"] tag, deduplicates territory names
    - Test 9: campaign premise is updated via tx.update(campaigns).set({premise, updatedAt})
    - Test 10: NPC with unknown locationName gets null currentLocationId
    - Test 11: NPC with no factionName skips membership relationship
  </behavior>
  <action>
    Create test file at `backend/src/worldgen/__tests__/scaffold-saver.test.ts`.

    Mock `../db/index.js` (getDb) and `../db/schema.js` (campaigns, factions, locations, npcs, relationships).
    Mock `node:crypto` randomUUID to return predictable IDs (uuid-1, uuid-2, etc.).

    Create a mock DB that tracks all insert/update/delete calls. The mock `getDb()` should return an object with a `transaction` method that calls its callback with a mock `tx`. The mock `tx` needs chainable methods: `insert(table).values(data).run()`, `update(table).set(data).where(condition).run()`, `delete(table).where(condition).run()`.

    Build a test WorldScaffold with:
    - 2 locations (one with isStarting:true, connected to each other)
    - 1 faction with 1 territory
    - 2 NPCs (one with factionName, one without; one with valid locationName, one with unknown)

    Verify all insert calls received correct data shapes. Verify adjacency is bidirectional. Verify membership relationship only created for NPC with factionName.
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix backend run test -- --run src/worldgen/__tests__/scaffold-saver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" backend/src/worldgen/__tests__/scaffold-saver.test.ts returns 10+
    - grep "saveScaffoldToDb" backend/src/worldgen/__tests__/scaffold-saver.test.ts returns matches
    - grep "bidirectional\|connectedTo" backend/src/worldgen/__tests__/scaffold-saver.test.ts returns matches
    - grep "Member\|Controls" backend/src/worldgen/__tests__/scaffold-saver.test.ts returns matches
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>scaffold-saver.test.ts passes with 10+ test cases covering all DB operations, adjacency, and relationship logic</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test worldbook-importer.ts</name>
  <files>backend/src/worldgen/__tests__/worldbook-importer.test.ts</files>
  <read_first>
    - backend/src/worldgen/worldbook-importer.ts
    - backend/src/engine/__tests__/oracle.test.ts (mock patterns)
  </read_first>
  <behavior>
    - Test 1: parseWorldBook extracts entries from valid WorldBook JSON with comment/content fields
    - Test 2: parseWorldBook strips HTML tags from content
    - Test 3: parseWorldBook deduplicates entries by name (case-insensitive)
    - Test 4: parseWorldBook skips entries with empty name or empty content
    - Test 5: parseWorldBook throws on invalid JSON structure (missing entries key)
    - Test 6: classifyEntries returns empty array for empty input
    - Test 7: classifyEntries calls generateObject with correct schema and prompt containing entry names
    - Test 8: classifyEntries returns classified entries from generateObject result
    - Test 9: importClassifiedEntries routes characters to npcs table, locations to locations table, factions to factions table
    - Test 10: importClassifiedEntries routes bestiary and lore_general entries to storeLoreCards
    - Test 11: importClassifiedEntries returns correct counts in ImportResult
  </behavior>
  <action>
    Create test file at `backend/src/worldgen/__tests__/worldbook-importer.test.ts`.

    Mock `../ai/generate-object-safe.js` (safeGenerateObject as generateObject), `../ai/index.js` (createModel), `../db/index.js` (getDb), `../db/schema.js`, `../vectors/lore-cards.js` (storeLoreCards), `../lib/index.js` (createLogger).

    For parseWorldBook tests: create test WorldBook JSON objects with various edge cases (HTML in content, duplicate names, empty entries). These are pure sync tests -- no mocks needed beyond createLogger.

    For classifyEntries tests: mock generateObject to return a predetermined classification result. Verify the prompt contains entry names.

    For importClassifiedEntries tests: mock getDb with chainable insert tracker. Mock storeLoreCards. Create a mix of classified entries (2 characters, 1 location, 1 faction, 1 bestiary, 1 lore_general). Verify characters go to npcs table, bestiary+lore_general go to storeLoreCards, and ImportResult counts are correct.
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix backend run test -- --run src/worldgen/__tests__/worldbook-importer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" backend/src/worldgen/__tests__/worldbook-importer.test.ts returns 10+
    - grep "parseWorldBook" backend/src/worldgen/__tests__/worldbook-importer.test.ts returns matches
    - grep "classifyEntries" backend/src/worldgen/__tests__/worldbook-importer.test.ts returns matches
    - grep "importClassifiedEntries" backend/src/worldgen/__tests__/worldbook-importer.test.ts returns matches
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>worldbook-importer.test.ts passes with 10+ test cases covering parsing, classification, and import routing</done>
</task>

</tasks>

<verification>
cd R:/Projects/WorldForge && npm --prefix backend run test -- --run src/worldgen/__tests__/scaffold-saver.test.ts src/worldgen/__tests__/worldbook-importer.test.ts
</verification>

<success_criteria>
- Both test files exist and pass
- scaffold-saver tests verify transaction behavior, all insert types, bidirectional adjacency, relationship creation
- worldbook-importer tests verify parsing (HTML strip, dedup, validation), classification (generateObject call), and import routing (DB tables + lore cards)
- No test uses `any` escape hatch without justification
</success_criteria>

<output>
After completion, create `.planning/phases/17-unit-test-coverage-real-tests-for-untested-backend-and-frontend-modules-desloppify-strict-95/17-01-SUMMARY.md`
</output>
