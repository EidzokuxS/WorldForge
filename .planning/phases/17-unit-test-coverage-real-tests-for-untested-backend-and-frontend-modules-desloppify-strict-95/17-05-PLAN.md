---
phase: 17-unit-test-coverage
plan: 05
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified:
  - backend/src/routes/__tests__/worldgen.test.ts
  - .desloppify/config.json
autonomous: true
requirements:
  - TEST-ROUTES-2
  - DESLOPPIFY-CLEANUP
must_haves:
  truths:
    - "worldgen route test verifies /roll-seeds returns rolled seeds"
    - "worldgen route test verifies /generate streams SSE progress events"
    - "worldgen route test verifies /save-edits saves scaffold and marks generation complete"
    - "worldgen route test verifies /parse-worldbook parses and returns entries"
    - "worldgen route test verifies /import-worldbook classifies and imports entries"
    - "desloppify ignore patterns removed for tested modules"
    - "All backend and frontend tests pass together"
  artifacts:
    - path: "backend/src/routes/__tests__/worldgen.test.ts"
      provides: "Unit tests for worldgen routes"
      min_lines: 100
    - path: ".desloppify/config.json"
      provides: "Updated config with removed ignore patterns"
  key_links:
    - from: "backend/src/routes/__tests__/worldgen.test.ts"
      to: "backend/src/routes/worldgen.ts"
      via: "import default app"
      pattern: "import.*from.*worldgen\\.js"
---

<objective>
Write real unit tests for the worldgen route (the largest untested route), then remove desloppify ignore patterns for all newly tested modules.

Purpose: worldgen.ts has 362 LOC with many endpoints (roll-seeds, suggest-seeds, generate, save-edits, regenerate-section, parse-worldbook, import-worldbook). Tests verify HTTP contracts. After all tests pass, remove desloppify ignore patterns to improve strict score toward 95+.
Output: worldgen route test file + cleaned desloppify config.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/routes/worldgen.ts
@backend/src/routes/__tests__/campaigns.test.ts
@backend/src/routes/schemas.ts
@.desloppify/config.json
</context>

<interfaces>
From backend/src/routes/worldgen.ts (endpoints):
```typescript
// Sync endpoints:
POST /roll-seeds -> rollWorldSeeds()
POST /roll-seed -> rollSeed(body.category)
POST /suggest-seeds -> suggestWorldSeeds(...)
POST /suggest-seed -> suggestSingleSeed(...)
POST /save-edits -> saveScaffoldToDb + markGenerationComplete

// SSE streaming:
POST /generate -> streamSSE with premise/locations/factions/npcs/lore steps
POST /regenerate-section -> generateLocationsStep|generateFactionsStep|generateNpcsStep

// WorldBook:
POST /parse-worldbook -> parseWorldBook(body.json)
POST /import-worldbook -> classifyEntries + importClassifiedEntries
```

Current desloppify ignore patterns to remove:
```
test_coverage::backend/src/worldgen/*
test_coverage::backend/src/campaign/*
test_coverage::backend/src/routes/*
test_coverage::backend/src/ai/storyteller.ts::*
test_coverage::backend/src/ai/test-connection.ts::*
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test routes/worldgen.ts</name>
  <files>backend/src/routes/__tests__/worldgen.test.ts</files>
  <read_first>
    - backend/src/routes/worldgen.ts
    - backend/src/routes/__tests__/campaigns.test.ts (test pattern)
    - backend/src/routes/schemas.ts
    - backend/src/routes/helpers.ts (resolveGenerator, resolveEmbedder, requireActiveCampaign)
  </read_first>
  <behavior>
    - Test 1: POST /roll-seeds returns rolled seeds object
    - Test 2: POST /roll-seed with valid category returns single rolled seed
    - Test 3: POST /suggest-seeds calls suggestWorldSeeds with premise and returns result
    - Test 4: POST /suggest-seed calls suggestSingleSeed and returns result
    - Test 5: POST /save-edits calls saveScaffoldToDb and markGenerationComplete
    - Test 6: POST /save-edits with no active campaign returns error
    - Test 7: POST /parse-worldbook parses valid WorldBook JSON and returns entries array
    - Test 8: POST /parse-worldbook with invalid JSON returns 400
    - Test 9: POST /import-worldbook classifies entries and imports them
    - Test 10: POST /import-worldbook returns import counts
    - Test 11: POST /generate returns SSE stream (verify response is streaming)
    - Test 12: POST /regenerate-section calls the correct step generator based on section param
  </behavior>
  <action>
    Create test file at `backend/src/routes/__tests__/worldgen.test.ts`.

    Mock all worldgen service functions (`../../worldgen/index.js`): rollWorldSeeds, rollSeed, suggestWorldSeeds, suggestSingleSeed, generateWorldScaffold, generateRefinedPremiseStep, generateLocationsStep, generateFactionsStep, generateNpcsStep, saveScaffoldToDb, extractLoreCards.
    Mock `../../worldgen/worldbook-importer.js`: parseWorldBook, classifyEntries, importClassifiedEntries.
    Mock `../../campaign/index.js`: markGenerationComplete.
    Mock `../../vectors/lore-cards.js`: deleteCampaignLore, storeLoreCards.
    Mock `../../settings/index.js`: loadSettings.
    Mock `../../ai/index.js`: resolveRoleModel, createModel.
    Mock `../../ai/with-model-fallback.js`: resolveFallbackProvider.
    Mock `../../lib/index.js`: getErrorMessage, getErrorStatus, createLogger.
    Mock `../helpers.js`: parseBody (let through or mock), requireActiveCampaign, resolveGenerator, resolveEmbedder.

    Mount worldgen routes on Hono test app at `/api/worldgen`.

    For sync endpoints (roll-seeds, roll-seed, suggest-seeds, save-edits, parse-worldbook, import-worldbook): standard request/response testing.

    For /generate (SSE): this uses streamSSE which is harder to test. Verify the response status is 200 and content-type includes text/event-stream. Don't try to parse SSE events -- the streaming internals are Hono's responsibility.

    For /save-edits: mock requireActiveCampaign to return campaign, mock parseBody to return scaffold data. Verify saveScaffoldToDb and markGenerationComplete called.

    For /parse-worldbook: mock parseBody to return `{json: {entries: {"0": {comment:"Hero", content:"A brave warrior"}}}}`. Mock parseWorldBook to return parsed entries.

    For /import-worldbook: mock classifyEntries and importClassifiedEntries. Verify importClassifiedEntries returns counts.
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix backend run test -- --run src/routes/__tests__/worldgen.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" backend/src/routes/__tests__/worldgen.test.ts returns 10+
    - grep "roll-seeds\|save-edits\|parse-worldbook\|import-worldbook" backend/src/routes/__tests__/worldgen.test.ts returns matches
    - grep "saveScaffoldToDb\|markGenerationComplete\|classifyEntries" backend/src/routes/__tests__/worldgen.test.ts returns matches
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>worldgen.test.ts passes with 10+ test cases covering all major endpoints</done>
</task>

<task type="auto">
  <name>Task 2: Remove desloppify ignore patterns + verify all tests</name>
  <files>.desloppify/config.json</files>
  <read_first>
    - .desloppify/config.json
  </read_first>
  <action>
    Step 1: Run ALL tests to confirm everything passes.
    ```
    cd R:/Projects/WorldForge && npm --prefix backend run test -- --run
    cd R:/Projects/WorldForge && npm --prefix frontend run test -- --run
    ```

    Step 2: Read `.desloppify/config.json` and remove these ignore patterns from the `ignore` array:
    - `"test_coverage::backend/src/worldgen/*"`
    - `"test_coverage::backend/src/campaign/*"`
    - `"test_coverage::backend/src/routes/*"`
    - `"test_coverage::backend/src/ai/storyteller.ts::*"`
    - `"test_coverage::backend/src/ai/test-connection.ts::*"`

    Keep these patterns (genuinely untestable or deferred):
    - `"test_coverage::frontend/*"` -- React testing deferred
    - `"test_coverage::backend/src/db/*"` -- schema/init, no logic
    - `"test_coverage::backend/src/index.ts::*"` -- entry point

    Also keep all non-test_coverage ignores unchanged (orphaned::, smells::, structural::).

    Step 3: Write the updated config.json.

    Step 4: Run `npx desloppify scan --path .` from project root to verify the strict score. Report the score.

    Note: If strict score is still below 95, identify which remaining findings are blocking and report them -- do NOT add more ignore patterns.
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix backend run test -- --run && npm --prefix frontend run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - grep "test_coverage::backend/src/worldgen" .desloppify/config.json returns NO matches
    - grep "test_coverage::backend/src/campaign" .desloppify/config.json returns NO matches
    - grep "test_coverage::backend/src/routes" .desloppify/config.json returns NO matches
    - grep "test_coverage::backend/src/ai/storyteller" .desloppify/config.json returns NO matches
    - grep "test_coverage::frontend" .desloppify/config.json returns matches (kept)
    - grep "test_coverage::backend/src/db" .desloppify/config.json returns matches (kept)
    - All backend tests pass
    - All frontend tests pass
  </acceptance_criteria>
  <done>Desloppify ignore patterns removed for all newly-tested modules. All tests pass. Strict score reported.</done>
</task>

</tasks>

<verification>
cd R:/Projects/WorldForge && npm --prefix backend run test -- --run && npm --prefix frontend run test -- --run
</verification>

<success_criteria>
- worldgen.test.ts exists and passes with 10+ test cases
- desloppify config.json has 5 fewer test_coverage ignore patterns
- All backend and frontend tests pass together
- Desloppify strict score measured and reported
</success_criteria>

<output>
After completion, create `.planning/phases/17-unit-test-coverage-real-tests-for-untested-backend-and-frontend-modules-desloppify-strict-95/17-05-SUMMARY.md`
</output>
