---
phase: 17-unit-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/campaign/__tests__/manager.test.ts
  - backend/src/campaign/__tests__/checkpoints.test.ts
autonomous: true
requirements:
  - TEST-CAMPAIGN
must_haves:
  truths:
    - "manager test verifies createCampaign creates directory, config.json, state.db, and DB row"
    - "manager test verifies loadCampaign reads config, connects DB, returns CampaignMeta"
    - "manager test verifies deleteCampaign removes directory and clears activeCampaign"
    - "manager test verifies listCampaigns reads campaign directories and returns sorted list"
    - "manager test verifies validation rejects empty name/premise"
    - "checkpoints test verifies createCheckpoint backs up state.db, vectors/, chat_history.json"
    - "checkpoints test verifies loadCheckpoint restores all files and reconnects DB/vectors"
    - "checkpoints test verifies pruneAutoCheckpoints keeps only keepCount most recent"
  artifacts:
    - path: "backend/src/campaign/__tests__/manager.test.ts"
      provides: "Unit tests for campaign manager"
      min_lines: 120
    - path: "backend/src/campaign/__tests__/checkpoints.test.ts"
      provides: "Unit tests for checkpoint system"
      min_lines: 100
  key_links:
    - from: "backend/src/campaign/__tests__/manager.test.ts"
      to: "backend/src/campaign/manager.ts"
      via: "import createCampaign, loadCampaign, etc."
      pattern: "import.*createCampaign"
    - from: "backend/src/campaign/__tests__/checkpoints.test.ts"
      to: "backend/src/campaign/checkpoints.ts"
      via: "import createCheckpoint, loadCheckpoint, etc."
      pattern: "import.*createCheckpoint"
---

<objective>
Write real unit tests for campaign/manager.ts and campaign/checkpoints.ts.

Purpose: These modules handle campaign lifecycle (create/load/delete/list) and checkpoint save/restore. They involve heavy FS + DB interaction that must be mocked. Tests verify actual business logic -- validation, file operations, state management.
Output: Two test files with comprehensive coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/campaign/manager.ts
@backend/src/campaign/checkpoints.ts
@backend/src/campaign/paths.ts
</context>

<interfaces>
From backend/src/campaign/manager.ts:
```typescript
export function readCampaignConfig(campaignId: string): CampaignConfigFile;
export function listCampaigns(): CampaignMeta[];
export async function createCampaign(name: string, premise: string, seeds?: WorldSeeds): Promise<CampaignMeta>;
export async function loadCampaign(id: string): Promise<CampaignMeta>;
export async function deleteCampaign(id: string): Promise<void>;
export function markGenerationComplete(campaignId: string, refinedPremise: string): void;
export function getActiveCampaign(): CampaignMeta | null;
export function incrementTick(campaignId: string): number;
```

From backend/src/campaign/checkpoints.ts:
```typescript
export async function createCheckpoint(campaignId: string, opts?): Promise<CheckpointMeta>;
export function listCheckpoints(campaignId: string): CheckpointMeta[];
export async function loadCheckpoint(campaignId: string, checkpointId: string): Promise<CheckpointMeta>;
export function deleteCheckpoint(campaignId: string, checkpointId: string): void;
export function pruneAutoCheckpoints(campaignId: string, keepCount?: number): void;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test campaign/manager.ts</name>
  <files>backend/src/campaign/__tests__/manager.test.ts</files>
  <read_first>
    - backend/src/campaign/manager.ts
    - backend/src/campaign/paths.ts
    - backend/src/campaign/__tests__/chat-history.test.ts (existing mock patterns)
  </read_first>
  <behavior>
    - Test 1: createCampaign rejects empty name with AppError 400
    - Test 2: createCampaign rejects empty premise with AppError 400
    - Test 3: createCampaign creates campaign directory with recursive:true
    - Test 4: createCampaign writes config.json with name, premise, seeds, createdAt
    - Test 5: createCampaign calls connectDb, runMigrations, inserts campaign row
    - Test 6: createCampaign calls openVectorDb and sets activeCampaign
    - Test 7: createCampaign cleans up on error (rmSync, closeDb, closeVectorDb)
    - Test 8: loadCampaign throws 404 for missing directory
    - Test 9: loadCampaign reads config, connects DB, returns CampaignMeta
    - Test 10: deleteCampaign removes directory with rmSync
    - Test 11: deleteCampaign disconnects DB/vectors if deleting active campaign
    - Test 12: listCampaigns returns sorted campaign list from directory scan
    - Test 13: listCampaigns skips invalid campaign directories
    - Test 14: readCampaignConfig throws 404 for missing config.json
    - Test 15: readCampaignConfig throws 500 for invalid JSON
    - Test 16: markGenerationComplete updates config.json with generationComplete:true
    - Test 17: incrementTick reads current tick, writes incremented tick, returns new value
    - Test 18: getActiveCampaign returns null when no campaign loaded
  </behavior>
  <action>
    Create test file at `backend/src/campaign/__tests__/manager.test.ts`.

    Mock these modules:
    - `node:fs` -- existsSync, mkdirSync, writeFileSync, readFileSync, readdirSync, rmSync
    - `../db/index.js` -- connectDb, closeDb
    - `../db/migrate.js` -- runMigrations
    - `../db/schema.js` -- campaigns
    - `../vectors/index.js` -- openVectorDb, closeVectorDb
    - `../worldgen/index.js` -- parseWorldSeeds (return input or undefined)
    - `../lib/index.js` -- AppError class, createLogger
    - `./paths.js` -- assertSafeId, CAMPAIGNS_DIR, getCampaignConfigPath, getCampaignDir

    For `connectDb` mock: return object with `insert().values().run()`, `select().from().where().get()` chain.

    Test createCampaign: verify fs.mkdirSync called for campaign dir + vectors dir, fs.writeFileSync called for chat_history.json and config.json, connectDb called with correct path, campaign row inserted.

    Test loadCampaign: mock fs.existsSync to return true, fs.readFileSync to return valid config JSON, connectDb returns mock with select chain.

    Test deleteCampaign: verify fs.rmSync called, and if active campaign matches, closeDb/closeVectorDb called.

    Test listCampaigns: mock readdirSync to return directory entries, readCampaignConfig for each.

    Use beforeEach to reset all mocks and clear module state. Note: activeCampaign is module-level state -- use `getActiveCampaign()` to verify it.
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix backend run test -- --run src/campaign/__tests__/manager.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" backend/src/campaign/__tests__/manager.test.ts returns 15+
    - grep "createCampaign\|loadCampaign\|deleteCampaign\|listCampaigns" backend/src/campaign/__tests__/manager.test.ts returns matches
    - grep "AppError\|400\|404\|500" backend/src/campaign/__tests__/manager.test.ts returns matches
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>manager.test.ts passes with 15+ test cases covering CRUD, validation, error handling, and state management</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test campaign/checkpoints.ts</name>
  <files>backend/src/campaign/__tests__/checkpoints.test.ts</files>
  <read_first>
    - backend/src/campaign/checkpoints.ts
    - backend/src/campaign/paths.ts
    - backend/src/campaign/__tests__/chat-history.test.ts (existing mock patterns)
  </read_first>
  <behavior>
    - Test 1: createCheckpoint creates checkpoint directory with mkdirSync
    - Test 2: createCheckpoint calls getSqliteConnection().backup() for safe DB copy
    - Test 3: createCheckpoint copies vectors/ directory if it exists
    - Test 4: createCheckpoint copies chat_history.json if it exists
    - Test 5: createCheckpoint writes meta.json with id, name, description, createdAt, auto flag
    - Test 6: createCheckpoint ID format is {timestamp}-{sanitized-name}
    - Test 7: createCheckpoint cleans up on backup failure (rmSync checkpoint dir)
    - Test 8: createCheckpoint with auto:true sets auto flag in meta
    - Test 9: listCheckpoints returns empty array when checkpoints dir doesn't exist
    - Test 10: listCheckpoints reads meta.json from each subdirectory, returns sorted by createdAt desc
    - Test 11: listCheckpoints skips directories without meta.json
    - Test 12: loadCheckpoint throws 404 for missing checkpoint
    - Test 13: loadCheckpoint disconnects DB/vectors, copies files back, reconnects
    - Test 14: deleteCheckpoint removes checkpoint directory
    - Test 15: deleteCheckpoint throws 404 for missing checkpoint
    - Test 16: pruneAutoCheckpoints deletes oldest auto checkpoints beyond keepCount
    - Test 17: pruneAutoCheckpoints skips manual checkpoints
    - Test 18: sanitizeName converts special chars to hyphens, lowercases, trims to 40 chars
  </behavior>
  <action>
    Create test file at `backend/src/campaign/__tests__/checkpoints.test.ts`.

    Mock these modules:
    - `node:fs` -- existsSync, mkdirSync, readFileSync, writeFileSync, copyFileSync, cpSync, rmSync, readdirSync
    - `../db/index.js` -- connectDb, closeDb, getSqliteConnection
    - `../db/migrate.js` -- runMigrations
    - `../vectors/connection.js` -- openVectorDb, closeVectorDb
    - `../lib/index.js` -- AppError
    - `./paths.js` -- assertSafeId, getCampaignDir, getChatHistoryPath, getCheckpointsDir, getCheckpointDir

    For `getSqliteConnection` mock: return `{ backup: vi.fn().mockResolvedValue(undefined) }`.

    For paths mock: return predictable paths like `/campaigns/test-id`, `/campaigns/test-id/checkpoints/cp-1`.

    Test createCheckpoint: verify backup called, cpSync for vectors, copyFileSync for chat_history, writeFileSync for meta.json. Verify meta contains auto flag.

    Test listCheckpoints: mock readdirSync to return [{name:"cp-1",isDirectory:()=>true},{name:"cp-2",isDirectory:()=>true}], mock readFileSync to return meta JSON for each.

    Test loadCheckpoint: verify closeDb/closeVectorDb called, copyFileSync restores state.db, cpSync restores vectors, connectDb/runMigrations/openVectorDb called.

    Test pruneAutoCheckpoints: create scenario with 5 auto + 2 manual checkpoints, keepCount=3. Verify 2 oldest auto checkpoints deleted, manual untouched.
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix backend run test -- --run src/campaign/__tests__/checkpoints.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" backend/src/campaign/__tests__/checkpoints.test.ts returns 15+
    - grep "createCheckpoint\|loadCheckpoint\|deleteCheckpoint\|pruneAutoCheckpoints" backend/src/campaign/__tests__/checkpoints.test.ts returns matches
    - grep "backup\|copyFileSync\|cpSync" backend/src/campaign/__tests__/checkpoints.test.ts returns matches
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>checkpoints.test.ts passes with 15+ test cases covering create/load/delete/prune with correct FS operations</done>
</task>

</tasks>

<verification>
cd R:/Projects/WorldForge && npm --prefix backend run test -- --run src/campaign/__tests__/manager.test.ts src/campaign/__tests__/checkpoints.test.ts
</verification>

<success_criteria>
- Both test files exist and pass
- manager tests verify CRUD lifecycle, validation, error cleanup, state management
- checkpoints tests verify backup/restore file operations, pruning logic, sanitization
- Tests use proper mocking of fs, DB, and vector connections
</success_criteria>

<output>
After completion, create `.planning/phases/17-unit-test-coverage-real-tests-for-untested-backend-and-frontend-modules-desloppify-strict-95/17-02-SUMMARY.md`
</output>
