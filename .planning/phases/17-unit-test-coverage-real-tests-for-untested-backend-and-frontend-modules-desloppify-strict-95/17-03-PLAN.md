---
phase: 17-unit-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/__tests__/world-data-helpers.test.ts
  - frontend/lib/__tests__/v2-card-parser.test.ts
autonomous: true
requirements:
  - TEST-FRONTEND
must_haves:
  truths:
    - "world-data-helpers test verifies buildIdMaps creates correct ID-to-name maps"
    - "world-data-helpers test verifies buildRelationshipMaps correctly identifies Controls and Member relationships"
    - "world-data-helpers test verifies toEditableScaffold transforms WorldData with ID-based references to name-based references"
    - "v2-card-parser test verifies parseV2Json extracts name, description, personality, scenario, tags from V2 card"
    - "v2-card-parser test verifies parseV2Json handles V3 nested data format"
    - "v2-card-parser test verifies parseV2Png extracts character data from PNG tEXt chunk"
    - "v2-card-parser test verifies error handling for invalid JSON and missing PNG data"
  artifacts:
    - path: "frontend/lib/__tests__/world-data-helpers.test.ts"
      provides: "Unit tests for world data transformation helpers"
      min_lines: 80
    - path: "frontend/lib/__tests__/v2-card-parser.test.ts"
      provides: "Unit tests for V2/V3 card parser"
      min_lines: 60
  key_links:
    - from: "frontend/lib/__tests__/world-data-helpers.test.ts"
      to: "frontend/lib/world-data-helpers.ts"
      via: "import buildIdMaps, buildRelationshipMaps, toEditableScaffold"
      pattern: "import.*buildIdMaps"
    - from: "frontend/lib/__tests__/v2-card-parser.test.ts"
      to: "frontend/lib/v2-card-parser.ts"
      via: "import parseV2CardFile"
      pattern: "import.*parseV2CardFile"
---

<objective>
Write real unit tests for frontend pure logic modules: world-data-helpers.ts and v2-card-parser.ts.

Purpose: These are pure transformation functions with zero React dependencies. world-data-helpers converts DB-format WorldData (with UUIDs) into human-readable EditableScaffold (with names). v2-card-parser extracts character data from SillyTavern V2/V3 card files (JSON and PNG). Both are ideal TDD candidates.
Output: Two test files with comprehensive coverage of all exported functions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/lib/world-data-helpers.ts
@frontend/lib/v2-card-parser.ts
@frontend/lib/api-types.ts
@frontend/vitest.config.ts
</context>

<interfaces>
From frontend/lib/world-data-helpers.ts:
```typescript
export function buildIdMaps(world: WorldData): WorldIdMaps;
export function buildRelationshipMaps(world: WorldData, idMaps: WorldIdMaps): { factionTerritories, npcFaction };
export function toEditableScaffold(world: WorldData, premise: string, lore: LoreCardItem[]): EditableScaffold;
```

From frontend/lib/v2-card-parser.ts:
```typescript
export interface V2ImportPayload { name, description, personality, scenario, tags }
export async function parseV2CardFile(file: File): Promise<V2ImportPayload>;
// Internal: parseV2Json(text), parseV2Png(buffer) -- tested indirectly through parseV2CardFile
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test world-data-helpers.ts</name>
  <files>frontend/lib/__tests__/world-data-helpers.test.ts</files>
  <read_first>
    - frontend/lib/world-data-helpers.ts
    - frontend/lib/api-types.ts
    - frontend/vitest.config.ts
  </read_first>
  <behavior>
    - Test 1: buildIdMaps creates locationIdToName map with correct entries
    - Test 2: buildIdMaps creates factionIdToName map with correct entries
    - Test 3: buildIdMaps creates npcIdToName map with correct entries
    - Test 4: buildRelationshipMaps identifies Controls relationships (faction->location territory)
    - Test 5: buildRelationshipMaps identifies Member relationships (npc->faction membership)
    - Test 6: buildRelationshipMaps ignores relationships with unknown IDs
    - Test 7: toEditableScaffold converts location connectedTo from IDs to names
    - Test 8: toEditableScaffold maps NPC currentLocationId to locationName
    - Test 9: toEditableScaffold sets NPC locationName to "" when currentLocationId is null
    - Test 10: toEditableScaffold maps NPC factionName from Member relationships
    - Test 11: toEditableScaffold handles NPC goals in both snake_case and camelCase formats
    - Test 12: toEditableScaffold transforms lore cards into simplified objects
    - Test 13: toEditableScaffold maps faction territoryNames from Controls relationships
    - Test 14: toEditableScaffold filters out unknown connectedTo IDs (returns only valid names)
  </behavior>
  <action>
    Create test file at `frontend/lib/__tests__/world-data-helpers.test.ts`.

    No mocks needed -- these are pure functions. Create fixture WorldData objects with:
    - 2 locations (loc-1 "Tavern", loc-2 "Market") connected to each other
    - 1 faction (fac-1 "Guild") with Controls relationship to loc-1
    - 2 NPCs (npc-1 "Bartender" at loc-1, npc-2 "Wanderer" with no location) with npc-1 having Member relationship to fac-1
    - 2 relationships: Controls (fac-1 -> loc-1), Member (npc-1 -> fac-1)
    - 1 lore card item

    Test each function independently with the fixture data. Verify exact output shapes.

    For goals format test: create an NPC with goals as `{short_term: ["goal"]}` and another with `{shortTerm: ["goal"]}` -- both should resolve to `{shortTerm: ["goal"], longTerm: []}`.

    Ensure the `__tests__` directory exists under `frontend/lib/`.
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix frontend run test -- --run lib/__tests__/world-data-helpers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" frontend/lib/__tests__/world-data-helpers.test.ts returns 12+
    - grep "buildIdMaps\|buildRelationshipMaps\|toEditableScaffold" frontend/lib/__tests__/world-data-helpers.test.ts returns matches
    - grep "Controls\|Member" frontend/lib/__tests__/world-data-helpers.test.ts returns matches
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>world-data-helpers.test.ts passes with 12+ test cases covering all three exported functions with edge cases</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test v2-card-parser.ts</name>
  <files>frontend/lib/__tests__/v2-card-parser.test.ts</files>
  <read_first>
    - frontend/lib/v2-card-parser.ts
    - frontend/vitest.config.ts
  </read_first>
  <behavior>
    - Test 1: parseV2CardFile with JSON file extracts V2 card fields (name, description, personality, scenario, tags)
    - Test 2: parseV2CardFile handles V2 spec format (spec:"chara_card_v2" with nested data)
    - Test 3: parseV2CardFile handles root-level format (no spec, data at root)
    - Test 4: parseV2CardFile defaults missing fields to empty string/array
    - Test 5: parseV2CardFile throws on invalid JSON
    - Test 6: parseV2CardFile throws on missing name field
    - Test 7: parseV2CardFile with PNG extracts character data from tEXt chunk
    - Test 8: parseV2CardFile with PNG throws for invalid PNG (bad signature)
    - Test 9: parseV2CardFile with PNG throws when no chara tEXt chunk found
  </behavior>
  <action>
    Create test file at `frontend/lib/__tests__/v2-card-parser.test.ts`.

    For JSON tests: create mock File objects using `new File([jsonString], "card.json", {type: "application/json"})`. Test V2 format (`{spec:"chara_card_v2", data:{name:"Hero",...}}`), V3 format (same but with spec_version), and simple format (`{name:"Hero",...}`).

    For PNG tests: construct a minimal valid PNG buffer with a tEXt chunk containing base64-encoded V2 card JSON. The PNG structure:
    1. 8-byte PNG signature: [137, 80, 78, 71, 13, 10, 26, 10]
    2. IHDR chunk (13 bytes data): minimal 1x1 pixel header
    3. tEXt chunk: keyword "chara" + null byte + base64 of V2 JSON
    4. IEND chunk

    Use `new File([buffer], "card.png", {type: "image/png"})` for PNG File mock.

    Test invalid PNG: pass a file with wrong signature bytes.
    Test missing chara: pass a valid PNG with no tEXt chunk (just IHDR + IEND).

    Note: `atob` and `btoa` are available in vitest environment. If not, use `Buffer.from(...).toString('base64')` for encoding.
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix frontend run test -- --run lib/__tests__/v2-card-parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" frontend/lib/__tests__/v2-card-parser.test.ts returns 8+
    - grep "parseV2CardFile" frontend/lib/__tests__/v2-card-parser.test.ts returns matches
    - grep "tEXt\|chara\|PNG\|signature" frontend/lib/__tests__/v2-card-parser.test.ts returns matches
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>v2-card-parser.test.ts passes with 8+ test cases covering JSON parsing (V2/V3/simple), PNG extraction, and error handling</done>
</task>

</tasks>

<verification>
cd R:/Projects/WorldForge && npm --prefix frontend run test -- --run lib/__tests__/world-data-helpers.test.ts lib/__tests__/v2-card-parser.test.ts
</verification>

<success_criteria>
- Both test files exist and pass
- world-data-helpers tests verify all three exported functions with ID-to-name resolution, relationship mapping, and edge cases
- v2-card-parser tests verify JSON and PNG parsing with multiple V2/V3 card formats
- No React dependencies needed -- pure logic tests only
</success_criteria>

<output>
After completion, create `.planning/phases/17-unit-test-coverage-real-tests-for-untested-backend-and-frontend-modules-desloppify-strict-95/17-03-SUMMARY.md`
</output>
