---
phase: 20-npc-system-e2e-all-tiers-creation-interaction-simulation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [e2e/20-02-npc-browser-e2e.ts]
autonomous: true
gap_closure: true
requirements: [NPC-BROWSER-NARRATIVE, NPC-BROWSER-QUICKACTIONS]

must_haves:
  truths:
    - "getLastNarrativeText() returns non-empty string containing assistant narrative text"
    - "getQuickActions() returns array of quick action labels when quick actions are present"
    - "NPC name appears in at least 1 narrative response (programmatically confirmed, not just screenshots)"
  artifacts:
    - path: "e2e/20-02-npc-browser-e2e.ts"
      provides: "Fixed CSS selectors for narrative extraction and quick actions"
      contains: "div.group.relative"
  key_links:
    - from: "e2e/20-02-npc-browser-e2e.ts"
      to: "frontend/components/game/narrative-log.tsx"
      via: "CSS selector matching DOM structure"
      pattern: "div\\.group\\.relative.*> p"
    - from: "e2e/20-02-npc-browser-e2e.ts"
      to: "frontend/components/game/quick-actions.tsx"
      via: "CSS selector matching QuickActions component"
      pattern: "border-t.*border-border.*bg-muted"
---

<objective>
Fix broken CSS selectors in the browser E2E test that prevent automated verification of NPC narrative mentions and quick actions.

Purpose: Gaps 1 and 2 from VERIFICATION.md are test infrastructure bugs, not product bugs. The selectors in `getLastNarrativeText()` and `getQuickActions()` do not match the actual DOM structure, causing all narrative extraction and quick action detection to fail. Phase 21 already found the correct selectors (`.mx-auto.max-w-3xl div.group.relative > p` for narrative, and the QuickActions component renders inside `div.flex.flex-wrap.gap-2.border-t.border-border.bg-muted\/30 button` for quick actions).
Output: Updated `e2e/20-02-npc-browser-e2e.ts` with working selectors, re-run producing non-empty narrative text and detected quick actions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-npc-system-e2e-all-tiers-creation-interaction-simulation/20-VERIFICATION.md
@.planning/phases/20-npc-system-e2e-all-tiers-creation-interaction-simulation/20-02-SUMMARY.md

@e2e/20-02-npc-browser-e2e.ts
@frontend/components/game/narrative-log.tsx
@frontend/components/game/quick-actions.tsx

<interfaces>
<!-- Correct DOM selectors discovered by Phase 21 browser E2E -->

From narrative-log.tsx (assistant messages):
- Container: `.mx-auto.max-w-3xl` (wraps all messages)
- Assistant message wrapper: `div.group.relative` (each assistant message)
- Assistant text: `p` inside `div.group.relative` with classes `cursor-pointer whitespace-pre-wrap font-serif text-base leading-relaxed text-foreground`
- User message wrapper: `div` with classes `whitespace-pre-wrap border-l border-border pl-3 text-sm italic leading-relaxed text-muted-foreground`

Working selector for assistant text (from Phase 21 e2e/21-02-memory-browser-e2e.ts):
```typescript
var msgs = await page.$$(".mx-auto.max-w-3xl div.group.relative > p");
```

From quick-actions.tsx:
- Container: `div.flex.flex-wrap.gap-2.px-4.py-2.border-t.border-border.bg-muted\/30`
- Buttons: `button` children with classes `rounded-md border border-border bg-background px-3 py-1.5 text-sm`

Working selector for quick action buttons:
```typescript
var qaButtons = await page.$$("div.border-t.border-border.bg-muted\\/30 button");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix narrative and quick action CSS selectors</name>
  <files>e2e/20-02-npc-browser-e2e.ts</files>
  <read_first>
    - e2e/20-02-npc-browser-e2e.ts (lines 127-180 — getLastNarrativeText and getQuickActions functions)
    - e2e/21-02-memory-browser-e2e.ts (lines 76-82 — getLastAssistantText with working selector)
    - frontend/components/game/narrative-log.tsx (lines 114-154 — assistant message DOM structure)
    - frontend/components/game/quick-actions.tsx (lines 16-33 — QuickActions component DOM)
  </read_first>
  <action>
    Update `getLastNarrativeText()` in e2e/20-02-npc-browser-e2e.ts:
    - Replace the broken selector `.mx-auto.w-full.max-w-3xl .group.relative p.font-serif` with the working selector `.mx-auto.max-w-3xl div.group.relative > p`
    - Remove the fallback selector `section p.font-serif.text-base` (unnecessary with correct primary selector)
    - The `.w-full` class is NOT present on the container in the current DOM -- Phase 21 confirmed `.mx-auto.max-w-3xl` is the correct container selector

    Update `getQuickActions()` in e2e/20-02-npc-browser-e2e.ts:
    - Replace the broken selector `.flex.flex-wrap.gap-2.px-4.py-2 button` with `div.border-t.border-border button` (the QuickActions component renders a div with `border-t border-border bg-muted/30` containing button children)
    - The old selector was too specific and missed the `border-t border-border` classes that uniquely identify the QuickActions container

    Do NOT change any other test logic, turn count, delays, or assertions -- only fix the two selector functions.
  </action>
  <verify>
    <automated>grep -n "mx-auto.max-w-3xl div.group.relative" e2e/20-02-npc-browser-e2e.ts && grep -n "border-t.border-border" e2e/20-02-npc-browser-e2e.ts</automated>
  </verify>
  <acceptance_criteria>
    - getLastNarrativeText uses selector `.mx-auto.max-w-3xl div.group.relative > p`
    - getQuickActions uses selector `div.border-t.border-border button`
    - No other changes to the file
  </acceptance_criteria>
  <done>Both selector functions updated to match actual DOM structure. No other test logic changed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run browser E2E and verify narrative extraction works</name>
  <files>e2e/20-02-npc-browser-e2e.ts, e2e/screenshots/20-02-results.json</files>
  <read_first>
    - e2e/20-02-npc-browser-e2e.ts (full file — understand the test flow)
    - e2e/screenshots/20-02-results.json (current broken results showing narrativeMentionsNpc: false)
  </read_first>
  <action>
    Prerequisites: backend running on localhost:3001, frontend running on localhost:3000, E2E Dark Fantasy campaign (b85729f8) loaded.

    Run the updated browser E2E test:
    ```bash
    cd e2e && npx tsx 20-02-npc-browser-e2e.ts
    ```

    Wait for all 10 turns to complete (expect ~20-30 min with 60s delays and 300s turn timeouts).

    After completion, check results.json:
    - At least 1 turn should have `narrativeMentionsNpc: true` (narrative text extracted and NPC name found)
    - At least 1 turn should have `quickActionsCount > 0` (quick action buttons detected)

    If narrative extraction still returns empty strings, debug by adding a temporary `console.log` of the raw page HTML around the narrative container to identify the actual DOM structure. Then fix the selector.

    CRITICAL: Add 60s delay between turns to avoid GLM rate limits. Do NOT reduce delays.
    CRITICAL: Do NOT accept fallback/degraded behavior as passing. Narrative text must contain actual NPC names, not empty strings.
  </action>
  <verify>
    <automated>node -e "var r=JSON.parse(require('fs').readFileSync('e2e/screenshots/20-02-results.json','utf8')); var nm=r.filter(t=>t.narrativeMentionsNpc); var qa=r.filter(t=>t.quickActionsCount>0); console.log('NPC mentions:',nm.length,'Quick actions:',qa.length); if(nm.length===0) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - results.json has at least 1 turn with narrativeMentionsNpc: true
    - results.json has at least 1 turn with quickActionsCount > 0
    - No page crashes (0 console errors of type PAGE ERROR)
  </acceptance_criteria>
  <done>Browser E2E re-run produces programmatic confirmation of NPC narrative mentions and quick action detection. Gaps 1 and 2 closed.</done>
</task>

</tasks>

<verification>
- `grep "div.group.relative" e2e/20-02-npc-browser-e2e.ts` returns match
- `grep "border-t.border-border" e2e/20-02-npc-browser-e2e.ts` returns match
- results.json contains at least 1 turn with narrativeMentionsNpc: true
- results.json contains at least 1 turn with quickActionsCount > 0
</verification>

<success_criteria>
Gaps 1 and 2 from VERIFICATION.md are closed: broken CSS selectors fixed, re-run produces automated confirmation that NPC names appear in narrative text and quick actions are detected.
</success_criteria>

<output>
After completion, create `.planning/phases/20-npc-system-e2e-all-tiers-creation-interaction-simulation/20-03-SUMMARY.md`
</output>
