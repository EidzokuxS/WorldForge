---
phase: 25-replace-premise-override-heuristics-with-structured-divergence-interpretation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/types.ts
  - shared/src/index.ts
  - backend/src/worldgen/types.ts
  - backend/src/worldgen/premise-divergence.ts
  - backend/src/worldgen/index.ts
  - backend/src/routes/schemas.ts
  - backend/src/routes/worldgen.ts
  - backend/src/campaign/manager.ts
  - backend/src/worldgen/seed-suggester.ts
  - backend/src/worldgen/scaffold-generator.ts
  - frontend/lib/api.ts
  - frontend/components/title/use-new-campaign-wizard.ts
  - backend/src/worldgen/__tests__/premise-divergence.test.ts
  - backend/src/routes/__tests__/worldgen.test.ts
  - backend/src/campaign/__tests__/manager.test.ts
autonomous: true
requirements:
  - P25-01
  - P25-02
  - P25-03
must_haves:
  truths:
    - "Known-IP premise interpretation produces a structured divergence artifact before downstream generation runs."
    - "Canonical research data remains intact; divergence is cached beside ipContext instead of mutating canon."
    - "Suggest-seeds, generate, and regenerate-section can reuse the same premiseDivergence artifact."
  artifacts:
    - "shared/src/types.ts defines PremiseDivergence and related directive types."
    - "backend/src/worldgen/premise-divergence.ts exports the structured interpreter."
    - "backend/src/campaign/manager.ts persists and reloads premiseDivergence in campaign config."
  key_links:
    - "backend/src/routes/worldgen.ts computes or loads premiseDivergence and passes it into seed/scaffold generation."
    - "frontend/lib/api.ts and frontend/components/title/use-new-campaign-wizard.ts preserve hidden divergence handoff without UX changes."
---

<objective>
Establish the structured divergence contract and cache handoff for known-IP worldgen.

Purpose: Replace the premise-override mental model with a reusable artifact that every later step can consume.
Output: Shared types, interpreter module, route/cache plumbing, and regression tests proving divergence is computed once and carried safely.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-replace-premise-override-heuristics-with-structured-divergence-interpretation/25-CONTEXT.md
@.planning/phases/25-replace-premise-override-heuristics-with-structured-divergence-interpretation/25-RESEARCH.md
@.planning/phases/23-unified-research-world-generation-pipeline/23-01-SUMMARY.md
@.planning/phases/23-unified-research-world-generation-pipeline/23-02-SUMMARY.md
@.planning/phases/24-worldgen-known-ip-quality/24-04-SUMMARY.md
@shared/src/types.ts
@backend/src/campaign/manager.ts
@backend/src/routes/worldgen.ts
@backend/src/routes/schemas.ts
@backend/src/worldgen/seed-suggester.ts
@backend/src/worldgen/scaffold-generator.ts
@frontend/lib/api.ts
@frontend/components/title/use-new-campaign-wizard.ts

<interfaces>
From shared/src/types.ts:
```ts
export interface IpResearchContext {
  franchise: string;
  keyFacts: string[];
  tonalNotes: string[];
  canonicalNames?: {
    locations?: string[];
    factions?: string[];
    characters?: string[];
  };
  excludedCharacters?: string[];
  source: "mcp" | "llm";
}
```

From backend/src/worldgen/types.ts:
```ts
export interface GenerateScaffoldRequest {
  campaignId: string;
  name: string;
  premise: string;
  seeds?: Partial<WorldSeeds>;
  role: ResolvedRole;
  fallbackRole?: ResolvedRole;
  ipContext?: IpResearchContext | null;
  research?: ResearchConfig;
}
```

From frontend/lib/api.ts:
```ts
export function suggestSeeds(...): Promise<WorldSeeds & { _ipContext?: IpContext | null }>;
export async function generateWorld(
  campaignId: string,
  onProgress?: (progress: GenerationProgress) => void,
  ipContext?: IpContext | null,
): Promise<GenerateWorldResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define PremiseDivergence contracts and persistence surfaces</name>
  <files>shared/src/types.ts, shared/src/index.ts, backend/src/worldgen/types.ts, backend/src/routes/schemas.ts, backend/src/campaign/manager.ts, frontend/lib/api.ts, frontend/components/title/use-new-campaign-wizard.ts</files>
  <behavior>
    - Test 1: campaign config round-trips `premiseDivergence` beside `ipContext` without mutating legacy cached `ipContext`.
    - Test 2: route and API schemas accept optional `premiseDivergence` while remaining compatible with requests that only send `ipContext`.
  </behavior>
  <action>Add a shared `PremiseDivergence` artifact beside `IpResearchContext` per D-01, D-03, and D-06. Model the artifact as prompt-ready world-state directives, not name-suppression data: include divergence mode, protagonist role interpretation, preserved canon facts, changed canon facts, current-state directives, and uncertainty/ambiguity notes. Extend campaign config and route schemas with optional `premiseDivergence`; add load/save helpers mirroring the existing Phase 23 `ipContext` cache pattern. Preserve backward compatibility for existing configs and current frontend flows, but thread hidden `_premiseDivergence` handoff through the existing wizard/API plumbing with no UX changes.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/campaign/__tests__/manager.test.ts src/routes/__tests__/worldgen.test.ts &amp;&amp; npm --prefix frontend exec tsc --noEmit</automated>
  </verify>
  <done>Shared and route/campaign contracts compile, old cached configs still load, and the wizard can carry divergence alongside `_ipContext` without visible UI changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement structured premise interpretation and request-or-cache handoff</name>
  <files>backend/src/worldgen/premise-divergence.ts, backend/src/worldgen/index.ts, backend/src/worldgen/seed-suggester.ts, backend/src/worldgen/scaffold-generator.ts, backend/src/routes/worldgen.ts, backend/src/worldgen/__tests__/premise-divergence.test.ts, backend/src/routes/__tests__/worldgen.test.ts, backend/src/campaign/__tests__/manager.test.ts</files>
  <behavior>
    - Test 1: the motivating Voices of the Void premise returns `mode: "diverged"` with explicit protagonist replacement and preserved canon facts still intact.
    - Test 2: "I arrive in the Naruto world as an outsider" returns canonical or coexisting directives, not protagonist replacement.
    - Test 3: suggest-seeds, generate, and regenerate-section reuse cached divergence when available instead of reinterpreting ad hoc.
  </behavior>
  <action>Create `interpretPremiseDivergence(ipContext, premise, role)` in a dedicated module using structured `safeGenerateObject` only, per D-01, D-02, D-03, and D-05. Do not mutate `ipContext`, strip canonical names, or remove key facts. Wire `/suggest-seeds`, `/generate`, `/regenerate-section`, `suggestWorldSeeds()`, and `generateWorldScaffold()` to compute divergence once or load it from cache using the established Phase 23 request-or-cache pattern. Keep the result as a first-class artifact that later plans can thread into prompts; at this stage, focus on correct interpretation and transport, not prompt rewrites.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldgen/__tests__/premise-divergence.test.ts src/routes/__tests__/worldgen.test.ts src/campaign/__tests__/manager.test.ts</automated>
  </verify>
  <done>Every known-IP worldgen entry point has access to the same cached `premiseDivergence`, and interpretation no longer depends on mutating `IpResearchContext` or on regex/string heuristics.</done>
</task>

</tasks>

<verification>
Run the targeted backend tests plus frontend typecheck. Confirm the VotV motivating premise yields an explicit divergence artifact before any DNA or scaffold prompt is built.
</verification>

<success_criteria>
`PremiseDivergence` exists as a shared contract, request/cache plumbing carries it end-to-end, and the backend can distinguish canonical vs diverged known-IP premises without altering canonical research data.
</success_criteria>

<output>
After completion, create `.planning/phases/25-replace-premise-override-heuristics-with-structured-divergence-interpretation/25-01-SUMMARY.md`
</output>
