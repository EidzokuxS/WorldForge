---
phase: 25-replace-premise-override-heuristics-with-structured-divergence-interpretation
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - backend/src/worldgen/scaffold-steps/prompt-utils.ts
  - backend/src/worldgen/seed-suggester.ts
  - backend/src/worldgen/scaffold-generator.ts
  - backend/src/worldgen/scaffold-steps/premise-step.ts
  - backend/src/worldgen/scaffold-steps/locations-step.ts
  - backend/src/worldgen/scaffold-steps/factions-step.ts
  - backend/src/worldgen/scaffold-steps/npcs-step.ts
  - backend/src/worldgen/lore-extractor.ts
  - backend/src/worldgen/__tests__/seed-suggester.test.ts
  - backend/src/worldgen/__tests__/lore-extractor.test.ts
  - backend/src/worldgen/__tests__/npcs-step.test.ts
  - backend/src/worldgen/__tests__/scaffold-resilience.test.ts
autonomous: true
requirements:
  - P25-04
  - P25-05
must_haves:
  truths:
    - "DNA, refined premise, locations, factions, NPCs, and lore all receive explicit divergence instructions."
    - "Known-IP prompts reason from canonical baseline plus divergence consequences, not from excluded-name suppression."
    - "Unchanged canon stays explicit in prompt contracts, preserving Phase 24 fidelity."
  artifacts:
    - "backend/src/worldgen/scaffold-steps/prompt-utils.ts exposes divergence-aware prompt builders."
    - "All scaffold step modules and lore-extractor accept `premiseDivergence` inputs."
    - "Regression tests assert divergence content appears in prompt-bearing worldgen steps."
  key_links:
    - "backend/src/worldgen/scaffold-generator.ts passes `premiseDivergence` through every known-IP step."
    - "backend/src/worldgen/lore-extractor.ts grounds lore on canonical facts plus divergence directives."
---

<objective>
Migrate all known-IP prompt contracts from exclusion-centric heuristics to structured divergence instructions.

Purpose: Make downstream generation operate on world-state interpretation instead of one-off character suppression.
Output: Divergence-aware prompt helpers, updated worldgen step signatures, and regression coverage across DNA, scaffold, and lore.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-replace-premise-override-heuristics-with-structured-divergence-interpretation/25-CONTEXT.md
@.planning/phases/25-replace-premise-override-heuristics-with-structured-divergence-interpretation/25-RESEARCH.md
@.planning/phases/24-worldgen-known-ip-quality/24-01-SUMMARY.md
@.planning/phases/24-worldgen-known-ip-quality/24-02-SUMMARY.md
@.planning/phases/24-worldgen-known-ip-quality/24-03-SUMMARY.md
@backend/src/worldgen/scaffold-steps/prompt-utils.ts
@backend/src/worldgen/seed-suggester.ts
@backend/src/worldgen/scaffold-steps/premise-step.ts
@backend/src/worldgen/scaffold-steps/locations-step.ts
@backend/src/worldgen/scaffold-steps/factions-step.ts
@backend/src/worldgen/scaffold-steps/npcs-step.ts
@backend/src/worldgen/lore-extractor.ts

<interfaces>
From backend/src/worldgen/scaffold-steps/prompt-utils.ts:
```ts
export function buildIpContextBlock(ipContext: IpResearchContext | null): string;
export function buildCanonicalList(
  ipContext: IpResearchContext | null,
  type: "locations" | "factions" | "characters",
): string;
```

From backend/src/worldgen/scaffold-generator.ts:
```ts
export async function generateWorldScaffold(
  req: GenerateScaffoldRequest,
  onProgress?: (progress: GenerationProgress) => void,
): Promise<{ scaffold: WorldScaffold; enrichedIpContext: IpResearchContext | null }>;
```

From backend/src/worldgen/lore-extractor.ts:
```ts
export async function extractLoreCards(
  scaffold: WorldScaffold,
  role: ResolvedRole,
  fallbackRole?: ResolvedRole,
  ipContext?: IpResearchContext | null,
): Promise<ExtractedLoreCard[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace exclusion-centric prompt helpers with divergence-aware contracts</name>
  <files>backend/src/worldgen/scaffold-steps/prompt-utils.ts, backend/src/worldgen/seed-suggester.ts, backend/src/worldgen/scaffold-steps/premise-step.ts, backend/src/worldgen/__tests__/seed-suggester.test.ts, backend/src/worldgen/__tests__/scaffold-resilience.test.ts</files>
  <behavior>
    - Test 1: DNA prompts include preserved canon facts plus divergence directives, not only excluded characters.
    - Test 2: refined premise generation describes the current world state implied by divergence rather than a blind canon summary.
  </behavior>
  <action>Add divergence-aware prompt builders such as `buildPremiseDivergenceBlock()` and a canonical-preservation contract per D-04, D-05, and D-06. Update `suggestWorldSeeds()`, `suggestSingleSeed()`, and `generateRefinedPremiseStep()` to reason from `canonical baseline + interpreted divergence + generation target`. Remove prompt language that treats `excludedCharacters` or pruned `canonicalNames.characters` as the primary representation of change. Ambiguity must resolve conservatively: preserve canon unless the divergence artifact explicitly changes the current state.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldgen/__tests__/seed-suggester.test.ts src/worldgen/__tests__/scaffold-resilience.test.ts</automated>
  </verify>
  <done>DNA and refined-premise prompts carry explicit divergence blocks, preserve unchanged canon, and stop relying on exclusion-only wording to express world changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread divergence through locations, factions, NPCs, lore, and scaffold orchestration</name>
  <files>backend/src/worldgen/scaffold-generator.ts, backend/src/worldgen/scaffold-steps/locations-step.ts, backend/src/worldgen/scaffold-steps/factions-step.ts, backend/src/worldgen/scaffold-steps/npcs-step.ts, backend/src/worldgen/lore-extractor.ts, backend/src/worldgen/__tests__/npcs-step.test.ts, backend/src/worldgen/__tests__/lore-extractor.test.ts, backend/src/worldgen/__tests__/scaffold-resilience.test.ts</files>
  <behavior>
    - Test 1: known-IP step prompts receive the same cached divergence artifact all the way through locations, factions, NPCs, and lore.
    - Test 2: a protagonist replacement case changes current cast/state without erasing unrelated canon entities.
    - Test 3: a relationship or political divergence case changes only the affected facts while preserving untouched canon.
  </behavior>
  <action>Update all scaffold step signatures and orchestration to accept `premiseDivergence` from Plan 25-01. For known IPs, each step must explicitly operate on canonical baseline plus divergence consequences per D-04, D-05, and D-06. In NPC and lore prompts, describe who is active in the present world, which relationships or allegiances changed, and which canon facts remain intact; do not reintroduce replaced protagonists or reverted world-state facts unless the divergence object says they coexist.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldgen/__tests__/npcs-step.test.ts src/worldgen/__tests__/lore-extractor.test.ts src/worldgen/__tests__/scaffold-resilience.test.ts</automated>
  </verify>
  <done>Every known-IP generator step consumes `premiseDivergence`, the canonical-fidelity contract remains explicit, and downstream generation reflects world-state changes instead of character-name filtering.</done>
</task>

</tasks>

<verification>
Run the targeted worldgen test suite and inspect the prompt assertions for at least one protagonist replacement case and one non-replacement divergence case.
</verification>

<success_criteria>
All prompt-bearing known-IP generation steps receive a structured divergence artifact and preserve Phase 24 canonical fidelity while applying only divergence-driven consequences.
</success_criteria>

<output>
After completion, create `.planning/phases/25-replace-premise-override-heuristics-with-structured-divergence-interpretation/25-02-SUMMARY.md`
</output>
