---
phase: 25-replace-premise-override-heuristics-with-structured-divergence-interpretation
plan: 03
type: execute
wave: 3
depends_on:
  - 25-01
  - 25-02
files_modified:
  - backend/src/worldgen/ip-context-overrides.ts
  - backend/src/worldgen/index.ts
  - backend/src/worldgen/__tests__/ip-context-overrides.test.ts
  - backend/src/worldgen/__tests__/premise-divergence.test.ts
  - backend/src/worldgen/__tests__/seed-suggester.test.ts
  - backend/src/worldgen/__tests__/lore-extractor.test.ts
  - backend/src/routes/__tests__/worldgen.test.ts
  - backend/src/campaign/__tests__/manager.test.ts
autonomous: true
requirements:
  - P25-06
must_haves:
  truths:
    - "The old premise-override helper is no longer on the critical path for known-IP worldgen."
    - "Regression tests prove canonical and diverged scenarios both behave correctly."
    - "Phase 24 fidelity remains intact while divergence changes only the affected world-state facts."
  artifacts:
    - "Legacy override module is removed or reduced to a compatibility shim that delegates to structured divergence."
    - "Regression fixtures cover protagonist replacement, relationship divergence, political divergence, and outsider-arrival non-replacement."
    - "Route and cache tests prove regenerate-section reuses cached divergence."
  key_links:
    - "backend/src/worldgen/index.ts exports the new divergence path and no longer exposes the old helper as the live mechanism."
    - "backend/src/routes/__tests__/worldgen.test.ts and backend/src/campaign/__tests__/manager.test.ts lock cache reuse behavior."
---

<objective>
Retire the old override mechanism and lock Phase 25 behavior with focused regressions.

Purpose: Finish the migration safely, proving the system now solves general world-state interpretation without broad regressions.
Output: Legacy cleanup, compatibility handling, and high-signal regression tests for canonical and diverged known-IP cases.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-replace-premise-override-heuristics-with-structured-divergence-interpretation/25-CONTEXT.md
@.planning/phases/25-replace-premise-override-heuristics-with-structured-divergence-interpretation/25-RESEARCH.md
@.planning/phases/24-worldgen-known-ip-quality/24-03-SUMMARY.md
@backend/src/worldgen/ip-context-overrides.ts
@backend/src/worldgen/index.ts
@backend/src/worldgen/__tests__/ip-context-overrides.test.ts
@backend/src/worldgen/__tests__/premise-divergence.test.ts
@backend/src/routes/__tests__/worldgen.test.ts

<interfaces>
From backend/src/worldgen/index.ts:
```ts
export { suggestWorldSeeds, suggestSingleSeed } from "./seed-suggester.js";
export {
  generateWorldScaffold,
  generateRefinedPremiseStep,
  generateLocationsStep,
  generateFactionsStep,
  generateNpcsStep,
} from "./scaffold-generator.js";
export { applyPremiseCharacterOverrides } from "./ip-context-overrides.js";
```

From backend/src/worldgen/ip-context-overrides.ts:
```ts
export async function applyPremiseCharacterOverrides(
  ipContext: IpResearchContext | null | undefined,
  premise: string,
  role: ResolvedRole,
): Promise<IpResearchContext | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the legacy override heuristic from the live worldgen path</name>
  <files>backend/src/worldgen/ip-context-overrides.ts, backend/src/worldgen/index.ts, backend/src/worldgen/__tests__/ip-context-overrides.test.ts</files>
  <action>Per D-01, D-02, and D-03, remove `applyPremiseCharacterOverrides()` as the live known-IP mechanism. Either delete the module entirely or reduce it to a narrow compatibility shim that delegates to the new divergence interpreter without mutating `IpResearchContext`. Keep tolerance for old cached data only where needed for migration safety, but eliminate any live prompt or route logic that depends on `excludedCharacters` semantics.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldgen/__tests__/ip-context-overrides.test.ts src/worldgen/__tests__/premise-divergence.test.ts</automated>
  </verify>
  <done>No known-IP generation entry point relies on canon-pruning or string-suppression heuristics, and any remaining legacy surface is explicitly compatibility-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock high-signal regression scenarios for divergence interpretation and canon preservation</name>
  <files>backend/src/worldgen/__tests__/premise-divergence.test.ts, backend/src/worldgen/__tests__/seed-suggester.test.ts, backend/src/worldgen/__tests__/lore-extractor.test.ts, backend/src/routes/__tests__/worldgen.test.ts, backend/src/campaign/__tests__/manager.test.ts</files>
  <behavior>
    - Test 1: "Voices of the Void, but I'm playing with my own char instead of Dr Kel, I've just arrived" changes the current protagonist role while preserving the rest of canon.
    - Test 2: "Naruto, but Sakura was trained by Orochimaru" changes relationship/current-state directives without suppressing unrelated canon cast.
    - Test 3: "Star Wars, but Order 66 failed" changes political/current-state facts while preserving canonical factions, characters, and locations.
    - Test 4: "I arrive in the Naruto world as an outsider" stays canonical or coexisting and never implies protagonist replacement.
    - Test 5: regenerate-section and cached campaign config reuse the same `premiseDivergence` artifact instead of reinterpreting ad hoc.
  </behavior>
  <action>Add explicit regression fixtures and assertions covering interpreter output, prompt blocks, route/cache handoff, and canonical-preservation guarantees per D-05 and D-06. Use the four scenario classes above so the suite proves the system now solves world-state interpretation, not only character suppression. Keep the coverage tight and executable in under a minute.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldgen/__tests__/premise-divergence.test.ts src/worldgen/__tests__/seed-suggester.test.ts src/worldgen/__tests__/lore-extractor.test.ts src/routes/__tests__/worldgen.test.ts src/campaign/__tests__/manager.test.ts</automated>
  </verify>
  <done>The phase has focused regressions for canonical, divergent, and ambiguous premises, and those tests prove cached divergence reuse plus Phase 24 fidelity preservation.</done>
</task>

</tasks>

<verification>
Run the final targeted regression suite and confirm the motivating VotV case plus the three broader divergence scenarios all pass.
</verification>

<success_criteria>
The old override heuristic is off the critical path, structured divergence is the only live interpretation model, and the targeted regression suite proves no canonical-fidelity regression.
</success_criteria>

<output>
After completion, create `.planning/phases/25-replace-premise-override-heuristics-with-structured-divergence-interpretation/25-03-SUMMARY.md`
</output>
