---
phase: 42-targeted-oracle-and-start-condition-runtime-effects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/engine/target-context.ts
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/__tests__/oracle.test.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
autonomous: true
requirements:
  - GSEM-01
must_haves:
  truths:
    - "Player actions against supported concrete targets feed non-empty target-aware context into Oracle evaluation instead of always using `targetTags: []`."
    - "Phase 42 first-class target support covers `character`, `item`, and `location/object`; unsupported or unresolved targets degrade honestly to non-targeted rulings."
    - "The same action can produce different Oracle input and potentially different odds when a supported concrete target is resolved."
    - "Target extraction is backend-owned and bounded: prefer existing parsed intent/method when they already expose a concrete target, otherwise use one narrow target-candidate classifier seam that coordinates with movement detection instead of inventing a second free-text parser stack."
  artifacts:
    - path: "backend/src/engine/target-context.ts"
      provides: "normalized target-resolution seam for supported gameplay entity types with honest fallback and explicit target-candidate extraction"
      contains: "detectActionTargetCandidate|resolveActionTargetContext|deriveTargetTags"
    - path: "backend/src/engine/turn-processor.ts"
      provides: "player-action Oracle path that supplies resolved target context instead of hard-coded empty target tags"
      contains: "processTurn"
    - path: "backend/src/engine/__tests__/turn-processor.test.ts"
      provides: "regressions proving targeted and untargeted actions diverge in Oracle input"
      contains: "target-aware oracle"
  key_links:
    - from: "backend/src/engine/turn-processor.ts"
      to: "backend/src/engine/target-context.ts"
      via: "player action resolution populates target-aware Oracle payload before callOracle"
      pattern: "resolveActionTargetContext|targetTags"
    - from: "backend/src/engine/target-context.ts"
      to: "backend/src/engine/tool-executor.ts"
      via: "target resolution reuses or mirrors existing runtime entity lookup semantics instead of inventing a parallel ontology"
      pattern: "resolveEntity|resolveCharacterByName"
---

<objective>
Make Oracle rulings target-aware for supported concrete targets in the player action path.

Purpose: satisfy `GSEM-01` by replacing the current `targetTags: []` player-turn seam with real target resolution for supported entity types while preserving an honest non-targeted fallback.
Output: a reusable target-context helper, player-turn Oracle wiring for supported targets, and tests proving targeted actions differ from untargeted ones.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-CONTEXT.md
@.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-RESEARCH.md
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-HANDOFF.md
@backend/src/engine/oracle.ts
@backend/src/engine/turn-processor.ts
@backend/src/engine/tool-executor.ts
@backend/src/engine/__tests__/oracle.test.ts
@backend/src/engine/__tests__/turn-processor.test.ts

<interfaces>
From `backend/src/engine/oracle.ts`:
```ts
export interface OraclePayload {
  intent: string;
  method: string;
  actorTags: string[];
  targetTags: string[];
  environmentTags: string[];
  sceneContext: string;
}
```

From `backend/src/engine/turn-processor.ts`:
```ts
export async function* processTurn(options: TurnOptions): AsyncGenerator<TurnEvent>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock target-aware Oracle regressions in tests</name>
  <read_first>
@.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-CONTEXT.md
@.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-RESEARCH.md
@backend/src/engine/oracle.ts
@backend/src/engine/turn-processor.ts
@backend/src/engine/__tests__/oracle.test.ts
@backend/src/engine/__tests__/turn-processor.test.ts
  </read_first>
  <files>backend/src/engine/__tests__/oracle.test.ts, backend/src/engine/__tests__/turn-processor.test.ts</files>
  <acceptance_criteria>
    - `npm --prefix backend exec vitest run src/engine/__tests__/oracle.test.ts src/engine/__tests__/turn-processor.test.ts` fails only because target-aware player-turn resolution is not implemented yet.
    - `rg -n "target-aware|targetTags|unsupported target|location/object|item" backend/src/engine/__tests__/oracle.test.ts backend/src/engine/__tests__/turn-processor.test.ts` finds direct coverage for `GSEM-01`.
  </acceptance_criteria>
  <behavior>
    - Test 1: the same player action with a supported concrete target and without a target yields different Oracle payload input.
    - Test 2: supported `character`, `item`, and `location/object` targets contribute non-empty target tags when they resolve.
    - Test 3: unresolved or unsupported targets fall back to `targetTags: []` intentionally rather than pretending target-aware resolution succeeded.
    - Test 4: Oracle contract coverage still reflects `targetTags` as a first-class payload field.
    - Test 5: movement-only actions do not trigger a conflicting second target parse path, and mixed actions stay on one explicit extraction contract.
  </behavior>
  <action>Write failing regressions first around the current player-turn seam that hard-codes `targetTags: []`. Use current live supported entity categories from context and research: `character`, `item`, and `location/object`. Explicitly pin the honest fallback path for unsupported or unresolved targets so implementation cannot silently overclaim coverage. Also lock the extraction contract: if the current parsed action intent/method already yields a concrete target candidate, that path wins; otherwise one bounded target-candidate classifier seam may run, coordinated with existing movement detection so the turn loop does not grow a second conflicting free-text parser stack. Keep scope bounded to the player Oracle path; do not add travel/time behavior, companion systems, or inventory-authority redesign.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/oracle.test.ts src/engine/__tests__/turn-processor.test.ts</automated>
  </verify>
  <done>The tests prove the remaining Phase 42 target-aware gap is implementation, not ambiguity about supported target types or fallback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement normalized target resolution for player Oracle calls</name>
  <read_first>
@backend/src/engine/oracle.ts
@backend/src/engine/turn-processor.ts
@backend/src/engine/tool-executor.ts
@backend/src/engine/__tests__/oracle.test.ts
@backend/src/engine/__tests__/turn-processor.test.ts
  </read_first>
  <files>backend/src/engine/target-context.ts, backend/src/engine/turn-processor.ts</files>
  <acceptance_criteria>
    - `rg -n "resolveActionTargetContext|targetTags|fallback" backend/src/engine/target-context.ts backend/src/engine/turn-processor.ts` shows one reusable target-resolution seam and no hard-coded `targetTags: []` player path for supported targets.
    - `npm --prefix backend exec vitest run src/engine/__tests__/oracle.test.ts src/engine/__tests__/turn-processor.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: player Oracle calls use resolved target context for supported target types before evaluating the action.
    - Test 2: the target taxonomy stays bounded to `character`, `item`, and `location/object`, with faction handling only where direct runtime context already exists.
    - Test 3: unsupported or unresolved targets explicitly degrade to non-targeted Oracle evaluation.
    - Test 4: the implementation reuses existing runtime entity semantics rather than inventing a new parallel target ontology.
    - Test 5: target-tag derivation is explicit by entity type: characters use derived runtime character tags, while items and locations/objects use normalized runtime tags already stored on those entities.
  </behavior>
  <action>Create a normalized target-context helper for player actions. Use a bounded extraction strategy: first consume any concrete target candidate already recoverable from the parsed player action intent/method; if that is insufficient, call one narrow target-candidate classifier seam that is explicitly coordinated with `detectMovement()` so movement-only turns and mixed turns do not fork into contradictory parsing paths. Resolve supported targets from live runtime state using existing entity lookup semantics as the baseline, then derive Oracle tags explicitly per entity type: `character` uses derived runtime character tags, `item` uses normalized stored item tags, and `location/object` uses normalized stored location tags. Update `turn-processor.ts` so player Oracle calls use that helper instead of always sending `targetTags: []`. Keep the implementation backend-first and text-compatible; do not require a new target-selection UI in this phase unless tests prove the backend-only path is insufficient. Do not widen scope into travel/time, faction strategy systems, or generic prompt cleanup.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/oracle.test.ts src/engine/__tests__/turn-processor.test.ts</automated>
  </verify>
  <done>Player-turn Oracle evaluation is now genuinely target-aware for supported target types, with an explicit honest fallback when no supported target resolves.</done>
</task>

</tasks>

<verification>
Run the Oracle and turn-processor suite above. Confirm the player-turn path no longer always uses empty target tags, that supported target types materially change Oracle input, and that fallback behavior stays explicit.
</verification>

<success_criteria>
The repo now has one honest target-aware Oracle seam for player actions. Acting against a concrete supported entity is mechanically different from acting into the void, and unsupported targets fail gracefully instead of faking coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-01-SUMMARY.md`
</output>
