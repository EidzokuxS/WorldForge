---
phase: 42-targeted-oracle-and-start-condition-runtime-effects
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - backend/src/engine/start-condition-runtime.ts
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/prompt-assembler.ts
  - backend/src/routes/character.ts
  - backend/src/routes/__tests__/character.test.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/character/__tests__/loadout-deriver.test.ts
autonomous: true
requirements:
  - GSEM-02
must_haves:
  truths:
    - "Structured start conditions have backend-observable mechanical effects in early gameplay instead of existing only as narration flavor."
    - "The mechanically binding start-condition fields for this phase are `startLocationId`, `arrivalMode`, `startingVisibility`, `entryPressure`, `companions`, and `immediateSituation`."
    - "Start-condition-driven mechanics survive reload, retry, and checkpoint restore by living in authoritative runtime state or being deterministically re-derived from canonical saved state."
  artifacts:
    - path: "backend/src/engine/start-condition-runtime.ts"
      provides: "translation from canonical start conditions into bounded runtime flags, action gating, and Oracle modifiers for the opening state"
      contains: "deriveStartConditionEffects|applyStartConditionEffects"
    - path: "backend/src/routes/character.ts"
      provides: "save/handoff path that preserves or materializes start-condition-driven mechanics without widening inventory authority"
      contains: "save-character"
    - path: "backend/src/engine/turn-processor.ts"
      provides: "early-game consumption of start-condition-derived mechanics in live play"
      contains: "processTurn"
  key_links:
    - from: "backend/src/routes/character.ts"
      to: "backend/src/engine/start-condition-runtime.ts"
      via: "canonical saved start conditions become backend-owned runtime effects instead of prompt-only flavor"
      pattern: "startConditions|deriveStartConditionEffects"
    - from: "backend/src/engine/turn-processor.ts"
      to: "backend/src/engine/start-condition-runtime.ts"
      via: "opening-state flags and modifiers influence action evaluation and early-scene behavior"
      pattern: "startConditions|applyStartConditionEffects"
    - from: "backend/src/engine/prompt-assembler.ts"
      to: "backend/src/engine/start-condition-runtime.ts"
      via: "prompt surfacing stays aligned with the new runtime mechanics instead of diverging into flavor-only narration"
      pattern: "Opening Situation|Visibility|entryPressure"
---

<objective>
Turn structured start conditions into bounded early-game runtime mechanics.

Purpose: satisfy `GSEM-02` by making saved opening-state fields mechanically consequential through runtime flags, action gating, and Oracle modifiers that survive reload/retry/checkpoint restore.
Output: a backend-owned start-condition runtime seam, save/handoff integration, and regression coverage for persistence and early-game effects.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-CONTEXT.md
@.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-RESEARCH.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-VERIFICATION.md
@backend/src/routes/character.ts
@backend/src/worldgen/starting-location.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/turn-processor.ts
@backend/src/character/loadout-deriver.ts
@backend/src/routes/__tests__/character.test.ts
@backend/src/engine/__tests__/turn-processor.test.ts
@backend/src/character/__tests__/loadout-deriver.test.ts

<interfaces>
From `node_modules/@worldforge/shared/src/types.ts`:
```ts
export interface CharacterStartConditions {
  startLocationId?: string | null;
  arrivalMode?: string | null;
  immediateSituation?: string | null;
  entryPressure?: string[];
  companions?: string[];
  startingVisibility?: string | null;
  resolvedNarrative?: string | null;
  sourcePrompt?: string | null;
}
```

From `backend/src/routes/character.ts`:
```ts
app.post("/save-character", async (c) => { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock start-condition runtime-effect regressions in tests</name>
  <read_first>
@.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-CONTEXT.md
@.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-RESEARCH.md
@backend/src/routes/character.ts
@backend/src/engine/turn-processor.ts
@backend/src/character/loadout-deriver.ts
@backend/src/routes/__tests__/character.test.ts
@backend/src/engine/__tests__/turn-processor.test.ts
@backend/src/character/__tests__/loadout-deriver.test.ts
  </read_first>
  <files>backend/src/routes/__tests__/character.test.ts, backend/src/engine/__tests__/turn-processor.test.ts, backend/src/character/__tests__/loadout-deriver.test.ts</files>
  <acceptance_criteria>
    - `npm --prefix backend exec vitest run src/routes/__tests__/character.test.ts src/engine/__tests__/turn-processor.test.ts src/character/__tests__/loadout-deriver.test.ts` fails only because backend-owned start-condition runtime effects are not fully implemented yet.
    - `rg -n "arrivalMode|startingVisibility|entryPressure|companions|immediateSituation|opening state|start condition" backend/src/routes/__tests__/character.test.ts backend/src/engine/__tests__/turn-processor.test.ts backend/src/character/__tests__/loadout-deriver.test.ts` finds direct coverage for `GSEM-02`.
  </acceptance_criteria>
  <behavior>
    - Test 1: saved start conditions influence early gameplay mechanically, not just prompt wording.
    - Test 2: `startLocationId`, `arrivalMode`, `startingVisibility`, `entryPressure`, `companions`, and `immediateSituation` are all represented in the runtime contract for opening-state mechanics.
    - Test 3: companion effects remain narrow runtime presence/context, not party management.
    - Test 4: existing start-condition-derived loadout behavior remains intact and aligned with the new mechanics.
  </behavior>
  <action>Write failing regressions first around the currently prompt-heavy opening-state seam. Lock that the selected start-condition fields become backend-observable early-game mechanics, that `immediateSituation` stays bounded to structured opening effects rather than arbitrary rules, and that companion handling remains narrow. Keep scope away from travel/time, generic inventory redesign, and broader docs cleanup.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/routes/__tests__/character.test.ts src/engine/__tests__/turn-processor.test.ts src/character/__tests__/loadout-deriver.test.ts</automated>
  </verify>
  <done>The tests prove the remaining Phase 42 start-condition gap is implementation detail, not ambiguity about which fields matter or how broad the mechanic may become.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement backend-owned opening-state effects and preserve them through restore flows</name>
  <read_first>
@backend/src/routes/character.ts
@backend/src/worldgen/starting-location.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/turn-processor.ts
@backend/src/character/loadout-deriver.ts
@backend/src/routes/__tests__/character.test.ts
@backend/src/engine/__tests__/turn-processor.test.ts
@backend/src/character/__tests__/loadout-deriver.test.ts
  </read_first>
  <files>backend/src/engine/start-condition-runtime.ts, backend/src/routes/character.ts, backend/src/engine/turn-processor.ts, backend/src/engine/prompt-assembler.ts</files>
  <acceptance_criteria>
    - `rg -n "deriveStartConditionEffects|applyStartConditionEffects|arrivalMode|startingVisibility|entryPressure|companions|immediateSituation" backend/src/engine/start-condition-runtime.ts backend/src/routes/character.ts backend/src/engine/turn-processor.ts backend/src/engine/prompt-assembler.ts` shows one bounded start-condition runtime seam consumed by save/handoff and live gameplay.
    - `npm --prefix backend exec vitest run src/routes/__tests__/character.test.ts src/engine/__tests__/turn-processor.test.ts src/character/__tests__/loadout-deriver.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: canonical start conditions are translated into bounded opening-state mechanics such as scene flags, action gating/unlocking, and Oracle modifiers.
    - Test 2: `immediateSituation` contributes structured opening-state effects without becoming a free-form rules interpreter.
    - Test 3: companion semantics stay at presence/context level.
    - Test 4: opening-state mechanics survive reload, retry, and checkpoint restore by being authoritative runtime state or deterministic derivation from canonical saved data.
    - Test 5: no travel/time semantics or broader inventory-authority redesign leak into this phase.
  </behavior>
  <action>Create a bounded backend seam for start-condition runtime effects. Translate canonical saved start conditions into early-game mechanics using the selected combination surface from context: scene flags, action gating/unlocking, and Oracle modifiers. Wire that seam through save/handoff and live gameplay so the early opening state is mechanically real. Keep implementation honest about the unresolved dependency on Phase 38 by avoiding new inventory truth assumptions beyond existing canonical loadout behavior. Ensure the chosen representation survives reload/retry/checkpoint by living in authoritative runtime state or by deterministic re-derivation from canonical saved start conditions already preserved by Phase 41 restore. Do not widen scope into travel/time, party management, or prompt-only duct tape.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/routes/__tests__/character.test.ts src/engine/__tests__/turn-processor.test.ts src/character/__tests__/loadout-deriver.test.ts</automated>
  </verify>
  <done>Structured start conditions now have real early-game mechanical consequences that persist through the repaired runtime boundary, without leaking into later travel or inventory phases.</done>
</task>

</tasks>

<verification>
Run the character, turn-processor, and loadout suite above. Confirm start-condition fields affect gameplay mechanically, remain bounded in scope, and survive the persistence guarantees repaired in earlier phases.
</verification>

<success_criteria>
Structured start conditions are no longer flavor-only. The opening state of a character now matters mechanically in bounded, testable ways that survive save/load/retry/checkpoint, while staying out of travel-time and party-management scope.
</success_criteria>

<output>
After completion, create `.planning/phases/42-targeted-oracle-and-start-condition-runtime-effects/42-02-SUMMARY.md`
</output>
