---
phase: 41-checkpoint-complete-simulation-restore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/campaign/restore-bundle.ts
  - backend/src/campaign/checkpoints.ts
  - backend/src/engine/state-snapshot.ts
  - backend/src/campaign/__tests__/checkpoints.test.ts
  - backend/src/engine/__tests__/state-snapshot.test.ts
autonomous: true
requirements:
  - RINT-03
must_haves:
  truths:
    - "Checkpoint create/load round-trips the authoritative campaign bundle: `state.db`, `config.json`, `chat_history.json`, and checkpoint `vectors/`."
    - "Config-backed runtime metadata such as `currentTick`, `ipContext`, `premiseDivergence`, `worldbookSelection`, and `personaTemplates` comes back from checkpoint load through normal campaign reads."
    - "Checkpoint restore and Phase 39 turn-boundary restore share one invalidate-copy-reopen contract without widening Phase 39 into vector rollback."
  artifacts:
    - path: "backend/src/campaign/restore-bundle.ts"
      provides: "shared authoritative bundle capture/restore helper with optional vector-directory replacement"
      contains: "restoreCampaignBundle"
    - path: "backend/src/campaign/checkpoints.ts"
      provides: "checkpoint capture/load that includes `config.json` and routes restore through the shared bundle helper"
      contains: "loadCheckpoint"
    - path: "backend/src/engine/state-snapshot.ts"
      provides: "Phase 39 rollback bundle that reuses the same restore semantics while keeping vectors excluded"
      contains: "restoreSnapshot"
  key_links:
    - from: "backend/src/campaign/checkpoints.ts"
      to: "backend/src/campaign/restore-bundle.ts"
      via: "checkpoint create/load uses one shared authoritative bundle contract per D-01, D-02, and D-04"
      pattern: "config.json|vectors|restoreCampaignBundle|captureCampaignBundle"
    - from: "backend/src/engine/state-snapshot.ts"
      to: "backend/src/campaign/restore-bundle.ts"
      via: "turn-boundary restore reuses the same invalidate-copy-reopen primitive while excluding vectors"
      pattern: "restoreCampaignBundle|includeVectors"
    - from: "backend/src/campaign/restore-bundle.ts"
      to: "backend/src/campaign/manager.ts"
      via: "bundle replacement always reloads the campaign through `loadCampaign()` after handle invalidation"
      pattern: "closeDb|closeVectorDb|loadCampaign"
---

<objective>
Converge checkpoint and turn-boundary restore onto one authoritative bundle contract.

Purpose: satisfy the config-inclusive half of `RINT-03` by making checkpoint capture/load restore the same runtime-authoritative artifacts Phase 41 promises, while keeping Phase 39's vector-exclusion intact.
Output: shared bundle-capture and bundle-restore primitives, checkpoint round-trip coverage for `config.json`, and turn-boundary restore reimplemented on the same lower-level semantics.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-CONTEXT.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-RESEARCH.md
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-HANDOFF.md
@.planning/phases/39-honest-turn-boundary-retry-undo/39-01-SUMMARY.md
@docs/memory.md
@backend/src/campaign/checkpoints.ts
@backend/src/engine/state-snapshot.ts
@backend/src/campaign/manager.ts
@backend/src/campaign/__tests__/checkpoints.test.ts
@backend/src/engine/__tests__/state-snapshot.test.ts

<interfaces>
From `backend/src/campaign/checkpoints.ts`:
```ts
export async function createCheckpoint(
  campaignId: string,
  opts?: { name?: string; description?: string; auto?: boolean }
): Promise<CheckpointMeta>

export async function loadCheckpoint(
  campaignId: string,
  checkpointId: string
): Promise<CheckpointMeta>
```

From `backend/src/engine/state-snapshot.ts`:
```ts
export interface TurnSnapshot {
  campaignId: string;
  bundleDir: string;
  capturedAt: number;
}

export async function captureSnapshot(campaignId: string): Promise<TurnSnapshot>
export async function restoreSnapshot(campaignId: string, snapshot: TurnSnapshot): Promise<void>
```

From `backend/src/campaign/manager.ts`:
```ts
export function readCampaignConfig(campaignId: string): CampaignConfigFile
export async function loadCampaign(id: string): Promise<CampaignMeta>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the config-inclusive checkpoint bundle contract in backend tests</name>
  <read_first>
@.planning/phases/41-checkpoint-complete-simulation-restore/41-CONTEXT.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-RESEARCH.md
@backend/src/campaign/checkpoints.ts
@backend/src/engine/state-snapshot.ts
@backend/src/campaign/__tests__/checkpoints.test.ts
@backend/src/engine/__tests__/state-snapshot.test.ts
  </read_first>
  <files>backend/src/campaign/__tests__/checkpoints.test.ts, backend/src/engine/__tests__/state-snapshot.test.ts</files>
  <acceptance_criteria>
    - `npm --prefix backend exec vitest run src/campaign/__tests__/checkpoints.test.ts src/engine/__tests__/state-snapshot.test.ts` fails only because the shared Phase 41 bundle contract is not implemented yet.
    - `rg -n "config.json|currentTick|worldbookSelection|personaTemplates|restoreCampaignBundle|includeVectors" backend/src/campaign/__tests__/checkpoints.test.ts backend/src/engine/__tests__/state-snapshot.test.ts` finds explicit regressions for D-01, D-02, D-04, and D-05.
  </acceptance_criteria>
  <behavior>
    - Test 1: `createCheckpoint()` captures `state.db`, `config.json`, `chat_history.json`, and checkpoint `vectors/` as one coherent bundle per D-01 and D-02.
    - Test 2: `loadCheckpoint()` restores `config.json`-backed runtime metadata through `readCampaignConfig()` and reopens through the same reload path Phase 39 restore uses per D-04 and D-05.
    - Test 3: `restoreSnapshot()` still excludes vectors while reusing the same lower-level restore semantics as checkpoint load per D-04.
    - Test 4: best-effort artifacts outside the promised campaign boundary remain excluded per D-09.
  </behavior>
  <action>Write failing tests first in the checkpoint and state-snapshot suites. Add assertions that checkpoint capture includes `config.json` beside `state.db`, `chat_history.json`, and `vectors/` per D-01 and D-02; prove that `currentTick`, `ipContext`, `premiseDivergence`, `worldbookSelection`, and `personaTemplates` round-trip through checkpoint load; and pin that checkpoint load and Phase 39 restore both go through one invalidate-copy-reopen contract per D-04 and D-05. The tests must also assert that Phase 39 still excludes vector rollback and that `meta.json` or other auxiliary artifacts are not promoted into the authoritative runtime boundary per D-08 and D-09.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/campaign/__tests__/checkpoints.test.ts src/engine/__tests__/state-snapshot.test.ts</automated>
  </verify>
  <done>The backend tests pin the exact Phase 41 artifact contract and prove the only missing work is the shared config-inclusive restore helper plus wiring in checkpoint and turn-boundary code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared authoritative bundle helper for checkpoints and turn-boundary restore</name>
  <read_first>
@backend/src/campaign/checkpoints.ts
@backend/src/engine/state-snapshot.ts
@backend/src/campaign/manager.ts
@backend/src/campaign/__tests__/checkpoints.test.ts
@backend/src/engine/__tests__/state-snapshot.test.ts
  </read_first>
  <files>backend/src/campaign/restore-bundle.ts, backend/src/campaign/checkpoints.ts, backend/src/engine/state-snapshot.ts</files>
  <acceptance_criteria>
    - `rg -n "restoreCampaignBundle|captureCampaignBundle|config.json|includeVectors|loadCampaign" backend/src/campaign/restore-bundle.ts backend/src/campaign/checkpoints.ts backend/src/engine/state-snapshot.ts` shows one shared bundle helper and config-aware checkpoint handling.
    - `npm --prefix backend exec vitest run src/campaign/__tests__/checkpoints.test.ts src/engine/__tests__/state-snapshot.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: checkpoint create/load now round-trips the full authoritative bundle promised by Phase 41.
    - Test 2: turn-boundary restore reuses the same helper but keeps `vectors/` excluded.
    - Test 3: bundle replacement closes stale DB/vector handles and reopens through `loadCampaign()` instead of checkpoint-local reconnect code.
    - Test 4: out-of-scope best-effort artifacts remain excluded from the restore helper.
  </behavior>
  <action>Create `backend/src/campaign/restore-bundle.ts` with shared capture and restore primitives for authoritative campaign artifacts. Use SQLite backup semantics for `state.db`, copy `config.json` and `chat_history.json`, and gate directory replacement for `vectors/` behind an explicit option so checkpoint restore includes vectors per D-01 while Phase 39 snapshot restore remains narrower per D-04. Update `backend/src/campaign/checkpoints.ts` to capture `config.json`, treat it as authoritative runtime state per D-02, and route load through the shared helper plus `loadCampaign()` instead of ad-hoc `connectDb()`/`openVectorDb()` reconnect logic per D-05. Update `backend/src/engine/state-snapshot.ts` to reuse the same restore primitive with `includeVectors: false`, preserving Phase 39 semantics and avoiding scope leakage into inventory authority, target-aware mechanics, travel, or docs per D-08. Do not add any restore behavior for images or other best-effort artifacts beyond the promised bundle per D-09.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/campaign/__tests__/checkpoints.test.ts src/engine/__tests__/state-snapshot.test.ts</automated>
  </verify>
  <done>Checkpoint load and turn-boundary restore now share one authoritative bundle helper, checkpoints include `config.json`, and the helper always reopens campaign runtime through the existing manager reload path.</done>
</task>

</tasks>

<verification>
Run the checkpoint and state-snapshot Vitest suite above. Confirm the authoritative checkpoint bundle is exactly `state.db`, `config.json`, `chat_history.json`, and checkpoint `vectors/`, that config-backed runtime metadata round-trips through `readCampaignConfig()`, and that Phase 39 restore still excludes vectors while reusing the same lower-level restore helper.
</verification>

<success_criteria>
Phase 41 has one coherent artifact contract instead of two drifting restore models. Checkpoints now preserve config-backed runtime metadata, and the repo has a shared bundle primitive that later plans can extend with discarded-timeline invalidation without reopening artifact scope.
</success_criteria>

<output>
After completion, create `.planning/phases/41-checkpoint-complete-simulation-restore/41-01-SUMMARY.md`
</output>
