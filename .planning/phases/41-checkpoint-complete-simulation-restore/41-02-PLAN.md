---
phase: 41-checkpoint-complete-simulation-restore
plan: 02
type: execute
wave: 2
depends_on:
  - 41-01
files_modified:
  - backend/src/campaign/runtime-state.ts
  - backend/src/campaign/checkpoints.ts
  - backend/src/routes/chat.ts
  - backend/src/vectors/episodic-events.ts
  - backend/src/campaign/__tests__/checkpoints.test.ts
  - backend/src/routes/__tests__/chat.test.ts
  - backend/src/vectors/__tests__/episodic-events.test.ts
autonomous: true
requirements:
  - RINT-03
  - SIMF-03
must_haves:
  truths:
    - "Checkpoint load clears discarded-timeline runtime state, so later turns use only the restored branch."
    - "After checkpoint restore, history no longer reports a stale live-turn snapshot and queued same-turn reflection evidence from the abandoned branch is gone."
    - "Checkpoint load, retry, and undo end on the same restored campaign boundary for NPC autonomy, reflection, faction ticks, and other rollback-critical simulation inputs."
  artifacts:
    - path: "backend/src/campaign/runtime-state.ts"
      provides: "campaign-scoped runtime-state ownership for last-turn snapshots and active-turn guards with restore-time invalidation helpers"
      contains: "clearCampaignRuntimeState"
    - path: "backend/src/vectors/episodic-events.ts"
      provides: "new campaign-scoped, tick-agnostic committed-event queue clear helper used only during restore invalidation while normal tick drains stay unchanged"
      contains: "clearPendingCommittedEvents"
    - path: "backend/src/campaign/checkpoints.ts"
      provides: "checkpoint load that clears runtime state before reopening the restored campaign"
      contains: "loadCheckpoint"
    - path: "backend/src/routes/chat.ts"
      provides: "gameplay routes that consume shared runtime-state helpers instead of route-local maps"
      contains: "hasLiveTurnSnapshot"
  key_links:
    - from: "backend/src/campaign/checkpoints.ts"
      to: "backend/src/campaign/runtime-state.ts"
      via: "restore-time invalidation of live turn snapshots and active-turn guards per D-03, D-06, and D-07"
      pattern: "clearCampaignRuntimeState"
    - from: "backend/src/campaign/checkpoints.ts"
      to: "backend/src/vectors/episodic-events.ts"
      via: "restore-time clearing of same-turn committed-event evidence queues per D-07"
      pattern: "clearPendingCommittedEvents"
    - from: "backend/src/routes/chat.ts"
      to: "backend/src/campaign/runtime-state.ts"
      via: "action/retry/undo and history all read one campaign-scoped runtime-state seam instead of stale route-local structures"
      pattern: "setLastTurnSnapshot|getLastTurnSnapshot|hasLiveTurnSnapshot|tryBeginTurn"
---

<objective>
Invalidate discarded-timeline runtime state so checkpoint restore is simulation-coherent.

Purpose: finish `RINT-03` and satisfy `SIMF-03` by making checkpoint load clear stale in-memory state and align with the same campaign boundary contract used by retry and undo.
Output: campaign-scoped runtime-state helpers, restore-time invalidation of live-turn and same-turn evidence queues, and regressions proving post-restore simulation only reads restored-branch state.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-CONTEXT.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-RESEARCH.md
@.planning/phases/39-honest-turn-boundary-retry-undo/39-01-SUMMARY.md
@.planning/phases/40-live-reflection-progression-triggers/40-01-SUMMARY.md
@.planning/phases/40-live-reflection-progression-triggers/40-03-SUMMARY.md
@backend/src/campaign/checkpoints.ts
@backend/src/routes/chat.ts
@backend/src/vectors/episodic-events.ts
@backend/src/campaign/__tests__/checkpoints.test.ts
@backend/src/routes/__tests__/chat.test.ts
@backend/src/vectors/__tests__/episodic-events.test.ts

<interfaces>
From `backend/src/routes/chat.ts`:
```ts
const lastTurnSnapshots = new Map<string, TurnSnapshot>();
const campaignsWithActiveTurn = new Set<string>();
```

From `backend/src/vectors/episodic-events.ts`:
```ts
export function readPendingCommittedEvents(
  campaignId: string,
  tick: number,
): PendingCommittedEvent[]

export function drainPendingCommittedEvents(
  campaignId: string,
  tick: number,
): PendingCommittedEvent[]
```

From `backend/src/campaign/checkpoints.ts`:
```ts
export async function loadCheckpoint(
  campaignId: string,
  checkpointId: string
): Promise<CheckpointMeta>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock discarded-timeline invalidation regressions for checkpoint restore</name>
  <read_first>
@.planning/phases/41-checkpoint-complete-simulation-restore/41-CONTEXT.md
@backend/src/campaign/checkpoints.ts
@backend/src/routes/chat.ts
@backend/src/vectors/episodic-events.ts
@backend/src/campaign/__tests__/checkpoints.test.ts
@backend/src/routes/__tests__/chat.test.ts
@backend/src/vectors/__tests__/episodic-events.test.ts
  </read_first>
  <files>backend/src/campaign/__tests__/checkpoints.test.ts, backend/src/routes/__tests__/chat.test.ts, backend/src/vectors/__tests__/episodic-events.test.ts</files>
  <acceptance_criteria>
    - `npm --prefix backend exec vitest run src/campaign/__tests__/checkpoints.test.ts src/routes/__tests__/chat.test.ts src/vectors/__tests__/episodic-events.test.ts` fails only because restore-time runtime invalidation is not implemented yet.
    - `rg -n "clearCampaignRuntimeState|clearPendingCommittedEvents|hasLiveTurnSnapshot|discarded timeline|checkpoint restore" backend/src/campaign/__tests__/checkpoints.test.ts backend/src/routes/__tests__/chat.test.ts backend/src/vectors/__tests__/episodic-events.test.ts` finds direct regressions for D-03, D-06, and D-07.
  </acceptance_criteria>
  <behavior>
    - Test 1: loading a checkpoint clears any existing live-turn snapshot and active-turn guard for that campaign before the restored campaign is reopened.
    - Test 2: loading a checkpoint clears queued same-turn committed-event evidence for that campaign so reflection and auxiliary embedding cannot read the abandoned branch.
    - Test 3: after checkpoint restore, `/chat/history` reports no live turn snapshot for the restored campaign, and retry/undo cannot act on discarded-branch runtime state.
    - Test 4: campaign-scoped invalidation leaves other campaigns' runtime state untouched.
  </behavior>
  <action>Write failing regressions first around the exact runtime leaks identified in D-03, D-06, and D-07. In `backend/src/routes/__tests__/chat.test.ts`, pin that stale `hasLiveTurnSnapshot` state disappears after checkpoint load and that retry/undo no longer see the abandoned branch. In `backend/src/vectors/__tests__/episodic-events.test.ts`, pin a campaign-scoped queue clear helper so same-turn committed evidence from a discarded timeline cannot reach reflection or auxiliary embedding after restore. In `backend/src/campaign/__tests__/checkpoints.test.ts`, assert that checkpoint load triggers the new campaign-scoped invalidation seam before reopening the restored campaign. Keep scope confined to restore fidelity per D-08: no inventory redesign, no target-aware mechanics, no travel behavior, and no docs cleanup.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/campaign/__tests__/checkpoints.test.ts src/routes/__tests__/chat.test.ts src/vectors/__tests__/episodic-events.test.ts</automated>
  </verify>
  <done>The tests prove the only remaining Phase 41 gap is missing campaign-scoped runtime invalidation before checkpoint reopen, with no ambiguity about live-turn snapshots, active-turn guards, or same-turn evidence queues.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement campaign-scoped runtime invalidation and align checkpoint load with retry/undo boundaries</name>
  <read_first>
@backend/src/campaign/checkpoints.ts
@backend/src/routes/chat.ts
@backend/src/vectors/episodic-events.ts
@backend/src/campaign/__tests__/checkpoints.test.ts
@backend/src/routes/__tests__/chat.test.ts
@backend/src/vectors/__tests__/episodic-events.test.ts
  </read_first>
  <files>backend/src/campaign/runtime-state.ts, backend/src/campaign/checkpoints.ts, backend/src/routes/chat.ts, backend/src/vectors/episodic-events.ts</files>
  <acceptance_criteria>
    - `rg -n "clearCampaignRuntimeState|setLastTurnSnapshot|getLastTurnSnapshot|hasLiveTurnSnapshot|clearPendingCommittedEvents" backend/src/campaign/runtime-state.ts backend/src/campaign/checkpoints.ts backend/src/routes/chat.ts backend/src/vectors/episodic-events.ts` shows one campaign-scoped runtime invalidation seam reused by checkpoint load and gameplay routes.
    - `npm --prefix backend exec vitest run src/campaign/__tests__/checkpoints.test.ts src/routes/__tests__/chat.test.ts src/vectors/__tests__/episodic-events.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: checkpoint load clears route-owned live-turn state and evidence queues before reload.
    - Test 2: action/retry/undo/history all consume shared runtime-state helpers rather than route-local maps.
    - Test 3: same-turn evidence clearing is campaign-scoped and does not leak across campaigns.
    - Test 4: checkpoint load, retry, and undo all operate against the same restored campaign boundary for rollback-critical simulation inputs.
    - Test 5: Phase 41 only clears known campaign-scoped runtime queues and guards at restore time; long-lived auxiliary async completions are noted as a bounded risk, not silently expanded into a restore-epoch redesign inside this phase.
  </behavior>
  <action>Extract the route-local `lastTurnSnapshots` and `campaignsWithActiveTurn` state from `backend/src/routes/chat.ts` into a new `backend/src/campaign/runtime-state.ts` module so restore code can clear campaign-scoped runtime state without importing route internals directly, per D-03 and D-07. Update `chat.ts` to use that module for begin/end-turn guards, snapshot storage, and the `hasLiveTurnSnapshot` history response. Add a new `clearPendingCommittedEvents(campaignId: string)` helper to `backend/src/vectors/episodic-events.ts` that removes all queued same-turn evidence for one campaign regardless of tick; this restore helper must remain distinct from the existing tick-filtered `readPendingCommittedEvents()` / `drainPendingCommittedEvents()` flow used by normal turn processing. Then update `backend/src/campaign/checkpoints.ts` to call both invalidation seams before the shared bundle restore reopens the campaign, ensuring later turns, retry, undo, NPC autonomy, reflection, and faction simulation all see the restored branch only per D-06. Bound the phase explicitly: it must clear the known campaign-scoped queues and guards that make restore hybrid today, and may document the low/medium risk of long-lived auxiliary async completions from the discarded branch, but it must not broaden into restore-epoch redesign unless tests prove that narrower invalidation is insufficient. Keep the work strictly on restore fidelity and simulation coherence per D-08, and do not widen artifact scope beyond the authoritative bundle already defined in Plan 41-01 per D-09.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/campaign/__tests__/checkpoints.test.ts src/routes/__tests__/chat.test.ts src/vectors/__tests__/episodic-events.test.ts</automated>
  </verify>
  <done>Checkpoint restore now clears discarded-timeline runtime state before reopen, gameplay routes consume one campaign-scoped runtime-state seam, and later simulation work can only proceed from restored-branch state. The plan also leaves an explicit, non-blocking note that any remaining long-lived async bleed would be a separate follow-up seam, not hidden scope creep inside Phase 41.</done>
</task>

</tasks>

<verification>
Run the checkpoint, chat-route, and episodic-events Vitest suite above. Confirm checkpoint load clears live-turn snapshots, active-turn guards, and pending same-turn evidence for the restored campaign, and confirm retry/undo/history all observe the same restored campaign boundary afterward.
</verification>

<success_criteria>
Checkpoint restore is no longer a hybrid state. Loading a checkpoint discards stale singleton/runtime pointers, stale retry/undo state, and stale same-turn evidence so NPC autonomy, reflection, faction updates, and later turns all continue from the restored timeline only.
</success_criteria>

<output>
After completion, create `.planning/phases/41-checkpoint-complete-simulation-restore/41-02-SUMMARY.md`
</output>
