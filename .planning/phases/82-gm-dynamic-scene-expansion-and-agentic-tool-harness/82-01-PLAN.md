# 82-01 Plan - Baseline Regressions And Tool Affordance Contract

## Goal

Lock the current failure shape before behavior changes: dynamic scene tools exist but are under-described, partially lifecycle-free, and risky around support NPC placement.

## Requirements Covered

Supports P82-R1 through P82-R8.

## Tasks

1. Add focused RED tests for `reveal_location` ephemeral lifecycle gaps, support NPC broad/current-scene placement, cleanup absence, and dynamic tool observation shape.
2. Add prompt/contract tests proving the GM sees dynamic scene/support NPC affordances as optional local tools, not mandatory every-turn behavior.
3. Add negative tests for remote/offscreen dynamic creation from a player turn.
4. Run GitNexus `detect_changes` and record the clean planning baseline before implementation edits.
5. Record GitNexus impact for the symbols that later plans will edit.
6. Incorporate `82-REVIEWS.md` findings before executing Plans 82-02 through 82-05.

## Verification

- Focused tests fail for the intended gaps before implementation.
- Review amendments are reflected in requirements/spec/validation before runtime changes begin.
- `npm --prefix backend run typecheck` remains green before implementation edits.
- `git diff --check` is clean.

## Completion Notes

Do not make runtime fixes in this plan beyond minimal test scaffolding and contract text needed to express the RED cases.
