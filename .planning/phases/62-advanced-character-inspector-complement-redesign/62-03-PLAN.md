---
phase: 62-advanced-character-inspector-complement-redesign
plan: 03
type: execute
wave: 3
depends_on:
  - "62-01"
  - "62-02"
files_modified:
  - .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md
autonomous: true
requirements:
  - P62-R1
  - P62-R2
  - P62-R3
  - P62-R4
  - P62-R5
must_haves:
  truths:
    - "npm --prefix frontend run typecheck exits 0"
    - "npm --prefix frontend run lint exits 0"
    - "npm --prefix frontend test character-record-inspector -- --run exits 0"
    - "npm --prefix frontend test -- --run exits 0 (full frontend Vitest suite passes)"
    - "git diff --name-only (unstaged + staged + status) confirms only 2 source files touched: character-record-inspector.tsx + test file; npcs-section.tsx untouched"
    - "Grep audit confirms socialContext.originMode is absent from rewritten inspector and provenance.importMode is present"
    - "Grep audit confirms socialStatus, relationshipRefs, beliefs, drives, frictions are all referenced in rewritten inspector"
    - "Grep audit confirms hasAnyComplementSection helper exists and hasItems trims entries"
    - "PinchTab smoke (SUPPLEMENTAL, non-blocking): attached evidence of Advanced panel rendering; GO verdict determined by static+unit evidence only"
    - "62-VALIDATION.md records exit codes, grep audits, PinchTab supplemental evidence, final GO verdict based on static+unit signals"
  artifacts:
    - path: ".planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md"
      provides: "Evidence bundle for phase 62 closeout — blocking sections (1-8) + supplemental PinchTab section (9)"
      min_lines: 40
  key_links:
    - from: "62-VALIDATION.md"
      to: "frontend/components/world-review/character-record-inspector.tsx"
      via: "grep + test command outputs captured"
      pattern: "typecheck|lint|test|PinchTab|importMode|originMode|hasAnyComplementSection"
---

<objective>
Run the full verification matrix for Phase 62 and capture evidence in `62-VALIDATION.md`. This plan does NOT modify production source code; it runs typecheck, lint, targeted Vitest, full frontend Vitest, worktree-aware diff-scope checks, grep audits (including importMode-present / originMode-absent), and a PinchTab browser smoke as SUPPLEMENTAL evidence (non-blocking). Every failing step in the blocking set (1-8) is surfaced as a gap — no patching around failures. PinchTab (section 9) is attached as evidence but does NOT gate GO/NOGO.

Purpose: Prove the Advanced inspector redesign is correct end-to-end (types clean, lint clean, unit tests green, no originMode references, importMode+new fields present) before Phase 62 is marked complete. Determinism is maintained by making blocking verdict depend only on static + unit signals.
Output: `62-VALIDATION.md` populated with command outputs, grep audits, PinchTab supplemental evidence (best-effort), and a final GO/NO-GO verdict computed from sections 1-8 only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-advanced-character-inspector-complement-redesign/62-CONTEXT.md
@.planning/phases/62-advanced-character-inspector-complement-redesign/62-01-SUMMARY.md
@.planning/phases/62-advanced-character-inspector-complement-redesign/62-02-SUMMARY.md
@frontend/components/world-review/character-record-inspector.tsx
@frontend/components/world-review/__tests__/character-record-inspector.test.tsx
@frontend/components/world-review/npcs-section.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run static + unit verification (BLOCKING) and capture in 62-VALIDATION.md sections 1-8</name>
  <files>.planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md</files>
  <read_first>
    - .planning/phases/62-advanced-character-inspector-complement-redesign/62-01-SUMMARY.md
    - .planning/phases/62-advanced-character-inspector-complement-redesign/62-02-SUMMARY.md
    - frontend/components/world-review/character-record-inspector.tsx (post-rewrite)
    - frontend/components/world-review/__tests__/character-record-inspector.test.tsx (post-rewrite)
  </read_first>
  <action>
    Create `62-VALIDATION.md` and run these commands, capturing the full tail of each output under the matching section header. Record the exit code on the next line via `echo "exit: $?"`. Do NOT truncate exit codes. Shell is Git Bash on Windows — bash syntax works.

    Commands to run (in order):

    1. **Typecheck** (BLOCKING):
       - `npm --prefix frontend run typecheck 2>&1 | tail -30`
       - Record exit code. EXPECTED: exit 0.

    2. **Lint** (BLOCKING):
       - `npm --prefix frontend run lint 2>&1 | tail -30`
       - Record exit code. EXPECTED: exit 0. (Non-blocking warnings allowed; errors are NOT.)

    3. **Targeted Vitest** (BLOCKING):
       - `npm --prefix frontend test character-record-inspector -- --run 2>&1 | tail -40`
       - Record exit code. EXPECTED: exit 0. Record the `Test Files  N passed` and `Tests  N passed` lines verbatim.

    4. **Full frontend Vitest** (BLOCKING):
       - `npm --prefix frontend test -- --run 2>&1 | tail -60`
       - Record exit code. EXPECTED: exit 0. If any unrelated test was flaky before Phase 62, note it in a `Pre-existing` subsection (pre-existing flakes do not block GO as long as `character-record-inspector` tests pass).

    5. **Diff scope check (worktree-aware, LOCKED — review P2 fix)** (BLOCKING):
       - Run ALL THREE:
         - `git diff --name-only -- frontend/components/world-review/ 2>&1` (unstaged).
         - `git diff --name-only --cached -- frontend/components/world-review/ 2>&1` (staged).
         - `git status --short -- frontend/components/world-review/ 2>&1` (combined worktree view).
       - EXPECTED: Across all three outputs, ONLY `frontend/components/world-review/character-record-inspector.tsx` and `frontend/components/world-review/__tests__/character-record-inspector.test.tsx` may appear. `npcs-section.tsx` MUST NOT appear in any of the three. (This replaces the previous `main...HEAD` check which validated committed branch history, not worktree state.)

    6. **Duplicate-absence grep audit** (BLOCKING):
       ```
       grep -c "PowerStatsSection\|label=\"Display name\"\|label=\"Current location\"\|label=\"Faction\"\|label=\"Persona\"\|label=\"Active goals\"\|label=\"Long-term goals\"\|Runtime & Provenance" frontend/components/world-review/character-record-inspector.tsx
       ```
       EXPECTED: output `0`.

       Also run the originMode absence check (LOCKED — review P2):
       ```
       grep -n "socialContext\.originMode\|originMode" frontend/components/world-review/character-record-inspector.tsx
       ```
       EXPECTED: exit 1 (zero matches in the rewritten inspector). Record the full grep output (empty).

    7. **Added-section + new-field grep audit** (BLOCKING):
       ```
       for term in "title=\"Profile\"" "title=\"Loadout\"" "title=\"Starting Conditions\"" "title=\"Provenance\"" "title=\"Runtime & State\"" "No additional data" "identity.baseFacts" "loadout.currencyNotes" "startConditions.arrivalMode" "provenance.archetypePrompt" "provenance.importMode" "hasAnyComplementSection" "socialStatus" "relationshipRefs" "motivations?.beliefs\|motivations.beliefs" "motivations?.drives\|motivations.drives" "motivations?.frictions\|motivations.frictions" "label=\"Beliefs\"" "label=\"Drives\"" "label=\"Frictions\"" "label=\"Social status\"" "label=\"Relationships\""; do
         printf '%s => ' "$term"; grep -c "$term" frontend/components/world-review/character-record-inspector.tsx
       done
       ```
       EXPECTED: every term has count `>= 1`.

       Also confirm `hasItems` trims entries:
       ```
       grep -nE "\.trim\(\)\.length|item\.trim\(\)" frontend/components/world-review/character-record-inspector.tsx
       ```
       EXPECTED: exit 0 (at least one match — trim-before-length check is present).

    8. **No-IP fixture audit** (BLOCKING):
       ```
       grep -ci "naruto\|sasuke\|uchiha\|sharingan\|konoha\|hokage\|akatsuki\|madara\|itachi\|kakashi\|gojo\|geto\|jujutsu\|saiyan\|luffy\|airbender\|geralt\|jedi\|sith\|hogwarts" frontend/components/world-review/__tests__/character-record-inspector.test.tsx
       ```
       EXPECTED: output `0`.

    File structure of `62-VALIDATION.md`:

    ```markdown
    # Phase 62 - Validation Report

    **Date:** {date}
    **Scope:** CharacterRecordInspector rewrite + test rewrite
    **Modified files:** `character-record-inspector.tsx`, `character-record-inspector.test.tsx`
    **Verdict policy:** Blocking sections are 1-8 (static + unit). Section 9 (PinchTab) is SUPPLEMENTAL and does NOT affect GO verdict.

    ## 1. Typecheck
    {command}
    {output}
    exit: {code}

    ## 2. Lint
    ...

    ## 3. Targeted Vitest
    ...

    ## 4. Full Frontend Vitest
    ...

    ## 5. Diff Scope Check (worktree-aware)
    ### 5a. Unstaged (git diff --name-only)
    ...
    ### 5b. Staged (git diff --name-only --cached)
    ...
    ### 5c. Status (git status --short)
    ...

    ## 6. Duplicate-Absence Grep Audit
    ### 6a. Removed labels / PowerStats / old heading
    ...
    ### 6b. originMode absence
    ...

    ## 7. Added-Section + New-Field Grep Audit
    ### 7a. Sections + new fields present
    ...
    ### 7b. hasItems trim semantics
    ...

    ## 8. No-IP Fixture Audit
    ...

    ## Verdict (computed from sections 1-8 ONLY)
    {GO | NO-GO} - {one-line reasoning}
    ```

    DO NOT mark Verdict GO if ANY of steps 1-8 produced a non-expected output. Record the failure and stop — Task 2 (PinchTab supplemental) runs regardless of Verdict, but its results do not change Verdict.
  </action>
  <verify>
    <automated>test -f .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md && grep -q "Verdict" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md && echo "VALIDATION FILE OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "^## 1\. Typecheck" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "^## 2\. Lint" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "^## 3\. Targeted Vitest" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "^## 4\. Full Frontend Vitest" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "^## 5\. Diff Scope Check" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "### 5a\. Unstaged" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "### 5b\. Staged" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "### 5c\. Status" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "^## 6\. Duplicate-Absence Grep Audit" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "### 6b\. originMode absence" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "^## 7\. Added-Section" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "^## 8\. No-IP Fixture Audit" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "^## Verdict" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -c "exit: 0" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` returns a count `>= 4` (typecheck + lint + targeted + full Vitest all at exit 0).
    - `npm --prefix frontend run typecheck` rerun here exits 0.
    - `npm --prefix frontend test character-record-inspector -- --run` rerun here exits 0.
    - Worktree check: `git diff --name-only -- frontend/components/world-review/npcs-section.tsx` returns empty output AND `git diff --name-only --cached -- frontend/components/world-review/npcs-section.tsx` returns empty output AND `git status --short -- frontend/components/world-review/npcs-section.tsx` returns empty output (basic card untouched in any worktree slot).
    - `grep -c "socialContext\.originMode\|originMode" frontend/components/world-review/character-record-inspector.tsx` → output `0`.
    - `grep -c "provenance\.importMode" frontend/components/world-review/character-record-inspector.tsx` → `>= 1`.
  </acceptance_criteria>
  <done>
    `62-VALIDATION.md` exists with sections 1-8 populated and a Verdict line computed from sections 1-8. All exit codes captured. All rerun acceptance commands exit 0. originMode is absent, importMode is present.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PinchTab browser smoke — append Section 9 SUPPLEMENTAL evidence to 62-VALIDATION.md (non-blocking)</name>
  <files>.planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md</files>
  <read_first>
    - .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md (populated by Task 1; Verdict already computed from sections 1-8)
    - frontend/components/world-review/character-record-inspector.tsx (post-rewrite — reference for expected headings)
    - frontend/components/world-review/npcs-section.tsx (basic card — must NOT be modified)
  </read_first>
  <action>
    Drive PinchTab programmatically to collect supplemental (non-blocking) evidence that the Advanced panel renders non-duplicatively in the live app. Append Section 9 to `62-VALIDATION.md`. Claude drives this autonomously per the memory workarounds (`var`-only variables, programmatic clicks, no `!` in evaluate). **No human interaction required. Section 9 outcome does NOT change the Verdict set in Task 1 — Verdict is determined by static + unit evidence only (sections 1-8). If PinchTab fails to connect, seed campaign is unavailable, or assertions fail, mark Section 9 as `SUPPLEMENTAL: SKIPPED` or `SUPPLEMENTAL: PARTIAL` and proceed.**

    Steps:

    1. Start PinchTab if not already running: `BRIDGE_HEADLESS=true pinchtab &`. If port 9867 is already bound, reuse the existing instance. If PinchTab cannot be started, append `## 9. PinchTab Smoke Evidence` with body `SUPPLEMENTAL: SKIPPED — PinchTab unavailable` and stop.
    2. Start dev servers as background tasks using the Bash tool with `run_in_background: true`:
       - Backend: command `npm --prefix backend run dev` with `run_in_background=true`.
       - Frontend: command `npm --prefix frontend run dev` with `run_in_background=true`.
       - Readiness probe: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/api/health` expecting `200`, and `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/` expecting `200`. Use a polling loop bounded to 60 seconds; on timeout, mark Section 9 as `SUPPLEMENTAL: SKIPPED — dev servers did not become ready within 60s` and stop.
    3. Navigate PinchTab to `http://localhost:3000/` and select (or create) an ORIGINAL-WORLD campaign that has at least one Key NPC with populated draft fields (biography, profile, starting conditions, provenance, socialStatus, relationshipRefs, beliefs/drives/frictions). If creating, use original-world seeds only — NO IP franchise names. Example safe seed terms: `Dunespire Hold`, `Storm Marches`, `Commander Kael`, `Wind Cutting`. If no suitable campaign exists AND creating one is not feasible in this session, mark Section 9 as `SUPPLEMENTAL: SKIPPED — no seeded campaign available` and stop.
    4. Navigate to `/campaign/{id}/review`, open the NPC tab via the tab control.
    5. Use PinchTab `/evaluate` with the following JS (uses `var` only per workarounds; programmatic click; no ref clicks):
       ```js
       var summaries = Array.from(document.querySelectorAll("details > summary"));
       var advanced = summaries.find(function(s){ return /advanced/i.test(s.textContent || ""); });
       if (advanced) { advanced.click(); }
       ```
    6. Capture the accessibility tree (via PinchTab `/snapshot`) of the expanded Advanced panel region. Extract the list of `<h4>` heading texts and the list of label strings (MetaGrid labels + TextBlock labels + ListBlock labels) inside it.
    7. Append a `## 9. PinchTab Smoke Evidence (SUPPLEMENTAL — non-blocking)` section to `62-VALIDATION.md` containing:
       - Banner: `SUPPLEMENTAL: {OK | PARTIAL | SKIPPED}` on the first line.
       - Campaign ID used.
       - NPC name used (must be an original-world name).
       - A code-fenced dump of the Advanced panel's headings list (expected content: some subset of `Overview`, `Identity Core`, `Profile`, `Live Dynamics`, `Capabilities`, `Runtime & State`, `Loadout`, `Starting Conditions`, `Provenance`, plus a `Raw JSON` `<summary>`).
       - The assertion grid below, each line marked `PASS`, `FAIL`, or `N/A` (N/A only if Section 9 is SKIPPED/PARTIAL):
         - `ADVANCED_HAS_OVERVIEW: PASS|FAIL|N/A`
         - `ADVANCED_HAS_RUNTIME_STATE: PASS|FAIL|N/A`
         - `ADVANCED_NO_POWERSTATS_HEADING: PASS|FAIL|N/A`
         - `ADVANCED_NO_DISPLAY_NAME_LABEL: PASS|FAIL|N/A`
         - `ADVANCED_NO_CURRENT_LOCATION_LABEL: PASS|FAIL|N/A`
         - `ADVANCED_NO_FACTION_LABEL: PASS|FAIL|N/A`
         - `ADVANCED_NO_PERSONA_LABEL: PASS|FAIL|N/A`
         - `ADVANCED_NO_ACTIVE_GOALS_LABEL: PASS|FAIL|N/A`
         - `ADVANCED_NO_LONG_TERM_GOALS_LABEL: PASS|FAIL|N/A`
         - `ADVANCED_RAW_JSON_PRESENT: PASS|FAIL|N/A`
       - **DO NOT modify the `## Verdict` line set by Task 1.** Section 9 is purely supplemental evidence. If any assertion is FAIL, append a note under the assertion grid: `Note: live-DOM assertion(s) did not pass — investigate before shipping, but does not block GO since static + unit evidence passed.`

    Keep the dev servers running in the background; end-of-plan cleanup is handled by the phase SUMMARY step.
  </action>
  <verify>
    <automated>grep -q "^## 9\. PinchTab Smoke Evidence" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md && grep -q "SUPPLEMENTAL" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md && echo "SECTION 9 PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^## 9\. PinchTab Smoke Evidence" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0.
    - `grep -q "SUPPLEMENTAL: \(OK\|PARTIAL\|SKIPPED\)" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0 (explicit banner present).
    - `grep -q "non-blocking" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` exits 0 (non-blocking nature documented in header or body).
    - When banner is `OK`: `grep -cE "ADVANCED_HAS_OVERVIEW: (PASS|FAIL)" .planning/phases/62-advanced-character-inspector-complement-redesign/62-VALIDATION.md` returns `1` AND same for the remaining 9 assertion IDs listed in the action.
    - When banner is `SKIPPED` or `PARTIAL`: assertion lines may be `N/A` — no blocker.
    - Verdict line unchanged from Task 1: the Verdict line that was written in Task 1 is preserved byte-for-byte (Task 2 does not overwrite it).
  </acceptance_criteria>
  <done>
    `62-VALIDATION.md` contains Section 9 with a SUPPLEMENTAL banner and (if OK) a 10-line PASS/FAIL assertion grid. Task 1's Verdict line is preserved unchanged. PinchTab evidence is attached whenever feasible; skipping it is acceptable and clearly marked.
  </done>
</task>

</tasks>

<verification>
- All 8 blocking command sections in `62-VALIDATION.md` show expected outputs and exit 0.
- Section 9 PinchTab evidence is attached with an explicit SUPPLEMENTAL banner (OK / PARTIAL / SKIPPED).
- Final Verdict line is `GO`, computed from sections 1-8 only.
- `git diff --name-only -- frontend/components/world-review/npcs-section.tsx` returns empty (basic card untouched in unstaged worktree).
- `git diff --name-only --cached -- frontend/components/world-review/npcs-section.tsx` returns empty (basic card untouched in staged worktree).
- `git status --short -- frontend/components/world-review/npcs-section.tsx` returns empty (basic card untouched in combined worktree view).
- `grep -c "socialContext\.originMode\|originMode" frontend/components/world-review/character-record-inspector.tsx` returns `0`.
- `grep -c "provenance\.importMode" frontend/components/world-review/character-record-inspector.tsx` returns `>= 1`.
</verification>

<success_criteria>
1. Typecheck, lint, targeted test, full frontend test all exit 0.
2. `npcs-section.tsx` is unchanged across unstaged, staged, and combined status worktree views.
3. Grep audits confirm zero duplicates, zero `originMode` references, and all locked added-sections/new-fields present (socialStatus, relationshipRefs, beliefs, drives, frictions, hasAnyComplementSection, trim-normalized hasItems).
4. No IP fixture names in test file.
5. PinchTab smoke is attached as SUPPLEMENTAL evidence (OK/PARTIAL/SKIPPED) and does NOT gate the Verdict.
6. `62-VALIDATION.md` records all evidence and a `GO` verdict computed from sections 1-8.
</success_criteria>

<output>
After completion, create `.planning/phases/62-advanced-character-inspector-complement-redesign/62-03-SUMMARY.md` referencing `62-VALIDATION.md`.
</output>
