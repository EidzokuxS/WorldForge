---
phase: 62-advanced-character-inspector-complement-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - "62-01"
files_modified:
  - frontend/components/world-review/__tests__/character-record-inspector.test.tsx
autonomous: true
requirements:
  - P62-R1
  - P62-R2
  - P62-R3
  - P62-R4
  - P62-R5
must_haves:
  truths:
    - "Test asserts Display name / Current location / Faction / Persona / Power Stats / Active goals / Long-term goals do NOT render in Advanced"
    - "Test asserts Biography renders inside Overview"
    - "Test asserts Profile section renders with species/gender/ageText/appearance/backgroundSummary"
    - "Test asserts Identity Core renders Social status (from socialContext.socialStatus) and Relationships (from socialContext.relationshipRefs)"
    - "Test asserts Live Dynamics renders beliefDrift/currentStrains/earnedChanges AND beliefs/drives/frictions, and no goal rows"
    - "Test asserts Loadout section renders inventorySeed/equippedItemRefs/signatureItems/currencyNotes"
    - "Test asserts Starting Conditions section renders sourcePrompt/arrivalMode/startLocationId/immediateSituation/entryPressure/companions/startingVisibility/resolvedNarrative"
    - "Test asserts Provenance section renders sourceKind/importMode/templateId/archetypePrompt/worldgenOrigin/legacyTags"
    - "Test asserts Overview badges contain provenance.importMode text AND NOT socialContext.originMode text"
    - "Test asserts invariant-only draft (only canonicalStatus/sourceKind/hp/activityState populated) → 'No additional data' fallback renders AND all 9 complement sections are absent"
    - "Test asserts Raw JSON summary is rendered alongside 'No additional data' fallback (Raw JSON always present)"
    - "Fixtures use only original-world names (Commander Kael, Dunespire Hold, Wind Cutting, Wardstone Plaza); no IP names"
  artifacts:
    - path: "frontend/components/world-review/__tests__/character-record-inspector.test.tsx"
      provides: "Test coverage for 10 locked sections + duplicate absence + invariant-only empty-state + importMode-not-originMode + no-IP fixtures"
      contains: "describe(\"CharacterRecordInspector\""
  key_links:
    - from: "frontend/components/world-review/__tests__/character-record-inspector.test.tsx"
      to: "frontend/components/world-review/character-record-inspector.tsx"
      via: "import { CharacterRecordInspector } from \"../character-record-inspector\""
      pattern: "from \"\\.\\./character-record-inspector\""
---

<objective>
Rewrite `frontend/components/world-review/__tests__/character-record-inspector.test.tsx` so it validates the new Advanced panel contract from Plan 62-01: zero duplicates with basic card, correct section order, all added sections present (including socialStatus/relationshipRefs/beliefs/drives/frictions), invariant-only empty-state fallback, Overview badges use provenance.importMode (not socialContext.originMode), and original-world fixture names.

Purpose: The existing tests assert the OLD contract (Display name / Current location / Faction / Persona / Power Stats all in Advanced). They will all fail after Plan 62-01 unless rewritten. This plan replaces them with tests that pin the new locked contract including the amended CONTEXT.md rules (invariant-only empty state, new field additions, correct badge source).
Output: A rewritten Vitest + @testing-library/react test file that exits 0 under `npm --prefix frontend test character-record-inspector -- --run`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-advanced-character-inspector-complement-redesign/62-CONTEXT.md
@.planning/phases/62-advanced-character-inspector-complement-redesign/62-01-PLAN.md
@.planning/phases/62-advanced-character-inspector-complement-redesign/62-01-SUMMARY.md
@frontend/components/world-review/character-record-inspector.tsx
@frontend/components/world-review/__tests__/character-record-inspector.test.tsx
@shared/src/types.ts
@frontend/components/world-review/npcs-section.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite character-record-inspector.test.tsx to match new contract (including invariant-only empty state + new fields + importMode badge)</name>
  <files>frontend/components/world-review/__tests__/character-record-inspector.test.tsx</files>
  <read_first>
    - frontend/components/world-review/__tests__/character-record-inspector.test.tsx (current test to rewrite)
    - frontend/components/world-review/character-record-inspector.tsx (rewritten by Plan 62-01 — source of truth for new contract, hasAnyComplementSection helper, hasItems trim semantics)
    - .planning/phases/62-advanced-character-inspector-complement-redesign/62-CONTEXT.md (Test Coverage Checklist + No-IP fixtures + invariant-only rule)
    - .planning/phases/62-advanced-character-inspector-complement-redesign/62-01-SUMMARY.md (exact section titles + structure shipped in Plan 62-01)
    - shared/src/types.ts lines 250-449 (exact field names for fixtures, including CharacterRelationshipRef shape)
  </read_first>
  <action>
    Replace the entire test file with a new test suite. Keep: `// @vitest-environment jsdom` header, `import "@testing-library/jest-dom/vitest"`, `import { render, screen, within } from "@testing-library/react"`, `import userEvent from "@testing-library/user-event"`, `import { describe, expect, it } from "vitest"`, `import type { CharacterDraft, CharacterRecord, PowerStats } from "@worldforge/shared"`, and `import { CharacterRecordInspector } from "../character-record-inspector"`.

    FIXTURE RULES (LOCKED):
    - Use only original-world names. Allowed examples: `Commander Kael`, `Dunespire Hold`, `Wind Cutting`, `Wind Blade`, `Wardstone Plaza`, `Storm Marches`, `Border Patrol`, `warded perimeter`.
    - FORBIDDEN: Naruto, Sasuke, Uchiha, Sharingan, Konoha, Hokage, Akatsuki, Madara, Itachi, Kakashi, Gojo, Geto, Cursed Energy, Jujutsu, Dragon Ball, Saiyan, One Piece, Luffy, Avatar, Airbender, Witcher, Geralt, Star Wars, Jedi, Sith, Harry Potter, Hogwarts, or any other real-IP franchise term.

    REWRITTEN TEST STRUCTURE:

    Build a `makeFullDraft()` helper that populates EVERY section of `CharacterDraft`. Use these exact literal values (keep them stable so grep acceptance criteria match):
    - `identity.role = "npc"`, `identity.tier = "key"`, `identity.displayName = "Commander Kael"`, `identity.canonicalStatus = "original"`.
    - `identity.baseFacts.biography = "Kael commanded the Dunespire border watch for twelve turns of the seal."` (will be asserted via `/Kael commanded the Dunespire/i`).
    - `identity.baseFacts.socialRole = ["commander", "border patrol director"]`.
    - `identity.baseFacts.hardConstraints = ["Will not abandon patrol units under his command."]`.
    - `identity.behavioralCore.selfImage = "A hardline commander keeping order in bad terrain."`.
    - `identity.behavioralCore.motives = ["Hold the border line"]`.
    - `identity.behavioralCore.pressureResponses = ["Locks down and prioritizes tactical control"]`.
    - `identity.behavioralCore.taboos = ["Showing weakness to rivals"]`.
    - `identity.behavioralCore.attachments = ["His patrol unit"]`.
    - `identity.liveDynamics.activeGoals = ["Negotiate safe passage corridors"]` (MUST NOT appear in Advanced — basic card owns goals).
    - `identity.liveDynamics.beliefDrift = ["Sorcerer councils cannot be trusted blindly."]`.
    - `identity.liveDynamics.currentStrains = ["Pressure from monster incursions near the warded perimeter"]`.
    - `identity.liveDynamics.earnedChanges = ["Softened stance toward outside scholars after the Wardstone incident."]`.
    - `profile.species = "Human"`, `profile.gender = "Male"`, `profile.ageText = "35"`.
    - `profile.appearance = "Dunespire marshal in patrol gear"`.
    - `profile.backgroundSummary = "A veteran border commander from Dunespire Hold."`.
    - `profile.personaSummary = "Efficient, stern, suspicious of outsiders."` (MUST NOT appear in Advanced).
    - `socialContext.currentLocationName = "Dunespire Hold (Warded City)"` (MUST NOT appear in Advanced).
    - `socialContext.factionName = "Dunespire Hold"` (MUST NOT appear in Advanced as a MetaGrid label — basic card footer owns it).
    - `socialContext.originMode = "resident"` (MUST NOT appear in Advanced — Overview badges now use `provenance.importMode` instead).
    - `socialContext.socialStatus = ["Border authority", "Wind Cutting master"]` (MUST appear in Identity Core).
    - `socialContext.relationshipRefs = [{ /* use CharacterRelationshipRef shape from shared/src/types.ts; include a human-readable label such as "Rival: Councillor Vey of Wardstone Plaza" so it shows up in the Relationships ListBlock */ }]`.
    - `motivations.shortTermGoals = ["Assess the border threat"]` (MUST NOT appear in Advanced).
    - `motivations.longTermGoals = ["Keep Dunespire autonomous"]` (MUST NOT appear in Advanced).
    - `motivations.beliefs = ["The border must hold."]` (MUST appear in Live Dynamics).
    - `motivations.drives = ["Control"]` (MUST appear in Live Dynamics).
    - `motivations.frictions = ["Distrust of sorcerer politics"]` (MUST appear in Live Dynamics).
    - `capabilities.traits = ["Cautious strategist"]`.
    - `capabilities.skills = [{ name: "Wind Cutting Mastery", tier: "Master" }]`.
    - `capabilities.specialties = ["Wind Blade"]`, `capabilities.flaws = ["Overextends patrol units"]`, `capabilities.wealthTier = "Comfortable"` or a valid `CharacterWealthTier` value — check shared/src/types.ts and use a populated non-null value.
    - `state.hp = 5`, `state.conditions = ["Winded"]`, `state.statusFlags = ["On alert"]`, `state.activityState = "active"`.
    - `loadout.inventorySeed = ["Wind Blade", "patrol emblem"]`.
    - `loadout.equippedItemRefs = ["Wind Blade"]`.
    - `loadout.signatureItems = ["Wind Blade"]`.
    - `loadout.currencyNotes = "12 marks of the Dunespire treasury"`.
    - `startConditions.sourcePrompt = "Begin at the warded perimeter during the incursion alert."`.
    - `startConditions.arrivalMode = "already-present"`.
    - `startConditions.startLocationId = "loc-dunespire-perimeter"`.
    - `startConditions.immediateSituation = "Responding to rising warded-barrier incursions."`.
    - `startConditions.entryPressure = ["incursion alert", "patrol roll call"]`.
    - `startConditions.companions = ["Patrol Corporal Nim"]`.
    - `startConditions.startingVisibility = "visible"`.
    - `startConditions.resolvedNarrative = "Kael stands at the Wardstone Plaza, patrol roster in hand."`.
    - `provenance.sourceKind = "worldgen"`.
    - `provenance.importMode = "v2-card"` (a non-null CharacterImportMode — pick a valid enum value; will be asserted as the badge text that replaces the old originMode badge).
    - `provenance.templateId = "tpl-border-commander"`.
    - `provenance.archetypePrompt = "stoic border commander, wind-cutting school"`.
    - `provenance.worldgenOrigin = "original-storm-marches"`.
    - `provenance.legacyTags = ["Wind Cutting Master", "Border Authority"]`.
    - `powerStats` — populate with a valid `PowerStats` object (copy `MOCK_POWER_STATS` from current test; PowerStats must exist on the record so we can assert it is NOT rendered in Advanced).

    ALSO add a `makeInvariantOnlyDraft()` helper that returns a `CharacterDraft` where ONLY the four invariant fields are populated and every other optional field is empty/null/[]:
    - `identity.role = "npc"`, `identity.tier = "key"`, `identity.displayName = "Commander Kael"`.
    - `identity.canonicalStatus = "original"` (INVARIANT).
    - `identity.baseFacts.biography = ""`, `identity.baseFacts.socialRole = []`, `identity.baseFacts.hardConstraints = []`.
    - `identity.behavioralCore.selfImage = ""`, `identity.behavioralCore.motives = []`, `identity.behavioralCore.pressureResponses = []`, `identity.behavioralCore.taboos = []`, `identity.behavioralCore.attachments = []`.
    - `identity.liveDynamics.activeGoals = []`, `identity.liveDynamics.beliefDrift = []`, `identity.liveDynamics.currentStrains = []`, `identity.liveDynamics.earnedChanges = []`.
    - `profile.*` = empty strings, `profile.personaSummary = ""`.
    - `socialContext.currentLocationName = ""`, `socialContext.factionName = ""`, `socialContext.originMode = null`, `socialContext.socialStatus = []`, `socialContext.relationshipRefs = []`.
    - `motivations.shortTermGoals = []`, `motivations.longTermGoals = []`, `motivations.beliefs = []`, `motivations.drives = []`, `motivations.frictions = []`.
    - `capabilities.traits = []`, `capabilities.skills = []`, `capabilities.specialties = []`, `capabilities.flaws = []`, `capabilities.wealthTier = ""` (or whatever "empty" enum value is valid).
    - `state.hp = 3` (INVARIANT — some number), `state.activityState = "idle"` (INVARIANT — some string), `state.conditions = []`, `state.statusFlags = []`.
    - `loadout.inventorySeed = []`, `loadout.equippedItemRefs = []`, `loadout.signatureItems = []`, `loadout.currencyNotes = ""`.
    - `startConditions.*` — every string null/"", every list [].
    - `provenance.sourceKind = "worldgen"` (INVARIANT — required enum), `provenance.importMode = null`, `provenance.templateId = null`, `provenance.archetypePrompt = null`, `provenance.worldgenOrigin = null`, `provenance.legacyTags = []`.
    - `powerStats = undefined`.
    This fixture is the EXACT contract of "invariant-only" — `hasAnyComplementSection` MUST return `false`.

    TESTS (each one an `it(...)` block):

    1. `it("renders all 9 complement sections (Overview + Identity Core + Profile + Live Dynamics + Capabilities + Runtime & State + Loadout + Starting Conditions + Provenance) in order when every field is populated", ...)` — with the full draft, open Advanced, assert headings via:
       ```ts
       const headings = screen.getAllByRole("heading", { level: 4 }).map((h) => h.textContent?.trim());
       expect(headings).toEqual([
         "Overview",
         "Identity Core",
         "Profile",
         "Live Dynamics",
         "Capabilities",
         "Runtime & State",
         "Loadout",
         "Starting Conditions",
         "Provenance",
       ]);
       ```
       Raw JSON uses a `<summary>` not an `<h4>` — assert separately via `expect(screen.getByText(/^raw json$/i)).toBeInTheDocument()`.

    2. `it("never renders basic-card duplicates in Advanced", ...)` — with the full draft, click Advanced, then assert:
       - `expect(screen.queryByText("Display name")).not.toBeInTheDocument()`.
       - `expect(screen.queryByText("Current location")).not.toBeInTheDocument()`.
       - `expect(screen.queryByText(/^Faction$/i)).not.toBeInTheDocument()`.
       - `expect(screen.queryByText(/^Persona$/i)).not.toBeInTheDocument()`.
       - `expect(screen.queryByText(/^Power Stats$/i)).not.toBeInTheDocument()`.
       - `expect(screen.queryByText(/^Active goals$/i)).not.toBeInTheDocument()`.
       - `expect(screen.queryByText(/^Long-term goals$/i)).not.toBeInTheDocument()`.
       - `expect(screen.queryByText(/Efficient, stern, suspicious of outsiders/i)).not.toBeInTheDocument()` (persona summary text absent).
       - `expect(screen.queryByText(/Negotiate safe passage corridors/i)).not.toBeInTheDocument()` (activeGoals absent).
       - `expect(screen.queryByText(/Assess the border threat/i)).not.toBeInTheDocument()` (shortTermGoals absent).
       - `expect(screen.queryByText(/Keep Dunespire autonomous/i)).not.toBeInTheDocument()` (longTermGoals absent).

    3. `it("renders biography in Overview section", ...)` — scope with `within(screen.getByRole("heading", { name: /^overview$/i }).closest("section")!)` and assert `getByText(/Kael commanded the Dunespire/i)` is present.

    4. `it("Overview badges contain provenance.importMode text and NOT socialContext.originMode text", ...)` — LOCKED from review BLOCKER 2. With the full draft (where `provenance.importMode = "v2-card"` and `socialContext.originMode = "resident"`):
       - Scope into Overview section.
       - Positive: `expect(within(overview).getByText(/v2-card/i)).toBeInTheDocument()` (importMode badge present).
       - Negative: `expect(within(overview).queryByText(/^resident$/i)).not.toBeInTheDocument()` (originMode value absent from badge row).

    5. `it("renders Profile section with species / gender / ageText / appearance / backgroundSummary", ...)` — scope inside the Profile section; assert `/Human/`, `/Male/`, `/35/`, `/Dunespire marshal in patrol gear/`, `/A veteran border commander from Dunespire Hold/` all appear.

    6. `it("renders Identity Core socialStatus and relationshipRefs when populated", ...)` — NEW from review P1. Scope inside Identity Core section:
       - Assert `getByText(/Social status/i)` label present.
       - Assert `getByText(/Border authority/i)` present.
       - Assert `getByText(/Wind Cutting master/i)` present.
       - Assert `getByText(/Relationships/i)` label present.
       - Assert at least one of the relationship label strings (e.g. `/Rival: Councillor Vey/i`) is present.

    7. `it("renders Live Dynamics with beliefDrift / currentStrains / earnedChanges AND beliefs / drives / frictions, no goal rows", ...)` — LOCKED + EXPANDED. Scope inside Live Dynamics section:
       - Positive: `/Sorcerer councils cannot be trusted blindly/i`, `/Pressure from monster incursions near the warded perimeter/i`, `/Softened stance toward outside scholars after the Wardstone incident/i`, `/The border must hold/i` (beliefs), `/Control/i` (drives), `/Distrust of sorcerer politics/i` (frictions).
       - Label positive: `getByText(/^Beliefs$/i)`, `getByText(/^Drives$/i)`, `getByText(/^Frictions$/i)` present.
       - Negative: `queryByText(/Active goals/i)` null, `queryByText(/Long-term goals/i)` null within that section.

    8. `it("renders Loadout section with currencyNotes", ...)` — scope inside Loadout section; assert `/Wind Blade/`, `/patrol emblem/`, `/12 marks of the Dunespire treasury/` all present.

    9. `it("renders Starting Conditions section with all sub-fields", ...)` — scope inside Starting Conditions section; assert `/Begin at the warded perimeter during the incursion alert/`, `/already-present/`, `/loc-dunespire-perimeter/`, `/Responding to rising warded-barrier incursions/`, `/incursion alert/`, `/Patrol Corporal Nim/`, `/visible/`, `/Kael stands at the Wardstone Plaza, patrol roster in hand/` all present.

    10. `it("renders Provenance section with all sub-fields", ...)` — scope inside Provenance section; assert `/worldgen/`, `/v2-card/`, `/tpl-border-commander/`, `/stoic border commander, wind-cutting school/`, `/original-storm-marches/`, `/Wind Cutting Master/`, `/Border Authority/` all present.

    11. `it("suppresses a section whose only non-empty fields are invariant (invariant-only draft → fallback renders)", ...)` — NEW from review BLOCKER 1. Use `makeInvariantOnlyDraft()`; open Advanced; assert:
        - `expect(screen.getByText(/^No additional data$/i)).toBeInTheDocument()` (fallback paragraph rendered).
        - `expect(screen.queryByRole("heading", { name: /^overview$/i })).not.toBeInTheDocument()`.
        - `expect(screen.queryByRole("heading", { name: /^identity core$/i })).not.toBeInTheDocument()`.
        - `expect(screen.queryByRole("heading", { name: /^profile$/i })).not.toBeInTheDocument()`.
        - `expect(screen.queryByRole("heading", { name: /^live dynamics$/i })).not.toBeInTheDocument()`.
        - `expect(screen.queryByRole("heading", { name: /^capabilities$/i })).not.toBeInTheDocument()`.
        - `expect(screen.queryByRole("heading", { name: /^runtime & state$/i })).not.toBeInTheDocument()` (hp + activityState are invariant-only, section suppressed).
        - `expect(screen.queryByRole("heading", { name: /^loadout$/i })).not.toBeInTheDocument()`.
        - `expect(screen.queryByRole("heading", { name: /^starting conditions$/i })).not.toBeInTheDocument()`.
        - `expect(screen.queryByRole("heading", { name: /^provenance$/i })).not.toBeInTheDocument()` (sourceKind is invariant-only, section suppressed).

    12. `it("renders Raw JSON tail alongside fallback for invariant-only draft", ...)` — NEW from review P2. Use `makeInvariantOnlyDraft()`; open Advanced; assert BOTH `screen.getByText(/^No additional data$/i)` and `screen.getByText(/^raw json$/i)` are present.

    13. `it("uses only original-world fixture names (no IP franchise terms)", ...)` — self-check: iterate over `JSON.stringify(makeFullDraft())` and `JSON.stringify(makeInvariantOnlyDraft())`; assert `.not.toMatch(/naruto|sasuke|uchiha|sharingan|konoha|hokage|akatsuki|madara|itachi|kakashi|gojo|geto|cursed energy|jujutsu|dragon ball|saiyan|one piece|luffy|avatar|airbender|witcher|geralt|star wars|jedi|sith|harry potter|hogwarts/i)`. This locks the no-IP rule at runtime.

    REMOVE old tests (their contract no longer holds):
    - `"renders overview, identity core, live dynamics, power stats, capabilities, and runtime sections"` — replaced by test 1.
    - `"suppresses duplicate self-image text and system role labels"` — merge the `selfImage === personaSummary` dedup into test 2 (or keep as a dedicated test case assertion inside Identity Core section if needed).
    - `"renders shared PowerStatsSection atom when powerStats present"` — DELETE (Power Stats no longer in Advanced).
    - `"renders 'No power assessment' when powerStats absent"` — DELETE.

    TypeScript strict: `CharacterDraft` is typed from `@worldforge/shared`; use existing imports. No `any`. Use `as const` where needed for tier values.

    Addresses test coverage checklist from 62-CONTEXT.md (amended), BLOCKER 1 (invariant-only fallback), BLOCKER 2 (importMode vs originMode badge), P1 (new fields coverage), P2 (Raw JSON alongside fallback), and no-IP fixtures rule from `feedback_no_ip_in_prompts.md`.
  </action>
  <verify>
    <automated>npm --prefix frontend test character-record-inspector -- --run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix frontend test character-record-inspector -- --run` → exit 0.
    - `grep -c "Naruto\|Sasuke\|Uchiha\|Sharingan\|Konoha\|Hokage\|Akatsuki\|Madara\|Itachi\|Kakashi\|Gojo\|Geto\|Jujutsu\|Saiyan\|Luffy\|Airbender\|Geralt\|Jedi\|Sith\|Hogwarts" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → output `0`.
    - `grep -q "Commander Kael" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "Dunespire" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "Wind Cutting\|Wind Blade" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "never renders basic-card duplicates" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "renders biography in Overview" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "renders Profile section" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "renders Identity Core socialStatus and relationshipRefs" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "renders Live Dynamics with beliefDrift / currentStrains / earnedChanges AND beliefs / drives / frictions\|beliefs / drives / frictions" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "renders Loadout section" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "renders Starting Conditions section" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "renders Provenance section" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "Overview badges contain provenance.importMode text and NOT socialContext.originMode text" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "suppresses a section whose only non-empty fields are invariant\|invariant-only draft" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "renders Raw JSON tail alongside fallback\|raw json.*fallback\|fallback.*raw json" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "makeInvariantOnlyDraft" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "No additional data" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0.
    - `grep -q "v2-card" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 0 (importMode fixture value).
    - `grep -q "renders shared PowerStatsSection atom" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 1 (old test removed).
    - `grep -q "renders 'No power assessment'" frontend/components/world-review/__tests__/character-record-inspector.test.tsx` → exit 1 (old test removed).
    - `npm --prefix frontend run typecheck` → exit 0.
  </acceptance_criteria>
  <done>
    Test file compiles and all rewritten tests pass under Vitest; no IP fixture names are present; old power-stats tests are removed; invariant-only draft triggers fallback; importMode badge positive+negative assertions present; socialStatus/relationshipRefs/beliefs/drives/frictions coverage present.
  </done>
</task>

</tasks>

<verification>
- `npm --prefix frontend test character-record-inspector -- --run` → exit 0.
- All grep acceptance criteria in Task 1 pass.
</verification>

<success_criteria>
1. Tests assert every locked decision from 62-CONTEXT.md (amended): duplicate absence, added sections (including socialStatus/relationshipRefs/beliefs/drives/frictions), invariant-only empty-state fallback, Overview badges use importMode not originMode.
2. Fixtures contain zero IP franchise names.
3. Old PowerStats tests are removed.
4. All new tests pass under `npm --prefix frontend test character-record-inspector -- --run`.
5. Frontmatter `requirements:` traces all five: P62-R1, P62-R2, P62-R3, P62-R4, P62-R5.
</success_criteria>

<output>
After completion, create `.planning/phases/62-advanced-character-inspector-complement-redesign/62-02-SUMMARY.md`
</output>
