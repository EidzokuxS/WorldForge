---
phase: 75-cross-phase-promise-audit-and-location-presence-reality-clos
plan: 04
type: execute
wave: 3
depends_on:
  - "75-02"
files_modified:
  - backend/src/worldgen/scaffold-saver.ts
  - backend/src/worldgen/__tests__/scaffold-saver.test.ts
autonomous: true
requirements:
  - P75-R3
  - P75-R4
  - P75-R6
  - P75-R7
must_haves:
  truths:
    - "Scaffold persistence writes explicit persistent sublocations with valid parent ids."
    - "Generated NPCs assigned to sublocations receive broad `currentLocationId` and scoped `currentSceneLocationId`."
    - "Legacy flat scaffolds remain macro-compatible."
    - "Invalid explicit parent/scene references do not silently collapse dense worlds into one macro room."
  artifacts:
    - path: "backend/src/worldgen/scaffold-saver.ts"
      provides: "Deterministic scaffold hierarchy and actor placement persistence"
      contains: "currentSceneLocationId"
    - path: "backend/src/worldgen/__tests__/scaffold-saver.test.ts"
      provides: "Dense-world persistence regression coverage"
      contains: "persistent_sublocation"
  key_links:
    - from: "backend/src/worldgen/scaffold-saver.ts"
      to: "backend/src/db/schema.ts"
      via: "locations.parentLocationId and npcs.currentSceneLocationId writes"
      pattern: "parentLocationId|currentSceneLocationId"
    - from: "backend/src/worldgen/scaffold-saver.ts"
      to: "backend/src/engine/scene-presence.ts"
      via: "stored broad/scene actor columns consumed by resolver"
      pattern: "currentLocationId|currentSceneLocationId"
---

<objective>
Persist generated location hierarchy and NPC scene placement deterministically.

Purpose: Fix the data bridge that currently saves all generated locations as macro rows and all generated NPCs as broad-only actors.
Output: Two-pass location persistence, containment travel edges, NPC broad/scene placement, and dense scaffold saver regressions.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-01-SUMMARY.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-02-SUMMARY.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-REGRESSION-MATRIX.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-LOCATION-PRESENCE-TRACE.md
@backend/src/worldgen/scaffold-saver.ts
@backend/src/worldgen/__tests__/scaffold-saver.test.ts
@backend/src/worldgen/__tests__/fixtures/dense-location-scaffold.ts
@backend/src/db/schema.ts

<interfaces>
Storage fields already available:
```typescript
locations.kind: "macro" | "persistent_sublocation" | "ephemeral_scene";
locations.parentLocationId: string | null;
npcs.currentLocationId: string | null;
npcs.currentSceneLocationId: string | null;
players.currentLocationId: string | null;
players.currentSceneLocationId: string | null;
```
</interfaces>
</context>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Dense worlds persist scoped sublocations and distribute NPC presence | Saves parented sublocation rows and NPC scene ids |
| REQ | P75-R3 | Two-pass location insert maps explicit sublocations to parent ids |
| REQ | P75-R4 | NPC insert maps `sceneLocationName` to `currentSceneLocationId` |
| REQ | P75-R6 | Persistence uses only explicit scaffold fields and exact references |
| REQ | P75-R7 | Scaffold saver tests prove no all-NPC macro collapse |
| RESEARCH | Root cause is `insertLocations` hardcoded `macro` and `insertNpcs` broad-only | Directly changes those functions |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist locations in two passes with containment edges</name>
  <files>backend/src/worldgen/scaffold-saver.ts, backend/src/worldgen/__tests__/scaffold-saver.test.ts</files>
  <behavior>
    - RED: dense scaffold saves one macro row and multiple `persistent_sublocation` rows with `parentLocationId` set to the macro id.
    - RED: parent/child containment creates reachable graph edges or connected paths between macro and sublocation rows.
    - RED: duplicate scaffold location names fail deterministically before id maps are built.
    - RED: legacy scaffolds without hierarchy fields still save rows as `macro` with `parentLocationId: null`.
    - RED: invalid explicit parent references fail deterministically instead of being silently saved as macro.
  </behavior>
  <action>
    Do not create a worktree. Before editing production symbols, run GitNexus impact analysis for `insertLocations`, `updateAdjacency`, and `saveScaffoldToDb`. Stop and report if any result is HIGH or CRITICAL.

    Write RED tests in `backend/src/worldgen/__tests__/scaffold-saver.test.ts` using `makeDenseLocationScaffold`. Then update `backend/src/worldgen/scaffold-saver.ts`:
    - Reject duplicate location names before constructing any `Map<string, id>` so a later duplicate cannot overwrite the first row.
    - Allocate ids for all location names before inserts.
    - Insert macro rows first, then `persistent_sublocation` rows.
    - For a sublocation, resolve `parentLocationName` by exact scaffold name to a macro row id.
    - Persist sublocations as `kind: "persistent_sublocation"`, `parentLocationId` set, `persistence: "persistent"`, and existing description/tags/start fields preserved.
    - Add deterministic containment adjacency between macro and sublocation rows unless an equivalent edge already exists.
    - Preserve legacy rows with missing `kind` as macro-compatible.

    Do not infer hierarchy from text, name shape, known IP, franchise, or canon labels. Only explicit scaffold fields drive hierarchy.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/scaffold-saver.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>Tests prove parent ids, location kinds, containment reachability, legacy compatibility, and invalid-parent fail-closed behavior.</acceptance_criteria>
  <done>Generated scaffold locations persist as deterministic macro/sublocation hierarchy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist NPC broad and scene placement</name>
  <files>backend/src/worldgen/scaffold-saver.ts, backend/src/worldgen/__tests__/scaffold-saver.test.ts</files>
  <behavior>
    - RED: NPC assigned to a persistent sublocation saves `currentLocationId` as parent macro and `currentSceneLocationId` as sublocation id.
    - RED: NPC assigned to a macro saves both broad and scene ids as that macro id.
    - RED: NPCs in sibling sublocations under the same macro have distinct scene ids.
    - RED: if `sceneLocationName` points to a persistent sublocation whose parent macro conflicts with explicit `locationName`, save fails deterministically instead of silently overwriting broad placement.
    - RED: invalid explicit `sceneLocationName` fails deterministically; omitted scene field keeps legacy fallback behavior.
  </behavior>
  <action>
    Before editing production symbols, run GitNexus impact analysis for `insertNpcs` and any helper symbol introduced for resolving scaffold placement.

    Extend `insertNpcs` to resolve actor placement from explicit scaffold references:
    - `locationName` remains the broad/home location reference.
    - When `sceneLocationName` resolves to a persistent sublocation, set broad id to that sublocation parent macro id and scene id to the sublocation id.
    - If explicit `locationName` resolves to a different macro than the sublocation parent, fail validation; do not silently prefer either field.
    - When `sceneLocationName` resolves to a macro, set broad and scene to the macro id.
    - When `sceneLocationName` is omitted/null, keep legacy behavior for broad id and leave scene fallback compatible.
    - If an explicit `sceneLocationName` cannot be resolved, fail before partial save completes.

    Keep character draft reconciliation intact. For draft social context, preserve existing broad location name/id behavior unless a pre-existing draft field explicitly supports scene scope; do not invent new semantic fields inside character records in this plan.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/scaffold-saver.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>Dense scaffold saver tests show NPCs distributed by scene scope under one macro instead of all resolving to the same macro scene.</acceptance_criteria>
  <done>Generated NPC rows carry usable broad and current-scene ids for runtime presence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scaffold data -> SQLite | Generated or edited scaffold rows become durable game state. |
| exact references -> hierarchy/placement | Names from scaffold fields resolve to stored ids and actor scene state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04-01 | Tampering | parent/scene references | mitigate | Exact-reference validation and fail-closed handling for invalid explicit hierarchy fields. |
| T-75-04-02 | Spoofing | source/franchise meaning | mitigate | Saver uses only explicit scaffold fields; no string heuristics from names. |
| T-75-04-03 | Repudiation | dense-world persistence | mitigate | Scaffold saver tests assert persisted DB rows and actor ids. |
| T-75-04-04 | Denial of Service | cyclic or duplicate containment | mitigate | Deduplicate edges and reject invalid parent references plus duplicate names in tests. |
</threat_model>

<verification>
Run:
```powershell
npm --prefix backend run test -- src/worldgen/__tests__/scaffold-saver.test.ts
npm --prefix backend run typecheck
```
</verification>

<success_criteria>
- Dense scaffold saves parented persistent sublocation rows.
- NPCs assigned to sublocations persist distinct current scene ids.
- Legacy flat scaffolds still work.
- Invalid explicit hierarchy/scene references do not silently recreate macro collapse.
- Duplicate names and broad/scene conflicts fail deterministically before partial persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-04-SUMMARY.md`.
</output>
