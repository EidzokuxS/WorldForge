---
phase: 75-cross-phase-promise-audit-and-location-presence-reality-clos
plan: 03
type: execute
wave: 3
depends_on:
  - "75-02"
files_modified:
  - backend/src/worldgen/scaffold-steps/locations-step.ts
  - backend/src/worldgen/scaffold-steps/npcs-step.ts
  - backend/src/worldgen/scaffold-generator.ts
  - backend/src/worldgen/scaffold-steps/regen-helpers.ts
  - backend/src/routes/worldgen.ts
  - backend/src/worldgen/__tests__/scaffold-resilience.test.ts
  - backend/src/worldgen/__tests__/npcs-step.test.ts
  - backend/src/routes/__tests__/worldgen.test.ts
autonomous: true
requirements:
  - P75-R3
  - P75-R4
  - P75-R6
  - P75-R7
must_haves:
  truths:
    - "Worldgen location prompts ask for explicit macro and persistent sublocation rows when generated evidence supports dense places."
    - "Worldgen NPC prompts ask for explicit scene placement separate from broad/home location."
    - "Prompt contracts frame hierarchy as physical containment and placement, not backend-inferred franchise/canon meaning."
  artifacts:
    - path: "backend/src/worldgen/scaffold-steps/locations-step.ts"
      provides: "Location plan/detail schema and prompt contract for hierarchy"
      contains: "parentLocationName"
    - path: "backend/src/worldgen/scaffold-steps/npcs-step.ts"
      provides: "NPC plan schema and prompt contract for scene placement"
      contains: "sceneLocationName"
  key_links:
    - from: "backend/src/worldgen/scaffold-steps/locations-step.ts"
      to: "backend/src/worldgen/types.ts"
      via: "ScaffoldLocation hierarchy fields"
      pattern: "kind.*persistent_sublocation|parentLocationName"
    - from: "backend/src/worldgen/scaffold-steps/npcs-step.ts"
      to: "backend/src/worldgen/types.ts"
      via: "ScaffoldNpc scene placement field"
      pattern: "sceneLocationName"
---

<objective>
Make generated scaffolds produce explicit hierarchy and scene placement.

Purpose: The saver can only preserve scoped locations and actor presence if the LLM-facing contracts request those fields clearly and safely.
Output: Prompt/schema updates for locations and NPCs plus targeted prompt contract tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-02-SUMMARY.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-REGRESSION-MATRIX.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-RESEARCH.md
@backend/src/worldgen/scaffold-steps/locations-step.ts
@backend/src/worldgen/scaffold-steps/npcs-step.ts
@backend/src/worldgen/scaffold-steps/prompt-utils.ts
@backend/src/worldgen/__tests__/scaffold-resilience.test.ts
@backend/src/worldgen/__tests__/npcs-step.test.ts
</context>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Dense worlds use scoped sublocations instead of one macro room | Prompts request explicit contained persistent sublocation rows |
| REQ | P75-R3 | Location plan/detail schema carries `kind` and `parentLocationName` |
| REQ | P75-R4 | NPC plan schema carries `sceneLocationName` |
| REQ | P75-R6 | Prompt text forbids backend/model prompt shortcuts based on source-name inference |
| REQ | P75-R7 | Prompt contract tests lock dense urban fixture shape |
| RESEARCH | Existing location cap is flat 5-8 | Adds exact bounded combined cap: 5-12 total rows, <=6 macro rows, <=6 persistent sublocation rows, <=3 sublocations per macro |
| REVIEW | `validateLocation` can fallback to first location and hide invalid scene refs | Adds exact scene validation that accepts full known location namespace and fails explicit invalid scene refs |
| REVIEW | `/regenerate-section` NPC path may receive/pass only macro names | Adds route/generator tests requiring macro plus persistent sublocation names for NPC scene placement |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add location hierarchy prompt contract</name>
  <files>backend/src/worldgen/scaffold-steps/locations-step.ts, backend/src/worldgen/__tests__/scaffold-resilience.test.ts</files>
  <behavior>
    - RED: location prompt contract names `kind` and `parentLocationName` and explains `macro` vs `persistent_sublocation`.
    - RED: generated dense-place prompt asks for physically contained smaller places when source/scaffold evidence supports them.
    - RED: tests assert contract appears before world premise/source context and includes authority text forbidding source-name inference.
    - GREEN: legacy original-world and artifact-backed prompt tests still pass.
  </behavior>
  <action>
    Do not create a worktree. Before editing production symbols, run GitNexus impact analysis for `generateLocationsStep`, `locationPlanSchema`, and `locationDetailSingleSchema`.

    Write RED tests in `backend/src/worldgen/__tests__/scaffold-resilience.test.ts`. Then update `backend/src/worldgen/scaffold-steps/locations-step.ts` so the planning schema returns `kind?: "macro" | "persistent_sublocation"` and `parentLocationName?: string | null` for each planned location.

    Prompt rules:
    - Ask for macro places plus physically contained persistent sublocations when a dense macro place contains meaningful smaller places.
    - Bound the combined list explicitly and test the exact cap: 5-12 total location rows, no more than 6 macro rows, no more than 6 persistent sublocation rows, and no more than 3 generated sublocations under any one macro.
    - `parentLocationName` must exactly match a generated macro location name for sublocations.
    - `macro` rows have `parentLocationName: null`.
    - Do not ask the backend or model prompt text to infer source/canon/franchise meaning from raw names.

    When pushing detailed locations, copy planned `kind` and `parentLocationName` into the returned `ScaffoldLocation` rows. Normalize missing `kind` as macro-compatible only when the plan omitted the field.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/scaffold-resilience.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>Location prompt/schema tests prove hierarchy fields, bounded combined output, source-authority guardrails, and legacy compatibility.</acceptance_criteria>
  <done>Location generation can produce explicit macro and persistent sublocation rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add NPC scene-placement prompt contract</name>
  <files>backend/src/worldgen/scaffold-steps/npcs-step.ts, backend/src/worldgen/__tests__/npcs-step.test.ts</files>
  <behavior>
    - RED: NPC plan schema and prompt contract include `sceneLocationName`.
    - RED: tests prove `locationName` remains broad macro/home and `sceneLocationName` is current scene scope when known.
    - RED: explicit invalid `sceneLocationName` does not fall back to `locationNames[0]`; it is rejected or repaired before persistence.
    - RED: NPC generation and regeneration paths receive the full known-location namespace, including persistent sublocation names.
    - RED: tests prove invalid examples forbid invented backend/source interpretation.
    - GREEN: existing known-IP/artifact NPC classification tests keep artifact authority.
  </behavior>
  <action>
    Before editing production symbols, run GitNexus impact analysis for `planKeyNpcs`, `planSupportingNpcs`, `npcPlanSchema`, `PlannedNpc`, and `generateNpcsStep`.

    Write RED tests in `backend/src/worldgen/__tests__/npcs-step.test.ts`. Then update `backend/src/worldgen/scaffold-steps/npcs-step.ts` so plan outputs may include `sceneLocationName?: string | null`.

    Prompt rules:
    - `locationName` must exactly match the broad macro/home location when a broad location is known.
    - `sceneLocationName` must exactly match a known location or sublocation when the NPC's current scene is known.
    - If no scoped evidence exists, `sceneLocationName` is null/omitted and runtime fallback remains broad.
    - Do not classify source roles, canon status, or franchise ownership from location/NPC names.

    When mapping planned NPCs into `ScaffoldNpc`, preserve `sceneLocationName`. Keep existing `validateLocation` and `validateFaction` behavior for broad/faction fields; add exact-reference validation for scene names against the full known location-name namespace without creating new names or falling back to the first location.

    Also cover regeneration:
    - Ensure `generateScaffold` and `/regenerate-section section=npcs` pass all known generated location names, including persistent sublocations, into NPC prompts and validation.
    - Add a route or generator test proving a regenerated NPC may return `locationName` as the broad macro and `sceneLocationName` as a contained persistent sublocation.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/npcs-step.test.ts src/worldgen/__tests__/scaffold-resilience.test.ts src/routes/__tests__/worldgen.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>NPC prompt/schema tests prove scene placement is explicit, broad location compatibility remains, and artifact/source authority is preserved.</acceptance_criteria>
  <done>NPC generation can carry explicit current scene placement into the scaffold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt contract -> model output | Model receives schema and authority instructions before generating scaffold rows. |
| artifact/source data -> prompt | Source usage rules may guide generation but backend prompt text must not invent semantic meaning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03-01 | Tampering | location hierarchy output | mitigate | Zod schemas constrain `kind`; tests assert exact parent-reference instructions. |
| T-75-03-02 | Spoofing | NPC source/canon semantics | mitigate | NPC prompt tests assert source-rule authority and no backend string-inference language. |
| T-75-03-03 | Denial of Service | oversized dense location output | mitigate | Prompt contract contains exact bounded combined output cap: 5-12 total, <=6 macro, <=6 persistent sublocation, <=3 sublocations per macro. Tests lock this cap text. |
</threat_model>

<verification>
Run:
```powershell
npm --prefix backend run test -- src/worldgen/__tests__/scaffold-resilience.test.ts src/worldgen/__tests__/npcs-step.test.ts src/routes/__tests__/worldgen.test.ts
npm --prefix backend run typecheck
```
</verification>

<success_criteria>
- Location prompt contract can produce macro and persistent sublocation rows.
- NPC prompt contract can produce broad and current-scene placement.
- NPC generation/regeneration uses full known-location names for scene placement and rejects invalid explicit scene references.
- Prompt tests lock the backend/LLM authority boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-03-SUMMARY.md`.
</output>
