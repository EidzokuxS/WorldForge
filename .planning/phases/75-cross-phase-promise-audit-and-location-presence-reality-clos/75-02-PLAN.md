---
phase: 75-cross-phase-promise-audit-and-location-presence-reality-clos
plan: 02
type: execute
wave: 2
depends_on:
  - "75-01"
files_modified:
  - backend/src/worldgen/types.ts
  - backend/src/routes/schemas.ts
  - backend/src/routes/worldgen.ts
  - backend/src/routes/__tests__/schemas.test.ts
  - backend/src/routes/__tests__/worldgen.test.ts
  - frontend/lib/api-types.ts
  - frontend/lib/world-data-helpers.ts
  - frontend/lib/__tests__/world-data-helpers.test.ts
  - frontend/components/world-review/locations-section.tsx
  - frontend/components/world-review/__tests__/locations-section.test.tsx
  - frontend/components/world-review/npcs-section.tsx
  - frontend/components/world-review/__tests__/npcs-section.test.tsx
  - frontend/app/(non-game)/campaign/[id]/review/page.tsx
  - frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx
autonomous: true
requirements:
  - P75-R3
  - P75-R4
  - P75-R6
  - P75-R7
must_haves:
  truths:
    - "The scaffold contract can carry persistent sublocation hierarchy and NPC scene placement through generated and save-edits paths."
    - "Manual save-edits normalization preserves hierarchy and scene-placement fields instead of dropping them."
    - "World Review load/edit/regenerate/save round-trips preserve hierarchy and scene-placement fields instead of flattening the scaffold in the browser."
    - "Legacy flat scaffolds still parse and save as macro-compatible data."
    - "Backend schema/normalization does not infer source, canon, or franchise meaning from arbitrary strings."
  artifacts:
    - path: "backend/src/worldgen/types.ts"
      provides: "ScaffoldLocation and ScaffoldNpc hierarchy contract"
      contains: "parentLocationName"
    - path: "backend/src/routes/schemas.ts"
      provides: "save-edits Zod schema preserving hierarchy fields"
      contains: "sceneLocationName"
    - path: "backend/src/routes/worldgen.ts"
      provides: "normalizeSavedScaffold preserving hierarchy and NPC scene fields"
      contains: "normalizeSavedScaffold"
    - path: "frontend/lib/world-data-helpers.ts"
      provides: "World Review editable scaffold conversion preserving hierarchy and NPC scene fields"
      contains: "toEditableScaffold"
    - path: "frontend/components/world-review/locations-section.tsx"
      provides: "World Review location editor preserving kind and parent references"
      contains: "parentLocationName"
    - path: "frontend/components/world-review/npcs-section.tsx"
      provides: "World Review NPC editor preserving current scene references"
      contains: "sceneLocationName"
  key_links:
    - from: "backend/src/routes/schemas.ts"
      to: "backend/src/routes/worldgen.ts"
      via: "SaveEditsScaffold -> normalizeSavedScaffold"
      pattern: "parentLocationName|sceneLocationName"
    - from: "backend/src/worldgen/types.ts"
      to: "backend/src/worldgen/scaffold-saver.ts"
      via: "WorldScaffold contract consumed by saver in Plan 75-04"
      pattern: "ScaffoldLocation|ScaffoldNpc"
    - from: "frontend/lib/world-data-helpers.ts"
      to: "backend/src/routes/worldgen.ts"
      via: "World Review editable scaffold submitted to save-edits"
      pattern: "toEditableScaffold|saveWorldEdits"
---

<objective>
Add the explicit scaffold hierarchy contract and preserve it through save-edits.

Purpose: Generated and manually edited worlds need the same broad/scene shape before persistence can stop flattening dense locations.
Output: Extended scaffold types, route schemas, save-edits normalization, and RED/GREEN route/schema tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-REGRESSION-MATRIX.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-RESEARCH.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-LOCATION-PRESENCE-TRACE.md
@backend/src/worldgen/types.ts
@backend/src/routes/schemas.ts
@backend/src/routes/worldgen.ts
@backend/src/routes/__tests__/schemas.test.ts
@backend/src/routes/__tests__/worldgen.test.ts
@frontend/lib/api-types.ts
@frontend/lib/world-data-helpers.ts
@frontend/lib/__tests__/world-data-helpers.test.ts
@frontend/components/world-review/locations-section.tsx
@frontend/components/world-review/__tests__/locations-section.test.tsx
@frontend/components/world-review/npcs-section.tsx
@frontend/components/world-review/__tests__/npcs-section.test.tsx
@frontend/app/(non-game)/campaign/[id]/review/page.tsx
@frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx

<interfaces>
Target contract to implement:
```typescript
export interface ScaffoldLocation {
  name: string;
  description: string;
  tags: string[];
  isStarting: boolean;
  connectedTo: string[];
  kind?: "macro" | "persistent_sublocation";
  parentLocationName?: string | null;
}

export interface ScaffoldNpc {
  name: string;
  persona: string;
  tags: string[];
  goals: ScaffoldNpcGoals;
  draft?: CharacterDraft;
  locationName: string;
  sceneLocationName?: string | null;
  factionName: string | null;
  tier?: "key" | "supporting";
}
```
</interfaces>
</context>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Dense generated worlds persist/use scoped sublocations | Defines source data shape needed by persistence |
| REQ | P75-R3 | Adds `kind` and `parentLocationName` to scaffold location contract |
| REQ | P75-R4 | Adds `sceneLocationName` to scaffold NPC contract |
| REQ | P75-R6 | Fields are explicit LLM/scaffold data; no backend source-name inference |
| REQ | P75-R7 | RED/GREEN tests prove save-edits does not erase hierarchy data |
| RESEARCH | `normalizeSavedScaffold` drops unknown new fields | Updates schema and normalization together |
| REVIEW | World Review frontend can re-flatten hierarchy before save-edits | Adds frontend editable scaffold and component round-trip coverage |
| REVIEW | `scaffoldNpcSchema` has draft-backed transform branches | Adds explicit schema tests for legacy and draft-backed `sceneLocationName` preservation |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend scaffold type and save-edits schema</name>
  <files>backend/src/worldgen/types.ts, backend/src/routes/schemas.ts, backend/src/routes/__tests__/schemas.test.ts</files>
  <behavior>
    - RED: `saveEditsSchema` accepts a macro row plus persistent sublocation rows with `parentLocationName`.
    - RED: `saveEditsSchema` accepts NPCs with `locationName` as broad macro and `sceneLocationName` as current scene.
    - RED: `scaffoldNpcSchema` preserves `sceneLocationName` through both legacy NPC payloads and draft-backed NPC transform/reconciliation branches.
    - RED: legacy payloads without `kind`, `parentLocationName`, or `sceneLocationName` still parse.
    - GREEN: type and Zod contracts carry only explicit hierarchy/placement fields; no source/canon/franchise semantics are derived from names.
  </behavior>
  <action>
    Do not create a worktree. Before editing production symbols, run GitNexus impact analysis for `ScaffoldLocation`, `ScaffoldNpc`, `scaffoldLocationSchema`, `scaffoldNpcSchema`, and `saveEditsSchema`. If an impact result is HIGH or CRITICAL, stop and report the blast radius before editing.

    Write RED tests in `backend/src/routes/__tests__/schemas.test.ts` first, including draft-backed and legacy NPC cases. Then extend `backend/src/worldgen/types.ts` and `backend/src/routes/schemas.ts` with the target contract from this plan's interfaces block.

    `ScaffoldLocation.kind` is optional and constrained to `macro | persistent_sublocation`; omitted means legacy macro compatibility. `parentLocationName` is nullable/optional and is meaningful only for `persistent_sublocation`. `ScaffoldNpc.sceneLocationName` is nullable/optional and, when present, is explicit current scene placement; `locationName` remains broad/home compatibility.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/routes/__tests__/schemas.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>Schema tests fail before implementation, pass after implementation, and prove new fields are preserved at parse boundary.</acceptance_criteria>
  <done>Scaffold and save-edits schema contracts carry hierarchy and NPC scene placement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve hierarchy in save-edits normalization</name>
  <files>backend/src/routes/worldgen.ts, backend/src/routes/__tests__/worldgen.test.ts</files>
  <behavior>
    - RED: `/api/worldgen/save-edits` passes `kind`, `parentLocationName`, and `sceneLocationName` into `saveScaffoldToDb`.
    - RED: a manually edited dense scaffold does not collapse back to flat macro-only rows at normalization time.
    - GREEN: normalization still reconciles draft-backed NPCs and legacy NPCs through existing draft adapters.
  </behavior>
  <action>
    Before editing production symbols, run GitNexus impact analysis for `normalizeSavedScaffold` and the `/save-edits` route handler in `backend/src/routes/worldgen.ts`.

    Write RED tests in `backend/src/routes/__tests__/worldgen.test.ts` around the existing mocked `saveScaffoldToDb` assertions. Then update `normalizeSavedScaffold` so location entries copy `kind` and `parentLocationName`, and NPC entries copy `sceneLocationName`, while preserving existing tag/connectedTo/goals/draft normalization.

    Keep deterministic boundary: normalization may trim/copy/validate fields already in payload; it must not decide that a named place is a sublocation from its text alone.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/routes/__tests__/worldgen.test.ts src/routes/__tests__/schemas.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>Route tests prove save-edits passes explicit hierarchy and NPC scene fields into the scaffold saver.</acceptance_criteria>
  <done>Save-edits no longer drops the Phase 75 scaffold hierarchy contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve hierarchy through World Review frontend round-trip</name>
  <files>frontend/lib/api-types.ts, frontend/lib/world-data-helpers.ts, frontend/lib/__tests__/world-data-helpers.test.ts, frontend/components/world-review/locations-section.tsx, frontend/components/world-review/__tests__/locations-section.test.tsx, frontend/components/world-review/npcs-section.tsx, frontend/components/world-review/__tests__/npcs-section.test.tsx, frontend/app/(non-game)/campaign/[id]/review/page.tsx, frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx</files>
  <behavior>
    - RED: `toEditableScaffold` preserves `kind`, `parentLocationName`, and `sceneLocationName` from world/scaffold input instead of rebuilding name-only locations/NPCs.
    - RED: World Review location editing preserves and can update `kind` plus `parentLocationName`.
    - RED: World Review NPC editing preserves and can update `sceneLocationName` separately from broad `locationName`.
    - RED: review-page regeneration for NPCs passes the full location-name namespace, including persistent sublocations, so regenerated NPCs can legally reference scenes.
    - GREEN: legacy flat review scaffolds still load, edit, and save without requiring hierarchy fields.
  </behavior>
  <action>
    Before editing frontend symbols, run GitNexus impact analysis for `toEditableScaffold`, `LocationsSection`, `NpcsSection`, and the campaign review page component.

    Write RED tests in `frontend/lib/__tests__/world-data-helpers.test.ts`, `frontend/components/world-review/__tests__/locations-section.test.tsx`, `frontend/components/world-review/__tests__/npcs-section.test.tsx`, and `frontend/app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx`.

    Then update frontend types/helpers/components so the browser does not flatten Phase 75 scaffold fields before `saveWorldEdits`:
    - `ScaffoldLocation` includes optional `kind` and `parentLocationName`.
    - `ScaffoldNpc` includes optional `sceneLocationName`.
    - `toEditableScaffold` keeps stored hierarchy and scene fields when converting existing world data into editable scaffold shape.
    - World Review location controls expose or preserve macro vs persistent sublocation kind and exact parent references.
    - World Review NPC controls expose or preserve broad `locationName` and current-scene `sceneLocationName`.
    - Regenerate-section calls for NPCs use `scaffold.locations.map(location => location.name)` after hierarchy preservation, not a macro-only list.

    Keep UI changes utilitarian and scoped; do not create a new hierarchy authoring workflow beyond preserving/editing the fields needed for save-edits correctness.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- run lib/__tests__/world-data-helpers.test.ts components/world-review/__tests__/locations-section.test.tsx components/world-review/__tests__/npcs-section.test.tsx 'app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx'</automated>
    <automated>npm --prefix frontend run lint</automated>
  </verify>
  <acceptance_criteria>Frontend review/save tests prove hierarchy and NPC scene fields survive a normal World Review round-trip.</acceptance_criteria>
  <done>World Review can no longer re-flatten generated hierarchy before save-edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/save-edits -> backend schema | User-edited scaffold payload enters backend normalization. |
| LLM scaffold -> backend contract | Generated structured output becomes typed scaffold data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-02-01 | Tampering | `saveEditsSchema` | mitigate | Zod schema accepts only constrained `kind` values and explicit nullable references. |
| T-75-02-02 | Spoofing | source/canon role meaning | mitigate | Backend copies explicit location/scene references only and does not infer source roles from strings. |
| T-75-02-03 | Repudiation | save-edits normalization | mitigate | Route tests assert exact payload handed to `saveScaffoldToDb`. |
| T-75-02-04 | Tampering | World Review editable scaffold | mitigate | Frontend round-trip tests assert hierarchy and scene fields survive review/edit/save. |
</threat_model>

<verification>
Run:
```powershell
npm --prefix backend run test -- src/routes/__tests__/schemas.test.ts src/routes/__tests__/worldgen.test.ts
npm --prefix backend run typecheck
npm --prefix frontend run test -- run lib/__tests__/world-data-helpers.test.ts components/world-review/__tests__/locations-section.test.tsx components/world-review/__tests__/npcs-section.test.tsx 'app/(non-game)/campaign/[id]/review/__tests__/page.test.tsx'
npm --prefix frontend run lint
```
</verification>

<success_criteria>
- Types and route schemas expose `kind`, `parentLocationName`, and `sceneLocationName`.
- Save-edits normalization preserves all new fields.
- World Review load/edit/regenerate/save preserves all new fields.
- Legacy flat scaffold payloads remain compatible.
- No backend code infers source/canon/franchise meaning from location or NPC names.
</success_criteria>

<output>
After completion, create `.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-02-SUMMARY.md`.
</output>
