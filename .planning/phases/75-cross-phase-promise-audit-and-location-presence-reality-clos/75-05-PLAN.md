---
phase: 75-cross-phase-promise-audit-and-location-presence-reality-clos
plan: 05
type: execute
wave: 4
depends_on:
  - "75-04"
files_modified:
  - backend/src/worldgen/starting-location.ts
  - backend/src/worldgen/__tests__/starting-location.test.ts
  - backend/src/routes/character.ts
  - backend/src/routes/campaigns.ts
  - backend/src/routes/__tests__/character.test.ts
  - backend/src/routes/__tests__/campaigns.test.ts
  - backend/src/routes/__tests__/campaigns.inventory-authority.test.ts
autonomous: true
requirements:
  - P75-R4
  - P75-R5
  - P75-R7
must_haves:
  truths:
    - "Player character save starts the player at a concrete scene scope when the selected starting location is a persistent sublocation."
    - "`/api/campaigns/:id/world.currentScene` returns the current scene and excludes sibling-sublocation NPCs under the same macro."
    - "World route tests prove player-visible API data consumes scoped generated-world persistence."
  artifacts:
    - path: "backend/src/routes/character.ts"
      provides: "Player start broad/scene placement on save-character"
      contains: "currentSceneLocationId"
    - path: "backend/src/worldgen/starting-location.ts"
      provides: "Starting-location resolver that can choose exact scoped generated locations"
      contains: "resolveStartingLocation"
    - path: "backend/src/routes/campaigns.ts"
      provides: "`/world.currentScene` scoped presence payload"
      contains: "buildWorldCurrentScene"
  key_links:
    - from: "backend/src/routes/character.ts"
      to: "backend/src/db/schema.ts"
      via: "players.currentLocationId + players.currentSceneLocationId"
      pattern: "currentSceneLocationId"
    - from: "backend/src/worldgen/starting-location.ts"
      to: "backend/src/routes/character.ts"
      via: "selected starting location name can identify a macro or persistent sublocation"
      pattern: "locationName"
    - from: "backend/src/routes/campaigns.ts"
      to: "frontend/lib/api.ts"
      via: "`currentScene` response contract consumed by Plan 75-07"
      pattern: "currentScene"
---

<objective>
Align player start and `/world.currentScene` with scoped generated locations.

Purpose: The first visible game state must place the player in a concrete sublocation and expose authoritative current-scene actors through the world API.
Output: Player start placement fix/proof and `/world.currentScene` dense-world regression tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-04-SUMMARY.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-REGRESSION-MATRIX.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-LOCATION-PRESENCE-TRACE.md
@backend/src/worldgen/starting-location.ts
@backend/src/worldgen/__tests__/starting-location.test.ts
@backend/src/routes/character.ts
@backend/src/routes/campaigns.ts
@backend/src/routes/__tests__/character.test.ts
@backend/src/routes/__tests__/campaigns.test.ts
@backend/src/routes/__tests__/campaigns.inventory-authority.test.ts
@backend/src/worldgen/__tests__/fixtures/dense-location-scaffold.ts
</context>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Dense worlds distribute NPC/player scene presence | Player starts in broad macro plus concrete scene id |
| REQ | P75-R4 | `/world.currentScene` consumes NPC scene placement |
| REQ | P75-R5 | Opening state uses scoped player/NPC presence data |
| REQ | P75-R7 | Dense API tests prove sibling NPCs under same macro are excluded |
| RESEARCH | Starting-location resolution currently receives flat location candidates | Updates starting-location candidate shape/tests so a generated persistent sublocation can be selected and later persisted as broad+scene |
| RESEARCH | Character save currently sets broad and scene to same matched id | Fixes or proves this route with scoped rows |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve starting location and player start to broad plus scene ids</name>
  <files>backend/src/worldgen/starting-location.ts, backend/src/worldgen/__tests__/starting-location.test.ts, backend/src/routes/character.ts, backend/src/routes/__tests__/character.test.ts</files>
  <behavior>
    - RED: starting-location resolver candidates include `kind` and `parentLocationId`, and the model-facing contract can select a persistent sublocation by exact location name.
    - RED: `resolveDraftLocation` receives and preserves `kind` plus `parentLocationId` on all candidate locations used by save-character matching.
    - RED: saving a player whose selected/matched location is a persistent sublocation writes `players.currentLocationId` to the parent macro id and `players.currentSceneLocationId` to the sublocation id.
    - RED: saving a player at a macro writes both broad and scene ids to the macro id, preserving compatibility.
    - RED: unresolved parent ids return a route error instead of silently starting at the macro.
    - GREEN: existing starting-location resolution, save-character draft reconciliation, start condition effects, and inventory insert behavior remain intact.
  </behavior>
  <action>
    Do not create a worktree. Before editing production symbols, run GitNexus impact analysis for `resolveStartingLocation`, `resolveDraftLocation`, and the `/save-character` plus `/resolve-starting-location` route handlers in `backend/src/routes/character.ts`.

    Write RED tests in `backend/src/worldgen/__tests__/starting-location.test.ts` and `backend/src/routes/__tests__/character.test.ts`. Then update `backend/src/worldgen/starting-location.ts` and `backend/src/routes/character.ts`:
    - Include `kind` and `parentLocationId` when building starting-location resolver candidates and when querying campaign locations for save-character.
    - Preserve the selected resolver location name as an exact stored macro or persistent sublocation name; do not collapse a selected sublocation to its parent before save-character.
    - Introduce or update a small deterministic placement helper local to this route: macro match => broad=macro and scene=macro; persistent_sublocation match => broad=parent macro and scene=sublocation.
    - Use the resolved broad id for `players.currentLocationId` and the resolved scene id for `players.currentSceneLocationId`.
    - Keep `draft.socialContext.currentLocationName` as the exact matched stored location name selected by the player/model, including persistent sublocations. Durable DB columns remain authoritative for broad-vs-scene separation. Do not invent source/canon semantics.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/worldgen/__tests__/starting-location.test.ts src/routes/__tests__/character.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>Starting-location and character route tests prove macro and sublocation starts persist correct broad/scene ids and legacy behavior remains compatible.</acceptance_criteria>
  <done>Player start placement no longer collapses a selected persistent sublocation into a macro scene.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock `/world.currentScene` dense-location contract</name>
  <files>backend/src/routes/campaigns.ts, backend/src/routes/__tests__/campaigns.test.ts, backend/src/routes/__tests__/campaigns.inventory-authority.test.ts</files>
  <behavior>
    - RED: `/api/campaigns/:id/world` returns `currentScene.id` as the player's sublocation scene id.
    - RED: `currentScene.broadLocationId` remains the parent macro id.
    - RED: `currentScene.clearNpcIds` includes same-scene NPCs and excludes sibling-sublocation NPCs sharing the same macro.
    - GREEN: broad-only legacy rows still produce compatible currentScene payloads.
  </behavior>
  <action>
    Before editing production symbols, run GitNexus impact analysis for `buildWorldCurrentScene`, `toWorldSceneScopeId`, `buildWorldNpcPayload`, and the `GET /:id/world` route handler.

    Write RED route tests using dense macro/sublocation rows. If current code already passes, keep production changes minimal and retain tests as proof. If it fails, update `backend/src/routes/campaigns.ts` so `/world.currentScene` resolves scene scope from player broad/scene ids and existing `resolveScenePresence`, not a separate custom filter.

    The API contract must expose enough data for frontend People Here to use authoritative scene ids: `id`, `broadLocationId`, `sceneNpcIds`, `clearNpcIds`, and `awareness.byNpcId`.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/routes/__tests__/campaigns.test.ts src/routes/__tests__/campaigns.inventory-authority.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>`/world.currentScene` route tests prove same-scene NPC inclusion and same-macro sibling exclusion.</acceptance_criteria>
  <done>World API exposes authoritative scoped current scene for frontend/runtime consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| character save request -> player row | User-provided/imported character draft selects a campaign location. |
| SQLite state -> world API | Durable player/NPC scene ids become frontend-visible roster data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-05-01 | Tampering | player start placement | mitigate | Route tests assert parent resolution for persistent sublocations and error on broken parents. |
| T-75-05-02 | Information Disclosure | `/world.currentScene` | mitigate | API exposes only scoped presence already visible through current scene awareness. |
| T-75-05-03 | Spoofing | NPC presence | mitigate | Presence comes from stored ids and `resolveScenePresence`, not client or name inference. |
</threat_model>

<verification>
Run:
```powershell
npm --prefix backend run test -- src/worldgen/__tests__/starting-location.test.ts src/routes/__tests__/character.test.ts src/routes/__tests__/campaigns.test.ts src/routes/__tests__/campaigns.inventory-authority.test.ts
npm --prefix backend run typecheck
```
</verification>

<success_criteria>
- Starting-location resolution and player save write correct broad and scene ids for macro and persistent sublocation starts.
- `/world.currentScene` returns scoped actor ids and excludes same-macro sibling NPCs.
- Runtime/route code uses existing presence resolver where filtering is needed.
</success_criteria>

<output>
After completion, create `.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-05-SUMMARY.md`.
</output>
