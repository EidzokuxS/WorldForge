---
phase: 75-cross-phase-promise-audit-and-location-presence-reality-clos
plan: 06
type: execute
wave: 4
depends_on:
  - "75-04"
files_modified:
  - backend/src/engine/scene-frame.ts
  - backend/src/engine/prompt-assembler.ts
  - backend/src/engine/__tests__/scene-frame.test.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
autonomous: true
requirements:
  - P75-R5
  - P75-R7
must_haves:
  truths:
    - "SceneFrame active/support/background rosters use stored broad and scene scope, so sibling sublocation NPCs are not active in the player's scene."
    - "Prompt assembly scoped scene context includes present same-scene actor context and does not leak every broad-location NPC into the local scene."
    - "Turn/opening consumers prove the persistence fix reaches runtime prompt inputs."
  artifacts:
    - path: "backend/src/engine/scene-frame.ts"
      provides: "Canonical scene roster construction using scoped presence"
      contains: "buildSceneFrame"
    - path: "backend/src/engine/prompt-assembler.ts"
      provides: "Prompt scene/encounter sections using scoped broad plus scene presence"
      contains: "buildSceneSection"
  key_links:
    - from: "backend/src/engine/scene-frame.ts"
      to: "backend/src/engine/scene-presence.ts"
      via: "resolveScenePresence"
      pattern: "resolveScenePresence"
    - from: "backend/src/engine/prompt-assembler.ts"
      to: "backend/src/db/schema.ts"
      via: "npcs.currentLocationId + npcs.currentSceneLocationId"
      pattern: "currentSceneLocationId"
---

<objective>
Prove turn/opening runtime consumers use scoped location presence.

Purpose: Dense generated data must affect actual scene frame and prompt context, not stop at saved rows or API payloads.
Output: SceneFrame and prompt assembler regressions, plus implementation changes where current scoped data is not consumed.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-04-SUMMARY.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-REGRESSION-MATRIX.md
@.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-LOCATION-PRESENCE-TRACE.md
@backend/src/engine/scene-frame.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/scene-presence.ts
@backend/src/engine/__tests__/scene-frame.test.ts
@backend/src/engine/__tests__/prompt-assembler.test.ts
@backend/src/worldgen/__tests__/fixtures/dense-location-scaffold.ts
</context>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Dense generated worlds distribute scene participation | Runtime turn/opening consumers use scoped presence |
| REQ | P75-R5 | SceneFrame and prompt assembly consume `currentSceneLocationId` |
| REQ | P75-R7 | Dense fixture tests guard against all broad-location NPC leakage |
| RESEARCH | Prompt assembler queries NPC equipment by scene id as broad id | Adds RED/GREEN prompt assembler coverage and fix |
| REVIEW | `buildSceneSection` currently treats sublocation scene id as `npcs.currentLocationId` for equipment lookup | Tests the exact bug: NPC broad id is macro, NPC scene id is current sublocation, and equipment must still appear only for present same-scene NPCs |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock SceneFrame scoped roster behavior</name>
  <files>backend/src/engine/scene-frame.ts, backend/src/engine/__tests__/scene-frame.test.ts</files>
  <behavior>
    - RED: player in sublocation A sees same-scene clear NPCs in active roster.
    - RED: NPC in sibling sublocation B under the same macro is not active/support unless presence rules make it a hint.
    - RED: broad-only legacy player/NPC rows keep existing fallback behavior.
    - GREEN: SceneFrame keeps `resolveScenePresence` as canonical presence resolver.
  </behavior>
  <action>
    Do not create a worktree. Before editing production symbols, run GitNexus impact analysis for `buildSceneFrame`, `buildRoster`, and any helper touched in `backend/src/engine/scene-frame.ts`.

    Write RED tests in `backend/src/engine/__tests__/scene-frame.test.ts` with dense macro/sublocation rows. If current code already passes, retain the tests and avoid production churn. If it fails, update SceneFrame to pass broad and scene scope into `resolveScenePresence` consistently and classify active/support/background from that snapshot.

    Do not add a second independent presence algorithm. Do not infer source/canon/source-role meaning from location names.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/engine/__tests__/scene-frame.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>SceneFrame tests prove scoped scene rosters and legacy fallback behavior.</acceptance_criteria>
  <done>SceneFrame consumes scoped player/NPC rows for opening/turn scene participation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scope prompt assembler scene context</name>
  <files>backend/src/engine/prompt-assembler.ts, backend/src/engine/__tests__/prompt-assembler.test.ts</files>
  <behavior>
    - RED: prompt scene/encounter context for a sublocation includes same-scene clear NPC context.
    - RED: `buildSceneSection` equipment lookup includes a present NPC whose `currentLocationId` is the parent macro and `currentSceneLocationId` is the current sublocation.
    - RED: prompt scene/encounter context excludes sibling-sublocation NPC names/equipment from local scene context.
    - RED: broad-only legacy prompts remain compatible.
    - GREEN: prompt assembler reuses the scoped presence snapshot or broad+scene pair rather than broad-only `currentLocationId == sceneId` filtering.
  </behavior>
  <action>
    Before editing production symbols, run GitNexus impact analysis for `buildSceneSection`, `buildEncounterPromptContext`, `buildEncounterScopeSection`, and `assemblePrompt`.

    Write RED tests in `backend/src/engine/__tests__/prompt-assembler.test.ts` for the exact existing failure mode: `buildSceneSection` receives a sublocation scene id, the present NPC row has `currentLocationId` equal to the parent macro and `currentSceneLocationId` equal to that sublocation, and the prompt must include that NPC's local context/equipment while excluding sibling-scene NPC equipment. Then update `backend/src/engine/prompt-assembler.ts` so sublocation scene context can see NPCs whose broad id is the macro and scene id is the sublocation.

    Keep `buildSceneSection` focused on current scene facts: current scene row, items in that scene, connected paths, recent happenings for that scene, and NPC equipment for present same-scene actors. Do not leak all NPCs sharing the broad macro into local scene lines. Use the existing presence snapshot from `buildEncounterPromptContext` where possible.
  </action>
  <verify>
    <automated>npm --prefix backend run test -- src/engine/__tests__/prompt-assembler.test.ts src/engine/__tests__/scene-frame.test.ts</automated>
    <automated>npm --prefix backend run typecheck</automated>
  </verify>
  <acceptance_criteria>Prompt assembler tests prove scoped actor/equipment context and no all-broad NPC leakage.</acceptance_criteria>
  <done>Opening/turn prompt inputs reflect scoped generated-world presence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite state -> runtime frame | Stored actor rows become active/support/background runtime facts. |
| runtime frame -> LLM prompt | Scoped actor facts enter model-facing prompt context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-06-01 | Information Disclosure | prompt scene context | mitigate | Tests exclude sibling-sublocation NPC context from local scene prompt sections. |
| T-75-06-02 | Tampering | SceneFrame roster | mitigate | Use `resolveScenePresence` snapshot instead of ad hoc broad-only filtering. |
| T-75-06-03 | Spoofing | actor presence | mitigate | Presence is derived from stored ids, not character/location names. |
</threat_model>

<verification>
Run:
```powershell
npm --prefix backend run test -- src/engine/__tests__/scene-frame.test.ts src/engine/__tests__/prompt-assembler.test.ts
npm --prefix backend run typecheck
```
</verification>

<success_criteria>
- SceneFrame and prompt assembler tests prove dense sublocation scoping.
- Prompt context does not treat one macro location as one local room.
- Existing broad-only compatibility behavior remains covered.
</success_criteria>

<output>
After completion, create `.planning/phases/75-cross-phase-promise-audit-and-location-presence-reality-clos/75-06-SUMMARY.md`.
</output>
