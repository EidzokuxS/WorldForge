---
phase: 38-authoritative-inventory-equipment-state
plan: 03
type: execute
wave: 3
depends_on:
  - 38-02
files_modified:
  - frontend/lib/api-types.ts
  - frontend/lib/api.ts
  - frontend/app/game/page.tsx
  - frontend/components/game/character-panel.tsx
  - frontend/lib/__tests__/api.inventory-authority.test.ts
  - frontend/app/game/__tests__/page.test.tsx
  - frontend/components/game/__tests__/character-panel.test.tsx
autonomous: true
requirements:
  - RINT-04
must_haves:
  truths:
    - "The `/game` UI renders carried and equipped items from backend-derived authoritative payloads, not from a client-side merge of legacy fields."
    - "Reloaded gameplay shows the same equipment and inventory truth the backend world route emits."
    - "Character panel equipment and carried inventory stay aligned because they are fed from the same parsed payload contract."
  artifacts:
    - path: "frontend/lib/api.ts"
      provides: "Parsed world-data contract for authoritative player inventory/equipment"
    - path: "frontend/app/game/page.tsx"
      provides: "Gameplay page consumption of backend-derived inventory/equipment arrays"
    - path: "frontend/components/game/character-panel.tsx"
      provides: "Authoritative character equipment and inventory rendering"
  key_links:
    - from: "frontend/lib/api.ts"
      to: "backend/src/routes/campaigns.ts"
      via: "world payload parser for authoritative inventory/equipment arrays"
      pattern: "parseWorldData"
    - from: "frontend/app/game/page.tsx"
      to: "frontend/lib/api.ts"
      via: "page uses parsed player inventory/equipment instead of filtering raw world items for authority"
      pattern: "playerInventory|playerEquipment|worldData\\.player"
    - from: "frontend/components/game/character-panel.tsx"
      to: "frontend/app/game/page.tsx"
      via: "panel receives authoritative equipped and carried item arrays"
      pattern: "CharacterPanel"
---

<objective>
Switch frontend parsing and `/game` rendering to the repaired backend inventory/equipment contract so the player-facing UI reflects the same authoritative runtime truth used by gameplay and prompts.

Purpose: Close RINT-04 end-to-end by removing client-side authority drift after the backend payload is repaired.
Output: Updated frontend world types/parser, `/game` consumption of authoritative inventory/equipment arrays, and UI regressions for reload-safe rendering.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-authoritative-inventory-equipment-state/38-CONTEXT.md
@.planning/phases/38-authoritative-inventory-equipment-state/38-RESEARCH.md
@.planning/phases/38-authoritative-inventory-equipment-state/38-02-PLAN.md
@backend/src/routes/campaigns.ts
@frontend/lib/api-types.ts
@frontend/lib/api.ts
@frontend/app/game/page.tsx
@frontend/components/game/character-panel.tsx

<interfaces>
From frontend/components/game/character-panel.tsx:
```typescript
interface CharacterPanelProps {
  player: {
    // ...
    equippedItems: string[];
  } | null;
  items: Array<{ id: string; name: string; tags: string[] }>;
}
```

From frontend/app/game/page.tsx:
```typescript
const playerItems = useMemo(() => {
  if (!worldData || !player) return [];
  return worldData.items
    .filter((item) => item.ownerId === player.id)
    .map((item) => ({ id: item.id, name: item.name, tags: item.tags }));
}, [worldData, player]);
```

From frontend/lib/api-types.ts:
```typescript
export interface WorldData {
  items: Array<{
    id: string;
    name: string;
    tags: string[];
    ownerId: string | null;
    locationId: string | null;
  }>;
  player: {
    equippedItems: string[];
  } | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock frontend parsing and rendering against the authoritative world contract</name>
  <read_first>
    .planning/phases/38-authoritative-inventory-equipment-state/38-CONTEXT.md
    .planning/phases/38-authoritative-inventory-equipment-state/38-RESEARCH.md
    backend/src/routes/campaigns.ts
    frontend/lib/api-types.ts
    frontend/lib/api.ts
    frontend/app/game/page.tsx
    frontend/components/game/character-panel.tsx
    frontend/lib/__tests__/api.test.ts
    frontend/app/game/__tests__/page.test.tsx
    frontend/components/game/__tests__/character-panel.test.tsx
  </read_first>
  <files>frontend/lib/__tests__/api.inventory-authority.test.ts, frontend/app/game/__tests__/page.test.tsx, frontend/components/game/__tests__/character-panel.test.tsx</files>
  <behavior>
    - Test 1: `parseWorldData()` preserves backend-derived authoritative player inventory/equipment arrays and does not rely on `player.equippedItems` as the UI authority.
    - Test 2: `/game` uses authoritative player inventory/equipment arrays from parsed world data instead of deriving truth by filtering `world.items` and merging legacy player fields.
    - Test 3: CharacterPanel renders equipped and carried items from the authoritative props it receives, with empty states still working.
  </behavior>
  <action>Create failing frontend regressions that mirror the repaired backend contract from `38-02`. Keep tests focused on parser behavior, `/game` data flow, and panel rendering. Do not encode a client-side merge fallback from legacy `player.equippedItems`; the point of this plan is to remove that authority split.</action>
  <acceptance_criteria>
    - At least one parser test fails because the current frontend type/parser contract does not include authoritative inventory/equipment arrays.
    - At least one `/game` test fails because the page still derives inventory/equipment authority from raw world items plus legacy player fields.
    - At least one CharacterPanel test fails because its props are still centered on legacy `equippedItems: string[]`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run lib/__tests__/api.inventory-authority.test.ts app/game/__tests__/page.test.tsx components/game/__tests__/character-panel.test.tsx</automated>
  </verify>
  <done>Frontend regressions fail against the old mixed authority model and pin the new backend-driven contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire frontend world parsing and `/game` rendering to authoritative inventory/equipment payloads</name>
  <read_first>
    .planning/phases/38-authoritative-inventory-equipment-state/38-CONTEXT.md
    .planning/phases/38-authoritative-inventory-equipment-state/38-RESEARCH.md
    backend/src/routes/campaigns.ts
    frontend/lib/api-types.ts
    frontend/lib/api.ts
    frontend/app/game/page.tsx
    frontend/components/game/character-panel.tsx
    frontend/lib/__tests__/api.inventory-authority.test.ts
    frontend/app/game/__tests__/page.test.tsx
    frontend/components/game/__tests__/character-panel.test.tsx
  </read_first>
  <files>frontend/lib/api-types.ts, frontend/lib/api.ts, frontend/app/game/page.tsx, frontend/components/game/character-panel.tsx</files>
  <action>Extend the frontend world contract to parse the authoritative player inventory/equipment arrays emitted by `38-02`, including any structured item metadata required for equipped/signature display. Update `/game` to consume those arrays directly for player inventory/equipment rendering while continuing to use top-level `world.items` only for location-scene item lists. Update `CharacterPanel` props to accept authoritative equipped and carried item collections rather than a legacy `equippedItems: string[]` authority. Preserve existing empty states and reload behavior, but do not add a client-side authority merge from `player.equippedItems` or `characterRecord.loadout`.</action>
  <acceptance_criteria>
    - `frontend/lib/api.ts` and `frontend/lib/api-types.ts` model the authoritative payload shape from `38-02`.
    - `/game` no longer computes player authority by filtering top-level `world.items` and cross-checking legacy player fields.
    - CharacterPanel renders equipment and carried inventory from authoritative props and all targeted frontend tests pass.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run lib/__tests__/api.inventory-authority.test.ts app/game/__tests__/page.test.tsx components/game/__tests__/character-panel.test.tsx</automated>
  </verify>
  <done>The frontend displays the same authoritative inventory/equipment truth the backend world route emits, with no legacy authority split left in `/game`.</done>
</task>

</tasks>

<verification>
Run the targeted frontend suite and inspect the `/game` data flow in the tests to confirm that equipment and inventory come from one parsed backend contract rather than client-side reconstruction.
</verification>

<success_criteria>
Player-facing inventory/equipment UI is now backend-authoritative: reload-safe, aligned with prompt/world reads, and no longer dependent on legacy `equippedItems` or `loadout` as runtime truth.
</success_criteria>

<output>
After completion, create `.planning/phases/38-authoritative-inventory-equipment-state/38-03-SUMMARY.md`
</output>
