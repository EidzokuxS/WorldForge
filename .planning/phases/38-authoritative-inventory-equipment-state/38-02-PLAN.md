---
phase: 38-authoritative-inventory-equipment-state
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - backend/src/inventory/authority.ts
  - backend/src/engine/tool-schemas.ts
  - backend/src/engine/tool-executor.ts
  - backend/src/engine/turn-processor.ts
  - backend/src/character/record-adapters.ts
  - backend/src/engine/prompt-assembler.ts
  - backend/src/routes/campaigns.ts
  - backend/src/engine/__tests__/tool-schemas.inventory-authority.test.ts
  - backend/src/engine/__tests__/tool-executor.test.ts
  - backend/src/engine/__tests__/turn-processor.inventory-authority.test.ts
  - backend/src/character/__tests__/record-adapters.test.ts
  - backend/src/engine/__tests__/prompt-assembler.inventory-authority.test.ts
  - backend/src/routes/__tests__/campaigns.inventory-authority.test.ts
autonomous: true
requirements:
  - RINT-04
must_haves:
  truths:
    - "Picking up, dropping, equipping, and unequipping items all flow through `transfer_item` and mutate authoritative item rows directly, without reviving legacy inventory/equipment authority."
    - "Storyteller runtime can reach authoritative equip-state changes through the live `transfer_item` tool seam exposed by `createStorytellerTools()` during `processTurn()`."
    - "Prompt assembly reads equipped, signature, and carried state from authoritative item rows only after campaign start."
    - "World payloads and compatibility projections are mechanically derived from the same backend authority seam."
    - "Legacy `equippedItems` remains, if at all, as a one-way projection instead of a live backend read or write source."
  artifacts:
    - path: "backend/src/inventory/authority.ts"
      provides: "Shared authoritative inventory/equipment resolver reused by backend readers"
    - path: "backend/src/engine/tool-schemas.ts"
      provides: "Storyteller-facing `transfer_item` schema that can express pickup/drop/equip/unequip without new tool names"
    - path: "backend/src/engine/tool-executor.ts"
      provides: "Gameplay `transfer_item` mutation writes that update authoritative item-row equipment state"
    - path: "backend/src/engine/turn-processor.ts"
      provides: "Live storyteller invocation seam that passes the revised `transfer_item` tool bag through `processTurn()`"
    - path: "backend/src/engine/prompt-assembler.ts"
      provides: "Prompt player-state assembly without post-start legacy fallback"
    - path: "backend/src/routes/campaigns.ts"
      provides: "World payload with backend-derived authoritative player inventory/equipment projections"
  key_links:
    - from: "backend/src/engine/tool-schemas.ts"
      to: "backend/src/engine/tool-executor.ts"
      via: "revised `transfer_item` execute callback forwards structured carry/equip semantics without new tool names"
      pattern: "transfer_item"
    - from: "backend/src/engine/turn-processor.ts"
      to: "backend/src/engine/tool-schemas.ts"
      via: "processTurn passes the revised storyteller tool bag into `streamText()` for live tool reachability"
      pattern: "createStorytellerTools"
    - from: "backend/src/engine/tool-executor.ts"
      to: "backend/src/inventory/authority.ts"
      via: "`transfer_item` pickup/drop/equip/unequip semantics preserve the same item-row contract later readers consume"
      pattern: "transfer_item"
    - from: "backend/src/engine/prompt-assembler.ts"
      to: "backend/src/inventory/authority.ts"
      via: "shared authoritative resolver for player-state lines"
      pattern: "load.*Inventory|inventoryAuthority"
    - from: "backend/src/routes/campaigns.ts"
      to: "backend/src/inventory/authority.ts"
      via: "world payload player inventory/equipment projection"
      pattern: "inventoryItems|equippedItems|signatureItems"
    - from: "backend/src/character/record-adapters.ts"
      to: "backend/src/inventory/authority.ts"
      via: "compatibility projection derived from authoritative items"
      pattern: "toLegacyPlayerCharacter|projectPlayerRecord"
---

<objective>
Converge backend runtime writes and reads on the shared authoritative inventory/equipment seam so gameplay mutations, prompts, world payloads, and legacy compatibility outputs stop depending on `loadout` or `equippedItems` as live authority.

Purpose: Implement D-03, D-07, D-08, D-09, and D-11 on the backend before the frontend consumes the repaired contract, while keeping Phase 38 on the locked direction of extending `transfer_item` instead of adding new `equip` / `unequip` tools.
Output: Shared backend mutation/read semantics for `transfer_item`, prompt/world/projection surfaces, and regression coverage proving no post-start fallback, parallel-write authority, or unreachable storyteller seam survives.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-authoritative-inventory-equipment-state/38-CONTEXT.md
@.planning/phases/38-authoritative-inventory-equipment-state/38-RESEARCH.md
@.planning/phases/38-authoritative-inventory-equipment-state/38-01-PLAN.md
@backend/src/inventory/authority.ts
@backend/src/engine/tool-schemas.ts
@backend/src/engine/tool-executor.ts
@backend/src/engine/turn-processor.ts
@backend/src/character/record-adapters.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/routes/campaigns.ts

<interfaces>
From backend/src/engine/tool-executor.ts:
```typescript
db.update(items)
  .set({ ownerId: character.id, locationId: null })
  .where(eq(items.id, item.id))
  .run();
```

From backend/src/engine/tool-schemas.ts:
```typescript
transfer_item: tool({
  description:
    "Transfer an existing item to a different character or location. Clears previous ownership.",
  inputSchema: z.object({
    itemName: z.string(),
    targetName: z.string(),
    targetType: z.enum(["character", "location"]),
  }),
  execute: async (args) =>
    executeToolCall(campaignId, "transfer_item", args, tick),
}),
```

From backend/src/engine/turn-processor.ts:
```typescript
const tools = createStorytellerTools(campaignId, currentTick, oracleResult.outcome);
const result = streamText({
  model,
  system: systemPrompt,
  messages: storyMessages,
  tools,
});
```

From backend/src/character/record-adapters.ts:
```typescript
export function toLegacyPlayerCharacter(record: CharacterRecord): PlayerCharacter {
  return {
    // ...
    equippedItems: record.loadout.equippedItemRefs,
  };
}

export function projectPlayerRecord(record: CharacterRecord): PlayerRecordProjection {
  return {
    equippedItems: JSON.stringify(legacy.equippedItems),
    characterRecord: JSON.stringify(record),
  };
}
```

From backend/src/routes/campaigns.ts:
```typescript
return c.json({
  player: {
    ...playerRow,
    characterRecord: playerRecord,
    draft: playerDraft,
    character: toLegacyPlayerCharacter(playerRecord),
  },
  items: worldItems,
});
```

From backend/src/engine/prompt-assembler.ts:
```typescript
const equipped = effectivePlayerRecord.loadout.equippedItemRefs;
const signatureItems = effectivePlayerRecord.loadout.signatureItems;
const fallbackInventory = playerRecord.loadout.inventorySeed;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock `transfer_item` runtime reachability and authoritative mutation semantics</name>
  <read_first>
    .planning/phases/38-authoritative-inventory-equipment-state/38-CONTEXT.md
    .planning/phases/38-authoritative-inventory-equipment-state/38-RESEARCH.md
    backend/src/engine/tool-schemas.ts
    backend/src/inventory/authority.ts
    backend/src/engine/tool-executor.ts
    backend/src/engine/turn-processor.ts
    backend/src/engine/__tests__/tool-executor.test.ts
    backend/src/engine/__tests__/turn-processor.test.ts
  </read_first>
  <files>backend/src/engine/__tests__/tool-schemas.inventory-authority.test.ts, backend/src/engine/__tests__/tool-executor.test.ts, backend/src/engine/__tests__/turn-processor.inventory-authority.test.ts</files>
  <behavior>
    - Test 1: `createStorytellerTools()` still exposes exactly one item-state mutation tool name, `transfer_item`, and its schema now accepts structured arguments that express pickup/drop/equip/unequip without adding new `equip` / `unequip` tool names.
    - Test 2: `transfer_item` updates authoritative item ownership plus carried/equipped metadata on the same item row, without consulting `players.equippedItems` or `characterRecord.loadout`, for all four semantics: pickup, drop, equip, and unequip.
    - Test 3: A `processTurn()` integration test uses the live storyteller tool bag to invoke `transfer_item.execute(...)` during a mocked stream so Phase 38 proves runtime reachability beyond direct executor calls.
    - Test 4: Any retained compatibility projection is derived after the authoritative mutation, not written as a second state the next turn depends on.
  </behavior>
  <action>Create failing backend regressions for D-04, D-05, D-07, and D-08 plus the locked replan direction: do **not** add new `equip` / `unequip` tool names. Extend `transfer_item` in the tests to carry structured item-state arguments such as `equipState: "carried" | "equipped"` and an optional explicit slot field for character targets; location targets must clear equipped state. Use the shared authority module from `38-01` when asserting post-mutation state. For the live reachability proof, build a `processTurn()` test that lets the mocked `streamText()` call the actual `tools.transfer_item.execute()` callback and then asserts on the emitted `state_update` plus authoritative post-call reads. Do not accept tests that only prove direct `executeToolCall()` success.</action>
  <acceptance_criteria>
    - At least one tool-schema test fails because `transfer_item` does not yet expose structured equip-state arguments or because separate `equip` / `unequip` tool names would violate the locked direction.
    - At least one tool-executor test fails because current `transfer_item` behavior does not yet mutate the structured authoritative item-row fields required by `38-01` for pickup/drop/equip/unequip.
    - At least one `processTurn()` test fails because the current storyteller runtime seam cannot yet prove live reachability of the revised `transfer_item` semantics.
    - The failing assertions demonstrate post-mutation authoritative reads, not only tool response payloads.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/tool-schemas.inventory-authority.test.ts src/engine/__tests__/tool-executor.test.ts src/engine/__tests__/turn-processor.inventory-authority.test.ts</automated>
  </verify>
  <done>Runtime item-mutation regressions fail against the old authority seam, make `transfer_item` the only pickup/drop/equip/unequip tool path, and prove the storyteller runtime seam must reach it live.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock backend reader convergence on the authoritative inventory seam</name>
  <read_first>
    .planning/phases/38-authoritative-inventory-equipment-state/38-CONTEXT.md
    .planning/phases/38-authoritative-inventory-equipment-state/38-RESEARCH.md
    backend/src/inventory/authority.ts
    backend/src/character/record-adapters.ts
    backend/src/engine/prompt-assembler.ts
    backend/src/routes/campaigns.ts
    backend/src/engine/__tests__/prompt-assembler.test.ts
    backend/src/routes/__tests__/campaigns.test.ts
    backend/src/character/__tests__/record-adapters.test.ts
  </read_first>
  <files>backend/src/character/__tests__/record-adapters.test.ts, backend/src/engine/__tests__/prompt-assembler.inventory-authority.test.ts, backend/src/routes/__tests__/campaigns.inventory-authority.test.ts</files>
  <behavior>
    - Test 1: Prompt assembly uses authoritative equipped/signature/carried projections and never rehydrates post-start runtime inventory from `inventorySeed` or `players.equippedItems`, including after `transfer_item`-driven equip/unequip changes.
    - Test 2: `/api/campaigns/:id/world` returns backend-derived player inventory/equipment arrays from the authority module while preserving top-level world items for scene/location rendering, and those arrays reflect post-`transfer_item` pickup/drop/equip/unequip state.
    - Test 3: Any retained legacy `equippedItems` projection is derived from authoritative items, not from `characterRecord.loadout`.
  </behavior>
  <action>Create failing backend regressions for D-03, D-07, D-09, and D-11. Use the authority module introduced by `38-01` as the source seam and seed test fixtures with post-`transfer_item` pickup/drop/equip/unequip row states from Task 1 so success criterion 2 is provably owned end-to-end. Cover prompt assembly, world payload shaping, and compatibility projection logic separately so regressions cannot hide behind one consumer passing while another still reads legacy fields.</action>
  <acceptance_criteria>
    - At least one prompt-assembler test fails because current code still falls back to `inventorySeed` or `equippedItemRefs`.
    - At least one world-route test fails because the route does not yet expose backend-derived authoritative player inventory/equipment projections that reflect post-`transfer_item` equip-state changes.
    - At least one adapter test fails because compatibility output is still sourced from `record.loadout` rather than the authority seam.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix backend exec vitest run src/character/__tests__/record-adapters.test.ts src/engine/__tests__/prompt-assembler.inventory-authority.test.ts src/routes/__tests__/campaigns.inventory-authority.test.ts</automated>
  </verify>
  <done>Focused backend reader regressions fail against the old mixed-read behavior and pin the new authoritative contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewire runtime writes, prompt reads, and compatibility outputs to authoritative items only</name>
  <read_first>
    .planning/phases/38-authoritative-inventory-equipment-state/38-CONTEXT.md
    .planning/phases/38-authoritative-inventory-equipment-state/38-RESEARCH.md
    backend/src/inventory/authority.ts
    backend/src/engine/tool-schemas.ts
    backend/src/engine/tool-executor.ts
    backend/src/engine/turn-processor.ts
    backend/src/character/record-adapters.ts
    backend/src/engine/prompt-assembler.ts
    backend/src/routes/campaigns.ts
    backend/src/engine/__tests__/tool-schemas.inventory-authority.test.ts
    backend/src/engine/__tests__/tool-executor.test.ts
    backend/src/engine/__tests__/turn-processor.inventory-authority.test.ts
    backend/src/character/__tests__/record-adapters.test.ts
    backend/src/engine/__tests__/prompt-assembler.inventory-authority.test.ts
    backend/src/routes/__tests__/campaigns.inventory-authority.test.ts
  </read_first>
  <files>backend/src/inventory/authority.ts, backend/src/engine/tool-schemas.ts, backend/src/engine/tool-executor.ts, backend/src/engine/turn-processor.ts, backend/src/character/record-adapters.ts, backend/src/engine/prompt-assembler.ts, backend/src/routes/campaigns.ts</files>
  <action>Finish the backend convergence. In `tool-schemas.ts`, extend the existing `transfer_item` definition so character targets can declare structured carry/equip intent, such as `equipState: "carried" | "equipped"` plus an optional explicit slot field, while location targets represent drop/place semantics by clearing equip metadata. Do **not** add new `equip` / `unequip` tool names. In `tool-executor.ts`, make `transfer_item` the single authoritative pickup/drop/equip/unequip writer per D-07 and D-08: character-target transfers set owner plus structured equip metadata on the same item row, location-target transfers clear owner and equipped state, and same-owner equip/unequip transitions mutate only that row. Keep `turn-processor.ts` on the live storyteller seam by passing the revised tool bag through `createStorytellerTools()` and preserving `transfer_item` results in streamed `state_update` output so the new semantics are reachable in normal turns. Use the shared authority module from `38-01` to derive carried, equipped, and signature views for both players and NPCs per D-06 and D-07. Remove post-start fallback inventory/equipment reads from `prompt-assembler.ts`. In `/api/campaigns/:id/world`, add backend-derived player inventory/equipment arrays as the UI-facing authority contract while keeping top-level world/location items for location rendering. If `player.equippedItems` remains temporarily, compute it from the authoritative view only and mark it as transitional. Update `record-adapters.ts` so compatibility projections no longer source live truth from `characterRecord.loadout` or legacy row fields.</action>
  <acceptance_criteria>
    - `transfer_item` is the only storyteller tool path for pickup/drop/equip/unequip semantics, and targeted tool-schema plus runtime reachability tests pass without introducing new tool names.
    - Pickup, drop, equip, and unequip all update authoritative item rows directly through `transfer_item`, and targeted tool-executor tests pass.
    - Prompt assembly can render player equipment/inventory with no dependency on `inventorySeed`, `equippedItemRefs`, or `players.equippedItems` after campaign start, including after `transfer_item` equip/unequip transitions.
    - `/world` emits authoritative player inventory/equipment payloads derived from items, and targeted backend tests pass against post-`transfer_item` state.
    - Compatibility projections are one-way outputs from the authority seam, not secondary live authorities.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/tool-schemas.inventory-authority.test.ts src/engine/__tests__/tool-executor.test.ts src/engine/__tests__/turn-processor.inventory-authority.test.ts src/character/__tests__/record-adapters.test.ts src/engine/__tests__/prompt-assembler.inventory-authority.test.ts src/routes/__tests__/campaigns.inventory-authority.test.ts</automated>
  </verify>
  <done>Every backend write and read surface covered by this plan consumes the same authoritative inventory/equipment seam, `transfer_item` is the live item-state mutation path, and the targeted regressions pass green.</done>
</task>

</tasks>

<verification>
Run the targeted backend suites and inspect matcher expectations to confirm that `transfer_item` is the only item-state mutation tool path for pickup/drop/equip/unequip, that `processTurn()` can reach it through the live storyteller tool bag, and that prompt/world/compatibility reads consume the same resolver rather than route-local or legacy merges.
</verification>

<success_criteria>
Backend gameplay writes and reads are converged: `transfer_item` now expresses pickup/drop/equip/unequip end-to-end, the live storyteller runtime can reach that seam, and prompt assembly, world serialization, and retained compatibility output all derive inventory/equipment from authoritative item rows only.
</success_criteria>

<output>
After completion, create `.planning/phases/38-authoritative-inventory-equipment-state/38-02-SUMMARY.md`
</output>
