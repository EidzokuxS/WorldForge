---
phase: 38-authoritative-inventory-equipment-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/db/schema.ts
  - backend/src/inventory/index.ts
  - backend/src/inventory/authority.ts
  - backend/src/inventory/legacy-migration.ts
  - backend/src/routes/character.ts
  - backend/src/campaign/manager.ts
  - backend/src/inventory/__tests__/inventory-authority.test.ts
  - backend/src/routes/__tests__/chat.inventory-authority.test.ts
  - backend/src/campaign/__tests__/checkpoints.test.ts
  - backend/src/engine/__tests__/state-snapshot.test.ts
autonomous: true
requirements:
  - RINT-04
must_haves:
  truths:
    - "Fresh campaigns seed equipped and carried runtime items onto authoritative item rows, not only into character projections."
    - "Legacy campaigns reopen into the same authoritative item-row contract before gameplay resumes."
    - "Checkpoint, retry, and undo bundle-reopen paths cannot silently reintroduce legacy-only inventory authority."
  artifacts:
    - path: "backend/src/inventory/authority.ts"
      provides: "Shared authoritative inventory/equipment view derived only from item rows"
    - path: "backend/src/inventory/legacy-migration.ts"
      provides: "Idempotent fail-closed legacy backfill onto authoritative items"
    - path: "backend/src/inventory/__tests__/inventory-authority.test.ts"
      provides: "Regression coverage for schema, seeding, and migration invariants"
    - path: "backend/src/campaign/__tests__/checkpoints.test.ts"
      provides: "Checkpoint reopen coverage proving restored bundles rerun the inventory authority seam"
  key_links:
    - from: "backend/src/routes/character.ts"
      to: "backend/src/db/schema.ts"
      via: "canonical loadout item inserts encode explicit equipment metadata"
      pattern: "db\\.insert\\(items\\)"
    - from: "backend/src/campaign/manager.ts"
      to: "backend/src/inventory/legacy-migration.ts"
      via: "campaign load runs idempotent inventory authority migration"
      pattern: "ensure.*Inventory.*Authority|backfill"
    - from: "backend/src/campaign/restore-bundle.ts"
      to: "backend/src/campaign/manager.ts"
      via: "bundle restore reopens campaigns through loadCampaign before gameplay resumes"
      pattern: "loadCampaign"
---

<objective>
Establish the authoritative runtime inventory/equipment contract on `items` rows and guarantee that every campaign-open or bundle-restore path enters that contract before any gameplay read occurs.

Purpose: Lock the phase foundation required by D-01 through D-12 before backend readers or frontend consumers switch.
Output: Structured item-row equipment metadata, a shared backend authority module, and idempotent legacy backfill wired into campaign load/reopen seams.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-authoritative-inventory-equipment-state/38-CONTEXT.md
@.planning/phases/38-authoritative-inventory-equipment-state/38-RESEARCH.md
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-HANDOFF.md
@.planning/phases/44-gameplay-docs-baseline-alignment/44-CLAIM-RESOLUTION.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-01-SUMMARY.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-02-SUMMARY.md
@backend/src/db/schema.ts
@backend/src/routes/character.ts
@backend/src/campaign/manager.ts
@backend/src/campaign/restore-bundle.ts
@backend/src/engine/state-snapshot.ts
@backend/src/character/record-adapters.ts

<interfaces>
From backend/src/db/schema.ts:
```typescript
export const items = sqliteTable(
  "items",
  {
    id: text("id").primaryKey(),
    campaignId: text("campaign_id").notNull(),
    name: text("name").notNull(),
    tags: text("tags").notNull().default("[]"),
    ownerId: text("owner_id"),
    locationId: text("location_id").references(() => locations.id),
  },
);
```

From backend/src/routes/character.ts:
```typescript
const canonicalLoadout = deriveCanonicalLoadout(draft);
db.insert(items).values(
  canonicalLoadout.items.map((item) => ({
    id: crypto.randomUUID(),
    campaignId,
    name: item.name,
    tags: JSON.stringify(item.tags),
    ownerId: playerId,
    locationId: null,
  })),
).run();
```

From backend/src/campaign/restore-bundle.ts:
```typescript
export async function restoreCampaignBundle(
  campaignId: string,
  bundleDir: string,
  options: { includeVectors: boolean },
): Promise<void> {
  // ...
  await loadCampaign(campaignId);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock authoritative item-row and legacy-migration regressions</name>
  <read_first>
    .planning/phases/38-authoritative-inventory-equipment-state/38-CONTEXT.md
    .planning/phases/38-authoritative-inventory-equipment-state/38-RESEARCH.md
    backend/src/db/schema.ts
    backend/src/routes/character.ts
    backend/src/campaign/manager.ts
    backend/src/campaign/restore-bundle.ts
    backend/src/engine/state-snapshot.ts
    backend/src/character/record-adapters.ts
  </read_first>
  <files>backend/src/inventory/__tests__/inventory-authority.test.ts, backend/src/routes/__tests__/chat.inventory-authority.test.ts, backend/src/campaign/__tests__/checkpoints.test.ts, backend/src/engine/__tests__/state-snapshot.test.ts</files>
  <behavior>
    - Test 1: Fresh character-save seeding writes authoritative item rows with explicit structured equipped/signature metadata derived from canonical loadout slot values.
    - Test 2: Loading a pre-phase campaign with only `characterRecord.loadout` and/or `players.equippedItems` backfills authoritative items exactly once per D-10 and rewrites any retained compatibility projection from those rows.
    - Test 3: Contradictory legacy sources fail closed per D-12 instead of silently merging.
    - Test 4: Checkpoint restore reopens a legacy bundle through the same migration seam before the restored campaign is considered ready.
    - Test 5: Retry or undo restore reopens a legacy bundle through the same migration seam before chat routes continue.
  </behavior>
  <action>Create failing backend regressions for D-01, D-04, D-10, and D-12. Cover fresh-campaign bootstrap plus every reopen seam that Phase 38 owns: direct legacy load, checkpoint restore, and retry/undo bundle restore. Use `loadCampaign()` and restore-path tests to prove the migration seam is mandatory before gameplay resumes. Do not assert on prompt tags or `players.equippedItems` as live truth; assert on authoritative `items` rows plus any derived compatibility output only.</action>
  <acceptance_criteria>
    - At least one test fails because the current schema lacks structured equipment metadata on item rows.
    - At least one test fails because `loadCampaign()` does not yet backfill legacy inventory/equipment into authoritative items.
    - At least one checkpoint-restore test fails because restored bundles do not yet rerun the inventory authority seam.
    - At least one retry/undo restore-path test proves the migration seam must run after bundle reopen, not only on fresh campaign creation.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix backend exec vitest run src/inventory/__tests__/inventory-authority.test.ts src/routes/__tests__/chat.inventory-authority.test.ts src/campaign/__tests__/checkpoints.test.ts src/engine/__tests__/state-snapshot.test.ts</automated>
  </verify>
  <done>Targeted backend regressions fail for the right reasons and describe the exact schema, reopen, checkpoint, and restore authority seam Phase 38 must implement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement item-row authority, structured equipment metadata, and fail-closed backfill</name>
  <read_first>
    .planning/phases/38-authoritative-inventory-equipment-state/38-CONTEXT.md
    .planning/phases/38-authoritative-inventory-equipment-state/38-RESEARCH.md
    backend/src/db/schema.ts
    backend/src/routes/character.ts
    backend/src/campaign/manager.ts
    backend/src/campaign/restore-bundle.ts
    backend/src/character/loadout-deriver.ts
    backend/src/character/record-adapters.ts
    backend/src/inventory/__tests__/inventory-authority.test.ts
    backend/src/routes/__tests__/chat.inventory-authority.test.ts
    backend/src/campaign/__tests__/checkpoints.test.ts
    backend/src/engine/__tests__/state-snapshot.test.ts
  </read_first>
  <files>backend/src/db/schema.ts, backend/src/inventory/index.ts, backend/src/inventory/authority.ts, backend/src/inventory/legacy-migration.ts, backend/src/routes/character.ts, backend/src/campaign/manager.ts</files>
  <action>Add explicit structured equipment metadata to `items` per D-04, D-05, and D-17. Use small queryable fields on the item row model, not free-form tags, so the shared authority module can derive carried, equipped, and signature projections for both players and NPCs. Seed those fields from canonical loadout creation in `routes/character.ts`, and run an idempotent fail-closed backfill inside `loadCampaign()` after migrations so direct campaign load, checkpoint restore, retry, and undo reopen flows all inherit the same seam via `loadCampaign()`. Keep `characterRecord.loadout` and `players.equippedItems` as provenance or compatibility output only per D-02, D-03, and D-11. Runtime pickup/drop/equip/unequip writer semantics are intentionally owned by `38-02`; this plan must only guarantee that every reopened campaign re-enters authoritative item-row state before those writers run.</action>
  <acceptance_criteria>
    - `items` rows can represent possession plus equipped/signature state without consulting `characterRecord.loadout` or `players.equippedItems`.
    - Fresh save-character seeding writes the new structured metadata from canonical loadout slots.
    - `loadCampaign()` backfills legacy campaigns once, rewrites any temporary compatibility projection from authoritative items, reruns after checkpoint/retry/undo bundle restore, and throws a repairable error when legacy sources disagree.
    - Targeted backend tests from Task 1 pass green.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix backend exec vitest run src/inventory/__tests__/inventory-authority.test.ts src/routes/__tests__/chat.inventory-authority.test.ts src/campaign/__tests__/checkpoints.test.ts src/engine/__tests__/state-snapshot.test.ts</automated>
  </verify>
  <done>Authoritative item-row inventory/equipment state exists, migration is fail-closed and idempotent, and direct load plus checkpoint/retry/undo reopen flows all route through that foundation.</done>
</task>

</tasks>

<verification>
Re-run the targeted backend authority tests after implementation. Confirm that `loadCampaign()` is the migration entry point for direct load, checkpoint restore, and retry/undo bundle reopen paths, and that restored bundles cannot bypass it.
</verification>

<success_criteria>
Phase 38 now has a real backend authority seam: runtime item rows store equipped/carry metadata explicitly, legacy campaigns upgrade into that seam on reopen, and checkpoint/retry/undo restore paths cannot reintroduce split-brain inventory state.
</success_criteria>

<output>
After completion, create `.planning/phases/38-authoritative-inventory-equipment-state/38-01-SUMMARY.md`
</output>
