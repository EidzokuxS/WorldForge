# 51-02 Plan

1. Stop trusting raw `knownIP` input as the canonical franchise name; treat it as a hint.
2. Tighten sufficiency prompting so `missingTopics` become short missing-fact needs instead of blended prose.
3. Extend typed retrieval planning to use persisted frame context while keeping search jobs concise.
4. Prove the new behavior with focused `ip-researcher` tests.

Verification:
- `ip-researcher.test.ts`
- route regression for regenerate-section sufficiency args
