# 51-01 Plan

1. Add a persisted `worldgenResearchFrame` artifact to campaign config.
2. Build it deterministically from `ipContext`, `premiseDivergence`, and chosen `seeds`.
3. Save/load it on worldgen generate/regenerate flows.
4. Thread it into the existing worldgen request/sufficiency seams.

Verification:
- campaign manager persistence tests
- worldgen route tests proving save + pass-through
