# Phase 83-01 Plan - V4 Tokens, Shell, and Navigation

Status: Complete 2026-05-05

## Goal

Install the V4 design language at the foundation: global tokens, app frame, rail, header, buttons/tabs/form surfaces, and responsive constraints.

## Tasks

- [x] Replace large rounded/orb shell styling with V4 charcoal/ember/cold/gold tokens.
- [x] Update root font variables to include display/prose/body/mono roles without breaking build.
- [x] Restyle non-game shell to a flat rail + stage layout with responsive rail behavior.
- [x] Restyle sidebar brand, groups, menu buttons, footer, and active state.
- [x] Restyle page header, shared buttons, tabs, inputs, and textareas so existing pages inherit the V4 rhythm.
- [x] Verify `AppShell`, `LauncherPage`, and focused frontend typecheck/tests.

## Verification

- `npm --prefix frontend exec vitest run components/non-game-shell/__tests__/app-shell.test.tsx app/(non-game)/__tests__/page.test.tsx`
- Screenshot compare on `/`, `/settings`, and `/campaign/new`.
