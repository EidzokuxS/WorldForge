---
phase: 87-playtest-defect-burn-down-and-final-rerun
plan: 02
type: execute
wave: 2
depends_on: [87-01]
files_modified:
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/visible-narration-output-guard.ts
  - backend/src/engine/__tests__/turn-processor.empty-narration.test.ts
  - backend/src/engine/__tests__/visible-narration-output-guard.test.ts
  - frontend/lib/api.ts
  - frontend/lib/__tests__/api.test.ts
autonomous: true
requirements: [P87-R2, P87-R4]
must_haves:
  truths:
    - "Accepted player actions cannot finish with empty assistant text."
    - "If final narration is empty, the turn fails visibly and recoverably instead of committing a fake success."
    - "Frontend SSE parsing treats done-without-narrative as an error state, not a successful empty beat."
  artifacts:
    - path: "backend/src/engine/turn-processor.ts"
      provides: "Backend finalization gate for non-empty visible narration"
      contains: "narrativeText"
    - path: "backend/src/engine/visible-narration-output-guard.ts"
      provides: "Narration output validation"
      contains: "VisibleNarrationPacketGuardError"
    - path: "frontend/lib/api.ts"
      provides: "SSE action parser"
      contains: "parseTurnSSE"
  key_links:
    - from: "backend/src/engine/turn-processor.ts"
      to: "frontend/lib/api.ts"
      via: "SSE narrative/error/done events"
      pattern: "narrative|error|done"
---

<objective>
Burn down `P86-F002`: accepted turns that settle with empty assistant narration.

Purpose: an accepted action must produce visible narration or a clear recoverable error. Silent success breaks play and corrupts rerun evidence.
Output: backend and frontend guards that make empty narration impossible to count as a completed turn.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-exhaustive-live-playtest-matrix-and-findings-ledger/86-FINDINGS.md
@.planning/phases/87-playtest-defect-burn-down-and-final-rerun/87-CONTEXT.md
@.planning/phases/87-playtest-defect-burn-down-and-final-rerun/87-ACCEPTED-FINDINGS.md
@backend/src/engine/turn-processor.ts
@backend/src/engine/visible-narration-output-guard.ts
@frontend/lib/api.ts

<interfaces>
From `backend/src/engine/turn-processor.ts`:
```typescript
export interface TurnEvent {
  type: "oracle_result" | "scene-settling" | "narrative" | "reasoning" | "state_update" | "quick_actions" | "auto_checkpoint" | "finalizing_turn" | "done" | "error";
  data: unknown;
}
```

Current risk point: `turn-processor.ts` has branches where `if (narrativeText) { append/yield narrative }` is followed by `finalizing_turn` and `done`. Those branches must not let blank narration complete as success.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail closed on empty backend narration</name>
  <files>backend/src/engine/turn-processor.ts, backend/src/engine/visible-narration-output-guard.ts, backend/src/engine/__tests__/turn-processor.empty-narration.test.ts, backend/src/engine/__tests__/visible-narration-output-guard.test.ts</files>
  <behavior>
    - When final-visible narration returns `""` or whitespace after retries, `processTurn` yields `error` and does not yield `done`.
    - No assistant message is appended for an empty final-visible turn.
    - Clarification and no-op movement branches still emit intentional non-empty text.
  </behavior>
  <action>Add focused tests reproducing `P86-F002` with mocked final narration returning empty text. Implement a shared backend guard so every final-visible branch checks trimmed text before append/finalization. Do not invent replacement narration. Do not shorten provider timeouts. The accepted result is either real non-empty narration or a visible error that allows retry.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/turn-processor.empty-narration.test.ts src/engine/__tests__/visible-narration-output-guard.test.ts</automated>
  </verify>
  <done>Backend cannot emit `done` for an accepted action unless non-empty narration was emitted first or a clear error path was taken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make frontend reject done-without-narrative streams</name>
  <files>frontend/lib/api.ts, frontend/lib/__tests__/api.test.ts</files>
  <behavior>
    - `parseTurnSSE` reports an error when a turn stream reaches `done` without any non-empty `narrative` event for an action turn.
    - Streams with backend `error` events surface the backend error text.
    - Opening/history paths that already have existing assistant text are not misclassified.
  </behavior>
  <action>Add SSE parser tests for the empty-narration route from `P86-F002`. Track whether non-empty narrative text arrived before `done`; for action streams, treat missing narration as an error callback/state instead of success. Keep existing retry/undo/edit SSE behavior intact.</action>
  <verify>
    <automated>npm --prefix frontend run test -- run lib/__tests__/api.test.ts</automated>
  </verify>
  <done>Frontend no longer records an empty accepted beat as a completed player turn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI provider -> backend | Narration text is untrusted provider output. |
| Backend SSE -> frontend | Stream ordering controls player-visible completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-02-01 | Tampering | `visible-narration-output-guard.ts` | mitigate | Validate trimmed final-visible text before persistence/finalization. |
| T-87-02-02 | Repudiation | `turn-processor.ts` | mitigate | Emit explicit error event for empty narration instead of silent `done`. |
| T-87-02-03 | Denial of Service | `parseTurnSSE` | mitigate | Preserve normal done handling when narrative exists; fail only action turns with no visible text. |
</threat_model>

<verification>
Run backend and frontend focused tests, then `npm --prefix backend run typecheck` and `npm --prefix frontend run typecheck`.
</verification>

<success_criteria>
`P86-F002` accepted-turn empty narration is fixed without fake prose fallback or tool disabling.
</success_criteria>

<output>
After completion, create `.planning/phases/87-playtest-defect-burn-down-and-final-rerun/87-02-SUMMARY.md`.
</output>
