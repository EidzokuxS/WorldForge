---
phase: 87-playtest-defect-burn-down-and-final-rerun
plan: 03
type: execute
wave: 2
depends_on: [87-01]
files_modified:
  - backend/src/engine/gm-tool-loop.ts
  - backend/src/engine/narrator-packet.ts
  - backend/src/engine/tool-schemas.ts
  - backend/src/engine/tool-execution-context.ts
  - backend/src/engine/__tests__/gm-tool-loop.test.ts
  - backend/src/engine/__tests__/narrator-packet.test.ts
  - backend/src/engine/__tests__/tool-execution-context.test.ts
autonomous: true
requirements: [P87-R2, P87-R4]
must_haves:
  truths:
    - "Concrete future-relevant pressure cannot exist only in final prose."
    - "Successful runtime tool observations remain the source of narrator-settled consequences."
    - "Low-stakes sensory description remains allowed when it does not create durable actors, props, obligations, routes, or aftermath."
  artifacts:
    - path: "backend/src/engine/gm-tool-loop.ts"
      provides: "Tool-loop state commitment pressure"
      contains: "runGmToolLoop"
    - path: "backend/src/engine/narrator-packet.ts"
      provides: "Settled consequence packet"
      contains: "buildNarratorPacket"
    - path: "backend/src/engine/tool-execution-context.ts"
      provides: "Legal refs updated from observations"
      contains: "ToolExecutionContext"
  key_links:
    - from: "backend/src/engine/gm-tool-loop.ts"
      to: "backend/src/engine/narrator-packet.ts"
      via: "successful tool observations"
      pattern: "stepResults|summarizeRuntimeToolResultForNarrator"
---

<objective>
Burn down `P86-F001`: concrete pressure or changed local facts appearing in prose without authoritative world-state trace.

Purpose: make the world reactive beyond sentence-level improvisation. If a turn creates something that matters later, backend state or accepted tool observations must carry it.
Output: tests and engine changes that bind future-relevant pressure to state-bearing tools or settled packet evidence.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-exhaustive-live-playtest-matrix-and-findings-ledger/86-FINDINGS.md
@.planning/phases/87-playtest-defect-burn-down-and-final-rerun/87-CONTEXT.md
@.planning/phases/87-playtest-defect-burn-down-and-final-rerun/87-ACCEPTED-FINDINGS.md
@backend/src/engine/gm-tool-loop.ts
@backend/src/engine/narrator-packet.ts
@backend/src/engine/tool-schemas.ts
@backend/src/engine/tool-execution-context.ts

<interfaces>
`runGmToolLoop` returns:
```typescript
export interface GmToolLoopResult {
  intent: string;
  text: string;
  reasoningText?: string;
  stepResults: GmToolStepResult[];
  rawToolCalls: CollectedToolCall[];
}
```

State-bearing existing tools include `log_event`, `reveal_location`, `move_to`, `spawn_npc`, `spawn_item`, and other successful runtime tools summarized for `NarratorPacket`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require state-bearing observations for future-relevant tool turns</name>
  <files>backend/src/engine/gm-tool-loop.ts, backend/src/engine/tool-schemas.ts, backend/src/engine/__tests__/gm-tool-loop.test.ts</files>
  <behavior>
    - Mutation-heavy route actions from `P86-F001` cause the GM tool loop to call an existing state-bearing tool when it introduces actors, props, obligations, routes, combat posture, or aftermath that should matter later.
    - The tool loop must not satisfy this by returning prose-only text.
    - The false-claim guard still blocks free keys, permits, access, or authority from unsupported player claims.
  </behavior>
  <action>Add regression cases using Phase 86 action text examples: raised voices/inspection dispute, waxed cloth/barge manifests obligation, recessed door/narrow stair route, changed danger after violence. Update tool-loop contract/tool descriptions so future-relevant concrete changes are represented through existing backend tools. Do not create a new broad "persist_anything" tool; use existing typed tools and validation.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/gm-tool-loop.test.ts</automated>
  </verify>
  <done>Tool-loop tests fail on prose-only future-relevant pressure and pass only when accepted state-bearing tool observations exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep narrator packet tied to settled tool truth</name>
  <files>backend/src/engine/narrator-packet.ts, backend/src/engine/tool-execution-context.ts, backend/src/engine/__tests__/narrator-packet.test.ts, backend/src/engine/__tests__/tool-execution-context.test.ts</files>
  <behavior>
    - Successful `log_event`, `reveal_location`, `move_to`, `spawn_npc`, and `spawn_item` observations appear as concrete settled effects in `NarratorPacket`.
    - Failed/skipped tool calls and unsupported access-claim attempts do not become settled effects.
    - Runtime refs from successful observations remain legal for later same-loop tool calls.
  </behavior>
  <action>Extend packet and execution-context tests around successful tool observations from Phase 86-style scenes. Ensure narrator-visible settled effects include the actual text/name/location from the accepted result, not only UUIDs or generic action labels. Keep failed/skipped/guarded calls out of settled consequences.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/narrator-packet.test.ts src/engine/__tests__/tool-execution-context.test.ts</automated>
  </verify>
  <done>Final narrator receives concrete settled facts only from accepted backend/tool truth, and later tool calls can use refs created earlier in the same loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GM model -> runtime tools | Model requests state mutation through backend-validated tools. |
| Tool observations -> narration | Accepted backend results become player-visible truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-03-01 | Tampering | `gm-tool-loop.ts` | mitigate | Require typed state-bearing tool observations for future-relevant pressure; keep schemas authoritative. |
| T-87-03-02 | Information Disclosure | `narrator-packet.ts` | mitigate | Include only player-facing accepted tool summaries; keep private/offscreen terms filtered. |
| T-87-03-03 | Elevation of Privilege | False-claim boundary | mitigate | Preserve unconfirmed access proof guard tests from `P86-OK-001`. |
</threat_model>

<verification>
Run focused engine tests plus `npm --prefix backend run typecheck`. Then run `PHASE87_FINDING_FILTER=P86-F001 PHASE87_ASSERT_FIXED=1` dry-run after 87-01 harness work exists.
</verification>

<success_criteria>
`P86-F001` is addressed by state/tool truth, not by making narration vaguer or hiding mutation-heavy checks.
</success_criteria>

<output>
After completion, create `.planning/phases/87-playtest-defect-burn-down-and-final-rerun/87-03-SUMMARY.md`.
</output>
