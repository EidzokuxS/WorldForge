---
phase: 87-playtest-defect-burn-down-and-final-rerun
plan: 04
type: execute
wave: 2
depends_on: [87-01]
files_modified:
  - backend/src/engine/scene-frame.ts
  - backend/src/engine/model-facing-scene.ts
  - backend/src/engine/gm-turn-read.ts
  - backend/src/engine/__tests__/scene-frame.test.ts
  - backend/src/engine/__tests__/gm-turn-read.test.ts
  - backend/src/routes/__tests__/chat-turn-context.test.ts
autonomous: true
requirements: [P87-R2, P87-R4]
must_haves:
  truths:
    - "Legacy campaigns with recoverable current-location state can enter `/game` and build a SceneFrame."
    - "Recent route, sublocation, and social referents are available to GM Read in compact form."
    - "GM clarification is reserved for genuine ambiguity, not obvious recent-context references."
  artifacts:
    - path: "backend/src/engine/scene-frame.ts"
      provides: "SceneFrame recovery and recent referent candidates"
      contains: "buildSceneFrame"
    - path: "backend/src/engine/model-facing-scene.ts"
      provides: "Model-facing compact scene packet"
      contains: "buildModelFacingScenePacket"
    - path: "backend/src/engine/gm-turn-read.ts"
      provides: "GM path choice and clarification contract"
      contains: "clarification"
  key_links:
    - from: "backend/src/engine/scene-frame.ts"
      to: "backend/src/engine/gm-turn-read.ts"
      via: "target/movement/recent event candidates"
      pattern: "movementCandidates|recentEvents|targetCandidates"
---

<objective>
Burn down `P86-CAL-001` and `P86-F004`: SceneFrame entry failures and GM context drops around route/social references.

Purpose: keep the GM oriented in the recent scene so it can adjudicate like a GM instead of asking the player to restate obvious local context.
Output: recoverable SceneFrame construction, recent-referent packet support, and clarification regressions.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-exhaustive-live-playtest-matrix-and-findings-ledger/86-FINDINGS.md
@.planning/phases/87-playtest-defect-burn-down-and-final-rerun/87-CONTEXT.md
@.planning/phases/87-playtest-defect-burn-down-and-final-rerun/87-ACCEPTED-FINDINGS.md
@backend/src/engine/scene-frame.ts
@backend/src/engine/model-facing-scene.ts
@backend/src/engine/gm-turn-read.ts

<interfaces>
From `scene-frame.ts`:
```typescript
export interface SceneFrame {
  currentLocationId: string;
  currentSceneScopeId: string | null;
  recentEvents: SceneFrameRecentEvent[];
  targetCandidates: SceneFrameTargetCandidate[];
  movementCandidates: SceneFrameMovementCandidate[];
}
```

`gm-turn-read.ts` supports paths: `direct`, `roll_oracle`, `tool_plan`, `combat_transition`, `clarification`, `continue`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recover valid legacy SceneFrame entry</name>
  <files>backend/src/engine/scene-frame.ts, backend/src/engine/__tests__/scene-frame.test.ts, backend/src/routes/__tests__/chat-turn-context.test.ts</files>
  <behavior>
    - Campaigns with a valid player row and `currentLocationId` but missing `currentSceneLocationId` build a SceneFrame using the current broad location as the immediate scene scope.
    - Campaigns missing required player/location rows return a clear invalid-campaign error instead of `Cannot build SceneFrame`.
    - The repair path does not fabricate NPCs, locations, items, authority, or chat history.
  </behavior>
  <action>Add regression fixtures based on `P86-CAL-001` campaign shape: current broad location present, scene scope missing. Implement recoverable SceneFrame normalization by using the existing current location as scope when the DB proves it is legal. For unrecoverable campaigns, return a clear route error and mark the fixture invalid in `87-ACCEPTED-FINDINGS.md` rather than pretending the turn worked.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/scene-frame.test.ts src/routes/__tests__/chat-turn-context.test.ts</automated>
  </verify>
  <done>Recoverable legacy campaigns enter `/game`; unrecoverable fixtures fail with explicit invalid-campaign evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry recent route and social referents into GM Read</name>
  <files>backend/src/engine/scene-frame.ts, backend/src/engine/model-facing-scene.ts, backend/src/engine/gm-turn-read.ts, backend/src/engine/__tests__/scene-frame.test.ts, backend/src/engine/__tests__/gm-turn-read.test.ts</files>
  <behavior>
    - Recent event and movement candidate labels cover phrases such as `that connection`, `newly mentioned sublocation`, `slower route`, `nearby vendor`, and `the deal`.
    - GM Read chooses direct/tool/movement/roll when recent referents are unambiguous.
    - GM Read uses `clarification` only when multiple grounded recent candidates compete.
  </behavior>
  <action>Add compact recent-referent candidates to SceneFrame/model-facing scene data using existing recent events, movement candidates, target candidates, and recent conversation. Update the GM Read prompt contract so clarification asks only when no grounded candidate or multiple plausible candidates exist. Do not add a broad memory dump; cap entries and redact private/offscreen terms as existing model-facing scene helpers do.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/scene-frame.test.ts src/engine/__tests__/gm-turn-read.test.ts</automated>
  </verify>
  <done>Phase 86 context-drop examples are locked as regressions and no longer produce out-of-character clarification when the referent is grounded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Campaign DB -> SceneFrame | Persisted campaign rows become model-facing scene truth. |
| Recent conversation -> GM prompt | Player/assistant text influences model action interpretation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-04-01 | Tampering | `scene-frame.ts` | mitigate | Recover only from existing legal current-location rows; never fabricate missing world rows. |
| T-87-04-02 | Information Disclosure | `model-facing-scene.ts` | mitigate | Reuse private/offscreen redaction and cap recent referents. |
| T-87-04-03 | Denial of Service | `gm-turn-read.ts` | mitigate | Keep candidate lists bounded and test ambiguous-reference clarification separately. |
</threat_model>

<verification>
Run focused backend tests and `npm --prefix backend run typecheck`. Update `87-ACCEPTED-FINDINGS.md` with `P86-CAL-001` disposition after tests prove recoverable vs invalid fixture behavior.
</verification>

<success_criteria>
`P86-CAL-001` and `P86-F004` are fixed without hiding invalid campaigns or making the player restate obvious context.
</success_criteria>

<output>
After completion, create `.planning/phases/87-playtest-defect-burn-down-and-final-rerun/87-04-SUMMARY.md`.
</output>
