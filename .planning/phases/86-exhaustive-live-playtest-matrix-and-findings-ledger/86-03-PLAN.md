# 86-03 Plan: Route Execution Matrix

## Goal

Run five route types per campaign for about twenty turns per route where feasible, using checkpoint restore to keep branches isolated.

## Tasks

- [ ] Implement route restore discipline: restore campaign baseline before each route.
- [ ] Run `tourist-observer`, `social-pressure`, `exploration-location-graph`, `false-claim-boundary`, and `combat-power` for every campaign.
- [ ] Capture per-turn screenshot, visible prose, state/log pointers, route score, prose score, and hard/soft failures.
- [ ] Check UI effects and formatting during live response where the fiction triggers them: shake, flash, stage signal, bold, italic, color, and readability.
- [ ] Check combat and power mismatch on at least one route per campaign where fiction supports it.
- [ ] Check living-world mutation by deliberately avoiding the main hook in tourist routes.

## Verification

- [ ] Matrix coverage report shows route counts and skipped turns with reasons.
- [ ] No route is counted if evidence shape is incomplete.
- [ ] All hard failures are listed in `findings.json` and `86-FINDINGS.md`.

