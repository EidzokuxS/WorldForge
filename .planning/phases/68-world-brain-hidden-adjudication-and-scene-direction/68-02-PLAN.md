---
phase: 68-world-brain-hidden-adjudication-and-scene-direction
plan: 02
type: execute
wave: 2
depends_on: [68-01]
files_modified:
  - backend/src/engine/world-brain.ts
  - backend/src/engine/scene-assembly.ts
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/__tests__/scene-assembly.test.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
autonomous: true
requirements: [P68-R2, P68-R4, P68-R6]
must_haves:
  truths:
    - "Normal player turns run a judge-owned world-brain scene-direction pass after Oracle and before the hidden storyteller tool-driving pass."
    - "The existing hidden tool-driving pass stays in place but consumes explicit world-brain scene direction instead of inventing scene purpose, focal actors, or co-presence from scratch."
    - "Scene assembly carries authoritative world-brain causal metadata and a filtered player-perceivable derivative without widening into Phase 69 ownership migration."
  artifacts:
    - path: backend/src/engine/world-brain.ts
      provides: "The executable world-brain runner and hidden/visible direction transformations."
      contains: "runWorldBrainSceneDirection|toPlayerPerceivableWorldBrainDirection"
    - path: backend/src/engine/scene-assembly.ts
      provides: "SceneAssembly fields for world-brain causal metadata."
      contains: "sceneDirection|playerPerceivableSceneDirection"
    - path: backend/src/engine/turn-processor.ts
      provides: "Player-turn world-brain orchestration before hidden tool-driving."
      contains: "hidden-tool-driving|sceneDirection"
    - path: backend/src/engine/__tests__/scene-assembly.test.ts
      provides: "Focused regression coverage for SceneAssembly world-brain handoff."
      contains: "playerPerceivableSceneDirection"
  key_links:
    - from: backend/src/engine/turn-processor.ts
      to: backend/src/engine/world-brain.ts
      via: "judge-owned structured call before hidden tool-driving"
      pattern: "runWorldBrainSceneDirection"
    - from: backend/src/engine/turn-processor.ts
      to: backend/src/engine/scene-assembly.ts
      via: "assembled authoritative scene includes world-brain causal packet"
      pattern: "sceneDirection|playerPerceivableSceneDirection"
---

<objective>
Insert the Phase 68 world-brain seam into the normal player-turn path and make scene assembly the authoritative causal handoff without migrating hidden-pass ownership out of the existing storyteller seam.

Purpose: Establish the runtime bridge for normal turns while keeping Phase 68 additive and low-risk.
Output: Player-turn world-brain execution, SceneAssembly causal extensions, and integration regressions.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-CONTEXT.md
@.planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-RESEARCH.md
@backend/src/engine/turn-processor.ts
@backend/src/engine/scene-assembly.ts
@backend/src/engine/__tests__/turn-processor.test.ts

<interfaces>
Expected from Plan 68-01:
```ts
export interface WorldBrainSceneDirection {
  situationSummary: string;
  sceneQuestion: string;
  focalActorNames: string[];
  backgroundActorNames: string[];
  presenceReasons: Array<{ actorName: string; reason: string; perceivable: boolean }>;
  causalBeats: Array<{ summary: string; perceivable: boolean }>;
  narrationGuardrails: string[];
}

export function formatHiddenWorldBrainDirectionBlock(direction: WorldBrainSceneDirection): string;
export function toPlayerPerceivableWorldBrainDirection(direction: WorldBrainSceneDirection): WorldBrainSceneDirection;
```

Current turn handoff from `backend/src/engine/turn-processor.ts`:
```ts
export interface HiddenTurnSummary {
  currentTick: number;
  predictedTick: number;
  currentLocationId: string | null;
  currentSceneScopeId: string | null;
  oracleResult: OracleResult;
  toolCalls: Array<{ tool: string; args: unknown; result: unknown }>;
  openingScene: boolean;
  sceneAssembly?: SceneAssembly;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add player-turn world-brain and scene-assembly regressions first</name>
  <files>
    - backend/src/engine/__tests__/turn-processor.test.ts
    - backend/src/engine/__tests__/scene-assembly.test.ts
  </files>
  <action>
    Add focused regressions that lock D-01, D-03, D-10, D-14, and D-15 on the normal player-turn path:
    - `turn-processor.test.ts` proves the world-brain pass runs after Oracle and before hidden tool-driving for normal turns
    - the hidden tool-driving system prompt includes a bounded world-brain block
    - hidden tool-driving receives `sceneDirection` from the judge-owned seam instead of synthesizing scene purpose solely from raw presence
    - the old pre-phase path stays additive when no world-brain result exists
    - new `scene-assembly.test.ts` proves `SceneAssembly` stores both the raw world-brain direction and a filtered player-perceivable derivative
    - SceneAssembly prefers explicit world-brain presence reasons and causal beats over fallback loose inference when both are available
    - committed tool/state effects outrank stale world-brain causal suggestions in the assembled authoritative packet
    - tests explicitly assert that the hidden pass still exists and no tool-driving ownership migration happens in Phase 68
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "Test-Path 'backend/src/engine/__tests__/scene-assembly.test.ts'; rg -n 'world-brain|sceneDirection|playerPerceivableSceneDirection' 'backend/src/engine/__tests__/turn-processor.test.ts' 'backend/src/engine/__tests__/scene-assembly.test.ts'"</automated>
  </verify>
  <done>The new regression files pin the player-turn world-brain seam and SceneAssembly causal handoff before runtime wiring changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire world-brain into normal turns and scene assembly</name>
  <files>
    - backend/src/engine/world-brain.ts
    - backend/src/engine/scene-assembly.ts
    - backend/src/engine/turn-processor.ts
  </files>
  <behavior>
    - Test 1: Player turns call the world-brain seam after Oracle and before hidden tool-driving, using a judge-owned structured output call.
    - Test 2: Hidden tool-driving reads a bounded world-brain block instead of inventing scene purpose or focality from raw presence alone.
    - Test 3: SceneAssembly carries both raw and filtered world-brain direction without widening into Phase 69 hidden-pass migration.
  </behavior>
  <action>
    Implement the normal-turn bridge per D-01, D-02, D-03, D-10, D-11, D-14, and D-15:
    - extend `world-brain.ts` with `runWorldBrainSceneDirection(...)` using `safeGenerateObject` plus the judge role, not freeform prose generation
    - add a small reusable seed builder in `scene-assembly.ts` for opening-state/current-scene/presence/recent-context inputs so the world-brain pass and final assembly read the same factual substrate
    - extend `SceneAssembly` with `sceneDirection` and `playerPerceivableSceneDirection`
    - thread the raw world-brain result through `HiddenTurnSummary` and `assembleAuthoritativeScene(...)`
    - build a bounded `[WORLD-BRAIN SCENE DIRECTION]` block into the hidden tool-driving system prompt
    - reconcile contradictions in one place: `SceneAssembly` prefers committed tool/state effects over stale world-brain beats, while world-brain still outranks loose fallback inference
    - keep the current hidden storyteller tool-driving pass intact; do not move tool execution ownership, route flow, SSE flow, DB schema, Oracle math, or prompt-rewrite cleanup into this plan
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/engine/__tests__/world-brain.test.ts src/engine/__tests__/scene-assembly.test.ts src/engine/__tests__/turn-processor.test.ts</automated>
  </verify>
  <done>Normal turns now execute the additive world-brain seam, the hidden pass consumes it, and SceneAssembly carries the authoritative causal packet.</done>
</task>

<task type="auto">
  <name>Task 3: Run the normal-turn world-brain integration gate</name>
  <files>
    - backend/src/engine/world-brain.ts
    - backend/src/engine/scene-assembly.ts
    - backend/src/engine/turn-processor.ts
    - backend/src/engine/__tests__/scene-assembly.test.ts
    - backend/src/engine/__tests__/turn-processor.test.ts
  </files>
  <action>
    Run the focused world-brain integration gate so Plans 68-03 and 68-04 inherit a stable player-turn seam instead of debugging foundation drift.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/engine/__tests__/world-brain.test.ts src/engine/__tests__/scene-assembly.test.ts src/engine/__tests__/turn-processor.test.ts</automated>
  </verify>
  <done>The player-turn bridge is green under focused contract, scene-assembly, and turn-processor regressions.</done>
</task>

</tasks>

<verification>
- `cd backend && npx vitest run src/engine/__tests__/world-brain.test.ts src/engine/__tests__/scene-assembly.test.ts src/engine/__tests__/turn-processor.test.ts`
</verification>

<success_criteria>
The normal player-turn path runs a bounded judge-owned world-brain pass before hidden tool-driving, SceneAssembly carries that causal packet authoritatively, and the plan stays additive by explicitly not migrating full hidden-pass ownership.
</success_criteria>

<output>
After completion, create `.planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-02-SUMMARY.md`
</output>
