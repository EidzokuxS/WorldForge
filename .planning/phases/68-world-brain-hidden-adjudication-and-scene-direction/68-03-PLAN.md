---
phase: 68-world-brain-hidden-adjudication-and-scene-direction
plan: 03
type: execute
wave: 3
depends_on: [68-02]
files_modified:
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/prompt-assembler.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
autonomous: true
requirements: [P68-R3, P68-R5]
must_haves:
  truths:
    - "Opening scenes run the same bounded world-brain seam as normal turns before any visible narration is generated."
    - "Final visible narration consumes only player-perceivable world-brain direction threaded through SceneAssembly, not raw hidden scratch reasoning."
    - "Opening narration no longer free-invents why actors are present or what the immediate situation is from a bare presence list."
  artifacts:
    - path: backend/src/engine/turn-processor.ts
      provides: "Opening-scene world-brain execution plus final-visible handoff."
      contains: "processOpeningScene|opening-final-narration|worldBrain"
    - path: backend/src/engine/prompt-assembler.ts
      provides: "Final narration prompt sections that consume player-perceivable scene direction."
      contains: "SCENE DIRECTION|NARRATION GUARDRAILS"
    - path: backend/src/engine/__tests__/prompt-assembler.test.ts
      provides: "Regression coverage for final-visible world-brain consumption."
      contains: "playerPerceivableSceneDirection"
  key_links:
    - from: backend/src/engine/turn-processor.ts
      to: backend/src/engine/world-brain.ts
      via: "opening-scene structured pass before visible narration"
      pattern: "processOpeningScene|runWorldBrainSceneDirection"
    - from: backend/src/engine/prompt-assembler.ts
      to: backend/src/engine/scene-assembly.ts
      via: "final-visible prompt consumes filtered scene direction"
      pattern: "playerPerceivableSceneDirection|SCENE DIRECTION"
---

<objective>
Adopt the world-brain seam for opening scenes and make final visible narration consume only the filtered authoritative scene-direction packet assembled by the backend.

Purpose: Fix the opening/player-turn narrative handoff without leaking hidden scratch reasoning or widening Phase 68 into full ownership migration.
Output: Opening-scene world-brain integration plus final-visible prompt consumption regressions.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-CONTEXT.md
@.planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-RESEARCH.md
@.planning/phases/45-authoritative-scene-assembly-and-start-of-play-runtime/45-03-SUMMARY.md
@backend/src/engine/turn-processor.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/__tests__/prompt-assembler.test.ts

<interfaces>
Expected from Plan 68-02:
```ts
export interface SceneAssembly {
  // existing fields...
  sceneDirection: WorldBrainSceneDirection | null;
  playerPerceivableSceneDirection: WorldBrainSceneDirection | null;
}
```

Current final prompt entry point from `backend/src/engine/prompt-assembler.ts`:
```ts
export async function assembleFinalNarrationPrompt(options: {
  campaignId: string;
  contextWindow: number;
  sceneAssembly: SceneAssembly;
  actionResult?: AssembleOptions["actionResult"];
  embedderResult?: ResolveResult;
  playerAction?: string;
  judgeRole?: ResolvedRole;
}): Promise<FinalNarrationPrompt>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add opening-scene and final-visible world-brain regressions first</name>
  <files>
    - backend/src/engine/__tests__/turn-processor.test.ts
    - backend/src/engine/__tests__/prompt-assembler.test.ts
  </files>
  <action>
    Add focused regressions that lock D-08, D-12, D-13, and D-15:
    - `processOpeningScene(...)` must run the same world-brain seam as normal turns before visible narration
    - `assembleFinalNarrationPrompt(...)` must render a bounded `[SCENE DIRECTION]` section and a bounded `[NARRATION GUARDRAILS]` section from `sceneAssembly.playerPerceivableSceneDirection`
    - hidden-only `presenceReasons` and `causalBeats` must not appear in the visible prompt
    - opening prompt coverage must explicitly reject the old presence-list-only framing path
    - missing or empty focal-actor direction must fail explicitly instead of silently degrading to raw presence-only opening narration
    - no raw world-brain scratch object is passed directly to `generateText`; only the assembled prompt string contains the filtered visible block
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "rg -n 'SCENE DIRECTION|NARRATION GUARDRAILS|processOpeningScene|playerPerceivableSceneDirection' 'backend/src/engine/__tests__/turn-processor.test.ts' 'backend/src/engine/__tests__/prompt-assembler.test.ts'"</automated>
  </verify>
  <done>The opening-scene and final-visible regressions explicitly lock the Phase 68 visible-consumption contract before implementation changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire opening scenes and final narration to the world-brain handoff</name>
  <files>
    - backend/src/engine/turn-processor.ts
    - backend/src/engine/prompt-assembler.ts
  </files>
  <behavior>
    - Test 1: Opening scenes call the same judge-owned world-brain seam before visible narration.
    - Test 2: Final-visible narration consumes only the filtered player-perceivable scene-direction packet.
    - Test 3: Opening narration no longer relies on raw presence alone to explain why the scene exists.
  </behavior>
  <action>
    Implement D-08, D-12, D-13, and D-15:
    - update `processOpeningScene(...)` to build the same scene-direction seed used by normal turns, call the world-brain seam, and thread the result into `assembleAuthoritativeScene(...)`
    - extend `assembleFinalNarrationPrompt(...)` to render `[SCENE DIRECTION]` and `[NARRATION GUARDRAILS]` from `sceneAssembly.playerPerceivableSceneDirection`
    - keep visible narration bounded to authoritative assembled facts; do not pass raw hidden reasoning, do not let visible narration mutate state, and do not widen into post-generation prose rewriting
    - if the opening world-brain result cannot name at least one valid focal actor, fail explicitly rather than falling back to a raw presence dump
    - preserve the existing Phase 45 opening bootstrap behavior while replacing the old causal vacuum with explicit world-brain framing
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/prompt-assembler.test.ts</automated>
  </verify>
  <done>Opening scenes and final-visible narration both consume the bounded world-brain handoff, and the focused turn/prompt regressions pass.</done>
</task>

<task type="auto">
  <name>Task 3: Run the opening plus final-visible integration gate</name>
  <files>
    - backend/src/engine/turn-processor.ts
    - backend/src/engine/prompt-assembler.ts
    - backend/src/engine/__tests__/turn-processor.test.ts
    - backend/src/engine/__tests__/prompt-assembler.test.ts
  </files>
  <action>
    Run the focused Phase 68 gate for opening-scene adoption and final-visible prompt consumption so the remaining observability plan only has to prove instrumentation and closeout.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/prompt-assembler.test.ts</automated>
  </verify>
  <done>Opening-scene world-brain adoption and final-visible prompt consumption are green under the focused phase regressions.</done>
</task>

</tasks>

<verification>
- `cd backend && npx vitest run src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/prompt-assembler.test.ts`
</verification>

<success_criteria>
Both opening scenes and normal turns now hand final visible narration an authoritative, bounded, player-perceivable scene-direction packet, and visible prose no longer has to invent why the scene exists from raw presence lists.
</success_criteria>

<output>
After completion, create `.planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-03-SUMMARY.md`
</output>
