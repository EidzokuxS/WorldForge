---
phase: 68-world-brain-hidden-adjudication-and-scene-direction
plan: 04
type: execute
wave: 4
depends_on: [68-03]
files_modified:
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/__tests__/turn-processor.observability.test.ts
  - .planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-VALIDATION.md
autonomous: true
requirements: [P68-R8, P68-R9]
must_haves:
  truths:
    - "A compact Phase 58-style observability event records whether the world-brain pass ran for opening scenes and player turns, plus bounded counts and summary/question lengths only."
    - "Focused world-brain regressions, observability coverage, full backend Vitest, and backend typecheck all pass without widening scope into routes, DB schema, Oracle redesign, or Phase 69 migration."
    - "Phase 68 validation evidence records exact commands, outputs, and no-scope-creep checks."
  artifacts:
    - path: backend/src/engine/turn-processor.ts
      provides: "Compact world-brain observability event emission."
      contains: "world-brain.scene-direction"
    - path: backend/src/engine/__tests__/turn-processor.observability.test.ts
      provides: "Regression coverage for the compact world-brain event payload."
      contains: "world-brain.scene-direction"
    - path: .planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-VALIDATION.md
      provides: "Phase 68 executable verification record."
      contains: "status: complete"
  key_links:
    - from: backend/src/engine/turn-processor.ts
      to: backend/src/engine/__tests__/turn-processor.observability.test.ts
      via: "compact world-brain event payload assertions"
      pattern: "world-brain.scene-direction|focalActorCount|causalBeatCount"
    - from: .planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-VALIDATION.md
      to: backend/src/engine/__tests__/turn-processor.observability.test.ts
      via: "recorded execution evidence"
      pattern: "turn-processor.observability"
---

<objective>
Add bounded observability and phase-closeout proof for the Phase 68 world-brain seam so future debugging can inspect the new handoff without relying on raw prompt dumps or ad hoc manual spelunking.

Purpose: Prove the seam runs on both paths, stays compact, and does not widen scope.
Output: Compact world-brain observability, focused observability regression, and `68-VALIDATION.md`.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-CONTEXT.md
@.planning/phases/58-pipeline-observability-logging/58-04-SUMMARY.md
@backend/src/engine/turn-processor.ts
@backend/src/engine/__tests__/turn-processor.observability.test.ts

<interfaces>
Current observability precedent from Phase 58:
```ts
log.event("prompt.assembled", {
  pass,
  totalTokens,
  budgetUsed,
  sectionCount,
  assembledChars,
});
```

Phase 68 event must stay equally compact:
```ts
{
  source: "player-turn" | "opening-scene",
  ran: boolean,
  focalActorCount: number,
  backgroundActorCount: number,
  presenceReasonCount: number,
  causalBeatCount: number,
  perceivableBeatCount: number,
  situationSummaryLength: number,
  sceneQuestionLength: number,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add world-brain observability regressions first</name>
  <files>
    - backend/src/engine/__tests__/turn-processor.observability.test.ts
  </files>
  <action>
    Extend the existing observability test surface so it locks D-16 and D-17:
    - one player-turn path assertion and one opening-scene path assertion for a compact `world-brain.scene-direction` event
    - payload only records run-source, compact counts, and summary/question lengths
    - event count is exactly one per eligible player-turn run and exactly one per opening-scene run
    - no raw formatted prompt text, no giant arrays, and no hidden reasoning bodies in the event payload
    - explicit assertions that the event exists when the seam runs and that count/length fields stay numeric
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "rg -n 'world-brain.scene-direction|focalActorCount|causalBeatCount|sceneQuestionLength' 'backend/src/engine/__tests__/turn-processor.observability.test.ts'"</automated>
  </verify>
  <done>`turn-processor.observability.test.ts` contains explicit world-brain event coverage for both player-turn and opening-scene paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit the compact world-brain observability event</name>
  <files>
    - backend/src/engine/turn-processor.ts
  </files>
  <behavior>
    - Test 1: Player turns emit one compact world-brain event with numeric counts and length metadata.
    - Test 2: Opening scenes emit the same compact event with a distinct source label.
    - Test 3: The event payload never includes raw prompt bodies or hidden reasoning strings.
  </behavior>
  <action>
    Add one compact Phase 58-style observability event in `turn-processor.ts` after each world-brain run:
    - event name: `world-brain.scene-direction`
    - source labels: `player-turn` and `opening-scene`
    - payload fields limited to counts and summary/question lengths plus a `ran` boolean
    - emit exactly once per eligible run path
    - do not log raw world-brain formatted blocks, raw prompt bodies, or hidden reasoning text

    Keep this plan scoped to observability only. Do not change routes, SSE transport, DB schema, Oracle probability, hidden-pass ownership, or post-generation rewriting.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/engine/__tests__/turn-processor.observability.test.ts</automated>
  </verify>
  <done>The world-brain seam emits one compact event per run path and the focused observability regression passes.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full backend gate and write Phase 68 validation evidence</name>
  <files>
    - .planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-VALIDATION.md
  </files>
  <action>
    Run the full Phase 68 proof bundle and record it in `68-VALIDATION.md`:
    - focused world-brain bundle: `world-brain.test.ts`, `scene-assembly.test.ts`, `turn-processor.test.ts`, `prompt-assembler.test.ts`, `turn-processor.observability.test.ts`
    - full backend Vitest suite
    - backend typecheck
    - no-scope-creep diff checks proving no Phase 68 changes landed in routes, DB schema, Oracle math, or Phase 69 ownership areas

    Mark the validation doc complete only after all commands pass.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "cd backend; npx vitest run src/engine/__tests__/world-brain.test.ts src/engine/__tests__/scene-assembly.test.ts src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/prompt-assembler.test.ts src/engine/__tests__/turn-processor.observability.test.ts; npm test; npm run typecheck"</automated>
  </verify>
  <done>`68-VALIDATION.md` exists with `status: complete`, the focused Phase 68 bundle is green, the full backend suite is green, and backend typecheck passes.</done>
</task>

</tasks>

<verification>
- `cd backend && npx vitest run src/engine/__tests__/world-brain.test.ts src/engine/__tests__/scene-assembly.test.ts src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/prompt-assembler.test.ts src/engine/__tests__/turn-processor.observability.test.ts`
- `cd backend && npm test`
- `cd backend && npm run typecheck`
</verification>

<success_criteria>
Phase 68 closes with compact auditable observability, green focused regressions, green full backend verification, and written evidence that the phase stayed inside the user-approved additive boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-04-SUMMARY.md`
</output>
