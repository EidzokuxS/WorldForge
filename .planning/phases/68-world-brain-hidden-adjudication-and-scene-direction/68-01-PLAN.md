---
phase: 68-world-brain-hidden-adjudication-and-scene-direction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/engine/world-brain.ts
  - backend/src/engine/__tests__/world-brain.test.ts
autonomous: true
requirements: [P68-R1, P68-R7]
must_haves:
  truths:
    - "A backend-local `WorldBrainSceneDirection` contract exists with exactly the Phase 68 field set and explicit schema caps."
    - "The contract stays judge-owned and structured: no prose generation, no route/SSE coupling, no DB writes, and no hidden-pass ownership migration."
    - "Helpers exist to format a bounded hidden scene-direction block and a filtered player-perceivable scene-direction block from the same object."
  artifacts:
    - path: backend/src/engine/world-brain.ts
      provides: "Schema, caps, types, and formatter helpers for the Phase 68 world-brain seam."
      contains: "WorldBrainSceneDirection|worldBrainSceneDirectionSchema|formatHiddenWorldBrainDirectionBlock"
    - path: backend/src/engine/__tests__/world-brain.test.ts
      provides: "Regression coverage for schema shape, caps, and visible-vs-hidden formatting."
      contains: "sceneQuestion|presenceReasons|causalBeats|narrationGuardrails"
  key_links:
    - from: backend/src/engine/world-brain.ts
      to: backend/src/engine/__tests__/world-brain.test.ts
      via: "schema + formatter assertions"
      pattern: "situationSummary|sceneQuestion|presenceReasons|causalBeats"
---

<objective>
Define the bounded Phase 68 world-brain contract before any turn-path wiring so later plans integrate against one additive scene-direction interface instead of inventing parallel shapes.

Purpose: Lock the exact schema, caps, and formatting surface for the hidden adjudication seam.
Output: `world-brain.ts` plus contract-focused backend tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-CONTEXT.md
@.planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-RESEARCH.md
@backend/src/engine/scene-assembly.ts
@backend/src/engine/turn-processor.ts

<interfaces>
From `backend/src/engine/scene-assembly.ts`:
```ts
export interface SceneAssembly {
  openingScene: boolean;
  openingState: AuthoritativeOpeningState | null;
  currentScene: AuthoritativeSceneContext | null;
  presentNpcNames: string[];
  awareness: { contract; byNpcName; clearNpcNames; hintSignals; };
  recentContext: Array<{
    tick: number;
    summary: string;
    source: "location_recent_event" | "committed_event";
  }>;
  sceneEffects: SceneEffect[];
  playerPerceivableConsequences: string[];
}
```

From `backend/src/engine/storyteller-contract.ts`:
```ts
export type StorytellerPass = "hidden-tool-driving" | "final-visible";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add world-brain contract regression coverage first</name>
  <files>
    - backend/src/engine/__tests__/world-brain.test.ts
  </files>
  <action>
    Create a focused backend test file that locks the Phase 68 contract per D-04, D-05, and D-06:
    - exact top-level fields: `situationSummary`, `sceneQuestion`, `focalActorNames`, `backgroundActorNames`, `presenceReasons`, `causalBeats`, `narrationGuardrails`
    - explicit bounded caps: `situationSummary` <= 240 chars, `sceneQuestion` <= 140 chars, `focalActorNames` max 3, `backgroundActorNames` max 4, `presenceReasons` max 6, `causalBeats` max 6, `narrationGuardrails` max 4
    - `presenceReasons` items carry `{ actorName, reason, perceivable }`
    - `causalBeats` items carry `{ summary, perceivable }`
    - formatter coverage for both a hidden-pass block and a player-perceivable filtered block, proving hidden-only beats/reasons are omitted from the visible formatter
    - negative assertions that the module does not expose route, DB, or storyteller-stream helpers
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "Test-Path 'backend/src/engine/__tests__/world-brain.test.ts'; rg -n 'situationSummary|sceneQuestion|presenceReasons|causalBeats|narrationGuardrails|perceivable' 'backend/src/engine/__tests__/world-brain.test.ts'"</automated>
  </verify>
  <done>`backend/src/engine/__tests__/world-brain.test.ts` exists and pins the exact Phase 68 contract and caps before runtime integration starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the bounded world-brain contract module</name>
  <files>
    - backend/src/engine/world-brain.ts
  </files>
  <behavior>
    - Test 1: Contract parsing accepts only the capped Phase 68 schema and rejects overflow collections or strings.
    - Test 2: Hidden formatter includes the full bounded packet while visible formatter strips non-perceivable beats and reasons.
    - Test 3: Module remains backend-local and additive with no DB, route, or storyteller-stream coupling.
  </behavior>
  <action>
    Create `backend/src/engine/world-brain.ts` as the single Phase 68 contract module. Implement:
    - `worldBrainSceneDirectionSchema`
    - `WorldBrainSceneDirection` and supporting item types
    - shared numeric/string caps exported as constants so later plans reuse the same limits in prompt construction and observability
    - `formatHiddenWorldBrainDirectionBlock(direction)` for the existing hidden tool-driving pass
    - `toPlayerPerceivableWorldBrainDirection(direction)` plus `formatPlayerPerceivableWorldBrainDirectionBlock(direction)` for final-visible consumption later

    Keep this plan strictly additive per D-07 and D-09: no state mutation helpers, no `generateText` or `streamText`, no route wiring, no persistence, no prompt-assembler branching, and no Phase 69 ownership migration.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/engine/__tests__/world-brain.test.ts</automated>
  </verify>
  <done>`world-brain.ts` exports the bounded schema, types, and formatters, and the focused contract test passes green.</done>
</task>

<task type="auto">
  <name>Task 3: Run the foundation gate for the new contract</name>
  <files>
    - backend/src/engine/world-brain.ts
    - backend/src/engine/__tests__/world-brain.test.ts
  </files>
  <action>
    Run the focused backend gate for the new module and a backend typecheck so later Phase 68 plans can depend on the contract without burning context on syntax or typing cleanup.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "cd backend; npx vitest run src/engine/__tests__/world-brain.test.ts; npm run typecheck"</automated>
  </verify>
  <done>The world-brain contract is green under its dedicated test file and backend typecheck.</done>
</task>

</tasks>

<verification>
- `cd backend && npx vitest run src/engine/__tests__/world-brain.test.ts`
- `cd backend && npm run typecheck`
</verification>

<success_criteria>
A single additive `world-brain.ts` module defines the exact capped Phase 68 contract and formatter surface that later plans can wire into player turns, opening scenes, and final narration without revisiting schema shape.
</success_criteria>

<output>
After completion, create `.planning/phases/68-world-brain-hidden-adjudication-and-scene-direction/68-01-SUMMARY.md`
</output>
