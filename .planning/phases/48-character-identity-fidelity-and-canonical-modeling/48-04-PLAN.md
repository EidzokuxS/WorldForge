---
phase: 48-character-identity-fidelity-and-canonical-modeling
plan: 04
type: execute
wave: 3
depends_on:
  - 48-01
  - 48-02
files_modified:
  - frontend/lib/api-types.ts
  - frontend/lib/api.ts
  - frontend/lib/character-drafts.ts
  - frontend/lib/__tests__/character-drafts.test.ts
  - frontend/components/character-creation/character-card.tsx
  - frontend/components/character-creation/__tests__/character-card.identity.test.tsx
autonomous: true
requirements:
  - CHARF-01
must_haves:
  truths:
    - "Frontend parse/generate/import/save/load flows preserve richer identity and fidelity metadata instead of silently flattening or dropping it."
    - "The editor surface stays bounded to the existing draft workflow and does not turn Phase 48 into a major redesign."
    - "Backend-owned deep identity/source-bundle data can round-trip through the current UI even when only a smaller subset is directly editable."
  artifacts:
    - path: "frontend/lib/api-types.ts"
      provides: "Frontend contracts for richer identity payloads and fidelity/source-bundle metadata"
      contains: "CharacterDraft|identity|sourceBundle|continuity"
    - path: "frontend/lib/api.ts"
      provides: "World and character API normalization that preserves richer identity fields"
      contains: "draft|characterRecord|normalize"
    - path: "frontend/lib/character-drafts.ts"
      provides: "Draft projection helpers that keep richer identity fields while deriving compatibility views"
      contains: "characterDraftToParsedCharacter|characterDraftToScaffoldNpc"
    - path: "frontend/components/character-creation/character-card.tsx"
      provides: "Bounded editor updates for the richer model without redesigning the whole creation flow"
      contains: "profile|identity|motivations|startConditions"
    - path: "frontend/components/character-creation/__tests__/character-card.identity.test.tsx"
      provides: "Regression coverage that the editor does not silently discard richer identity data"
      contains: "identity|save/load|round-trip"
  key_links:
    - from: "frontend/lib/api.ts"
      to: "frontend/lib/character-drafts.ts"
      via: "normalized API payloads preserve richer identity fields before draft projection"
      pattern: "draft|characterRecord"
    - from: "frontend/lib/character-drafts.ts"
      to: "frontend/components/character-creation/character-card.tsx"
      via: "existing editor workflow consumes the richer draft while still deriving compatibility views"
      pattern: "CharacterDraft"
    - from: "frontend/lib/character-drafts.ts"
      to: "frontend/lib/__tests__/character-drafts.test.ts"
      via: "tests prove richer identity and backend-owned fidelity fields survive round-trips"
      pattern: "parsedCharacterToDraft|scaffoldNpcToDraft"
---

<objective>
Preserve the richer identity model through the bounded frontend draft/editor seams.

Purpose: keep CHARF-01 end-to-end without turning Phase 48 into a major character-editor redesign.
Output: frontend type/adapters/editor updates and tests that protect richer identity round-trips.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-CONTEXT.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-RESEARCH.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-VALIDATION.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-04-SUMMARY.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-05-SUMMARY.md
@frontend/lib/api.ts
@frontend/lib/character-drafts.ts
@frontend/components/character-creation/character-card.tsx

<interfaces>
From `frontend/lib/character-drafts.ts`:
```ts
export function characterDraftToParsedCharacter(
  draft: CharacterDraft,
): ParsedCharacter;

export function characterDraftToScaffoldNpc(
  draft: CharacterDraft,
): ScaffoldNpc;

export function parsedCharacterToDraft(
  character: ParsedCharacter,
): CharacterDraft;
```

From `frontend/components/character-creation/character-card.tsx`:
```ts
interface CharacterCardProps {
  draft: CharacterDraft;
  locationNames: string[];
  onChange: (draft: CharacterDraft) => void;
  onResolveStartingLocation: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 48-04-01: Preserve richer identity and fidelity metadata through frontend API normalization and draft adapters</name>
  <files>frontend/lib/api-types.ts, frontend/lib/api.ts, frontend/lib/character-drafts.ts, frontend/lib/__tests__/character-drafts.test.ts</files>
  <behavior>
    - Test 1: richer identity layers and optional source-bundle/continuity fields survive API normalization and draft round-trips.
    - Test 2: compatibility projections for parsed characters and scaffold NPCs are derived from the richer draft rather than erasing backend-owned fields.
    - Test 3: save/load flows keep backend-owned fidelity metadata even when the frontend edits only a bounded subset of fields.
  </behavior>
  <action>Update the frontend contracts and adapter helpers to preserve the richer identity model from 48-01/48-02. Per D-14 and the phase scope guard, keep the current `CharacterDraft` workflow and compatibility projections, but stop defaulting richer fields to empty data that silently erases backend-owned truth. `frontend/lib/api.ts` and `frontend/lib/character-drafts.ts` should preserve source-bundle, continuity, and richer identity layers during parse/generate/import/save/load normalization. Avoid introducing a second editor model or frontend-only ontology.</action>
  <verify>
    <automated>npx vitest run frontend/lib/__tests__/character-drafts.test.ts</automated>
  </verify>
  <done>The frontend adapter layer can round-trip richer character identity without flattening it back to summary-only state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 48-04-02: Keep the editor bounded while exposing only the minimum richer identity surface needed for safe round-trips</name>
  <files>frontend/components/character-creation/character-card.tsx, frontend/components/character-creation/__tests__/character-card.identity.test.tsx, frontend/lib/__tests__/character-drafts.test.ts</files>
  <behavior>
    - Test 1: editing currently exposed fields does not wipe richer backend-owned identity/fidelity metadata.
    - Test 2: the existing editor shell can show or preserve bounded richer identity cues without becoming a redesign project.
    - Test 3: save/load behavior keeps continuity-sensitive data intact for key/canonical characters.
  </behavior>
  <action>Update `CharacterCard` only as far as needed to safely preserve the richer identity model. Keep the UI scope bounded per the phase constraints: do not redesign the page or expose every deep identity/source-bundle field as an editable form. Instead, make the existing draft-backed editor preserve and, where justified, lightly surface the richer fields that matter for user trust in save/load and imported/canonical fidelity. Add a focused `character-card.identity.test.tsx` regression so future edits cannot silently strip backend-owned character fidelity state.</action>
  <verify>
    <automated>npx vitest run frontend/lib/__tests__/character-drafts.test.ts frontend/components/character-creation/__tests__/character-card.identity.test.tsx</automated>
  </verify>
  <done>The frontend remains bounded and recognizable while no longer being a place where richer character identity gets accidentally erased.</done>
</task>

</tasks>

<verification>
Run the targeted frontend suites, then use the phase-level manual check from `48-VALIDATION.md` to confirm a recognizable imported or canonical character still feels like the same person after save/load and several turns.
</verification>

<success_criteria>
Phase 48’s richer identity model survives the current UI and save/load workflow without forcing a major editor redesign or dropping backend-owned fidelity data.
</success_criteria>

<output>
After completion, create `.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-04-SUMMARY.md`
</output>
