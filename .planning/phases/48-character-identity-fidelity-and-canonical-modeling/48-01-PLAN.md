---
phase: 48-character-identity-fidelity-and-canonical-modeling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/types.ts
  - shared/src/index.ts
  - backend/src/character/canonical-source-bundle.ts
  - backend/src/character/record-adapters.ts
  - backend/src/routes/schemas.ts
  - backend/src/character/__tests__/record-adapters.test.ts
  - backend/src/character/__tests__/record-adapters.identity.test.ts
  - backend/src/routes/__tests__/schemas.test.ts
autonomous: true
requirements:
  - CHARF-01
must_haves:
  truths:
    - "The shared CharacterDraft/CharacterRecord lane explicitly captures base facts, behavioral core, and live campaign dynamics instead of relying on summary text plus tags."
    - "Imported and canonical characters carry a provenance-aware source bundle plus continuity policy inside the same shared ontology, not a parallel canonical-only record type."
    - "Legacy player/NPC storage, existing saved `characterRecord` JSON, and compatibility projections deterministically backfill into the richer model instead of leaving hollow identity layers."
    - "The richer identity fields are explicitly accepted and materialized by the backend Zod schemas, so they can cross the API boundary instead of existing only as in-process TypeScript."
  artifacts:
    - path: "shared/src/types.ts"
      provides: "One richer shared character contract with three-layer identity truth, richer draft-patch seams, and optional canonical/source-bundle fidelity metadata"
      contains: "baseFacts|behavioralCore|liveDynamics|CharacterDraftPatch|sourceBundle|continuity"
    - path: "backend/src/character/canonical-source-bundle.ts"
      provides: "Pure helpers for canon-facing sources, secondary cue sources, and WorldForge-owned synthesis metadata"
      contains: "canon|secondary|source|continuity"
    - path: "backend/src/character/record-adapters.ts"
      provides: "Hydration and projection between stored rows, legacy compatibility surfaces, and the richer shared record"
      contains: "hydrate|project|identity"
    - path: "backend/src/routes/schemas.ts"
      provides: "Backend API/schema materialization for richer draft and record payloads"
      contains: "characterDraftSchema|characterRecordSchema|materializeDraftRecord"
    - path: "backend/src/character/__tests__/record-adapters.identity.test.ts"
      provides: "Regression coverage for richer identity hydration and compatibility projection"
      contains: "base facts|behavioral core|live dynamics|source bundle"
    - path: "backend/src/routes/__tests__/schemas.test.ts"
      provides: "Regression coverage that richer identity fields survive parse/materialize/compatibility transforms"
      contains: "characterDraftSchema|saveCharacterSchema|supporting draft"
  key_links:
    - from: "shared/src/types.ts"
      to: "backend/src/character/record-adapters.ts"
      via: "record adapters import the shared richer identity contract instead of inventing backend-only shapes"
      pattern: "CharacterDraft|CharacterRecord"
    - from: "backend/src/character/canonical-source-bundle.ts"
      to: "backend/src/character/record-adapters.ts"
      via: "adapter hydration normalizes canonical/imported provenance through one helper seam"
      pattern: "sourceBundle|continuity|normalize"
    - from: "backend/src/character/record-adapters.ts"
      to: "backend/src/character/__tests__/record-adapters.identity.test.ts"
      via: "tests prove rich identity and legacy compatibility survive save/load projection"
      pattern: "hydrateStored|project.*Record"
    - from: "shared/src/types.ts"
      to: "backend/src/routes/schemas.ts"
      via: "route schemas accept and materialize the same richer identity/source-bundle fields over the network boundary"
      pattern: "characterDraftSchema|characterRecordSchema"
---

<objective>
Define the richer shared character identity contract and hydration seam for Phase 48.

Purpose: keep Phase 48 on the Phase 29/30 CharacterDraft/CharacterRecord lane while adding the stronger identity truth CHARF-01 needs.
Output: shared richer types, a provenance/source-bundle helper seam, and backend adapters/tests that preserve compatibility.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-CONTEXT.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-RESEARCH.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-VALIDATION.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-01-SUMMARY.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-03-SUMMARY.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-01-SUMMARY.md
@shared/src/types.ts
@backend/src/character/record-adapters.ts
@backend/src/routes/schemas.ts

<interfaces>
From `shared/src/types.ts`:
```ts
export interface CharacterDraft {
  identity: CharacterIdentityDraft;
  profile: CharacterProfile;
  socialContext: CharacterSocialContext;
  motivations: CharacterMotivations;
  capabilities: CharacterCapabilities;
  state: CharacterState;
  loadout: CharacterLoadout;
  startConditions: CharacterStartConditions;
  provenance: CharacterProvenance;
}

export interface CharacterRecord extends CharacterDraft {
  identity: CharacterIdentity;
}

export interface CharacterDraftPatch {
  identity?: Partial<CharacterIdentityDraft>;
}
```

From `backend/src/character/record-adapters.ts`:
```ts
export function createCharacterRecordFromDraft(
  draft: CharacterDraft,
  identity: Pick<CharacterRecord["identity"], "id" | "campaignId">,
): CharacterRecord;

export function hydrateStoredPlayerRecord(
  row: StoredPlayerRow,
  opts?: LegacyPlayerOptions,
): CharacterRecord;

export function hydrateStoredNpcRecord(
  row: StoredNpcRow,
  opts?: LegacyNpcOptions,
): CharacterRecord;
```

From `backend/src/routes/schemas.ts`:
```ts
export const characterDraftSchema = z.object({ ... });
export const characterRecordSchema = characterDraftSchema.extend({ ... });
export const saveCharacterSchema = z.object({ ... }).or(z.object({ ... })).transform(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 48-01-01: Extend the shared character lane and route schemas with richer identity layers and source-bundle contracts</name>
  <files>shared/src/types.ts, shared/src/index.ts, backend/src/character/canonical-source-bundle.ts, backend/src/character/record-adapters.ts, backend/src/routes/schemas.ts, backend/src/character/__tests__/record-adapters.identity.test.ts, backend/src/routes/__tests__/schemas.test.ts</files>
  <behavior>
    - Test 1: a CharacterDraft/CharacterRecord can carry explicit `baseFacts`, `behavioralCore`, and `liveDynamics` without forking player vs NPC or canonical vs native types.
    - Test 2: imported/canonical records can carry canon-facing fact sources plus secondary card/voice cues, while WorldForge remains the owner of the synthesized runtime model.
    - Test 3: key/canonical continuity metadata expresses stronger identity inertia without freezing all future change.
    - Test 4: `characterDraftSchema`, `characterRecordSchema`, and `materializeDraftRecord()` accept and preserve the richer identity fields across API payload parsing.
    - Test 5: `CharacterDraftPatch` can partially target `baseFacts`, `behavioralCore`, `liveDynamics`, `sourceBundle`, and `continuity` so downstream persona-template work patches the richer seam directly.
  </behavior>
  <action>Per D-01 through D-15, extend the existing shared `CharacterDraft` / `CharacterRecord` contract instead of introducing a second ontology. Add an explicit richer identity structure that covers: `baseFacts`, `behavioralCore`, and `liveDynamics`, plus an optional fidelity/continuity block for key or canonical characters. In the same shared-types pass, extend `CharacterDraftPatch` so downstream persona-template work can patch those richer layers directly instead of falling back to summary/tag seams. Add `backend/src/character/canonical-source-bundle.ts` as a pure helper seam for authoritative canon facts, secondary community-card cues, source provenance, and WorldForge-owned synthesis metadata. Update `record-adapters.ts` enough to materialize sane defaults and round-trip the new structures on hydrated records. In the same task, extend `backend/src/routes/schemas.ts` so the richer identity/source-bundle fields are part of `characterDraftSchema`, `characterRecordSchema`, and related transforms instead of being in-process only. Keep `profile`, `motivations`, `capabilities`, `state`, and `provenance` as part of the same shared model; do not create backend-only or canonical-only shadow types. The new identity contracts must explicitly reference D-07/D-08 and D-11/D-13 in code-facing naming and comments where needed.</action>
  <verify>
    <automated>npx vitest run backend/src/character/__tests__/record-adapters.identity.test.ts backend/src/routes/__tests__/schemas.test.ts</automated>
  </verify>
  <done>The repo has one richer shared character contract with explicit identity layers, provenance/source-bundle support, continuity metadata, and matching route schemas that accept those fields over the API boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 48-01-02: Preserve legacy hydration and schema compatibility projections on top of the richer shared model</name>
  <files>backend/src/character/record-adapters.ts, backend/src/routes/schemas.ts, backend/src/character/__tests__/record-adapters.test.ts, backend/src/character/__tests__/record-adapters.identity.test.ts, backend/src/routes/__tests__/schemas.test.ts</files>
  <behavior>
    - Test 1: legacy player and NPC rows still hydrate into valid richer records without dropping existing profile, goals, beliefs, loadout, or location data.
    - Test 2: compatibility projections for persona, tags, goals, and legacy payloads are derived from the richer model instead of becoming a second source of truth.
    - Test 3: imported/canonical continuity metadata survives record projection and re-hydration.
    - Test 4: additive route payload schemas for save-character and draft-backed transforms keep richer identity fields while preserving older compatibility aliases.
    - Test 5: existing saved `characterRecord` payloads that do not yet contain `baseFacts`, `behavioralCore`, `liveDynamics`, `sourceBundle`, or `continuity` are deterministically backfilled from legacy summaries, drives, frictions, beliefs, goals, and provenance fields.
  </behavior>
  <action>Make migration/backfill an explicit adapter-owned requirement, not implied behavior. `record-adapters.ts` must deterministically upgrade both legacy player/NPC rows and already-saved `characterRecord` JSON that lacks the new richer fields. Map existing shallow fields into the new baseline deterministically: biography/profile facts land in `baseFacts`, enduring motives/pressure behavior/drives/frictions land in `behavioralCore`, and mutable turn-era beliefs/goals/relationship strain land in `liveDynamics`. Add test-owned fixtures for the pre-Phase-48 stored JSON case so executors cannot satisfy this task by only handling freshly generated records. Keep one-way derived legacy views for `persona`, `tags`, and `goals`; do not let those derived views become writable truth again. Preserve Phase 29/30 compatibility guarantees for existing saves and route payloads while making the richer structure the real adapter authority. Explicitly cover `saveCharacterSchema` and related route transforms so the richer fields are proven to survive schema materialization and additive compatibility aliases together.</action>
  <verify>
    <automated>npx vitest run backend/src/character/__tests__/record-adapters.test.ts backend/src/character/__tests__/record-adapters.identity.test.ts backend/src/routes/__tests__/schemas.test.ts</automated>
  </verify>
  <done>Stored player/NPC rows and pre-Phase-48 saved `characterRecord` JSON deterministically backfill, hydrate, project, and persist richer identity data while schema-level and legacy compatibility readers still receive additive derived fields over the network boundary.</done>
</task>

</tasks>

<verification>
Prove that the shared lane is still singular: the richer identity layers, source bundle, and compatibility projections all live in the same CharacterDraft/CharacterRecord contract and pass adapter regressions.
</verification>

<success_criteria>
Phase 48 starts from one stronger shared character model with explicit continuity and provenance, not from another ontology or a canonical-only side path.
</success_criteria>

<output>
After completion, create `.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-01-SUMMARY.md`
</output>
