---
phase: 48-character-identity-fidelity-and-canonical-modeling
plan: 02
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - backend/src/character/prompt-contract.ts
  - backend/src/character/generator.ts
  - backend/src/character/npc-generator.ts
  - backend/src/character/persona-templates.ts
  - backend/src/character/__tests__/persona-templates.test.ts
  - backend/src/routes/character.ts
  - backend/src/routes/persona-templates.ts
  - backend/src/routes/campaigns.ts
  - backend/src/character/__tests__/generator.test.ts
  - backend/src/character/__tests__/npc-generator.test.ts
  - backend/src/routes/__tests__/character.test.ts
  - backend/src/routes/__tests__/persona-templates.test.ts
  - backend/src/routes/__tests__/campaigns.test.ts
autonomous: true
requirements:
  - CHARF-01
must_haves:
  truths:
    - "Player/native generation and imported-card parsing emit the richer shared identity structure instead of collapsing back into biography plus tags."
    - "NPC/import/canonical generation uses the same richer contract and source-bundle semantics as player generation, preserving the shared ontology from Phases 29 and 30."
    - "The existing `backend/src/character/__tests__/generator.test.ts` drift becomes an explicit guard that the prompt contract mentions the richer identity model and source-of-truth rules."
    - "Parse/generate/import/save and world payload routes return richer character fields over the API boundary, not just compatibility aliases."
    - "Persona templates patch richer identity layers through the shared draft seam instead of rewriting `personaSummary` or derived tags as primary truth."
  artifacts:
    - path: "backend/src/character/prompt-contract.ts"
      provides: "Centralized richer character prompt doctrine for all generation/import paths"
      contains: "baseFacts|behavioralCore|liveDynamics|sourceBundle"
    - path: "backend/src/character/generator.ts"
      provides: "Player/native/import generation that emits richer identity and source provenance"
      contains: "parseCharacterDescription|mapV2CardToCharacter|generateCharacter"
    - path: "backend/src/character/npc-generator.ts"
      provides: "NPC/import/canonical generation on the same richer contract"
      contains: "parseNpcDescription|mapV2CardToNpc|generateNpcFromArchetype"
    - path: "backend/src/character/__tests__/generator.test.ts"
      provides: "Regression coverage for the currently failing prompt-contract drift"
      contains: "shared draft contract|source bundle|richer identity"
    - path: "backend/src/character/__tests__/npc-generator.test.ts"
      provides: "Regression coverage for NPC/canonical import alignment on the richer contract"
      contains: "shared draft pipeline|canonical|import"
    - path: "backend/src/routes/character.ts"
      provides: "Parse/generate/import/save character route boundary carrying richer fields"
      contains: "createDraftResponse|save-character|draft"
    - path: "backend/src/routes/campaigns.ts"
      provides: "World payload boundary exposing richer character records/drafts"
      contains: "characterRecord|draft|player|npcs"
    - path: "backend/src/character/persona-templates.ts"
      provides: "Shared draft patching for richer identity layers"
      contains: "applyPersonaTemplatePatch|applyPersonaTemplate"
    - path: "backend/src/routes/persona-templates.ts"
      provides: "Persona template API boundary returning richer draft data plus derived compatibility aliases"
      contains: "templateId|draft|personaTemplate"
    - path: "backend/src/routes/__tests__/character.test.ts"
      provides: "Regression coverage for parse/generate/import/save character route contracts"
      contains: "draft|characterRecord|save-character"
    - path: "backend/src/routes/__tests__/campaigns.test.ts"
      provides: "Regression coverage for world payload character contracts"
      contains: "player|npcs|characterRecord|draft"
    - path: "backend/src/routes/__tests__/persona-templates.test.ts"
      provides: "Regression coverage for richer persona template patching through the API boundary"
      contains: "templateId|draft|personaTemplate"
  key_links:
    - from: "backend/src/character/prompt-contract.ts"
      to: "backend/src/character/generator.ts"
      via: "player/native/import prompts consume one shared richer contract builder"
      pattern: "buildCharacterPromptContract"
    - from: "backend/src/character/prompt-contract.ts"
      to: "backend/src/character/npc-generator.ts"
      via: "NPC and canonical import prompts consume the same richer contract"
      pattern: "buildCharacterPromptContract"
    - from: "backend/src/character/generator.ts"
      to: "backend/src/character/__tests__/generator.test.ts"
      via: "tests lock the richer prompt vocabulary and fix the current red baseline"
      pattern: "backgroundSummary|personaSummary|baseFacts|behavioralCore"
    - from: "backend/src/routes/character.ts"
      to: "backend/src/routes/__tests__/character.test.ts"
      via: "route tests prove richer identity fields survive parse/generate/import/save responses"
      pattern: "draft|characterRecord|save-character"
    - from: "backend/src/routes/campaigns.ts"
      to: "backend/src/routes/__tests__/campaigns.test.ts"
      via: "world payload tests prove player and NPC character contracts retain richer identity data after load"
      pattern: "player|npcs|characterRecord|draft"
    - from: "backend/src/character/persona-templates.ts"
      to: "backend/src/routes/persona-templates.ts"
      via: "template application patches richer draft layers before compatibility projections are returned"
      pattern: "applyPersonaTemplate"
---

<objective>
Align generation, template patching, and backend route boundaries with the richer shared identity model.

Purpose: remove prompt-contract drift and prove that richer character identity survives parse/generate/import/save/load/world payload network boundaries.
Output: shared richer prompt doctrine, updated player/NPC generators, template patching, and route regressions for the backend character contract.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-CONTEXT.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-RESEARCH.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-VALIDATION.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-01-PLAN.md
@backend/src/character/prompt-contract.ts
@backend/src/character/generator.ts
@backend/src/character/npc-generator.ts
@backend/src/character/persona-templates.ts
@backend/src/routes/character.ts
@backend/src/routes/persona-templates.ts
@backend/src/routes/campaigns.ts
@backend/src/character/__tests__/generator.test.ts

<interfaces>
From `backend/src/character/prompt-contract.ts`:
```ts
export function buildCharacterPromptContract(
  options?: {
    roleEmphasis?: string | null;
    includeExplicitUserFacts?: boolean;
    includeStartConditions?: boolean;
    includePersonaTemplates?: boolean;
    includeCanonicalLoadout?: boolean;
    includeDerivedTagsRule?: boolean;
  },
): string;
```

From `backend/src/character/generator.ts`:
```ts
export async function parseCharacterDescription(opts: {
  description: string;
  premise: string;
  locationNames: string[];
  role: ResolvedRole;
}): Promise<CharacterDraft>;

export async function mapV2CardToCharacter(opts: { ... }): Promise<CharacterDraft>;
```

From `backend/src/character/npc-generator.ts`:
```ts
export async function parseNpcDescription(opts: { ... }): Promise<CharacterDraft>;
export async function mapV2CardToNpc(opts: { ... }): Promise<CharacterDraft>;
```

From `backend/src/character/persona-templates.ts`:
```ts
export function applyPersonaTemplatePatch(
  draft: CharacterDraft,
  patch: CharacterDraftPatch,
  templateId?: string | null,
): CharacterDraft;

export function applyPersonaTemplate(
  draft: CharacterDraft,
  template: PersonaTemplate,
): CharacterDraft;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 48-02-01: Rebuild player/native/import prompt doctrine around the richer identity contract and fix the current generator drift</name>
  <files>backend/src/character/prompt-contract.ts, backend/src/character/generator.ts, backend/src/character/__tests__/generator.test.ts</files>
  <behavior>
    - Test 1: `parseCharacterDescription` prompts explicitly reference the richer shared contract and preserving authored facts verbatim.
    - Test 2: imported-card prompts use the shared richer contract, preserve outsider/native context correctly, and define source-bundle/secondary-cue behavior.
    - Test 3: archetype and generated-player prompts no longer act as thin summary builders; they ask for richer identity baseline fields instead.
  </behavior>
  <action>Use the richer contract from 48-01 to rewrite `prompt-contract.ts` and the player generation/import prompts in `generator.ts`. Fix the current red drift in `backend/src/character/__tests__/generator.test.ts` by making the contract language explicit about three-layer truth, authored fact preservation, and WorldForge-owned synthesis. Per D-01, D-02, D-03, D-11, D-12, and D-13, player/native generation must raise the baseline for all characters without overriding authored facts, while imported/canonical flows must preserve canon-facing facts and secondary card cues separately. Do not reintroduce tag-only or persona-summary-as-truth prompts. Keep the existing shared draft output surface; the change is richer structure and stronger instructions, not a new route shape.</action>
  <verify>
    <automated>npx vitest run backend/src/character/__tests__/generator.test.ts</automated>
  </verify>
  <done>The current failing `generator.test.ts` baseline is green and now guards the richer prompt contract instead of thin-summary drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 48-02-02: Move NPC, imported, and canonical generation onto the same richer contract and source-bundle path</name>
  <files>backend/src/character/prompt-contract.ts, backend/src/character/npc-generator.ts, backend/src/routes/character.ts, backend/src/routes/campaigns.ts, backend/src/character/__tests__/npc-generator.test.ts, backend/src/routes/__tests__/character.test.ts, backend/src/routes/__tests__/campaigns.test.ts</files>
  <behavior>
    - Test 1: NPC parsing and archetype generation consume the same richer contract terms as player generation.
    - Test 2: imported/canonical NPC prompts preserve canon facts, secondary cues, and continuity expectations without creating a separate canonical ontology.
    - Test 3: compatibility outputs (`persona`, `tags`, `goals`) are framed as projections from richer identity truth, not as primary outputs.
    - Test 4: parse/generate/import/save character routes and the campaign world payload preserve `baseFacts`, `behavioralCore`, `liveDynamics`, `sourceBundle`, and `continuity` across the API boundary instead of returning only compatibility aliases.
  </behavior>
  <action>Update `npc-generator.ts` to use the same richer prompt doctrine as the player generator, including D-04 through D-10 and D-14/D-15. Canonical and imported NPC flows must route through the shared identity/source-bundle model, preserve continuity/inertia expectations for key characters, and keep compatibility outputs clearly secondary. In the same task, expand `backend/src/routes/character.ts` and `backend/src/routes/campaigns.ts` so parse/generate/import/save responses and campaign world payloads explicitly round-trip the richer identity fields (`baseFacts`, `behavioralCore`, `liveDynamics`, `sourceBundle`, `continuity`) alongside any compatibility aliases. Extend `backend/src/character/__tests__/npc-generator.test.ts`, `backend/src/routes/__tests__/character.test.ts`, and `backend/src/routes/__tests__/campaigns.test.ts` so the network boundary is locked for parse/generate/import/save/load/world payload flows. Avoid changing world-review or editor UX here; this task owns backend generation plus route-boundary fidelity only.</action>
  <verify>
    <automated>npx vitest run backend/src/character/__tests__/generator.test.ts backend/src/character/__tests__/npc-generator.test.ts backend/src/routes/__tests__/character.test.ts backend/src/routes/__tests__/campaigns.test.ts</automated>
  </verify>
  <done>All character creation/import paths and backend character payload routes speak the same richer contract and source-bundle language, with route regressions proving the richer fields survive parse/generate/import/save/load/world payload boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 48-02-03: Make persona templates patch richer identity layers instead of rewriting derived persona/tag truth</name>
  <files>backend/src/character/persona-templates.ts, backend/src/routes/persona-templates.ts, backend/src/character/__tests__/persona-templates.test.ts, backend/src/routes/__tests__/persona-templates.test.ts</files>
  <behavior>
    - Test 1: persona template application patches the richer draft layers that actually drive behavior, including `baseFacts`, `behavioralCore`, and `liveDynamics`, rather than overwriting thin summary fields as truth.
    - Test 2: persona template route responses preserve richer identity/source-bundle fields and expose compatibility summaries only as derived output.
    - Test 3: applying a template to imported or canonical drafts does not erase `sourceBundle` or `continuity` metadata already present on the draft.
  </behavior>
  <action>Own `backend/src/character/persona-templates.ts` explicitly as an active seam. Update template patching so persona templates modify the richer identity layers created in 48-01 instead of rewriting `personaSummary`, tags, or goals as primary truth. Preserve any existing `sourceBundle` and `continuity` metadata when templates are applied to imported/canonical drafts, and keep any compatibility summaries/tags one-way derived. Extend `backend/src/routes/persona-templates.ts` plus the character- and route-level persona-template tests so template application through the API boundary returns richer draft data with compatibility aliases layered on top. This task is the regression guard that Phase 48 did not leave the template seam behind.</action>
  <verify>
    <automated>npx vitest run backend/src/character/__tests__/persona-templates.test.ts backend/src/routes/__tests__/persona-templates.test.ts</automated>
  </verify>
  <done>`backend/src/character/persona-templates.ts` is explicitly owned by Phase 48, template application patches richer identity truth, and API regressions prove templates do not collapse drafts back into persona-summary/tag truth.</done>
</task>

</tasks>

<verification>
Run the targeted generator, route, and persona-template suites and confirm that the previously failing generator drift is gone, persona templates patch richer identity truth, and backend payloads preserve richer fields across parse/generate/import/save/load/world boundaries.
</verification>

<success_criteria>
Characters no longer enter or cross WorldForge as thin summaries. Player, imported, and canonical generation, template application, and backend route payloads all preserve the richer shared identity model that Phase 48 needs downstream.
</success_criteria>

<output>
After completion, create `.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-02-SUMMARY.md`
</output>
