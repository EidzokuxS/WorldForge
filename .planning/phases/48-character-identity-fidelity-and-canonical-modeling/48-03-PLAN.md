---
phase: 48-character-identity-fidelity-and-canonical-modeling
plan: 03
type: execute
wave: 3
depends_on:
  - 48-01
  - 48-02
files_modified:
  - backend/src/character/runtime-tags.ts
  - backend/src/engine/prompt-assembler.ts
  - backend/src/engine/npc-agent.ts
  - backend/src/engine/npc-offscreen.ts
  - backend/src/engine/reflection-agent.ts
  - backend/src/engine/reflection-tools.ts
  - backend/src/engine/__tests__/npc-agent.test.ts
  - backend/src/engine/__tests__/npc-offscreen.test.ts
  - backend/src/engine/__tests__/prompt-assembler.character-identity.test.ts
  - backend/src/engine/__tests__/reflection-agent.test.ts
  - backend/src/engine/__tests__/reflection-agent.identity-boundaries.test.ts
autonomous: true
requirements:
  - CHARF-01
must_haves:
  truths:
    - "Runtime narration, NPC planning, and off-screen NPC simulation read richer identity truth before they read persona/tags/goals shorthand."
    - "Key/canonical characters keep stronger continuity and inertia in runtime prompts without being frozen into rigid scripts."
    - "Ordinary reflection updates live campaign dynamics first, while deeper identity change requires explicit earned promotion instead of a trivial one-turn overwrite."
  artifacts:
    - path: "backend/src/engine/prompt-assembler.ts"
      provides: "Scene-facing prompt context grounded in richer character identity layers"
      contains: "baseFacts|behavioralCore|liveDynamics|continuity"
    - path: "backend/src/engine/npc-agent.ts"
      provides: "NPC decision prompts driven from richer identity truth and continuity constraints"
      contains: "goals|beliefs|pressure|continuity"
    - path: "backend/src/engine/npc-offscreen.ts"
      provides: "Off-screen NPC simulation that updates from the richer identity model"
      contains: "hydrateStoredNpcRecord|goalProgress|identity"
    - path: "backend/src/engine/reflection-agent.ts"
      provides: "Reflection logic that defaults to mutating live dynamics, not stable identity truth"
      contains: "Recent evidence|liveDynamics|earned"
    - path: "backend/src/engine/__tests__/prompt-assembler.character-identity.test.ts"
      provides: "Regression coverage that runtime prompt assembly includes richer identity slices"
      contains: "behavioral core|continuity|live dynamics"
    - path: "backend/src/engine/__tests__/reflection-agent.identity-boundaries.test.ts"
      provides: "Regression coverage for continuity boundaries and earned identity change"
      contains: "live dynamics|promotion|identity boundaries"
  key_links:
    - from: "backend/src/character/runtime-tags.ts"
      to: "backend/src/engine/prompt-assembler.ts"
      via: "derived tags remain compatibility shorthand generated from the richer identity model"
      pattern: "deriveRuntimeCharacterTags"
    - from: "backend/src/character/record-adapters.ts"
      to: "backend/src/engine/npc-agent.ts"
      via: "runtime NPC planning hydrates the richer record before prompt assembly"
      pattern: "hydrateStoredNpcRecord"
    - from: "backend/src/engine/reflection-agent.ts"
      to: "backend/src/engine/reflection-tools.ts"
      via: "reflection tools enforce mutable live-dynamics writes and guarded deeper promotion"
      pattern: "set_belief|set_goal|relationship|promotion"
---

<objective>
Move live runtime behavior onto the richer identity model and enforce continuity boundaries.

Purpose: CHARF-01 is only real if prompt assembly, NPC reasoning, off-screen updates, and reflection all consume the stronger identity truth instead of shallow summaries.
Output: runtime prompt/agent/reflection code and tests migrated to identity-first behavior.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-CONTEXT.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-RESEARCH.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-VALIDATION.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-01-PLAN.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-02-PLAN.md
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/npc-agent.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/engine/reflection-agent.ts

<interfaces>
From `backend/src/engine/prompt-assembler.ts`:
```ts
export async function assemblePrompt(
  options: AssembleOptions,
): Promise<AssembledPrompt>;

export async function assembleFinalNarrationPrompt(options: {
  campaignId: string;
  contextWindow: number;
  sceneAssembly: SceneAssembly;
  actionResult?: AssembleOptions["actionResult"];
  embedderResult?: ResolveResult;
  playerAction?: string;
  judgeRole?: ResolvedRole;
}): Promise<FinalNarrationPrompt>;
```

From `backend/src/engine/npc-agent.ts`:
```ts
export async function tickNpcAgent(
  campaignId: string,
  npcId: string,
  tick: number,
  judgeProvider: ProviderConfig,
  embedderProvider?: ProviderConfig,
): Promise<NpcTickResult>;
```

From `backend/src/engine/reflection-agent.ts`:
```ts
export async function runReflection(
  campaignId: string,
  npcId: string,
  tick: number,
  judgeProvider: ProviderConfig,
  embedderProvider?: ProviderConfig,
): Promise<ReflectionResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 48-03-01: Rewire prompt assembly and NPC runtime planning around richer identity truth</name>
  <files>backend/src/character/runtime-tags.ts, backend/src/engine/prompt-assembler.ts, backend/src/engine/npc-agent.ts, backend/src/engine/npc-offscreen.ts, backend/src/engine/__tests__/npc-agent.test.ts, backend/src/engine/__tests__/npc-offscreen.test.ts, backend/src/engine/__tests__/prompt-assembler.character-identity.test.ts</files>
  <behavior>
    - Test 1: prompt assembly includes richer identity/context slices that describe what makes a character distinct, not just persona plus goals.
    - Test 2: NPC planning prompts read behavioral core, current live dynamics, and continuity/inertia guidance for key/canonical characters.
    - Test 3: off-screen NPC updates keep using the richer record and do not regress to tag/goals-only reasoning.
    - Test 4: derived tags remain compatibility shorthand generated from richer identity rather than re-becoming prompt truth.
  </behavior>
  <action>Per D-02, D-03, D-04, D-05, D-06, and D-15, rewire `prompt-assembler.ts`, `npc-agent.ts`, and `npc-offscreen.ts` so runtime behavior is identity-first. Surface the richer identity model in prompt sections: base facts, enduring behavioral core, active live dynamics, and continuity/fidelity cues for key/canonical characters. Keep `persona`, `tags`, and flattened goal lists only as compact compatibility shorthand. Update `runtime-tags.ts` if needed so derived tags continue to reflect the richer model without becoming a second writable truth lane. Add focused tests in `prompt-assembler.character-identity.test.ts` plus updates to the existing NPC runtime suites so this migration stays locked.</action>
  <verify>
    <automated>npx vitest run backend/src/engine/__tests__/npc-agent.test.ts backend/src/engine/__tests__/npc-offscreen.test.ts backend/src/engine/__tests__/prompt-assembler.character-identity.test.ts</automated>
  </verify>
  <done>Runtime narration and NPC planning are now grounded in the richer identity model, with canonical continuity cues explicitly present where needed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 48-03-02: Enforce live-dynamics-first reflection and earned deeper identity change</name>
  <files>backend/src/engine/reflection-agent.ts, backend/src/engine/reflection-tools.ts, backend/src/engine/__tests__/reflection-agent.test.ts, backend/src/engine/__tests__/reflection-agent.identity-boundaries.test.ts</files>
  <behavior>
    - Test 1: ordinary reflection updates land in the mutable live-dynamics layer instead of rewriting stable facts or core identity immediately.
    - Test 2: deeper identity shifts require explicit evidence or promotion logic and cannot be caused by one trivial scene.
    - Test 3: key/canonical continuity rules add inertia against shallow drift while still allowing earned change over time.
  </behavior>
  <action>Update reflection so it honors D-09 and D-10 explicitly. `reflection-agent.ts` and `reflection-tools.ts` should default to mutating the live campaign dynamics layer and only promote deeper identity changes when evidence is material and accumulated. Keep the existing reflection seam; do not invent a separate drift engine. Make the boundary explicit in tests by adding `reflection-agent.identity-boundaries.test.ts` and tightening the existing reflection regression coverage. The result should allow growth, damage, and reorientation without permitting instant personality rewrites from one weak event.</action>
  <verify>
    <automated>npx vitest run backend/src/engine/__tests__/reflection-agent.test.ts backend/src/engine/__tests__/reflection-agent.identity-boundaries.test.ts</automated>
  </verify>
  <done>Reflection preserves continuity by treating stable identity as durable truth and live dynamics as the normal mutation surface.</done>
</task>

</tasks>

<verification>
Run the prompt, NPC, off-screen, and reflection suites together to prove the richer identity model reaches actual runtime behavior and that continuity boundaries are enforced in code.
</verification>

<success_criteria>
Key/canonical, imported, and ordinary characters now behave from a richer runtime identity model, and live drift is bounded to earned changes rather than prompt noise.
</success_criteria>

<output>
After completion, create `.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-03-SUMMARY.md`
</output>
