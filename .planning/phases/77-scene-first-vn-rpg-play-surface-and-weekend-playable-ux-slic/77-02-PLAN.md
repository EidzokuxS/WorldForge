---
phase: 77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic
plan: 02
type: execute
wave: 2
depends_on:
  - "77-01"
files_modified:
  - frontend/components/game/play-surface/game-scene-shell.tsx
  - frontend/components/game/play-surface/scene-backdrop.tsx
  - frontend/components/game/play-surface/scene-hud.tsx
  - frontend/components/game/play-surface/stage-overlay.tsx
  - frontend/components/game/play-surface/narration-dock.tsx
  - frontend/components/game/play-surface/__tests__/game-scene-shell.test.tsx
  - frontend/components/game/play-surface/__tests__/narration-dock.test.tsx
autonomous: true
requirements:
  - P77-R1
  - P77-R2
  - P77-R6
must_haves:
  truths:
    - "A reusable scene-first shell can render place-first `/game` structure without permanent admin columns."
    - "`NarrationDock` displays one current beat with `Next`, `Auto`, and `Log` controls as local callbacks."
    - "The shell follows the UI-SPEC dark scene-first target and rejects the warm paper/editorial visual direction."
  artifacts:
    - path: "frontend/components/game/play-surface/game-scene-shell.tsx"
      provides: "Full-viewport scene shell composition"
      exports: ["GameSceneShell"]
    - path: "frontend/components/game/play-surface/scene-backdrop.tsx"
      provides: "Place-first visual layer"
      exports: ["SceneBackdrop"]
    - path: "frontend/components/game/play-surface/scene-hud.tsx"
      provides: "Thin game HUD overlay"
      exports: ["SceneHUD"]
    - path: "frontend/components/game/play-surface/stage-overlay.tsx"
      provides: "Persistent beat-scoped scene signals/effects"
      exports: ["StageOverlay"]
    - path: "frontend/components/game/play-surface/narration-dock.tsx"
      provides: "Current-beat VN/RPG narration reader"
      exports: ["NarrationDock"]
  key_links:
    - from: "frontend/components/game/play-surface/narration-dock.tsx"
      to: "frontend/lib/display-beats.ts"
      via: "DisplayBeat[] props"
      pattern: "DisplayBeat"
    - from: "frontend/components/game/play-surface/game-scene-shell.tsx"
      to: "frontend/app/game/page.tsx"
      via: "page shell replacement"
      pattern: "game-scene-shell"
---

<objective>
Build reusable scene-shell and beat-reader components without moving `/game` state yet.

Purpose: The central visual shell and local cadence controls need to be independently testable before the high-risk `GamePage` render tree replacement.
Output: `GameSceneShell`, `SceneBackdrop`, `SceneHUD`, `NarrationDock`, and component tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-CONTEXT.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-UI-SPEC.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-RESEARCH.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-PATTERNS.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-01-SUMMARY.md
@frontend/components/game/rich-text-message.tsx
@frontend/lib/display-beats.ts
@frontend/components/game/play-surface/types.ts
</context>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Visible place first, staged latest beat second | Shell/backdrop/HUD/narration components provide the default layout target |
| REQ | P77-R1 | `GameSceneShell`, `SceneBackdrop`, and `SceneHUD` implement the scene-first shell contract |
| REQ | P77-R2 | `NarrationDock` exposes local Next/Auto/Log controls over DisplayBeat props |
| REQ | P77-R6 | Mechanic beats can render in `NarrationDock` as fiction-facing beats |
| RESEARCH | Do not copy Opus warm paper palette | Component tests assert dark scene-first classes/data hooks and no cream/paper copy |
| CONTEXT | Marinara flow reference, not skin | Uses flow functions only: backdrop, bottom dock, local beat controls |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scene shell, backdrop, and HUD components</name>
  <files>frontend/components/game/play-surface/game-scene-shell.tsx, frontend/components/game/play-surface/scene-backdrop.tsx, frontend/components/game/play-surface/scene-hud.tsx, frontend/components/game/play-surface/__tests__/game-scene-shell.test.tsx</files>
  <behavior>
    - RED: `GameSceneShell` renders test hooks `game-scene-shell`, `scene-backdrop`, `scene-hud`, `narration-dock`, `action-dock`, `widget-rail`, and `drawer-host` when provided.
    - RED: The shell is viewport-bound (`h-dvh` or equivalent), dark, overflow-contained, and does not create permanent left/right admin columns.
    - RED: `SceneBackdrop` shows concrete place cues: scene name, broad location, and at least one mood/weather/time/detail string when supplied.
    - RED: `SceneBackdrop` renders a concrete stage composition: environment layer, floor/anchor plane, at least one prop/silhouette/actor marker slot, and optional image/background URL. It must not be an empty dark rectangle or pure gradient.
    - RED: `StageOverlay` renders beat-scoped signals/effects (`side_remark`, `whisper`, `glitch`, `flash`, `fade`, `screen_shake` class hooks) and keeps them visible until `Next` or a new backend turn boundary clears them.
    - RED: Wide desktop shell uses stage-attached side info/widgets so left/right negative space reads as stage/context, not empty dead bands.
    - RED: Shell/backdrop tests fail if the rendered default copy includes cream/paper/editorial wording.
  </behavior>
  <action>
    Create `SceneBackdrop` with props for scene name, broad location, location description/tags, mood/weather/time labels, and optional portrait/background URL. Use CSS/stylized visual layers only; do not add image-generation pipeline work. The first slice must read as a place, not a blank gradient.

    Create `SceneHUD` with player-facing status labels `Scene loading`, `Reading`, `Auto`, `Thinking`, `Settling`, and `Ready`. Include slots/callbacks for Home, Saves, and Settings icon buttons but do not navigate directly inside the component.

    Create `StageOverlay` as a visual layer above `SceneBackdrop` and below the dock. It receives `StageSignal[]`, current beat id, and clear/advance callbacks from the parent. Normal dialogue stays in `NarrationDock`; stage overlay is for persistent side signals, environmental effects, actor callouts tied to scene positions, and visual transitions. It must not auto-disappear on a timer before the player presses `Next`.

    Create `GameSceneShell` as the full-viewport root that composes backdrop, StageOverlay, HUD, presence slot, narration slot, action slot, widget rail slot, and drawer host slot. Use UI-SPEC dark neutral/blood-accent constraints. Do not include permanent Location/Character/Lore/Oracle columns. On wide viewports, provide named slots for left/right stage widgets such as player mini-card, scene map hint, visible actors, or current pressure so the composition does not collapse into a centered dock with empty black sides.

    These are new component symbols. If you choose to edit existing components while doing this task, run `gitnexus_impact` on each existing symbol before edits and report risk.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- run components/game/play-surface/__tests__/game-scene-shell.test.tsx</automated>
  </verify>
  <acceptance_criteria>Scene shell components render stable test hooks, place-first content, and dark game-surface hierarchy without permanent sidebars.</acceptance_criteria>
  <done>Scene shell target is implemented and independently testable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create NarrationDock with local cadence controls</name>
  <files>frontend/components/game/play-surface/narration-dock.tsx, frontend/components/game/play-surface/__tests__/narration-dock.test.tsx</files>
  <behavior>
    - RED: `NarrationDock` renders only the current beat by default, not the full message history.
    - RED: Clicking `Next` calls only `onNextBeat`; it does not require backend/API mocks.
    - RED: Toggling `Auto` calls only `onToggleAuto`; autoplay state is visible as `Auto`.
    - RED: Clicking `Log` calls only `onOpenLog`.
    - RED: A `mechanical_result` beat renders a fiction-facing result label and not `Chance`, `Roll`, JSON, or raw reasoning.
    - RED: No visible chunk/beat counter such as `chunk 1/3`, `beat 2/5`, or debug progress appears in the player-facing dock.
    - RED: `Continue` is not rendered inside `NarrationDock`; it belongs to `ActionDock` near the raw input in Plan 77-03.
  </behavior>
  <action>
    Create `NarrationDock` with props for `beats`, `currentBeatIndex`, `isAutoPlaying`, `onNextBeat`, `onToggleAuto`, `onOpenLog`, and optional `isBusy`/status copy. Use `RichTextMessage` for prose-bearing beat text. Use lucide icons where available and accessible labels/tooltips for icon controls.

    Keep `Next`, `Auto`, and `Log` purely callback-driven. Do not import API helpers. Do not label the log control as implementation language. Use UI-SPEC copy: `Next`, `Auto`, and `Log`.

    Mechanic beats must say player-facing labels such as `Clean success`, `Costly success`, `Miss`, `Bad break`, or `Close call`; raw chance/roll/reasoning stays out of this component.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- run components/game/play-surface/__tests__/narration-dock.test.tsx</automated>
    <automated>npm --prefix frontend run test -- run components/game/play-surface/__tests__/game-scene-shell.test.tsx components/game/play-surface/__tests__/narration-dock.test.tsx</automated>
  </verify>
  <acceptance_criteria>`NarrationDock` gives the VN/RPG cadence controls without backend turn side effects.</acceptance_criteria>
  <done>Beat-reader components are ready for `/game` wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DisplayBeat props -> visible UI | Local presentation data becomes player-facing text and must not reveal raw debug by default. |
| component callbacks -> parent page | Local controls communicate intent to parent state, not backend routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-02-01 | Information Disclosure | `NarrationDock` | mitigate | Component tests assert mechanic beats omit chance/roll/raw reasoning/JSON. |
| T-77-02-02 | Tampering | `NarrationDock` controls | mitigate | Next/Auto/Log are callback-only and import no transport helpers. |
| T-77-02-03 | Denial of Service | shell layout | mitigate | Stable viewport/min-height/overflow constraints prevent docks or drawers from resizing the whole page. |
</threat_model>

<verification>
Run the targeted shell and narration dock tests. Run `gitnexus_detect_changes({scope: "staged", repo: "WorldForge"})` before commit and confirm only new play-surface component symbols are affected.
</verification>

<success_criteria>
- Scene shell, backdrop, HUD, and narration dock exist with tests.
- Tests prove dark scene-first hierarchy, stable data hooks, local cadence controls, and debug containment for mechanic beats.
- `/game` production state and backend transport remain untouched in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-02-SUMMARY.md`.
</output>
