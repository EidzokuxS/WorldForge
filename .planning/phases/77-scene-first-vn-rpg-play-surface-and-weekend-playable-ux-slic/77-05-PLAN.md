---
phase: 77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic
plan: 05
type: execute
wave: 5
depends_on:
  - "77-04"
files_modified:
  - frontend/components/game/play-surface/presence-layer.tsx
  - frontend/components/game/play-surface/inspect-drawer.tsx
  - frontend/components/game/play-surface/__tests__/presence-layer.test.tsx
  - frontend/components/game/play-surface/__tests__/inspect-drawer.test.tsx
  - frontend/lib/display-beats.ts
  - frontend/lib/__tests__/display-beats.test.ts
  - frontend/app/game/page.tsx
  - frontend/app/game/__tests__/page.test.tsx
autonomous: true
requirements:
  - P77-R4
  - P77-R6
must_haves:
  truths:
    - "Visible/interactable actors are distinct from sensed nearby actors and off-screen anchors."
    - "Only visible/interactable actors can be selected from the scene layer."
    - "Oracle/mechanic outcomes are fiction-facing beats by default, with chance/roll/reasoning/JSON available only in Inspect."
    - "Default `/game` does not show raw debug terms such as Chance, Roll, Raw reasoning, JSON, scene-settling, payload, or Support Actions."
  artifacts:
    - path: "frontend/components/game/play-surface/presence-layer.tsx"
      provides: "Scene actor presence bands"
      exports: ["PresenceLayer"]
    - path: "frontend/components/game/play-surface/inspect-drawer.tsx"
      provides: "Optional mechanic/raw-detail inspection surface"
      exports: ["InspectDrawer"]
    - path: "frontend/lib/display-beats.ts"
      provides: "Mechanic/state beat enrichment"
      exports: ["deriveDisplayBeats"]
  key_links:
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/play-surface/presence-layer.tsx"
      via: "currentScene.clearNpcIds and awareness.hintSignals"
      pattern: "clearNpcIds"
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/play-surface/inspect-drawer.tsx"
      via: "lastOracleResult and latest debug reasoning"
      pattern: "lastOracleResult"
    - from: "frontend/lib/display-beats.ts"
      to: "frontend/components/game/play-surface/narration-dock.tsx"
      via: "fiction-facing mechanic beat"
      pattern: "mechanical_result"
---

<objective>
Add honest actor presence and mechanic/debug disclosure containment.

Purpose: The play surface must show who is actually present without turning same-area actors into targets, and it must make mechanics readable as game beats while keeping raw Oracle/debug data optional.
Output: `PresenceLayer`, `InspectDrawer`, mechanic beat hardening, and page/component tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-CONTEXT.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-UI-SPEC.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-RESEARCH.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-PATTERNS.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-04-SUMMARY.md
@frontend/app/game/page.tsx
@frontend/components/game/location-panel.tsx
@frontend/components/game/oracle-panel.tsx
@frontend/components/game/narrative-log.tsx
@frontend/lib/api-types.ts
@frontend/lib/display-beats.ts
</context>

<interfaces>
Existing world scene contract from `frontend/lib/api-types.ts`:
```typescript
export interface WorldCurrentScene {
  id: string | null;
  name: string | null;
  broadLocationId: string | null;
  broadLocationName: string | null;
  sceneNpcIds: string[];
  clearNpcIds: string[];
  awareness: {
    byNpcId: Record<string, WorldSceneAwarenessBand>;
    hintSignals: string[];
  };
}
```
</interfaces>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Actor presence and hidden debug/mechanics | PresenceLayer and InspectDrawer complete the player-facing distinction |
| REQ | P77-R4 | Clear actors, sensed hints, and off-screen anchors render as separate bands |
| REQ | P77-R6 | Oracle/dice/mechanic output is fiction-facing by default and raw only in Inspect |
| RESEARCH | Broad location is not immediate presence | Presence uses `currentScene.clearNpcIds` and awareness bands, not broad fallback for direct actor chips |
| CONTEXT | Actor presence must stay honest | Non-visible actors cannot be selected from the scene layer |
| CONTEXT | Debug is optional inspection | Inspect/debug remains closed by default and raw details are omitted from the normal scene |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render honest actor presence bands</name>
  <files>frontend/components/game/play-surface/presence-layer.tsx, frontend/components/game/play-surface/__tests__/presence-layer.test.tsx, frontend/app/game/page.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <behavior>
    - RED: Actors in `clearNpcIds` render as visible/interactable scene chips.
    - RED: Hint/sensed actors render as muted signals or count/copy, not direct target chips with their names unless backend explicitly supplies visible names.
    - RED: Same-broad/off-screen actors do not render in the scene layer as interactable chips.
    - RED: Clicking a visible actor opens the Character drawer scoped to that actor.
    - RED: Attempting to select a hinted/off-screen actor from the scene layer is impossible because no direct button exists.
    - RED: Character drawer selection is explicit: visible actor chip sets `selectedActorId`, rail `Character` opens the player character when no actor is selected.
    - RED: Stage signals and side remarks can appear as scene overlays without becoming selectable actors or disappearing before the current beat advances.
  </behavior>
  <action>
    Before editing `GamePage`, run `gitnexus_impact({target: "GamePage", direction: "upstream", repo: "WorldForge"})`. Also run impact for `getAuthoritativeSceneNpcs`, `getFallbackSceneNpcs`, and `buildScenePanelData` if you modify them.

    Create `PresenceLayer` with props for visible actors, sensed/hint signals, and optional selected actor id. It should render:
    - visible/interactable actors as foreground chips/buttons with accessible labels;
    - sensed nearby/hint signals as muted non-target cues;
    - no off-screen anchor actor chips in the scene layer.

    Update `GamePage` to derive visible actors from existing authoritative `npcsHere`/`currentScene.clearNpcIds` logic and pass hint signals from `scenePanelData`. Clicking a visible actor opens `activeDrawer="character"` and stores the selected actor id/name for the Character drawer header. Rail-opened `Character` uses the player character unless a visible actor was deliberately selected. Do not use broad-location NPC lists for direct scene chips.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- run components/game/play-surface/__tests__/presence-layer.test.tsx app/game/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>Scene presence visually distinguishes visible, sensed, and off-screen actors and preserves direct-interaction honesty.</acceptance_criteria>
  <done>PresenceLayer is wired into `/game` and covered by component/page tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move raw mechanics/debug into Inspect</name>
  <files>frontend/components/game/play-surface/inspect-drawer.tsx, frontend/components/game/play-surface/__tests__/inspect-drawer.test.tsx, frontend/lib/display-beats.ts, frontend/lib/__tests__/display-beats.test.ts, frontend/app/game/page.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <behavior>
    - RED: Default `/game` shows mechanic beats as `Clean success`, `Costly success`, `Miss`, `Bad break`, or `Close call`, not raw `Chance`/`Roll` fields.
    - RED: Opening Inspect shows Oracle chance, roll, outcome, and short reason when available.
    - RED: Raw reasoning and JSON/debug payload sections are absent unless the existing debug/raw reasoning setting allows them.
    - RED: Default `/game` query does not find `Raw reasoning`, `JSON`, `Chance`, `Roll`, `scene-settling`, `payload`, or `Support Actions`.
    - RED: The player-facing mechanic beat is understandable without opening Inspect.
    - RED: Inspect can show raw details without leaking `Chance`, `Roll`, provider traces, JSON, or payload copy into NarrationDock, ActionDock, SceneHUD, or PresenceLayer.
  </behavior>
  <action>
    Before editing existing symbols, run `gitnexus_impact({target: "deriveDisplayBeats", direction: "upstream", repo: "WorldForge"})` and `gitnexus_impact({target: "GamePage", direction: "upstream", repo: "WorldForge"})`. Stop and warn on HIGH or CRITICAL risk before proceeding. Also run impact for `OraclePanel` if you modify it directly instead of only reusing its data shape.

    Create `InspectDrawer` with tabs or sections for `Beat`, `Oracle`, `State`, `Events`, and optional `Debug`. Keep raw payloads/reasoning collapsed or absent unless the existing `settings.ui.showRawReasoning`/debug condition is true. Use UI-SPEC empty copy: `No mechanics for the current beat. Raw details appear here only when available.`

    Update `deriveDisplayBeats` so Oracle/mechanic data produces a fiction-facing `mechanical_result` beat with raw details attached for Inspect. Do not render chance/roll/reasoning in `NarrationDock`.

    Update `GamePage` DrawerHost Inspect content to render `InspectDrawer` with current beat, `lastOracleResult`, current status, latest debug reasoning, travel/state update summary, and any available raw data. Preserve `OraclePanel` as an implementation source only if useful; do not keep it visible in the default scene.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- run components/game/play-surface/__tests__/inspect-drawer.test.tsx lib/__tests__/display-beats.test.ts app/game/__tests__/page.test.tsx</automated>
    <automated>npm --prefix frontend run lint -- app/game/page.tsx components/game/play-surface/presence-layer.tsx components/game/play-surface/inspect-drawer.tsx lib/display-beats.ts</automated>
  </verify>
  <acceptance_criteria>Mechanics are readable as fiction-facing game beats by default, and raw audit details are contained in Inspect/debug affordances.</acceptance_criteria>
  <done>Presence and mechanic/debug containment satisfy P77-R4 and P77-R6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend currentScene -> visible targets | Backend awareness bands determine what the user can directly select. |
| backend Oracle/debug events -> player surface | Raw mechanics/debug data must not leak into the default play surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-05-01 | Information Disclosure | `PresenceLayer` | mitigate | Only `clearNpcIds` render as direct actor chips; hint/off-screen actors are non-target cues. |
| T-77-05-02 | Information Disclosure | `InspectDrawer` | mitigate | Raw reasoning/JSON are gated behind Inspect and existing debug setting. |
| T-77-05-03 | Tampering | actor selection | mitigate | Visible actor click only opens local Character drawer; it does not create a backend turn. |
| T-77-05-04 | Repudiation | mechanic summary | mitigate | Inspect retains raw Oracle details for audit while the default beat stays fiction-facing. |
</threat_model>

<verification>
Run targeted presence, inspect, display-beat, and page tests plus scoped lint. Before commit, run `gitnexus_detect_changes({scope: "staged", repo: "WorldForge"})` and record the frontend-only scope in the summary.
</verification>

<success_criteria>
- Visible/interactable, sensed nearby, and off-screen presence are distinct.
- Scene actor chips never imply broad-location co-presence.
- Oracle/mechanic details are fiction-facing by default and inspectable on demand.
- Default `/game` contains no raw debug labels.
</success_criteria>

<output>
After completion, create `.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-05-SUMMARY.md`.
</output>
