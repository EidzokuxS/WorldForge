---
phase: 77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic
plan: 03
type: execute
wave: 3
depends_on:
  - "77-01"
  - "77-02"
files_modified:
  - frontend/components/game/play-surface/action-dock.tsx
  - frontend/components/game/play-surface/__tests__/action-dock.test.tsx
  - frontend/app/game/use-game-play-surface-state.ts
  - frontend/app/game/page.tsx
  - frontend/app/game/__tests__/page.test.tsx
autonomous: true
requirements:
  - P77-R1
  - P77-R2
  - P77-R5
must_haves:
  truths:
    - "`/game` uses the scene shell, narration dock, and action dock as the first playable vertical slice."
    - "Visible `Continue` creates a backend turn through the existing action path while empty Send remains invalid."
    - "One raw narrative input and per-campaign draft persistence support freeform play without command syntax or action-category modes."
    - "`Next`, `Auto`, and `Log` stay local and do not call backend turn helpers."
  artifacts:
    - path: "frontend/components/game/play-surface/action-dock.tsx"
      provides: "Freeform action dock with raw narrative input, Send, Continue, and quick choices"
      exports: ["ActionDock"]
    - path: "frontend/app/game/page.tsx"
      provides: "First scene-shell `/game` wiring over existing runtime state"
      contains: "GameSceneShell"
    - path: "frontend/app/game/use-game-play-surface-state.ts"
      provides: "Extracted local presentation state for beats, draft, drawers, and stage signals"
      exports: ["useGamePlaySurfaceState"]
    - path: "frontend/app/game/__tests__/page.test.tsx"
      provides: "Page-level authority and draft persistence regressions"
      contains: "Continue"
  key_links:
    - from: "frontend/app/game/page.tsx"
      to: "frontend/lib/display-beats.ts"
      via: "derive DisplayBeat[] from messages/SSE state"
      pattern: "deriveDisplayBeats"
    - from: "frontend/app/game/page.tsx"
      to: "frontend/lib/use-campaign-draft.ts"
      via: "per-campaign controlled input"
      pattern: "useCampaignDraft"
    - from: "frontend/components/game/play-surface/action-dock.tsx"
      to: "frontend/app/game/page.tsx"
      via: "onSubmitAction and onContinue callbacks"
      pattern: "Continue"
---

<objective>
Wire the first playable `/game` scene-shell vertical slice.

Purpose: The user should reach a scene-first surface with staged text, freeform input, and first-class Continue before secondary drawers and deeper presence/mechanic polish are added.
Output: `ActionDock`, `/game` shell wiring, draft persistence, Continue backend turn path, and page tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-CONTEXT.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-UI-SPEC.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-RESEARCH.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-PATTERNS.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-01-SUMMARY.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-02-SUMMARY.md
@frontend/app/game/page.tsx
@frontend/app/game/__tests__/page.test.tsx
@frontend/components/game/quick-actions.tsx
@frontend/lib/api.ts
@frontend/lib/display-beats.ts
@frontend/lib/use-campaign-draft.ts
</context>

<interfaces>
Existing backend-turn callback contract in `frontend/app/game/page.tsx`:
```typescript
const submitAction = async (actionText: string) => {
  if (!actionText || isTurnBusy || !activeCampaign) return;
  const response = await chatAction(activeCampaign.id, actionText, actionText, "");
  ...
};
```

The duplicate `actionText` value in `chatAction(campaignId, playerAction, intent, method)` is legacy route compatibility only. Phase 77 must not present `intent` or `method` as product semantics, and must not add UI controls that classify the player's freeform action before the GM/LLM interprets it.

Existing API helper contract in `frontend/lib/api.ts`:
```typescript
export function chatAction(
  campaignId: string,
  playerAction: string,
  intent: string,
  method: string
): Promise<Response>;
```
</interfaces>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Player input third, with first-class Continue | `ActionDock` and `/game` wiring put freeform play and Continue in the bottom dock |
| REQ | P77-R1 | `/game` starts rendering through `GameSceneShell` instead of tri-column cockpit structure |
| REQ | P77-R2 | Page tests prove Next/Auto/Log do not call `chatAction`, `chatOpening`, `chatRetry`, `chatLookup`, or `parseTurnSSE` |
| REQ | P77-R5 | Action dock has one raw narrative input, Continue, and campaign draft persistence; no Act/Speak/Observe command taxonomy |
| RESEARCH | Keep `GamePage` as runtime state owner | `submitAction`, retry, undo, edit, opening, SSE parsing, and world refresh stay in `page.tsx` |
| CONTEXT | Send and Continue create backend turns | Visible `Continue` calls the same `submitAction`/`chatAction` route path as Send |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ActionDock controls</name>
  <files>frontend/components/game/play-surface/action-dock.tsx, frontend/components/game/play-surface/__tests__/action-dock.test.tsx</files>
  <behavior>
    - RED: Send calls `onSubmitAction` only when trimmed input is non-empty and not busy.
    - RED: `Continue` calls `onContinue` even when draft input is empty and not busy.
    - RED: Send and Continue are disabled while busy.
    - RED: Quick-choice chips call the supplied action callback only after parent supplies settled choices.
    - RED: `Continue` is placed with Send/input controls inside the bottom action lane, not isolated at a far screen edge.
    - RED: There is no visible `Speak`, `Act`, `Observe`, `Ask GM`, `intent`, `method`, or action-category mode in the dock.
  </behavior>
  <action>
    Create `ActionDock` using shadcn `Button`/`Textarea`, lucide Send icon, and UI-SPEC copy. The dock has one raw narrative input plus Send and visible `Continue`; it does not include required `Act`, `Speak`, `Observe`, or `Ask GM` modes. Primary CTA label is exactly `Continue`. Tooltip/accessibility copy follows `77-UI-SPEC.md`.

    Keep input controlled by props: `value`, `onChange`, `onSubmitAction`, `onContinue`, `disabled`, `isBusy`, `turnPhase`, and `quickActions`. Do not import backend API helpers. Preserve Enter-to-submit and Shift+Enter-newline behavior unless page tests already prove a different behavior.

    Use one natural-language input hint. Do not require command syntax, do not prefix the user's text with slash commands, and do not classify the draft into an intent before submit.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- run components/game/play-surface/__tests__/action-dock.test.tsx</automated>
  </verify>
  <acceptance_criteria>`ActionDock` supports freeform raw play, Send, Continue, and busy-state protection with no backend imports.</acceptance_criteria>
  <done>Action dock is independently tested and ready for page wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire scene shell, beat reader, draft persistence, and Continue into GamePage</name>
  <files>frontend/app/game/page.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <behavior>
    - RED: `/game` renders `game-scene-shell`, `scene-backdrop`, `narration-dock`, and `action-dock`.
    - RED: `Next`, `Auto`, and `Log` advance local display state or local drawer state and do not call backend turn helpers.
    - RED: Sending `Scout ahead` still calls `chatAction(fakeCampaign.id, "Scout ahead", "Scout ahead", "")`.
    - RED: Clicking visible `Continue` calls `chatAction(fakeCampaign.id, "Continue scene.", "Continue scene.", "")` through the existing turn path.
    - RED: Typing a draft, unmounting/remounting the same campaign, and re-rendering restores the draft from the campaign-scoped key.
    - RED: `GamePage` imports `CONTINUE_ACTION_PAYLOAD`; tests fail if the Continue payload literal is duplicated inline.
    - RED: `GamePage` no longer renders `[data-shell-region="game-columns"]` or equivalent permanent cockpit columns by default.
    - RED: Local beat index, autoplay, draft, active drawer, selected actor, and stage-signal state live in an extracted hook/helper rather than expanding the `GamePage` render body.
  </behavior>
  <action>
    Before editing `GamePage`, run `gitnexus_impact({target: "GamePage", direction: "upstream", repo: "WorldForge"})`. If impact is HIGH or CRITICAL, stop and report the blast radius before editing. Also run impact for any existing helper you modify, including `getAuthoritativeSceneNpcs`, `getFallbackSceneNpcs`, `buildScenePanelData`, or `submitAction` if GitNexus resolves them.

    First extract local presentation state into `frontend/app/game/use-game-play-surface-state.ts` or an equivalent small hook/helper owned by the `/game` route. This hook should contain beat index, autoplay, draft, active drawer, selected actor, and stage-signal lifecycle. It must not import backend transport helpers. Keep the old runtime data/SSE/backend state in `GamePage`.

    Update `frontend/app/game/page.tsx` to:
    - import `GameSceneShell`, `NarrationDock`, `ActionDock`, `deriveDisplayBeats`, and `useCampaignDraft`;
    - import `CONTINUE_ACTION_PAYLOAD` and use it for Continue tests and runtime calls;
    - keep backend ownership in `GamePage`: do not move `submitAction`, `submitLookup`, `requestOpeningScene`, `handleRetry`, `handleUndo`, `handleEdit`, or `refreshWorldData` into visual components;
    - derive `DisplayBeat[]` from `messages`, `turnPhase`, `sceneProgress`, `lastOracleResult`, `travelFeedback`, and `quickActions`;
    - keep `beatIndex` and `isAutoPlaying` as local React state;
    - reset/clamp beat index when the latest beat list changes;
    - wire `Next` and `Auto` to local state only;
    - wire `Log` to local drawer state only, even if the drawer UI itself is implemented in Plan 77-04;
    - replace the old tri-column first viewport with `GameSceneShell` plus the new narration/action docks;
    - preserve existing retry, undo, edit, lookup, quick-action buffering, finalizing, and travel feedback semantics.

    Add `handleContinueAction` that calls `submitAction(CONTINUE_ACTION_PAYLOAD)`. Keep empty Send invalid; only visible Continue may submit with empty draft. The exact `chatAction` payload must remain campaign addressed and use the existing `submitAction` path. Do not expose `intent`, `method`, or any action-category mode in the play UI. Explicitly remove the old `[data-shell-region="game-columns"]` first-viewport layout; if a compatibility wrapper remains, tests must prove it is not rendered by default.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- run app/game/__tests__/page.test.tsx components/game/play-surface/__tests__/action-dock.test.tsx components/game/play-surface/__tests__/narration-dock.test.tsx</automated>
    <automated>npm --prefix frontend run lint -- app/game/page.tsx components/game/play-surface/action-dock.tsx</automated>
  </verify>
  <acceptance_criteria>`/game` first viewport uses the scene-shell vertical slice; local cadence controls are local-only; Send/Continue use backend turn routes.</acceptance_criteria>
  <done>First playable `/game` scene-shell slice is wired and regression-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player input -> backend turn route | Untrusted text becomes a campaign-addressed turn only through Send or Continue. |
| local beat controls -> page state | Local presentation controls must not cross into backend route calls. |
| localStorage -> draft input | Persisted draft text rehydrates into the action field for the active campaign only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-03-01 | Tampering | `Next`/`Auto`/`Log` | mitigate | Page tests spy on chat helpers and prove these controls are local-only. |
| T-77-03-02 | Tampering | `Continue` | mitigate | Continue uses existing `submitAction` guard and campaign-addressed `chatAction` payload. |
| T-77-03-03 | Information Disclosure | draft persistence | mitigate | Draft hook from Plan 77-01 scopes storage by campaign id; page test proves same-campaign restore. |
| T-77-03-04 | Denial of Service | duplicate turns | mitigate | Busy state disables Send/Continue while `turnPhase` or `sceneProgress` is active. |
</threat_model>

<verification>
Run targeted page and component tests plus scoped lint. Before commit, run `gitnexus_detect_changes({scope: "staged", repo: "WorldForge"})` and record changed symbols/affected flows in the summary.
</verification>

<success_criteria>
- `/game` renders the first scene-shell vertical slice.
- `Next`, `Auto`, and `Log` are local-only.
- Send and Continue create backend turns through existing route helpers.
- Draft persistence is wired per campaign without backend changes.
</success_criteria>

<output>
After completion, create `.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-03-SUMMARY.md`.
</output>
