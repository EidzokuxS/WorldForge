---
phase: 77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/display-beats.ts
  - frontend/lib/__tests__/display-beats.test.ts
  - frontend/lib/use-campaign-draft.ts
  - frontend/lib/__tests__/use-campaign-draft.test.ts
  - frontend/components/game/play-surface/types.ts
autonomous: true
requirements:
  - P77-R2
  - P77-R5
  - P77-R6
must_haves:
  truths:
    - "Latest settled narration can be adapted into local DisplayBeat records without changing backend turn state."
    - "Per-campaign input drafts persist across drawer/open-close navigation and do not leak between campaigns."
    - "Mechanic/oracle data has a fiction-facing summary channel and a separate raw-detail channel for Inspect."
  artifacts:
    - path: "frontend/lib/display-beats.ts"
      provides: "Pure DisplayBeat adapter and presentation beat types"
      exports: ["CONTINUE_ACTION_PAYLOAD", "deriveDisplayBeats", "getInitialBeatIndex"]
    - path: "frontend/lib/use-campaign-draft.ts"
      provides: "Per-campaign draft persistence hook"
      exports: ["useCampaignDraft"]
    - path: "frontend/components/game/play-surface/types.ts"
      provides: "Shared play-surface prop/type contracts"
      exports: ["DisplayBeat", "DrawerKind", "AddressMode", "PresenceBand", "StageSignal"]
  key_links:
    - from: "frontend/lib/display-beats.ts"
      to: "frontend/lib/gameplay-text.ts"
      via: "existing message-kind helpers"
      pattern: "deriveGameMessageKind"
    - from: "frontend/lib/use-campaign-draft.ts"
      to: "frontend/app/game/page.tsx"
      via: "controlled input wiring"
      pattern: "campaignId"
    - from: "frontend/components/game/play-surface/types.ts"
      to: "frontend/components/game/play-surface/*"
      via: "shared local presentation contracts"
      pattern: "DisplayBeat"
---

<objective>
Create the frontend-only presentation contracts for Phase 77.

Purpose: The scene-first shell needs a safe local model for beat playback and draft persistence before the `/game` render tree is moved. These contracts preserve the backend authority decision: DisplayBeat and draft state are presentation/client state, not committed game truth.
Output: Pure DisplayBeat adapter, per-campaign draft hook, shared play-surface types, and unit tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@tasks/lessons.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-CONTEXT.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-UI-SPEC.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-RESEARCH.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-PATTERNS.md
@.planning/research/worldforge-design-lab-results.md
@frontend/lib/gameplay-text.ts
@frontend/components/game/oracle-panel.tsx
@frontend/app/game/page.tsx
</context>

<interfaces>
Existing contracts the executor should preserve:

From `frontend/components/game/oracle-panel.tsx`:
```typescript
export interface OracleResultData {
  chance: number;
  roll: number;
  outcome: "strong_hit" | "weak_hit" | "miss";
  reasoning: string;
}
```

From `frontend/lib/gameplay-text.ts`:
```typescript
export type GameMessageKind =
  | "narration"
  | "player"
  | "system"
  | "lookup"
  | "compare"
  | "mechanical"
  | "progress";

export function deriveGameMessageKind(role: ChatRole, content: string): GameMessageKind;
export function stripLookupPrefix(content: string): string;
```
</interfaces>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Staged latest beat and playable cadence over existing runtime state | Creates DisplayBeat and draft contracts used by later shell/action plans |
| REQ | P77-R2 | DisplayBeat adapter supports Next/Auto/Log as local presentation state |
| REQ | P77-R5 | Draft hook scopes input persistence per campaign |
| REQ | P77-R6 | Mechanic beats separate fiction-facing summary from raw Inspect details |
| RESEARCH | DisplayBeat should be frontend-only over existing messages/SSE data | `frontend/lib/display-beats.ts` is pure and imports no backend transport helper |
| CONTEXT | Backend authority stays intact | Tests assert the adapter/hook has no `chatAction`, `parseTurnSSE`, or API dependency |
| CONTEXT | Debug is optional inspection | Raw mechanic fields stay on `rawDetails`, not default text |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DisplayBeat adapter contract</name>
  <files>frontend/lib/display-beats.ts, frontend/lib/__tests__/display-beats.test.ts, frontend/components/game/play-surface/types.ts</files>
  <behavior>
    - RED: Latest assistant narration is split into 1-5 readable `narration`/`dialogue` beats using existing gameplay text helpers where possible.
    - RED: A progress state creates a `progress` beat with player-facing copy such as `Thinking`, `Settling`, or `Reading`, not raw SSE event names.
    - RED: An Oracle result creates a `mechanical_result` beat with labels `Clean success`, `Costly success`, or `Miss`, while `chance`, `roll`, and `reasoning` remain only in raw detail data.
    - RED: Travel feedback creates a `state_change` beat without inferring travel from the submitted action string.
    - RED: Quick actions create a `choice` beat only when supplied by settled quick-action state.
    - RED: A `side_remark` source creates a compact actor reaction beat that stays visually secondary to narration/dialogue.
    - RED: An `input_handoff` beat appears only when the turn is ready for player input and unlocks `ActionDock` without calling backend APIs.
    - RED: Stage signals/effects are presentation records that persist while the current beat is active and clear only on `Next` or a new backend turn boundary.
    - RED: `CONTINUE_ACTION_PAYLOAD` is exactly `"Continue scene."` and is the only source used by later Continue tests.
  </behavior>
  <action>
    Create `frontend/components/game/play-surface/types.ts` with exported local presentation types:
    `DisplayBeat`, `DisplayBeatKind`, `DrawerKind`, `AddressMode`, `PresenceBand`, `StageSignal`, and small supporting types for mechanic summaries, quick choices, and stage presentation signals. Keep these types frontend-only.

    Create `frontend/lib/display-beats.ts` with pure helpers. Recommended primary export:
    `deriveDisplayBeats(input: { messages; turnPhase; sceneProgress; oracleResult; travelFeedback; quickActions; sideRemarks?; stageSignals? }): DisplayBeat[]`.
    Use existing `ChatMessage` shape from `@worldforge/shared`, existing `deriveGameMessageKind`, and the existing `OracleResultData` type. Do not import `chatAction`, `parseTurnSSE`, `getWorldData`, or any backend API helper. Do not invent committed outcomes when data is absent; emit neutral progress/input-handoff beats instead.

    Export `CONTINUE_ACTION_PAYLOAD = "Continue scene."` from the same frontend presentation/transport boundary helper so every Continue implementation and test uses the same literal. The visible UI label remains `Continue`; this payload is route compatibility only.

    Before editing any existing source symbol, run `gitnexus_impact({target: "<symbol>", direction: "upstream", repo: "WorldForge"})`; for this task the expected source edits are new files only. If you decide to change `deriveGameMessageKind` or `OracleResultData`, run impact first and stop if risk is HIGH or CRITICAL.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- run lib/__tests__/display-beats.test.ts</automated>
  </verify>
  <acceptance_criteria>DisplayBeat tests prove local beat derivation, fiction-facing mechanics, and no backend transport dependency.</acceptance_criteria>
  <done>DisplayBeat adapter exists, is unit-tested, and preserves backend authority by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add per-campaign draft persistence</name>
  <files>frontend/lib/use-campaign-draft.ts, frontend/lib/__tests__/use-campaign-draft.test.ts, frontend/components/game/play-surface/types.ts</files>
  <behavior>
    - RED: A draft saved for `campaign-a` restores for `campaign-a` after remount.
    - RED: A draft saved for `campaign-a` does not appear for `campaign-b`.
    - RED: Passing `null` campaign id behaves as an in-memory controlled draft and does not write a global key.
    - RED: Clearing a submitted draft removes only that campaign's draft.
    - RED: If localStorage read/write throws or contains corrupt data, the hook falls back to in-memory draft state and does not crash render.
  </behavior>
  <action>
    Create `frontend/lib/use-campaign-draft.ts` exporting `useCampaignDraft(campaignId: string | null)`. Use `window.localStorage` only inside guarded browser effects so tests and SSR do not crash. Key drafts as `worldforge:game:draft:${campaignId}`. Return `{ draft, setDraft, clearDraft }` or an equivalent explicit contract.

    Keep the hook independent from drawer state. Drawer open/close will be tested later by wiring the hook above `DrawerHost`; this hook only guarantees campaign-scoped persistence.

    Do not add backend persistence and do not write campaign data through API calls. This implements P77-R5 as frontend draft continuity only.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- run lib/__tests__/use-campaign-draft.test.ts</automated>
    <automated>npm --prefix frontend run test -- run lib/__tests__/display-beats.test.ts lib/__tests__/use-campaign-draft.test.ts</automated>
  </verify>
  <acceptance_criteria>Draft state survives remount per campaign and cannot leak into another campaign.</acceptance_criteria>
  <done>Draft persistence contract is ready for `/game` wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend events -> DisplayBeat | Trusted settled backend data becomes untrusted UI presentation state that must not invent committed facts. |
| browser localStorage -> input UI | Per-campaign draft text persists locally and can leak if not keyed correctly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01-01 | Tampering | `deriveDisplayBeats` | mitigate | Pure adapter cannot call turn APIs and tests assert missing data creates neutral beats instead of fake outcomes. |
| T-77-01-02 | Information Disclosure | `useCampaignDraft` | mitigate | Key localStorage by campaign id and test cross-campaign isolation. |
| T-77-01-03 | Information Disclosure | mechanic raw details | mitigate | Raw chance/roll/reasoning remain in raw detail fields for Inspect; default labels are fiction-facing. |
</threat_model>

<verification>
Run the targeted frontend unit tests in both tasks. Before committing, run `gitnexus_detect_changes({scope: "staged", repo: "WorldForge"})` and confirm only the new frontend presentation/draft symbols are affected.
</verification>

<success_criteria>
- `DisplayBeat` and draft-persistence contracts exist in frontend-only files.
- Unit tests prove beat adapter, mechanic summary/raw split, and per-campaign draft isolation.
- No backend, shared package, API transport, or persistence file is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-01-SUMMARY.md`.
</output>
