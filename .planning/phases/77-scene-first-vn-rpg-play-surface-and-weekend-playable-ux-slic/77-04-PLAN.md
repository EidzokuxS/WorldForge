---
phase: 77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic
plan: 04
type: execute
wave: 4
depends_on:
  - "77-03"
files_modified:
  - frontend/components/game/play-surface/widget-rail.tsx
  - frontend/components/game/play-surface/drawer-host.tsx
  - frontend/components/game/play-surface/__tests__/drawer-host.test.tsx
  - frontend/app/game/page.tsx
  - frontend/app/game/__tests__/page.test.tsx
autonomous: true
requirements:
  - P77-R1
  - P77-R3
  - P77-R5
must_haves:
  truths:
    - "Log, World, Inventory, Journal, Character, Inspect, and Saves are opened as widgets/drawers rather than permanent columns."
    - "Opening and closing drawers preserves the action draft, current beat index, address mode, and scene state."
    - "Existing panels are reused as drawer data/content sources and no longer dominate the default play surface."
  artifacts:
    - path: "frontend/components/game/play-surface/widget-rail.tsx"
      provides: "Compact game tool rail"
      exports: ["WidgetRail"]
    - path: "frontend/components/game/play-surface/drawer-host.tsx"
      provides: "Single controlled drawer overlay host"
      exports: ["DrawerHost"]
    - path: "frontend/app/game/page.tsx"
      provides: "Drawer state and migrated panel homes"
      contains: "activeDrawer"
  key_links:
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/play-surface/drawer-host.tsx"
      via: "activeDrawer controlled prop"
      pattern: "activeDrawer"
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/location-panel.tsx"
      via: "World drawer content"
      pattern: "LocationPanel"
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/narrative-log.tsx"
      via: "Log drawer content"
      pattern: "NarrativeLog"
---

<objective>
Move secondary `/game` information into widgets and drawers.

Purpose: The default play screen must stop reading as a three-column admin cockpit while keeping the existing Log, World/Map, Character, Journal, Inventory, Inspect, and Saves surfaces reachable.
Output: `WidgetRail`, `DrawerHost`, migrated panel homes, drawer containment tests, and page tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-CONTEXT.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-UI-SPEC.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-RESEARCH.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-PATTERNS.md
@.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-03-SUMMARY.md
@frontend/app/game/page.tsx
@frontend/components/game/location-panel.tsx
@frontend/components/game/character-panel.tsx
@frontend/components/game/lore-panel.tsx
@frontend/components/game/narrative-log.tsx
@frontend/components/game/oracle-panel.tsx
@frontend/components/game/checkpoint-panel.tsx
</context>

<source_audit>
| Source | Item | Coverage In This Plan |
|--------|------|-----------------------|
| GOAL | Drawers/widgets for map/inventory/journal/inspect | Widget rail and DrawerHost make all secondary surfaces overlay-based |
| REQ | P77-R1 | Default `/game` no longer has permanent Location/Character/Lore/Oracle sidebars |
| REQ | P77-R3 | Log, World/Map, Character, Journal, Inventory, Inspect, and Saves have drawer/widget homes |
| REQ | P77-R5 | Drawer open/close preserves draft and selected address mode |
| RESEARCH | Existing panels should be reused as migrated drawers | Page wiring passes existing panel components as drawer content instead of rewriting data contracts |
| CONTEXT | Debug optional inspection | Inspect exists as a drawer, closed by default |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build WidgetRail and DrawerHost containment</name>
  <files>frontend/components/game/play-surface/widget-rail.tsx, frontend/components/game/play-surface/drawer-host.tsx, frontend/components/game/play-surface/__tests__/drawer-host.test.tsx</files>
  <behavior>
    - RED: Widget buttons for `Log`, `World`, `Inventory`, `Journal`, `Character`, `Inspect`, and `Saves` call `onOpenDrawer` with the expected drawer kind.
    - RED: Active drawer state is visually indicated without making every widget use the accent color.
    - RED: `DrawerHost` renders one overlay at a time and closes on the supplied close control.
    - RED: Drawer content scrolls inside the drawer and does not remount the action dock.
    - RED: Drawer labels use player-facing names, not `World Lore`, `Support Actions`, raw event names, or implementation labels.
    - RED: `WidgetRail` can render as a stage-attached compact rail without covering the narration dock, actor chips, or current player action controls at `2560x1440`, `1920x1080`, and mobile widths.
    - RED: `Character` drawer accepts a selected visible actor contract and falls back to the player character when no actor is selected.
    - RED: Inactive rail icons use restrained neutral styling; accent color is reserved for active, alerting, or stateful items.
  </behavior>
  <action>
    Create `WidgetRail` with lucide icons and accessible labels/tooltips. Required drawer names are exactly `Log`, `World`, `Inventory`, `Journal`, `Character`, `Inspect`, and `Saves`. Use active accent only for the active drawer.

    Create `DrawerHost` as a controlled overlay built on existing Radix/shadcn dialog primitives or the local dialog pattern. Props should include `activeDrawer`, `onClose`, selected actor context for the Character drawer, and content slots for each drawer. Desktop width target is 420-560px; mobile behavior should be full-width panel/bottom-sheet compatible. Use `ScrollArea` inside drawer content.

    Do not introduce a new overlay dependency or third-party shadcn registry. Do not put drawer panels inside decorative page cards.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- run components/game/play-surface/__tests__/drawer-host.test.tsx</automated>
  </verify>
  <acceptance_criteria>WidgetRail and DrawerHost provide a single controlled overlay system with player-facing labels and stable containment.</acceptance_criteria>
  <done>Drawer infrastructure is ready for `/game` migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate current panels into drawers on GamePage</name>
  <files>frontend/app/game/page.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <behavior>
    - RED: Default `/game` does not render permanent `aside-left` or `aside-right` admin columns.
    - RED: `LocationPanel` content appears when `World` opens, not as a default sidebar.
    - RED: `CharacterPanel` content appears when `Character` opens, not as a default sidebar.
    - RED: `LorePanel` content appears under `Journal`, not as `World Lore` on the default page.
    - RED: `NarrativeLog` full scrollback appears under `Log`, while `NarrationDock` keeps the latest beat.
    - RED: Type a draft, open and close Log, World, Inventory, Journal, Character, Inspect, and Saves; the draft and address mode remain unchanged.
    - RED: The Character drawer receives a selected visible actor contract (`selectedActorId` or player fallback) and does not silently show the wrong actor after an actor chip click.
    - RED: Migrating panels happens in two passes: Log/World/Character first, then Journal/Inventory/Inspect/Saves. Each pass has green tests before the next migration begins.
    - RED: The old `[data-shell-region="game-columns"]` layout is absent from the default play route.
    - RED: Drawer migration does not introduce duplicate permanent sidebars, duplicate player/campaign summaries, or fake player-facing fields.
  </behavior>
  <action>
    Before editing `GamePage`, run `gitnexus_impact({target: "GamePage", direction: "upstream", repo: "WorldForge"})`. Run impact for any existing component symbol you edit. If `LocationPanel`, `CharacterPanel`, `LorePanel`, `NarrativeLog`, `OraclePanel`, or `CheckpointPanel` need production edits, run impact first and report risk.

    Update `GamePage` to own `activeDrawer: DrawerKind | null` and `selectedActorId: string | null` through the extracted local play-surface hook from 77-03. Wire `WidgetRail` and `DrawerHost` into `GameSceneShell`.

    Migrate content homes in two verified slices to avoid turning `GamePage` into a one-pass rewrite. If tests fail after Slice A, stop and repair before moving on to Slice B:
    Slice A:
    - `Log`: render existing `NarrativeLog` with full `messages`, retry/undo/edit callbacks, and raw reasoning behavior unchanged.
    - `World`: render existing `LocationPanel` with current location, scene, paths, people, items, and `onMove`.
    - `Character`: render existing `CharacterPanel` with authoritative player fallback or selected visible actor details when available; actor selection must be explicit and test-covered.
    Slice B:
    - `Journal`: render existing `LorePanel` with campaign id.
    - `Inventory`: render carried and equipped item sections from `player.inventory` and `player.equipment`; do not derive inventory from `worldData.items`.
    - `Inspect`: provide a drawer slot that can render current Oracle/mechanic summary and raw details; Plan 77-05 will harden the default debug containment.
    - `Saves`: open the existing `CheckpointPanel` or render it through the drawer/modal path without changing checkpoint behavior.

    Remove the default permanent sidebars from the first viewport. Keep existing backend turn callbacks and data derivations in `GamePage`. If an existing panel is too coupled to move directly, wrap it in a thin adapter component instead of rewriting its internal behavior.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- run app/game/__tests__/page.test.tsx components/game/play-surface/__tests__/drawer-host.test.tsx</automated>
    <automated>npm --prefix frontend run lint -- app/game/page.tsx components/game/play-surface/widget-rail.tsx components/game/play-surface/drawer-host.tsx</automated>
  </verify>
  <acceptance_criteria>All secondary surfaces are reachable from widgets/drawers, and drawer navigation preserves draft, beat state, address mode, and scene state.</acceptance_criteria>
  <done>Current panel data survives as drawers/widgets rather than permanent columns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget click -> drawer state | Local UI navigation opens read-only or action-capable secondary surfaces. |
| drawer content -> backend actions | Existing movement/checkpoint/retry actions remain controlled by existing callbacks and busy guards. |
| drawer navigation -> draft state | Drawer open/close must not erase unsubmitted player input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04-01 | Information Disclosure | Inspect drawer | mitigate | Inspect is closed by default; Plan 77-05 hardens raw debug visibility. |
| T-77-04-02 | Tampering | World drawer travel controls | mitigate | Travel still calls existing `handleMove`/`submitAction` busy-guarded path. |
| T-77-04-03 | Information Disclosure | Inventory drawer | mitigate | Inventory uses authoritative player carried/equipped collections only. |
| T-77-04-04 | Denial of Service | drawer overlay | mitigate | Single controlled drawer host prevents stacked modal/drawer conflicts. |
</threat_model>

<verification>
Run targeted drawer and page tests plus scoped lint. Before commit, run `gitnexus_detect_changes({scope: "staged", repo: "WorldForge"})` and record that changes stay within frontend presentation/page symbols.
</verification>

<success_criteria>
- Default `/game` has no permanent Location/Character/Lore/Oracle sidebars.
- Log, World, Inventory, Journal, Character, Inspect, and Saves are reachable through widgets/drawers.
- Existing panel data and callbacks remain intact.
- Drawer navigation preserves the input draft and local scene state.
</success_criteria>

<output>
After completion, create `.planning/phases/77-scene-first-vn-rpg-play-surface-and-weekend-playable-ux-slic/77-04-SUMMARY.md`.
</output>
