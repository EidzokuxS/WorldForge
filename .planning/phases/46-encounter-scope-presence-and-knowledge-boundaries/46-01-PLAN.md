---
phase: 46-encounter-scope-presence-and-knowledge-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/engine/__tests__/scene-presence.test.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
  - backend/src/engine/__tests__/npc-agent.test.ts
  - backend/src/engine/__tests__/npc-offscreen.test.ts
  - backend/src/routes/__tests__/chat.test.ts
  - frontend/app/game/__tests__/page.test.tsx
  - frontend/components/game/__tests__/location-panel.test.tsx
autonomous: true
requirements:
  - SCEN-02
must_haves:
  truths:
    - "Phase 46 starts from explicit failing coverage for the current same-location leak instead of relying on prose complaints."
    - "The repo has a dedicated regression for the contract that real presence, player awareness, and NPC knowledge are different layers."
    - "The repo has a regression showing that actors can be present in the same broad location without being part of the same immediate scene."
    - "The repo has a regression showing that hidden-yet-present actors can still matter without being fully revealed."
    - "The repo has a regression showing that `/game` must not list every NPC in the same broad location as 'People Here'."
  artifacts:
    - path: "backend/src/engine/__tests__/scene-presence.test.ts"
      provides: "shared encounter-scope resolver contract coverage"
      contains: "presence|awareness|knowledge basis"
    - path: "backend/src/engine/__tests__/prompt-assembler.test.ts"
      provides: "final narration and hidden-pass encounter-scope filtering coverage"
      contains: "encounter scope|awareness|hint"
    - path: "backend/src/engine/__tests__/npc-agent.test.ts"
      provides: "NPC nearby-entity and recognition-boundary coverage"
      contains: "nearby entities|encounter scope|knowledge basis"
    - path: "backend/src/engine/__tests__/npc-offscreen.test.ts"
      provides: "off-screen routing coverage keyed to local scene scope rather than broad location membership"
      contains: "scene scope|off-screen"
    - path: "frontend/components/game/__tests__/location-panel.test.tsx"
      provides: "scene-scoped 'People Here' regression coverage"
      contains: "People Here|scene"
  key_links:
    - from: "backend/src/engine/__tests__/scene-presence.test.ts"
      to: "backend/src/engine/__tests__/npc-agent.test.ts"
      via: "the shared resolver defines the actor-routing contract consumed by NPC settlement"
      pattern: "presence|awareness|knowledge basis"
    - from: "backend/src/engine/__tests__/prompt-assembler.test.ts"
      to: "frontend/components/game/__tests__/location-panel.test.tsx"
      via: "backend scene truth and /game visible presence must agree on who is actually here"
      pattern: "encounter scope|People Here"
---

<objective>
Turn the Phase 46 gameplay complaints into failing tests before changing runtime behavior.

Purpose: lock the real contract in code so later implementation must solve scene scope, awareness, and knowledge boundaries across backend and `/game`, not just improve wording.
Output: red regressions that describe SCEN-02 precisely.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-CONTEXT.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-RESEARCH.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-VALIDATION.md
@backend/src/engine/scene-assembly.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/npc-agent.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/routes/chat.ts
@frontend/app/game/page.tsx
@frontend/components/game/location-panel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing backend regressions for shared scene scope, hidden presence, and justified knowledge</name>
  <read_first>
    .planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-CONTEXT.md
    .planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-RESEARCH.md
    backend/src/engine/scene-assembly.ts
    backend/src/engine/prompt-assembler.ts
    backend/src/engine/npc-agent.ts
    backend/src/engine/npc-offscreen.ts
    backend/src/routes/chat.ts
    backend/src/engine/__tests__/prompt-assembler.test.ts
    backend/src/engine/__tests__/npc-agent.test.ts
    backend/src/engine/__tests__/npc-offscreen.test.ts
    backend/src/routes/__tests__/chat.test.ts
  </read_first>
  <files>backend/src/engine/__tests__/scene-presence.test.ts, backend/src/engine/__tests__/prompt-assembler.test.ts, backend/src/engine/__tests__/npc-agent.test.ts, backend/src/engine/__tests__/npc-offscreen.test.ts, backend/src/routes/__tests__/chat.test.ts</files>
  <acceptance_criteria>
    - `rg -n "presence|awareness|knowledge basis|encounter scope|hidden but present" backend/src/engine/__tests__/scene-presence.test.ts backend/src/engine/__tests__/prompt-assembler.test.ts backend/src/engine/__tests__/npc-agent.test.ts backend/src/engine/__tests__/npc-offscreen.test.ts backend/src/routes/__tests__/chat.test.ts` finds new Phase 46 regressions.
    - `npm --prefix backend exec vitest run src/engine/__tests__/scene-presence.test.ts src/engine/__tests__/prompt-assembler.test.ts src/engine/__tests__/npc-agent.test.ts src/engine/__tests__/npc-offscreen.test.ts src/routes/__tests__/chat.test.ts` fails because encounter scope is not wired yet.
  </acceptance_criteria>
  <action>Add failing backend regressions that pin the new contract. Introduce a dedicated `scene-presence.test.ts` that describes a shared resolver output separating who is physically in the local scene, what the player can notice, and what an NPC is justified in recognizing. Extend prompt-assembler tests so broad-location actors no longer automatically enter the visible scene or hidden-pass prompt as fully present. Extend NPC-agent tests so `Nearby entities` is filtered by immediate scene scope and by justified recognition rather than broad location membership. Extend off-screen tests so actors in the same macro location but outside the immediate encounter still route through off-screen or non-local handling. Extend route/turn sequencing tests so pre-visible settlement still works once present-scene membership narrows. Keep the tests concrete enough that a broad-location implementation stays red.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/scene-presence.test.ts src/engine/__tests__/prompt-assembler.test.ts src/engine/__tests__/npc-agent.test.ts src/engine/__tests__/npc-offscreen.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>Backend regressions force the runtime to distinguish scene presence, player awareness, and justified NPC knowledge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add failing `/game` regressions for scene-scoped visible presence</name>
  <read_first>
    .planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-CONTEXT.md
    .planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-RESEARCH.md
    frontend/app/game/page.tsx
    frontend/components/game/location-panel.tsx
    frontend/app/game/__tests__/page.test.tsx
  </read_first>
  <files>frontend/app/game/__tests__/page.test.tsx, frontend/components/game/__tests__/location-panel.test.tsx</files>
  <acceptance_criteria>
    - `rg -n "People Here|encounter scope|same broad location" frontend/app/game/__tests__/page.test.tsx frontend/components/game/__tests__/location-panel.test.tsx` finds new Phase 46 regressions.
    - `npm --prefix frontend exec vitest run app/game/__tests__/page.test.tsx components/game/__tests__/location-panel.test.tsx` fails because `/game` still derives presence from broad location membership.
  </acceptance_criteria>
  <action>Add failing frontend regressions that prove the current `/game` experience is wrong when a large location contains several different local scenes. Cover both page-level derived state and the location panel presentation so the eventual fix cannot hide behind backend-only changes. The new tests should prove that “People Here” means immediate scene presence, not everyone whose `currentLocationId` matches the player’s broad location.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run app/game/__tests__/page.test.tsx components/game/__tests__/location-panel.test.tsx</automated>
  </verify>
  <done>Frontend regressions pin the product rule that large locations are not one shared room.</done>
</task>

</tasks>

<verification>
Run both targeted suites and confirm they fail for the right reasons: the code still treats broad location membership as immediate presence and over-shares actor knowledge.
</verification>

<success_criteria>
Phase 46 begins from red tests that describe the real encounter-scope bug instead of relying on subjective playtest notes alone.
</success_criteria>

<output>
After completion, create `.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-01-SUMMARY.md`
</output>
