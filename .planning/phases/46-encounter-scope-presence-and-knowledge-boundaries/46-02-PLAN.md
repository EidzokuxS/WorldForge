---
phase: 46-encounter-scope-presence-and-knowledge-boundaries
plan: 02
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - backend/src/db/schema.ts
  - backend/drizzle/
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/tool-executor.ts
  - backend/src/engine/npc-tools.ts
  - backend/src/engine/scene-presence.ts
  - backend/src/engine/scene-assembly.ts
  - backend/src/routes/chat.ts
  - backend/src/routes/campaigns.ts
  - backend/src/engine/__tests__/scene-presence.test.ts
  - backend/src/engine/__tests__/tool-executor.test.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/routes/__tests__/chat.test.ts
autonomous: true
requirements:
  - SCEN-02
must_haves:
  truths:
    - "The runtime now has one shared encounter-scope seam instead of several ad hoc same-location filters."
    - "Broad location membership and immediate local scene scope are different concepts in persisted/runtime state."
    - "Awareness and knowledge remain derived read-time layers; only local scene scope needs durable runtime backing."
    - "Legacy campaigns reopen safely by falling back to broad location when no narrower scene scope exists yet."
    - "Authoritative movement and arrival paths now set, clear, or realign local scene scope instead of leaving the new field inert."
    - "Phase 45 sequencing remains intact: local present-scene settlement still happens before final visible narration."
  artifacts:
    - path: "backend/src/engine/scene-presence.ts"
      provides: "shared encounter-scope, awareness, and knowledge-basis resolver"
      contains: "presence|awareness|knowledge basis|scene scope"
    - path: "backend/src/db/schema.ts"
      provides: "durable local scene-scope contract"
      contains: "scene_scope|current_scene"
    - path: "backend/src/routes/campaigns.ts"
      provides: "world-read compatibility for current scene scope"
      contains: "sceneScope|currentScene"
    - path: "backend/src/engine/tool-executor.ts"
      provides: "authoritative player movement and arrival synchronization for scene scope"
      contains: "scene scope|currentSceneLocationId"
  key_links:
    - from: "backend/src/db/schema.ts"
      to: "backend/src/engine/scene-presence.ts"
      via: "durable scene-scope field feeds the shared resolver with compatibility fallback"
      pattern: "scene scope"
    - from: "backend/src/engine/scene-presence.ts"
      to: "backend/src/routes/chat.ts"
      via: "present-scene routing must now consume the shared resolver"
      pattern: "presence|awareness"
    - from: "backend/src/engine/tool-executor.ts"
      to: "backend/src/engine/scene-presence.ts"
      via: "movement and arrival paths must keep durable scene scope aligned with later reads"
      pattern: "currentSceneLocationId|scene scope"
---

<objective>
Introduce the shared encounter-scope seam and the minimal durable data needed to distinguish a broad location from the immediate local scene.

Purpose: stop every downstream system from guessing scene membership from `currentLocationId`.
Output: one authoritative resolver and compatibility-safe scene-scope persistence.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-CONTEXT.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-RESEARCH.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-VALIDATION.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-01-PLAN.md
@backend/src/db/schema.ts
@backend/src/engine/scene-assembly.ts
@backend/src/routes/chat.ts
@backend/src/routes/campaigns.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add minimal durable scene-scope state with safe fallback for legacy campaigns</name>
  <read_first>
    .planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-RESEARCH.md
    backend/src/db/schema.ts
    backend/src/routes/chat.ts
    backend/src/routes/campaigns.ts
  </read_first>
  <files>backend/src/db/schema.ts, backend/drizzle/, backend/src/engine/turn-processor.ts, backend/src/engine/tool-executor.ts, backend/src/engine/npc-tools.ts, backend/src/routes/chat.ts, backend/src/routes/campaigns.ts, backend/src/engine/__tests__/tool-executor.test.ts, backend/src/engine/__tests__/turn-processor.test.ts</files>
  <acceptance_criteria>
    - `rg -n "sceneScope|currentSceneLocationId|current_scene_location_id" backend/src/db/schema.ts backend/src/routes/chat.ts backend/src/routes/campaigns.ts` finds the new durable contract and compatibility reads.
    - `rg -n "currentSceneLocationId|scene scope" backend/src/engine/turn-processor.ts backend/src/engine/tool-executor.ts backend/src/engine/npc-tools.ts` finds authoritative set/clear/sync logic on movement and arrival paths.
    - Legacy campaigns still load without mandatory backfill because scene scope falls back to broad location when unset.
    - `npm --prefix backend exec vitest run src/engine/__tests__/tool-executor.test.ts src/engine/__tests__/turn-processor.test.ts src/routes/__tests__/chat.test.ts` passes.
  </acceptance_criteria>
  <action>Add the smallest durable runtime field needed to represent the immediate local scene separately from the broad location, for example a nullable `currentSceneLocationId` on player/NPC rows or an equivalent shared field. Wire it through schema and migrations in the normal Drizzle path. Do not attempt a heavy migration; old rows must simply reopen with `currentSceneLocationId ?? currentLocationId`. Keep awareness and knowledge out of persistence for this phase. Crucially, do not stop at schema plus reads: update the authoritative mutation paths that already move or place actors so local scene scope is set, cleared, or realigned when it matters. That includes player travel/arrival handling, opening-scene initialization, and NPC movement/off-screen re-entry paths. Add tests that prove the field is not inert and that legacy fallback still works when no narrow scene scope exists yet.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/tool-executor.test.ts src/engine/__tests__/turn-processor.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>The repo can represent a local encounter inside a large location, and real movement/arrival paths keep that scope aligned without breaking legacy campaigns.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the shared presence snapshot resolver</name>
  <read_first>
    .planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-RESEARCH.md
    backend/src/engine/scene-assembly.ts
    backend/src/routes/chat.ts
    backend/src/engine/__tests__/scene-presence.test.ts
  </read_first>
  <files>backend/src/engine/scene-presence.ts, backend/src/engine/scene-assembly.ts, backend/src/routes/chat.ts, backend/src/engine/__tests__/scene-presence.test.ts, backend/src/routes/__tests__/chat.test.ts</files>
  <acceptance_criteria>
    - `rg -n "PresenceSnapshot|awareness|knowledge basis|hint|clear" backend/src/engine/scene-presence.ts backend/src/engine/scene-assembly.ts` finds the new shared resolver and awareness bands.
    - `npm --prefix backend exec vitest run src/engine/__tests__/scene-presence.test.ts src/routes/__tests__/chat.test.ts` passes.
  </acceptance_criteria>
  <action>Create one backend seam, such as `scene-presence.ts`, that resolves the current local scene into: who is physically present, what the player can notice right now, and what each NPC is justified in knowing or recognizing. Keep the rules lightweight: broad location plus local scene scope, visibility/start-condition effects, location-local recent events, obvious scene effects, and runtime character traits should be enough. Model awareness in simple bands such as `none`, `hint`, and `clear`, and model justified knowledge as compact reasons like current perception, prior connection, reputation, or report. This resolver becomes the shared read model for the rest of the phase.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/scene-presence.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>The runtime has one shared encounter-scope truth instead of several drifting same-location shortcuts.</done>
</task>

</tasks>

<verification>
Confirm the repo now has a compatibility-safe local scene scope and one authoritative resolver that downstream systems can read.
</verification>

<success_criteria>
Phase 46 gets a real foundation: the code can now represent “same district, different immediate scene” without a full spatial simulator.
</success_criteria>

<output>
After completion, create `.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-02-SUMMARY.md`
</output>
