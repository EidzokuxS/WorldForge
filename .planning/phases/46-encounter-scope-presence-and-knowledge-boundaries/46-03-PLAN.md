---
phase: 46-encounter-scope-presence-and-knowledge-boundaries
plan: 03
type: execute
wave: 3
depends_on:
  - 46-02
files_modified:
  - backend/src/engine/scene-assembly.ts
  - backend/src/engine/prompt-assembler.ts
  - backend/src/engine/npc-agent.ts
  - backend/src/engine/npc-offscreen.ts
  - backend/src/routes/chat.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
  - backend/src/engine/__tests__/npc-agent.test.ts
  - backend/src/engine/__tests__/npc-offscreen.test.ts
  - backend/src/routes/__tests__/chat.test.ts
autonomous: true
requirements:
  - SCEN-02
must_haves:
  truths:
    - "Both hidden-pass and final visible scene reads now consume encounter scope instead of broad location membership."
    - "Hidden or unnoticed actors remain real participants in the scene, but narration leaks only what the player can perceive."
    - "NPC prompts receive only physically present and justified nearby actors, not every actor in the broad location."
    - "Off-screen routing is based on immediate encounter scope, not merely sharing the player's broad location."
    - "Perceivable spillover remains possible without omniscient narration."
  artifacts:
    - path: "backend/src/engine/scene-assembly.ts"
      provides: "player-facing scene effects and present-cast assembly keyed to the shared snapshot"
      contains: "playerPerceivableConsequences|presentNpcNames|awareness"
    - path: "backend/src/engine/prompt-assembler.ts"
      provides: "hidden-pass and final-pass prompt filtering keyed to encounter scope"
      contains: "NPC STATES|encounter|awareness"
    - path: "backend/src/engine/npc-agent.ts"
      provides: "nearby-entity filtering and justified recognition for present-scene NPCs"
      contains: "Nearby entities|knowledge"
    - path: "backend/src/engine/npc-offscreen.ts"
      provides: "off-screen routing that no longer equates broad location with live scene presence"
      contains: "scene scope|off-screen"
  key_links:
    - from: "backend/src/engine/scene-presence.ts"
      to: "backend/src/engine/prompt-assembler.ts"
      via: "prompt input must match shared presence/awareness truth"
      pattern: "awareness|knowledge basis"
    - from: "backend/src/engine/scene-presence.ts"
      to: "backend/src/engine/npc-offscreen.ts"
      via: "actor routing must match the same local-scope truth used by narration and present NPCs"
      pattern: "scene scope|off-screen"
---

<objective>
Rewire all actor-routing and narration reads to the new shared encounter-scope resolver.

Purpose: make the same truth drive visible scene assembly, hidden prompt inputs, present-NPC settlement, and off-screen simulation.
Output: SCEN-02 enforced end-to-end on the backend.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-CONTEXT.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-RESEARCH.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-VALIDATION.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-02-PLAN.md
@backend/src/engine/scene-presence.ts
@backend/src/engine/scene-assembly.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/npc-agent.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/routes/chat.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire scene assembly and prompts to presence plus awareness bands</name>
  <read_first>
    backend/src/engine/scene-presence.ts
    backend/src/engine/scene-assembly.ts
    backend/src/engine/prompt-assembler.ts
    backend/src/engine/__tests__/prompt-assembler.test.ts
  </read_first>
  <files>backend/src/engine/scene-assembly.ts, backend/src/engine/prompt-assembler.ts, backend/src/engine/__tests__/prompt-assembler.test.ts</files>
  <acceptance_criteria>
    - `rg -n "awareness|hint|clear|knowledge basis|presentNpcNames" backend/src/engine/scene-assembly.ts backend/src/engine/prompt-assembler.ts` finds the new encounter-aware read path.
    - `npm --prefix backend exec vitest run src/engine/__tests__/prompt-assembler.test.ts` passes.
  </acceptance_criteria>
  <action>Update scene assembly so visible consequences and “present NPCs” come from the shared presence snapshot instead of broad location queries. Hidden or unnoticed actors must still exist in the scene model, but narration should only surface what the player can notice: a clear presence, a vague hint, or only environmental spillover. Update prompt assembly so the hidden pass and final pass both consume the filtered scene cast and justified context instead of telling the model that all `[NPC STATES]` actors at the player's broad location are present. Preserve the Phase 45 single visible narration pass and do not reopen output-quality work here.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/prompt-assembler.test.ts</automated>
  </verify>
  <done>Scene narration and prompt inputs now follow the same encounter-aware truth.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire present-NPC and off-screen routing to the shared scene scope</name>
  <read_first>
    backend/src/engine/scene-presence.ts
    backend/src/engine/npc-agent.ts
    backend/src/engine/npc-offscreen.ts
    backend/src/routes/chat.ts
    backend/src/engine/__tests__/npc-agent.test.ts
    backend/src/engine/__tests__/npc-offscreen.test.ts
    backend/src/routes/__tests__/chat.test.ts
  </read_first>
  <files>backend/src/engine/npc-agent.ts, backend/src/engine/npc-offscreen.ts, backend/src/routes/chat.ts, backend/src/engine/__tests__/npc-agent.test.ts, backend/src/engine/__tests__/npc-offscreen.test.ts, backend/src/routes/__tests__/chat.test.ts</files>
  <acceptance_criteria>
    - `rg -n "scene scope|Nearby entities|awareness|knowledge" backend/src/engine/npc-agent.ts backend/src/engine/npc-offscreen.ts backend/src/routes/chat.ts` finds the new routing logic.
    - `npm --prefix backend exec vitest run src/engine/__tests__/npc-agent.test.ts src/engine/__tests__/npc-offscreen.test.ts src/routes/__tests__/chat.test.ts` passes.
  </acceptance_criteria>
  <action>Update `tickPresentNpcs()` so present-scene NPC selection comes from the shared scene snapshot rather than the player’s broad location ID. Update `tickNpcAgent()` so nearby entities are filtered by the same immediate scene and justified recognition rules. Update off-screen routing so actors elsewhere in the same broad location can still be treated as non-local until they enter the immediate scene. Preserve the one-world contract from Phase 45: hidden participants can still prepare or react; they just do not leak into the player-facing scene or NPC knowledge unless perception/recognition says they should.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/npc-agent.test.ts src/engine/__tests__/npc-offscreen.test.ts src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <done>Actor routing and NPC reasoning now obey encounter scope instead of broad-location omniscience.</done>
</task>

</tasks>

<verification>
Run the backend Phase 46 smoke and confirm the same snapshot now drives visible narration, hidden prompt inputs, present-NPC routing, and off-screen separation.
</verification>

<success_criteria>
The backend no longer behaves like a large district is one tiny room. Presence, awareness, and knowledge are aligned and bounded by the same scene truth.
</success_criteria>

<output>
After completion, create `.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-03-SUMMARY.md`
</output>
