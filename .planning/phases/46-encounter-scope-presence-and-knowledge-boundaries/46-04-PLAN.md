---
phase: 46-encounter-scope-presence-and-knowledge-boundaries
plan: 04
type: execute
wave: 4
depends_on:
  - 46-03
files_modified:
  - backend/src/routes/campaigns.ts
  - frontend/lib/api.ts
  - frontend/lib/api-types.ts
  - frontend/app/game/page.tsx
  - frontend/components/game/location-panel.tsx
  - frontend/lib/__tests__/api.test.ts
  - frontend/app/game/__tests__/page.test.tsx
  - frontend/components/game/__tests__/location-panel.test.tsx
autonomous: true
requirements:
  - SCEN-02
must_haves:
  truths:
    - "The `/game` presentation layer no longer derives 'People Here' from broad location membership."
    - "Backend world payloads expose a scene-scoped compatibility surface so frontend does not have to re-implement encounter logic."
    - "The frontend parser lifts new scene-scoped world payload fields into `WorldData` instead of silently dropping them."
    - "Frontend can still show the current broad location while separately showing who is actually in the immediate scene."
    - "Large locations stop feeling like one room in the live UI, not just in backend logs or prompt inputs."
  artifacts:
    - path: "backend/src/routes/campaigns.ts"
      provides: "scene-scoped world/read payload for `/game`"
      contains: "currentScene|sceneNpcIds|awareness"
    - path: "frontend/app/game/page.tsx"
      provides: "scene-scoped derived state instead of currentLocationId-only NPC filtering"
      contains: "scene|People Here"
    - path: "frontend/components/game/location-panel.tsx"
      provides: "player-facing immediate-scene presentation"
      contains: "People Here"
    - path: "frontend/lib/api.ts"
      provides: "world payload parsing for currentScene and scene-scoped participants"
      contains: "getWorldData|parseWorldData|currentScene"
  key_links:
    - from: "backend/src/routes/campaigns.ts"
      to: "frontend/app/game/page.tsx"
      via: "frontend should consume backend-authoritative scene presence rather than recompute it from broad location rows"
      pattern: "currentScene|sceneNpcIds"
    - from: "frontend/lib/api.ts"
      to: "frontend/app/game/page.tsx"
      via: "page-level scene state must come through the shared API parser instead of raw ad hoc JSON reads"
      pattern: "getWorldData|currentScene"
---

<objective>
Make the live `/game` UI consume the repaired scene-presence contract so the player no longer sees broad-location NPCs as if they are all standing nearby.

Purpose: finish SCEN-02 on the user-facing path, not just in backend internals.
Output: backend world payload plus `/game` panels aligned to immediate scene presence.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-CONTEXT.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-RESEARCH.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-VALIDATION.md
@.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-03-PLAN.md
@backend/src/routes/campaigns.ts
@frontend/lib/api.ts
@frontend/lib/api-types.ts
@frontend/app/game/page.tsx
@frontend/components/game/location-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose scene-scoped world reads and consume them on `/game`</name>
  <read_first>
    backend/src/routes/campaigns.ts
    frontend/lib/api.ts
    frontend/lib/api-types.ts
    frontend/app/game/page.tsx
    frontend/components/game/location-panel.tsx
    frontend/lib/__tests__/api.test.ts
    frontend/app/game/__tests__/page.test.tsx
    frontend/components/game/__tests__/location-panel.test.tsx
  </read_first>
  <files>backend/src/routes/campaigns.ts, frontend/lib/api.ts, frontend/lib/api-types.ts, frontend/app/game/page.tsx, frontend/components/game/location-panel.tsx, frontend/lib/__tests__/api.test.ts, frontend/app/game/__tests__/page.test.tsx, frontend/components/game/__tests__/location-panel.test.tsx</files>
  <acceptance_criteria>
    - `rg -n "currentScene|sceneNpcIds|People Here" backend/src/routes/campaigns.ts frontend/lib/api.ts frontend/lib/api-types.ts frontend/app/game/page.tsx frontend/components/game/location-panel.tsx` finds the new scene-scoped compatibility surface.
    - `npm --prefix frontend exec vitest run lib/__tests__/api.test.ts app/game/__tests__/page.test.tsx components/game/__tests__/location-panel.test.tsx` passes.
  </acceptance_criteria>
  <action>Extend the world payload with the minimum scene-scoped data `/game` needs, such as the current local scene anchor and the IDs or summaries of actors actually in that scene. Keep the broad location panel intact as a place description, but stop deriving “People Here” by filtering all NPCs whose `currentLocationId` matches the player’s broad location. The frontend should consume backend-authored scene presence instead of re-implementing encounter logic in the page component. Update `frontend/lib/api.ts` and the raw payload/parser contract so new `currentScene` or scene-scoped participant fields are actually preserved in `WorldData`. Add at least one parser/API-contract regression in addition to page/panel tests so backend payload evolution cannot silently disappear inside the API layer. If useful, distinguish fully clear presence from hint-level awareness in a bounded way, but do not turn this into a major UI redesign.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run lib/__tests__/api.test.ts app/game/__tests__/page.test.tsx components/game/__tests__/location-panel.test.tsx</automated>
  </verify>
  <done>The live UI now reflects the repaired encounter-scope contract instead of broad-location membership.</done>
</task>

</tasks>

<verification>
Run the frontend smoke and confirm a large location can still be the current place while only a subset of actors appear as immediate-scene participants.
</verification>

<success_criteria>
The player's live `/game` view no longer makes large spaces feel like everyone in the district is clustered in one room.
</success_criteria>

<output>
After completion, create `.planning/phases/46-encounter-scope-presence-and-knowledge-boundaries/46-04-SUMMARY.md`
</output>
