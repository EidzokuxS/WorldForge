---
phase: 35-restore-npc-tier-visibility-and-manual-tier-control-in-world-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/api-types.ts
  - frontend/lib/character-drafts.ts
  - frontend/lib/world-data-helpers.ts
  - frontend/lib/__tests__/character-drafts.test.ts
  - frontend/lib/__tests__/world-data-helpers.test.ts
  - backend/src/routes/schemas.ts
  - backend/src/routes/__tests__/schemas.test.ts
  - backend/src/routes/__tests__/worldgen.test.ts
  - backend/src/routes/__tests__/campaigns.test.ts
  - backend/src/worldgen/__tests__/scaffold-saver.test.ts
autonomous: true
requirements:
  - P35-01
  - P35-02
  - P35-05
must_haves:
  truths:
    - "Editable scaffold NPCs preserve whether they are key or supporting when loaded from campaign world data."
    - "Draft-to-scaffold and scaffold-to-draft conversions no longer silently upcast supporting NPCs to key."
    - "Legacy NPCs loaded without an explicit scaffold tier follow one deterministic fallback rule: use `draft.identity.tier` first, then mapped DB/runtime row tier, and only fall back to `key` when neither seam exists."
    - "Supporting scaffold NPCs still persist to the DB as persistent while key scaffold NPCs persist as key."
    - "Regression coverage proves the tier survives load, edit, and save compatibility projections."
  artifacts:
    - path: "frontend/lib/api-types.ts"
      provides: "ScaffoldNpc contract with explicit key/supporting tier"
    - path: "frontend/lib/character-drafts.ts"
      provides: "Tier-preserving draft/scaffold projection helpers plus canonical `draft.identity.tier` synchronization"
    - path: "frontend/lib/world-data-helpers.ts"
      provides: "Editable scaffold conversion that carries NPC tier through load normalization"
    - path: "backend/src/routes/schemas.ts"
      provides: "saveEdits parser seam that normalizes canonical-draft and legacy scaffold NPC payloads"
    - path: "backend/src/routes/__tests__/schemas.test.ts"
      provides: "Schema regressions for supporting NPC save-edits payloads with and without canonical drafts"
    - path: "backend/src/routes/__tests__/worldgen.test.ts"
      provides: "Route regressions proving /api/worldgen/save-edits preserves supporting tier through parsing"
    - path: "frontend/lib/__tests__/character-drafts.test.ts"
      provides: "Frontend regressions for tier preservation across draft adapters"
    - path: "frontend/lib/__tests__/world-data-helpers.test.ts"
      provides: "Frontend regressions for tier preservation during world->editable scaffold normalization"
    - path: "backend/src/routes/__tests__/campaigns.test.ts"
      provides: "Route compatibility regressions for NPC tier in world payload aliases"
    - path: "backend/src/worldgen/__tests__/scaffold-saver.test.ts"
      provides: "Boundary regression proving supporting->persistent DB mapping remains intact"
  key_links:
    - from: "frontend/lib/character-drafts.ts"
      to: "frontend/lib/api-types.ts"
      via: "ScaffoldNpc tier contract"
      pattern: "tier: \"key\" | \"supporting\""
    - from: "frontend/lib/world-data-helpers.ts"
      to: "frontend/lib/character-drafts.ts"
      via: "draft-backed editable scaffold projection"
      pattern: "characterDraftToScaffoldNpc|draftToEditableNpc"
    - from: "frontend/lib/world-data-helpers.ts"
      to: "backend/src/routes/schemas.ts"
      via: "editable scaffold save payload must survive saveEditsSchema normalization"
      pattern: "scaffoldNpcSchema|input\\.tier \\?\\? legacy\\.tier"
    - from: "frontend/lib/world-data-helpers.ts"
      to: "backend/src/worldgen/scaffold-saver.ts"
      via: "editable scaffold save path must keep supporting->persistent boundary coherent"
      pattern: "supporting|persistent"
---

<objective>
Restore the NPC tier contract across the frontend scaffold seam before touching the review UI.

Purpose: Phase 24 already established key vs supporting NPC generation and the saver boundary already maps supporting to persistent. This plan repairs the frontend/shared contract regression from Phase 29 so load/edit/save flows stop dropping or inventing tier state.
Output: tier-aware `ScaffoldNpc` typings, tier-preserving draft/scaffold adapters, editable scaffold normalization that keeps tier, and regression coverage for both the frontend seam and the backend persistence boundary.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/24-worldgen-known-ip-quality/24-03-SUMMARY.md
@.planning/phases/24-worldgen-known-ip-quality/24-04-SUMMARY.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-05-SUMMARY.md
@backend/src/character/record-adapters.ts
@backend/src/routes/campaigns.ts
@backend/src/routes/schemas.ts
@backend/src/worldgen/scaffold-saver.ts
@frontend/lib/api-types.ts
@frontend/lib/character-drafts.ts
@frontend/lib/world-data-helpers.ts

<interfaces>
From `backend/src/character/record-adapters.ts`:
```typescript
export function toLegacyNpcDraft(record: CharacterRecord): ScaffoldNpc;

function mapRecordTierToScaffoldTier(
  tier: CharacterTier,
): ScaffoldNpc["tier"] {
  return tier === "key" ? "key" : "supporting";
}
```

From `backend/src/routes/schemas.ts`:
```typescript
const scaffoldNpcSchema = z
  .union([
    scaffoldNpcLegacySchema,
    scaffoldNpcLegacySchema.extend({
      draft: characterDraftSchema,
    }),
    z.object({
      draft: characterDraftSchema,
      locationName: z.string().optional(),
      factionName: z.string().nullable().optional(),
      tier: z.enum(["key", "supporting"]).default("key"),
    }),
  ])
  .transform((input) => {
    if ("draft" in input) {
      const legacy = toLegacyNpcDraft(
        materializeDraftRecord("draft-campaign", input.draft),
      );
      return {
        ...legacy,
        locationName: input.locationName ?? legacy.locationName,
        factionName: input.factionName ?? legacy.factionName,
        tier: input.tier ?? legacy.tier,
        draft: input.draft,
      };
    }

    return {
      ...input,
      draft: legacyNpcToDraft(input),
    };
  });
```

From `frontend/lib/api-types.ts` today:
```typescript
export interface ScaffoldNpc {
  name: string;
  persona: string;
  tags: string[];
  goals: { shortTerm: string[]; longTerm: string[] };
  locationName: string;
  factionName: string | null;
  draft?: CharacterDraft;
  _uid?: string;
}
```

From `frontend/lib/world-data-helpers.ts` today:
```typescript
const draftNpc = draft ? draftToEditableNpc(draft) : null;
return {
  name: draftNpc?.name ?? npc.name,
  persona: draftNpc?.persona ?? npc.persona,
  tags: draftNpc?.tags ?? npc.tags,
  goals: draftNpc?.goals ?? { shortTerm, longTerm },
  locationName: draftNpc?.locationName ?? ...,
  factionName: draftNpc?.factionName ?? ...,
  ...(draft ? { draft } : {}),
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore the frontend NPC tier contract across scaffold, draft, and editable-world adapters</name>
  <read_first>
    - frontend/lib/api-types.ts
    - frontend/lib/character-drafts.ts
    - frontend/lib/world-data-helpers.ts
    - backend/src/character/record-adapters.ts
    - frontend/lib/__tests__/world-data-helpers.test.ts
  </read_first>
  <files>frontend/lib/api-types.ts, frontend/lib/character-drafts.ts, frontend/lib/world-data-helpers.ts, frontend/lib/__tests__/character-drafts.test.ts, frontend/lib/__tests__/world-data-helpers.test.ts</files>
  <behavior>
    - Test 1: `characterDraftToScaffoldNpc` emits `tier: "key"` only for draft identity tier `key` and emits `tier: "supporting"` for persistent/supporting NPC drafts.
    - Test 2: `scaffoldNpcToDraft` explicitly writes or preserves `draft.identity.tier`, prefers explicit scaffold tier, and does not silently invent `key` for supporting review NPCs.
    - Test 3: `toEditableScaffold` returns NPCs with preserved tier whether the source is the canonical draft projection or the legacy world row fallback, using the deterministic fallback order `draft.identity.tier -> mapped row tier -> key only when neither exists`.
    - Test 4: blank/manual NPC draft creation uses an explicit tier-aware path instead of hard-coded key defaults.
  </behavior>
  <action>Update the frontend seam, not worldgen rules. Add `tier: "key" | "supporting"` to `ScaffoldNpc` in `frontend/lib/api-types.ts`. In `frontend/lib/character-drafts.ts`, make the draft/scaffold helpers preserve tier in both directions, explicitly verify the canonical draft seam around `draft.identity.tier`, add a small reusable helper for syncing scaffold tier into `draft.identity.tier`, and remove the hidden `key` default from the manual-empty-NPC path by making creation explicitly tier-aware. In `frontend/lib/world-data-helpers.ts`, include tier when normalizing world payloads into editable scaffold state and make the legacy fallback rule explicit: use `draft.identity.tier` first when a canonical draft exists, otherwise use the mapped DB/runtime row tier, and only fall back to `key` when neither seam exists in a true legacy payload. Do not change `backend/src/worldgen/scaffold-steps/npcs-step.ts` generation rules and do not change the saver boundary mapping from supporting to persistent.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run "lib/__tests__/character-drafts.test.ts" "lib/__tests__/world-data-helpers.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/lib/api-types.ts` contains `tier: "key" | "supporting"` on `ScaffoldNpc`
    - `frontend/lib/character-drafts.ts` contains a tier-preserving path in both `characterDraftToScaffoldNpc` and `scaffoldNpcToDraft`
    - `frontend/lib/character-drafts.ts` explicitly reads and writes `draft.identity.tier` or proves that seam through a dedicated sync helper
    - `frontend/lib/character-drafts.ts` does not hard-code `tier: "key"` in the empty-manual-NPC creation path
    - `frontend/lib/world-data-helpers.ts` includes `tier` in the NPC objects returned by `toEditableScaffold`
    - `frontend/lib/world-data-helpers.ts` implements the deterministic legacy fallback order `draft.identity.tier -> mapped row tier -> key`
    - `frontend/lib/__tests__/character-drafts.test.ts` exists and covers supporting vs key conversions
    - `frontend/lib/__tests__/world-data-helpers.test.ts` asserts tier survives canonical-draft and row-fallback normalization, including the missing-`tier` legacy case
    - `npm --prefix frontend exec vitest run "lib/__tests__/character-drafts.test.ts" "lib/__tests__/world-data-helpers.test.ts"` exits 0
  </acceptance_criteria>
  <done>The frontend scaffold/draft seam carries NPC tier explicitly, supporting NPCs no longer upcast during adapter hops, and editable scaffold load normalization preserves tier for save/edit compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the save-edits parser seam and backend compatibility boundary with targeted regressions</name>
  <read_first>
    - backend/src/routes/schemas.ts
    - backend/src/routes/__tests__/schemas.test.ts
    - backend/src/routes/__tests__/worldgen.test.ts
    - backend/src/routes/campaigns.ts
    - backend/src/worldgen/scaffold-saver.ts
    - backend/src/character/record-adapters.ts
    - backend/src/routes/__tests__/campaigns.test.ts
    - backend/src/worldgen/__tests__/scaffold-saver.test.ts
  </read_first>
  <files>backend/src/routes/schemas.ts, backend/src/routes/__tests__/schemas.test.ts, backend/src/routes/__tests__/worldgen.test.ts, backend/src/routes/__tests__/campaigns.test.ts, backend/src/worldgen/__tests__/scaffold-saver.test.ts</files>
  <action>Extend the backend regression suite so Phase 35 protects both the boundary that already works and the parser seam that can still regress supporting NPCs. In `backend/src/routes/__tests__/schemas.test.ts`, add `saveEditsSchema.safeParse` coverage for supporting-tier NPC payloads in both forms: a canonical draft-backed NPC that includes `draft` plus `tier: "supporting"`, and a legacy scaffold NPC row with no `draft` where missing or legacy scaffold tier must follow the same deterministic fallback rule as the frontend seam and materialize a canonical supporting draft when the legacy data implies supporting instead of falling back to key. Explicitly assert the canonical draft seam by checking `draft.identity.tier`, not just the outer scaffold `tier`. In `backend/src/routes/__tests__/worldgen.test.ts`, add `/api/worldgen/save-edits` request coverage for supporting NPC payloads with and without a draft and assert `saveScaffoldToDb` receives normalized NPCs that preserve `tier: "supporting"` and, when a draft is present, keep `draft.identity.tier` coherent. Keep the existing `backend/src/routes/__tests__/campaigns.test.ts` compatibility alias assertion for persistent DB rows surfacing as supporting at the review seam. In `backend/src/worldgen/__tests__/scaffold-saver.test.ts`, keep the supporting->persistent assertion explicit and add a complementary key->key assertion if it is missing. Only modify `backend/src/routes/schemas.ts` if the new schema or route regressions expose a real mismatch in the `scaffoldNpcSchema` transform, especially around the default-to-key fallback outside the defined legacy path.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run "src/routes/__tests__/schemas.test.ts" "src/routes/__tests__/worldgen.test.ts" "src/routes/__tests__/campaigns.test.ts" "src/worldgen/__tests__/scaffold-saver.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/routes/__tests__/schemas.test.ts` asserts `saveEditsSchema` preserves `tier: "supporting"` for a draft-backed NPC payload
    - `backend/src/routes/__tests__/schemas.test.ts` asserts a draft-backed save-edits NPC keeps `draft.identity.tier: "supporting"` coherent with outer scaffold tier
    - `backend/src/routes/__tests__/schemas.test.ts` asserts a legacy save-edits NPC payload without `draft` materializes `draft.identity.tier: "supporting"` instead of defaulting to key
    - `backend/src/routes/__tests__/worldgen.test.ts` posts save-edits payloads with and without `draft` and asserts `saveScaffoldToDb` receives supporting NPCs without tier regression, including the canonical draft seam
    - `backend/src/worldgen/__tests__/scaffold-saver.test.ts` explicitly asserts supporting scaffold NPCs persist as DB tier `persistent`
    - `backend/src/worldgen/__tests__/scaffold-saver.test.ts` explicitly asserts key scaffold NPCs persist as DB tier `key`
    - `backend/src/routes/__tests__/campaigns.test.ts` asserts the world payload keeps a supporting-compatible tier signal for persistent NPC rows via `draft`, `npc`, or both
    - `backend/src/routes/schemas.ts` is only modified if the new parser-seam regressions expose a real contract bug
    - `npm --prefix backend exec vitest run "src/routes/__tests__/schemas.test.ts" "src/routes/__tests__/worldgen.test.ts" "src/routes/__tests__/campaigns.test.ts" "src/worldgen/__tests__/scaffold-saver.test.ts"` exits 0
  </acceptance_criteria>
  <done>The backend regression suite now pins both seams Phase 35 depends on: save-edits parsing preserves supporting NPCs with and without canonical drafts, persistent DB rows still map back to supporting review NPCs, and supporting scaffold NPCs still save as persistent.</done>
</task>

</tasks>

<verification>
- `npm --prefix frontend exec vitest run "lib/__tests__/character-drafts.test.ts" "lib/__tests__/world-data-helpers.test.ts"` exits 0
- `npm --prefix backend exec vitest run "src/routes/__tests__/schemas.test.ts" "src/routes/__tests__/worldgen.test.ts" "src/routes/__tests__/campaigns.test.ts" "src/worldgen/__tests__/scaffold-saver.test.ts"` exits 0
- Editable scaffold NPCs carry explicit tier through load normalization
- Legacy NPCs missing scaffold `tier` follow the documented fallback order `draft.identity.tier -> mapped row tier -> key`
- Save-edits parser normalization preserves supporting NPC tier for both draft-backed and legacy NPC payloads
- Canonical draft seam checks prove `draft.identity.tier` stays coherent with scaffold tier
- Supporting NPCs remain supporting during draft/scaffold adapter hops
- Supporting scaffold NPCs still save as DB persistent
</verification>

<success_criteria>
The tier contract is restored across the review seam before any UI work begins: load, adapter projection, and save-boundary behavior all preserve key vs supporting coherently and are locked by targeted regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/35-restore-npc-tier-visibility-and-manual-tier-control-in-world-review/35-01-SUMMARY.md`
</output>
