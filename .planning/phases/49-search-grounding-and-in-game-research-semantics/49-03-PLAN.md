---
phase: 49-search-grounding-and-in-game-research-semantics
plan: 03
type: execute
wave: 3
depends_on:
  - 49-01
  - 49-02
files_modified:
  - backend/src/engine/grounded-lookup.ts
  - backend/src/engine/prompt-assembler.ts
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/routes/__tests__/chat.test.ts
  - frontend/components/settings/research-tab.tsx
  - frontend/components/settings/__tests__/research-tab.test.tsx
autonomous: true
requirements:
  - RES-01
must_haves:
  truths:
    - "Explicit gameplay lookups can answer canon facts, event clarifications, or power comparisons without running the normal scene-narration path."
    - "Ordinary scene prompts do not absorb raw research dumps; only bounded lookup artifacts can enter runtime narration when explicitly requested."
    - "Player-facing research settings copy no longer falsely describes research as a worldgen-only feature."
  artifacts:
    - path: "backend/src/engine/grounded-lookup.ts"
      provides: "Bounded runtime lookup service for fact recall, event clarification, and power comparison"
      contains: "lookup intent|power comparison|citations|sceneImpact"
    - path: "backend/src/engine/prompt-assembler.ts"
      provides: "Prompt boundary that accepts only compact lookup artifacts instead of raw research blobs"
      contains: "LOOKUP CONTEXT|bounded"
    - path: "backend/src/engine/turn-processor.ts"
      provides: "Lookup short-circuit or bounded lookup branch inside live gameplay"
      contains: "lookup|assistant|skip normal scene"
    - path: "frontend/components/settings/research-tab.tsx"
      provides: "Aligned copy for world formation, character grounding, and targeted live clarification"
      contains: "character grounding|live clarification"
  key_links:
    - from: "backend/src/engine/grounded-lookup.ts"
      to: "backend/src/engine/turn-processor.ts"
      via: "explicit lookup intents are resolved before the ordinary Oracle/scene path"
      pattern: "lookup|fact|power|event"
    - from: "backend/src/engine/grounded-lookup.ts"
      to: "backend/src/engine/prompt-assembler.ts"
      via: "only bounded lookup summaries can enter prompt assembly when runtime narration genuinely needs them"
      pattern: "lookupContext|LOOKUP CONTEXT"
    - from: "backend/src/engine/turn-processor.ts"
      to: "backend/src/routes/__tests__/chat.test.ts"
      via: "chat route regressions prove explicit lookup turns stay factual and do not contaminate ordinary scene output"
      pattern: "lookup|assistant|narrative"
    - from: "frontend/components/settings/research-tab.tsx"
      to: "frontend/components/settings/__tests__/research-tab.test.tsx"
      via: "UI copy regression proves research scope language is no longer worldgen-only"
      pattern: "character grounding|live clarification"
---

<objective>
Add bounded live gameplay lookup semantics and align the remaining user-facing research copy.

Purpose: satisfy D-16 through D-18 and the runtime half of RES-01 while keeping research separate from ordinary scene narration.
Output: a dedicated gameplay lookup service, a prompt boundary that blocks research-blob contamination, and settings copy that matches the broadened grounding scope.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-CONTEXT.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-RESEARCH.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-VALIDATION.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-02-PLAN.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-03-PLAN.md
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/turn-processor.ts
@backend/src/routes/__tests__/chat.test.ts
@frontend/components/settings/research-tab.tsx
@frontend/components/settings/__tests__/research-tab.test.tsx

<interfaces>
From `backend/src/engine/prompt-assembler.ts`:
```ts
export interface AssembleOptions {
  campaignId: string;
  contextWindow: number;
  storytellerPass?: "hidden-tool-driving" | "final-visible" | "opening";
  playerAction?: string;
}

export async function assemblePrompt(options: AssembleOptions): Promise<AssembledPrompt>;
```

From `backend/src/engine/turn-processor.ts`:
```ts
export interface TurnOptions {
  campaignId: string;
  playerAction: string;
  intent: string;
  method: string;
}

export async function* processTurn(options: TurnOptions): AsyncGenerator<TurnEvent>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 49-03-01: Add bounded gameplay lookup intent handling without contaminating scene narration</name>
  <files>backend/src/engine/grounded-lookup.ts, backend/src/engine/prompt-assembler.ts, backend/src/engine/turn-processor.ts, backend/src/engine/__tests__/prompt-assembler.test.ts, backend/src/engine/__tests__/turn-processor.test.ts, backend/src/routes/__tests__/chat.test.ts</files>
  <read_first>.planning/phases/49-search-grounding-and-in-game-research-semantics/49-CONTEXT.md, .planning/phases/49-search-grounding-and-in-game-research-semantics/49-RESEARCH.md, .planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-03-PLAN.md, backend/src/engine/prompt-assembler.ts, backend/src/engine/turn-processor.ts, backend/src/engine/__tests__/prompt-assembler.test.ts, backend/src/engine/__tests__/turn-processor.test.ts, backend/src/routes/__tests__/chat.test.ts</read_first>
  <behavior>
    - Test 1: explicit canon fact recall, event clarification, and power comparison requests route through a dedicated lookup seam.
    - Test 2: normal scene turns do not receive raw research snippets or giant search dumps in prompt assembly.
    - Test 3: the runtime keeps Phase 47's single-pass visible-output constraint and does not add a second narration rewrite stack.
  </behavior>
  <action>Create `backend/src/engine/grounded-lookup.ts` as the dedicated runtime lookup seam that consumes the retrieval planner from 49-01 plus the durable character/power grounding from 49-02. Support the minimum explicit lookup kinds recommended by research: canon fact recall, event clarification, and power comparison. Then wire `turn-processor.ts` so explicit lookup asks short-circuit or branch before the normal Oracle/scene-narration path and return a concise factual answer instead of a full scene turn. If prompt assembly needs lookup data at all, add only a bounded lookup artifact section in `prompt-assembler.ts`; never inject raw search results or large research summaries into ordinary scene prompts. Preserve the Phase 47 runtime constraint from `47-03-PLAN.md`: keep a single visible-output pass and do not add a second rewrite pipeline.</action>
  <verify>
    <automated>npx vitest run backend/src/engine/__tests__/prompt-assembler.test.ts backend/src/engine/__tests__/turn-processor.test.ts backend/src/routes/__tests__/chat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run backend/src/engine/__tests__/prompt-assembler.test.ts backend/src/engine/__tests__/turn-processor.test.ts backend/src/routes/__tests__/chat.test.ts` passes.
    - `rg -n "lookup|power comparison|event clarification|citations" backend/src/engine/grounded-lookup.ts backend/src/engine/turn-processor.ts` returns matches.
    - `rg -n "LOOKUP CONTEXT|lookupContext" backend/src/engine/prompt-assembler.ts` returns matches, and no raw-search section name is introduced.
  </acceptance_criteria>
  <done>Gameplay now has an explicit factual lookup path that stays bounded, grounded, and separate from ordinary scene narration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 49-03-02: Align research settings copy with the broadened grounding scope</name>
  <files>frontend/components/settings/research-tab.tsx, frontend/components/settings/__tests__/research-tab.test.tsx</files>
  <read_first>.planning/phases/49-search-grounding-and-in-game-research-semantics/49-CONTEXT.md, frontend/components/settings/research-tab.tsx, frontend/components/settings/__tests__/research-tab.test.tsx</read_first>
  <action>Update `frontend/components/settings/research-tab.tsx` so the user-facing copy matches the new semantics from D-03, D-16, D-17, and D-18. The research agent is no longer just "before world generation"; it now grounds world formation, character grounding, and targeted live clarification. Keep the surface minimal: revise copy and any directly related labels/help text, but do not turn this into a new research UI. Extend the component test to lock the broadened scope language and prevent drift back to worldgen-only wording.</action>
  <verify>
    <automated>npx vitest run frontend/components/settings/__tests__/research-tab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run frontend/components/settings/__tests__/research-tab.test.tsx` passes.
    - `rg -n "world formation|character grounding|live clarification" frontend/components/settings/research-tab.tsx` returns matches.
    - `rg -n "before world generation" frontend/components/settings/research-tab.tsx` returns no matches.
  </acceptance_criteria>
  <done>The settings surface no longer misdescribes research as worldgen-only, and the copy regression is locked in tests.</done>
</task>

</tasks>

<verification>
Run the prompt/turn/chat lookup suites plus the settings component test and confirm that explicit lookup turns stay bounded while ordinary scene narration remains free of research blobs.
</verification>

<success_criteria>
Live gameplay can answer grounded fact and power questions intentionally, without bloating ordinary scenes, and the visible research surface now accurately describes what the system does.
</success_criteria>

<output>
After completion, create `.planning/phases/49-search-grounding-and-in-game-research-semantics/49-03-SUMMARY.md`
</output>
