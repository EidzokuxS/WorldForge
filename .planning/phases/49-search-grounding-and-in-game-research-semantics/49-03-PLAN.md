---
phase: 49-search-grounding-and-in-game-research-semantics
plan: 03
type: execute
wave: 3
depends_on:
  - 49-01
  - 49-02
files_modified:
  - backend/src/engine/grounded-lookup.ts
  - backend/src/routes/chat.ts
  - backend/src/routes/schemas.ts
  - backend/src/routes/__tests__/chat.test.ts
  - frontend/lib/api.ts
  - frontend/app/game/page.tsx
  - frontend/app/game/__tests__/page.test.tsx
  - frontend/components/settings/research-tab.tsx
  - frontend/components/settings/__tests__/research-tab.test.tsx
autonomous: true
requirements:
  - RES-01
must_haves:
  truths:
    - "Explicit gameplay lookups enter through a dedicated `/api/chat/lookup` path and can answer canon facts, event clarifications, or power comparisons without running the normal scene-turn pipeline."
    - "Lookup SSE responses use a dedicated factual contract (`lookup_result` then `done`) rather than the normal Oracle/narrative event stream."
    - "The frontend has a minimal explicit lookup path that renders lookup answers inside the existing game log without requiring a new rich research UI."
    - "Player-facing research settings copy no longer falsely describes research as a worldgen-only feature."
  artifacts:
    - path: "backend/src/routes/schemas.ts"
      provides: "Dedicated body schema for explicit gameplay lookup requests"
      contains: "chatLookupBodySchema|lookupKind|subject|compareAgainst"
    - path: "backend/src/routes/chat.ts"
      provides: "Dedicated `/api/chat/lookup` SSE route that bypasses ordinary scene-turn processing"
      contains: "lookup|lookup_result|done"
    - path: "backend/src/engine/grounded-lookup.ts"
      provides: "Bounded runtime lookup service for fact recall, event clarification, and power comparison"
      contains: "lookup intent|power comparison|citations|sceneImpact"
    - path: "frontend/lib/api.ts"
      provides: "Client API helper and SSE parser support for dedicated lookup results"
      contains: "chatLookup|lookup_result|onLookupResult"
    - path: "frontend/app/game/page.tsx"
      provides: "Minimal explicit lookup trigger and rendering path inside the existing game page"
      contains: "lookup|chatLookup|assistant"
    - path: "frontend/components/settings/research-tab.tsx"
      provides: "Aligned copy for world formation, character grounding, and targeted live clarification"
      contains: "character grounding|live clarification"
  key_links:
    - from: "backend/src/routes/schemas.ts"
      to: "backend/src/routes/chat.ts"
      via: "the explicit lookup route owns request validation and keeps lookup traffic separate from `chatActionBodySchema`"
      pattern: "chatLookupBodySchema|lookupKind"
    - from: "backend/src/engine/grounded-lookup.ts"
      to: "backend/src/routes/chat.ts"
      via: "the `/api/chat/lookup` route resolves factual lookup requests directly and bypasses ordinary scene narration"
      pattern: "lookup|fact|power|event"
    - from: "backend/src/routes/chat.ts"
      to: "backend/src/routes/__tests__/chat.test.ts"
      via: "chat route regressions prove explicit lookup responses emit only factual lookup events and ordinary `/action` turns keep the Phase 47 narrative contract"
      pattern: "lookup_result|done|narrative"
    - from: "frontend/lib/api.ts"
      to: "frontend/app/game/page.tsx"
      via: "the client parser and page-level trigger/rendering path stay aligned on the new lookup SSE contract"
      pattern: "chatLookup|parseTurnSSE|lookup_result"
    - from: "frontend/components/settings/research-tab.tsx"
      to: "frontend/components/settings/__tests__/research-tab.test.tsx"
      via: "UI copy regression proves research scope language is no longer worldgen-only"
      pattern: "character grounding|live clarification"
---

<objective>
Add bounded live gameplay lookup semantics and align the remaining user-facing research copy.

Purpose: satisfy D-16 through D-18 and the runtime half of RES-01 while keeping research separate from ordinary scene narration.
Output: a dedicated gameplay lookup route/service with a concrete SSE contract, a minimal frontend lookup/rendering path, and settings copy that matches the broadened grounding scope.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-CONTEXT.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-RESEARCH.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-VALIDATION.md
@.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-03-SUMMARY.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-03-SUMMARY.md
@backend/src/routes/chat.ts
@backend/src/routes/schemas.ts
@backend/src/routes/__tests__/chat.test.ts
@frontend/lib/api.ts
@frontend/app/game/page.tsx
@frontend/app/game/__tests__/page.test.tsx
@frontend/components/settings/research-tab.tsx
@frontend/components/settings/__tests__/research-tab.test.tsx

<interfaces>
From `backend/src/routes/schemas.ts`:
```ts
export const chatActionBodySchema = z.object({
  campaignId: campaignIdSchema,
  playerAction: z.string().min(1).max(2000),
  intent: z.string().min(1).max(2000),
  method: z.string().max(500).default(""),
});
```

From `frontend/lib/api.ts`:
```ts
export interface TurnSSEHandlers {
  onNarrative: (text: string) => void;
  onOracleResult: (result: { chance: number; roll: number; outcome: string; reasoning: string }) => void;
  onStateUpdate: (update: { tool: string; args: unknown; result: unknown }) => void;
  onQuickActions: (actions: Array<{ label: string; action: string }>) => void;
  onDone: () => void;
  onError: (error: string) => void;
}

export async function parseTurnSSE(body: ReadableStream<Uint8Array>, handlers: TurnSSEHandlers): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 49-03-01: Add a dedicated `/api/chat/lookup` SSE path for explicit grounded gameplay lookup</name>
  <files>backend/src/engine/grounded-lookup.ts, backend/src/routes/chat.ts, backend/src/routes/schemas.ts, backend/src/routes/__tests__/chat.test.ts, frontend/lib/api.ts, frontend/app/game/page.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <read_first>.planning/phases/49-search-grounding-and-in-game-research-semantics/49-CONTEXT.md, .planning/phases/49-search-grounding-and-in-game-research-semantics/49-RESEARCH.md, .planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-03-SUMMARY.md, .planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-03-SUMMARY.md, backend/src/routes/chat.ts, backend/src/routes/schemas.ts, backend/src/routes/__tests__/chat.test.ts, frontend/lib/api.ts, frontend/app/game/page.tsx, frontend/app/game/__tests__/page.test.tsx</read_first>
  <behavior>
    - Test 1: explicit canon fact recall, event clarification, and power comparison requests enter through `/api/chat/lookup` with a dedicated request schema and bypass the normal scene-turn path.
    - Test 2: the lookup SSE stream emits `lookup_result` followed by `done`, with no `scene-settling`, `oracle_result`, `narrative`, `state_update`, or `quick_actions` events.
    - Test 3: the minimal frontend path can trigger lookup intentionally and render the factual answer inside the existing game log, while ordinary `/api/chat/action` turns still preserve the Phase 47 single-visible-narrative contract.
  </behavior>
  <action>Create `backend/src/routes/schemas.ts` ownership for explicit lookup requests with a dedicated `chatLookupBodySchema` rather than overloading `chatActionBodySchema`. The new schema should be bounded and explicit: `{ campaignId, lookupKind, subject, compareAgainst?, question? }`, where `lookupKind` is restricted to the intent taxonomy already owned by Plan 49-01 (`world_canon_fact`, `character_canon_fact`, `power_profile`, `event_clarification`). Then add `POST /api/chat/lookup` in `backend/src/routes/chat.ts`; this route is the only runtime entry point for explicit lookup in Phase 49 and must call `backend/src/engine/grounded-lookup.ts` directly. Do not classify lookups inside `processTurn()` and do not widen the retrieval-intent taxonomy locally in this plan. The SSE contract is fixed in this plan: emit exactly `lookup_result` with `{ lookupKind, subject, answer, citations, uncertaintyNotes, sceneImpact }`, then `done`; emit `error` on failure. No `scene-settling`, `oracle_result`, `narrative`, `state_update`, or `quick_actions` events belong on the lookup route. Treat `.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-03-SUMMARY.md` as the guard for ordinary `/api/chat/action` turns and leave that path intact. On the frontend, keep the player-facing surface minimal per D-16 through D-18: extend `frontend/lib/api.ts` with `chatLookup()` and parser support for `lookup_result`, and update `frontend/app/game/page.tsx` so an explicit slash-style lookup trigger (for example `/lookup ...` and `/compare ...`) calls the new route and renders the answer as a tagged assistant entry in the existing log. Do not build a separate research panel or rich result surface in this phase.</action>
  <verify>
    <automated>npx vitest run backend/src/routes/__tests__/chat.test.ts frontend/app/game/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run backend/src/routes/__tests__/chat.test.ts frontend/app/game/__tests__/page.test.tsx` passes.
    - Route tests prove `/api/chat/lookup` validates through `chatLookupBodySchema`, emits `lookup_result` then `done`, and never emits ordinary scene-turn SSE events.
    - Route tests also prove `/api/chat/action` still emits the existing narrative contract from `.planning/phases/47-storyteller-output-quality-and-anti-slop-prompting/47-03-SUMMARY.md`.
    - Frontend page tests prove an explicit lookup trigger calls `chatLookup()` rather than `chatAction()` and renders the returned factual answer in the existing log without introducing a new research UI surface.
  </acceptance_criteria>
  <done>Gameplay now has an explicit factual lookup route, contract, and minimal rendering path that stay bounded, grounded, and separate from ordinary scene narration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 49-03-02: Align research settings copy with the broadened grounding scope</name>
  <files>frontend/components/settings/research-tab.tsx, frontend/components/settings/__tests__/research-tab.test.tsx</files>
  <read_first>.planning/phases/49-search-grounding-and-in-game-research-semantics/49-CONTEXT.md, frontend/components/settings/research-tab.tsx, frontend/components/settings/__tests__/research-tab.test.tsx</read_first>
  <action>Update `frontend/components/settings/research-tab.tsx` so the user-facing copy matches the new semantics from D-03, D-16, D-17, and D-18. The research agent is no longer just "before world generation"; it now grounds world formation, character grounding, and targeted live clarification. Keep the surface minimal: revise copy and any directly related labels/help text, but do not turn this into a new research UI. Extend the component test to lock the broadened scope language and prevent drift back to worldgen-only wording.</action>
  <verify>
    <automated>npx vitest run frontend/components/settings/__tests__/research-tab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run frontend/components/settings/__tests__/research-tab.test.tsx` passes.
    - `rg -n "world formation|character grounding|live clarification" frontend/components/settings/research-tab.tsx` returns matches.
    - `rg -n "before world generation" frontend/components/settings/research-tab.tsx` returns no matches.
  </acceptance_criteria>
  <done>The settings surface no longer misdescribes research as worldgen-only, and the copy regression is locked in tests.</done>
</task>

</tasks>

<verification>
Run the chat-route and game-page lookup suites plus the settings component test and confirm that explicit lookup turns stay bounded while ordinary `/api/chat/action` narration keeps the existing Phase 47 contract.
</verification>

<success_criteria>
Live gameplay can answer grounded fact and power questions intentionally through a dedicated lookup route and minimal explicit UI path, without bloating ordinary scenes, and the visible research surface now accurately describes what the system does.
</success_criteria>

<output>
After completion, create `.planning/phases/49-search-grounding-and-in-game-research-semantics/49-03-SUMMARY.md`
</output>
