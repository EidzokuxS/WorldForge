---
phase: 49-search-grounding-and-in-game-research-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/worldgen/retrieval-intent.ts
  - backend/src/worldgen/ip-researcher.ts
  - backend/src/routes/worldgen.ts
  - backend/src/worldgen/__tests__/ip-researcher.test.ts
  - backend/src/routes/__tests__/worldgen.test.ts
autonomous: true
requirements:
  - RES-01
must_haves:
  truths:
    - "Worldgen research splits mixed-premise asks into narrow retrieval jobs instead of issuing one blended canon query."
    - "Known-IP world canon is still stored and reused through the existing `ipContext` lane, not a second preload/cache system."
    - "Generate and regenerate flows can reuse cached canon, then enrich only the missing topics for the requested scaffold step."
  artifacts:
    - path: "backend/src/worldgen/retrieval-intent.ts"
      provides: "Typed retrieval-intent planner for focused world-canon search jobs"
      contains: "RetrievalIntent|world_canon_fact|buildWorldgenResearchPlan"
    - path: "backend/src/worldgen/ip-researcher.ts"
      provides: "Intent-driven known-IP research and targeted sufficiency enrichment"
      contains: "researchKnownIP|evaluateResearchSufficiency"
    - path: "backend/src/routes/worldgen.ts"
      provides: "Single-lane `ipContext` reuse and write-back during suggest/generate/regenerate flows"
      contains: "loadIpContext|saveIpContext|enrichedIpContext"
    - path: "backend/src/worldgen/__tests__/ip-researcher.test.ts"
      provides: "Regression coverage for mixed-premise decomposition and targeted gap filling"
      contains: "mixed premise|retrieval intent|missingTopics"
    - path: "backend/src/routes/__tests__/worldgen.test.ts"
      provides: "Regression coverage that cached canon reuse stays in the existing route/config lane"
      contains: "loadIpContext|saveIpContext|generate"
  key_links:
    - from: "backend/src/worldgen/retrieval-intent.ts"
      to: "backend/src/worldgen/ip-researcher.ts"
      via: "known-IP research and sufficiency checks both consume the same focused retrieval planner"
      pattern: "buildWorldgenResearchPlan|RetrievalIntent"
    - from: "backend/src/worldgen/ip-researcher.ts"
      to: "backend/src/routes/worldgen.ts"
      via: "worldgen routes persist and reuse enriched canon through `ipContext` only"
      pattern: "researchKnownIP|evaluateResearchSufficiency|saveIpContext"
    - from: "backend/src/routes/worldgen.ts"
      to: "backend/src/routes/__tests__/worldgen.test.ts"
      via: "route tests prove request-body context, cached context, and targeted enrichment stay on one lane"
      pattern: "ipContext|generate|regenerate"
---

<objective>
Lock explicit retrieval intent into worldgen research and keep world canon on the existing `ipContext` reuse path.

Purpose: satisfy D-01 through D-06 and the worldgen half of RES-01 without inventing a duplicate canon store.
Output: a typed worldgen retrieval planner, intent-driven `ip-researcher` behavior, and route regressions that prove `ipContext` remains the only world-canon lane.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-CONTEXT.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-RESEARCH.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-VALIDATION.md
@.planning/phases/44-gameplay-docs-baseline-alignment/44-03-SUMMARY.md
@backend/src/worldgen/ip-researcher.ts
@backend/src/worldgen/mcp-research.ts
@backend/src/lib/web-search.ts
@backend/src/routes/worldgen.ts
@backend/src/worldgen/__tests__/ip-researcher.test.ts
@backend/src/routes/__tests__/worldgen.test.ts

<interfaces>
From `shared/src/types.ts`:
```ts
export interface IpResearchContext {
  franchise: string;
  keyFacts: string[];
  tonalNotes: string[];
  canonicalNames?: {
    locations?: string[];
    factions?: string[];
    characters?: string[];
  };
  source: "mcp" | "llm";
}
```

From `backend/src/worldgen/ip-researcher.ts`:
```ts
export interface ResearchableRequest {
  premise: string;
  name: string;
  knownIP?: string;
  research?: { searchProvider?: SearchProvider; braveApiKey?: string; zaiApiKey?: string };
}

export async function researchKnownIP(
  req: ResearchableRequest,
  role: ResolvedRole,
  maxSearchSteps?: number,
): Promise<IpResearchContext | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 49-01-01: Add a typed worldgen retrieval-intent planner and wire `ip-researcher` to focused search units</name>
  <files>backend/src/worldgen/retrieval-intent.ts, backend/src/worldgen/ip-researcher.ts, backend/src/worldgen/__tests__/ip-researcher.test.ts</files>
  <read_first>.planning/phases/49-search-grounding-and-in-game-research-semantics/49-CONTEXT.md, .planning/phases/49-search-grounding-and-in-game-research-semantics/49-RESEARCH.md, backend/src/worldgen/ip-researcher.ts, backend/src/worldgen/mcp-research.ts, backend/src/lib/web-search.ts, backend/src/worldgen/__tests__/ip-researcher.test.ts</read_first>
  <behavior>
    - Test 1: mixed-premise worldgen inputs produce multiple narrow retrieval jobs rather than one blended franchise query.
    - Test 2: sufficiency enrichment asks for concrete missing topics that map back to typed retrieval intents.
    - Test 3: search fallback remains explicit about which retrieval jobs were attempted instead of silently collapsing into a generic mega-query.
  </behavior>
  <action>Create `backend/src/worldgen/retrieval-intent.ts` as the worldgen-only planner that turns a franchise/premise/scaffold-step request into narrow retrieval jobs per D-01, D-02, D-05, and D-06. Wire `backend/src/worldgen/ip-researcher.ts` so both initial known-IP research and `evaluateResearchSufficiency()` consume that planner. Keep the planner concrete and typed: locations, factions, NPCs, rules, and event-history gaps should become separate jobs with explicit query text and purpose. Preserve MCP/LLM fallback behavior, but do not allow either path to revert to a single vague blended search string when planning succeeds. Do not introduce a second canon cache, preload pass, or new settings toggle.</action>
  <verify>
    <automated>npx vitest run backend/src/worldgen/__tests__/ip-researcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run backend/src/worldgen/__tests__/ip-researcher.test.ts` passes.
    - `rg -n "RetrievalIntent|buildWorldgenResearchPlan|world_canon_fact" backend/src/worldgen/retrieval-intent.ts` returns matches.
    - `rg -n "evaluateResearchSufficiency|missingTopics|retrieval" backend/src/worldgen/ip-researcher.ts` returns matches.
  </acceptance_criteria>
  <done>Worldgen research planning is explicit, typed, and test-owned, and `ip-researcher` no longer relies on blended canon searches as its nominal behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 49-01-02: Keep world canon on the existing `ipContext` lane and lock route reuse/enrichment behavior</name>
  <files>backend/src/routes/worldgen.ts, backend/src/routes/__tests__/worldgen.test.ts</files>
  <read_first>.planning/phases/49-search-grounding-and-in-game-research-semantics/49-CONTEXT.md, backend/src/routes/worldgen.ts, backend/src/worldgen/ip-researcher.ts, backend/src/campaign/manager.ts, backend/src/routes/__tests__/worldgen.test.ts</read_first>
  <action>Update `backend/src/routes/worldgen.ts` so suggest/generate/regenerate flows keep reusing the existing `loadIpContext()` / `saveIpContext()` seam while writing back any targeted enrichments discovered by the new planner. Cover the exact precedence order the repo already uses: request-body `ipContext` first, cached campaign `ipContext` second, on-demand research third. Do not create a parallel world-canon key in config, a detached research table, or a second world-lore store. Extend `backend/src/routes/__tests__/worldgen.test.ts` so route behavior proves D-04 through D-06: no wasteful preload pass, no duplicate canon cache, and targeted enrichment reuses the existing lane.</action>
  <verify>
    <automated>npx vitest run backend/src/worldgen/__tests__/ip-researcher.test.ts backend/src/routes/__tests__/worldgen.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run backend/src/worldgen/__tests__/ip-researcher.test.ts backend/src/routes/__tests__/worldgen.test.ts` passes.
    - `rg -n "loadIpContext|saveIpContext|enrichedIpContext" backend/src/routes/worldgen.ts` returns matches.
    - `rg -n "duplicate canon|second store|preload" backend/src/routes/__tests__/worldgen.test.ts` returns new assertions or comments guarding against a parallel store.
  </acceptance_criteria>
  <done>Worldgen routes still use one world-canon lane, and the route tests prove reuse plus targeted write-back instead of duplicate canon storage.</done>
</task>

</tasks>

<verification>
Run the worldgen research and route suites and confirm the new planner narrows mixed asks while route behavior still persists canon through `ipContext` only.
</verification>

<success_criteria>
World formation now asks focused canon questions, enriches only what is missing, and keeps reusable world knowledge on the existing `ipContext`/lore path instead of inventing a second store.
</success_criteria>

<output>
After completion, create `.planning/phases/49-search-grounding-and-in-game-research-semantics/49-01-SUMMARY.md`
</output>
