---
phase: 49-search-grounding-and-in-game-research-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - shared/src/types.ts
  - shared/src/index.ts
  - backend/src/character/grounded-character-profile.ts
  - backend/src/character/record-adapters.ts
  - backend/src/routes/schemas.ts
  - backend/src/character/archetype-researcher.ts
  - backend/src/routes/character.ts
  - backend/src/character/__tests__/record-adapters.identity.test.ts
  - backend/src/routes/__tests__/schemas.test.ts
  - backend/src/character/__tests__/archetype-researcher.test.ts
  - backend/src/routes/__tests__/character.test.ts
autonomous: true
requirements:
  - RES-01
must_haves:
  truths:
    - "Important characters can carry durable grounded canon and power summaries inside the same Phase 48 character lane instead of relying on repeated live search."
    - "Power comparisons are backed by an explicit structured profile with citations and uncertainty, not raw model intuition."
    - "The `/api/worldgen/research-character` seam returns compact structured grounding artifacts compatible with `sourceBundle` and stored `characterRecord` JSON."
  artifacts:
    - path: "shared/src/types.ts"
      provides: "Shared character grounding and power-profile contracts adjacent to Phase 48 identity/source seams"
      contains: "CharacterGroundingProfile|PowerProfile|grounding"
    - path: "backend/src/character/grounded-character-profile.ts"
      provides: "Backend synthesis helpers for compact grounded character and power summaries"
      contains: "synthesize|powerProfile|uncertainty|sources"
    - path: "backend/src/character/record-adapters.ts"
      provides: "Normalization and persistence-safe handling for the new grounding field inside stored `characterRecord` JSON"
      contains: "normalizeCharacterDraftRecord|grounding"
    - path: "backend/src/routes/schemas.ts"
      provides: "Route/schema materialization for grounded character/power fields"
      contains: "characterDraftSchema|characterRecordSchema|grounding"
    - path: "backend/src/character/archetype-researcher.ts"
      provides: "Character research that produces durable structured grounding instead of only archetype prose"
      contains: "researchArchetype|powerProfile|sources"
    - path: "backend/src/routes/character.ts"
      provides: "Character route boundary that returns structured grounding with draft/record payloads"
      contains: "research-character|characterRecord|draft"
  key_links:
    - from: "shared/src/types.ts"
      to: "backend/src/character/record-adapters.ts"
      via: "grounding data is normalized inside the same character-record lane already used by Phase 48"
      pattern: "grounding|CharacterGroundingProfile"
    - from: "backend/src/character/grounded-character-profile.ts"
      to: "backend/src/character/archetype-researcher.ts"
      via: "research output is converted into compact durable grounded summaries with citations and uncertainty"
      pattern: "synthesize|PowerProfile|sources"
    - from: "backend/src/routes/schemas.ts"
      to: "backend/src/routes/character.ts"
      via: "route payloads accept and emit the new grounding field over the existing API seam"
      pattern: "characterDraftSchema|characterRecordSchema|grounding"
    - from: "backend/src/routes/character.ts"
      to: "backend/src/routes/__tests__/character.test.ts"
      via: "route regressions prove `research-character` returns structured grounding and power data"
      pattern: "research-character|grounding|powerProfile"
---

<objective>
Attach durable grounded character and power artifacts to the existing Phase 48 character lane.

Purpose: satisfy D-07 through D-12 without inventing a detached research subsystem or re-googling key character facts during play.
Output: shared grounding types, a backend grounding synthesizer, and character route behavior that returns structured canon/power profiles with citations and uncertainty.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-CONTEXT.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-RESEARCH.md
@.planning/phases/49-search-grounding-and-in-game-research-semantics/49-VALIDATION.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-01-SUMMARY.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-02-SUMMARY.md
@.planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-03-SUMMARY.md
@shared/src/types.ts
@backend/src/character/record-adapters.ts
@backend/src/routes/schemas.ts
@backend/src/character/archetype-researcher.ts
@backend/src/routes/character.ts

<interfaces>
From `shared/src/types.ts`:
```ts
export interface CharacterDraft {
  identity: CharacterIdentityDraft;
  profile: CharacterProfile;
  socialContext: CharacterSocialContext;
  motivations: CharacterMotivations;
  capabilities: CharacterCapabilities;
  state: CharacterState;
  loadout: CharacterLoadout;
  startConditions: CharacterStartConditions;
  provenance: CharacterProvenance;
  sourceBundle?: CharacterSourceBundle;
  continuity?: CharacterContinuityPolicy;
}

export interface CharacterRecord {
  identity: CharacterIdentity;
  sourceBundle?: CharacterSourceBundle;
  continuity?: CharacterContinuityPolicy;
}
```

From `backend/src/character/record-adapters.ts`:
```ts
export function createCharacterRecordFromDraft(
  draft: CharacterDraft,
  identity: Pick<CharacterRecord["identity"], "id" | "campaignId">,
): CharacterRecord;

export function toCharacterDraft(record: CharacterRecord): CharacterDraft;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 49-02-01: Define shared grounded character and power-profile contracts on the Phase 48 record lane</name>
  <files>shared/src/types.ts, shared/src/index.ts, backend/src/character/record-adapters.ts, backend/src/routes/schemas.ts, backend/src/character/__tests__/record-adapters.identity.test.ts, backend/src/routes/__tests__/schemas.test.ts</files>
  <read_first>.planning/phases/49-search-grounding-and-in-game-research-semantics/49-CONTEXT.md, .planning/phases/48-character-identity-fidelity-and-canonical-modeling/48-01-SUMMARY.md, shared/src/types.ts, backend/src/character/record-adapters.ts, backend/src/routes/schemas.ts, backend/src/character/__tests__/record-adapters.identity.test.ts, backend/src/routes/__tests__/schemas.test.ts</read_first>
  <behavior>
    - Test 1: `CharacterDraft` / `CharacterRecord` can carry an optional grounded character profile without creating a second ontology.
    - Test 2: the grounding field includes a structured power profile with strengths, constraints, vulnerabilities, citations, and uncertainty notes.
    - Test 3: stored `characterRecord` JSON and route schemas preserve the new grounding field through save/load/materialize transforms.
  </behavior>
  <action>Add a shared optional `grounding` field adjacent to `sourceBundle` and `continuity` on the existing Phase 48 character contracts. The new shared types must include a compact durable character summary and a structured `PowerProfile` per D-07, D-08, D-10, D-11, and D-12. Keep the field compatible with the existing `characterRecord` JSON storage path: normalize it in `record-adapters.ts`, add it to `backend/src/routes/schemas.ts`, and extend the adapter/schema tests so the data survives round-trip persistence. Do not create a detached per-character research database, a backend-only shadow type, or a second character ontology. Keep citations and uncertainty explicit; do not encode fake precision.</action>
  <verify>
    <automated>npx vitest run backend/src/character/__tests__/record-adapters.identity.test.ts backend/src/routes/__tests__/schemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run backend/src/character/__tests__/record-adapters.identity.test.ts backend/src/routes/__tests__/schemas.test.ts` passes.
    - `rg -n "CharacterGroundingProfile|PowerProfile|grounding\\?:" shared/src/types.ts` returns matches.
    - `rg -n "grounding" backend/src/character/record-adapters.ts backend/src/routes/schemas.ts` returns matches.
  </acceptance_criteria>
  <done>The shared character lane can now persist compact grounded canon and power data with citations and uncertainty through the same schema/adapter path Phase 48 already owns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 49-02-02: Rebuild `research-character` around durable grounded profiles instead of archetype-only prose</name>
  <files>backend/src/character/grounded-character-profile.ts, backend/src/character/archetype-researcher.ts, backend/src/routes/character.ts, backend/src/character/__tests__/archetype-researcher.test.ts, backend/src/routes/__tests__/character.test.ts</files>
  <read_first>.planning/phases/49-search-grounding-and-in-game-research-semantics/49-CONTEXT.md, backend/src/character/archetype-researcher.ts, backend/src/routes/character.ts, backend/src/character/__tests__/archetype-researcher.test.ts, backend/src/routes/__tests__/character.test.ts, shared/src/types.ts</read_first>
  <action>Create `backend/src/character/grounded-character-profile.ts` to synthesize compact grounded character artifacts from retrieved inputs: identity-relevant facts, abilities, constraints, signature moves, strong points, vulnerabilities, citations, and a structured power profile with uncertainty notes. Then rework `backend/src/character/archetype-researcher.ts` and `/api/worldgen/research-character` so the route no longer stops at archetype prose. Keep the existing route path and draft/record response shape, but return `draft.grounding` and `characterRecord.grounding` as the durable output. Make the research layer compatible with D-09: store compact structured grounding, not raw research blobs. Preserve `sourceBundle` as the provenance lane and keep the new grounding field additive to it, not a replacement for it.</action>
  <verify>
    <automated>npx vitest run backend/src/character/__tests__/archetype-researcher.test.ts backend/src/routes/__tests__/character.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run backend/src/character/__tests__/archetype-researcher.test.ts backend/src/routes/__tests__/character.test.ts` passes.
    - `rg -n "powerProfile|uncertainty|sources" backend/src/character/grounded-character-profile.ts backend/src/character/archetype-researcher.ts` returns matches.
    - `rg -n "grounding" backend/src/routes/character.ts backend/src/routes/__tests__/character.test.ts` returns matches.
  </acceptance_criteria>
  <done>The character-research route now produces durable grounded canon/power artifacts on the existing character lane, with route regressions proving the data reaches draft and record payloads.</done>
</task>

</tasks>

<verification>
Run the adapter/schema and character research suites and confirm that Phase 48 records can carry durable grounded character/power summaries without introducing a detached storage path.
</verification>

<success_criteria>
Key character facts and power semantics are now durable, structured, and source-aware inside the existing character record lane, so runtime play no longer depends on vibes or repeated live search for those basics.
</success_criteria>

<output>
After completion, create `.planning/phases/49-search-grounding-and-in-game-research-semantics/49-02-SUMMARY.md`
</output>
