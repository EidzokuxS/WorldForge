---
phase: 54-draft-backed-npc-edit-persistence-and-review-convergence
plan: 2
type: execute
wave: 2
depends_on:
  - 54-01
files_modified:
  - frontend/lib/world-data-helpers.ts
  - frontend/lib/__tests__/world-data-helpers.test.ts
  - frontend/components/world-review/npcs-section.tsx
  - frontend/components/world-review/character-record-inspector.tsx
  - frontend/components/world-review/__tests__/npcs-section.test.tsx
  - frontend/components/world-review/__tests__/character-record-inspector.test.tsx
autonomous: true
requirements:
  - UX-02
must_haves:
  truths:
    - "World Review reload still prefers `draft`, but now the preferred values match the last saved edit because backend truth converged in Plan 54-01."
    - "The advanced inspector remains additive and read-only; no new editable inspector/model path is introduced."
    - "Frontend proof stays bounded to helper and review-surface trust, not a UI redesign."
  artifacts:
    - path: "frontend/lib/__tests__/world-data-helpers.test.ts"
      provides: "Draft-first reload proof against a repaired world payload."
    - path: "frontend/components/world-review/__tests__/npcs-section.test.tsx"
      provides: "World Review card remains the shallow editable surface and does not move persistence logic into the UI."
    - path: "frontend/components/world-review/__tests__/character-record-inspector.test.tsx"
      provides: "Advanced inspector remains read-only/additive after the backend round-trip repair."
  key_links:
    - from: "backend /api/campaigns/:id/world payload"
      to: "frontend/lib/world-data-helpers.ts"
      via: "toEditableScaffold(world, premise, lore)"
      pattern: "draftNpc?.name|draftNpc?.persona|draftNpc?.tags"
    - from: "frontend/lib/world-data-helpers.ts"
      to: "frontend/components/world-review/npcs-section.tsx"
      via: "editable scaffold NPC props"
      pattern: "npcs="
    - from: "frontend/components/world-review/npcs-section.tsx"
      to: "frontend/components/world-review/character-record-inspector.tsx"
      via: "read-only advanced metadata panel"
      pattern: "CharacterRecordInspector"
---

<objective>
Prove the repaired backend truth actually lands in the existing World Review surface without reopening inspector scope or adding frontend-only persistence logic.

Purpose: Close the reload-trust side of the audit while keeping Phase 52’s additive/read-only inspector boundary intact.
Output: Frontend/helper regression proof for draft-first reload and inspector read-only stability.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-draft-backed-npc-edit-persistence-and-review-convergence/54-RESEARCH.md
@.planning/phases/54-draft-backed-npc-edit-persistence-and-review-convergence/54-VALIDATION.md
@.planning/phases/52-advanced-character-inspector-and-full-record-visibility/52-VERIFICATION.md
@frontend/lib/world-data-helpers.ts
@frontend/lib/__tests__/world-data-helpers.test.ts
@frontend/components/world-review/npcs-section.tsx
@frontend/components/world-review/character-record-inspector.tsx
@frontend/components/world-review/__tests__/npcs-section.test.tsx
@frontend/components/world-review/__tests__/character-record-inspector.test.tsx

<interfaces>
From `frontend/lib/world-data-helpers.ts`:

```typescript
return {
  name: draftNpc?.name ?? npc.name,
  persona: draftNpc?.persona ?? npc.persona,
  tags: draftNpc?.tags ?? npc.tags,
  goals: draftNpc?.goals ?? { shortTerm, longTerm },
  locationName: draftNpc?.locationName ?? ...,
  factionName: draftNpc?.factionName ?? ...,
  tier,
  characterRecord: npc.characterRecord ?? null,
  ...(draft ? { draft } : {}),
};
```

From `frontend/components/world-review/npcs-section.tsx`:

```tsx
<CharacterRecordInspector
  draft={npc.draft ? scaffoldNpcToDraft(npc) : null}
  characterRecord={npc.characterRecord ?? null}
/>
```

From Phase 52 verification:
- The inspector is already intended to be additive and read-only.
- Existing NPC card interactions must keep passing after inspector-related proof is extended.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Run pre-edit impact analysis for the frontend trust and inspector seams</name>
  <read_first>
    - .planning/phases/54-draft-backed-npc-edit-persistence-and-review-convergence/54-VALIDATION.md
    - frontend/lib/world-data-helpers.ts
    - frontend/components/world-review/npcs-section.tsx
    - frontend/components/world-review/character-record-inspector.tsx
  </read_first>
  <files>frontend/lib/world-data-helpers.ts, frontend/components/world-review/npcs-section.tsx, frontend/components/world-review/character-record-inspector.tsx</files>
  <acceptance_criteria>
    - The execution notes include GitNexus upstream impact results for `toEditableScaffold`, `NpcsSection`, and `CharacterRecordInspector`.
    - Any `HIGH` or `CRITICAL` result is treated as a stop/escalation point before editing the corresponding symbol.
    - The chosen implementation stays within the bounded frontend trust scope described in `54-RESEARCH.md`.
  </acceptance_criteria>
  <action>Before editing any frontend/helper symbol in this plan, run GitNexus impact analysis for `toEditableScaffold`, `NpcsSection`, and `CharacterRecordInspector` per AGENTS. Record the results in execution notes and treat `HIGH` or `CRITICAL` risk as a stop condition that requires plan-level escalation rather than silent scope creep.</action>
  <verify>
    <automated>gitnexus_impact({ repo: "WorldForge", target: "toEditableScaffold", direction: "upstream" }) + gitnexus_impact({ repo: "WorldForge", target: "NpcsSection", direction: "upstream" }) + gitnexus_impact({ repo: "WorldForge", target: "CharacterRecordInspector", direction: "upstream" })</automated>
  </verify>
  <done>The executor has the frontend risk picture before changing any trust/helper or inspector symbol, and high-risk edits are explicitly blocked from proceeding silently.</done>
</task>

<task type="auto">
  <name>Task 1: Prove draft-first reload now reflects the saved edit instead of stale draft truth</name>
  <read_first>
    - frontend/lib/world-data-helpers.ts
    - frontend/lib/__tests__/world-data-helpers.test.ts
    - backend/src/routes/campaigns.ts
    - .planning/phases/54-draft-backed-npc-edit-persistence-and-review-convergence/54-RESEARCH.md
    - .planning/phases/54-draft-backed-npc-edit-persistence-and-review-convergence/54-VALIDATION.md
  </read_first>
  <files>frontend/lib/world-data-helpers.ts, frontend/lib/__tests__/world-data-helpers.test.ts</files>
  <acceptance_criteria>
    - `rg -n "draft-backed npc reload|prefers draft|saved edits" frontend/lib/__tests__/world-data-helpers.test.ts` finds the new reload-proof case.
    - `rg -n "draftNpc\\?\\.name|draftNpc\\?\\.persona|draftNpc\\?\\.tags|draftNpc\\?\\.goals" frontend/lib/world-data-helpers.ts` still shows draft-first preference after the task.
    - If helper code changes are required, they remain additive and keep the shared `draft` lane as the preferred source instead of reintroducing raw-row-first fallback behavior.
  </acceptance_criteria>
  <action>Extend the helper regression suite with a backend-like world payload fixture whose `characterRecord`, `draft`, and compatibility `npc` already reflect the reconciled save from Plan 54-01. Prove `toEditableScaffold()` still prefers `draft`, but the preferred values now equal the last saved shallow edit after reload. Only change `frontend/lib/world-data-helpers.ts` if the repaired payload exposes a real compatibility gap; if you touch it, keep the function draft-first and do not invent a parallel editable NPC model or UI-side reconciliation patch.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run lib/__tests__/world-data-helpers.test.ts</automated>
  </verify>
  <done>The helper layer explicitly proves that reload stays draft-first and now surfaces the user’s saved draft-backed NPC edits instead of stale draft values.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the world-review surface bounded and the advanced inspector read-only</name>
  <read_first>
    - frontend/components/world-review/npcs-section.tsx
    - frontend/components/world-review/character-record-inspector.tsx
    - frontend/components/world-review/__tests__/npcs-section.test.tsx
    - frontend/components/world-review/__tests__/character-record-inspector.test.tsx
    - .planning/phases/52-advanced-character-inspector-and-full-record-visibility/52-VERIFICATION.md
  </read_first>
  <files>frontend/components/world-review/npcs-section.tsx, frontend/components/world-review/character-record-inspector.tsx, frontend/components/world-review/__tests__/npcs-section.test.tsx, frontend/components/world-review/__tests__/character-record-inspector.test.tsx</files>
  <acceptance_criteria>
    - `rg -n "read-only|additive|Advanced" frontend/components/world-review/__tests__/npcs-section.test.tsx frontend/components/world-review/__tests__/character-record-inspector.test.tsx` finds assertions that keep the inspector bounded to metadata display.
    - `npm --prefix frontend exec vitest run components/world-review/__tests__/npcs-section.test.tsx components/world-review/__tests__/character-record-inspector.test.tsx` passes.
    - No component code introduces editable controls inside `CharacterRecordInspector` or shifts save/persistence logic out of the existing shallow NPC card surface.
  </acceptance_criteria>
  <action>Extend the world-review component tests so the repaired backend round-trip does not quietly reopen Phase 52 scope. The NPC card remains the shallow editable surface, while `CharacterRecordInspector` stays a read-only advanced metadata/details panel. Prefer test-only closure; only change `npcs-section.tsx` or `character-record-inspector.tsx` if a real prop/wiring regression blocks the proof. Do not redesign the UI and do not make the inspector editable.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run components/world-review/__tests__/npcs-section.test.tsx components/world-review/__tests__/character-record-inspector.test.tsx</automated>
  </verify>
  <done>The frontend proof explicitly shows that save/load trust improved without changing the editor/inspector scope: the inspector remains additive/read-only and the World Review cards remain the editable surface.</done>
</task>

</tasks>

<verification>
Run the Phase 54 frontend smoke and then the full backend+frontend convergence smoke:

```bash
npm --prefix frontend exec vitest run lib/__tests__/world-data-helpers.test.ts components/world-review/__tests__/npcs-section.test.tsx components/world-review/__tests__/character-record-inspector.test.tsx
npm --prefix backend exec vitest run src/routes/__tests__/worldgen.test.ts src/worldgen/__tests__/scaffold-saver.test.ts src/routes/__tests__/campaigns.test.ts && npm --prefix frontend exec vitest run lib/__tests__/world-data-helpers.test.ts components/world-review/__tests__/npcs-section.test.tsx components/world-review/__tests__/character-record-inspector.test.tsx
```
</verification>

<success_criteria>
World Review reload trust is proven end-to-end: the frontend still prefers backend-provided draft truth, that truth now matches the user’s saved edit, and the advanced inspector remains a read-only additive surface.
</success_criteria>

<output>
After completion, create `.planning/phases/54-draft-backed-npc-edit-persistence-and-review-convergence/54-02-SUMMARY.md`
</output>
