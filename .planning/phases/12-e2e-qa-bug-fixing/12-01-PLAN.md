---
phase: 12-e2e-qa-bug-fixing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/__tests__/campaigns.test.ts
  - backend/src/routes/__tests__/settings.test.ts
autonomous: true
requirements:
  - QA-PRECHECK
must_haves:
  truths:
    - "All 44 backend test files pass with 0 failures"
    - "npm --prefix backend test -- --run exits with code 0"
  artifacts:
    - path: "backend/src/routes/__tests__/campaigns.test.ts"
      provides: "Fixed campaign route tests"
    - path: "backend/src/routes/__tests__/settings.test.ts"
      provides: "Fixed settings route tests"
  key_links:
    - from: "campaigns.test.ts"
      to: "../../lib/index.js mock"
      via: "vi.mock with createLogger"
      pattern: "createLogger"
---

<objective>
Fix the 2 failing backend test files before any browser-based QA begins.

Purpose: Both `campaigns.test.ts` and `settings.test.ts` fail because their `vi.mock("../../lib/index.js")` does not export `createLogger`, which is imported by transitive dependencies (lore-cards.ts, worldbook-importer.ts). This must be green before browser testing.
Output: All 44 test files pass (702+ tests, 0 failures)
</objective>

<execution_context>
@.planning/phases/12-e2e-qa-bug-fixing/12-CONTEXT.md
</execution_context>

<context>
@backend/src/routes/__tests__/campaigns.test.ts
@backend/src/routes/__tests__/settings.test.ts
@backend/src/lib/logger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix createLogger mock in failing test files</name>
  <files>backend/src/routes/__tests__/campaigns.test.ts, backend/src/routes/__tests__/settings.test.ts</files>
  <action>
Both test files mock `../../lib/index.js` but the mock does not include `createLogger`, which is now exported from the lib barrel. The transitive import chain (routes -> lore-cards.ts / worldbook-importer.ts -> lib/index.js -> logger.ts) needs `createLogger` in the mock.

Fix approach: In each test file's `vi.mock("../../lib/index.js", ...)`, add `createLogger` to the mock return. Use `importOriginal` to spread actual exports and only override what's needed, OR explicitly add:
```typescript
createLogger: vi.fn(() => ({
  info: vi.fn(),
  warn: vi.fn(),
  error: vi.fn(),
  debug: vi.fn(),
}))
```

After fixing, run the full test suite to confirm 0 failures.
  </action>
  <verify>
    <automated>npm --prefix backend test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>All 44 test files pass. Exit code 0. No "FAIL" lines in output.</done>
</task>

<task type="auto">
  <name>Task 2: Run typecheck to confirm no type errors</name>
  <files>backend/src/routes/__tests__/campaigns.test.ts, backend/src/routes/__tests__/settings.test.ts</files>
  <action>
Run `npm --prefix backend run typecheck` to verify no TypeScript errors were introduced by the mock fixes. If any errors exist, fix them.
  </action>
  <verify>
    <automated>npm --prefix backend run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>TypeScript compilation succeeds with 0 errors.</done>
</task>

</tasks>

<verification>
- `npm --prefix backend test -- --run` exits 0 with 44 passed test files
- `npm --prefix backend run typecheck` exits 0
</verification>

<success_criteria>
All backend tests pass. No TypeScript errors. The codebase is in a clean state for browser-based QA.
</success_criteria>

<output>
After completion, create `.planning/phases/12-e2e-qa-bug-fixing/12-01-SUMMARY.md`
</output>
