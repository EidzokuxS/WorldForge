---
phase: 29-unified-character-ontology-and-tag-system
plan: 05
type: execute
wave: 3
depends_on:
  - "29-02"
files_modified:
  - backend/src/routes/campaigns.ts
  - backend/src/routes/__tests__/campaigns.test.ts
  - frontend/lib/api-types.ts
  - frontend/lib/api.ts
  - frontend/lib/world-data-helpers.ts
  - frontend/lib/__tests__/api.test.ts
  - frontend/lib/__tests__/world-data-helpers.test.ts
  - frontend/app/campaign/[id]/character/page.tsx
  - frontend/app/campaign/[id]/review/page.tsx
  - frontend/components/character-creation/character-card.tsx
  - frontend/components/world-review/npcs-section.tsx
  - frontend/components/character-creation/__tests__/character-card.test.tsx
  - frontend/components/world-review/__tests__/npcs-section.test.tsx
autonomous: true
requirements:
  - P29-05
  - P29-06
must_haves:
  truths:
    - "Campaign/world payloads expose shared player/NPC character data that frontend editors can consume without reconstructing meaning from flat legacy fields."
    - "Character creation and world-review editing use the shared player/NPC draft model instead of unrelated protagonist and NPC editor contracts."
    - "The existing non-game UI stays structurally familiar; this plan migrates data usage, not the broader Phase 32 visual overhaul."
  artifacts:
    - path: "backend/src/routes/campaigns.ts"
      provides: "World payload shaped around canonical character data plus migration-safe compatibility fields"
    - path: "frontend/lib/api-types.ts"
      provides: "Frontend shared player/NPC draft and world payload types"
    - path: "frontend/lib/world-data-helpers.ts"
      provides: "World-review adapters that map canonical world payloads into editable scaffold state"
    - path: "frontend/components/character-creation/character-card.tsx"
      provides: "Character editor consuming grouped shared character fields"
    - path: "frontend/components/world-review/npcs-section.tsx"
      provides: "NPC editor consuming grouped shared character fields"
  key_links:
    - from: "backend/src/routes/campaigns.ts"
      to: "frontend/lib/api-types.ts"
      via: "world payload contract for player/NPC canonical fields"
      pattern: "characterRecord|derivedTags|motivations|capabilities"
    - from: "frontend/lib/api.ts"
      to: "backend/src/routes/character.ts"
      via: "shared creation endpoint responses using additive `draft` payloads"
      pattern: "draft|character|npc"
    - from: "frontend/lib/world-data-helpers.ts"
      to: "frontend/components/world-review/npcs-section.tsx"
      via: "shared scaffold/editor state for NPCs"
      pattern: "CharacterDraft|locationName|factionName"
---

<objective>
Move route payloads and frontend editors onto the shared player/NPC data contract.

Purpose: Phase 29 is only fully usable once the review and creation surfaces stop assuming separate player and NPC data shapes. This plan updates the route payloads, client types, and current editors to use the shared model without drifting into the Phase 32 redesign.
Output: canonical world payload usage in backend/frontend, shared editor state, and regression coverage for the current creation/review UI.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-CONTEXT.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-character-ontology-spec.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-02-PLAN.md
@backend/src/routes/campaigns.ts
@frontend/lib/api-types.ts
@frontend/lib/api.ts
@frontend/lib/world-data-helpers.ts
@frontend/app/campaign/[id]/character/page.tsx
@frontend/app/campaign/[id]/review/page.tsx
@frontend/components/character-creation/character-card.tsx
@frontend/components/world-review/npcs-section.tsx

<decisions>
- D-01: One canonical character model serves both player and NPC flows.
- D-02: Flat tags are derived runtime output, not the edit surface.
- D-05: Phase 29 owns shared player/NPC data usage across routes and editors.
- D-06: Do not implement Phase 30 start-condition persistence or canonical loadout behavior.
- D-07: Do not turn this into the Phase 32 UI overhaul; preserve existing layout patterns where possible.
</decisions>

<interfaces>
Current frontend split:
```ts
export interface ParsedCharacter {
  name: string;
  race: string;
  gender: string;
  age: string;
  appearance: string;
  tags: string[];
  hp: number;
  equippedItems: string[];
  locationName: string;
}

export interface ScaffoldNpc {
  name: string;
  persona: string;
  tags: string[];
  goals: { shortTerm: string[]; longTerm: string[] };
  locationName: string;
  factionName: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose canonical character payloads through campaign and API client contracts</name>
  <files>backend/src/routes/campaigns.ts, backend/src/routes/__tests__/campaigns.test.ts, frontend/lib/api-types.ts, frontend/lib/api.ts, frontend/lib/world-data-helpers.ts, frontend/lib/__tests__/api.test.ts, frontend/lib/__tests__/world-data-helpers.test.ts</files>
  <behavior>
    - Test 1: `/api/campaigns/:id/world` exposes player and NPC canonical fields plus any needed compatibility projections for the current UI.
    - Test 2: frontend API helpers consume the new shared `draft` response from character routes while tolerating temporary compatibility aliases.
    - Test 3: world-data helper transforms canonical NPC/player payloads into editable review state without relying on raw tag/persona/goals-only assumptions.
  </behavior>
  <action>Update `backend/src/routes/campaigns.ts` so world payloads return the canonical player/NPC data needed by editors and review helpers, with additive compatibility projections only where the current frontend still expects them. In `frontend/lib/api-types.ts` and `frontend/lib/api.ts`, replace the split protagonist/NPC contracts with a shared draft shape and make the client prefer the new `draft` response introduced in Plan 29-02 while still accepting legacy alias fields during the migration. Refactor `frontend/lib/world-data-helpers.ts` to map canonical world payloads into review state by reading grouped fields instead of assuming NPC meaning lives only in `persona`, `tags`, and `goals`. Keep the result compatible with the current pages and do not redesign routes or layout patterns.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/routes/__tests__/campaigns.test.ts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix frontend exec vitest run lib/__tests__/api.test.ts lib/__tests__/world-data-helpers.test.ts</automated>
  </verify>
  <done>Backend world payloads and frontend client helpers now share one canonical player/NPC data contract with migration-safe compatibility handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update current character and NPC editors to use the shared model without a UI redesign</name>
  <files>frontend/app/campaign/[id]/character/page.tsx, frontend/app/campaign/[id]/review/page.tsx, frontend/components/character-creation/character-card.tsx, frontend/components/world-review/npcs-section.tsx, frontend/components/character-creation/__tests__/character-card.test.tsx, frontend/components/world-review/__tests__/npcs-section.test.tsx</files>
  <behavior>
    - Test 1: the character page edits shared grouped fields for protagonists and saves via the shared draft payload.
    - Test 2: the NPC review section edits the same grouped fields for NPCs instead of a one-off reduced NPC model.
    - Test 3: the current UI structure remains recognizable; this plan migrates data handling, not the Phase 32 layout overhaul.
  </behavior>
  <action>Refactor the character and world-review pages to hold shared draft state rather than separate `ParsedCharacter` and `ScaffoldNpc` mental models. Update `character-card.tsx` and `npcs-section.tsx` so they edit grouped shared fields using the existing component language and helpers already in the repo. Preserve the current page flow and overall component layout per D-07; this is a data-contract migration, not a visual redesign. Keep the current free-text starting-situation helper as a compatibility seam only, and do not add Phase 30 persisted start-condition UX. Likewise, do not add the broader persona-template or loadout UI that belongs to Phase 30/32.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run components/character-creation/__tests__/character-card.test.tsx components/world-review/__tests__/npcs-section.test.tsx</automated>
  </verify>
  <done>The existing character and NPC editors now use the shared character draft model while staying within the current UI shell and flow boundaries.</done>
</task>

</tasks>

<verification>
Run the backend campaign-route regression plus the four frontend tests, then check that the current creation/review flows use one shared player/NPC draft contract without introducing Phase 30 or Phase 32 scope.
</verification>

<success_criteria>
Phase 29 now reaches the user-facing editor layer: campaign world payloads, frontend API helpers, and the current character/review editors all speak the same shared player/NPC contract while the UI shell remains otherwise intact.
</success_criteria>

<output>
After completion, create `.planning/phases/29-unified-character-ontology-and-tag-system/29-05-SUMMARY.md`
</output>
