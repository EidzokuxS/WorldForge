---
phase: 29-unified-character-ontology-and-tag-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/types.ts
  - shared/src/index.ts
  - backend/src/db/schema.ts
  - backend/src/character/record-adapters.ts
  - backend/src/character/runtime-tags.ts
  - backend/src/routes/schemas.ts
  - backend/src/character/__tests__/record-adapters.test.ts
  - backend/src/routes/__tests__/schemas.test.ts
autonomous: true
requirements:
  - P29-01
  - P29-02
must_haves:
  truths:
    - "Player and NPC data share one canonical draft/record contract instead of separate flat models, per D-01 and D-03."
    - "Any runtime tag array kept in storage or prompts is explicitly derived from structured character fields, not authored by hand, per D-02."
    - "The repo has a migration-safe persistence bridge for canonical character data without starting Phase 30 scenario/loadout behavior."
  artifacts:
    - path: "shared/src/types.ts"
      provides: "Shared CharacterDraft and CharacterRecord contracts with grouped ontology fields"
    - path: "backend/src/character/record-adapters.ts"
      provides: "Legacy row-to-record and record-to-legacy bridge helpers"
    - path: "backend/src/character/runtime-tags.ts"
      provides: "Central derived-runtime-tag rules"
    - path: "backend/src/db/schema.ts"
      provides: "Player/NPC persistence seam for canonical record data and derived tag cache"
  key_links:
    - from: "shared/src/types.ts"
      to: "backend/src/character/record-adapters.ts"
      via: "shared ontology contracts consumed by backend adapters"
      pattern: "CharacterDraft|CharacterRecord"
    - from: "backend/src/character/runtime-tags.ts"
      to: "backend/src/db/schema.ts"
      via: "derived tag cache written beside canonical record storage"
      pattern: "derive.*Runtime.*Tags|derivedTags"
    - from: "backend/src/routes/schemas.ts"
      to: "shared/src/types.ts"
      via: "request payload validation for shared character drafts"
      pattern: "characterDraft|characterRecord|saveCharacter"
---

<objective>
Create the contract and storage foundation that every later Phase 29 plan builds on.

Purpose: Phase 29 fails if executors keep patching separate player, NPC, and scaffold shapes. This plan locks one canonical ontology, central derived-tag logic, and a migration seam that preserves existing behavior while the rest of the repo migrates.
Output: shared character contracts, backend adapter helpers, runtime-tag derivation rules, and schema support for canonical character storage.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-CONTEXT.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-character-ontology-spec.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-phase-29-30-handoff.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-01-SUMMARY.md
@shared/src/types.ts
@backend/src/db/schema.ts
@backend/src/routes/schemas.ts

<decisions>
- D-01: One canonical character model serves both player and NPC flows; role and tier are attributes, not separate mental models.
- D-02: Flat tags are retained only as derived runtime outputs where still useful.
- D-03: Profile facets such as race, gender, age, and appearance are shared across player and NPC flows.
- D-04: Equipment moves toward a first-class shared model instead of a player-only `equippedItems` string list.
- D-05: Phase 29 owns the ontology, canonical data model, migration seams, adapters, and shared derivation layer.
- D-06: Phase 29 must not fully implement richer start-condition scenario behavior from Phase 30.
- D-07: Phase 29 must not turn into the Phase 31 prompt rewrite or Phase 32 UI overhaul.
</decisions>

<interfaces>
Current shared/player and scaffold seams:
```ts
export interface PlayerCharacter {
  name: string;
  race: string;
  gender: string;
  age: string;
  appearance: string;
  tags: string[];
  hp: number;
  equippedItems: string[];
  locationName: string;
}

export interface ScaffoldNpc {
  name: string;
  persona: string;
  tags: string[];
  goals: { shortTerm: string[]; longTerm: string[] };
  locationName: string;
  factionName: string | null;
  tier?: "key" | "supporting";
}
```

Current save payload seam:
```ts
export const saveCharacterSchema = z.object({
  campaignId: z.string().min(1),
  character: z.object({
    name: z.string().min(1),
    race: z.string().max(100).default(""),
    gender: z.string().max(100).default(""),
    age: z.string().max(100).default(""),
    appearance: z.string().max(1000).default(""),
    tags: z.array(z.string()),
    hp: z.number().int().min(1).max(5),
    equippedItems: z.array(z.string()),
    locationName: z.string().min(1),
  }),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared character contracts, adapters, and derived-tag rules</name>
  <files>shared/src/types.ts, shared/src/index.ts, backend/src/character/record-adapters.ts, backend/src/character/runtime-tags.ts, backend/src/character/__tests__/record-adapters.test.ts</files>
  <behavior>
    - Test 1: a legacy player row and a legacy NPC row both hydrate into the same `CharacterRecord` shape with role and tier carried as attributes.
    - Test 2: derived runtime tags come only from canonical buckets such as capabilities, motivations, conditions, and social context, never from free-form legacy tag passthrough.
    - Test 3: adapters can emit a compatibility view for legacy callers without losing canonical structured data.
  </behavior>
  <action>Add the canonical ontology contracts from the Phase 28 spec directly into shared types per D-01 through D-05: identity, profile, socialContext, motivations, capabilities, state, loadout, and provenance, wrapped in shared `CharacterDraft` and `CharacterRecord` types. Keep any Phase 30-facing fields such as richer start-condition details or canonical loadout materialization either absent or inert/optional; do not implement their behavior yet per D-06. In backend adapters, centralize all row-to-record and record-to-legacy translation so later plans never reconstruct meaning ad hoc from `players` or `npcs` rows. Add one pure `deriveRuntimeCharacterTags(record)` helper that maps structured buckets to compact runtime tags and documents which buckets are allowed to participate per D-02. Do not keep any code path that treats flat tags as the authoritative edit surface once the adapter exists.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/character/__tests__/record-adapters.test.ts</automated>
  </verify>
  <done>Shared character contracts exist, backend adapters can hydrate legacy rows into them, and runtime tags have one central derivation helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the persistence and validation seam for canonical character records</name>
  <files>backend/src/db/schema.ts, backend/src/routes/schemas.ts, backend/src/routes/__tests__/schemas.test.ts</files>
  <behavior>
    - Test 1: player and NPC persistence schemas include a canonical character-record field plus a derived-tag cache field while preserving legacy columns for migration safety.
    - Test 2: `saveCharacterSchema` and scaffold-related schemas accept the shared draft shape needed by later plans without requiring Phase 30 start-condition fields.
    - Test 3: older minimal payloads still validate through explicit compatibility bridges where Phase 29 needs additive migration support.
  </behavior>
  <action>Extend `backend/src/db/schema.ts` with a migration-safe canonical storage seam on both `players` and `npcs` per D-05. Keep the existing columns that gameplay still depends on, but add explicit JSON text columns for the canonical record payload and for the derived runtime-tag cache so later plans can migrate readers incrementally instead of smashing the table layout. In `backend/src/routes/schemas.ts`, introduce shared Zod schemas for the canonical character draft/record payloads, use them in `saveCharacterSchema`, and extend scaffold NPC validation so Phase 29 can carry grouped character data through save/edit flows. Preserve `resolveStartingLocationSchema` and do not require full `startConditions` or canonical loadout resolution here per D-06. Add schema regressions proving the additive migration path.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/routes/__tests__/schemas.test.ts</automated>
  </verify>
  <done>The DB and route layer have one explicit storage/validation seam for canonical character data, while legacy columns remain available as compatibility bridges.</done>
</task>

</tasks>

<verification>
Run the adapter and schema tests, then confirm there is exactly one named shared character contract and exactly one runtime-tag derivation helper for later plans to consume.
</verification>

<success_criteria>
Phase 29 now has a contract-first foundation: one canonical shared character model, one derived-tag rule set, and a storage/validation seam that preserves compatibility while the rest of the repo migrates.
</success_criteria>

<output>
After completion, create `.planning/phases/29-unified-character-ontology-and-tag-system/29-01-SUMMARY.md`
</output>
