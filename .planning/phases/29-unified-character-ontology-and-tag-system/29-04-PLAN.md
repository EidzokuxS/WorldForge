---
phase: 29-unified-character-ontology-and-tag-system
plan: 04
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - backend/src/engine/tool-executor.ts
  - backend/src/engine/npc-tools.ts
  - backend/src/engine/reflection-agent.ts
  - backend/src/engine/reflection-tools.ts
  - backend/src/engine/state-snapshot.ts
  - backend/src/engine/__tests__/tool-executor.test.ts
  - backend/src/engine/__tests__/reflection-agent.test.ts
  - backend/src/engine/__tests__/reflection-progression.test.ts
  - backend/src/engine/__tests__/state-snapshot.test.ts
autonomous: true
requirements:
  - P29-06
must_haves:
  truths:
    - "Runtime mutations update canonical character data and regenerate derived runtime tags together, rather than mutating raw tag arrays as the source of truth."
    - "NPC tool flows and reflection flows still change goals, beliefs, skill/wealth tiers, and temporary NPC spawns without regressing behavior."
    - "Undo/snapshot logic remains valid after canonical character data starts changing during play."
  artifacts:
    - path: "backend/src/engine/tool-executor.ts"
      provides: "Canonical mutation bridge for storyteller tool writes"
    - path: "backend/src/engine/npc-tools.ts"
      provides: "NPC action tools reading/writing through canonical character adapters"
    - path: "backend/src/engine/reflection-tools.ts"
      provides: "Structured capability and motivation updates with derived-tag regeneration"
    - path: "backend/src/engine/state-snapshot.ts"
      provides: "Rollback-safe snapshot support for canonical character fields"
  key_links:
    - from: "backend/src/engine/tool-executor.ts"
      to: "backend/src/character/runtime-tags.ts"
      via: "regenerate derived tags after canonical mutations"
      pattern: "derive.*Runtime.*Tags|tags"
    - from: "backend/src/engine/reflection-tools.ts"
      to: "backend/src/character/record-adapters.ts"
      via: "skill and wealth updates applied to canonical capabilities"
      pattern: "capabilities|motivations|beliefs"
    - from: "backend/src/engine/state-snapshot.ts"
      to: "backend/src/engine/tool-executor.ts"
      via: "rollback after canonical-record mutations"
      pattern: "characterRecord|derivedTags|snapshot"
---

<objective>
Migrate runtime mutation paths onto the canonical character model.

Purpose: Read-side migration is insufficient if tools and reflection still mutate raw `tags`, `goals`, and `beliefs` blobs directly. This plan moves runtime writes through the same canonical adapter seam while preserving gameplay behavior and undo safety.
Output: canonical mutation handling for storyteller tools, NPC tools, reflection tools, and snapshots.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-CONTEXT.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-character-ontology-spec.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-phase-29-30-handoff.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-01-PLAN.md
@backend/src/engine/tool-executor.ts
@backend/src/engine/npc-tools.ts
@backend/src/engine/reflection-agent.ts
@backend/src/engine/reflection-tools.ts
@backend/src/engine/state-snapshot.ts

<decisions>
- D-01: One canonical character model serves both player and NPC flows.
- D-02: Flat tags remain derived compatibility output, not authored source of truth.
- D-04: Equipment stays a bridge field in Phase 29; do not implement Phase 30 canonical loadout materialization here.
- D-05: Phase 29 owns adapters and migration seams.
- D-06: Do not add start-condition behavior in these runtime mutation paths.
</decisions>

<interfaces>
Current mutation hotspots:
```ts
// tool-executor.ts
const currentTags = parseTags(entity.tags);
db.update(table).set({ tags: JSON.stringify(currentTags) });

// reflection-tools.ts
const goals = parseNpcGoals(npc.goals);
db.update(npcs).set({ goals: JSON.stringify(goals) });

// state-snapshot.ts
playerTags: player?.tags ?? "[]",
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route storyteller and NPC tool mutations through canonical character updates</name>
  <files>backend/src/engine/tool-executor.ts, backend/src/engine/npc-tools.ts, backend/src/engine/__tests__/tool-executor.test.ts</files>
  <behavior>
    - Test 1: add/remove tag style mutations update canonical character fields plus regenerated derived tags instead of editing raw `tags` as the sole source of truth.
    - Test 2: spawned temporary NPCs are created with a valid minimal canonical record and legacy compatibility columns.
    - Test 3: NPC action tools still read actor tags and update goals through the canonical adapter seam.
  </behavior>
  <action>Refactor `tool-executor.ts` and `npc-tools.ts` so mutations pass through canonical record helpers from Plan 29-01. For storyteller tag tools, translate add/remove operations into edits on the correct structured bucket where feasible, then regenerate the derived runtime-tag cache per D-02; where a tag has no named structured bucket yet, route it through one explicit compatibility bucket instead of silently editing the raw tag string as primary truth. When spawning temporary NPCs, create a minimal but valid canonical record with role/tier/profile/motivations/state defaults, then backfill the legacy row fields needed by existing runtime code. Preserve movement, HP, and inventory behavior; do not introduce Phase 30 loadout or start-condition logic.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/tool-executor.test.ts</automated>
  </verify>
  <done>Storyteller and NPC tool mutations now write through the canonical character seam and keep the derived tag cache in sync.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move reflection updates and rollback safety onto canonical character data</name>
  <files>backend/src/engine/reflection-agent.ts, backend/src/engine/reflection-tools.ts, backend/src/engine/state-snapshot.ts, backend/src/engine/__tests__/reflection-agent.test.ts, backend/src/engine/__tests__/reflection-progression.test.ts, backend/src/engine/__tests__/state-snapshot.test.ts</files>
  <behavior>
    - Test 1: reflection updates beliefs, goals, wealth tiers, and skill tiers through canonical motivation/capability buckets, then regenerates derived tags.
    - Test 2: reflection-triggered runtime behavior stays compatible with existing prompts and NPC progression semantics.
    - Test 3: snapshots capture and restore the canonical character payload plus the derived-tag cache so undo still works after Phase 29 mutations.
  </behavior>
  <action>Refactor reflection and snapshot logic to stop treating `beliefs`, `goals`, and `tags` JSON columns as the authoritative write target. Apply reflection updates to canonical motivations and capabilities first, regenerate the derived runtime tags, and only then project the legacy columns needed by unconverted systems. Keep the existing wealth/skill tier semantics from Phase 7, but store them in canonical capability buckets rather than inventing new tag-only rules. Update `state-snapshot.ts` so undo captures and restores canonical character payloads alongside any legacy compatibility fields required by current gameplay. Do not broaden this into prompt rewriting or UI work.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/reflection-agent.test.ts src/engine/__tests__/reflection-progression.test.ts src/engine/__tests__/state-snapshot.test.ts</automated>
  </verify>
  <done>Reflection and undo now operate safely against canonical character data while keeping existing gameplay mechanics and progression behavior intact.</done>
</task>

</tasks>

<verification>
Run the reflection, tool-executor, and snapshot regressions, then confirm runtime writes and rollbacks no longer depend on raw tag/goals/beliefs blobs as the primary authority.
</verification>

<success_criteria>
All major gameplay mutation paths now share the same ontology bridge: runtime writes update canonical character data first, regenerate derived tags deterministically, and preserve rollback safety.
</success_criteria>

<output>
After completion, create `.planning/phases/29-unified-character-ontology-and-tag-system/29-04-SUMMARY.md`
</output>
