---
phase: 29-unified-character-ontology-and-tag-system
plan: 03
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - backend/src/engine/prompt-assembler.ts
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/npc-agent.ts
  - backend/src/engine/npc-offscreen.ts
  - backend/src/engine/parse-helpers.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/engine/__tests__/npc-agent.test.ts
  - backend/src/engine/__tests__/npc-offscreen.test.ts
autonomous: true
requirements:
  - P29-04
must_haves:
  truths:
    - "Prompt assembly reads structured player and NPC records first, then derives any compact runtime tag views from them per D-02."
    - "Oracle-facing and NPC-agent-facing runtime readers no longer depend on raw `tags` / `persona` / `goals` JSON as the primary meaning source."
    - "Legacy saves still work because runtime readers hydrate through the shared adapters instead of assuming the new columns already exist."
  artifacts:
    - path: "backend/src/engine/prompt-assembler.ts"
      provides: "Structured prompt sections built from canonical character views"
    - path: "backend/src/engine/turn-processor.ts"
      provides: "Oracle actor/environment tag inputs sourced from derived runtime tags"
    - path: "backend/src/engine/npc-agent.ts"
      provides: "NPC tick prompt assembly sourced from canonical NPC views"
    - path: "backend/src/engine/npc-offscreen.ts"
      provides: "Off-screen NPC simulation sourced from canonical NPC views"
  key_links:
    - from: "backend/src/engine/turn-processor.ts"
      to: "backend/src/character/runtime-tags.ts"
      via: "derived actor tags for Oracle evaluation"
      pattern: "derive.*Runtime.*Tags|actorTags"
    - from: "backend/src/engine/prompt-assembler.ts"
      to: "backend/src/character/record-adapters.ts"
      via: "structured player and NPC view hydration"
      pattern: "toCharacterRecord|from.*Row"
    - from: "backend/src/engine/npc-agent.ts"
      to: "backend/src/engine/npc-offscreen.ts"
      via: "shared canonical NPC context instead of ad hoc JSON parsing"
      pattern: "CharacterRecord|motivations|profile"
---

<objective>
Migrate read-only runtime consumers to the shared character model.

Purpose: Phase 29 is not complete if runtime keeps reconstructing character meaning from raw tags and JSON blobs. This plan switches prompt assembly, Oracle inputs, and NPC read paths to canonical character views without doing the larger Phase 31 prompt rewrite.
Output: structured runtime readers for prompt assembly, turn processing, NPC ticks, and off-screen simulation, backed by compatibility-safe hydration.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-CONTEXT.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-character-ontology-spec.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-phase-29-30-handoff.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-01-PLAN.md
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/turn-processor.ts
@backend/src/engine/npc-agent.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/engine/parse-helpers.ts

<decisions>
- D-01: One canonical character model serves both player and NPC flows.
- D-02: Flat tags are retained only as derived runtime outputs.
- D-03: Profile and motivation fields must be readable for both protagonists and NPCs.
- D-07: This phase must not become the wholesale prompt rewrite promised to Phase 31.
</decisions>

<interfaces>
Current runtime read hotspots:
```ts
// prompt-assembler.ts
const tags = safeParseTags(player.tags);
const equipped = safeParseTags(player.equippedItems);
const goals = safeParseGoals(npc.goals);
const beliefs = safeParseTags(npc.beliefs);

// turn-processor.ts
actorTags = JSON.parse(player.tags) as string[];

// npc-agent.ts
const npcTags = parseTags(npc.tags);
const goals = parseNpcGoals(npc.goals);
const beliefs = parseBeliefs(npc.beliefs);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Switch prompt assembly and Oracle inputs to structured character views</name>
  <files>backend/src/engine/prompt-assembler.ts, backend/src/engine/turn-processor.ts, backend/src/engine/__tests__/prompt-assembler.test.ts, backend/src/engine/__tests__/turn-processor.test.ts</files>
  <behavior>
    - Test 1: prompt sections render profile, motivations, capabilities, and state from canonical player/NPC records before falling back to legacy columns.
    - Test 2: Oracle actor tags in `turn-processor.ts` come from the centralized derived-tag helper rather than raw stored `tags`.
    - Test 3: legacy rows with no canonical record blob still produce the same gameplay-visible prompt/runtime behavior through the adapter fallback.
  </behavior>
  <action>Refactor `prompt-assembler.ts` and `turn-processor.ts` to hydrate canonical player/NPC views through the Plan 29-01 adapter instead of manually parsing raw JSON fields. Build prompt lines from grouped fields first: profile facts, motivations, capabilities, state, and loadout bridge data. When the runtime still needs compact tag arrays for Oracle evaluation or concise prompt lines, call the centralized runtime-tag derivation helper per D-02 instead of reading raw stored tags directly. Keep current section names and output ordering stable; do not perform the broader narrative/prompt-family rewrite deferred to Phase 31 per D-07.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/prompt-assembler.test.ts src/engine/__tests__/turn-processor.test.ts</automated>
  </verify>
  <done>Prompt assembly and Oracle preparation now read canonical character data first and only use derived tag snapshots for compact runtime needs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Switch NPC runtime readers to canonical NPC views</name>
  <files>backend/src/engine/npc-agent.ts, backend/src/engine/npc-offscreen.ts, backend/src/engine/parse-helpers.ts, backend/src/engine/__tests__/npc-agent.test.ts, backend/src/engine/__tests__/npc-offscreen.test.ts</files>
  <behavior>
    - Test 1: NPC agent prompts are built from canonical profile, motivations, capabilities, and derived runtime tags, not raw persona/tag blobs.
    - Test 2: off-screen simulation reads and writes the structured NPC context while preserving current location and goal-progress behavior.
    - Test 3: helper parsing remains backward-compatible for legacy rows during migration.
  </behavior>
  <action>Update the NPC runtime readers so `npc-agent.ts` and `npc-offscreen.ts` stop treating `persona`, `tags`, and `goals` JSON as the primary model. Hydrate canonical NPC views via the adapters, derive compact trait/tag summaries from those views where the LLM prompt still benefits from them, and keep location presence plus goal progress behavior stable. Limit `parse-helpers.ts` to backward-compatibility parsing utilities rather than letting it remain the live domain model. Do not touch tool mutation logic in this plan; that belongs to Plan 29-04.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/npc-agent.test.ts src/engine/__tests__/npc-offscreen.test.ts</automated>
  </verify>
  <done>NPC runtime read paths use canonical structured views with legacy fallbacks, and the old ad hoc JSON parsing stops being the primary source of meaning.</done>
</task>

</tasks>

<verification>
Run the four engine reader test files and inspect the prompt/runtime code paths to confirm all direct reads now enter through the canonical character adapter or the derived-tag helper.
</verification>

<success_criteria>
Runtime reads are now ontology-aware: prompt assembly, Oracle inputs, and NPC context no longer depend on the flat tag pile as the primary character model, yet existing campaigns still behave the same through compatibility fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/29-unified-character-ontology-and-tag-system/29-03-SUMMARY.md`
</output>
