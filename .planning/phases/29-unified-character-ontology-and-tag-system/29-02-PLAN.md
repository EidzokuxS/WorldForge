---
phase: 29-unified-character-ontology-and-tag-system
plan: 02
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - backend/src/character/generator.ts
  - backend/src/character/npc-generator.ts
  - backend/src/character/index.ts
  - backend/src/routes/character.ts
  - backend/src/worldgen/types.ts
  - backend/src/worldgen/scaffold-steps/npcs-step.ts
  - backend/src/worldgen/scaffold-saver.ts
  - backend/src/routes/__tests__/character.test.ts
  - backend/src/worldgen/__tests__/npcs-step.test.ts
  - backend/src/worldgen/__tests__/scaffold-saver.test.ts
autonomous: true
requirements:
  - P29-03
must_haves:
  truths:
    - "Player parse/generate/import flows and key-NPC parse/generate/import flows emit the same underlying draft contract per D-01."
    - "Worldgen NPC creation and scaffold save logic use the shared draft/record pipeline instead of a separate reduced NPC model."
    - "Character save keeps current location-based start handling intact without implementing Phase 30 start-state specifics."
  artifacts:
    - path: "backend/src/character/generator.ts"
      provides: "Player draft generation aligned to the shared ontology"
    - path: "backend/src/character/npc-generator.ts"
      provides: "NPC draft generation aligned to the shared ontology"
    - path: "backend/src/worldgen/types.ts"
      provides: "Scaffold NPC contract carrying shared character draft fields"
    - path: "backend/src/worldgen/scaffold-saver.ts"
      provides: "Shared draft-to-persistence save path for worldgen NPCs"
  key_links:
    - from: "backend/src/routes/character.ts"
      to: "backend/src/character/generator.ts"
      via: "player creation endpoints emitting shared draft payloads"
      pattern: "parse-character|generate-character|research-character|import-v2-card"
    - from: "backend/src/routes/character.ts"
      to: "backend/src/character/npc-generator.ts"
      via: "key-NPC creation endpoints emitting shared draft payloads"
      pattern: "role === \"key\"|draft"
    - from: "backend/src/worldgen/scaffold-steps/npcs-step.ts"
      to: "backend/src/worldgen/scaffold-saver.ts"
      via: "shared draft fields carried from generation into DB persistence"
      pattern: "CharacterDraft|characterRecord|canonicalCharacter"
---

<objective>
Move all character-producing writers onto the shared draft pipeline.

Purpose: The contract from Plan 29-01 only matters if character generation, NPC generation, and scaffold save paths stop inventing their own shapes. This plan makes the repo's write paths converge on one draft model while preserving current game startup behavior.
Output: shared draft output from character/NPC routes and shared scaffold NPC persistence through the same ontology bridge.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-CONTEXT.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-character-ontology-spec.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-phase-29-30-handoff.md
@.planning/phases/29-unified-character-ontology-and-tag-system/29-01-PLAN.md
@backend/src/character/generator.ts
@backend/src/character/npc-generator.ts
@backend/src/routes/character.ts
@backend/src/worldgen/types.ts
@backend/src/worldgen/scaffold-steps/npcs-step.ts
@backend/src/worldgen/scaffold-saver.ts

<decisions>
- D-01: One canonical character model serves both player and NPC flows.
- D-02: Flat tags remain derived compatibility output, not source of truth.
- D-03: Profile fields and motivation fields must be available for both protagonists and NPCs.
- D-04: Equipment/loadout stays a shared bridge concept, but Phase 29 does not materialize Phase 30 canonical loadouts yet.
- D-05: Phase 29 owns adapters and migration seams.
- D-06: Do not implement richer start-condition scenario behavior here.
</decisions>

<interfaces>
Current split generation return contracts:
```ts
export type ParsedCharacter = z.infer<typeof characterSchema>;
export type GeneratedNpc = z.infer<typeof npcSchema>;

export type CharacterResult =
  | { role: "player"; character: ParsedCharacter }
  | { role: "key"; npc: ScaffoldNpc };
```

Current scaffold NPC contract:
```ts
export interface ScaffoldNpc {
  name: string;
  persona: string;
  tags: string[];
  goals: { shortTerm: string[]; longTerm: string[] };
  locationName: string;
  factionName: string | null;
  tier?: "key" | "supporting";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unify player and key-NPC route outputs around one shared character draft</name>
  <files>backend/src/character/generator.ts, backend/src/character/npc-generator.ts, backend/src/character/index.ts, backend/src/routes/character.ts, backend/src/routes/__tests__/character.test.ts</files>
  <behavior>
    - Test 1: parse/generate/research/import endpoints for both `role: "player"` and `role: "key"` produce the same underlying grouped draft shape.
    - Test 2: character route responses stay migration-friendly by exposing the new shared draft while preserving additive compatibility for existing caller fields during Phase 29.
    - Test 3: saving a protagonist persists through the canonical adapter path while keeping current location-based start behavior unchanged.
  </behavior>
  <action>Rewrite the player and NPC generators so both build the same shared draft buckets from their LLM output per D-01 through D-03. Preserve role-specific prompting where needed, but stop returning unrelated data models. In `backend/src/routes/character.ts`, expose a shared `draft` payload for all creation endpoints and keep additive `character` / `npc` aliases only as a temporary compatibility bridge until Plan 29-05 updates the frontend. Route all protagonist saves through the canonical adapter from Plan 29-01, then backfill the legacy player columns needed by current gameplay. Keep `/resolve-starting-location` and the location-name save boundary working exactly as they do now; do not add persisted Phase 30 start-condition objects here per D-06.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/routes/__tests__/character.test.ts</automated>
  </verify>
  <done>All direct character-creation endpoints now produce one shared draft contract and save protagonists through the canonical adapter path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align worldgen NPC generation and scaffold persistence to the shared draft model</name>
  <files>backend/src/worldgen/types.ts, backend/src/worldgen/scaffold-steps/npcs-step.ts, backend/src/worldgen/scaffold-saver.ts, backend/src/worldgen/__tests__/npcs-step.test.ts, backend/src/worldgen/__tests__/scaffold-saver.test.ts</files>
  <behavior>
    - Test 1: scaffold NPC generation returns the shared character draft fields needed by later review and runtime readers, not a reduced NPC-only shape.
    - Test 2: scaffold saver persists those drafts through the same canonical adapter logic used by direct NPC creation.
    - Test 3: existing faction/location relationships and tier mapping still survive the migration.
  </behavior>
  <action>Update `backend/src/worldgen/types.ts` and the NPC scaffold step so worldgen NPCs stop being the odd reduced model described in the Phase 28 audit. Carry shared profile, motivations, capabilities, and provenance fields through the scaffold contract while still exposing whatever compatibility aliases current review code needs until Plan 29-05. In `backend/src/worldgen/scaffold-saver.ts`, persist scaffold NPCs through the same canonical record bridge used elsewhere, then populate legacy columns (`persona`, `goals`, `beliefs`, `tags`, `tier`, `currentLocationId`) from the adapter output so gameplay remains stable. Do not introduce Phase 30 persona-template storage or canonical item materialization here.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldgen/__tests__/npcs-step.test.ts src/worldgen/__tests__/scaffold-saver.test.ts</automated>
  </verify>
  <done>Worldgen NPCs now use the shared draft pipeline and persist through the same canonical adapter seam as direct character creation.</done>
</task>

</tasks>

<verification>
Run the route and worldgen regressions, then confirm that every player/NPC writer in the repo now produces or consumes the shared draft contract from Plan 29-01.
</verification>

<success_criteria>
Phase 29 no longer has separate writer-side models for players, direct-created NPCs, and scaffold NPCs. The creation pipeline now converges on one shared draft contract with compatibility aliases only where the rest of the repo still needs them.
</success_criteria>

<output>
After completion, create `.planning/phases/29-unified-character-ontology-and-tag-system/29-02-SUMMARY.md`
</output>
