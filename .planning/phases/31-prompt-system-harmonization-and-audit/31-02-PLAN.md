---
phase: 31-prompt-system-harmonization-and-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - backend/src/engine/prompt-assembler.ts
  - backend/src/engine/tool-schemas.ts
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
  - backend/src/engine/__tests__/bug-fixes-verification.test.ts
autonomous: true
requirements:
  - P31-02
  - P31-06
must_haves:
  truths:
    - "Runtime narration, outcome framing, and tool obligations read from one coherent storyteller contract instead of contradictory inline copies."
    - "Runtime prompt text acknowledges canonical character/start/loadout context before derived tags."
    - "Regression coverage proves stale or duplicated runtime instructions were removed."
  artifacts:
    - path: "backend/src/engine/prompt-assembler.ts"
      provides: "Runtime prompt assembly that imports authoritative storyteller fragments"
    - path: "backend/src/engine/tool-schemas.ts"
      provides: "Tool descriptions limited to semantics instead of re-authoring narrator policy"
    - path: "backend/src/engine/turn-processor.ts"
      provides: "Outcome framing that supplements runtime authority rather than competing with it"
  key_links:
    - from: "backend/src/engine/prompt-assembler.ts"
      to: "backend/src/engine/storyteller-contract.ts"
      via: "imported runtime authority fragments"
      pattern: "storyteller-contract"
    - from: "backend/src/engine/tool-schemas.ts"
      to: "backend/src/engine/prompt-assembler.ts"
      via: "non-contradictory runtime/tool boundary"
      pattern: "offer_quick_actions|set_condition|move_to"
---

<objective>
Refactor the runtime storyteller family so only one layer owns narrator policy and canonical-character framing.

Purpose: remove the highest-risk prompt drift in the repo by untangling `SYSTEM_RULES`, tool descriptions, and outcome overlays into a coherent runtime contract.
Output: runtime files that import shared storyteller fragments, plus negative-assertion regressions for stale or duplicate instructions.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-prompt-system-harmonization-and-audit/31-RESEARCH.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-prompt-contract-rules.md
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/tool-schemas.ts
@backend/src/engine/turn-processor.ts
@backend/src/engine/__tests__/prompt-assembler.test.ts
@backend/src/engine/__tests__/bug-fixes-verification.test.ts

<interfaces>
From `backend/src/engine/prompt-assembler.ts`:
```typescript
export interface AssembleOptions {
  campaignId: string;
  contextWindow: number;
  actionResult?: { chance: number; roll: number; outcome: string; reasoning: string };
  embedderResult?: ResolveResult;
  playerAction?: string;
  judgeRole?: ResolvedRole;
}
```

From `backend/src/engine/tool-schemas.ts`:
```typescript
export function createStorytellerTools(
  campaignId: string,
  tick: number,
  outcomeTier?: string,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild runtime narration around the shared storyteller contract</name>
  <files>backend/src/engine/prompt-assembler.ts, backend/src/engine/turn-processor.ts, backend/src/engine/__tests__/prompt-assembler.test.ts</files>
  <behavior>
    - Test 1: assembled runtime prompts still include the hard output constraints and outcome fidelity rules.
    - Test 2: runtime prompt text now describes canonical player/NPC/start/loadout context before derived tags and no longer uses the unqualified "all characters use a tag-based system" wording.
    - Test 3: tests fail when stale tag-first or duplicate runtime policy strings survive.
  </behavior>
  <action>Refactor `prompt-assembler.ts` to import from `backend/src/engine/storyteller-contract.ts` and keep `SYSTEM_RULES` as a composition of authoritative fragments instead of an ever-growing monolith. Preserve high-value hard constraints (narrative-only output, bracket ban, outcome fidelity, inventory truth, world consistency) while replacing stale tag-first worldview lines with canonical-field-first wording backed by Phase 29/30 data already exposed in `buildPlayerStateSection()` and NPC hydration. In `turn-processor.ts`, keep outcome-tier instructions strictly outcome-specific; do not leave it silently re-authoring generic storyteller policy already owned by the contract helper. Extend `prompt-assembler.test.ts` with negative assertions for removed stale text and positive assertions for the canonical-field/start-context wording.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/prompt-assembler.test.ts src/engine/__tests__/turn-processor.test.ts</automated>
  </verify>
  <done>The runtime assembler uses the shared storyteller contract, canonical player/start context is explicit, and prompt tests fail if tag-only runtime wording returns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce storyteller tool descriptions to support semantics only</name>
  <files>backend/src/engine/tool-schemas.ts, backend/src/engine/__tests__/bug-fixes-verification.test.ts</files>
  <behavior>
    - Test 1: tool descriptions retain essential execution semantics for `offer_quick_actions`, `set_condition`, and `move_to`.
    - Test 2: regression coverage fails if tool descriptions quietly redefine storyteller policy that now belongs in the runtime contract helper.
    - Test 3: quick-action and HP obligations remain enforced through the runtime family without contradictory duplicate wording in tool descriptions.
  </behavior>
  <action>Trim `tool-schemas.ts` so descriptions explain tool intent, required inputs, and tool-specific semantic constraints, but no longer act as a second hidden narrator rulebook. Preserve AI-SDK usability for self-correction, yet remove the duplicated "after every narration" / HP-policy prose wherever `storyteller-contract.ts` is now authoritative. Extend `bug-fixes-verification.test.ts` with negative assertions guarding against duplicate-authority reintroduction; if the file becomes awkward, keep the assertions tightly scoped and do not broaden it into a snapshot suite.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/bug-fixes-verification.test.ts</automated>
  </verify>
  <done>Tool descriptions are semantically useful but no longer redefine storyteller policy, and regression tests explicitly catch duplicate-authority regressions.</done>
</task>

</tasks>

<verification>
Run the runtime-focused test slice to confirm prompt assembly, outcome framing, and tool descriptions now align to one authoritative contract.
</verification>

<success_criteria>
Runtime narration no longer spreads mandatory behavior across contradictory copies, and targeted tests prove both the presence of new contract fragments and the removal of stale ones.
</success_criteria>

<output>
After completion, create `.planning/phases/31-prompt-system-harmonization-and-audit/31-02-SUMMARY.md`
</output>
