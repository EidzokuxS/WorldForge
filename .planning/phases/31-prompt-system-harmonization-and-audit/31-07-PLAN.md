---
phase: 31-prompt-system-harmonization-and-audit
plan: 07
type: execute
wave: 3
depends_on:
  - 31-01
files_modified:
  - backend/src/engine/oracle.ts
  - backend/src/engine/world-engine.ts
  - backend/src/engine/__tests__/oracle.test.ts
  - backend/src/engine/__tests__/world-engine.test.ts
autonomous: true
requirements:
  - P31-05
  - P31-06
must_haves:
  truths:
    - "Oracle and faction/world-engine prompts remain tightly scoped to their own jobs while following the shared contract language where relevant."
    - "Deterministic calibration bands and world-engine action semantics remain protected."
    - "Tests fail if stale generic worldview instructions replace the harmonized support-prompt wording."
  artifacts:
    - path: "backend/src/engine/oracle.ts"
      provides: "Deterministic judge prompt aligned to the shared prompt vocabulary"
    - path: "backend/src/engine/world-engine.ts"
      provides: "Faction macro prompt aligned to the shared prompt vocabulary"
    - path: "backend/src/engine/__tests__/oracle.test.ts"
      provides: "Regression coverage for Oracle calibration plus stale-phrase removal"
    - path: "backend/src/engine/__tests__/world-engine.test.ts"
      provides: "Regression coverage for faction macro prompt wording"
  key_links:
    - from: "backend/src/engine/oracle.ts"
      to: "backend/src/character/prompt-contract.ts"
      via: "shared terminology only where actor/target evidence references canonical records indirectly"
      pattern: "actorTags|targetTags|environmentTags"
    - from: "backend/src/engine/world-engine.ts"
      to: "backend/src/worldgen/scaffold-steps/prompt-utils.ts"
      via: "shared naming/canon vocabulary expectations"
      pattern: "faction|territory|world state"
---

<objective>
Finish the support-family audit on Oracle and faction/world-engine prompts without regressing their narrow task boundaries.

Purpose: these support prompts should be coherent with the Phase 31 shared contract, but they must stay specialized and deterministic rather than absorbing unrelated runtime or character-family rules.
Output: harmonized judge/faction prompt wording plus regression assertions that protect calibration and macro-action semantics.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-prompt-system-harmonization-and-audit/31-RESEARCH.md
@backend/src/engine/oracle.ts
@backend/src/engine/world-engine.ts
@backend/src/engine/__tests__/oracle.test.ts
@backend/src/engine/__tests__/world-engine.test.ts

<interfaces>
From `backend/src/engine/oracle.ts`:
```typescript
export interface OraclePayload {
  intent: string;
  method: string;
  actorTags: string[];
  targetTags: string[];
  environmentTags: string[];
  sceneContext: string;
}

export interface OracleResult {
  chance: number;
  roll: number;
  outcome: OutcomeTier;
  reasoning: string;
}
```

From `backend/src/engine/world-engine.ts`:
```typescript
export interface FactionTickResult {
  factionId: string;
  factionName: string;
  toolCalls: Array<{ tool: string; args: unknown; result: unknown }>;
  error?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit the Oracle prompt without changing its deterministic contract</name>
  <files>backend/src/engine/oracle.ts, backend/src/engine/__tests__/oracle.test.ts</files>
  <behavior>
    - Test 1: Oracle still enforces temperature 0, 1-99 chance bounds, and the existing calibration bands.
    - Test 2: prompt wording remains tightly scoped to probability judgment and does not drift into stale generic prompt-system worldview text.
    - Test 3: negative assertions fail if stale wording or contradictory support-prompt instructions reappear.
  </behavior>
  <action>Audit `oracle.ts` so its system prompt language stays deterministic and calibration-focused while aligning terminology with the shared Phase 31 contract where it references actor/target evidence. Do not widen Oracle into a character generator or runtime narrator. Keep its output schema, roll/outcome resolution, and fallback behavior intact. Extend `oracle.test.ts` with narrowly scoped assertions for preserved calibration language and the absence of stale worldview text.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/oracle.test.ts</automated>
  </verify>
  <done>The Oracle prompt remains deterministic and calibrated, but any stale support-prompt worldview drift is now test-detectable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audit faction/world-engine prompts for coherent shared vocabulary</name>
  <files>backend/src/engine/world-engine.ts, backend/src/engine/__tests__/world-engine.test.ts</files>
  <behavior>
    - Test 1: faction prompts still require concrete, observable macro actions and preserve existing tool semantics.
    - Test 2: prompt wording aligns to the shared contract and world-state vocabulary without falling back to generic tag-only worldview statements.
    - Test 3: negative assertions fail if stale or contradictory prompt guidance returns.
  </behavior>
  <action>Audit `world-engine.ts` so the faction macro prompt stays focused on goals, territory, neighbors, and chronicle-backed world state while using harmonized terminology that fits the Phase 31 contract. Do not broaden the file into runtime narration or rewrite world simulation mechanics. Extend `world-engine.test.ts` with positive assertions for preserved macro-action semantics and negative assertions for stale generic worldview text.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/world-engine.test.ts</automated>
  </verify>
  <done>The world-engine prompt remains task-specific and coherent with the shared contract, and tests now protect both macro-action semantics and stale-instruction removal.</done>
</task>

</tasks>

<verification>
Run the Oracle and faction-engine test slice to confirm support-family harmonization did not disturb deterministic judgment or macro-simulation semantics.
</verification>

<success_criteria>
Oracle and faction/world-engine prompts stay tightly scoped, share coherent vocabulary with the rest of the prompt system, and are protected against stale-instruction regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/31-prompt-system-harmonization-and-audit/31-07-SUMMARY.md`
</output>
