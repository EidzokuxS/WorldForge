---
phase: 31-prompt-system-harmonization-and-audit
plan: 05
type: execute
wave: 3
depends_on:
  - 31-01
files_modified:
  - backend/src/worldgen/scaffold-steps/prompt-utils.ts
  - backend/src/worldgen/seed-suggester.ts
  - backend/src/worldgen/lore-extractor.ts
  - backend/src/worldgen/__tests__/seed-suggester.test.ts
  - backend/src/worldgen/__tests__/lore-extractor.test.ts
autonomous: true
requirements:
  - P31-05
  - P31-06
must_haves:
  truths:
    - "Worldgen helper layers remain authoritative for canon/divergence rules while adopting the shared character/start vocabulary where relevant."
    - "Seed and lore prompt families do not reintroduce stale tag-first or split-character assumptions."
    - "Known-IP canon preservation remains protected by targeted regressions."
  artifacts:
    - path: "backend/src/worldgen/scaffold-steps/prompt-utils.ts"
      provides: "Worldgen helper fragments that remain the canon/divergence authority"
    - path: "backend/src/worldgen/seed-suggester.ts"
      provides: "DNA suggestion prompts aligned to shared prompt vocabulary"
    - path: "backend/src/worldgen/lore-extractor.ts"
      provides: "Lore extraction prompts aligned to shared prompt vocabulary"
  key_links:
    - from: "backend/src/worldgen/seed-suggester.ts"
      to: "backend/src/worldgen/scaffold-steps/prompt-utils.ts"
      via: "shared worldgen helper reuse"
      pattern: "buildIpContextBlock|buildPremiseDivergenceBlock|buildKnownIpGenerationContract"
    - from: "backend/src/worldgen/lore-extractor.ts"
      to: "backend/src/worldgen/scaffold-steps/prompt-utils.ts"
      via: "shared worldgen helper reuse"
      pattern: "buildIpContextBlock|buildPremiseDivergenceBlock|buildKnownIpGenerationContract"
---

<objective>
Audit the non-NPC worldgen prompt helpers so they stay aligned with the shared contract while preserving Phase 25 canon/divergence authority.

Purpose: worldgen already has the strongest helper reuse in the repo; this plan should keep that advantage while removing any remaining stale assumptions rather than rewriting worldgen wholesale.
Output: updated helper wording where needed, plus regressions for seed and lore prompt families.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-prompt-system-harmonization-and-audit/31-RESEARCH.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-prompt-contract-rules.md
@backend/src/worldgen/scaffold-steps/prompt-utils.ts
@backend/src/worldgen/seed-suggester.ts
@backend/src/worldgen/lore-extractor.ts
@backend/src/worldgen/__tests__/seed-suggester.test.ts
@backend/src/worldgen/__tests__/lore-extractor.test.ts

<interfaces>
From `backend/src/worldgen/scaffold-steps/prompt-utils.ts`:
```typescript
export function buildIpContextBlock(ipContext: IpResearchContext | null): string;
export function buildPremiseDivergenceBlock(premiseDivergence: PremiseDivergence | null): string;
export function buildKnownIpGenerationContract(
  ipContext: IpResearchContext | null,
  premiseDivergence: PremiseDivergence | null,
  taskLabel: string,
): string;
export function buildStopSlopRules(): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keep prompt-utils authoritative while adding shared contract hooks only where needed</name>
  <files>backend/src/worldgen/scaffold-steps/prompt-utils.ts, backend/src/worldgen/seed-suggester.ts, backend/src/worldgen/__tests__/seed-suggester.test.ts</files>
  <behavior>
    - Test 1: seed prompts still include the Phase 25 canon/divergence blocks and stop-slop rules.
    - Test 2: any shared character/start vocabulary added to worldgen stays family-local and does not replace `prompt-utils.ts` as the canon/divergence authority.
    - Test 3: negative assertions fail if stale tag-first or split-character assumptions return to DNA prompts.
  </behavior>
  <action>Audit `prompt-utils.ts` and `seed-suggester.ts` together. Preserve `buildIpContextBlock()`, `buildPremiseDivergenceBlock()`, and `buildKnownIpGenerationContract()` as the authoritative worldgen helpers, but add or thread shared contract wording only where the family needs to reference character/start semantics. Do not spread Phase 31 character helpers across every worldgen file blindly and do not weaken any known-IP preservation language. Extend `seed-suggester.test.ts` with negative assertions for stale worldview text while keeping existing canon/divergence assertions green.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldgen/__tests__/seed-suggester.test.ts</automated>
  </verify>
  <done>Worldgen helper reuse remains authoritative, seed prompts stay Phase-25-safe, and tests catch any return of stale prompt assumptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audit lore extraction prompts against the same shared contract rules</name>
  <files>backend/src/worldgen/lore-extractor.ts, backend/src/worldgen/__tests__/lore-extractor.test.ts</files>
  <behavior>
    - Test 1: lore extraction still carries preserved/changed canon facts and world-state directives for known-IP flows.
    - Test 2: prompt wording avoids stale tag-first or split-character assumptions when referencing people or start-state facts.
    - Test 3: negative assertions fail if the harmonized wording is lost while canon-preservation assertions still pass.
  </behavior>
  <action>Audit `lore-extractor.ts` so its prompt text follows the Phase 31 shared contract rules where it references characters or authored world facts, while preserving the existing worldgen helper layering and lore-specific task boundary. Do not turn lore extraction into a generic character prompt and do not broaden the file into a new helper registry. Extend `lore-extractor.test.ts` with negative assertions for stale assumptions alongside the existing canon/divergence protections.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldgen/__tests__/lore-extractor.test.ts</automated>
  </verify>
  <done>Lore extraction prompt text follows the shared contract rules without weakening known-IP canon preservation, and tests explicitly protect both behaviors.</done>
</task>

</tasks>

<verification>
Run the worldgen helper test slice to confirm Phase 25 canon protections survive while stale prompt assumptions are removed.
</verification>

<success_criteria>
Worldgen seed and lore prompt families still use `prompt-utils.ts` as their authority, but no longer carry stale character-contract assumptions, and the regressions prove both properties together.
</success_criteria>

<output>
After completion, create `.planning/phases/31-prompt-system-harmonization-and-audit/31-05-SUMMARY.md`
</output>
