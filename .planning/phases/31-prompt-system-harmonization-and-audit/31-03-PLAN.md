---
phase: 31-prompt-system-harmonization-and-audit
plan: 03
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - backend/src/character/generator.ts
  - backend/src/character/npc-generator.ts
  - backend/src/character/archetype-researcher.ts
  - backend/src/character/__tests__/generator.test.ts
  - backend/src/character/__tests__/npc-generator.test.ts
  - backend/src/character/__tests__/archetype-researcher.test.ts
autonomous: true
requirements:
  - P31-03
  - P31-06
must_haves:
  truths:
    - "Player and NPC prompt families read as role-specific views over one shared draft contract."
    - "Archetype research guidance feeds the same canonical draft vocabulary instead of drifting into a separate upstream prompt worldview."
    - "Explicit user facts stay authoritative while derived tags remain a compatibility projection."
    - "Character regressions fail when legacy split-schema or tag-only instructions return."
  artifacts:
    - path: "backend/src/character/generator.ts"
      provides: "Player prompt family aligned to the shared character contract"
    - path: "backend/src/character/npc-generator.ts"
      provides: "NPC prompt family aligned to the shared character contract"
    - path: "backend/src/character/archetype-researcher.ts"
      provides: "Archetype research prompt aligned to the same canonical field vocabulary used by downstream player/NPC drafting"
    - path: "backend/src/character/__tests__/generator.test.ts"
      provides: "Positive and negative assertions for player prompt wording"
    - path: "backend/src/character/__tests__/npc-generator.test.ts"
      provides: "Positive and negative assertions for NPC prompt wording"
    - path: "backend/src/character/__tests__/archetype-researcher.test.ts"
      provides: "Regression coverage for archetype research prompt categories and stale wording removal"
  key_links:
    - from: "backend/src/character/generator.ts"
      to: "backend/src/character/prompt-contract.ts"
      via: "shared canonical character fragments"
      pattern: "prompt-contract"
    - from: "backend/src/character/npc-generator.ts"
      to: "backend/src/character/prompt-contract.ts"
      via: "shared canonical character fragments"
      pattern: "prompt-contract"
    - from: "backend/src/character/archetype-researcher.ts"
      to: "backend/src/character/generator.ts"
      via: "researched archetype summaries feeding the downstream shared draft prompt family"
      pattern: "researchArchetype|generateCharacterFromArchetype|generateNpcFromArchetype"
---

<objective>
Harmonize player, NPC, and archetype drafting prompts around one canonical draft vocabulary without losing their role-specific emphasis.

Purpose: Phase 29/30 made the data model canonical; this plan makes the character prompt families and their archetype-research feeder describe the same model instead of teaching different mental models to the LLM.
Output: shared-contract character prompts plus regression coverage for explicit-user-fact preservation, archetype prompt alignment, and stale split-schema removal.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-prompt-system-harmonization-and-audit/31-RESEARCH.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-prompt-contract-rules.md
@backend/src/character/generator.ts
@backend/src/character/npc-generator.ts
@backend/src/character/archetype-researcher.ts
@backend/src/character/__tests__/generator.test.ts
@backend/src/character/__tests__/npc-generator.test.ts
@backend/src/character/__tests__/archetype-researcher.test.ts
@backend/src/character/record-adapters.ts

<interfaces>
From `backend/src/character/generator.ts`:
```typescript
export type ParsedCharacter = CharacterDraft;

export async function parseCharacterDescription(...): Promise<ParsedCharacter>;
export async function mapV2CardToCharacter(...): Promise<ParsedCharacter>;
export async function generateCharacter(...): Promise<ParsedCharacter>;
export async function generateCharacterFromArchetype(...): Promise<ParsedCharacter>;
```

From `backend/src/character/npc-generator.ts`:
```typescript
export type GeneratedNpc = CharacterDraft;

export async function parseNpcDescription(...): Promise<GeneratedNpc>;
export async function mapV2CardToNpc(...): Promise<GeneratedNpc>;
export async function generateNpcFromArchetype(...): Promise<GeneratedNpc>;
```

From `backend/src/character/archetype-researcher.ts`:
```typescript
export async function researchArchetype(opts: {
  archetype: string;
  role: ResolvedRole;
  research: ResearchConfig;
}): Promise<string | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite player drafting prompts onto the shared character contract</name>
  <files>backend/src/character/generator.ts, backend/src/character/__tests__/generator.test.ts</files>
  <behavior>
    - Test 1: explicit user-provided fields such as name, age, gender, species, and appearance remain verbatim-authoritative.
    - Test 2: parse/generate/import/archetype prompt text uses canonical field-group vocabulary and no longer says "Use the tag-only system."
    - Test 3: derived tags are described as a compact runtime/output view rather than the primary model.
  </behavior>
  <action>Refactor the player prompt builders in `generator.ts` to compose from `backend/src/character/prompt-contract.ts`. Preserve the existing strong behavior around verbatim user facts and import-mode handling, but replace legacy tag-only language with canonical-field-first wording covering profile, motivations, capabilities, loadout, and `startConditions`. Keep the external structured output schema stable enough for `fromLegacyPlayerCharacter()` to remain the bridge; Phase 31 is harmonizing prompt semantics, not replacing working adapter seams. Extend `generator.test.ts` with negative assertions for the removed tag-only text and positive assertions for the new shared vocabulary.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/character/__tests__/generator.test.ts</automated>
  </verify>
  <done>Player prompt families compose from the shared contract, preserve explicit user facts, and tests fail if the old tag-only worldview comes back.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite NPC and archetype drafting prompts onto the same shared contract</name>
  <files>backend/src/character/npc-generator.ts, backend/src/character/archetype-researcher.ts, backend/src/character/__tests__/npc-generator.test.ts, backend/src/character/__tests__/archetype-researcher.test.ts</files>
  <behavior>
    - Test 1: NPC parse/import/archetype prompts now reference the same canonical field groups as player prompts, with role-specific emphasis rather than a separate schema worldview.
    - Test 2: negative assertions fail if legacy `persona/tags/goals/location/faction`-only wording remains authoritative.
    - Test 3: archetype research prompts ask for motivations, capabilities, backstory, and signature traits in language that feeds the canonical draft contract rather than a disconnected summary worldview.
    - Test 4: import-mode guidance still keeps outsider/native semantics in canonical biography/state instead of noisy tags.
  </behavior>
  <action>Refactor `npc-generator.ts` to consume the same `prompt-contract.ts` fragments as the player family, layering only NPC-specific emphasis such as faction/social context and immediate world role. In `archetype-researcher.ts`, rewrite the research prompt so the upstream summary categories align to the same canonical ontology vocabulary expected by `generateCharacterFromArchetype()` and `generateNpcFromArchetype()` instead of describing a disconnected freeform archetype blob. Keep the existing MCP/fallback flow and return type intact; this is prompt-family harmonization, not search-provider redesign. Update `npc-generator.test.ts` with explicit negative assertions for the legacy split-schema wording and extend `archetype-researcher.test.ts` so it checks for canonical-field-aligned research categories rather than only fallback behavior.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/character/__tests__/npc-generator.test.ts src/character/__tests__/archetype-researcher.test.ts</automated>
  </verify>
  <done>NPC drafting prompts and archetype research guidance are role-specific views over the same canonical contract as player prompts, and regression tests catch any return to the legacy split-schema worldview or detached archetype wording.</done>
</task>

</tasks>

<verification>
Run the character-family test slice to confirm player, NPC, and archetype prompt families now teach one ontology while preserving explicit-user-fact guarantees.
</verification>

<success_criteria>
Character generation, parsing, import, and archetype prompt families all describe one canonical draft model, with tests proving both presence of the new language and removal of the stale split.
</success_criteria>

<output>
After completion, create `.planning/phases/31-prompt-system-harmonization-and-audit/31-03-SUMMARY.md`
</output>
