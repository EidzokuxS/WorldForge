---
phase: 31-prompt-system-harmonization-and-audit
plan: 04
type: execute
wave: 3
depends_on:
  - 31-01
  - 31-03
files_modified:
  - backend/src/worldgen/starting-location.ts
  - backend/src/worldgen/scaffold-steps/npcs-step.ts
  - backend/src/worldgen/__tests__/starting-location.test.ts
  - backend/src/worldgen/__tests__/npcs-step.test.ts
autonomous: true
requirements:
  - P31-04
  - P31-05
  - P31-06
must_haves:
  truths:
    - "Start-state prompts reason about structured `startConditions`, not a location-plus-flavor shortcut."
    - "Character-adjacent worldgen NPC prompts emit canonical-field-aware guidance instead of legacy split-schema instructions."
    - "Tests prove stale location-only or legacy NPC-card wording was removed."
  artifacts:
    - path: "backend/src/worldgen/starting-location.ts"
      provides: "Structured start-condition prompt contract aligned to Phase 30 seams"
    - path: "backend/src/worldgen/scaffold-steps/npcs-step.ts"
      provides: "NPC worldgen detail prompts aligned to the canonical character contract"
    - path: "backend/src/worldgen/__tests__/starting-location.test.ts"
      provides: "Regression coverage for structured start-condition wording"
    - path: "backend/src/worldgen/__tests__/npcs-step.test.ts"
      provides: "Regression coverage for canonical worldgen NPC wording"
  key_links:
    - from: "backend/src/worldgen/starting-location.ts"
      to: "backend/src/character/prompt-contract.ts"
      via: "shared start-condition vocabulary"
      pattern: "startConditions|arrivalMode|immediateSituation"
    - from: "backend/src/worldgen/scaffold-steps/npcs-step.ts"
      to: "backend/src/character/prompt-contract.ts"
      via: "canonical character field-group wording"
      pattern: "prompt-contract"
---

<objective>
Bring start-state resolution and worldgen NPC detailing onto the canonical prompt contract established by Phases 29 and 30.

Purpose: these families still sit closest to the old location-only and legacy NPC-card assumptions, so they need explicit harmonization before broader worldgen/support audits.
Output: structured start-condition prompt text and canonical-field-aware worldgen NPC prompts with negative-assertion tests.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-prompt-system-harmonization-and-audit/31-RESEARCH.md
@.planning/phases/28-research-design-synthesis-for-character-systems-prompts-ui-and-external-references/28-phase-31-handoff.md
@.planning/phases/30-start-conditions-canonical-loadouts-and-persona-templates/30-02-SUMMARY.md
@backend/src/worldgen/starting-location.ts
@backend/src/worldgen/scaffold-steps/npcs-step.ts
@backend/src/worldgen/__tests__/starting-location.test.ts
@backend/src/worldgen/__tests__/npcs-step.test.ts

<interfaces>
From `shared/src/types.ts`:
```typescript
export interface ResolvedStartConditions {
  locationId: string;
  locationName: string;
  startConditions: CharacterStartConditions;
  narrative: string | null;
}
```

From `backend/src/worldgen/scaffold-steps/npcs-step.ts`:
```typescript
interface PlannedNpc {
  name: string;
  role: string;
  locationName: string;
  factionName: string | null;
  tier: "key" | "supporting";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite start-state resolution around structured startConditions</name>
  <files>backend/src/worldgen/starting-location.ts, backend/src/worldgen/__tests__/starting-location.test.ts</files>
  <behavior>
    - Test 1: the prompt explicitly asks for structured arrival mode, immediate situation, entry pressure, companions, visibility, and resolved narrative as one start-state contract.
    - Test 2: compatibility aliases (`locationId`, `locationName`, `narrative`) remain additive, but negative assertions fail if the prompt collapses back to location-plus-flavor wording.
    - Test 3: the shared character/start helper is the source of wording rather than new inline one-off text.
  </behavior>
  <action>Refactor `starting-location.ts` to consume the shared character/start prompt helper from Plan 31-01. Keep the Phase 30 structured result shape intact, but rewrite the prompt so `startConditions` is clearly the authoritative object and `narrative` is the compatibility alias. Do not add UI or route scope here; stay inside the prompt contract and the existing resolver seam. Extend `starting-location.test.ts` with negative assertions for location-only wording and positive assertions for the structured start-condition fields.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldgen/__tests__/starting-location.test.ts</automated>
  </verify>
  <done>The start-state prompt is explicitly structured around `startConditions`, compatibility aliases remain intact, and tests fail if the resolver slips back into location-only semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite worldgen NPC detail prompts to teach the canonical character contract</name>
  <files>backend/src/worldgen/scaffold-steps/npcs-step.ts, backend/src/worldgen/__tests__/npcs-step.test.ts</files>
  <behavior>
    - Test 1: detail prompts ask for canonical character facets or explicitly canonical-field-compatible wording, not a legacy NPC-card worldview.
    - Test 2: known-IP canon/divergence rules remain intact and tests still guard against reintroducing replaced protagonists.
    - Test 3: negative assertions fail if stale legacy NPC-card or tag-only instructions return.
  </behavior>
  <action>Update `npcs-step.ts` so the detail-writing stage teaches the model the same canonical character semantics as the shared character prompt contract while preserving the existing scaffold output surface and Phase 25 canon/divergence helper reuse. Keep plan/detail batching, tier behavior, and `fromLegacyScaffoldNpc()` compatibility intact; the goal is contract harmonization, not a worldgen shape rewrite. Extend `npcs-step.test.ts` with assertions for the new canonical-field-aware language and for the continued absence of displaced canonical protagonists in diverged settings.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldgen/__tests__/npcs-step.test.ts</automated>
  </verify>
  <done>Worldgen NPC detail prompts speak the canonical character contract, preserve Phase 25 known-IP behavior, and tests catch both stale wording and canon-divergence regressions.</done>
</task>

</tasks>

<verification>
Run the start-state and worldgen-NPC tests together to confirm the character-adjacent worldgen seam is harmonized without regressing Phase 25/30 behavior.
</verification>

<success_criteria>
Start-state resolution and worldgen NPC detailing both consume the shared canonical contract, and their tests prove removal of the old location-only and legacy NPC-card prompt assumptions.
</success_criteria>

<output>
After completion, create `.planning/phases/31-prompt-system-harmonization-and-audit/31-04-SUMMARY.md`
</output>
