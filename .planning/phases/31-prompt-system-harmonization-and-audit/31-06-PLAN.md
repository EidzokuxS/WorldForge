---
phase: 31-prompt-system-harmonization-and-audit
plan: 06
type: execute
wave: 3
depends_on:
  - 31-01
files_modified:
  - backend/src/engine/npc-agent.ts
  - backend/src/engine/reflection-agent.ts
  - backend/src/engine/__tests__/npc-agent.test.ts
  - backend/src/engine/__tests__/reflection-agent.test.ts
autonomous: true
requirements:
  - P31-05
  - P31-06
must_haves:
  truths:
    - "NPC autonomous and reflection prompts describe canonical character state, beliefs, goals, and derived tags coherently."
    - "Support prompts keep their task boundaries instead of turning into vague personality prose."
    - "Tests fail if stale legacy blobs become authoritative again."
  artifacts:
    - path: "backend/src/engine/npc-agent.ts"
      provides: "NPC autonomous prompt aligned to canonical record fields"
    - path: "backend/src/engine/reflection-agent.ts"
      provides: "Reflection prompt aligned to canonical record fields and evidence-driven updates"
    - path: "backend/src/engine/__tests__/npc-agent.test.ts"
      provides: "Regression coverage for canonical NPC-agent prompt wording"
    - path: "backend/src/engine/__tests__/reflection-agent.test.ts"
      provides: "Regression coverage for canonical reflection prompt wording"
  key_links:
    - from: "backend/src/engine/npc-agent.ts"
      to: "backend/src/character/prompt-contract.ts"
      via: "shared character vocabulary"
      pattern: "prompt-contract|deriveRuntimeCharacterTags|hydrateStoredNpcRecord"
    - from: "backend/src/engine/reflection-agent.ts"
      to: "backend/src/character/prompt-contract.ts"
      via: "shared character vocabulary"
      pattern: "prompt-contract|hydrateStoredNpcRecord"
---

<objective>
Harmonize the NPC-support prompt families that operate on live canonical character state after the turn loop.

Purpose: NPC agency and reflection already read canonical records in code; this plan makes their prompts describe that same model consistently and protects the result with focused regressions.
Output: updated NPC-agent and reflection prompts plus tests that prove legacy blobs no longer outrank canonical records.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-prompt-system-harmonization-and-audit/31-RESEARCH.md
@backend/src/engine/npc-agent.ts
@backend/src/engine/reflection-agent.ts
@backend/src/engine/__tests__/npc-agent.test.ts
@backend/src/engine/__tests__/reflection-agent.test.ts
@backend/src/character/record-adapters.ts

<interfaces>
From `backend/src/engine/npc-agent.ts`:
```typescript
export interface NpcTickResult {
  npcId: string;
  npcName: string;
  toolCalls: Array<{ tool: string; args: unknown; result: unknown }>;
  error?: string;
}
```

From `backend/src/engine/reflection-agent.ts`:
```typescript
export const REFLECTION_THRESHOLD = 10;

export interface ReflectionResult {
  npcId: string;
  npcName: string;
  toolCalls: Array<{ tool: string; args: unknown; result: unknown }>;
  error?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite NPC autonomous prompts to read canonical state first</name>
  <files>backend/src/engine/npc-agent.ts, backend/src/engine/__tests__/npc-agent.test.ts</files>
  <behavior>
    - Test 1: the NPC autonomous prompt still includes goals, beliefs, nearby entities, and memories required for coherent action selection.
    - Test 2: canonical record fields stay authoritative while derived tags are presented as compact compatibility evidence.
    - Test 3: negative assertions fail if legacy persona/goals/beliefs blobs become the authoritative prompt language again.
  </behavior>
  <action>Refactor the system-prompt assembly in `npc-agent.ts` to consume the shared character prompt vocabulary from Plan 31-01 while preserving its task boundary: one scene-local autonomous action. Keep nearby-entity, relationship, and episodic-memory context intact, but stop relying on legacy blob phrasing as the conceptual contract. Extend `npc-agent.test.ts` so it asserts both the canonical-field-first wording and the absence of legacy authoritative language.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/npc-agent.test.ts</automated>
  </verify>
  <done>NPC-agent prompts teach the canonical record model first, still choose one action coherently, and tests catch any reversion to legacy-authority blobs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite reflection prompts to stay evidence-driven and canonical</name>
  <files>backend/src/engine/reflection-agent.ts, backend/src/engine/__tests__/reflection-agent.test.ts</files>
  <behavior>
    - Test 1: reflection prompts still remain evidence-driven and respect the existing threshold/progression semantics.
    - Test 2: canonical beliefs/goals/state wording is explicit and no stale generic tag-only worldview is reintroduced.
    - Test 3: negative assertions fail if the reflection prompt drifts back to legacy blob authority.
  </behavior>
  <action>Refactor `reflection-agent.ts` to align its prompt wording with the shared canonical character contract while preserving the current evidence-driven reflection task, threshold behavior, and tool usage. Keep wealth/skill progression semantics intact and do not let the prompt collapse into vague introspection prose. Extend `reflection-agent.test.ts` with positive assertions for canonical-field wording and negative assertions for stale legacy-authority phrasing.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/reflection-agent.test.ts</automated>
  </verify>
  <done>Reflection prompts stay evidence-driven, canonical, and regression-protected against stale or blob-authoritative wording.</done>
</task>

</tasks>

<verification>
Run the NPC support test slice to confirm autonomous and reflection prompts now align to the shared character contract without regressing behavior.
</verification>

<success_criteria>
NPC support prompts use canonical field semantics consistently, and focused tests prove that legacy persona/goals/beliefs blobs no longer define prompt authority.
</success_criteria>

<output>
After completion, create `.planning/phases/31-prompt-system-harmonization-and-audit/31-06-SUMMARY.md`
</output>
