---
phase: 09-persistence
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - backend/src/routes/chat.ts
  - frontend/lib/api.ts
  - frontend/lib/api-types.ts
  - frontend/components/game/checkpoint-panel.tsx
  - frontend/app/game/page.tsx
autonomous: false
requirements: [SAVE-03, SAVE-04]

must_haves:
  truths:
    - "When player HP <= 2 at turn start, auto-checkpoint is created before processing"
    - "Auto-checkpoints are pruned to last 3"
    - "Player sees checkpoint list in game UI with name, timestamp, and auto flag"
    - "Player can create, load, and delete checkpoints from UI"
  artifacts:
    - path: "backend/src/routes/chat.ts"
      provides: "Auto-checkpoint hook before turn processing"
      contains: "createCheckpoint"
    - path: "frontend/components/game/checkpoint-panel.tsx"
      provides: "Checkpoint list UI with save/load/delete"
      exports: ["CheckpointPanel"]
    - path: "frontend/lib/api.ts"
      provides: "Checkpoint API client functions"
      contains: "checkpoint"
  key_links:
    - from: "backend/src/routes/chat.ts"
      to: "backend/src/campaign/checkpoints.ts"
      via: "createCheckpoint + pruneAutoCheckpoints before turn"
      pattern: "createCheckpoint.*auto.*true"
    - from: "frontend/components/game/checkpoint-panel.tsx"
      to: "frontend/lib/api.ts"
      via: "API calls for CRUD operations"
      pattern: "fetchCheckpoints|createCheckpoint|loadCheckpoint|deleteCheckpoint"
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/checkpoint-panel.tsx"
      via: "Renders CheckpointPanel in game layout"
      pattern: "CheckpointPanel"
---

<objective>
Auto-checkpoint before lethal encounters + Frontend checkpoint management UI.

Purpose: SAVE-03 (visible checkpoint list) and SAVE-04 (auto-checkpoint before lethal HP). Player can visually manage saves and the game auto-protects before dangerous situations.
Output: Auto-checkpoint in chat route + CheckpointPanel component + API client functions.
</objective>

<execution_context>
@C:/Users/robra/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-persistence/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From backend/src/campaign/checkpoints.ts (created in Plan 01):
```typescript
export type CheckpointMeta = { id: string; name: string; description: string; createdAt: number; auto: boolean };
export function createCheckpoint(campaignId: string, opts?: { name?: string; description?: string; auto?: boolean }): Promise<CheckpointMeta>;
export function listCheckpoints(campaignId: string): CheckpointMeta[];
export function loadCheckpoint(campaignId: string, checkpointId: string): Promise<CheckpointMeta>;
export function deleteCheckpoint(campaignId: string, checkpointId: string): void;
export function pruneAutoCheckpoints(campaignId: string, keepCount?: number): void;
```

From backend/src/db/schema.ts:
```typescript
// players table has: hp: integer("hp").notNull().default(5)
// check("players_hp_range_check", sql`${table.hp} >= 0 AND ${table.hp} <= 5`)
```

From frontend/lib/api.ts:
```typescript
const API_BASE = process.env.NEXT_PUBLIC_API_BASE ?? "http://localhost:3001";
// Uses get(), post(), del() helpers that call fetch() with API_BASE
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-checkpoint in chat route + frontend API client</name>
  <read_first>
    - backend/src/routes/chat.ts (find the /action handler, line ~281, where to add auto-checkpoint)
    - backend/src/db/schema.ts (players table structure)
    - frontend/lib/api.ts (existing pattern for API helpers)
    - frontend/lib/api-types.ts (existing type exports)
  </read_first>
  <files>
    backend/src/routes/chat.ts,
    frontend/lib/api.ts,
    frontend/lib/api-types.ts
  </files>
  <action>
1. In `backend/src/routes/chat.ts`, inside the `POST /action` handler, BEFORE `captureSnapshot()` (around line 310):
   - Import `createCheckpoint`, `pruneAutoCheckpoints` from campaign/index
   - Import `players` from db/schema, `eq` from drizzle-orm, `getDb` from db/index
   - Query player HP: `const player = getDb().select({ hp: players.hp }).from(players).where(eq(players.campaignId, activeCampaign.id)).get()`
   - If `player && player.hp <= 2`:
     - `await createCheckpoint(activeCampaign.id, { name: 'auto-danger', description: 'Auto-save: low HP', auto: true })`
     - `pruneAutoCheckpoints(activeCampaign.id, 3)`
   - Wrap in try/catch -- auto-checkpoint failure must NEVER block gameplay (log warning only)

2. In `frontend/lib/api-types.ts`, add:
   ```typescript
   export type CheckpointMeta = { id: string; name: string; description: string; createdAt: number; auto: boolean };
   ```

3. In `frontend/lib/api.ts`, add 4 functions:
   - `fetchCheckpoints(campaignId: string): Promise<CheckpointMeta[]>` -- GET `/api/campaigns/${id}/checkpoints`
   - `createCheckpoint(campaignId: string, name?: string, description?: string): Promise<CheckpointMeta>` -- POST `/api/campaigns/${id}/checkpoints` with body `{ name, description }`
   - `loadCheckpoint(campaignId: string, checkpointId: string): Promise<CheckpointMeta>` -- POST `/api/campaigns/${id}/checkpoints/${checkpointId}/load`
   - `deleteCheckpoint(campaignId: string, checkpointId: string): Promise<void>` -- DELETE `/api/campaigns/${id}/checkpoints/${checkpointId}`
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix backend run typecheck && npm --prefix frontend run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "auto-danger" backend/src/routes/chat.ts
    - grep -q "pruneAutoCheckpoints" backend/src/routes/chat.ts
    - grep -q "hp <= 2" backend/src/routes/chat.ts
    - grep -q "CheckpointMeta" frontend/lib/api-types.ts
    - grep -q "fetchCheckpoints" frontend/lib/api.ts
    - grep -q "loadCheckpoint" frontend/lib/api.ts
    - grep -q "deleteCheckpoint" frontend/lib/api.ts
  </acceptance_criteria>
  <done>
    Auto-checkpoint fires before turns when HP <= 2. Auto-checkpoints pruned to 3. Frontend has 4 checkpoint API client functions + CheckpointMeta type. Both typecheck and lint pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: CheckpointPanel component + game page integration</name>
  <read_first>
    - frontend/app/game/page.tsx (understand layout, where to add panel trigger)
    - frontend/components/game/character-panel.tsx (existing panel pattern for styling)
    - frontend/lib/api.ts (checkpoint API functions from Task 1)
  </read_first>
  <files>
    frontend/components/game/checkpoint-panel.tsx,
    frontend/app/game/page.tsx
  </files>
  <action>
1. Create `frontend/components/game/checkpoint-panel.tsx`:
   - Props: `{ campaignId: string; open: boolean; onClose: () => void }`
   - State: `checkpoints: CheckpointMeta[]`, `loading: boolean`, `saveName: string`
   - On mount (when open=true): call `fetchCheckpoints(campaignId)`, populate list
   - **Save section**: Input field for checkpoint name + "Save" button. On click: `createCheckpoint(campaignId, saveName)`, then refresh list. Toast or inline success message.
   - **Checkpoint list**: Each item shows:
     - Name (with "Auto" badge if `auto: true`, use a small tag/pill)
     - Timestamp formatted as locale date+time (`new Date(createdAt).toLocaleString()`)
     - Description (if any)
     - "Load" button (with confirm dialog: "Loading will replace current state. Continue?")
     - "Delete" button (with confirm dialog: "Delete this checkpoint?")
   - Load action: call `loadCheckpoint(campaignId, id)`, then `window.location.reload()` to force full state refresh
   - Delete action: call `deleteCheckpoint(campaignId, id)`, then remove from local list
   - Styling: Use Tailwind dark theme classes consistent with existing panels. Shadcn Button, Input, Dialog components.
   - Empty state: "No checkpoints yet. Save your first checkpoint above."

2. In `frontend/app/game/page.tsx`:
   - Import CheckpointPanel
   - Add state: `const [checkpointOpen, setCheckpointOpen] = useState(false)`
   - Add a "Saves" button (or icon button with Save/BookmarkIcon from lucide-react) in the game header/toolbar area
   - Render `<CheckpointPanel campaignId={campaign.id} open={checkpointOpen} onClose={() => setCheckpointOpen(false)} />`
   - Panel renders as a slide-over, dialog, or side panel -- use discretion for best UX fit with existing layout
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix frontend run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "CheckpointPanel" frontend/components/game/checkpoint-panel.tsx
    - grep -q "fetchCheckpoints" frontend/components/game/checkpoint-panel.tsx
    - grep -q "CheckpointPanel" frontend/app/game/page.tsx
    - grep -q "checkpointOpen" frontend/app/game/page.tsx
  </acceptance_criteria>
  <done>
    CheckpointPanel shows list of checkpoints with save/load/delete. Integrated into game page with toggle button. Auto-checkpoints display "Auto" badge. Load confirms before replacing state. Lint passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify checkpoint system end-to-end</name>
  <files>none</files>
  <action>
    Human verifies the complete checkpoint system works end-to-end. See how-to-verify steps below.
  </action>
  <what-built>Complete checkpoint save/load system with auto-checkpoint and UI management.</what-built>
  <how-to-verify>
    1. Start backend (`cd backend && npm run dev`) and frontend (`cd frontend && npm run dev`)
    2. Load an existing campaign or create a new one and generate world + character
    3. Play a few turns to establish some game state
    4. Open the Saves panel from the game page
    5. Create a manual checkpoint with a name -- verify it appears in the list
    6. Play more turns to change state
    7. Load the checkpoint -- verify the game reverts to the saved state (check chat history, world data)
    8. Delete a checkpoint -- verify it disappears from the list
    9. (Optional) If you can engineer HP <= 2 via gameplay, verify an auto-checkpoint appears
  </how-to-verify>
  <verify>Human confirms checkpoint create/load/delete works in UI</verify>
  <done>User has verified checkpoint system works end-to-end and typed "approved"</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Backend typecheck passes
- Frontend lint passes
- Auto-checkpoint fires when HP <= 2 (non-blocking)
- UI shows checkpoint list with CRUD operations
- Loading a checkpoint fully restores DB + vectors + chat history
</verification>

<success_criteria>
- Checkpoint list visible in game UI with name, timestamp, auto badge
- Manual save creates checkpoint, load restores state, delete removes it
- Auto-checkpoint created before turns when HP <= 2
- Auto-checkpoints pruned to 3 max
- Checkpoint load forces full page refresh for clean state
</success_criteria>

<output>
After completion, create `.planning/phases/09-persistence/09-02-SUMMARY.md`
</output>
