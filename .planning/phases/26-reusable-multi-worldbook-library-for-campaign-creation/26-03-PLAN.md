---
phase: 26-reusable-multi-worldbook-library-for-campaign-creation
plan: 03
type: execute
wave: 3
depends_on:
  - "26-02"
files_modified:
  - frontend/lib/api-types.ts
  - frontend/lib/api.ts
  - frontend/components/title/use-new-campaign-wizard.ts
  - frontend/components/title/new-campaign-dialog.tsx
  - frontend/components/title/__tests__/use-new-campaign-wizard.test.tsx
  - frontend/components/title/__tests__/new-campaign-dialog.test.tsx
autonomous: true
requirements:
  - P26-04
  - P26-05
must_haves:
  truths:
    - "Step 1 lets the user select existing reusable worldbooks and upload additional new ones in the same session."
    - "The wizard no longer merges worldbook context in the browser; it sends selected reusable items to backend routes."
    - "A selected worldbook set makes premise optional in the same way the old single-worldbook flow did."
    - "Direct create and DNA-based create both preserve the selected source set without regressing no-worldbook campaigns."
  artifacts:
    - path: "frontend/components/title/use-new-campaign-wizard.ts"
      provides: "Multi-source wizard state and backend-only handoff"
    - path: "frontend/components/title/new-campaign-dialog.tsx"
      provides: "Step 1 library selection and upload UI"
    - path: "frontend/lib/api.ts"
      provides: "Frontend API helpers for listing/importing/selecting reusable worldbooks"
  key_links:
    - from: "frontend/components/title/use-new-campaign-wizard.ts"
      to: "frontend/lib/api.ts"
      via: "list/import/suggest/create calls using selected reusable items"
      pattern: "listWorldbookLibrary|importWorldbookLibrary|suggestSeeds"
    - from: "frontend/components/title/new-campaign-dialog.tsx"
      to: "frontend/components/title/use-new-campaign-wizard.ts"
      via: "Step 1 selection and upload controls"
      pattern: "selectedWorldbooks|handleWorldbookUpload"
    - from: "frontend/components/title/use-new-campaign-wizard.ts"
      to: "backend route compatibility from Plan 26-02"
      via: "worldbookSelection and selectedWorldbooks payloads"
      pattern: "worldbookSelection|selectedWorldbooks"
---

<objective>
Refactor the new-campaign wizard from a single transient upload into a reusable multi-source selection flow.

Purpose: The backend work is invisible unless Step 1 exposes the reusable library cleanly and stops doing client-side composition. This plan migrates the wizard and dialog to the new source-set model while keeping the current “Create World” mental model intact.
Output: Frontend API helpers, collection-based wizard state, Step 1 library selection/upload UI, and focused frontend tests covering the new flow.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-reusable-multi-worldbook-library-for-campaign-creation/26-CONTEXT.md
@.planning/phases/26-reusable-multi-worldbook-library-for-campaign-creation/26-RESEARCH.md
@frontend/lib/api.ts
@frontend/lib/api-types.ts
@frontend/components/title/use-new-campaign-wizard.ts
@frontend/components/title/new-campaign-dialog.tsx
@frontend/components/title/__tests__/new-campaign-dialog.test.tsx
@backend/src/routes/worldgen.ts
@backend/src/routes/campaigns.ts

<decisions>
- D-01: Step 1 must support both library selection and new uploads in the same session while preserving the existing create-world mental model.
- D-02: Processed worldbooks are reusable library items outside campaigns, and the selected set must be recorded on the campaign.
- D-03: Merge/composition belongs to the backend, not the client.
- D-04: Existing no-worldbook and single-worldbook behavior must stay compatible; advanced library management UI is deferred.
</decisions>

<interfaces>
Expected backend-facing API after Plan 26-02:
```ts
export interface WorldbookLibraryItem {
  id: string;
  displayName: string;
  originalFileName: string;
  normalizedSourceHash: string;
  entryCount: number;
  classificationVersion: number;
  createdAt: number;
  updatedAt: number;
}

export function listWorldbookLibrary(): Promise<{ items: WorldbookLibraryItem[] }>;
export function importWorldbookLibrary(
  displayName: string,
  worldbook: object,
): Promise<{ item: WorldbookLibraryItem; existed: boolean }>;
```

Current client-side worldbook state in frontend/components/title/use-new-campaign-wizard.ts:
```ts
const [worldbookFile, setWorldbookFile] = useState<File | null>(null);
const [worldbookEntries, setWorldbookEntries] = useState<ClassifiedWorldBookEntry[] | null>(null);
const [worldbookStatus, setWorldbookStatus] = useState<"idle" | "parsing" | "classifying" | "done" | "error">("idle");
```

Current client-side merge helper in frontend/lib/api.ts:
```ts
export function worldbookToIpContext(
  entries: ClassifiedWorldBookEntry[],
  name: string,
): IpResearchContext;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace single-worldbook wizard state with reusable source collection state</name>
  <files>frontend/lib/api-types.ts, frontend/lib/api.ts, frontend/components/title/use-new-campaign-wizard.ts, frontend/components/title/__tests__/use-new-campaign-wizard.test.tsx</files>
  <read_first>
    - frontend/lib/api.ts
    - frontend/lib/api-types.ts
    - frontend/components/title/use-new-campaign-wizard.ts
    - frontend/components/title/__tests__/new-campaign-dialog.test.tsx
    - backend/src/routes/worldgen.ts
    - backend/src/routes/campaigns.ts
  </read_first>
  <behavior>
    - Test 1: opening the wizard loads reusable library items and preserves them in hook state.
    - Test 2: uploading a new JSON worldbook calls the backend library import endpoint, adds/updates the returned item in state, and auto-selects it for the current session.
    - Test 3: `handleNextToDna()` sends `selectedWorldbooks` instead of client-side `worldbookEntries`.
    - Test 4: direct create sends `worldbookSelection[]` to campaign creation and does not call `worldbookToIpContext()`.
  </behavior>
  <action>Refactor `use-new-campaign-wizard.ts` from the old single-file state (`worldbookFile`, `worldbookEntries`, `worldbookStatus`) to a collection model per D-01 through D-04. Add frontend API types/helpers for listing reusable library items and importing a new worldbook into the library. On dialog open, fetch the library list once and store it in hook state. Replace upload handling so a dropped/selected JSON file is parsed locally only to validate JSON, then sent to the new backend import endpoint; when the backend returns `{ item, existed }`, merge that item into the local library list and mark it selected. Track selection as an ordered set of reusable items, not raw classified entries. Update `suggestSeeds()` calls to send `selectedWorldbooks`, update campaign creation to send `worldbookSelection[]`, and remove the hook’s client-side fallback that derived `ipContext` via `worldbookToIpContext()` before generation. Keep `worldbookToIpContext()` only as a deprecated compatibility helper if other callers still import it, but the wizard must stop using it.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run components/title/__tests__/use-new-campaign-wizard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The hook manages a reusable library list plus a selected source collection instead of one transient upload slot.
    - Uploading a JSON file persists or reuses a library record through the backend and auto-selects the returned item.
    - `suggestSeeds()` and campaign creation use `selectedWorldbooks` / `worldbookSelection[]`, not client-side merged `worldbookEntries`.
    - The wizard no longer depends on `worldbookToIpContext()` for the main Phase 26 flow.
  </acceptance_criteria>
  <done>The wizard state is collection-based, backend-driven, and no longer performs worldbook merge/composition in the browser.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redesign Step 1 UI for library selection plus upload-in-session</name>
  <files>frontend/components/title/new-campaign-dialog.tsx, frontend/components/title/__tests__/new-campaign-dialog.test.tsx</files>
  <read_first>
    - frontend/components/title/new-campaign-dialog.tsx
    - frontend/components/title/__tests__/new-campaign-dialog.test.tsx
    - frontend/components/title/use-new-campaign-wizard.ts
  </read_first>
  <behavior>
    - Test 1: Step 1 renders a reusable worldbook section showing existing library items and selection state.
    - Test 2: upload controls remain available in the same Step 1 surface and reflect import progress/errors.
    - Test 3: when at least one worldbook is selected, the premise placeholder and create-state gating treat premise as optional.
  </behavior>
  <action>Update `new-campaign-dialog.tsx` so Step 1 exposes one coherent knowledge-source section per D-01 and D-04: a lightweight library list with selection controls, the existing upload affordance for adding new JSON files in-session, and status/error copy that reflects backend persistence rather than transient classification only. Preserve the current Create World / Next -> World DNA mental model and button layout, but change all copy and gating logic from singular “WorldBook uploaded” semantics to “selected worldbooks” semantics. Keep the UI intentionally scoped: selection, upload, and basic status only. Do not add rename/delete/conflict-resolution screens. Ensure the placeholder logic, `canCreate`, and empty-premise messaging now key off whether any worldbooks are selected, not whether one transient upload finished classifying.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run components/title/__tests__/new-campaign-dialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Step 1 shows reusable library items and selection state in the same surface as the upload affordance.
    - Upload progress/error UI matches the new backend import flow.
    - Premise becomes optional when one or more worldbooks are selected, not only when a single upload finished.
    - No advanced management or conflict-resolution UI is introduced in this phase.
  </acceptance_criteria>
  <done>Step 1 now exposes reusable multi-worldbook selection cleanly while keeping the existing campaign-creation flow familiar.</done>
</task>

</tasks>

<verification>
Run the focused frontend hook and dialog tests. Confirm the wizard can list reusable worldbooks, import/select new ones in-session, and create or DNA-suggest without any client-side merge path.
</verification>

<acceptance_criteria>
- Step 1 supports selecting reusable library items and uploading new worldbooks in one session.
- The wizard sends selected reusable items to backend routes instead of composing ipContext locally.
- Empty-premise compatibility now keys off selected worldbooks, preserving the old optional-premise behavior.
- No-worldbook campaigns still follow the original create/generate flow unchanged.
</acceptance_criteria>

<success_criteria>
The campaign-creation UI now exposes reusable multi-worldbook selection end-to-end: users can reuse saved sources, add new ones on the spot, and drive both Create World and World DNA through backend-owned composition without breaking existing workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/26-reusable-multi-worldbook-library-for-campaign-creation/26-03-SUMMARY.md`
</output>
