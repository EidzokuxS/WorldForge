---
phase: 26-reusable-multi-worldbook-library-for-campaign-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/types.ts
  - shared/src/index.ts
  - backend/src/campaign/manager.ts
  - backend/src/campaign/__tests__/manager.test.ts
  - backend/src/worldbook-library/paths.ts
  - backend/src/worldbook-library/manager.ts
  - backend/src/worldbook-library/index.ts
  - backend/src/worldbook-library/__tests__/manager.test.ts
  - backend/src/routes/schemas.ts
  - backend/src/routes/worldgen.ts
  - backend/src/routes/__tests__/worldgen.test.ts
autonomous: true
requirements:
  - P26-01
must_haves:
  truths:
    - "Processed worldbooks persist outside campaigns and can be listed across sessions without reclassification."
    - "Re-uploading the same parsed worldbook resolves to the same stored library record instead of duplicating content."
    - "Campaign config can preserve the selected worldbook source set beside cached ipContext and premiseDivergence."
    - "The reusable library path never appears as a broken campaign entry in the campaign list."
  artifacts:
    - path: "backend/src/worldbook-library/manager.ts"
      provides: "File-backed reusable worldbook storage with stable identity and index management"
    - path: "shared/src/types.ts"
      provides: "Shared library item and campaign selection contracts"
    - path: "backend/src/campaign/manager.ts"
      provides: "Campaign config persistence for worldbookSelection"
    - path: "backend/src/routes/worldgen.ts"
      provides: "List/import endpoints for the reusable worldbook library"
  key_links:
    - from: "backend/src/routes/worldgen.ts"
      to: "backend/src/worldbook-library/manager.ts"
      via: "library list/import handlers"
      pattern: "listWorldbookLibrary|importWorldbookToLibrary"
    - from: "backend/src/campaign/manager.ts"
      to: "shared/src/types.ts"
      via: "worldbookSelection config persistence"
      pattern: "worldbookSelection"
    - from: "backend/src/campaign/manager.ts"
      to: "backend/src/worldbook-library/paths.ts"
      via: "underscored directory filtering in listCampaigns"
      pattern: "_worldbook-library"
---

<objective>
Establish the reusable local-first worldbook library foundation and the campaign-side source snapshot contract.

Purpose: Phase 26 fails if reusable storage, stable identity, and campaign traceability are fuzzy. This plan creates the file-backed library, dedupe rules, and persistence seam that every later plan builds on.
Output: Shared types, campaign config support for `worldbookSelection`, a file-backed library manager under `campaigns/_worldbook-library`, and backend endpoints to list/import stored worldbooks.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-reusable-multi-worldbook-library-for-campaign-creation/26-CONTEXT.md
@.planning/phases/26-reusable-multi-worldbook-library-for-campaign-creation/26-RESEARCH.md
@.planning/phases/23-unified-research-world-generation-pipeline/23-02-SUMMARY.md
@.planning/phases/25-replace-premise-override-heuristics-with-structured-divergence-interpretation/25-03-SUMMARY.md
@shared/src/types.ts
@backend/src/campaign/manager.ts
@backend/src/routes/worldgen.ts
@backend/src/routes/schemas.ts
@backend/src/worldgen/worldbook-importer.ts

<decisions>
- D-01: Step 1 of campaign creation becomes a broader multi-source flow, not a separate single-upload path.
- D-02: Processed worldbooks live outside campaigns, persist classified entries plus reusable metadata, and selected sources must be recorded on the campaign.
- D-03: Merge/composition lives on the backend and must preserve provenance with deterministic duplicate handling.
- D-04: Existing single-worldbook and no-worldbook flows remain compatible; storage stays local-first and file-backed; advanced management UI is deferred.
</decisions>

<interfaces>
From shared/src/types.ts:
```ts
export interface IpResearchContext {
  franchise: string;
  keyFacts: string[];
  tonalNotes: string[];
  canonicalNames?: {
    locations?: string[];
    factions?: string[];
    characters?: string[];
  };
  excludedCharacters?: string[];
  source: "mcp" | "llm";
}
```

From backend/src/worldgen/worldbook-importer.ts:
```ts
export interface ClassifiedEntry {
  name: string;
  type: WorldBookEntryType;
  summary: string;
}

export function parseWorldBook(json: unknown): WorldBookEntry[];
export async function classifyEntries(
  entries: WorldBookEntry[],
  role: ResolvedRole,
): Promise<ClassifiedEntry[]>;
```

Current campaign config seam in backend/src/campaign/manager.ts:
```ts
type CampaignConfigFile = {
  name: string;
  premise: string;
  seeds?: WorldSeeds;
  ipContext?: IpResearchContext;
  premiseDivergence?: PremiseDivergence;
  generationComplete?: boolean;
  currentTick?: number;
  createdAt: number;
  updatedAt?: number;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define reusable worldbook contracts and campaign selection persistence</name>
  <files>shared/src/types.ts, shared/src/index.ts, backend/src/campaign/manager.ts, backend/src/campaign/__tests__/manager.test.ts</files>
  <read_first>
    - shared/src/types.ts
    - shared/src/index.ts
    - backend/src/campaign/manager.ts
    - backend/src/campaign/__tests__/manager.test.ts
    - .planning/phases/25-replace-premise-override-heuristics-with-structured-divergence-interpretation/25-03-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: campaign config round-trips `worldbookSelection[]` beside `ipContext` and `premiseDivergence`.
    - Test 2: `listCampaigns()` ignores the reserved `_worldbook-library` directory instead of trying to parse it as a campaign.
    - Test 3: `createCampaign()` accepts an initial worldbook selection snapshot without changing empty-premise compatibility.
  </behavior>
  <action>Extend the shared contract surface for reusable worldbooks per D-02 and D-04. Add explicit types for the reusable library summary item and the campaign-level `worldbookSelection[]` snapshot in `shared/src/types.ts`, then re-export them from `shared/src/index.ts`. Update `backend/src/campaign/manager.ts` so `CampaignConfigFile`, `readCampaignConfig()`, `createCampaign()`, and any save/load helpers can persist `worldbookSelection[]` beside the existing `ipContext` and `premiseDivergence` cache fields. Keep the selection snapshot structured, not free-form text: include stable library `id`, display name, normalized source hash, entry count, and timestamps needed for later debugging. Also harden `listCampaigns()` to skip underscored directories so `campaigns/_worldbook-library/` never pollutes the normal campaign list. Do not add a database table or campaign-owned storage here; keep everything file-backed and backward-compatible.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/campaign/__tests__/manager.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `shared/src/types.ts` exports reusable worldbook summary/selection types instead of duplicating shapes in frontend or backend.
    - `backend/src/campaign/manager.ts` persists and reloads `worldbookSelection[]` without mutating `ipContext` or `premiseDivergence`.
    - `listCampaigns()` skips `_worldbook-library` and any other underscored storage directory.
    - Existing empty-premise campaign creation behavior still works when no worldbooks are selected.
  </acceptance_criteria>
  <done>Shared contracts exist, campaigns can persist selected source snapshots, and the reusable library directory is invisible to campaign listing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the file-backed worldbook library manager and import/list routes</name>
  <files>backend/src/worldbook-library/paths.ts, backend/src/worldbook-library/manager.ts, backend/src/worldbook-library/index.ts, backend/src/worldbook-library/__tests__/manager.test.ts, backend/src/routes/schemas.ts, backend/src/routes/worldgen.ts, backend/src/routes/__tests__/worldgen.test.ts</files>
  <read_first>
    - backend/src/worldgen/worldbook-importer.ts
    - backend/src/routes/schemas.ts
    - backend/src/routes/worldgen.ts
    - backend/src/routes/__tests__/worldgen.test.ts
    - .gitignore
  </read_first>
  <behavior>
    - Test 1: importing the same parsed worldbook twice returns the same library item id and does not reclassify on the second import.
    - Test 2: listing the worldbook library returns metadata sorted predictably for frontend selection.
    - Test 3: the import route accepts raw worldbook JSON, persists classified entries under `campaigns/_worldbook-library`, and returns `{ item, existed }`.
  </behavior>
  <action>Create a new backend-only `worldbook-library` module under `backend/src/worldbook-library/` per D-02 and D-04. Store immutable record files in `campaigns/_worldbook-library/records/` plus a lightweight `index.json`; do not add SQLite, Drizzle schema, or a fake campaign container. Use a stable content hash derived from normalized parsed WorldBook entries so identical re-uploads bypass classification and reuse the existing record. Persist classified entries, file/display metadata, entry count, classification version, and timestamps in each record. Then extend `backend/src/routes/schemas.ts` and `backend/src/routes/worldgen.ts` with two focused endpoints: `GET /api/worldgen/worldbook-library` for listing reusable items and `POST /api/worldgen/worldbook-library/import` for parse -> dedupe -> classify-if-needed -> persist. Keep the existing `/parse-worldbook` route intact for compatibility, but do not let the new library flow depend on an active campaign. Route handlers must follow the standard `parseBody()` + outer `try/catch` pattern and return stable summaries from the new manager.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldbook-library/__tests__/manager.test.ts src/routes/__tests__/worldgen.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/worldbook-library/manager.ts` manages `campaigns/_worldbook-library/index.json` and per-record JSON files.
    - Stable ids come from normalized parsed content, not filenames.
    - `GET /api/worldgen/worldbook-library` lists reusable items without requiring an active campaign.
    - `POST /api/worldgen/worldbook-library/import` returns an existing item for duplicate uploads instead of reclassifying.
    - No new database tables, migrations, or central service are introduced.
  </acceptance_criteria>
  <done>The project has a reusable file-backed worldbook library with stable dedupe and backend endpoints to list/import records.</done>
</task>

</tasks>

<verification>
Run the targeted backend manager and route tests. Confirm the reusable library sits under `campaigns/_worldbook-library` and duplicate uploads reuse the same id without campaign-side storage hacks.
</verification>

<acceptance_criteria>
- Reusable worldbooks are stored outside campaign directories with content-hash identity.
- Campaign config can preserve `worldbookSelection[]` beside cached known-IP artifacts.
- Backend list/import routes expose the reusable library without requiring an active campaign.
- The campaign list remains clean and does not treat `_worldbook-library` as a campaign.
</acceptance_criteria>

<success_criteria>
The phase now has a durable reusable storage layer: worldbooks can be imported once, re-listed later, deduped by content, and referenced from campaign config without any database or client-owned persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/26-reusable-multi-worldbook-library-for-campaign-creation/26-01-SUMMARY.md`
</output>
