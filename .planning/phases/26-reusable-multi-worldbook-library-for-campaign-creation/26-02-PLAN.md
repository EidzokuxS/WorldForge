---
phase: 26-reusable-multi-worldbook-library-for-campaign-creation
plan: 02
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - backend/src/worldbook-library/composition.ts
  - backend/src/worldbook-library/index.ts
  - backend/src/worldgen/worldbook-importer.ts
  - backend/src/routes/schemas.ts
  - backend/src/routes/campaigns.ts
  - backend/src/routes/worldgen.ts
  - backend/src/worldgen/__tests__/worldbook-composition.test.ts
  - backend/src/routes/__tests__/campaigns.test.ts
  - backend/src/routes/__tests__/worldgen.test.ts
autonomous: true
requirements:
  - P26-02
  - P26-03
  - P26-05
must_haves:
  truths:
    - "Selected worldbooks are composed into one generation context on the backend, not in the browser."
    - "Composition is deterministic across source order and preserves enough provenance to reconstruct which records were selected."
    - "Campaign creation, suggest-seeds, and generate remain compatible with no-worldbook and old single-worldbook callers."
    - "When a campaign has selected library sources but no cached ipContext yet, generation can compose the context from the saved selection before falling back to franchise research."
  artifacts:
    - path: "backend/src/worldbook-library/composition.ts"
      provides: "Deterministic multi-worldbook composition with provenance metadata"
    - path: "backend/src/routes/worldgen.ts"
      provides: "Route-level backend composition for suggest-seeds and generate"
    - path: "backend/src/routes/campaigns.ts"
      provides: "Campaign creation persistence of the selected worldbook snapshot"
  key_links:
    - from: "backend/src/routes/worldgen.ts"
      to: "backend/src/worldbook-library/composition.ts"
      via: "selected source composition before suggest/generate"
      pattern: "composeWorldbookSelection|composeSelectedWorldbooks"
    - from: "backend/src/routes/campaigns.ts"
      to: "backend/src/campaign/manager.ts"
      via: "createCampaign initial worldbookSelection handoff"
      pattern: "worldbookSelection"
    - from: "backend/src/routes/schemas.ts"
      to: "backend/src/routes/worldgen.ts"
      via: "selectedWorldbooks schema support with backward-compatible worldbookEntries fallback"
      pattern: "selectedWorldbooks|worldbookEntries"
---

<objective>
Wire deterministic backend composition into campaign creation and worldgen without breaking current single-source or source-free flows.

Purpose: Phase 26 only pays off if the reusable library actually feeds DNA suggestion and world generation through one canonical backend algorithm. This plan makes the backend the sole authority for composition, provenance, and compatibility.
Output: Composition helper(s), route/schema updates, and backend regressions covering no-worldbook, legacy single-worldbook, and mixed library-source flows.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-reusable-multi-worldbook-library-for-campaign-creation/26-CONTEXT.md
@.planning/phases/26-reusable-multi-worldbook-library-for-campaign-creation/26-RESEARCH.md
@.planning/phases/23-unified-research-world-generation-pipeline/23-02-SUMMARY.md
@.planning/phases/25-replace-premise-override-heuristics-with-structured-divergence-interpretation/25-03-SUMMARY.md
@shared/src/types.ts
@backend/src/campaign/manager.ts
@backend/src/routes/campaigns.ts
@backend/src/routes/worldgen.ts
@backend/src/routes/schemas.ts
@backend/src/worldgen/worldbook-importer.ts
@backend/src/worldbook-library/manager.ts

<decisions>
- D-01: Step 1 becomes a multi-source flow combining reusable library items and new uploads in one session.
- D-02: Selected library items must be recorded on the campaign, not only folded into merged ipContext.
- D-03: Merge/composition stays on the backend and must preserve provenance with deterministic duplicate handling.
- D-04: Existing suggest-seeds and generate entry points remain compatible with single-worldbook and no-worldbook callers.
</decisions>

<interfaces>
Expected shared contract after Plan 26-01:
```ts
export interface WorldbookLibraryItem {
  id: string;
  displayName: string;
  originalFileName: string;
  normalizedSourceHash: string;
  entryCount: number;
  classificationVersion: number;
  createdAt: number;
  updatedAt: number;
}

export interface CampaignWorldbookSelection {
  id: string;
  displayName: string;
  normalizedSourceHash: string;
  entryCount: number;
  createdAt: number;
  updatedAt: number;
}
```

Current worldgen request seams:
```ts
export const suggestSeedsSchema = z.object({
  premise: z.string().optional().default(""),
  name: z.string().optional(),
  franchise: z.string().optional(),
  research: z.boolean().optional(),
  worldbookEntries: z.array(...).optional(),
});

export const generateWorldSchema = z.object({
  campaignId: z.string().min(1),
  ipContext: ipContextSchema,
  premiseDivergence: premiseDivergenceSchema,
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic backend composition with provenance-aware grouping</name>
  <files>backend/src/worldbook-library/composition.ts, backend/src/worldbook-library/index.ts, backend/src/worldgen/worldbook-importer.ts, backend/src/worldgen/__tests__/worldbook-composition.test.ts</files>
  <read_first>
    - backend/src/worldbook-library/manager.ts
    - backend/src/worldgen/worldbook-importer.ts
    - backend/src/worldgen/seed-suggester.ts
    - .planning/phases/26-reusable-multi-worldbook-library-for-campaign-creation/26-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the same selected records produce the same merged result regardless of request order.
    - Test 2: duplicate entity names across sources normalize deterministically by type + normalized name instead of upload order.
    - Test 3: composition returns both merged `IpResearchContext` and a provenance structure that retains source ids/names for debugging.
  </behavior>
  <action>Create a dedicated backend composition helper per D-03. Load selected library records by id from the Plan 26-01 manager, attach source metadata to every classified entry, then build a deterministic merged set by grouping on a normalized entity key such as `type:name.trim().toLowerCase()`. Sort sources and grouped entries by stable fields (`displayName`, `id`, normalized key, summary) before choosing the merged representative so the result never depends on upload/request order. Reuse `worldbookToIpContext()` or extract helper logic from `backend/src/worldgen/worldbook-importer.ts` as needed, but keep the final composition authority on the backend. Return a composition result shaped for route usage: merged `ipContext`, the normalized selected-source snapshot, and provenance metadata sufficient to trace which record(s) contributed each merged fact. Do not add client-side merge helpers or rely on `worldbookEntries` order luck.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldgen/__tests__/worldbook-composition.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Composition logic lives in backend code and can be called by routes without browser help.
    - Duplicate/conflicting names are grouped by normalized key, not by upload order.
    - The composition result includes provenance metadata plus merged `ipContext`.
    - The helper can compose multiple selected library items into one generation context deterministically.
  </acceptance_criteria>
  <done>A backend composition module exists that merges selected library sources into one reproducible context while retaining provenance data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire campaign creation and worldgen routes to the backend composer with compatibility fallbacks</name>
  <files>backend/src/routes/schemas.ts, backend/src/routes/campaigns.ts, backend/src/routes/worldgen.ts, backend/src/routes/__tests__/campaigns.test.ts, backend/src/routes/__tests__/worldgen.test.ts</files>
  <read_first>
    - backend/src/routes/schemas.ts
    - backend/src/routes/campaigns.ts
    - backend/src/routes/worldgen.ts
    - backend/src/routes/__tests__/campaigns.test.ts
    - backend/src/routes/__tests__/worldgen.test.ts
    - backend/src/campaign/manager.ts
  </read_first>
  <behavior>
    - Test 1: `POST /api/campaigns` accepts `worldbookSelection[]` and passes it through to `createCampaign()`.
    - Test 2: `POST /api/worldgen/suggest-seeds` composes selected library items on the backend when `selectedWorldbooks` is present.
    - Test 3: `POST /api/worldgen/generate` rebuilds ipContext from saved campaign selection when no cached/request ipContext exists, before falling back to franchise research.
    - Test 4: old callers using `worldbookEntries` or no worldbook data still keep their current behavior.
  </behavior>
  <action>Extend the route contract surface per D-01 through D-04. Update `createCampaignSchema` to accept optional `worldbookSelection[]` and pass it through `backend/src/routes/campaigns.ts` into `createCampaign()`. Update `suggestSeedsSchema` so new callers can send `selectedWorldbooks` while legacy callers can still send `worldbookEntries`; in `/suggest-seeds`, prefer backend composition from selected library items, then fall back to the old single-worldbook `worldbookEntries` path, then franchise research. Update `/generate` so if the request and config cache lack `ipContext` but the campaign has a saved `worldbookSelection[]`, it composes the selection on the backend and saves the resulting `ipContext` before generation. Keep `PremiseDivergence` handling intact and compatible with the Phase 25 request-or-cache pattern. Do not remove the old `worldbookEntries` request field in this phase; keep it as a compatibility seam while the frontend migrates.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/routes/__tests__/campaigns.test.ts src/routes/__tests__/worldgen.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `createCampaignSchema` and `campaigns.ts` persist `worldbookSelection[]`.
    - `suggest-seeds` prefers backend composition from selected library items and still accepts old `worldbookEntries`.
    - `generate` can derive `ipContext` from saved selection when the wizard skipped DNA or the cache is empty.
    - No-worldbook and legacy single-worldbook requests still pass unchanged.
  </acceptance_criteria>
  <done>Campaign creation and worldgen routes now use one backend composition path for reusable library sources without regressing existing entry points.</done>
</task>

</tasks>

<verification>
Run the targeted backend route and composition tests. Confirm three flows stay green: no-worldbook generation, legacy `worldbookEntries` requests, and new multi-library selection requests.
</verification>

<acceptance_criteria>
- Backend composition is the only canonical merge path for selected reusable worldbooks.
- Suggest-seeds and generate can consume saved library selections without client-side merging.
- Campaign creation persists the source snapshot needed for downstream review and re-composition.
- Legacy request shapes still work during the migration.
</acceptance_criteria>

<success_criteria>
Worldgen and campaign creation now treat selected reusable worldbooks as a first-class backend input: the same source set can drive DNA suggestion, direct generation, and later debugging without breaking older flows.
</success_criteria>

<output>
After completion, create `.planning/phases/26-reusable-multi-worldbook-library-for-campaign-creation/26-02-SUMMARY.md`
</output>
