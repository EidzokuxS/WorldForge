---
phase: 43-travel-and-location-state-contract-resolution
plan: 05
type: execute
wave: 5
depends_on:
  - 43-04
files_modified:
  - frontend/lib/api-types.ts
  - frontend/lib/api.ts
  - frontend/lib/world-data-helpers.ts
  - frontend/lib/__tests__/world-data-helpers.test.ts
  - frontend/components/game/location-panel.tsx
  - frontend/components/game/__tests__/location-panel.test.tsx
  - frontend/app/game/page.tsx
  - frontend/app/game/__tests__/page.test.tsx
autonomous: true
requirements:
  - GSEM-03
  - GSEM-04
must_haves:
  truths:
    - "The player can see travel cost and path context for reachable destinations instead of only raw path names."
    - "The player can inspect location-local recent happenings on the gameplay screen when revisiting a place."
    - "When travel resolves, `/game` shows player-visible destination and tick-cost feedback instead of making multi-tick movement effectively invisible."
    - "Frontend world parsing stays aligned with the backend Phase 43 contract rather than flattening it back into `connectedTo` only, while remaining compatible with older optional payload shapes."
  artifacts:
    - path: "frontend/lib/api.ts"
      provides: "Phase 43 world payload parsing for connected paths, recent happenings, and compatibility-safe optional fields"
      contains: "connectedPaths|recentHappenings"
    - path: "frontend/components/game/location-panel.tsx"
      provides: "player-visible rendering of travel cost and location-local recent happenings"
      contains: "Recent Happenings"
    - path: "frontend/app/game/page.tsx"
      provides: "gameplay page wiring from world payload to location panel"
      contains: "connectedPaths"
  key_links:
    - from: "frontend/lib/api.ts"
      to: "frontend/app/game/page.tsx"
      via: "parsed world payload preserves connected path cost and recent-happenings data for gameplay"
      pattern: "connectedPaths|recentHappenings"
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/location-panel.tsx"
      via: "current location panel receives authoritative travel and local-history state instead of rebuilding it ad hoc"
      pattern: "recentHappenings|travelCost"
---

<objective>
Make the repaired Phase 43 contract visible to the player through frontend parsing and the gameplay location panel.

Purpose: finish GSEM-03 and GSEM-04 by ensuring the player can actually see travel cost/path semantics and location-local recent happenings on `/game`.
Output: frontend world-data parsing for the richer backend payload, compatibility-safe helper updates, and location-panel/page rendering for travel cost plus recent local history.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-CONTEXT.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-04-PLAN.md
@frontend/lib/api-types.ts
@frontend/lib/api.ts
@frontend/lib/world-data-helpers.ts
@frontend/components/game/location-panel.tsx
@frontend/app/game/page.tsx
@frontend/lib/__tests__/world-data-helpers.test.ts
@frontend/components/game/__tests__/location-panel.test.tsx
@frontend/app/game/__tests__/page.test.tsx

<interfaces>
From `frontend/lib/api-types.ts`:
```ts
export interface WorldData {
  locations: Array<{
    id: string;
    campaignId: string;
    name: string;
    description: string;
    tags: string[];
    connectedTo: string[];
    isStarting: boolean;
  }>;
}
```

From `frontend/components/game/location-panel.tsx`:
```ts
interface LocationPanelProps {
  connectedLocations: Array<{ id: string; name: string }>;
  onMove: (locationName: string) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update frontend world-contract parsing and compatibility helpers for Phase 43</name>
  <read_first>
@shared/src/types.ts
@frontend/lib/api-types.ts
@frontend/lib/api.ts
@frontend/lib/world-data-helpers.ts
@frontend/lib/__tests__/world-data-helpers.test.ts
  </read_first>
  <files>frontend/lib/api-types.ts, frontend/lib/api.ts, frontend/lib/world-data-helpers.ts, frontend/lib/__tests__/world-data-helpers.test.ts</files>
  <acceptance_criteria>
    - `rg -n "connectedPaths|recentHappenings|locationKind|persistence" frontend/lib/api-types.ts frontend/lib/api.ts frontend/lib/world-data-helpers.ts` finds the richer Phase 43 payload contract on the frontend.
    - `npm --prefix frontend exec vitest run lib/__tests__/world-data-helpers.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: frontend parsing accepts backend `connectedPaths` and `recentHappenings` without flattening them away.
    - Test 2: world-review helper code still derives legacy connected-name lists from the new path structure where older consumers still need names.
    - Test 3: richer Phase 43 payload fields are optional/nullable enough that world-review and campaign-creation surfaces do not break when they are absent.
    - Test 4: frontend types reuse shared location-domain vocabulary instead of duplicating enums ad hoc.
  </behavior>
  <action>Update the frontend world contract to match the Phase 43 backend payload. Extend `api-types.ts` and `api.ts` so parsed locations carry `connectedPaths`, `recentHappenings`, and lifecycle metadata from shared types, but keep the richer fields optional/nullable so world-review, campaign-creation, and older bootstrap payloads remain compatible while backend rollout is in flight. Update `world-data-helpers.ts` to derive any legacy name-only `connectedTo` views from normalized path objects when present, with safe fallback to existing `connectedTo` arrays only when `connectedPaths` is absent. Treat `connectedTo` as read-only compatibility data during Phase 43 rollout, not as a second editable authority, and do not add new frontend logic that prefers or rewrites it over `connectedPaths`; remaining consumers should be explicit Phase 44 cleanup debt. Import shared location enums/types where possible; do not duplicate a second frontend-only taxonomy.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run lib/__tests__/world-data-helpers.test.ts</automated>
  </verify>
  <done>The frontend can parse the backend's richer Phase 43 location payload and still support older helper consumers without flattening the repaired contract away.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render travel cost/path semantics and local recent happenings in the gameplay location panel</name>
  <read_first>
@frontend/lib/api.ts
@frontend/components/game/location-panel.tsx
@frontend/app/game/page.tsx
@frontend/components/game/__tests__/location-panel.test.tsx
@frontend/app/game/__tests__/page.test.tsx
  </read_first>
  <files>frontend/components/game/location-panel.tsx, frontend/components/game/__tests__/location-panel.test.tsx, frontend/app/game/page.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <acceptance_criteria>
    - `rg -n "Recent Happenings|travel cost|ticks|connectedPaths" frontend/components/game/location-panel.tsx frontend/app/game/page.tsx` finds player-visible travel and local-history rendering.
    - `npm --prefix frontend exec vitest run app/game/__tests__/page.test.tsx components/game/__tests__/location-panel.test.tsx` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: the location panel lists reachable destinations with travel cost and, when available, a short path summary instead of only raw path names.
    - Test 2: the location panel renders recent happenings for the current location with an honest empty state when nothing notable is recorded.
    - Test 3: gameplay page wiring reads the new path/history fields directly from `worldData` and still issues move actions against destination names.
    - Test 4: `/game` shows player-visible travel feedback when a streamed `location_change` state update carries multi-tick travel cost or path details, instead of inferring travel locally on button click and risking drift from backend authority.
  </behavior>
  <action>Update `/game` and `location-panel.tsx` so the repaired backend contract is player-visible. Replace the current name-only `connectedLocations` derivation with `connectedPaths`, including travel cost text and compact path context where useful, while gracefully handling payloads where those richer fields are absent. Add a `Recent Happenings` section that renders location-local recent-event summaries from the backend with a bounded empty state instead of silence. Keep the move interaction simple: buttons still dispatch `go to {destination name}`. Also consume the richer `location_change` state update from Plan 43-02 so `/game` can surface honest player-visible travel feedback for multi-tick moves, such as a transient system line or status copy indicating destination and ticks consumed. That feedback should be driven by the streamed backend event, not speculative client-side inference from the click, so frontend timing stays aligned with the authoritative travel-turn pipeline. Do not widen scope into map UI, transit planners, or separate history screens.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run app/game/__tests__/page.test.tsx components/game/__tests__/location-panel.test.tsx</automated>
  </verify>
  <done>The player can see the repaired Phase 43 contract directly on `/game`: travel has visible cost/path semantics, and locations show their own recent happenings on revisit.</done>
</task>

</tasks>

<verification>
Run the targeted frontend helper and gameplay rendering suites above. Confirm `/game` now consumes `connectedPaths` and `recentHappenings` directly instead of rebuilding location state from flat adjacency IDs alone.
</verification>

<success_criteria>
Phase 43 becomes player-visible. Travel looks path-bound and time-costed, and local recent happenings are inspectable from the gameplay UI without any docs-only handwaving.
</success_criteria>

<output>
After completion, create `.planning/phases/43-travel-and-location-state-contract-resolution/43-05-SUMMARY.md`
</output>
