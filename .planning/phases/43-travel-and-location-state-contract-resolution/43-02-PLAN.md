---
phase: 43-travel-and-location-state-contract-resolution
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - backend/src/campaign/manager.ts
  - backend/src/campaign/index.ts
  - backend/src/engine/location-graph.ts
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/npc-tools.ts
  - backend/src/engine/tool-executor.ts
  - backend/src/engine/__tests__/location-graph.test.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/engine/__tests__/npc-agent.test.ts
  - backend/src/engine/__tests__/tool-executor.test.ts
autonomous: true
requirements:
  - GSEM-03
must_haves:
  truths:
    - "Moving between major locations resolves through the authoritative graph instead of one-hop adjacency-only teleports per D-08."
    - "Player-visible travel consumes observable abstract time via explicit edge cost per D-09."
    - "Player and NPC movement share one path-resolution contract instead of diverging movement rules."
    - "Multi-tick player travel advances the authoritative campaign tick to the resulting boundary once, and post-turn simulation observes that final tick without synthetic intermediate travel turns."
  artifacts:
    - path: "backend/src/engine/location-graph.ts"
      provides: "authoritative path resolution and travel-cost accumulation"
      contains: "resolveTravelPath"
    - path: "backend/src/engine/turn-processor.ts"
      provides: "player movement path application, shared destination resolution, and travel-time tick advancement"
      contains: "travelCost|location_change"
    - path: "backend/src/engine/npc-tools.ts"
      provides: "NPC movement through the same location graph authority"
      contains: "resolveTravelPath"
  key_links:
    - from: "backend/src/engine/turn-processor.ts"
      to: "backend/src/engine/location-graph.ts"
      via: "player movement resolves a full path before updating location or tick"
      pattern: "resolveTravelPath|travelCost"
    - from: "backend/src/engine/npc-tools.ts"
      to: "backend/src/engine/location-graph.ts"
      via: "NPC move_to reuses the same path resolver and reachability rules"
      pattern: "resolveTravelPath"
    - from: "backend/src/engine/tool-executor.ts"
      to: "backend/src/engine/location-graph.ts"
      via: "storyteller move_to and reveal_location share the same destination resolution and normalized-edge writes as inline player movement"
      pattern: "resolveLocationTarget|resolveTravelPath|travelCost"
---

<objective>
Implement the backend travel graph and time-cost authority for Phase 43.

Purpose: satisfy GSEM-03 by replacing adjacency-only instant movement with one authoritative graph resolver that both player and NPC flows reuse.
Output: graph resolver, authoritative multi-tick travel contract, shared player/NPC/tool movement wiring, and backend movement state updates that follow normalized edge data rather than raw `connectedTo`.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-CONTEXT.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-RESEARCH.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-01-PLAN.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-02-SUMMARY.md
@backend/src/campaign/manager.ts
@backend/src/campaign/index.ts
@backend/src/engine/turn-processor.ts
@backend/src/engine/npc-tools.ts
@backend/src/engine/tool-executor.ts
@backend/src/engine/__tests__/location-graph.test.ts
@backend/src/engine/__tests__/turn-processor.test.ts
@backend/src/engine/__tests__/npc-agent.test.ts
@backend/src/engine/__tests__/tool-executor.test.ts

<interfaces>
From `backend/src/campaign/manager.ts`:
```ts
export function incrementTick(campaignId: string): number
```

From `backend/src/engine/turn-processor.ts`:
```ts
yield {
  type: "state_update",
  data: {
    type: "location_change",
    locationId: destination.id,
    locationName: destination.name,
  },
};
```

From `backend/src/engine/tool-executor.ts`:
```ts
// storyteller tool entry points that currently resolve movement by destination name
case "move_to":
case "reveal_location":
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the authoritative travel-path resolver and travel-time tick advance</name>
  <read_first>
@.planning/phases/43-travel-and-location-state-contract-resolution/43-RESEARCH.md
@backend/src/campaign/manager.ts
@backend/src/campaign/index.ts
@backend/src/engine/location-graph.ts
@backend/src/db/schema.ts
@backend/src/engine/__tests__/location-graph.test.ts
  </read_first>
  <files>backend/src/campaign/manager.ts, backend/src/campaign/index.ts, backend/src/engine/location-graph.ts, backend/src/engine/__tests__/location-graph.test.ts</files>
  <acceptance_criteria>
    - `rg -n "resolveTravelPath|listConnectedPaths|travelCost|advanceCampaignTick" backend/src/engine/location-graph.ts backend/src/campaign/manager.ts backend/src/campaign/index.ts` finds one resolver seam plus explicit tick-advance support.
    - `npm --prefix backend exec vitest run src/engine/__tests__/location-graph.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: the graph resolver chooses a real discovered path between locations and sums integer edge travel cost.
    - Test 2: archived or expired ephemeral scene nodes are not treated as normal traversal targets.
    - Test 3: travel can span more than one edge when a believable graph path exists, satisfying D-08 without building a transit simulator.
    - Test 4: campaign tick advancement can consume explicit travel cost instead of always incrementing by exactly one.
    - Test 5: destination-name resolution returns one canonical location ID for both inline movement detection output and tool-driven movement inputs before path resolution runs.
  </behavior>
  <action>Create `backend/src/engine/location-graph.ts` as the authoritative read/write seam for normalized edges. Implement a canonical destination resolver that turns player/tool destination names into one location ID before any movement path is evaluated, then implement a graph resolver that accepts campaign, source node, destination node, and discovery/lifecycle constraints and returns `path`, `totalCost`, and the ordered traversed locations. Add a campaign tick helper in `backend/src/campaign/manager.ts` and export it from `backend/src/campaign/index.ts` so travel turns can advance by explicit cost while non-travel turns preserve the current one-tick behavior. Make the Phase 43 contract explicit: player travel advances the authoritative global campaign tick by `totalCost`, but rollback-critical post-turn simulation still runs once per player turn at the resulting tick boundary rather than synthesizing intermediate turns per traversed edge. Threshold-based systems therefore observe the jumped tick honestly, while this plan avoids building per-edge transit subturns. Use direct integer `travelCost` from edges; do not infer cost from names, tags, or path length. Keep expired/archived ephemeral scene nodes out of normal reachability unless a dedicated archival read path asks for them.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/engine/__tests__/location-graph.test.ts</automated>
  </verify>
  <done>The backend has one reusable graph resolver and explicit travel-time tick primitive that later movement code can consume without reinventing travel semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire player, NPC, and reveal-location movement onto the shared graph contract</name>
  <read_first>
@backend/src/engine/location-graph.ts
@backend/src/engine/turn-processor.ts
@backend/src/engine/npc-tools.ts
@backend/src/engine/tool-executor.ts
@backend/src/engine/__tests__/turn-processor.test.ts
@backend/src/engine/__tests__/npc-agent.test.ts
@backend/src/engine/__tests__/tool-executor.test.ts
  </read_first>
  <files>backend/src/engine/turn-processor.ts, backend/src/engine/npc-tools.ts, backend/src/engine/tool-executor.ts, backend/src/engine/__tests__/turn-processor.test.ts, backend/src/engine/__tests__/npc-agent.test.ts, backend/src/engine/__tests__/tool-executor.test.ts</files>
  <acceptance_criteria>
    - `rg -n "resolveTravelPath|travelCost|connectedPaths|location_change" backend/src/engine/turn-processor.ts backend/src/engine/npc-tools.ts backend/src/engine/tool-executor.ts` shows movement no longer reading raw `connectedTo` as the Phase 43 authority.
    - `npm --prefix backend test -- src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/npc-agent.test.ts src/engine/__tests__/tool-executor.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: player travel resolves a full path and advances campaign time by total travel cost.
    - Test 2: player movement emits enough structured state for later API/UI work to show destination, travel cost, and tick advance honestly.
    - Test 3: NPC `move_to` accepts or rejects destinations using the same graph contract as the player path.
    - Test 4: inline movement detection and storyteller `move_to` share one destination-name-to-ID resolver before path resolution.
    - Test 5: `reveal_location` inserts typed nodes plus normalized bidirectional edges instead of only mutating adjacency JSON.
  </behavior>
  <action>Replace the ad hoc movement logic in `turn-processor.ts`, `npc-tools.ts`, and `tool-executor.ts` with the shared `location-graph.ts` seam. Keep the two player entry points explicit but unified: inline movement detection in `turn-processor.ts` and storyteller `move_to` in `tool-executor.ts` should remain separate callers, but both must first resolve destination name to one canonical location ID through the same helper and then call the same path resolver. Player travel must resolve a full graph path from current node to destination, update the authoritative location, and advance tick by `totalCost` so time passes observably per D-09. NPC movement must use the same resolver for reachability and path semantics, but it stays inside existing simulation cadence and does not perform a second independent global tick advance. `reveal_location` must create typed nodes and normalized bidirectional edge rows inside the one-coherent graph per D-05 rather than only appending IDs into `connectedTo`. Make unreachable-destination behavior explicit and shared across both player entry points. Emit structured `location_change` state with travel-cost/tick-advance fields so later API/UI work can show multi-tick travel honestly. Keep scope bounded to graph-travel semantics; do not build UI, route payload shaping, or generic route-planning UX in this plan.</action>
  <verify>
    <automated>npm --prefix backend test -- src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/npc-agent.test.ts src/engine/__tests__/tool-executor.test.ts</automated>
  </verify>
  <done>Travel is now backend-owned, path-bound, and time-costed for the player while NPC movement shares the same reachability model.</done>
</task>

</tasks>

<verification>
Run the location-graph, turn-processor, and NPC movement suites above. Confirm player travel now advances explicit cost and that NPC movement no longer uses a different adjacency contract.
</verification>

<success_criteria>
The live backend no longer effectively teleports actors across the world graph. Movement follows normalized edges, major-location travel can span intermediate nodes, and travel consumes explicit time the simulation can observe.
</success_criteria>

<output>
After completion, create `.planning/phases/43-travel-and-location-state-contract-resolution/43-02-SUMMARY.md`
</output>
