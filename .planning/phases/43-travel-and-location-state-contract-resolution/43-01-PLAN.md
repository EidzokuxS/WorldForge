---
phase: 43-travel-and-location-state-contract-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/types.ts
  - backend/src/db/schema.ts
  - backend/src/db/migrate.ts
  - backend/src/worldgen/scaffold-saver.ts
  - backend/src/worldgen/__tests__/scaffold-saver.test.ts
  - backend/src/engine/__tests__/location-graph.test.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/engine/__tests__/npc-agent.test.ts
  - backend/src/routes/__tests__/campaigns.test.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
autonomous: true
requirements:
  - GSEM-03
  - GSEM-04
must_haves:
  truths:
    - "Phase 43 has one executable location contract that distinguishes macro locations, persistent sublocations, and ephemeral scene nodes per D-04, D-05, and D-06."
    - "Backend regressions now describe externally checkable Phase 43 behavior for multi-edge travel cost, shared player/NPC traversal, and location-local recent happenings before implementation starts."
    - "Any later implementation that falls back to adjacency-only travel or global-only history will fail the targeted Phase 43 backend suite instead of silently shipping."
  artifacts:
    - path: "shared/src/types.ts"
      provides: "shared location graph and location-history contracts"
      contains: "LocationKind"
    - path: "backend/src/db/schema.ts"
      provides: "authoritative location metadata plus normalized edge and location-recent-event tables"
      contains: "location_edges|location_recent_events"
    - path: "backend/src/db/migrate.ts"
      provides: "safe additive migration and backfill for pre-Phase-43 campaign databases"
      contains: "location_edges|connected_to"
    - path: "backend/src/engine/__tests__/location-graph.test.ts"
      provides: "travel graph contract coverage for path resolution and travel-cost accumulation"
      contains: "resolveTravelPath"
  key_links:
    - from: "shared/src/types.ts"
      to: "backend/src/db/schema.ts"
      via: "shared type names mirror persisted location kind, persistence, edge, and recent-event fields"
      pattern: "LocationKind|LocationPersistence|location_edges|location_recent_events"
    - from: "backend/src/db/schema.ts"
      to: "backend/src/engine/__tests__/turn-processor.test.ts"
      via: "tests pin travel semantics against the new normalized location authority instead of raw adjacency JSON"
      pattern: "travel_cost|location_edges|connectedPaths"
    - from: "backend/src/db/migrate.ts"
      to: "backend/src/worldgen/scaffold-saver.ts"
      via: "existing campaigns and newly generated scaffolds both land on the same typed-location defaults and normalized-edge projection"
      pattern: "kind|persistence|location_edges|connectedTo"
---

<objective>
Define the authoritative Phase 43 location contract and lock the backend regressions that later plans must satisfy.

Purpose: satisfy D-01 through D-06 before implementation starts so travel and local-history work build on one coherent runtime model instead of another patch on top of `connectedTo`.
Output: shared location-domain types, normalized schema plus migration/backfill defaults, worldgen compatibility hooks, and failing backend tests for travel and location-local history behavior.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-CONTEXT.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-RESEARCH.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-VALIDATION.md
@.planning/phases/36-gameplay-docs-to-runtime-reconciliation-audit/36-HANDOFF.md
@backend/src/db/schema.ts
@backend/src/db/migrate.ts
@backend/src/worldgen/scaffold-saver.ts
@backend/src/engine/turn-processor.ts
@backend/src/engine/npc-tools.ts
@backend/src/routes/campaigns.ts
@backend/src/engine/prompt-assembler.ts
@shared/src/types.ts

<interfaces>
From `backend/src/db/schema.ts`:
```ts
export const locations = sqliteTable("locations", {
  id: text("id").primaryKey(),
  campaignId: text("campaign_id").notNull(),
  name: text("name").notNull(),
  description: text("description").notNull(),
  tags: text("tags").notNull().default("[]"),
  isStarting: integer("is_starting", { mode: "boolean" }).notNull().default(false),
  connectedTo: text("connected_to").notNull().default("[]"),
});
```

From `backend/src/engine/turn-processor.ts`:
```ts
export async function detectMovement(
  action: string,
  judgeProvider: ProviderConfig,
): Promise<string | null>
```

From `backend/src/worldgen/scaffold-saver.ts`:
```ts
export function saveScaffoldToDb(
  campaignId: string,
  scaffold: WorldScaffold,
): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the shared Phase 43 location contract, migration/backfill path, and worldgen-safe defaults</name>
  <read_first>
@.planning/phases/43-travel-and-location-state-contract-resolution/43-CONTEXT.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-RESEARCH.md
@shared/src/types.ts
@backend/src/db/schema.ts
@backend/src/db/migrate.ts
@backend/src/worldgen/scaffold-saver.ts
  </read_first>
  <files>shared/src/types.ts, backend/src/db/schema.ts, backend/src/db/migrate.ts, backend/src/worldgen/scaffold-saver.ts, backend/src/worldgen/__tests__/scaffold-saver.test.ts</files>
  <acceptance_criteria>
    - `rg -n "LocationKind|LocationPersistence|macro|persistent_sublocation|ephemeral_scene" shared/src/types.ts` finds the three-class location taxonomy from D-04 in shared contracts.
    - `rg -n "location_edges|location_recent_events|parent_location_id|persistence|expires_at_tick|archived_at_tick" backend/src/db/schema.ts` finds normalized persistence, edge, and recent-event authority fields.
    - `rg -n "connected_to" backend/src/db/schema.ts` still shows any temporary compatibility column explicitly, not as the only authoritative travel model.
    - `rg -n "location_edges|kind|persistence|connected_to|anchor_location_id" backend/src/db/migrate.ts backend/src/worldgen/scaffold-saver.ts` finds an explicit additive migration/backfill path for existing campaigns plus compatibility defaults for freshly generated worlds.
  </acceptance_criteria>
  <action>Create the Phase 43 contract surface in shared and SQLite. In `shared/src/types.ts`, add explicit location-domain contracts for node class, persistence semantics, connected path summaries, and recent-happenings summaries so backend and frontend can share one vocabulary per D-04, D-05, and D-06. In `backend/src/db/schema.ts`, extend `locations` with explicit lifecycle fields (`kind`, `parentLocationId`, `anchorLocationId`, `persistence`, `expiresAtTick`, `archivedAtTick`) and add normalized `location_edges` plus `location_recent_events` tables with campaign-scoped indexes. Use the repo's existing Drizzle migration path for schema shape changes: update `schema.ts`, generate the new migration artifacts under `backend/drizzle/` via `npm --prefix backend run db:generate`, and keep `backend/src/db/migrate.ts` as the runner for generated migrations plus any narrowly scoped compatibility backfill that must execute after the schema is present. Do not replace that flow with ad hoc schema mutations hidden only inside `migrate.ts`. For the Phase 43 compatibility backfill, make first load safe for existing campaigns: backfill legacy rows to `kind=macro` and `persistence=persistent`, leave `parentLocationId` and `anchorLocationId` null unless a later writer sets them, and populate normalized `location_edges` from existing `connectedTo` JSON so pre-Phase-43 campaigns remain playable on first load, while malformed legacy `connectedTo` payloads fail closed to an empty edge set plus logged warning rather than aborting migration. Clarify in task comments or helper naming that `parentLocationId` is hierarchical containment while `anchorLocationId` is ephemeral-scene consequence spillover. In `backend/src/worldgen/scaffold-saver.ts`, persist freshly generated worlds with the same compatibility defaults and normalized edges so current worldgen output remains valid without requiring generator-prompt rewrites in this phase. Keep `locations.connectedTo` only as a read-only compatibility projection derived from normalized edges while Phase 43 migrates readers; do not let later tasks continue using it as graph authority or establish permanent dual-write authority. Treat any remaining `connectedTo` readers as explicit Phase 44 cleanup debt. Do not add coordinates, map rendering fields, or transit-simulation data.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/worldgen/__tests__/scaffold-saver.test.ts</automated>
  </verify>
  <done>The repo has one explicit contract for typed location nodes, normalized travel edges, and location-local event projection storage, and both existing campaigns and newly generated worlds have an explicit default/backfill path into that contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock backend regressions for path-bound travel and local recent happenings</name>
  <read_first>
@.planning/phases/43-travel-and-location-state-contract-resolution/43-CONTEXT.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-VALIDATION.md
@backend/src/engine/turn-processor.ts
@backend/src/engine/npc-tools.ts
@backend/src/routes/campaigns.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/__tests__/turn-processor.test.ts
@backend/src/engine/__tests__/npc-agent.test.ts
@backend/src/routes/__tests__/campaigns.test.ts
@backend/src/engine/__tests__/prompt-assembler.test.ts
  </read_first>
  <files>backend/src/engine/__tests__/location-graph.test.ts, backend/src/engine/__tests__/turn-processor.test.ts, backend/src/engine/__tests__/npc-agent.test.ts, backend/src/routes/__tests__/campaigns.test.ts, backend/src/engine/__tests__/prompt-assembler.test.ts</files>
  <acceptance_criteria>
    - `rg -n "travel cost|resolveTravelPath|recent happenings|connectedPaths|ephemeral scene" backend/src/engine/__tests__/location-graph.test.ts backend/src/engine/__tests__/turn-processor.test.ts backend/src/engine/__tests__/npc-agent.test.ts backend/src/routes/__tests__/campaigns.test.ts backend/src/engine/__tests__/prompt-assembler.test.ts` finds direct coverage for GSEM-03 and GSEM-04.
    - `npm --prefix backend test -- src/engine/__tests__/location-graph.test.ts src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/npc-agent.test.ts src/routes/__tests__/campaigns.test.ts src/engine/__tests__/prompt-assembler.test.ts` fails only because the Phase 43 implementation is not wired yet.
  </acceptance_criteria>
  <behavior>
    - Test 1: graph traversal resolves a believable multi-edge path with summed travel cost instead of adjacency-only teleport movement.
    - Test 2: player movement and NPC `move_to` use the same graph resolver and reject unreachable destinations consistently.
    - Test 3: `/api/campaigns/:id/world` returns normalized path data and per-location recent-happenings payloads rather than only raw `connectedTo` IDs.
    - Test 4: prompt assembly surfaces recent happenings for the current location, including archived ephemeral-scene spillover when anchored to the current persistent place.
    - Test 5: temporary scene locations can archive without erasing their consequences from recent-happenings reads.
  </behavior>
  <action>Write the missing Phase 43 regressions the validation strategy calls out. Add a dedicated `location-graph.test.ts` for path resolution and cost accumulation. Extend turn-processor and NPC tests so player movement and NPC movement both depend on one graph contract per D-08 and D-09. Extend route and prompt tests so backend readers expect `connectedPaths` and location-local `recentHappenings`, including the archived-ephemeral-scene consequence rule from D-13. Keep these tests bounded to the minimal complete repair; do not add map rendering, generic gossip propagation, or transit optimization cases.</action>
  <verify>
    <automated>npm --prefix backend test -- src/engine/__tests__/location-graph.test.ts src/engine/__tests__/turn-processor.test.ts src/engine/__tests__/npc-agent.test.ts src/routes/__tests__/campaigns.test.ts src/engine/__tests__/prompt-assembler.test.ts</automated>
  </verify>
  <done>The backend test suite now states the required Phase 43 behavior explicitly enough that later implementation plans can execute against contracts instead of rediscovering the scope.</done>
</task>

</tasks>

<verification>
Run the backend Phase 43 regression set above. Confirm the repo now has shared type/schema authority plus failing tests for travel cost, normalized path data, and location-local recent happenings.
</verification>

<success_criteria>
Phase 43 now starts from one explicit location-system contract and a pinned regression suite. Later plans do not need to infer what macro locations, persistent sublocations, ephemeral scene nodes, travel cost, or local recent happenings are supposed to mean.
</success_criteria>

<output>
After completion, create `.planning/phases/43-travel-and-location-state-contract-resolution/43-01-SUMMARY.md`
</output>
