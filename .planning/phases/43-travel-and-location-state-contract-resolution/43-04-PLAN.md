---
phase: 43-travel-and-location-state-contract-resolution
plan: 04
type: execute
wave: 4
depends_on:
  - 43-02
  - 43-03
files_modified:
  - backend/src/routes/campaigns.ts
  - backend/src/routes/__tests__/campaigns.test.ts
  - backend/src/engine/prompt-assembler.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
autonomous: true
requirements:
  - GSEM-04
must_haves:
  truths:
    - "Revisiting a location exposes concrete recent happenings through backend read surfaces instead of only through global chronicle prose per D-11 and D-12."
    - "The world API and prompt assembly read the same authoritative location-history seam rather than parallel ad hoc summaries."
    - "Archived ephemeral-scene consequences remain visible to both world payloads and prompts when anchored to the current persistent place per D-13."
  artifacts:
    - path: "backend/src/routes/campaigns.ts"
      provides: "world payload with normalized connected-path and recent-happenings reads"
      contains: "recentHappenings"
    - path: "backend/src/engine/prompt-assembler.ts"
      provides: "scene prompt surfacing of current-location recent happenings"
      contains: "Recent happenings here"
    - path: "backend/src/routes/__tests__/campaigns.test.ts"
      provides: "world-route regression coverage for connected paths and recent happenings"
      contains: "recentHappenings"
  key_links:
    - from: "backend/src/routes/campaigns.ts"
      to: "backend/src/engine/location-events.ts"
      via: "world payload reads normalized local-history rows and path summaries from the authoritative seam"
      pattern: "recentHappenings|listRecentLocationEvents|connectedPaths"
    - from: "backend/src/engine/prompt-assembler.ts"
      to: "backend/src/engine/location-events.ts"
      via: "prompt assembly reads the same recent-happenings seam as the API for current-location narration"
      pattern: "Recent happenings here|listRecentLocationEvents"
---

<objective>
Expose Phase 43 location-local history and normalized path data through backend read surfaces.

Purpose: finish the backend half of GSEM-04 by ensuring prompts and `/api/campaigns/:id/world` read the same authoritative recent-happenings seam instead of inventing parallel summaries.
Output: world-route payload shaping for `connectedPaths` plus `recentHappenings`, and prompt assembly that surfaces current-location history from the shared seam.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-CONTEXT.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-03-PLAN.md
@backend/src/routes/campaigns.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/routes/__tests__/campaigns.test.ts
@backend/src/engine/__tests__/prompt-assembler.test.ts

<interfaces>
From `backend/src/routes/campaigns.ts`:
```ts
return c.json({
  locations: worldLocations,
  npcs: ...,
  factions: ...,
  relationships: ...,
  player: ...,
  items: worldItems,
});
```

From `backend/src/engine/prompt-assembler.ts`:
```ts
export function assembleScenePrompt(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose normalized path and local-history reads through the world API</name>
  <read_first>
@backend/src/engine/location-events.ts
@backend/src/engine/location-graph.ts
@backend/src/routes/campaigns.ts
@backend/src/routes/__tests__/campaigns.test.ts
  </read_first>
  <files>backend/src/routes/campaigns.ts, backend/src/routes/__tests__/campaigns.test.ts</files>
  <acceptance_criteria>
    - `rg -n "connectedPaths|recentHappenings|travelCost" backend/src/routes/campaigns.ts` finds normalized path and recent-history fields in the world payload.
    - `npm --prefix backend test -- src/routes/__tests__/campaigns.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: `/api/campaigns/:id/world` returns per-location `connectedPaths` with travel-cost information derived from the authoritative graph seam.
    - Test 2: `/api/campaigns/:id/world` returns per-location `recentHappenings` from the location-history projection, including anchored archived-scene spillover when relevant.
    - Test 3: empty locations return an honest bounded fallback shape instead of silently omitting the contract.
  </behavior>
  <action>Update `backend/src/routes/campaigns.ts` so each location payload exposes normalized `connectedPaths` from the graph resolver and `recentHappenings` from `location-events.ts`, both shaped for direct frontend consumption. Keep the payload bounded to player-visible state: short path summaries plus a limited local-history window. Do not add frontend parsing, new UI components, or semantic-search-only fallbacks in this plan.</action>
  <verify>
    <automated>npm --prefix backend test -- src/routes/__tests__/campaigns.test.ts</automated>
  </verify>
  <done>The world API now exposes authoritative connected-path and local-history reads in one stable payload contract instead of forcing clients to infer them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface current-location recent happenings through prompt assembly</name>
  <read_first>
@backend/src/engine/location-events.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/engine/__tests__/prompt-assembler.test.ts
  </read_first>
  <files>backend/src/engine/prompt-assembler.ts, backend/src/engine/__tests__/prompt-assembler.test.ts</files>
  <acceptance_criteria>
    - `rg -n "Recent happenings here|recentHappenings|listRecentLocationEvents" backend/src/engine/prompt-assembler.ts` finds prompt-level readback from the shared seam.
    - `npm --prefix backend test -- src/engine/__tests__/prompt-assembler.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: prompt assembly surfaces recent happenings for the current location from `location-events.ts` instead of only global chronicle summaries.
    - Test 2: anchored archived-scene spillover appears when relevant to the current persistent location.
    - Test 3: empty-history locations get an honest bounded fallback instead of silent omission or unbounded event dumps.
  </behavior>
  <action>Update `backend/src/engine/prompt-assembler.ts` to read the same location-history seam that the world API exposes. Inject a bounded `Recent happenings here` section for the current location, including anchored archived-scene consequences when relevant, so revisit narration is grounded in authoritative local memory. Keep the prompt bounded to a short meaningful window; do not dump whole event logs or invent a second history source.</action>
  <verify>
    <automated>npm --prefix backend test -- src/engine/__tests__/prompt-assembler.test.ts</automated>
  </verify>
  <done>The prompt now reads the same authoritative local-history seam as the world API, so revisit narration reflects concrete current-location memory.</done>
</task>

</tasks>

<verification>
Run the campaigns-route and prompt-assembler suites above. Confirm backend read surfaces now agree on `connectedPaths` plus `recentHappenings` and both consume the shared location-history seam.
</verification>

<success_criteria>
Phase 43 now has coherent backend read surfaces: the world payload and prompt assembly both expose authoritative location-local history, and neither has to reconstruct it from vectors or stale prose.
</success_criteria>

<output>
After completion, create `.planning/phases/43-travel-and-location-state-contract-resolution/43-04-SUMMARY.md`
</output>
