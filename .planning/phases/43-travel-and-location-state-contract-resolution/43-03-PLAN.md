---
phase: 43-travel-and-location-state-contract-resolution
plan: 03
type: execute
wave: 3
depends_on:
  - 43-01
  - 43-02
files_modified:
  - backend/src/engine/location-events.ts
  - backend/src/vectors/episodic-events.ts
  - backend/src/engine/tool-executor.ts
  - backend/src/engine/npc-tools.ts
  - backend/src/engine/npc-offscreen.ts
  - backend/src/engine/faction-tools.ts
  - backend/src/engine/prompt-assembler.ts
  - backend/src/routes/campaigns.ts
  - backend/src/vectors/__tests__/episodic-events.test.ts
  - backend/src/engine/__tests__/tool-executor.test.ts
  - backend/src/routes/__tests__/campaigns.test.ts
  - backend/src/engine/__tests__/prompt-assembler.test.ts
autonomous: true
requirements:
  - GSEM-04
must_haves:
  truths:
    - "Revisiting a location exposes concrete recent happenings tied to that location per D-11 and D-12."
    - "Recent happenings are written from authoritative runtime events and survive retry, undo, and checkpoint restore per D-14."
    - "Ephemeral scene nodes can archive without erasing their consequences from later location-local history per D-13."
  artifacts:
    - path: "backend/src/engine/location-events.ts"
      provides: "authoritative write/read seam for location-local recent happenings"
      contains: "recordLocationRecentEvent"
    - path: "backend/src/routes/campaigns.ts"
      provides: "world payload with per-location recent happenings"
      contains: "recentHappenings"
    - path: "backend/src/engine/prompt-assembler.ts"
      provides: "scene prompt surfacing of local recent happenings"
      contains: "Recent happenings here"
  key_links:
    - from: "backend/src/vectors/episodic-events.ts"
      to: "backend/src/engine/location-events.ts"
      via: "authoritative event writes project into location-local history instead of vector retrieval being the only source"
      pattern: "sourceEventId|recordLocationRecentEvent"
    - from: "backend/src/routes/campaigns.ts"
      to: "backend/src/engine/prompt-assembler.ts"
      via: "API and prompt read the same location-local recent-happenings seam"
      pattern: "recentHappenings|listRecentLocationEvents"
---

<objective>
Implement location-local recent happenings, ephemeral-scene lifecycle handling, and backend read surfaces.

Purpose: satisfy GSEM-04 and the D-11 through D-14 decision set by making location-local history a real runtime projection instead of a doc-only promise.
Output: authoritative location-event write/read helpers, scene-lifecycle preservation rules, prompt surfacing, and world API payloads that expose local recent happenings.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-CONTEXT.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-RESEARCH.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-VALIDATION.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-02-SUMMARY.md
@backend/src/vectors/episodic-events.ts
@backend/src/engine/tool-executor.ts
@backend/src/engine/npc-tools.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/engine/faction-tools.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/routes/campaigns.ts

<interfaces>
From `backend/src/vectors/episodic-events.ts`:
```ts
export async function storeEpisodicEvent(
  campaignId: string,
  event: Omit<EpisodicEvent, "id" | "vector">
): Promise<string>
```

From `backend/src/routes/campaigns.ts`:
```ts
return c.json({
  locations: worldLocations,
  npcs: ...,
  factions: ...,
  relationships: ...,
  player: ...,
  items: worldItems,
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement write-through location recent-events projection and ephemeral-scene consequence retention</name>
  <read_first>
@.planning/phases/43-travel-and-location-state-contract-resolution/43-CONTEXT.md
@backend/src/vectors/episodic-events.ts
@backend/src/engine/tool-executor.ts
@backend/src/engine/npc-tools.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/engine/faction-tools.ts
@backend/src/vectors/__tests__/episodic-events.test.ts
@backend/src/engine/__tests__/tool-executor.test.ts
  </read_first>
  <files>backend/src/engine/location-events.ts, backend/src/vectors/episodic-events.ts, backend/src/engine/tool-executor.ts, backend/src/engine/npc-tools.ts, backend/src/engine/npc-offscreen.ts, backend/src/engine/faction-tools.ts, backend/src/vectors/__tests__/episodic-events.test.ts, backend/src/engine/__tests__/tool-executor.test.ts</files>
  <acceptance_criteria>
    - `rg -n "recordLocationRecentEvent|listRecentLocationEvents|sourceEventId|anchorLocationId|archivedAtTick" backend/src/engine/location-events.ts backend/src/vectors/episodic-events.ts backend/src/engine/tool-executor.ts backend/src/engine/npc-tools.ts backend/src/engine/npc-offscreen.ts backend/src/engine/faction-tools.ts` finds one write/read seam plus ephemeral-scene lifecycle handling.
    - `npm --prefix backend exec vitest run src/vectors/__tests__/episodic-events.test.ts src/engine/__tests__/tool-executor.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: authoritative gameplay and simulation writers attach concrete location context to committed events instead of storing empty location metadata.
    - Test 2: a location-facing projection row is written for recent happenings with traceability back to the source episodic event when one exists.
    - Test 3: archived ephemeral scene locations preserve their consequences through anchored recent-happenings reads instead of disappearing cleanly from history.
    - Test 4: the local-history seam stays in SQLite-backed campaign state, not vector search only.
  </behavior>
  <action>Create `backend/src/engine/location-events.ts` as the authoritative projection seam for local recent happenings. Reuse committed event writers that already know concrete location context: player `log_event`, present-NPC event writes, off-screen NPC summaries, and faction/world events that target locations. Update `storeEpisodicEvent` callers so location metadata is concrete whenever runtime state knows it, then project a cached local-history row with `locationId`, `tick`, `eventType`, `summary`, `importance`, and optional `sourceEventId`. For ephemeral scene locations, use the explicit parent/anchor metadata from Plan 01 so archiving a scene node keeps its consequences readable from the anchored persistent place rather than erasing them. Do not invent a second gossip or rumor system.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/vectors/__tests__/episodic-events.test.ts src/engine/__tests__/tool-executor.test.ts</automated>
  </verify>
  <done>Authoritative runtime writers now feed a location-local recent-events projection, and expiring scene nodes no longer erase meaningful consequences.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface location-local recent happenings through prompt assembly and world payloads</name>
  <read_first>
@backend/src/engine/location-events.ts
@backend/src/engine/prompt-assembler.ts
@backend/src/routes/campaigns.ts
@backend/src/routes/__tests__/campaigns.test.ts
@backend/src/engine/__tests__/prompt-assembler.test.ts
  </read_first>
  <files>backend/src/engine/prompt-assembler.ts, backend/src/routes/campaigns.ts, backend/src/routes/__tests__/campaigns.test.ts, backend/src/engine/__tests__/prompt-assembler.test.ts</files>
  <acceptance_criteria>
    - `rg -n "recentHappenings|Recent happenings here|connectedPaths" backend/src/routes/campaigns.ts backend/src/engine/prompt-assembler.ts` finds shared readback of the Phase 43 location-history seam.
    - `npm --prefix backend test -- src/routes/__tests__/campaigns.test.ts src/engine/__tests__/prompt-assembler.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: `/api/campaigns/:id/world` returns per-location `recentHappenings` and normalized `connectedPaths` for player-visible inspection.
    - Test 2: prompt assembly surfaces recent happenings for the current location, including anchored archived-scene spillover when relevant.
    - Test 3: empty locations render an honest "nothing notable recorded" fallback instead of silently omitting the contract.
  </behavior>
  <action>Update backend readers to consume the same Phase 43 seam. In `routes/campaigns.ts`, expand each location payload with normalized path summaries and location-local recent happenings so the UI can read honest state without semantic-search calls. In `prompt-assembler.ts`, inject recent happenings for the current location into the scene contract so revisit narration can reflect concrete local history instead of only global chronicle text. Keep prompt text bounded to the last few meaningful events; do not dump every archived record or collapse back into world-chronicle-only summaries.</action>
  <verify>
    <automated>npm --prefix backend test -- src/routes/__tests__/campaigns.test.ts src/engine/__tests__/prompt-assembler.test.ts</automated>
  </verify>
  <done>Both prompt assembly and `/api/campaigns/:id/world` now read the same location-local recent-happenings state that authoritative writers maintain.</done>
</task>

</tasks>

<verification>
Run the episodic-events, tool-executor, campaigns-route, and prompt-assembler suites above. Confirm backend readers and writers now agree on location-local recent happenings and archived-scene consequence retention.
</verification>

<success_criteria>
Revisiting a place now has mechanically grounded local memory. The backend persists it, the prompt can narrate it, and the world API can expose it without relying on fuzzy retrieval or stale prose.
</success_criteria>

<output>
After completion, create `.planning/phases/43-travel-and-location-state-contract-resolution/43-03-SUMMARY.md`
</output>
