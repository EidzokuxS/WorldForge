---
phase: 43-travel-and-location-state-contract-resolution
plan: 03
type: execute
wave: 3
depends_on:
  - 43-01
  - 43-02
files_modified:
  - backend/src/engine/location-events.ts
  - backend/src/vectors/episodic-events.ts
  - backend/src/engine/tool-executor.ts
  - backend/src/engine/npc-tools.ts
  - backend/src/engine/npc-offscreen.ts
  - backend/src/engine/faction-tools.ts
  - backend/src/vectors/__tests__/episodic-events.test.ts
  - backend/src/engine/__tests__/tool-executor.test.ts
autonomous: true
requirements:
  - GSEM-04
must_haves:
  truths:
    - "Authoritative runtime events write concrete location-local recent happenings instead of leaving location history as prompt-only or vector-only inference per D-11 and D-12."
    - "Location-local recent happenings survive retry, undo, and checkpoint restore because they are persisted in campaign-backed state per D-14."
    - "Ephemeral scene nodes can archive without erasing their consequences from the anchored local-history projection per D-13."
  artifacts:
    - path: "backend/src/engine/location-events.ts"
      provides: "authoritative write/read seam for location-local recent happenings"
      contains: "recordLocationRecentEvent"
    - path: "backend/src/vectors/episodic-events.ts"
      provides: "location-aware episodic event write-through into local-history storage"
      contains: "sourceEventId"
    - path: "backend/src/engine/__tests__/tool-executor.test.ts"
      provides: "runtime-writer regression coverage for location-local projections"
      contains: "recordLocationRecentEvent"
  key_links:
    - from: "backend/src/vectors/episodic-events.ts"
      to: "backend/src/engine/location-events.ts"
      via: "authoritative event writes project into location-local history instead of vector retrieval being the only source"
      pattern: "sourceEventId|recordLocationRecentEvent"
    - from: "backend/src/engine/tool-executor.ts"
      to: "backend/src/engine/location-events.ts"
      via: "player-runtime event commits write location-local projection rows at the time authoritative events are recorded"
      pattern: "recordLocationRecentEvent|locationId"
---

<objective>
Implement authoritative write-through location recent happenings and ephemeral-scene consequence retention.

Purpose: satisfy the persistence half of GSEM-04 and the D-11 through D-14 decision set by making location-local history a real runtime projection instead of a doc-only promise.
Output: authoritative location-event write/read helpers, writer integration across runtime event sources, and scene-lifecycle preservation rules that later backend readers can consume.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-CONTEXT.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-RESEARCH.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-VALIDATION.md
@.planning/phases/41-checkpoint-complete-simulation-restore/41-02-SUMMARY.md
@backend/src/vectors/episodic-events.ts
@backend/src/engine/tool-executor.ts
@backend/src/engine/npc-tools.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/engine/faction-tools.ts

<interfaces>
From `backend/src/vectors/episodic-events.ts`:
```ts
export async function storeEpisodicEvent(
  campaignId: string,
  event: Omit<EpisodicEvent, "id" | "vector">
): Promise<string>
```

From `backend/src/routes/campaigns.ts`:
```ts
return c.json({
  locations: worldLocations,
  npcs: ...,
  factions: ...,
  relationships: ...,
  player: ...,
  items: worldItems,
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement write-through location recent-events projection and ephemeral-scene consequence retention</name>
  <read_first>
@.planning/phases/43-travel-and-location-state-contract-resolution/43-CONTEXT.md
@backend/src/vectors/episodic-events.ts
@backend/src/engine/tool-executor.ts
@backend/src/engine/npc-tools.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/engine/faction-tools.ts
@backend/src/vectors/__tests__/episodic-events.test.ts
@backend/src/engine/__tests__/tool-executor.test.ts
  </read_first>
  <files>backend/src/engine/location-events.ts, backend/src/vectors/episodic-events.ts, backend/src/engine/tool-executor.ts, backend/src/engine/npc-tools.ts, backend/src/engine/npc-offscreen.ts, backend/src/engine/faction-tools.ts, backend/src/vectors/__tests__/episodic-events.test.ts, backend/src/engine/__tests__/tool-executor.test.ts</files>
  <acceptance_criteria>
    - `rg -n "recordLocationRecentEvent|listRecentLocationEvents|sourceEventId|anchorLocationId|archivedAtTick" backend/src/engine/location-events.ts backend/src/vectors/episodic-events.ts backend/src/engine/tool-executor.ts backend/src/engine/npc-tools.ts backend/src/engine/npc-offscreen.ts backend/src/engine/faction-tools.ts` finds one write/read seam plus ephemeral-scene lifecycle handling.
    - `npm --prefix backend exec vitest run src/vectors/__tests__/episodic-events.test.ts src/engine/__tests__/tool-executor.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: authoritative gameplay and simulation writers attach concrete location context to committed events instead of storing empty location metadata.
    - Test 2: a location-facing projection row is written for recent happenings with traceability back to the source episodic event when one exists.
    - Test 3: archived ephemeral scene locations preserve their consequences through anchored recent-happenings reads instead of disappearing cleanly from history.
    - Test 4: the local-history seam stays in SQLite-backed campaign state, not vector search only.
  </behavior>
  <action>Create `backend/src/engine/location-events.ts` as the authoritative projection seam for local recent happenings. Reuse committed event writers that already know concrete location context: player `log_event`, present-NPC event writes, off-screen NPC summaries, and faction/world events that target locations. Update `storeEpisodicEvent` callers so location metadata is concrete whenever runtime state knows it, then project a cached local-history row with `locationId`, `tick`, `eventType`, `summary`, `importance`, and optional `sourceEventId`. For ephemeral scene locations, use the explicit parent/anchor metadata from Plan 01 so archiving a scene node keeps its consequences readable from the anchored persistent place rather than erasing them. Do not invent a second gossip or rumor system.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/vectors/__tests__/episodic-events.test.ts src/engine/__tests__/tool-executor.test.ts</automated>
  </verify>
  <done>Authoritative runtime writers now feed a location-local recent-events projection, and expiring scene nodes no longer erase meaningful consequences.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate all authoritative runtime writers with the location-history projection seam</name>
  <read_first>
@backend/src/engine/location-events.ts
@backend/src/engine/tool-executor.ts
@backend/src/engine/npc-tools.ts
@backend/src/engine/npc-offscreen.ts
@backend/src/engine/faction-tools.ts
@backend/src/engine/__tests__/tool-executor.test.ts
@backend/src/vectors/__tests__/episodic-events.test.ts
  </read_first>
  <files>backend/src/engine/tool-executor.ts, backend/src/engine/npc-tools.ts, backend/src/engine/npc-offscreen.ts, backend/src/engine/faction-tools.ts, backend/src/vectors/episodic-events.ts, backend/src/vectors/__tests__/episodic-events.test.ts, backend/src/engine/__tests__/tool-executor.test.ts</files>
  <acceptance_criteria>
    - `rg -n "recordLocationRecentEvent|sourceEventId|anchorLocationId|locationId" backend/src/engine/tool-executor.ts backend/src/engine/npc-tools.ts backend/src/engine/npc-offscreen.ts backend/src/engine/faction-tools.ts backend/src/vectors/episodic-events.ts` finds all intended writers attached to the shared projection seam.
    - `npm --prefix backend exec vitest run src/vectors/__tests__/episodic-events.test.ts src/engine/__tests__/tool-executor.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: player-facing event commits write location-local projection rows when concrete location context exists.
    - Test 2: present-NPC, off-screen NPC, and faction/world writers record location-local recent happenings through the same seam instead of parallel ad hoc storage.
    - Test 3: projection rows keep traceability to source episodic events where available and preserve anchor-parent semantics for archived scene spillover.
  </behavior>
  <action>Finish the writer-side integration for the Phase 43 seam. Ensure player `log_event`, NPC/world simulation writers, and any reusable episodic-event helpers all call into `location-events.ts` when authoritative location context is known, with anchored parent metadata preserved for archived ephemeral scenes per D-13. Keep this plan scoped to write-through persistence only; do not add prompt text, route payload shaping, or frontend-facing read transforms here.</action>
  <verify>
    <automated>npm --prefix backend exec vitest run src/vectors/__tests__/episodic-events.test.ts src/engine/__tests__/tool-executor.test.ts</automated>
  </verify>
  <done>All authoritative runtime writers now feed one persisted location-history projection that later backend readers can expose without inventing new storage paths.</done>
</task>

</tasks>

<verification>
Run the episodic-events and tool-executor suites above. Confirm authoritative runtime writers now persist location-local recent happenings and that archived scene-node consequences survive in the anchored projection.
</verification>

<success_criteria>
The backend now has a trustworthy write-through local-history projection: runtime events persist by location, archived scenes preserve their consequences, and later read surfaces can consume one authoritative seam instead of rediscovering history from vectors or prose.
</success_criteria>

<output>
After completion, create `.planning/phases/43-travel-and-location-state-contract-resolution/43-03-SUMMARY.md`
</output>
