---
phase: 43-travel-and-location-state-contract-resolution
plan: 06
type: execute
wave: 1
depends_on:
  - 43-05
files_modified:
  - frontend/app/game/page.tsx
  - frontend/app/game/__tests__/page.test.tsx
  - backend/src/engine/turn-processor.ts
  - backend/src/engine/__tests__/turn-processor.test.ts
  - backend/src/worldgen/__tests__/scaffold-saver.test.ts
autonomous: true
requirements:
  - GSEM-03
  - GSEM-04
gap_closure: true
must_haves:
  truths:
    - "The `/game` location panel no longer offers the player's current location as a travel option."
    - "If a current-location travel request still reaches gameplay transport through manual text, stale UI state, or a legacy payload, the backend resolves it as a clean no-op instead of emitting normal travel completion behavior."
    - "Regression coverage separates fresh-campaign path generation from legacy/manual no-op handling so future failures identify whether the breakage came from new runtime behavior or compatibility data."
  artifacts:
    - path: "frontend/app/game/page.tsx"
      provides: "current-location travel affordance filtering before `/game` renders path buttons"
      contains: "connectedPaths"
    - path: "backend/src/engine/turn-processor.ts"
      provides: "authoritative current-location travel no-op short-circuit"
      contains: "location_change"
    - path: "backend/src/worldgen/__tests__/scaffold-saver.test.ts"
      provides: "fresh-campaign guardrail against self-loop travel defaults"
      contains: "location_edges"
  key_links:
    - from: "frontend/app/game/page.tsx"
      to: "frontend/components/game/location-panel.tsx"
      via: "filtered connected-path props exclude the current location before rendering travel buttons"
      pattern: "connectedPaths"
    - from: "backend/src/engine/turn-processor.ts"
      to: "backend/src/engine/location-graph.ts"
      via: "current-location resolution short-circuits before emitting `location_change` or advancing travel ticks"
      pattern: "resolveTravelPath|location_change|travelCost"
    - from: "backend/src/worldgen/__tests__/scaffold-saver.test.ts"
      to: "frontend/app/game/__tests__/page.test.tsx"
      via: "fresh-campaign self-loop prevention and legacy compatibility filtering are covered by separate regressions"
      pattern: "connectedTo|connectedPaths|location_edges"
---

<objective>
Close the live Phase 43 smoke gap around current-location travel affordances and no-op travel semantics.

Purpose: keep the Phase 43 travel contract honest by removing self-travel affordances on `/game`, short-circuiting current-location travel requests cleanly in backend runtime, and separating fresh-campaign versus legacy/manual regression coverage so migration-specific breakage is easy to localize.
Output: one gap-closure patch plan covering `/game` travel-option filtering, backend no-op travel handling, and fresh-vs-legacy regression guardrails without reopening the broader revisit/archive UAT scope.
</objective>

<execution_context>
@C:/Users/robra/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-CONTEXT.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-HUMAN-UAT.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-VERIFICATION.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-05-PLAN.md
@.planning/phases/43-travel-and-location-state-contract-resolution/43-05-SUMMARY.md
@frontend/app/game/page.tsx
@frontend/app/game/__tests__/page.test.tsx
@backend/src/engine/turn-processor.ts
@backend/src/engine/location-graph.ts
@backend/src/engine/__tests__/turn-processor.test.ts
@backend/src/worldgen/__tests__/scaffold-saver.test.ts

<interfaces>
From `frontend/app/game/page.tsx`:
```ts
const connectedPaths = useMemo(() => {
  if (!worldData || !currentLocation) return [];
  if ((currentLocation.connectedPaths?.length ?? 0) > 0) {
    return currentLocation.connectedPaths!.map((path) => ({
      id: path.toLocationId,
      name: path.toLocationName ?? ...,
      travelCost: path.travelCost,
    }));
  }

  return currentLocation.connectedTo
    .map((id) => worldData.locations.find((location) => location.id === id))
    .filter(Boolean)
    .map((location) => ({ id: location.id, name: location.name, travelCost: null }));
}, [worldData, currentLocation]);
```

From `backend/src/engine/location-graph.ts`:
```ts
if (fromLocationId === toLocationId) {
  return {
    destinationId: toLocationId,
    locationIds: [fromLocationId],
    edgeIds: [],
    totalTravelCost: 0,
  };
}
```

From `backend/src/engine/turn-processor.ts`:
```ts
if (travelPath && destinationLocation) {
  persistPlayerLocation(db, player, destinationLocation.id, destinationLocation.name);
  successfulTravel = {
    locationId: destinationLocation.id,
    locationName: destinationLocation.name,
    travelCost: travelPath.totalTravelCost,
    tickAdvance: travelPath.totalTravelCost,
  };
  yield {
    type: "state_update",
    data: { type: "location_change", ...successfulTravel },
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filter current-location travel affordances on `/game` for both authoritative and legacy payload shapes</name>
  <read_first>
@.planning/phases/43-travel-and-location-state-contract-resolution/43-HUMAN-UAT.md
@frontend/app/game/page.tsx
@frontend/app/game/__tests__/page.test.tsx
  </read_first>
  <files>frontend/app/game/page.tsx, frontend/app/game/__tests__/page.test.tsx</files>
  <acceptance_criteria>
    - `npm --prefix frontend exec vitest run app/game/__tests__/page.test.tsx` passes.
    - The page test suite contains one explicit fresh-contract case for `connectedPaths` and one explicit legacy-compatibility case for `connectedTo`, both proving the current location is excluded from rendered move options.
    - `/game` keeps showing real reachable destinations and their travel cost/path metadata after filtering the self-target.
  </acceptance_criteria>
  <behavior>
    - Test 1: when authoritative `connectedPaths` includes the current location due a fresh-runtime regression, `/game` filters that self-target before rendering path buttons.
    - Test 2: when only legacy/stale `connectedTo` compatibility data is available and includes the current location, `/game` still filters the self-target before rendering path buttons.
    - Test 3: filtering does not strip legitimate reachable destinations or their travel metadata from the location panel.
  </behavior>
  <action>Update `/game` path derivation so the current location is removed from travel affordances before the location panel renders. Apply the filter to both authoritative `connectedPaths` and the legacy `connectedTo` fallback per the live smoke gap, because the goal is to keep `/game` safe whether the bad input comes from a fresh runtime regression or from migrated/stale compatibility data. Keep this change local to gameplay travel affordances: do not reopen location-history UI, revisit flows, or broader layout work. Extend `frontend/app/game/__tests__/page.test.tsx` with separate fresh-authoritative and legacy-compatibility regressions so a future failure reveals which data path regressed.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run app/game/__tests__/page.test.tsx</automated>
  </verify>
  <done>`/game` no longer offers the current location as a move target, and the tests identify whether a future self-travel regression came from authoritative `connectedPaths` or legacy fallback data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Short-circuit backend current-location travel and separate fresh-versus-legacy guardrails</name>
  <read_first>
@.planning/phases/43-travel-and-location-state-contract-resolution/43-HUMAN-UAT.md
@backend/src/engine/turn-processor.ts
@backend/src/engine/location-graph.ts
@backend/src/engine/__tests__/turn-processor.test.ts
@backend/src/worldgen/__tests__/scaffold-saver.test.ts
  </read_first>
  <files>backend/src/engine/turn-processor.ts, backend/src/engine/__tests__/turn-processor.test.ts, backend/src/worldgen/__tests__/scaffold-saver.test.ts</files>
  <acceptance_criteria>
    - `npm --prefix backend test -- src/engine/__tests__/turn-processor.test.ts src/worldgen/__tests__/scaffold-saver.test.ts` passes.
    - `turn-processor.test.ts` contains an explicit no-op current-location travel case proving no `location_change` update, no travel tick advance, and no duplicate arrival-style narration path.
    - `scaffold-saver.test.ts` contains an explicit fresh-campaign guardrail proving default scaffold persistence does not create self-loop travel edges or self-target compatibility projections.
  </acceptance_criteria>
  <behavior>
    - Test 1: if manual text or stale UI state asks to travel to the player's current location, gameplay transport resolves it as a clean no-op instead of a normal travel completion.
    - Test 2: the no-op path does not emit `location_change`, does not call `advanceCampaignTick`, and does not route through duplicate arrival/you-are-already-here narration.
    - Test 3: fresh scaffold persistence keeps default travel edges free of self-loops so fresh-campaign runtime regressions are distinguishable from compatibility or migration issues.
  </behavior>
  <action>Adjust the player-movement seam in `backend/src/engine/turn-processor.ts` so a destination that resolves to the player's current location short-circuits cleanly before normal travel completion handling. Preserve the existing graph resolver for real movement, but treat `from === to` as a bounded no-op at the player transport layer: do not emit `location_change`, do not advance travel time, and do not let the request fall through as if the player meaningfully arrived somewhere new. Keep the response path intentionally small and deterministic so legacy/stale payloads or direct manual text cannot create duplicate arrival narration. Then extend regression coverage in `turn-processor.test.ts` for the legacy/manual no-op path and in `scaffold-saver.test.ts` for the fresh-campaign path, so future failures clearly indicate whether the bug came from fresh graph generation or compatibility data reaching transport.</action>
  <verify>
    <automated>npm --prefix backend test -- src/engine/__tests__/turn-processor.test.ts src/worldgen/__tests__/scaffold-saver.test.ts</automated>
  </verify>
  <done>Backend transport now treats current-location travel as a clean no-op, and the regression suite separates fresh-campaign self-loop prevention from legacy/manual no-op handling.</done>
</task>

</tasks>

<verification>
Run the frontend page suite plus the targeted backend turn-processor and scaffold-saver suites. Confirm the current location is absent from `/game` travel options, backend no-op travel does not emit normal arrival state, and the tests clearly distinguish fresh-campaign versus legacy/manual regression paths.
</verification>

<success_criteria>
Phase 43's live smoke gap is closed without reopening broader playtest scope: `/game` does not offer travel to the current location, backend transport short-circuits no-op travel cleanly if it still arrives, and the regression suite makes fresh-runtime versus compatibility/migration failures easy to diagnose.
</success_criteria>

<output>
After completion, create `.planning/phases/43-travel-and-location-state-contract-resolution/43-06-SUMMARY.md`
</output>
