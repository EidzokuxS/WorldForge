---
phase: 21-memory-persistence-and-world-context-e2e
plan: 03
type: execute
wave: 1
depends_on: ["21-01", "21-02"]
files_modified: ["e2e/21-03-episodic-gap-closure.ts", "e2e/21-03-results.json"]
autonomous: true
gap_closure: true
requirements: ["MEMORY-API-EPISODIC"]

must_haves:
  truths:
    - "LanceDB episodic_events table has at least 1 row with non-empty vector after gameplay turns"
    - "searchEpisodicEvents returns semantically relevant results when queried"
  artifacts:
    - path: "e2e/21-03-episodic-gap-closure.ts"
      provides: "Gap closure test for episodic event storage and retrieval"
    - path: "e2e/21-03-results.json"
      provides: "Test results proving episodic events exist and are searchable"
  key_links:
    - from: "e2e/21-03-episodic-gap-closure.ts"
      to: "POST /api/chat"
      via: "SSE turn submission with log_event-triggering prompt"
      pattern: "fetch.*api/chat"
---

<objective>
Close Gaps 1 and 2 from 21-VERIFICATION.md: verify episodic event storage and retrieval end-to-end.

Purpose: The original tests ran 3 turns with 0 log_event tool calls and accepted this as PASS. This gap closure test must:
1. First check if episodic events already exist from prior gameplay (120+ messages in campaign)
2. If not, play turns with prompts strongly requesting event logging, with 60s delays between turns
3. Verify episodic_events LanceDB table has rows with embedded vectors
4. Verify semantic search returns relevant results

Output: e2e/21-03-episodic-gap-closure.ts + e2e/21-03-results.json
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-memory-persistence-and-world-context-e2e/21-VERIFICATION.md
@.planning/phases/21-memory-persistence-and-world-context-e2e/21-01-SUMMARY.md
@e2e/21-01-memory-api-tests.ts
@backend/src/vectors/episodic-events.ts
@backend/src/routes/chat.ts
@backend/src/routes/lore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Episodic event storage and retrieval gap closure test</name>
  <files>e2e/21-03-episodic-gap-closure.ts, e2e/21-03-results.json</files>
  <read_first>
    - e2e/21-01-memory-api-tests.ts (SSE parser pattern, campaign ID, turn submission pattern)
    - backend/src/vectors/episodic-events.ts (storeEpisodicEvent, searchEpisodicEvents, TABLE_NAME="episodic_events", EpisodicEvent interface)
    - backend/src/routes/chat.ts (embedAndUpdateEvent post-turn callback at line 112)
    - backend/src/routes/lore.ts (lore search endpoint pattern for reference)
  </read_first>
  <action>
    Create e2e/21-03-episodic-gap-closure.ts that closes Gaps 1 and 2 from VERIFICATION.md.

    The test has 3 areas:

    **Area 1: Check existing episodic events**
    - There is no REST API for episodic events. Instead, use an indirect approach:
    - Play 1 turn with a prompt that STRONGLY triggers log_event: "I perform a dramatic ritual sacrifice of the ancient artifact, shattering it into pieces and releasing dark magic into the air. This is a pivotal, world-changing moment that must be remembered." (prompts with "pivotal", "world-changing", "must be remembered" increase log_event probability)
    - Wait 60 seconds for GLM rate limit recovery
    - Parse SSE events and count log_event tool calls specifically (look for event type "state_updates" containing log_event tool results, OR search the events array for any entry where data contains "log_event")
    - CRITICAL: Do NOT accept 0 log_event calls as passing. If turn 1 produces 0 log_event calls, play turn 2 (after 60s delay) with an even more dramatic prompt: "I write in my journal: Record this moment - the artifact is destroyed, dark energy released, the temple is crumbling. This event changes everything."
    - If BOTH turns produce 0 log_event calls, the area FAILS (do not soften criteria)
    - If at least 1 log_event call is observed, area PASSES

    **Area 2: Verify episodic event embedding completed**
    - After the turn(s) from Area 1, wait EMBED_WAIT_MS (15000ms) for async embedding to complete
    - Play another turn with a semantically related prompt: "I recall what happened with the artifact -- what dark magic was released?"
    - This turn should trigger the prompt assembler to call buildEpisodicMemorySection, which internally calls searchEpisodicEvents
    - If the narrative references the prior event (artifact, dark magic, ritual), that proves episodic retrieval worked
    - Parse the narrative text and check for keywords: "artifact" OR "dark" OR "ritual" OR "magic" OR "shatter" OR "destroy" (case-insensitive)
    - If narrative contains at least 1 keyword match, area PASSES (proves episodic memory was retrieved and influenced narration)
    - If narrative is 0 chars (rate limit), wait 60s and retry once
    - CRITICAL: 0-char narrative on both attempts = FAIL (not "provider limitation")

    **Area 3: Direct episodic event count verification**
    - This is a backup verification: use the lore search endpoint (GET /api/campaigns/:id/lore/search?q=artifact+ritual&limit=5) to verify the lore system is responsive
    - Then play a final turn asking "What significant events have occurred recently?" and verify narrative length > 0
    - Area passes if narrative > 100 chars (proving prompt assembly with episodic context works)

    Constants:
    - BASE = "http://localhost:3001"
    - CAMPAIGN_ID = "b85729f8-0de4-4d93-a0c3-e1c45646219c" (same E2E Dark Fantasy campaign)
    - TURN_DELAY_MS = 60_000 (60s between turns for GLM rate limits)
    - EMBED_WAIT_MS = 15_000 (15s for async embedding)

    Reuse SSE parser from 21-01-memory-api-tests.ts (parseSSEStream, submitAction patterns).

    Acceptance criteria enforcement:
    - Area 1: At least 1 log_event call across all turns. 0 = FAIL.
    - Area 2: Narrative with keyword match proving episodic retrieval. 0-char = FAIL.
    - Area 3: Non-empty narrative (>100ch) proving prompt assembly works. 0-char = FAIL.
    - Overall: 2/3 areas must pass for PASS (threshold 2). Quality = passedCount/3 * 5.0.
    - NEVER soften these criteria. Rate limit = retry after 60s, not excuse.

    Write results to e2e/21-03-results.json with structure:
    ```json
    {
      "phase": "21",
      "plan": "03",
      "gap_closure": true,
      "timestamp": "<ISO>",
      "areas": [
        { "area": "Episodic Event Storage (log_event calls)", "passed": bool, "details": "..." },
        { "area": "Episodic Event Retrieval (semantic influence)", "passed": bool, "details": "..." },
        { "area": "Prompt Assembly with Episodic Context", "passed": bool, "details": "..." }
      ],
      "passedCount": N,
      "totalAreas": 3,
      "qualityScore": N,
      "threshold": 2,
      "overall": "PASS|FAIL"
    }
    ```
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npx tsx e2e/21-03-episodic-gap-closure.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - e2e/21-03-results.json exists with gap_closure: true
    - At least 1 log_event tool call observed (Area 1 passed)
    - Narrative contains episodic memory keyword (Area 2 passed) OR narrative > 100ch (Area 3 passed)
    - overall is "PASS" (at least 2/3 areas)
    - No area accepted 0 as passing
  </acceptance_criteria>
  <done>Episodic event storage verified with at least 1 log_event call, retrieval verified via narrative influence or prompt assembly confirmation</done>
</task>

<task type="auto">
  <name>Task 2: Analyze results and produce SUMMARY</name>
  <files>.planning/phases/21-memory-persistence-and-world-context-e2e/21-03-SUMMARY.md</files>
  <read_first>
    - e2e/21-03-results.json (test results)
    - .planning/phases/21-memory-persistence-and-world-context-e2e/21-VERIFICATION.md (gaps being closed)
  </read_first>
  <action>
    Read e2e/21-03-results.json and create 21-03-SUMMARY.md documenting:
    - Which gaps were closed (Gap 1: episodic storage, Gap 2: episodic retrieval)
    - Exact log_event call count observed
    - Whether episodic retrieval influenced narrative
    - Any rate limit issues and how they were handled
    - Follow SUMMARY template from prior plans (21-01-SUMMARY.md pattern)
    - Include gap_closure: true in frontmatter
  </action>
  <verify>
    <automated>test -f "R:/Projects/WorldForge/.planning/phases/21-memory-persistence-and-world-context-e2e/21-03-SUMMARY.md" && echo "SUMMARY exists"</automated>
  </verify>
  <acceptance_criteria>
    - 21-03-SUMMARY.md exists with gap_closure: true in frontmatter
    - Documents specific log_event count (not "0 accepted")
    - Documents episodic retrieval evidence
  </acceptance_criteria>
  <done>SUMMARY documents gap closure results with concrete evidence of episodic event storage and retrieval</done>
</task>

</tasks>

<verification>
- e2e/21-03-results.json shows at least 2/3 areas passed
- No area scored PASS with 0 evidence
- Gap 1 (episodic storage) has concrete log_event count > 0
- Gap 2 (episodic retrieval) has narrative keyword match or non-empty narrative
</verification>

<success_criteria>
- At least 1 log_event tool call confirmed across all turns
- Episodic retrieval demonstrated via narrative content referencing prior events
- Results file has gap_closure: true flag
- No criteria softening (0 is never acceptable)
</success_criteria>

<output>
After completion, create `.planning/phases/21-memory-persistence-and-world-context-e2e/21-03-SUMMARY.md`
</output>
