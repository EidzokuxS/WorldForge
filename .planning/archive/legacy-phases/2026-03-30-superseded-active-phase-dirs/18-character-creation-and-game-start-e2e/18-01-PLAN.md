---
phase: 18-character-creation-and-game-start-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [CHAR-API-PARSE, CHAR-API-GENERATE, CHAR-API-IMPORT, CHAR-API-SAVE, CHAR-API-LOCATION]

must_haves:
  truths:
    - "parse-character returns valid ParsedCharacter with name, race, gender, age, appearance, tags from free-text description"
    - "generate-character returns valid ParsedCharacter from archetype/concept prompt"
    - "import-v2-card returns valid ParsedCharacter from SillyTavern V2/V3 card JSON"
    - "save-character writes player record to DB and returns campaign ID"
    - "resolve-starting-location returns an isStarting location from the campaign scaffold"
  artifacts:
    - path: "e2e/18-01-char-api-tests.ts"
      provides: "API-level verification script for all character endpoints"
  key_links:
    - from: "POST /api/worldgen/parse-character"
      to: "backend/src/character/generator.ts"
      via: "parseCharacterDescription()"
      pattern: "generateObject.*ParsedCharacter"
    - from: "POST /api/worldgen/generate-character"
      to: "backend/src/character/generator.ts"
      via: "generateCharacter()"
      pattern: "generateObject.*ParsedCharacter"
    - from: "POST /api/worldgen/save-character"
      to: "backend/src/db/schema.ts"
      via: "insert into players table"
      pattern: "db\\.insert.*players"
---

<objective>
Verify all 5 character creation and game start API endpoints work with real GLM 4.7 Flash calls. Test parse-character (free-text), generate-character (archetype), import-v2-card (SillyTavern card), save-character (DB persist), and resolve-starting-location (isStarting location).

Purpose: Confirm backend character pipeline is solid before browser E2E in Plan 02. Phase 17 established the pattern of API-first verification to avoid long browser timeout issues.
Output: Verified API responses + test script artifacts.
</objective>

<execution_context>
@C:/Users/robra/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-world-generation-pipeline-e2e/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify parse-character and generate-character with GLM</name>
  <files>e2e/18-01-char-api-tests.ts</files>
  <read_first>
    - backend/src/character/generator.ts (parseCharacterDescription, generateCharacter functions)
    - backend/src/routes/worldgen.ts (parse-character, generate-character route handlers)
    - shared/src/types.ts (ParsedCharacter type definition)
  </read_first>
  <action>
Using an existing campaign with a generated world (from Phase 17 tests or create fresh via API), test the character creation API endpoints:

1. **POST /api/worldgen/parse-character** with `role: "player"` and a free-text character description (e.g., "A grizzled dwarven blacksmith named Thorgrim who lost his family to a dragon attack. He carries a war hammer and wears burn-scarred leather armor. Strong but slow, with a talent for metalworking."). Verify response contains:
   - name (non-empty string)
   - race, gender, age, appearance fields populated
   - tags array with relevant trait tags
   - No empty required fields

2. **POST /api/worldgen/generate-character** with `role: "player"` and a concept/archetype prompt (e.g., `{ concept: "roguish elven bard", campaignId: "{id}" }`). Verify response contains the same ParsedCharacter structure with thematically appropriate values.

3. **POST /api/worldgen/research-character** with `role: "player"` and a known archetype (e.g., `{ concept: "Witcher monster hunter", campaignId: "{id}" }`). Verify the research step enriches the character with lore-grounded details.

Quality check: Each generated character must have at least 3 tags, non-empty name/race/appearance, and thematically consistent content. Score each >= 4.5/5.

Use GLM provider (already configured in settings). Ensure backend is running on :3001.
  </action>
  <verify>
    <automated>curl -s -X POST http://localhost:3001/api/worldgen/parse-character -H "Content-Type: application/json" -d '{"role":"player","description":"A test warrior named Kael"}' | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log('name:', d.name, 'tags:', d.tags?.length); process.exit(d.name ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - parse-character returns ParsedCharacter with non-empty name, race, appearance, and >= 3 tags
    - generate-character returns ParsedCharacter with thematically matching content for given concept
    - research-character enriches character with lore details
    - All responses have valid JSON structure matching ParsedCharacter schema
    - Quality >= 4.5/5 for each generated character
  </acceptance_criteria>
  <done>All 3 character creation endpoints return valid, quality ParsedCharacter objects with GLM 4.7 Flash</done>
</task>

<task type="auto">
  <name>Task 2: Verify save-character and resolve-starting-location with GLM</name>
  <files>e2e/18-01-char-api-tests.ts</files>
  <read_first>
    - backend/src/routes/worldgen.ts (save-character, resolve-starting-location route handlers)
    - backend/src/db/schema.ts (players table schema)
    - backend/src/campaign/manager.ts (campaign loading, player retrieval)
  </read_first>
  <action>
Using a character from Task 1's parse-character or generate-character result:

1. **POST /api/worldgen/save-character** with the ParsedCharacter object + campaignId. Verify:
   - Response returns success with campaign ID
   - Player record exists in DB (verify via GET /api/campaigns/:id/world or similar endpoint that returns player data)
   - Player has correct name, race, tags from the submitted character

2. **POST /api/worldgen/resolve-starting-location** with the campaign ID. Verify:
   - Response returns a location object with isStarting=true (or the resolved starting location)
   - The location is one from the campaign's scaffold
   - Player is placed at this location in the DB

3. **Game readiness check**: After save + location resolution, verify:
   - GET /api/campaigns/active returns the campaign with player data
   - GET /api/chat/history returns chat history ready for first turn (may include premise)
   - The campaign is in a state where POST /api/chat would work (don't actually send a turn)

Quality check: save-character -> resolve-starting-location -> game ready is the critical path. Each step must complete without errors.
  </action>
  <verify>
    <automated>curl -s http://localhost:3001/api/campaigns/active | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); const p=d.player||d.data?.player; console.log('player:', p?.name); process.exit(p?.name ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - save-character writes player to DB with correct name, race, tags
    - resolve-starting-location returns an isStarting location from the scaffold
    - Campaign is ready for gameplay after save + location resolution
    - GET /api/campaigns/active shows player data
    - GET /api/chat/history returns valid response
  </acceptance_criteria>
  <done>Character save -> starting location -> game readiness pipeline verified end-to-end via API with GLM</done>
</task>

</tasks>

<verification>
All 5 character API endpoints (parse-character, generate-character, research-character, save-character, resolve-starting-location) return valid responses with real GLM calls. The full pipeline from character creation to game readiness works without errors.
</verification>

<success_criteria>
- All 5 endpoints return valid, non-error responses
- ParsedCharacter objects have name, race, appearance, tags (>= 3)
- Character saved to DB with correct fields
- Starting location resolved from scaffold
- Campaign in game-ready state after full pipeline
- Quality >= 4.5/5 for generated characters
</success_criteria>

<output>
After completion, create `.planning/phases/18-character-creation-and-game-start-e2e/18-01-SUMMARY.md`
</output>
