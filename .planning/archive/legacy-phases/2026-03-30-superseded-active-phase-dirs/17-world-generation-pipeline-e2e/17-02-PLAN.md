---
phase: 17-world-generation-pipeline-e2e
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified: []
autonomous: true
requirements:
  - WGEN-BROWSER-FLOW
  - WGEN-WORLD-REVIEW
  - WGEN-ORIGINAL-WORLD

must_haves:
  truths:
    - "User can create a new campaign from title screen with a concept premise"
    - "World DNA panel displays seed categories, allows toggling and editing"
    - "AI-suggested seeds populate the DNA panel with thematic values"
    - "Scaffold generation shows SSE progress in the UI overlay"
    - "World review page displays all 5 sections: premise, locations, factions, NPCs, lore"
    - "User can edit scaffold content in the world review page"
    - "Original world (non-IP) path works without IP research step"
    - "Known IP path triggers research and enriches generation"
  artifacts: []
  key_links:
    - from: "TitleScreen"
      to: "WorldDnaPanel"
      via: "campaign creation flow"
      pattern: "WorldDnaPanel"
    - from: "WorldDnaPanel"
      to: "/api/worldgen/generate"
      via: "generate button click"
      pattern: "worldgen/generate"
    - from: "scaffold generation"
      to: "world-review page"
      via: "redirect after scaffold save"
      pattern: "campaign/.*?/review"
    - from: "world-review"
      to: "/api/worldgen/save-edits"
      via: "save button"
      pattern: "save-edits"
---

<objective>
Verify the complete world generation browser flow end-to-end using Playwright MCP -- from title screen through campaign creation, World DNA, scaffold generation with SSE progress, to the world review page with all 5 sections. Test both known IP path (Witcher) and original world path.

Purpose: Confirm the entire user-facing world generation experience works correctly in the browser with real LLM calls.
Output: QA verification confirming both IP and original world browser flows produce quality content and all UI interactions work.
</objective>

<execution_context>
@C:/Users/robra/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/robra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-world-generation-pipeline-e2e/17-CONTEXT.md
@.planning/phases/17-world-generation-pipeline-e2e/17-01-SUMMARY.md

@frontend/app/title/page.tsx
@frontend/components/title/TitleScreen.tsx
@frontend/components/title/WorldDnaPanel.tsx
@frontend/app/campaign/[id]/review/page.tsx
@frontend/components/world-review/PremiseSection.tsx
@frontend/components/world-review/LocationsSection.tsx
@frontend/components/world-review/FactionsSection.tsx
@frontend/components/world-review/NpcsSection.tsx
@frontend/components/world-review/LoreSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Browser E2E -- Known IP campaign (Witcher) through world review</name>
  <read_first>
    frontend/components/title/TitleScreen.tsx
    frontend/components/title/WorldDnaPanel.tsx
    frontend/app/campaign/[id]/review/page.tsx
  </read_first>
  <files></files>
  <action>
Ensure both backend (:3001) and frontend (:3000) are running. Use Playwright MCP to drive the full browser flow:

1. **Navigate to title screen** at `http://localhost:3000`. Verify the title screen renders with "New Campaign" button.

2. **Start new campaign**: Click "New Campaign" button. Enter concept: "A dark fantasy world inspired by The Witcher, where monster hunters roam a war-torn continent hunting supernatural creatures for coin". Submit the concept.

3. **World DNA panel**: After concept submission, the World DNA panel should appear. Verify:
   - All 6 seed categories are visible (era, geographicScale, toneStyle, magicTechLevel, culturalFlavor, wildCard)
   - "Suggest Seeds" button is visible and clickable
   - Click "Suggest Seeds" -- wait for AI to generate thematic seeds
   - Verify seeds populated with Witcher-appropriate values (dark fantasy era, medieval-ish scale, etc.)
   - Toggle at least one category off and verify it becomes disabled
   - Edit one seed value manually and verify it updates
   - Toggle the category back on

4. **Generate world**: Click the "Generate World" button. Verify:
   - Progress overlay appears showing SSE pipeline steps
   - Pipeline progresses through: premise, locations, factions, NPCs, lore
   - Each step shows progress indicator or status text
   - Generation completes (may take 30-90 seconds with GLM)

5. **World review page**: After generation completes, page should redirect to world review. Verify all 5 sections are visible:
   - **Premise section**: Shows the generated world premise text
   - **Locations section**: Shows >= 3 locations with names, descriptions, tags
   - **Factions section**: Shows >= 2 factions with names, descriptions, goals
   - **NPCs section**: Shows >= 3 NPCs with names, personas, tags, goals
   - **Lore section**: Shows lore cards grouped by category

6. **Quality assessment**: Score the generated world content:
   - Thematic consistency with Witcher IP (dark fantasy, monster hunters, medieval)
   - Variety and depth of locations, factions, NPCs
   - IP research enrichment visible in content (canon-appropriate names, concepts)
   - Overall quality must be >= 4.5/5

7. **Cleanup**: Delete the test campaign via API after verification.

Take screenshots at key points: title screen, World DNA panel with seeds, generation progress, world review page.
  </action>
  <verify>
    <automated>curl -s http://localhost:3000 -o /dev/null -w "%{http_code}" | grep -q "200" && echo "PASS: Frontend accessible" || echo "FAIL: Frontend not accessible"</automated>
  </verify>
  <acceptance_criteria>
    - Title screen renders with "New Campaign" button
    - World DNA panel shows all 6 seed categories after concept input
    - Suggest Seeds populates thematic values via GLM
    - Seed toggling and manual editing work in the UI
    - Scaffold generation progress overlay shows pipeline steps
    - World review page shows all 5 sections with populated data
    - Generated content is thematically consistent with Witcher IP
    - Quality score >= 4.5/5
  </acceptance_criteria>
  <done>Known IP (Witcher) campaign created through browser with world generation, DNA panel, and world review all functioning correctly with quality >= 4.5/5</done>
</task>

<task type="auto">
  <name>Task 2: Browser E2E -- Original world campaign (non-IP) through world review</name>
  <read_first>
    frontend/components/title/TitleScreen.tsx
    frontend/components/title/WorldDnaPanel.tsx
    frontend/components/world-review/helpers.tsx
  </read_first>
  <files></files>
  <action>
Use Playwright MCP to test the original world (non-IP) path:

1. **Navigate to title screen** at `http://localhost:3000`. Click "New Campaign".

2. **Enter original concept**: Use a completely original premise with no known IP: "A post-apocalyptic underwater civilization where bio-luminescent coral cities house the last remnants of humanity, governed by the Tide Council and threatened by deep-sea leviathans". Submit.

3. **World DNA panel**: Verify seeds panel appears. This time:
   - Click "Randomize" (roll all seeds) -- verify random seeds populate all 6 categories
   - Note that IP research should NOT trigger for this original premise
   - Click "Suggest Seeds" to get AI-themed seeds for this unique concept
   - Verify suggested seeds match the underwater post-apocalyptic theme (not generic fantasy)
   - Leave seeds as-is (no manual edits this time)

4. **Generate world**: Click "Generate World". Verify:
   - Progress overlay shows pipeline steps
   - IP research step should be skipped or very brief (no known IP to research)
   - Pipeline completes through all steps
   - Generation completes successfully

5. **World review page**: Verify all sections:
   - **Premise**: Reflects the underwater post-apocalyptic theme
   - **Locations**: >= 3 locations appropriate to the setting (coral cities, deep trenches, etc.)
   - **Factions**: >= 2 factions (Tide Council + at least one other)
   - **NPCs**: >= 3 NPCs with personas fitting the setting
   - **Lore**: Lore cards with world-specific concepts

6. **Test editing in world review**:
   - Edit a location name in the locations section
   - Edit a faction description
   - Save edits and verify they persist (section should update without full page reload)
   - Use "Regenerate" on the locations section and verify only locations are regenerated

7. **Quality assessment**:
   - Originality and internal consistency of the generated world
   - Seeds correctly influence the generation (underwater + post-apocalyptic themes)
   - No bleed-through from generic fantasy tropes
   - Quality >= 4.5/5

8. **Cleanup**: Delete the test campaign via API.

Take screenshots at key points: DNA panel with random seeds, DNA panel with suggested seeds, generation progress, world review with all sections.
  </action>
  <verify>
    <automated>curl -s http://localhost:3001/api/campaigns | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log(Array.isArray(d)?'PASS: Campaigns API works':'FAIL: Campaigns API broken')"</automated>
  </verify>
  <acceptance_criteria>
    - Original (non-IP) premise accepted and processed
    - Random seed rolling works via Randomize button
    - AI seed suggestion generates theme-appropriate values for original concept
    - IP research does not trigger for non-IP premise (or completes quickly with no enrichment)
    - Scaffold generation completes all pipeline steps
    - World review shows all 5 sections with thematically consistent content
    - Editing locations/factions in world review works and persists
    - Regenerate-section replaces only the targeted section
    - Quality score >= 4.5/5 for original world content
  </acceptance_criteria>
  <done>Original world (non-IP) campaign created through browser with full world gen pipeline, world review editing and regeneration verified, quality >= 4.5/5</done>
</task>

</tasks>

<verification>
- Both known IP and original world paths complete successfully through the browser
- World DNA panel works: toggle, edit, randomize, suggest
- Scaffold generation SSE progress visible in UI
- World review page displays all 5 sections with populated data
- Editing and regeneration work in world review
- Generated content quality >= 4.5/5 for both scenarios
</verification>

<success_criteria>
- Known IP (Witcher) browser flow: title -> DNA -> generate -> review works end-to-end
- Original world browser flow: title -> DNA -> generate -> review works end-to-end
- World DNA interactions (toggle, edit, randomize, suggest) all function
- World review editing and section regeneration work
- All generated content quality >= 4.5/5
</success_criteria>

<output>
After completion, create `.planning/phases/17-world-generation-pipeline-e2e/17-02-SUMMARY.md`
</output>
