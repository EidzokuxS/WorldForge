---
phase: quick
plan: 260404-e0m
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/worldbook-library/composition.ts
  - backend/src/worldgen/worldbook-importer.ts
  - backend/src/worldgen/scaffold-steps/prompt-utils.ts
  - backend/src/worldgen/__tests__/worldbook-composition.test.ts
  - shared/src/types.ts
autonomous: true
requirements: [QUICK-260404-e0m]

must_haves:
  truths:
    - "Multi-worldbook composition produces source-tagged keyFacts grouped by source priority"
    - "Supplementary sources are capped at ~15 entries to prevent context flooding"
    - "buildIpContextBlock renders PRIMARY source facts before SUPPLEMENTARY facts with distinct priority headers"
    - "Single-worldbook campaigns work exactly as before (backward compatible)"
  artifacts:
    - path: "shared/src/types.ts"
      provides: "IpResearchContext.sourceGroups optional field"
    - path: "backend/src/worldbook-library/composition.ts"
      provides: "Per-source entry tracking with source tagging"
    - path: "backend/src/worldgen/scaffold-steps/prompt-utils.ts"
      provides: "Source-grouped prompt rendering with priority headers"
    - path: "backend/src/worldgen/__tests__/worldbook-composition.test.ts"
      provides: "Tests for source-grouped composition and entry capping"
  key_links:
    - from: "backend/src/worldbook-library/composition.ts"
      to: "shared/src/types.ts"
      via: "IpResearchContext.sourceGroups"
      pattern: "sourceGroups"
    - from: "backend/src/worldgen/scaffold-steps/prompt-utils.ts"
      to: "shared/src/types.ts"
      via: "reads sourceGroups to render grouped prompt"
      pattern: "sourceGroups"
---

<objective>
Fix lorebook over-influence in worldgen when multiple worldbooks are selected. Currently a 67-entry SCP worldbook overwhelms a 15-entry VotV worldbook despite the premise saying "mainly VotV, small SCP elements", because all keyFacts are dumped flat with equal weight.

Purpose: Premise intent should drive source priority — primary source gets full representation, supplementary sources get capped entries with lower-priority framing.
Output: Source-grouped IpResearchContext composition + priority-aware prompt rendering in buildIpContextBlock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260404-e0m-fix-lorebook-over-influence-in-worldgen-/260404-e0m-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From shared/src/types.ts:
```typescript
export interface IpResearchContext {
  franchise: string;
  keyFacts: string[];
  tonalNotes: string[];
  canonicalNames?: {
    locations?: string[];
    factions?: string[];
    characters?: string[];
  };
  excludedCharacters?: string[];
  source: "mcp" | "llm";
}
```

From backend/src/worldbook-library/composition.ts:
```typescript
export interface ComposeWorldbookSelectionResult {
  ipContext: IpResearchContext;
  worldbookSelection: CampaignWorldbookSelection[];
  provenance: {
    sources: CampaignWorldbookSelection[];
    groups: WorldbookCompositionGroup[];
  };
}

export function composeWorldbookLibraryRecords(
  records: StoredWorldbookLibraryRecord[],
): ComposeWorldbookSelectionResult;
```

From backend/src/worldgen/scaffold-steps/prompt-utils.ts:
```typescript
export function buildIpContextBlock(ipContext: IpResearchContext | null): string;
```

From backend/src/worldgen/worldbook-importer.ts:
```typescript
export function worldbookToIpContext(
  entries: ClassifiedEntry[],
  worldbookName: string,
): IpResearchContext;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Source-tagged composition and capped supplementary entries</name>
  <files>
    shared/src/types.ts,
    backend/src/worldbook-library/composition.ts,
    backend/src/worldgen/worldbook-importer.ts,
    backend/src/worldgen/__tests__/worldbook-composition.test.ts
  </files>
  <behavior>
    - Test: Single-worldbook composition returns sourceGroups with one group marked "primary", keyFacts unchanged (backward compat)
    - Test: Two-worldbook composition with premise "mainly Alpha" tags Alpha as primary, Beta as supplementary
    - Test: Supplementary source with 60+ entries is capped to 15 entries in sourceGroups
    - Test: Without premise hint, all sources default to "primary" (no regression)
    - Test: canonicalNames are merged across sources (existing behavior preserved)
  </behavior>
  <action>
1. **Extend IpResearchContext** in `shared/src/types.ts`: Add an optional `sourceGroups` field:
   ```typescript
   sourceGroups?: Array<{
     sourceName: string;
     priority: "primary" | "supplementary";
     keyFacts: string[];
     canonicalNames?: {
       locations?: string[];
       factions?: string[];
       characters?: string[];
     };
   }>;
   ```
   The existing flat `keyFacts`, `tonalNotes`, and `canonicalNames` fields remain as-is for backward compatibility. `sourceGroups` is additive metadata used only by multi-source prompt rendering.

2. **Update `composeWorldbookLibraryRecords()`** in `backend/src/worldbook-library/composition.ts`:
   - Accept an optional `premise?: string` parameter (second argument).
   - After building `groups`, instead of merging all entries into one flat `worldbookToIpContext()` call, build per-source entry lists from `groups[].contributions` (group by `sourceDisplayName`).
   - Determine priority: parse premise for signals like "mainly X", "primarily X", "based on X", "in the world of X" (case-insensitive). If a source displayName (or substring) matches the primary signal, mark it `"primary"`. All others are `"supplementary"`. If no signal found or single source, all are `"primary"`.
   - Cap supplementary source entries at 15. When capping, prefer entries whose name or summary contains words from the premise (simple word overlap scoring). If no premise overlap, take the first 15 (already sorted).
   - Call `worldbookToIpContext()` on the full merged entries as before (flat keyFacts stay for backward compat), then attach `sourceGroups` to the returned `ipContext`.

3. **Update `composeSelectedWorldbooks()`** to accept and pass through the optional `premise` parameter.

4. **Update callers in `backend/src/routes/worldgen.ts`**: Pass `premise` string to `composeSelectedWorldbooks()` where available (it's already in `result.data.premise` and in `config.premise`). This is a simple argument addition at the two call sites.

5. **Add a new helper** `worldbookToIpContextForSource()` in `worldbook-importer.ts` that returns per-source keyFacts + canonicalNames without wrapping in a full IpResearchContext. Or just extract the mapping logic from `worldbookToIpContext()` into a shared internal helper.

6. **Write tests** in `backend/src/worldgen/__tests__/worldbook-composition.test.ts` covering the behaviors listed above.
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npx --prefix backend vitest run backend/src/worldgen/__tests__/worldbook-composition.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - IpResearchContext has optional sourceGroups field
    - Multi-worldbook composition produces source-tagged groups with priority labels
    - Supplementary sources capped at 15 entries
    - Single-worldbook and no-premise paths produce identical output to before
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Priority-aware prompt rendering in buildIpContextBlock</name>
  <files>
    backend/src/worldgen/scaffold-steps/prompt-utils.ts
  </files>
  <action>
1. **Update `buildIpContextBlock()`** in `prompt-utils.ts`: When `ipContext.sourceGroups` is present and has more than one group, render a source-grouped format instead of the flat format:

   ```
   FRANCHISE REFERENCE ({franchise}, verified via {source}):

   {canonicalNames block — merged, same as current}

   PRIMARY SOURCE — {sourceName} ({N} entries):
   These facts define the core world. Treat as ground truth.
     - fact 1
     - fact 2
     ...

   SUPPLEMENTARY REFERENCE — {sourceName} ({N} of {total} entries):
   Background flavor material. Include ONLY when fitting the premise naturally.
   Do NOT let supplementary content dominate the primary source.
     - fact 1
     - fact 2
     ...

   Tone:
     - tone notes...

   CANONICAL FIDELITY RULES (MANDATORY):
   1. The PREMISE is the primary creative guide.
   2. PRIMARY SOURCE entries define the world's foundation — use all of them.
   3. SUPPLEMENTARY entries are reference material — cherry-pick what fits, ignore what doesn't.
   4-8. (keep existing rules 1-8, renumbered)
   ```

2. When `sourceGroups` is absent or has only one group, render exactly the current flat format (zero behavioral change for single-worldbook or research-based ipContext).

3. Keep the function signature identical: `buildIpContextBlock(ipContext: IpResearchContext | null): string`. No new parameters needed — all info is in the ipContext object.

4. Ensure the priority header language is clear and directive: "Do NOT let supplementary content dominate" is the key instruction that fixes the over-influence bug.
  </action>
  <verify>
    <automated>cd R:/Projects/WorldForge && npm --prefix backend run typecheck</automated>
  </verify>
  <done>
    - buildIpContextBlock renders source-grouped format when sourceGroups present with multiple groups
    - buildIpContextBlock renders unchanged flat format for single-source or no sourceGroups
    - PRIMARY source entries listed first with "ground truth" framing
    - SUPPLEMENTARY entries listed with "background flavor" framing and dominance warning
    - Backend typechecks clean
  </done>
</task>

</tasks>

<verification>
1. `npx --prefix backend vitest run backend/src/worldgen/__tests__/worldbook-composition.test.ts` — composition tests pass
2. `npm --prefix backend run typecheck` — no type errors
3. Manual: Create a campaign with 2 worldbooks (one large, one small) and a premise favoring the small one. Verify the generated world reflects the premise's primary source, not the larger worldbook.
</verification>

<success_criteria>
- Multi-worldbook campaigns with premise priority signals produce source-grouped prompts where the primary source is framed as ground truth and supplementary sources are capped and de-emphasized
- Single-worldbook campaigns and research-based (non-worldbook) campaigns produce identical output to before
- All existing tests continue to pass
- Backend typechecks clean
</success_criteria>

<output>
After completion, create `.planning/quick/260404-e0m-fix-lorebook-over-influence-in-worldgen-/260404-e0m-SUMMARY.md`
</output>
